// Auto-generated from MetaModelica source
/*
 * This file is part of OpenModelica.
 *
 * Copyright (c) 1998-2026, Open Source Modelica Consortium (OSMC),
 * c/o Linköpings universitet, Department of Computer and Information Science,
 * SE-58183 Linköping, Sweden.
 *
 * All rights reserved.
 *
 * THIS PROGRAM IS PROVIDED UNDER THE TERMS OF AGPL VERSION 3 LICENSE OR
 * THIS OSMC PUBLIC LICENSE (OSMC-PL) VERSION 1.8.
 * ANY USE, REPRODUCTION OR DISTRIBUTION OF THIS PROGRAM CONSTITUTES
 * RECIPIENT'S ACCEPTANCE OF THE OSMC PUBLIC LICENSE OR THE GNU AGPL
 * VERSION 3, ACCORDING TO RECIPIENTS CHOICE.
 *
 * The OpenModelica software and the OSMC (Open Source Modelica Consortium)
 * Public License (OSMC-PL) are obtained from OSMC, either from the above
 * address, from the URLs:
 * http://www.openmodelica.org or
 * https://github.com/OpenModelica/ or
 * http://www.ida.liu.se/projects/OpenModelica,
 * and in the OpenModelica distribution.
 *
 * GNU AGPL version 3 is obtained from:
 * https://www.gnu.org/licenses/licenses.html#GPL
 *
 * This program is distributed WITHOUT ANY WARRANTY; without
 * even the implied warranty of MERCHANTABILITY or FITNESS
 * FOR A PARTICULAR PURPOSE, EXCEPT AS EXPRESSLY SET FORTH
 * IN THE BY RECIPIENT SELECTED SUBSIDIARY LICENSE CONDITIONS OF OSMC-PL.
 *
 * See the full OSMC Public License conditions for more details.
 *
 */
#![allow(warnings)]
#![allow(unreachable_patterns, unreachable_code, non_camel_case_types, non_snake_case, dead_code, unused_imports, unused_variables, non_upper_case_globals, unused_mut)]

use std::sync::Arc;
use anyhow::{Result, bail};
use loop_unwrap::unwrap_break_err;
use metamodelica::*; // Built-in types and functions
use const_str;
use arcstr::{ArcStr, literal, format};

use crate::BackendDAEOptimize;
use crate::BackendDAETransform;
use crate::BackendDAEUtil;
use crate::BackendDump;
use crate::BackendEquation;
use crate::BackendVarTransform;
use crate::BackendVariable;
use crate::CommonSubExpression::isCSECref;
use crate::Differentiate;
use crate::ExpressionSolve;
use crate::HashTableSimCodeEqCache;
use crate::Matching;
use crate::PriorityQueue;
use crate::ReduceDAE;
use crate::SimCodeDump;
use crate::SymbolicJacobian;
use crate::VisualXML;
use openmodelica_ast::Absyn;
use openmodelica_backend_types::BackendDAE;
use openmodelica_backend_types::ZeroCrossings;
use openmodelica_frontend::Builtin;
use openmodelica_frontend::Ceval;
use openmodelica_frontend::CheckModel;
use openmodelica_frontend::FGraph;
use openmodelica_frontend::HashSet;
use openmodelica_frontend::HashSetExp;
use openmodelica_frontend::HashTableExpToIndex;
use openmodelica_frontend::Static;
use openmodelica_frontend_base::ComponentReference;
use openmodelica_frontend_base::DAEDump;
use openmodelica_frontend_base::DAEUtil;
use openmodelica_frontend_base::Expression;
use openmodelica_frontend_base::ExpressionDump;
use openmodelica_frontend_base::ExpressionSimplify;
use openmodelica_frontend_base::Inline;
use openmodelica_frontend_base::Types;
use openmodelica_frontend_base::ValuesUtil;
use openmodelica_frontend_dump::AbsynUtil;
use openmodelica_frontend_dump::AvlTreePathFunction;
use openmodelica_frontend_dump::ComponentReferenceBasics;
use openmodelica_frontend_dump::ElementSource;
use openmodelica_frontend_dump::ExpressionBasics;
use openmodelica_frontend_dump::FCore;
use openmodelica_frontend_dump::HashTable;
use openmodelica_frontend_dump::HashTableCrIListArray;
use openmodelica_frontend_dump::HashTableCrILst;
use openmodelica_frontend_dump::SCodeUtil;
use openmodelica_frontend_dump::TypesDump;
use openmodelica_frontend_types::ClassInf;
use openmodelica_frontend_types::DAE;
use openmodelica_frontend_types::SCode;
use openmodelica_frontend_types::Values;
use openmodelica_nf_frontend::NFUnit as Unit;
use openmodelica_program_util::ProgramUtil;
use openmodelica_simcode_types::AvlTreeCRToInt;
use openmodelica_simcode_types::HashTableCrefSimVar;
use openmodelica_simcode_types::HpcOmSimCode;
use openmodelica_simcode_types::SimCode;
use openmodelica_simcode_types::SimCodeFunction;
use openmodelica_simcode_types::SimCodeVar;
use openmodelica_simcode_util::SimCodeFunctionUtil;
use openmodelica_simcode_util::SimCodeUtilShared;
use openmodelica_susan::Tpl;
use openmodelica_util::Autoconf;
use openmodelica_util::AvlSetString;
use openmodelica_util::BaseHashSet;
use openmodelica_util::BaseHashTable;
use openmodelica_util::Config;
use openmodelica_util::Debug;
use openmodelica_util::Error;
use openmodelica_util::ExecStat::execStat;
use openmodelica_util::ExpandableArray;
use openmodelica_util::FMI;
use openmodelica_util::File;
use openmodelica_util::Flags;
use openmodelica_util::FlagsUtil;
use openmodelica_util::Global;
use openmodelica_util::Graph;
use openmodelica_util::HashSetString;
use openmodelica_util::SemanticVersion;
use openmodelica_util::Settings;
use openmodelica_util::StringUtil;
use openmodelica_util::System;
use openmodelica_util::UnorderedSet;
use openmodelica_util::Util;
use openmodelica_util_datatypes_basic::Array;
use openmodelica_util_datatypes_basic::DoubleEnded;
use openmodelica_util_datatypes_basic::GCExt;
use openmodelica_util_datatypes_basic::List;
use openmodelica_util_datatypes_basic::Mutable;

// public imports
// protected imports
pub const UNDERLINE: &'static str = "========================================";

fn compareEqSystems(mut eq1: Arc<SimCode::SimEqSystem>, mut eq2: Arc<SimCode::SimEqSystem>) -> Result<bool> {
    let mut b: bool = false;
    b = simEqSystemIndex(eq1.clone())? > simEqSystemIndex(eq2.clone())?;
    Ok(b)
}

pub fn sortEqSystems(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut outEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    outEqs = List::flatten(({
        let mut __acc: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
        for mut eq in (eqs.clone()).into_iter().cloned() {
            let __x = expandEntwined(eq.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }))?;
    outEqs = List::sort(outEqs.clone(), (std::sync::Arc::new(compareEqSystems) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>))?;
    Ok(outEqs)
}

fn expandEntwined(mut eq: Arc<SimCode::SimEqSystem>) -> Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> {
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    eqs = (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ SimCode::SimEqSystem::SES_ENTWINED_ASSIGN { .. } => metamodelica::cons(eq.clone(), var_field!((*eq).single_calls, SimCode::SimEqSystem::SES_ENTWINED_ASSIGN).clone()),
        _ => list![eq.clone()],
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    eqs
}

pub fn hashEqSystem(mut eq: Arc<SimCode::SimEqSystem>) -> Result<i32> {
    let mut hash: i32 = 0;
    hash = (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ SimCode::SimEqSystem::SES_RESIDUAL { .. } => {
            ExpressionBasics::hashExp(var_field!((*eq).exp, SimCode::SimEqSystem::SES_RESIDUAL).clone())?
        },
        Deref @ SimCode::SimEqSystem::SES_FOR_RESIDUAL { .. } => {
            ExpressionBasics::hashExp(var_field!((*eq).exp, SimCode::SimEqSystem::SES_FOR_RESIDUAL).clone())?
        },
        Deref @ SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { .. } => {
            ExpressionBasics::hashExp(var_field!((*eq).exp, SimCode::SimEqSystem::SES_GENERIC_RESIDUAL).clone())?
        },
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { .. } => {
            ComponentReferenceBasics::hashComponentRef(var_field!((*eq).cref, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone())? + 7 * ExpressionBasics::hashExp(var_field!((*eq).exp, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone())?
        },
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { .. } => {
            ComponentReferenceBasics::hashComponentRef(var_field!((*eq).cref, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS).clone())? + 7 * ExpressionBasics::hashExp(var_field!((*eq).exp, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS).clone())?
        },
        Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { .. } => {
            ExpressionBasics::hashExp(var_field!((*eq).lhs, SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN).clone())? + 7 * ExpressionBasics::hashExp(var_field!((*eq).exp, SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN).clone())?
        },
        Deref @ SimCode::SimEqSystem::SES_ALGORITHM { statements: Deref @ metamodelica::List::Cons { head: stmt @ Deref @ DAE::Statement::STMT_ASSERT { .. }, tail: Deref @ metamodelica::List::Nil }, .. } => {
            ExpressionBasics::hashExp(var_field!((**stmt).cond, DAE::Statement::STMT_ASSERT).clone())? + 7 * ExpressionBasics::hashExp(var_field!((**stmt).msg, DAE::Statement::STMT_ASSERT).clone())? + 49 * ExpressionBasics::hashExp(var_field!((**stmt).level, DAE::Statement::STMT_ASSERT).clone())?
        },
        _ => {
            metamodelica::valueConstructor((&*eq.clone()))?
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(hash)
}

pub fn compareEqSystemsEquality(mut eq1: Arc<SimCode::SimEqSystem>, mut eq2: Arc<SimCode::SimEqSystem>) -> Result<bool> {
    let mut b: bool = false;
    b = (::match_deref::match_deref! { match &((eq1.clone(), eq2.clone())) {
        (Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { .. }, Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { .. }) => {
            if (0 == ComponentReferenceBasics::crefCompareGeneric(var_field!((*eq1).cref, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone(), var_field!((*eq2).cref, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone())?) {ExpressionBasics::expEqual(var_field!((*eq1).exp, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone(), var_field!((*eq2).exp, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone())?} else {false}
        },
        (Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { .. }, Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { .. }) => {
            if (ExpressionBasics::expEqual(var_field!((*eq1).lhs, SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN).clone(), var_field!((*eq2).lhs, SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN).clone())?) {ExpressionBasics::expEqual(var_field!((*eq1).exp, SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN).clone(), var_field!((*eq2).exp, SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN).clone())?} else {false}
        },
        (Deref @ SimCode::SimEqSystem::SES_ALGORITHM { statements: Deref @ metamodelica::List::Cons { head: stmt1 @ Deref @ DAE::Statement::STMT_ASSERT { .. }, tail: Deref @ metamodelica::List::Nil }, .. }, Deref @ SimCode::SimEqSystem::SES_ALGORITHM { statements: Deref @ metamodelica::List::Cons { head: stmt2 @ Deref @ DAE::Statement::STMT_ASSERT { .. }, tail: Deref @ metamodelica::List::Nil }, .. }) => {
            if (ExpressionBasics::expEqual(var_field!((**stmt1).cond, DAE::Statement::STMT_ASSERT).clone(), var_field!((**stmt2).cond, DAE::Statement::STMT_ASSERT).clone())?) {if (ExpressionBasics::expEqual(var_field!((**stmt1).msg, DAE::Statement::STMT_ASSERT).clone(), var_field!((**stmt2).msg, DAE::Statement::STMT_ASSERT).clone())?) {ExpressionBasics::expEqual(var_field!((**stmt1).level, DAE::Statement::STMT_ASSERT).clone(), var_field!((**stmt2).level, DAE::Statement::STMT_ASSERT).clone())?} else {false}} else {false}
        },
        _ => {
            false
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(b)
}

// =============================================================================
// section to create SimCode from BackendDAE
//
// =============================================================================
pub fn createSimCode(mut inBackendDAE: Arc<BackendDAE::BackendDAE>, mut inInitDAE: Arc<BackendDAE::BackendDAE>, mut inInitDAE_lambda0: Option<Arc<BackendDAE::BackendDAE>>, mut inInlineData: Option<BackendDAE::InlineData>, mut inRemovedInitialEquationLst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, mut inClassName: Arc<Absyn::Path>, mut filenamePrefix: ArcStr, mut inFileDir: ArcStr, mut functions: Arc<metamodelica::List<Arc<SimCodeFunction::Function::Function>>>, mut externalFunctionIncludes: Arc<metamodelica::List<ArcStr>>, mut includeDirs: Arc<metamodelica::List<ArcStr>>, mut libs: Arc<metamodelica::List<ArcStr>>, mut libPaths: Arc<metamodelica::List<ArcStr>>, mut program: Absyn::Program, mut simSettingsOpt: Option<SimCode::SimulationSettings>, mut recordDecls: Arc<metamodelica::List<SimCodeFunction::RecordDeclaration>>, mut literals: (i32, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::Exp>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), Arc<metamodelica::List<Arc<DAE::Exp>>>), mut args: Arc<Absyn::FunctionArgs>, mut isFMU: bool, mut FMUVersion: ArcStr, mut fmuTargetName: ArcStr, mut inFMIDer: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>>) -> Result<(SimCode::SimCode, (i32, Arc<metamodelica::List<(i32, i32)>>))> {
    let mut simCode: SimCode::SimCode = <SimCode::SimCode as ::std::default::Default>::default();
    let mut outMapping: (i32, Arc<metamodelica::List<(i32, i32)>>) = (0, metamodelica::nil());
    let mut dlow: Arc<BackendDAE::BackendDAE> = Arc::new(<BackendDAE::BackendDAE as ::std::default::Default>::default());
    let mut initDAE_lambda0: Arc<BackendDAE::BackendDAE> = Arc::new(<BackendDAE::BackendDAE as ::std::default::Default>::default());
    let mut removedEqs: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
    let mut eventInfo: BackendDAE::EventInfo = <BackendDAE::EventInfo as ::std::default::Default>::default();
    let mut shared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
    let mut symJacs: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>> = metamodelica::nil();
    let mut globalKnownVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut ifcpp: bool = false;
    let mut varToArrayIndexMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, (Arc<metamodelica::List<i32>>, metamodelica::Array<i32>))>>), i32, (HashTableCrIListArray::FuncHashCref, HashTableCrIListArray::FuncCrefEqual, HashTableCrIListArray::FuncCrefStr, HashTableCrIListArray::FuncExpStr));
    let mut varToIndexMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (HashTableCrILst::FuncHashCref, HashTableCrILst::FuncCrefEqual, HashTableCrILst::FuncCrefStr, HashTableCrILst::FuncExpStr));
    let mut maxDelayedExpIndex: i32 = 0;
    let mut uniqueEqIndex: i32 = 0;
    let mut numStateSets: i32 = 0;
    let mut sccOffset: i32 = 0;
    let mut numberofFixedParameters: i32 = 0;
    let mut reasonableSize: i32 = 0;
    let mut modelStructure: Option<SimCode::FmiModelStructure> = None;
    let mut fmiSimulationFlags: Option<SimCode::FmiSimulationFlags> = None;
    let mut backendMapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    let mut extObjInfo: SimCode::ExtObjInfo = <SimCode::ExtObjInfo as ::std::default::Default>::default();
    let mut crefToSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
    let mut makefileParams: SimCodeFunction::MakefileParams = <SimCodeFunction::MakefileParams as ::std::default::Default>::default();
    let mut modelInfo: SimCode::ModelInfo = <SimCode::ModelInfo as ::std::default::Default>::default();
    let mut crefToClockIndexHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr));
    let mut systemIndexMap: metamodelica::Array<i32> = Default::default();
    let mut clockedSysts: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let mut contSysts: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let mut timeEvents: Arc<metamodelica::List<BackendDAE::TimeEvent>> = metamodelica::nil();
    let mut zeroCrossingsSet: BackendDAE::ZeroCrossingSet = <BackendDAE::ZeroCrossingSet as ::std::default::Default>::default();
    let mut sampleZCSet: BackendDAE::ZeroCrossingSet = <BackendDAE::ZeroCrossingSet as ::std::default::Default>::default();
    let mut de_relations: DoubleEnded::MutableList<BackendDAE::ZeroCrossing> = <DoubleEnded::MutableList<BackendDAE::ZeroCrossing> as ::std::default::Default>::default();
    let mut zeroCrossings: Arc<metamodelica::List<BackendDAE::ZeroCrossing>> = metamodelica::nil();
    let mut sampleZC: Arc<metamodelica::List<BackendDAE::ZeroCrossing>> = metamodelica::nil();
    let mut relations: Arc<metamodelica::List<BackendDAE::ZeroCrossing>> = metamodelica::nil();
    let mut classAttributes: Arc<metamodelica::List<Arc<DAE::ClassAttributes>>> = metamodelica::nil();
    let mut discreteModelVars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut iterationVarsLst1: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut iterationVarsLst2: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut constraints: Arc<metamodelica::List<Arc<DAE::Constraint>>> = metamodelica::nil();
    let mut lits: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
    let mut clockedPartitions: Arc<metamodelica::List<SimCode::ClockedPartition>> = metamodelica::nil();
    let mut LinearMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut SymbolicJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut SymbolicJacsTemp: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut SymbolicJacsStateSelect: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut SymbolicJacsStateSelectInternal: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut SymbolicJacsNLS: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut SymbolicJacsFMI: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut SymbolicJacsdatarecon: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut algorithmAndEquationAsserts: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut localKnownVars: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut equationsForZeroCrossings: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut initialEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut initialEquations_lambda0: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut jacobianEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut maxValueEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut minValueEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut nominalValueEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut parameterEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut removedEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut removedInitialEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut startValueEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut stateSets: Arc<metamodelica::List<SimCode::StateSet>> = metamodelica::nil();
    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut jacobianSimvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut algebraicEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut odeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut inlineEquationsTemp: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut inlineEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut tmpSimVars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    let mut varInfo: SimCode::VarInfo = <SimCode::VarInfo as ::std::default::Default>::default();
    let mut sensitivityVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut countSenParams: i32 = 0;
    let mut equationSccMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut eqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut delayedExps: Arc<metamodelica::List<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))>> = metamodelica::nil();
    let mut spatialInfo: SimCode::SpatialDistributionInfo = <SimCode::SpatialDistributionInfo as ::std::default::Default>::default();
    let mut inlineData: BackendDAE::InlineData = <BackendDAE::InlineData as ::std::default::Default>::default();
    let mut inlineSimKnVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut emptyVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut SymEuler_help: i32 = 0;
    let mut dtSimVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut dtVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut eqCache: (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (HashTableSimCodeEqCache::FuncHashCref, HashTableSimCodeEqCache::FuncCrefEqual, HashTableSimCodeEqCache::FuncCrefStr, HashTableSimCodeEqCache::FuncExpStr));
    let mut dataReconJac: Arc<BackendDAE::Jacobian> = Arc::new(BackendDAE::Jacobian::EMPTY_JACOBIAN);
    let mut dataReconJacH: Option<Arc<BackendDAE::Jacobian>> = None;
    let mut setcVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut datareconinputvars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut setBVars: Option<BackendDAE::Variables> = None;
    let mut tmpsetcVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut tmpdatareconinputvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut tmpsetBVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut dataReconSimJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
    let mut dataReconSimJacH: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
    let mut numRelatedBoundaryConditions: i32 = 0;
    let mut fullPathPrefix: ArcStr = arcstr::literal!("");
    let mut fileNamePrefixHash: ArcStr = arcstr::literal!("");
    let mut iterationVarsStr: ArcStr = arcstr::literal!("");
    let mut omsiInitEquations: Arc<SimCode::OMSIFunction> = Arc::new(<SimCode::OMSIFunction as ::std::default::Default>::default());
    let mut omsiSimEquations: Arc<SimCode::OMSIFunction> = Arc::new(<SimCode::OMSIFunction as ::std::default::Default>::default());
    let mut omsiOptData: Option<SimCode::OMSIData> = None;
    let mut theSettings: SimCode::SimulationSettings = <SimCode::SimulationSettings as ::std::default::Default>::default();
    let debug: bool = false;
    match '__try0: {
        unwrap_break_err!(execStat((literal!("Backend phase and start with SimCode phase")).clone()), '__try0);
        dlow = unwrap_break_err!(BackendDAEUtil::markNonlinearIterationVariables(inBackendDAE.clone()), '__try0);
        if isSome(simSettingsOpt.clone()) {
            let __pa1 = ::match_deref::match_deref! { match &(simSettingsOpt.clone()) {
                Some(__pa1) => __pa1.clone(),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            theSettings = __pa1.clone();
            shared = dlow.shared.clone();
            assign_field!(shared.timeInterval = Some(Arc::new(DAE::Exp::RCONST { real: theSettings.stepSize.clone() })));
            assign_field!(dlow.shared = shared.clone());
        }
        System::tmpTickReset(0);
        uniqueEqIndex = 1;
        ifcpp = stringEqual((unwrap_break_err!(Config::simCodeTarget(), '__try0)).clone(), (literal!("Cpp")).clone());
        backendMapping = unwrap_break_err!(setUpBackendMapping(inBackendDAE.clone()), '__try0);
        if unwrap_break_err!(Flags::isSet(Flags::VISUAL_XML.clone()), '__try0) {
            dlow = unwrap_break_err!(VisualXML::visualizationInfoXML(dlow.clone(), (filenamePrefix.clone()).clone(), program.clone()), '__try0);
        }
        if unwrap_break_err!(Flags::isSet(Flags::ITERATION_VARS.clone()), '__try0) {
            unwrap_break_err!(BackendDAEOptimize::listAllIterationVariables(dlow.clone()), '__try0);
        }
        if !(unwrap_break_err!(Config::simCodeTarget(), '__try0) == literal!("omsic")) {
            (initialEquations, uniqueEqIndex, tempvars) = unwrap_break_err!(createInitialEquations(inInitDAE.clone(), uniqueEqIndex.clone(), metamodelica::nil()), '__try0);
            if isSome(inInitDAE_lambda0.clone()) {
                let __pa2 = ::match_deref::match_deref! { match &(inInitDAE_lambda0.clone()) {
                    Some(__pa2) => __pa2.clone(),
                    _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
                } };
                initDAE_lambda0 = __pa2.clone();
                (initialEquations_lambda0, uniqueEqIndex, tempvars) = unwrap_break_err!(createInitialEquations_lambda0(initDAE_lambda0.clone(), uniqueEqIndex.clone(), tempvars.clone()), '__try0);
            } else {
                initialEquations_lambda0 = metamodelica::nil();
            }
            let (__pa3, (__pa4, _), __pa5) = unwrap_break_err!(createNonlinearResidualEquations(inRemovedInitialEquationLst.clone(), (uniqueEqIndex.clone(), 0), tempvars.clone(), dlow.shared.functionTree.clone()), '__try0);
            removedInitialEquations = __pa3.clone();
            uniqueEqIndex = __pa4.clone();
            tempvars = __pa5.clone();
            removedInitialEquations = fixNonlinearResidualIndices(removedInitialEquations.clone());
            unwrap_break_err!(execStat((literal!("simCode: created initialization part")).clone()), '__try0);
        } else {
            initialEquations_lambda0 = metamodelica::nil();
            initialEquations = metamodelica::nil();
            removedInitialEquations = metamodelica::nil();
            tempvars = metamodelica::nil();
            (omsiInitEquations, uniqueEqIndex) = unwrap_break_err!(createAllEquationOMSI(inInitDAE.eqs.clone(), dlow.shared.clone(), metamodelica::nil(), uniqueEqIndex.clone()), '__try0);
        }
        let (__pa11, __pa6, __pa7, __pa8, __pa9, __pa10) = ::match_deref::match_deref! { match &(dlow.shared.clone()) {
            __pa11 @ Deref @ BackendDAE::Shared { eventInfo: __pa6, symjacs: __pa7, classAttrs: __pa8, constraints: __pa9, globalKnownVars: __pa10, .. } => (__pa11.clone(), __pa6.clone(), __pa7.clone(), __pa8.clone(), __pa9.clone(), __pa10.clone()),
            _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
        } };
        eventInfo = __pa6.clone();
        symJacs = __pa7.clone();
        classAttributes = __pa8.clone();
        constraints = __pa9.clone();
        globalKnownVars = __pa10.clone();
        shared = __pa11.clone();
        removedEqs = unwrap_break_err!(BackendDAEUtil::collapseRemovedEqs(dlow.clone()), '__try0);
        timeEvents = eventInfo.timeEvents.clone();
        (zeroCrossings, relations, sampleZC) = (match eventInfo.clone() {
        BackendDAE::EventInfo { samples: mut sampleZCSet, relations: mut de_relations, zeroCrossings: mut zeroCrossingsSet, .. } => (ZeroCrossings::toList(zeroCrossingsSet.clone()), DoubleEnded::toListNoCopyNoClear(de_relations.clone()), ZeroCrossings::toList(sampleZCSet.clone())),
    });
        if ifcpp.clone() {
            zeroCrossings = listAppend(relations.clone(), sampleZC.clone());
        }
        (clockedSysts, contSysts) = unwrap_break_err!(List::splitOnTrue(dlow.eqs.clone(), (std::sync::Arc::new(fnptr!(BackendDAEUtil::isClockedSyst, Arc<BackendDAE::EqSystem>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>) -> Result<bool> + 'static>)), '__try0);
        unwrap_break_err!(execStat((literal!("simCode: created event and clocks part")).clone()), '__try0);
        if unwrap_break_err!(Flags::getConfigEnum(Flags::SYM_SOLVER.clone()), '__try0) > 0 {
            SymEuler_help = unwrap_break_err!(Flags::getConfigEnum(Flags::SYM_SOLVER.clone()), '__try0);
            unwrap_break_err!(FlagsUtil::setConfigEnum(Flags::SYM_SOLVER.clone(), 0), '__try0);
        }
        if !(unwrap_break_err!(Config::simCodeTarget(), '__try0) == literal!("omsic")) {
            (uniqueEqIndex, odeEquations, algebraicEquations, localKnownVars, allEquations, equationsForZeroCrossings, tempvars, equationSccMapping, eqBackendSimCodeMapping, backendMapping, sccOffset) = unwrap_break_err!(createEquationsForSystems(contSysts.clone(), shared.clone(), uniqueEqIndex.clone(), zeroCrossings.clone(), tempvars.clone(), 1, backendMapping.clone(), true), '__try0);
            omsiOptData = None;
            if debug.clone() {
                unwrap_break_err!(execStat((literal!("simCode: createEquationsForSystems")).clone()), '__try0);
            }
        } else {
            odeEquations = metamodelica::nil();
            algebraicEquations = metamodelica::nil();
            localKnownVars = metamodelica::nil();
            allEquations = metamodelica::nil();
            equationsForZeroCrossings = metamodelica::nil();
            equationSccMapping = metamodelica::nil();
            eqBackendSimCodeMapping = metamodelica::nil();
            sccOffset = 0;
            (omsiSimEquations, uniqueEqIndex) = unwrap_break_err!(createAllEquationOMSI(contSysts.clone(), shared.clone(), zeroCrossings.clone(), uniqueEqIndex.clone()), '__try0);
            (uniqueEqIndex, removedEquations) = unwrap_break_err!(BackendEquation::traverseEquationArray(removedEqs.clone(), (std::sync::Arc::new(traversedlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (uniqueEqIndex.clone(), metamodelica::nil())), '__try0);
            assign_field!(omsiSimEquations.equations = listAppend(omsiSimEquations.equations.clone(), removedEquations.clone()));
            omsiOptData = Some(SimCode::OMSIData { initialization: omsiInitEquations.clone(), simulation: omsiSimEquations.clone() });
            if debug.clone() {
                let () = (match omsiOptData.clone() {
        Some(mut omsiData @ SimCode::OMSIData { initialization: _, .. }) => {
            dumpOMSIData(omsiData.clone(), (literal!("Dump OMSI Data")).clone());
            ()
        },
        _ => bail!("match: no arm matched"),
    });
            }
        }
        if SymEuler_help.clone() > 0 {
            unwrap_break_err!(FlagsUtil::setConfigEnum(Flags::SYM_SOLVER.clone(), SymEuler_help.clone()), '__try0);
        }
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createEquationsForSystems for inline module")).clone()), '__try0);
        }
        outMapping = (uniqueEqIndex.clone(), equationSccMapping.clone());
        unwrap_break_err!(execStat((literal!("simCode: created simulation system equations")).clone()), '__try0);
        (uniqueEqIndex, removedEquations) = unwrap_break_err!(BackendEquation::traverseEquationArray(removedEqs.clone(), (std::sync::Arc::new(traversedlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (uniqueEqIndex.clone(), metamodelica::nil())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: traversedlowEqToSimEqSystem")).clone()), '__try0);
        }
        (clockedPartitions, uniqueEqIndex, backendMapping, equationSccMapping, eqBackendSimCodeMapping, tempvars) = unwrap_break_err!(translateClockedEquations(clockedSysts.clone(), dlow.shared.clone(), sccOffset.clone(), uniqueEqIndex.clone(), backendMapping.clone(), equationSccMapping.clone(), eqBackendSimCodeMapping.clone(), tempvars.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: translateClockedEquations")).clone()), '__try0);
        }
        (uniqueEqIndex, startValueEquations, _) = unwrap_break_err!(BackendDAEUtil::foldEqSystem(dlow.clone(), (std::sync::Arc::new(createStartValueEquations) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, BackendDAE::Variables)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, BackendDAE::Variables)> + 'static>), (uniqueEqIndex.clone(), metamodelica::nil(), globalKnownVars.clone())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createStartValueEquations")).clone()), '__try0);
        }
        (uniqueEqIndex, nominalValueEquations) = unwrap_break_err!(createValueEquationsShared(dlow.shared.clone(), (std::sync::Arc::new(createInitialAssignmentsFromNominal) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> + 'static>), (uniqueEqIndex.clone(), nominalValueEquations.clone())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createNominalValueEquationsShared")).clone()), '__try0);
        }
        (uniqueEqIndex, nominalValueEquations) = unwrap_break_err!(BackendDAEUtil::foldEqSystem(dlow.clone(), (std::sync::Arc::new(createNominalValueEquations) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> + 'static>), (uniqueEqIndex.clone(), nominalValueEquations.clone())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createNominalValueEquations")).clone()), '__try0);
        }
        (uniqueEqIndex, minValueEquations) = unwrap_break_err!(createValueEquationsShared(dlow.shared.clone(), (std::sync::Arc::new(createInitialAssignmentsFromMin) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> + 'static>), (uniqueEqIndex.clone(), minValueEquations.clone())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createMinValueEquationsShared")).clone()), '__try0);
        }
        (uniqueEqIndex, minValueEquations) = unwrap_break_err!(BackendDAEUtil::foldEqSystem(dlow.clone(), (std::sync::Arc::new(createMinValueEquations) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> + 'static>), (uniqueEqIndex.clone(), minValueEquations.clone())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createMinValueEquations")).clone()), '__try0);
        }
        (uniqueEqIndex, maxValueEquations) = unwrap_break_err!(createValueEquationsShared(dlow.shared.clone(), (std::sync::Arc::new(createInitialAssignmentsFromMax) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> + 'static>), (uniqueEqIndex.clone(), maxValueEquations.clone())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createMaxValueEquationsShared")).clone()), '__try0);
        }
        (uniqueEqIndex, maxValueEquations) = unwrap_break_err!(BackendDAEUtil::foldEqSystem(dlow.clone(), (std::sync::Arc::new(createMaxValueEquations) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> + 'static>), (uniqueEqIndex.clone(), maxValueEquations.clone())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createMaxValueEquations")).clone()), '__try0);
        }
        (uniqueEqIndex, parameterEquations, numberofFixedParameters) = unwrap_break_err!(createParameterEquations(uniqueEqIndex.clone(), parameterEquations.clone(), globalKnownVars.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createParameterEquations")).clone()), '__try0);
        }
        (uniqueEqIndex, algorithmAndEquationAsserts) = unwrap_break_err!(BackendDAEUtil::foldEqSystem(dlow.clone(), (std::sync::Arc::new(createAlgorithmAndEquationAsserts) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> + 'static>), (uniqueEqIndex.clone(), metamodelica::nil())), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createAlgorithmAndEquationAsserts")).clone()), '__try0);
        }
        discreteModelVars = unwrap_break_err!(BackendDAEUtil::foldEqSystem(dlow.clone(), (std::sync::Arc::new(extractDiscreteModelVars) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> + 'static>), metamodelica::nil()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: extractDiscreteModelVars")).clone()), '__try0);
        }
        makefileParams = unwrap_break_err!(SimCodeFunctionUtil::createMakefileParams(includeDirs.clone(), libs.clone(), libPaths.clone(), false, isFMU.clone()), '__try0);
        (delayedExps, maxDelayedExpIndex) = unwrap_break_err!(extractDelayedExpressions(dlow.clone()), '__try0);
        spatialInfo = unwrap_break_err!(extractSpatialDistributionInfo(dlow.clone()), '__try0);
        unwrap_break_err!(execStat((literal!("simCode: created of all other equations (e.g. parameter, nominal, assert, etc)")).clone()), '__try0);
        algebraicEquations = List::appendElt(removedEquations.clone().reverse(), algebraicEquations.clone().reverse());
        allEquations = List::append_reverse(allEquations.clone(), removedEquations.clone());
        if isSome(inInlineData.clone()) {
            let __pa12 = ::match_deref::match_deref! { match &(inInlineData.clone()) {
                Some(__pa12) => __pa12.clone(),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            inlineData = __pa12.clone();
            (uniqueEqIndex, inlineEquationsTemp, _, _, _, _, tempvars, _, _, _, _) = unwrap_break_err!(createEquationsForSystems(inlineData.inlineSystems.clone(), shared.clone(), uniqueEqIndex.clone(), metamodelica::nil(), tempvars.clone(), uniqueEqIndex.clone(), openmodelica_simcode_types::SimCode::BackendMapping::NO_MAPPING, false), '__try0);
            inlineEquations = unwrap_break_err!(List::flatten(inlineEquationsTemp.clone()), '__try0);
        } else {
            inlineEquations = metamodelica::nil();
        }
        (dlow, stateSets, uniqueEqIndex, tempvars, numStateSets) = unwrap_break_err!(createStateSets(dlow.clone(), metamodelica::nil(), uniqueEqIndex.clone(), tempvars.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createStateSets")).clone()), '__try0);
        }
        modelInfo = unwrap_break_err!(createModelInfo(inClassName.clone(), program.clone(), dlow.clone(), inInitDAE.clone(), functions.clone(), metamodelica::nil(), numStateSets.clone(), spatialInfo.maxIndex.clone(), (inFileDir.clone()).clone(), (clockedSysts.clone().len() as i32), tempvars.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createModelInfo and variables")).clone()), '__try0);
        }
        if boolAnd(ifcpp.clone(), unwrap_break_err!(Flags::getConfigBool(Flags::LABELED_REDUCTION.clone()), '__try0)) {
            unwrap_break_err!(FlagsUtil::setConfigBool(Flags::GENERATE_LABELED_SIMCODE.clone(), true), '__try0);
        }
        if ifcpp.clone() {
            if unwrap_break_err!(Flags::getConfigBool(Flags::GENERATE_LABELED_SIMCODE.clone()), '__try0) {
                (allEquations, modelInfo) = unwrap_break_err!(ReduceDAE::buildLabels(allEquations.clone(), modelInfo.clone(), metamodelica::nil(), args.clone()), '__try0);
                if debug.clone() {
                    unwrap_break_err!(execStat((literal!("ReduceDAE: buildLabels")).clone()), '__try0);
                }
            }
        }
        tmpSimVars = modelInfo.vars.clone();
        if ifcpp.clone() {
            if unwrap_break_err!(Flags::getConfigBool(Flags::REDUCE_TERMS.clone()), '__try0) {
                (allEquations, modelInfo) = unwrap_break_err!(ReduceDAE::reduceTerms(allEquations.clone(), modelInfo.clone(), args.clone()), '__try0);
                unwrap_break_err!(FlagsUtil::setConfigBool(Flags::REDUCE_TERMS.clone(), false), '__try0);
                unwrap_break_err!(FlagsUtil::disableDebug(Flags::REDUCE_DAE.clone()), '__try0);
                if debug.clone() {
                    unwrap_break_err!(execStat((literal!("ReduceDAE: reduceTerms")).clone()), '__try0);
                }
            }
        }
        extObjInfo = unwrap_break_err!(createExtObjInfo(shared.clone()), '__try0);
        zeroCrossings = unwrap_break_err!(updateZeroCrossEqnIndex(zeroCrossings.clone(), eqBackendSimCodeMapping.clone(), unwrap_break_err!(BackendDAEUtil::equationArraySizeBDAE(dlow.clone()), '__try0)), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: update zero crossing index")).clone()), '__try0);
        }
        SymbolicJacsNLS = metamodelica::nil();
        (initialEquations, modelInfo, SymbolicJacsTemp) = unwrap_break_err!(addAlgebraicLoopsModelInfo(initialEquations.clone(), modelInfo.clone()), '__try0);
        SymbolicJacsNLS = listAppend(SymbolicJacsTemp.clone(), SymbolicJacsNLS.clone());
        (initialEquations_lambda0, modelInfo, SymbolicJacsTemp) = unwrap_break_err!(addAlgebraicLoopsModelInfo(initialEquations_lambda0.clone(), modelInfo.clone()), '__try0);
        SymbolicJacsNLS = listAppend(SymbolicJacsTemp.clone(), SymbolicJacsNLS.clone());
        (parameterEquations, modelInfo, SymbolicJacsTemp) = unwrap_break_err!(addAlgebraicLoopsModelInfo(parameterEquations.clone(), modelInfo.clone()), '__try0);
        SymbolicJacsNLS = listAppend(SymbolicJacsTemp.clone(), SymbolicJacsNLS.clone());
        (allEquations, modelInfo, SymbolicJacsTemp) = unwrap_break_err!(addAlgebraicLoopsModelInfo(allEquations.clone(), modelInfo.clone()), '__try0);
        SymbolicJacsNLS = listAppend(SymbolicJacsTemp.clone(), SymbolicJacsNLS.clone());
        (clockedPartitions, modelInfo, SymbolicJacsTemp) = unwrap_break_err!(addAlgebraicLoopsClockPartitions(clockedPartitions.clone(), modelInfo.clone()), '__try0);
        SymbolicJacsNLS = listAppend(SymbolicJacsTemp.clone(), SymbolicJacsNLS.clone());
        (inlineEquations, modelInfo, SymbolicJacsTemp) = unwrap_break_err!(addAlgebraicLoopsModelInfo(inlineEquations.clone(), modelInfo.clone()), '__try0);
        SymbolicJacsNLS = listAppend(SymbolicJacsTemp.clone(), SymbolicJacsNLS.clone());
        if isSome(shared.dataReconciliationData.clone()) {
            let BackendDAE::DATA_RECON { symbolicJacobian: __pa13, setcVars: __pa14, datareconinputs: __pa15, setBVars: __pa16, symbolicJacobianH: __pa17, relatedBoundaryConditions: __pa18 } = (unwrap_break_err!(Util::getOption(shared.dataReconciliationData.clone()), '__try0)) else { break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")) };
            dataReconJac = __pa13.clone();
            setcVars = __pa14.clone();
            datareconinputvars = __pa15.clone();
            setBVars = __pa16.clone();
            dataReconJacH = __pa17.clone();
            numRelatedBoundaryConditions = __pa18.clone();
            let (__pa19, __pa20, __pa21) = ::match_deref::match_deref! { match &(unwrap_break_err!(createSymbolicSimulationJacobian(dataReconJac.clone(), uniqueEqIndex.clone(), tempvars.clone(), false), '__try0)) {
                (Some(__pa19), __pa20, __pa21) => (__pa19.clone(), __pa20.clone(), __pa21.clone()),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            dataReconSimJac = __pa19.clone();
            uniqueEqIndex = __pa20.clone();
            tempvars = __pa21.clone();
            if isSome(dataReconJacH.clone()) {
                let (__pa22, __pa23, __pa24) = ::match_deref::match_deref! { match &(unwrap_break_err!(createSymbolicSimulationJacobian(unwrap_break_err!(Util::getOption(dataReconJacH.clone()), '__try0), uniqueEqIndex.clone(), tempvars.clone(), false), '__try0)) {
                    (Some(__pa22), __pa23, __pa24) => (__pa22.clone(), __pa23.clone(), __pa24.clone()),
                    _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
                } };
                dataReconSimJacH = __pa22.clone();
                uniqueEqIndex = __pa23.clone();
                tempvars = __pa24.clone();
                (SymbolicJacsdatarecon, modelInfo, SymbolicJacsTemp) = addAlgebraicLoopsModelInfoSymJacs(list![dataReconSimJac.clone(), dataReconSimJacH.clone()], modelInfo.clone());
            } else {
                (SymbolicJacsdatarecon, modelInfo, SymbolicJacsTemp) = addAlgebraicLoopsModelInfoSymJacs(list![dataReconSimJac.clone()], modelInfo.clone());
            }
            SymbolicJacsNLS = listAppend(SymbolicJacsTemp.clone(), SymbolicJacsNLS.clone());
            (_, iterationVarsLst1) = unwrap_break_err!(BackendDAEOptimize::listAllIterationVariables0(inInitDAE.eqs.clone()), '__try0);
            (_, iterationVarsLst2) = unwrap_break_err!(BackendDAEOptimize::listAllIterationVariables0(inBackendDAE.eqs.clone()), '__try0);
            iterationVarsLst1 = List::unique(listAppend(iterationVarsLst1.clone(), iterationVarsLst2.clone()));
            iterationVarsStr = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("List of iteration variables(")); __mm_s.push_str(&*intString((iterationVarsLst1.clone().len() as i32))); __mm_s.push_str(&*literal!("):")); __mm_s.push_str(&*literal!("\n=============================\n")); ArcStr::from(__mm_s) }).clone();
            if iterationVarsLst1.clone().is_empty() {
                iterationVarsStr = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*iterationVarsStr.clone()); __mm_s.push_str(&*literal!("No iteration variables found.\n")); ArcStr::from(__mm_s) }).clone();
            } else {
                for mut var in &*iterationVarsLst1.clone() {
                    let mut var = var.clone();
                    iterationVarsStr = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*iterationVarsStr.clone()); __mm_s.push_str(&*unwrap_break_err!(ComponentReference::crefStr(var.clone()), '__try0)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                }
            }
            unwrap_break_err!(System::writeFile(({ let mut __mm_s = String::new(); __mm_s.push_str(&*shared.info.fileNamePrefix.clone()); __mm_s.push_str(&*literal!("_iterationVars.txt")); ArcStr::from(__mm_s) }).clone(), (iterationVarsStr.clone()).clone()), '__try0);
        }
        (stateSets, modelInfo, SymbolicJacsStateSelect, SymbolicJacsStateSelectInternal) = unwrap_break_err!(addAlgebraicLoopsModelInfoStateSets(stateSets.clone(), modelInfo.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: collect and index LS/NLS in modelInfo")).clone()), '__try0);
        }
        if unwrap_break_err!(FMI::isFMIVersion20((FMUVersion.clone()).clone()), '__try0) {
            (SymbolicJacsFMI, modelStructure, modelInfo, SymbolicJacsTemp, uniqueEqIndex) = unwrap_break_err!(createFMIModelStructure(inFMIDer.clone(), modelInfo.clone(), uniqueEqIndex.clone(), inInitDAE.clone(), inBackendDAE.clone()), '__try0);
            SymbolicJacsNLS = listAppend(SymbolicJacsTemp.clone(), SymbolicJacsNLS.clone());
            if debug.clone() {
                unwrap_break_err!(execStat((literal!("simCode: create FMI model structure")).clone()), '__try0);
            }
        }
        if isFMU.clone() {
            fmiSimulationFlags = unwrap_break_err!(createFMISimulationFlags(true), '__try0);
        }
        (LinearMatrices, uniqueEqIndex) = unwrap_break_err!(createJacobianLinearCode(symJacs.clone(), modelInfo.clone(), uniqueEqIndex.clone(), shared.clone()), '__try0);
        (SymbolicJacs, modelInfo, SymbolicJacsTemp) = addAlgebraicLoopsModelInfoSymJacs(LinearMatrices.clone(), modelInfo.clone());
        SymbolicJacs = unwrap_break_err!(List::flatten(list![SymbolicJacsFMI.clone(), SymbolicJacs.clone(), SymbolicJacsStateSelect.clone(), SymbolicJacsdatarecon.clone()]), '__try0);
        (delayedExps, maxDelayedExpIndex, SymbolicJacsNLS) = unwrap_break_err!(extractJacobianDelayedExpressions(delayedExps.clone(), maxDelayedExpIndex.clone(), SymbolicJacsNLS.clone()), '__try0);
        (delayedExps, maxDelayedExpIndex, SymbolicJacs) = unwrap_break_err!(extractJacobianDelayedExpressions(delayedExps.clone(), maxDelayedExpIndex.clone(), SymbolicJacs.clone()), '__try0);
        (delayedExps, maxDelayedExpIndex, SymbolicJacsTemp) = unwrap_break_err!(extractJacobianDelayedExpressions(delayedExps.clone(), maxDelayedExpIndex.clone(), SymbolicJacsTemp.clone()), '__try0);
        (delayedExps, maxDelayedExpIndex, SymbolicJacsStateSelectInternal) = unwrap_break_err!(extractJacobianDelayedExpressions(delayedExps.clone(), maxDelayedExpIndex.clone(), SymbolicJacsStateSelectInternal.clone()), '__try0);
        jacobianEquations = unwrap_break_err!(collectAllJacobianEquations(SymbolicJacs.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: create Jacobian linear code")).clone()), '__try0);
        }
        SymbolicJacs = unwrap_break_err!(List::flatten(list![SymbolicJacsNLS.clone().reverse(), SymbolicJacs.clone(), SymbolicJacsTemp.clone(), SymbolicJacsStateSelectInternal.clone()]), '__try0);
        jacobianSimvars = unwrap_break_err!(collectAllJacobianVars(SymbolicJacs.clone()), '__try0);
        modelInfo = setJacobianVars(jacobianSimvars.clone(), modelInfo.clone());
        seedVars = unwrap_break_err!(collectAllSeedVars(SymbolicJacs.clone()), '__try0);
        modelInfo = setSeedVars(seedVars.clone(), modelInfo.clone());
        unwrap_break_err!(execStat((literal!("simCode: created linear, non-linear and system jacobian parts")).clone()), '__try0);
        systemIndexMap = unwrap_break_err!(List::fold(allEquations.clone(), (std::sync::Arc::new(getSystemIndexMap) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> + 'static>), arrayCreate(uniqueEqIndex.clone(), -1)), '__try0);
        odeEquations = unwrap_break_err!(List::map1List(odeEquations.clone(), (std::sync::Arc::new(setSystemIndexMap) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, metamodelica::Array<i32>) -> Result<Arc<SimCode::SimEqSystem>> + 'static>), systemIndexMap.clone()), '__try0);
        algebraicEquations = unwrap_break_err!(List::map1List(algebraicEquations.clone(), (std::sync::Arc::new(setSystemIndexMap) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, metamodelica::Array<i32>) -> Result<Arc<SimCode::SimEqSystem>> + 'static>), systemIndexMap.clone()), '__try0);
        modelInfo = addNumEqns(modelInfo.clone(), uniqueEqIndex.clone());
        odeEquations = unwrap_break_err!(makeEqualLengthLists(odeEquations.clone(), unwrap_break_err!(Config::noProc(), '__try0)), '__try0);
        algebraicEquations = unwrap_break_err!(makeEqualLengthLists(algebraicEquations.clone(), unwrap_break_err!(Config::noProc(), '__try0)), '__try0);
        odeEquations = unwrap_break_err!(List::filterOnFalse(odeEquations.clone(), std::sync::Arc::new(fnptr!(listEmpty, _))), '__try0);
        algebraicEquations = unwrap_break_err!(List::filterOnFalse(algebraicEquations.clone(), std::sync::Arc::new(fnptr!(listEmpty, _))), '__try0);
        if unwrap_break_err!(Flags::isSet(Flags::EXEC_HASH.clone()), '__try0) {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("*** SimCode -> generate cref2simVar hashtable: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        }
        crefToSimVarHT = unwrap_break_err!(createCrefToSimVarHT(modelInfo.clone()), '__try0);
        if unwrap_break_err!(Flags::isSet(Flags::EXEC_HASH.clone()), '__try0) {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("*** SimCode -> generate cref2simVar hashtable done!: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        }
        if unwrap_break_err!(Flags::getConfigEnum(Flags::SYM_SOLVER.clone()), '__try0) > 0 {
            let __pa25 = ::match_deref::match_deref! { match &(inInlineData.clone()) {
                Some(__pa25) => __pa25.clone(),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            inlineData = __pa25.clone();
            emptyVars = BackendVariable::emptyVars(BaseHashTable::bigBucketSize.clone());
            inlineSimKnVars = unwrap_break_err!(adjustStatesForInlineSolver(modelInfo.vars.stateVars.clone()), '__try0);
            let __pa26 = ::match_deref::match_deref! { match &(unwrap_break_err!(BackendVariable::getVar(ComponentReferenceBasics::makeCrefIdent((arcstr::literal!(BackendDAE::symSolverDT)).clone(), DAE::T_REAL_DEFAULT().clone(), metamodelica::nil()), inlineData.knownVariables.clone()), '__try0)) {
                (Deref @ metamodelica::List::Cons { head: __pa26, tail: Deref @ metamodelica::List::Nil }, _) => __pa26.clone(),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            dtVar = __pa26.clone();
            dtSimVar = unwrap_break_err!(dlowvarToSimvar(dtVar.clone(), None, emptyVars.clone(), None), '__try0);
            dtSimVar.index = (inlineSimKnVars.clone().len() as i32);
            inlineSimKnVars = metamodelica::cons(dtSimVar.clone(), inlineSimKnVars.clone());
            crefToSimVarHT = unwrap_break_err!(List::fold(inlineSimKnVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), crefToSimVarHT.clone()), '__try0);
        }
        if unwrap_break_err!(Flags::getConfigBool(Flags::CALCULATE_SENSITIVITIES.clone()), '__try0) {
            tmpSimVars = modelInfo.vars.clone();
            (sensitivityVars, countSenParams) = unwrap_break_err!(createSimVarsForSensitivities(tmpSimVars.stateVars.clone(), tmpSimVars.paramVars.clone(), numberofFixedParameters.clone()), '__try0);
            (sensitivityVars, _) = rewriteIndex(sensitivityVars.clone(), 0);
            tmpSimVars.sensitivityVars = sensitivityVars.clone();
            modelInfo.vars = tmpSimVars.clone();
            varInfo = modelInfo.varInfo.clone();
            varInfo.numSensitivityParameters = countSenParams.clone();
            modelInfo.varInfo = varInfo.clone();
        }
        if isSome(shared.dataReconciliationData.clone()) {
            tmpSimVars = modelInfo.vars.clone();
            (tmpsetcVars, _) = unwrap_break_err!(BackendVariable::traverseBackendDAEVars(setcVars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone())), '__try0);
            (tmpsetcVars, _) = rewriteIndex(tmpsetcVars.clone(), 0);
            tmpSimVars.dataReconSetcVars = tmpsetcVars.clone();
            modelInfo.vars = tmpSimVars.clone();
            (tmpdatareconinputvars, _) = unwrap_break_err!(BackendVariable::traverseBackendDAEVars(datareconinputvars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone())), '__try0);
            (tmpdatareconinputvars, _) = rewriteIndex(tmpdatareconinputvars.clone().reverse(), 0);
            tmpSimVars.dataReconinputVars = tmpdatareconinputvars.clone();
            modelInfo.vars = tmpSimVars.clone();
            if isSome(setBVars.clone()) {
                (tmpsetBVars, _) = unwrap_break_err!(BackendVariable::traverseBackendDAEVars(unwrap_break_err!(Util::getOption(setBVars.clone()), '__try0), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone())), '__try0);
                (tmpsetBVars, _) = rewriteIndex(tmpsetBVars.clone(), 0);
                tmpSimVars.dataReconSetBVars = tmpsetBVars.clone();
                modelInfo.vars = tmpSimVars.clone();
                varInfo = modelInfo.varInfo.clone();
                varInfo.numSetbVars = (tmpsetBVars.clone().len() as i32);
                modelInfo.varInfo = varInfo.clone();
            }
            varInfo = modelInfo.varInfo.clone();
            varInfo.numSetcVars = (tmpsetcVars.clone().len() as i32);
            varInfo.numDataReconVars = (tmpdatareconinputvars.clone().len() as i32);
            varInfo.numRelatedBoundaryConditions = numRelatedBoundaryConditions.clone();
            modelInfo.varInfo = varInfo.clone();
        }
        backendMapping = unwrap_break_err!(setBackendVarMapping(inBackendDAE.clone(), crefToSimVarHT.clone(), modelInfo.clone(), backendMapping.clone()), '__try0);
        (varToArrayIndexMapping, varToIndexMapping) = unwrap_break_err!(SimCodeUtilShared::createVarToArrayIndexMapping(modelInfo.clone()), '__try0);
        (crefToClockIndexHT, _) = unwrap_break_err!(List::fold(inBackendDAE.eqs.clone().reverse(), (std::sync::Arc::new(collectClockedVars) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, ((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), i32)) -> Result<((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), i32)> + 'static>), (HashTable::emptyHashTable(), 1)), '__try0);
        unwrap_break_err!(execStat((literal!("simCode: some other stuff during SimCode phase")).clone()), '__try0);
        if unwrap_break_err!(Config::simCodeTarget(), '__try0) != literal!("Cpp") {
            reasonableSize = Util::nextPrime(10 + ((metamodelica::OrderedFloat(1.4_f64) * (metamodelica::OrderedFloat((unwrap_break_err!(BackendDAEUtil::equationArraySizeBDAE(inBackendDAE.clone()), '__try0) + unwrap_break_err!(BackendDAEUtil::equationArraySizeBDAE(inInitDAE.clone()), '__try0) + (parameterEquations.clone().len() as i32)) as f64))).0.floor() as i32));
            eqCache = HashTableSimCodeEqCache::emptyHashTableSized(reasonableSize.clone());
            (allEquations, eqCache) = unwrap_break_err!(aliasSimEqs(allEquations.clone(), eqCache.clone()), '__try0);
            (odeEquations, eqCache) = unwrap_break_err!(aliasSimEqSystems(odeEquations.clone(), eqCache.clone()), '__try0);
            (algebraicEquations, eqCache) = unwrap_break_err!(aliasSimEqSystems(algebraicEquations.clone(), eqCache.clone()), '__try0);
            (initialEquations, eqCache) = unwrap_break_err!(aliasSimEqs(initialEquations.clone(), eqCache.clone()), '__try0);
            (initialEquations_lambda0, eqCache) = unwrap_break_err!(aliasSimEqs(initialEquations_lambda0.clone(), eqCache.clone()), '__try0);
            (removedEquations, eqCache) = unwrap_break_err!(aliasSimEqs(removedEquations.clone(), eqCache.clone()), '__try0);
            (removedInitialEquations, eqCache) = unwrap_break_err!(aliasSimEqs(removedInitialEquations.clone(), eqCache.clone()), '__try0);
            (algorithmAndEquationAsserts, eqCache) = unwrap_break_err!(aliasSimEqs(algorithmAndEquationAsserts.clone(), eqCache.clone()), '__try0);
            (jacobianEquations, eqCache) = unwrap_break_err!(aliasSimEqs(jacobianEquations.clone(), eqCache.clone()), '__try0);
            (parameterEquations, eqCache) = unwrap_break_err!(aliasSimEqs(parameterEquations.clone(), eqCache.clone()), '__try0);
            unwrap_break_err!(execStat((literal!("simCode: alias equations")).clone()), '__try0);
        }
        fileNamePrefixHash = (unwrap_break_err!(Util::hashFileNamePrefix((filenamePrefix.clone()).clone()), '__try0)).clone();
        if isFMU.clone() {
            if unwrap_break_err!(Config::simCodeTarget(), '__try0) == literal!("omsic") {
                fullPathPrefix = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*fileNamePrefixHash.clone()); __mm_s.push_str(&*literal!(".fmutmp")); ArcStr::from(__mm_s) }).clone();
            } else {
                fullPathPrefix = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*fileNamePrefixHash.clone()); __mm_s.push_str(&*literal!(".fmutmp/sources/")); ArcStr::from(__mm_s) }).clone();
            }
        } else {
            fullPathPrefix = (literal!("")).clone();
        }
        simCode = SimCode::SimCode { scalarized: true, omsiData: omsiOptData.clone(), inlineEquations: inlineEquations.clone(), daeModeData: None, partitionData: SimCode::emptyPartitionData.clone(), fmiSimulationFlags: fmiSimulationFlags.clone(), modelStructure: modelStructure.clone(), backendMapping: Some(backendMapping.clone()), crefToClockIndexHT: crefToClockIndexHT.clone(), crefToSimVarHT: crefToSimVarHT.clone(), varToIndexMapping: varToIndexMapping.clone(), varToArrayIndexMapping: varToArrayIndexMapping.clone(), valueReferences: if (isFMU.clone()) {unwrap_break_err!(getValueReferenceMapping(modelInfo.clone()), '__try0)} else {Arc::new(openmodelica_simcode_types::AvlTreeCRToInt::Tree::EMPTY)}, hpcomData: HpcOmSimCode::emptyHpcomData().clone(), fmuTargetName: (fmuTargetName.clone()).clone(), fullPathPrefix: (fullPathPrefix.clone()).clone(), fileNamePrefix: (filenamePrefix.clone()).clone(), simulationSettingsOpt: simSettingsOpt.clone(), jacobianMatrices: SymbolicJacs.clone(), spatialInfo: spatialInfo.clone(), delayedExps: SimCode::DelayedExpression { delayedExps: delayedExps.clone(), maxDelayedIndex: maxDelayedExpIndex.clone() }, makefileParams: makefileParams.clone(), extObjInfo: extObjInfo.clone(), discreteModelVars: discreteModelVars.clone(), timeEvents: timeEvents.clone(), relations: ZeroCrossings::updateIndices(relations.clone()), zeroCrossings: ZeroCrossings::updateIndices(zeroCrossings.clone()), classAttributes: classAttributes.clone(), constraints: constraints.clone(), stateSets: stateSets.clone(), jacobianEquations: jacobianEquations.clone(), equationsForZeroCrossings: equationsForZeroCrossings.clone(), algorithmAndEquationAsserts: algorithmAndEquationAsserts.clone(), removedEquations: removedEquations.clone(), parameterEquations: parameterEquations.clone(), maxValueEquations: maxValueEquations.clone(), minValueEquations: minValueEquations.clone(), nominalValueEquations: nominalValueEquations.clone(), startValueEquations: startValueEquations.clone(), removedInitialEquations: removedInitialEquations.clone(), initialEquations_lambda0: initialEquations_lambda0.clone(), initialEquations: initialEquations.clone(), clockedPartitions: clockedPartitions.clone(), algebraicEquations: algebraicEquations.clone(), odeEquations: odeEquations.clone(), allEquations: allEquations.clone(), localKnownVars: localKnownVars.clone(), generic_loop_calls: metamodelica::nil(), externalFunctionIncludes: externalFunctionIncludes.clone(), recordDecls: recordDecls.clone(), literals: metamodelica::nil(), modelInfo: modelInfo.clone() };
        let (__pa28, (_, _, __pa29)) = unwrap_break_err!(traverseExpsSimCode(simCode.clone(), (std::sync::Arc::new(SimCodeFunctionUtil::findLiteralsHelper) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (i32, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::Exp>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), Arc<metamodelica::List<Arc<DAE::Exp>>>)) -> Result<(Arc<DAE::Exp>, (i32, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::Exp>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), Arc<metamodelica::List<Arc<DAE::Exp>>>))> + 'static>), literals.clone()), '__try0);
        simCode = __pa28.clone();
        lits = __pa29.clone();
        simCode = setSimCodeLiterals(simCode.clone(), lits.clone().reverse());
        unwrap_break_err!(execStat((literal!("simCode: all other stuff during SimCode phase")).clone()), '__try0);
        if unwrap_break_err!(Flags::isSet(Flags::DUMP_SIMCODE.clone()), '__try0) {
            unwrap_break_err!(dumpSimCodeDebug(simCode.clone()), '__try0);
        }
        Ok::<_, anyhow::Error>((LinearMatrices.clone(), SymbolicJacs.clone(), SymbolicJacsNLS.clone(), SymbolicJacsStateSelect.clone(), SymbolicJacsStateSelectInternal.clone(), SymbolicJacsTemp.clone(), algebraicEquations.clone(), algorithmAndEquationAsserts.clone(), allEquations.clone(), backendMapping.clone(), classAttributes.clone(), clockedPartitions.clone(), clockedSysts.clone(), constraints.clone(), contSysts.clone(), crefToClockIndexHT.clone(), crefToSimVarHT.clone(), delayedExps.clone(), discreteModelVars.clone(), dlow.clone(), eqBackendSimCodeMapping.clone(), equationSccMapping.clone(), equationsForZeroCrossings.clone(), eventInfo.clone(), extObjInfo.clone(), fileNamePrefixHash.clone(), fullPathPrefix.clone(), globalKnownVars.clone(), ifcpp.clone(), initialEquations.clone(), initialEquations_lambda0.clone(), inlineEquations.clone(), jacobianEquations.clone(), jacobianSimvars.clone(), lits.clone(), localKnownVars.clone(), makefileParams.clone(), maxDelayedExpIndex.clone(), maxValueEquations.clone(), minValueEquations.clone(), modelInfo.clone(), nominalValueEquations.clone(), numStateSets.clone(), numberofFixedParameters.clone(), odeEquations.clone(), omsiOptData.clone(), outMapping.clone(), parameterEquations.clone(), relations.clone(), removedEqs.clone(), removedEquations.clone(), removedInitialEquations.clone(), sampleZC.clone(), sccOffset.clone(), seedVars.clone(), shared.clone(), simCode.clone(), spatialInfo.clone(), startValueEquations.clone(), stateSets.clone(), symJacs.clone(), systemIndexMap.clone(), tempvars.clone(), timeEvents.clone(), tmpSimVars.clone(), uniqueEqIndex.clone(), varToArrayIndexMapping.clone(), varToIndexMapping.clone(), zeroCrossings.clone()))
    } {
        Ok((__try0_o0, __try0_o1, __try0_o2, __try0_o3, __try0_o4, __try0_o5, __try0_o6, __try0_o7, __try0_o8, __try0_o9, __try0_o10, __try0_o11, __try0_o12, __try0_o13, __try0_o14, __try0_o15, __try0_o16, __try0_o17, __try0_o18, __try0_o19, __try0_o20, __try0_o21, __try0_o22, __try0_o23, __try0_o24, __try0_o25, __try0_o26, __try0_o27, __try0_o28, __try0_o29, __try0_o30, __try0_o31, __try0_o32, __try0_o33, __try0_o34, __try0_o35, __try0_o36, __try0_o37, __try0_o38, __try0_o39, __try0_o40, __try0_o41, __try0_o42, __try0_o43, __try0_o44, __try0_o45, __try0_o46, __try0_o47, __try0_o48, __try0_o49, __try0_o50, __try0_o51, __try0_o52, __try0_o53, __try0_o54, __try0_o55, __try0_o56, __try0_o57, __try0_o58, __try0_o59, __try0_o60, __try0_o61, __try0_o62, __try0_o63, __try0_o64, __try0_o65, __try0_o66, __try0_o67, __try0_o68)) => {
            LinearMatrices = __try0_o0;
            SymbolicJacs = __try0_o1;
            SymbolicJacsNLS = __try0_o2;
            SymbolicJacsStateSelect = __try0_o3;
            SymbolicJacsStateSelectInternal = __try0_o4;
            SymbolicJacsTemp = __try0_o5;
            algebraicEquations = __try0_o6;
            algorithmAndEquationAsserts = __try0_o7;
            allEquations = __try0_o8;
            backendMapping = __try0_o9;
            classAttributes = __try0_o10;
            clockedPartitions = __try0_o11;
            clockedSysts = __try0_o12;
            constraints = __try0_o13;
            contSysts = __try0_o14;
            crefToClockIndexHT = __try0_o15;
            crefToSimVarHT = __try0_o16;
            delayedExps = __try0_o17;
            discreteModelVars = __try0_o18;
            dlow = __try0_o19;
            eqBackendSimCodeMapping = __try0_o20;
            equationSccMapping = __try0_o21;
            equationsForZeroCrossings = __try0_o22;
            eventInfo = __try0_o23;
            extObjInfo = __try0_o24;
            fileNamePrefixHash = __try0_o25;
            fullPathPrefix = __try0_o26;
            globalKnownVars = __try0_o27;
            ifcpp = __try0_o28;
            initialEquations = __try0_o29;
            initialEquations_lambda0 = __try0_o30;
            inlineEquations = __try0_o31;
            jacobianEquations = __try0_o32;
            jacobianSimvars = __try0_o33;
            lits = __try0_o34;
            localKnownVars = __try0_o35;
            makefileParams = __try0_o36;
            maxDelayedExpIndex = __try0_o37;
            maxValueEquations = __try0_o38;
            minValueEquations = __try0_o39;
            modelInfo = __try0_o40;
            nominalValueEquations = __try0_o41;
            numStateSets = __try0_o42;
            numberofFixedParameters = __try0_o43;
            odeEquations = __try0_o44;
            omsiOptData = __try0_o45;
            outMapping = __try0_o46;
            parameterEquations = __try0_o47;
            relations = __try0_o48;
            removedEqs = __try0_o49;
            removedEquations = __try0_o50;
            removedInitialEquations = __try0_o51;
            sampleZC = __try0_o52;
            sccOffset = __try0_o53;
            seedVars = __try0_o54;
            shared = __try0_o55;
            simCode = __try0_o56;
            spatialInfo = __try0_o57;
            startValueEquations = __try0_o58;
            stateSets = __try0_o59;
            symJacs = __try0_o60;
            systemIndexMap = __try0_o61;
            tempvars = __try0_o62;
            timeEvents = __try0_o63;
            tmpSimVars = __try0_o64;
            uniqueEqIndex = __try0_o65;
            varToArrayIndexMapping = __try0_o66;
            varToIndexMapping = __try0_o67;
            zeroCrossings = __try0_o68;
        }
        Err(__try0_err) => {
            Error::addInternalError((literal!("function createSimCode failed [Transformation from optimised DAE to simulation code structure failed]")).clone(), metamodelica::sourceInfo!())?;
            return Err(__try0_err);
        }
    }
    Ok((simCode, outMapping))
}

fn getParamAsserts(mut eqIn: Arc<BackendDAE::Equation>, mut vars: BackendDAE::Variables, mut tplIn: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, Arc<metamodelica::List<Arc<BackendDAE::Equation>>>)) -> Result<(Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, Arc<metamodelica::List<Arc<BackendDAE::Equation>>>)> {
    let mut tplOut: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, Arc<metamodelica::List<Arc<BackendDAE::Equation>>>) = (metamodelica::nil(), metamodelica::nil());
    tplOut = 'mc: {
        let __mc_input = (eqIn.clone(), tplIn.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Equation::ALGORITHM { alg: Deref @ DAE::Algorithm { statementLst: stmts }, .. }, (varDep, paramDep)) => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut varLst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
                    let mut varLstLst: Arc<metamodelica::List<Arc<metamodelica::List<BackendDAE::Var>>>> = metamodelica::nil();
                    crefs = List::fold(stmts.clone(), (std::sync::Arc::new(DAEUtil::getAssertConditionCrefs) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Statement>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> + 'static>), metamodelica::nil())?;
                    (varLstLst, _) = List::map1_2(crefs.clone(), (std::sync::Arc::new(BackendVariable::getVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, BackendDAE::Variables) -> Result<(Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<i32>>)> + 'static>), vars.clone())?;
                    varLst = List::flatten(varLstLst.clone())?;
                    let true = (List::any(varLst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::isParam, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    Ok((varDep.clone(), metamodelica::cons(eqIn.clone(), paramDep.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut varDep: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut paramDep: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    (varDep, paramDep) = tplIn.clone();
                    Ok((metamodelica::cons(eqIn.clone(), varDep.clone()), paramDep.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(tplOut)
}

fn translateClockedEquations(mut inSysts: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>>, mut inShared: Arc<BackendDAE::Shared>, mut iSccOffset: i32, mut iuniqueEqIndex: i32, mut iBackendMapping: SimCode::BackendMapping, mut ieqSccMapping: Arc<metamodelica::List<(i32, i32)>>, mut ieqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>>, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<SimCode::ClockedPartition>>, i32, SimCode::BackendMapping, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut outPartitions: Arc<metamodelica::List<SimCode::ClockedPartition>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = iuniqueEqIndex.clone();
    let mut oBackendMapping: SimCode::BackendMapping = iBackendMapping.clone();
    let mut oeqSccMapping: Arc<metamodelica::List<(i32, i32)>> = ieqSccMapping.clone();
    let mut oeqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>> = ieqBackendSimCodeMapping.clone();
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = itempvars.clone();
    let mut subPartIdx: i32 = 0;
    let mut removedEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut equations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut simSubPartition: SimCode::SubPartition = <SimCode::SubPartition as ::std::default::Default>::default();
    let mut holdEvents: bool = false;
    let mut ass1: metamodelica::Array<i32> = Default::default();
    let mut stateeqnsmark: metamodelica::Array<i32> = Default::default();
    let mut zceqnsmarks: metamodelica::Array<i32> = Default::default();
    let mut funcs: Arc<AvlTreePathFunction::Tree> = Arc::new(AvlTreePathFunction::Tree::EMPTY);
    let mut comps: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>> = metamodelica::nil();
    let mut sccOffset: i32 = iSccOffset.clone();
    let mut varIxs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut simVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut clockedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut prevClockedVars: Arc<metamodelica::List<(SimCodeVar::SimVar, bool)>> = metamodelica::nil();
    let mut simSubPartitions: metamodelica::Array<Option<SimCode::SubPartition>> = Default::default();
    let mut subPartition: BackendDAE::SubPartition = <BackendDAE::SubPartition as ::std::default::Default>::default();
    let mut isPrevVar: metamodelica::Array<bool> = Default::default();
    let mut simEq: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    simSubPartitions = arrayCreate(metamodelica::arrayLength(inShared.partitionsInfo.subPartitions.clone()), None);
    funcs = BackendDAEUtil::getFunctions(inShared.clone())?;
    for mut syst in &*inSysts.clone() {
        let mut syst = syst.clone();
        let BackendDAE::CLOCKED_PARTITION { subPartIdx: __pa0 } = (syst.partitionKind.clone()) else { bail!("pattern mismatch") };
        subPartIdx = __pa0.clone();
        let (__pa1, __pa2) = ::match_deref::match_deref! { match &(syst.matching.clone()) {
            Deref @ BackendDAE::Matching::MATCHING { comps: __pa1, ass1: __pa2, .. } => (__pa1.clone(), __pa2.clone()),
            _ => bail!("pattern mismatch"),
        } };
        comps = __pa1.clone();
        ass1 = __pa2.clone();
        subPartition = inShared.partitionsInfo.subPartitions.borrow()[(subPartIdx.clone()-1) as usize].clone();
        (syst, _, _) = BackendDAEUtil::getAdjacencyMatrixfromOption(syst.clone(), openmodelica_backend_types::BackendDAE::IndexType::ABSOLUTE, Some(funcs.clone()), BackendDAEUtil::isInitializationDAE(inShared.clone()))?;
        stateeqnsmark = arrayCreate(BackendDAEUtil::equationArraySizeDAE(syst.clone()), 0);
        stateeqnsmark = BackendDAEUtil::markStateEquations(syst.clone(), stateeqnsmark.clone(), ass1.clone())?;
        zceqnsmarks = arrayCreate(BackendDAEUtil::equationArraySizeDAE(syst.clone()), 0);
        (_, _, equations, _, ouniqueEqIndex, clockedVars, oeqSccMapping, oeqBackendSimCodeMapping, oBackendMapping) = createEquationsForSystem(stateeqnsmark.clone(), zceqnsmarks.clone(), syst.clone(), inShared.clone(), comps.clone(), ouniqueEqIndex.clone(), metamodelica::nil(), sccOffset.clone(), oeqSccMapping.clone(), oeqBackendSimCodeMapping.clone(), oBackendMapping.clone(), true)?;
        sccOffset = (comps.clone().len() as i32) + sccOffset.clone();
        GCExt::free(stateeqnsmark.clone());
        GCExt::free(zceqnsmarks.clone());
        (ouniqueEqIndex, removedEquations) = BackendEquation::traverseEquationArray(syst.removedEqs.clone(), (std::sync::Arc::new(traversedlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (ouniqueEqIndex.clone(), metamodelica::nil()))?;
        prevClockedVars = metamodelica::nil();
        isPrevVar = arrayCreate(BackendVariable::varsSize(syst.orderedVars.clone()), false);
        for mut cr in &*subPartition.prevVars.clone() {
            let mut cr = cr.clone();
            (_, varIxs) = BackendVariable::getVar(cr.clone(), syst.orderedVars.clone())?;
            for mut i in &*varIxs.clone() {
                let mut i = i.clone();
                {let _arr = isPrevVar.clone(); _arr.borrow_mut()[(i.clone()-1) as usize] = true; _arr};
            }
        }
        for mut i in 1..=BackendVariable::varsSize(syst.orderedVars.clone()) {
            var = BackendVariable::getVarAt(syst.orderedVars.clone(), i.clone())?;
            simVar = dlowvarToSimvar(var.clone(), Some(inShared.aliasVars.clone()), inShared.globalKnownVars.clone(), None)?;
            prevClockedVars = metamodelica::cons((simVar.clone(), isPrevVar.borrow()[(i.clone()-1) as usize].clone()), prevClockedVars.clone());
            clockedVars = metamodelica::cons(simVar.clone(), clockedVars.clone());
            if isPrevVar.borrow()[(i.clone()-1) as usize].clone() {
                cr = simVar.name.clone();
                simVar.name = ComponentReference::crefPrefixPrevious(cr.clone());
                clockedVars = metamodelica::cons(simVar.clone(), clockedVars.clone());
                simEq = Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: ouniqueEqIndex.clone(), cref: simVar.name.clone(), exp: Arc::new(DAE::Exp::CREF { componentRef: cr.clone(), ty: simVar.type_.clone() }), source: DAE::emptyElementSource().clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
                equations = metamodelica::cons(simEq.clone(), equations.clone());
                ouniqueEqIndex = ouniqueEqIndex.clone() + 1;
            }
        }
        GCExt::free(isPrevVar.clone());
        simSubPartition = SimCode::SubPartition { vars: prevClockedVars.clone(), equations: equations.clone(), removedEquations: removedEquations.clone(), subClock: subPartition.clock.clone(), holdEvents: subPartition.holdEvents.clone() };
        assert!(isNone(simSubPartitions.borrow()[(subPartIdx.clone()-1) as usize].clone()), "{}", &*(literal!("SimCodeUtil.translateClockedEquations failed")).clone());
        {let _arr = simSubPartitions.clone(); _arr.borrow_mut()[(subPartIdx.clone()-1) as usize] = Some(simSubPartition.clone()); _arr};
    }
    outPartitions = createClockedSimPartitions(inShared.partitionsInfo.basePartitions.clone(), simSubPartitions.clone())?;
    GCExt::free(simSubPartitions.clone());
    Ok((outPartitions, ouniqueEqIndex, oBackendMapping, oeqSccMapping, oeqBackendSimCodeMapping, otempvars))
}

fn createClockedSimPartitions(mut basePartitions: metamodelica::Array<BackendDAE::BasePartition>, mut subPartitions: metamodelica::Array<Option<SimCode::SubPartition>>) -> Result<Arc<metamodelica::List<SimCode::ClockedPartition>>> {
    let mut clockedPartitions: Arc<metamodelica::List<SimCode::ClockedPartition>> = metamodelica::nil();
    let mut off: i32 = 1;
    let mut basePartition: BackendDAE::BasePartition = <BackendDAE::BasePartition as ::std::default::Default>::default();
    let mut simSubPartitions: Arc<metamodelica::List<SimCode::SubPartition>> = metamodelica::nil();
    for mut i in 1..=metamodelica::arrayLength(basePartitions.clone()) {
        basePartition = basePartitions.borrow()[(i.clone()-1) as usize].clone();
        if basePartition.nSubClocks.clone() > 0 {
            simSubPartitions = List::map(Array::getRange(off.clone(), off.clone() + basePartition.nSubClocks.clone() - 1, subPartitions.clone())?, (std::sync::Arc::new(Util::getOption) as std::sync::Arc<dyn ::std::ops::Fn(_) -> Result<_> + 'static>))?;
            simSubPartitions = simSubPartitions.clone().reverse();
        } else {
            simSubPartitions = metamodelica::nil();
        }
        off = off.clone() + basePartition.nSubClocks.clone();
        clockedPartitions = metamodelica::cons(SimCode::ClockedPartition { baseClock: basePartition.clock.clone(), subPartitions: simSubPartitions.clone() }, clockedPartitions.clone());
    }
    clockedPartitions = clockedPartitions.clone().reverse();
    Ok(clockedPartitions)
}

pub fn collectClockedVars(mut inEqSystem: Arc<BackendDAE::EqSystem>, mut inTpl: ((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), i32)) -> Result<((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), i32)> {
    let mut outTpl: ((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr)), i32);
    let mut inHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr));
    let mut outHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr));
    let mut clockIndex: i32 = 0;
    (inHT, clockIndex) = inTpl.clone();
    outTpl = (::match_deref::match_deref! { match &(inEqSystem.clone()) {
        Deref @ BackendDAE::EqSystem { partitionKind: BackendDAE::BaseClockPartitionKind::CLOCKED_PARTITION { subPartIdx: _ }, .. } => {
            (outHT, _) = BackendVariable::traverseBackendDAEVars(inEqSystem.orderedVars.clone(), (std::sync::Arc::new(collectClockedVars1) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, ((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), i32)) -> Result<(BackendDAE::Var, ((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), i32))> + 'static>), (inHT.clone(), clockIndex.clone()))?;
            (outHT.clone(), clockIndex.clone() + 1)
        },
        _ => inTpl.clone(),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(outTpl)
}

fn collectClockedVars1(mut inVar: BackendDAE::Var, mut inTpl: ((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), i32)) -> Result<(BackendDAE::Var, ((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)), i32))> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outTpl: ((metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr)), i32);
    let mut clkHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr));
    let mut clockIndex: i32 = 0;
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    (clkHT, clockIndex) = inTpl.clone();
    (outVar, outTpl) = (match inVar.clone() {
        BackendDAE::Var { varName: ref cref, .. } => {
            clkHT = BaseHashTable::add((cref.clone(), clockIndex.clone()), clkHT.clone())?;
            clkHT = BaseHashTable::add((ComponentReference::crefPrefixPrevious(cref.clone()), clockIndex.clone()), clkHT.clone())?;
            (inVar.clone(), (clkHT.clone(), clockIndex.clone()))
        },
        _ => (inVar.clone(), inTpl.clone()),
    });
    Ok((outVar, outTpl))
}

pub fn getClockIndex(mut simVar: SimCodeVar::SimVar, mut simCode: SimCode::SimCode) -> Result<Option<i32>> {
    let mut clockIndex: Option<i32> = None;
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut clkHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr));
    cref = getSimVarCompRef(simVar.clone());
    clockIndex = (match simCode.clone() {
        SimCode::SimCode { crefToClockIndexHT: mut clkHT, .. } => if (BaseHashTable::hasKey(cref.clone(), clkHT.clone())?) {Some(BaseHashTable::get(cref.clone(), clkHT.clone())?)} else {None},
    });
    Ok(clockIndex)
}

fn getSimVarCompRef(mut inVar: SimCodeVar::SimVar) -> Arc<DAE::ComponentRef> {
    let mut outComp: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    outComp = inVar.name.clone();
    outComp
}

pub fn getSubPartitions(mut inPartitions: Arc<metamodelica::List<SimCode::ClockedPartition>>) -> Result<Arc<metamodelica::List<SimCode::SubPartition>>> {
    let mut outSubPartitions: Arc<metamodelica::List<SimCode::SubPartition>> = metamodelica::nil();
    outSubPartitions = List::flatten(List::map(inPartitions.clone(), (std::sync::Arc::new(fnptr!(getSubPartition, SimCode::ClockedPartition)) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::ClockedPartition) -> Result<Arc<metamodelica::List<SimCode::SubPartition>>> + 'static>))?)?;
    Ok(outSubPartitions)
}

pub fn getSubPartition(mut inPartition: SimCode::ClockedPartition) -> Arc<metamodelica::List<SimCode::SubPartition>> {
    let mut outSubPartitions: Arc<metamodelica::List<SimCode::SubPartition>> = metamodelica::nil();
    outSubPartitions = inPartition.subPartitions.clone();
    outSubPartitions
}

pub fn getClockedEquations(mut inSubPartitions: Arc<metamodelica::List<SimCode::SubPartition>>) -> Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> {
    let mut outEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    for mut part in &*inSubPartitions.clone() {
        let mut part = part.clone();
        outEqs = listAppend(part.equations.clone(), outEqs.clone());
        outEqs = listAppend(part.removedEquations.clone(), outEqs.clone());
    }
    outEqs
}

fn addTempVars(mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>, mut tempVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<()> {
    let mut ix: i32 = 0;
    for mut e in &*tempVars.clone() {
        let mut e = e.clone();
        ix = (::match_deref::match_deref! { match &(e.type_.clone()) {
        Deref @ DAE::Type::T_INTEGER { .. } => ((SimVarsIndex::intAlg.clone()) as i32),
        Deref @ DAE::Type::T_ENUMERATION { .. } => ((SimVarsIndex::intAlg.clone()) as i32),
        Deref @ DAE::Type::T_BOOL { .. } => ((SimVarsIndex::boolAlg.clone()) as i32),
        Deref @ DAE::Type::T_STRING { .. } => ((SimVarsIndex::stringAlg.clone()) as i32),
        _ => ((SimVarsIndex::alg.clone()) as i32),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
        {let _arr = simVars.clone(); let _val = metamodelica::cons(e.clone(), simVars.borrow()[(ix.clone()-1) as usize].clone()); _arr.borrow_mut()[(ix.clone()-1) as usize] = _val; _arr};
    }
    Ok(())
}

pub fn setJacobianVars(mut iJacobianVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut iModelInfo: SimCode::ModelInfo) -> SimCode::ModelInfo {
    let mut oModelInfo: SimCode::ModelInfo = iModelInfo.clone();
    let mut vars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    if !(iJacobianVars.clone().is_empty()) {
        vars = oModelInfo.vars.clone();
        vars.jacobianVars = iJacobianVars.clone();
        oModelInfo.vars = vars.clone();
    }
    oModelInfo
}

pub fn setSeedVars(mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut modelInfo: SimCode::ModelInfo) -> SimCode::ModelInfo {
    let mut modelInfo: SimCode::ModelInfo = modelInfo;
    let mut vars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    vars = modelInfo.vars.clone();
    vars.seedVars = seedVars.clone();
    modelInfo.vars = vars.clone();
    modelInfo
}

pub fn addNumEqns(mut modelInfo: SimCode::ModelInfo, mut numEqns: i32) -> SimCode::ModelInfo {
    let mut omodelInfo: SimCode::ModelInfo = modelInfo.clone();
    let mut varInfo: SimCode::VarInfo = <SimCode::VarInfo as ::std::default::Default>::default();
    varInfo = omodelInfo.varInfo.clone();
    varInfo.numEquations = numEqns.clone();
    omodelInfo.varInfo = varInfo.clone();
    omodelInfo
}

fn getSystemIndexMap(mut inEqn: Arc<SimCode::SimEqSystem>, mut inSysIndexMap: metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> {
    let mut outSysIndexMap: metamodelica::Array<i32> = Default::default();
    outSysIndexMap = (::match_deref::match_deref! { match &(inEqn.clone()) {
        Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { indexLinearSystem: systemIndex, index, .. }, alternativeTearing: None, .. } => {
            let mut sysIndexMap: metamodelica::Array<i32> = Default::default();
            sysIndexMap = {let _arr = inSysIndexMap.clone(); _arr.borrow_mut()[(index.clone()-1) as usize] = systemIndex.clone(); _arr};
            sysIndexMap.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { indexNonLinearSystem: systemIndex, eqs, index, .. }, alternativeTearing: None, .. } => {
            let mut sysIndexMap: metamodelica::Array<i32> = Default::default();
            sysIndexMap = List::fold(eqs.clone(), (std::sync::Arc::new(getSystemIndexMap) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> + 'static>), inSysIndexMap.clone())?;
            sysIndexMap = {let _arr = sysIndexMap.clone(); _arr.borrow_mut()[(index.clone()-1) as usize] = systemIndex.clone(); _arr};
            sysIndexMap.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { indexLinearSystem: systemIndex, index, .. }, alternativeTearing: Some(Deref @ SimCode::LinearSystem { indexLinearSystem: systemIndex2, index: index2, .. }), .. } => {
            let mut sysIndexMap: metamodelica::Array<i32> = Default::default();
            {let _arr = inSysIndexMap.clone(); _arr.borrow_mut()[(index.clone()-1) as usize] = systemIndex.clone(); _arr};
            sysIndexMap = {let _arr = inSysIndexMap.clone(); _arr.borrow_mut()[(index2.clone()-1) as usize] = systemIndex2.clone(); _arr};
            sysIndexMap.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { indexNonLinearSystem: systemIndex, eqs, index, .. }, alternativeTearing: Some(Deref @ SimCode::NonlinearSystem { indexNonLinearSystem: systemIndex2, eqs: eqs2, index: index2, .. }), .. } => {
            let mut sysIndexMap: metamodelica::Array<i32> = Default::default();
            sysIndexMap = List::fold(eqs.clone(), (std::sync::Arc::new(getSystemIndexMap) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> + 'static>), inSysIndexMap.clone())?;
            sysIndexMap = {let _arr = sysIndexMap.clone(); _arr.borrow_mut()[(index.clone()-1) as usize] = systemIndex.clone(); _arr};
            sysIndexMap = List::fold(eqs2.clone(), (std::sync::Arc::new(getSystemIndexMap) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> + 'static>), inSysIndexMap.clone())?;
            sysIndexMap = {let _arr = sysIndexMap.clone(); _arr.borrow_mut()[(index2.clone()-1) as usize] = systemIndex2.clone(); _arr};
            sysIndexMap.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_MIXED { indexMixedSystem: systemIndex, index, cont, .. } => {
            let mut sysIndexMap: metamodelica::Array<i32> = Default::default();
            getSystemIndexMap(cont.clone(), inSysIndexMap.clone())?;
            sysIndexMap = {let _arr = inSysIndexMap.clone(); _arr.borrow_mut()[(index.clone()-1) as usize] = systemIndex.clone(); _arr};
            sysIndexMap.clone()
        },
        _ => {
            inSysIndexMap.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(outSysIndexMap)
}

fn setSystemIndexMap(mut inEqn: Arc<SimCode::SimEqSystem>, mut inSysIndexMap: metamodelica::Array<i32>) -> Result<Arc<SimCode::SimEqSystem>> {
    let mut outEqn: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    outEqn = (::match_deref::match_deref! { match &(inEqn.clone()) {
        simEq @ Deref @ SimCode::SimEqSystem::SES_LINEAR { alternativeTearing: alternativeTearingL, lSystem, .. } => {
            let mut sysIndex: i32 = 0;
            let mut simEq = (*simEq).clone();
            let mut alternativeTearingL = (*alternativeTearingL).clone();
            let mut lSystem = (*lSystem).clone();
            sysIndex = inSysIndexMap.borrow()[(lSystem.index.clone()-1) as usize].clone();
            assign_field!(lSystem.indexLinearSystem = sysIndex.clone());
            assign_variant_field!(simEq => SimCode::SimEqSystem::SES_LINEAR; lSystem = lSystem.clone());
            if isSome(alternativeTearingL.clone()) {
                lSystem = Util::getOption(alternativeTearingL.clone())?;
                sysIndex = inSysIndexMap.borrow()[(lSystem.index.clone()-1) as usize].clone();
                assign_field!(lSystem.indexLinearSystem = sysIndex.clone());
                alternativeTearingL = Some(lSystem.clone());
                assign_variant_field!(simEq => SimCode::SimEqSystem::SES_LINEAR; alternativeTearing = alternativeTearingL.clone());
            }
            simEq.clone()
        },
        simEq @ Deref @ SimCode::SimEqSystem::SES_NONLINEAR { alternativeTearing: alternativeTearingNL, nlSystem, .. } => {
            let mut sysIndex: i32 = 0;
            let mut simEq = (*simEq).clone();
            let mut alternativeTearingNL = (*alternativeTearingNL).clone();
            let mut nlSystem = (*nlSystem).clone();
            sysIndex = inSysIndexMap.borrow()[(nlSystem.index.clone()-1) as usize].clone();
            assign_field!(nlSystem.indexNonLinearSystem = sysIndex.clone());
            assign_variant_field!(simEq => SimCode::SimEqSystem::SES_NONLINEAR; nlSystem = nlSystem.clone());
            if isSome(alternativeTearingNL.clone()) {
                nlSystem = Util::getOption(alternativeTearingNL.clone())?;
                sysIndex = inSysIndexMap.borrow()[(nlSystem.index.clone()-1) as usize].clone();
                assign_field!(nlSystem.indexNonLinearSystem = sysIndex.clone());
                alternativeTearingNL = Some(nlSystem.clone());
                assign_variant_field!(simEq => SimCode::SimEqSystem::SES_NONLINEAR; alternativeTearing = alternativeTearingNL.clone());
            }
            simEq.clone()
        },
        simEq @ Deref @ SimCode::SimEqSystem::SES_MIXED { cont, .. } => {
            let mut sysIndex: i32 = 0;
            let mut simEq = (*simEq).clone();
            let mut cont = (*cont).clone();
            sysIndex = inSysIndexMap.borrow()[(var_field!((*simEq).index, SimCode::SimEqSystem::SES_MIXED).clone()-1) as usize].clone();
            cont = setSystemIndexMap(cont.clone(), inSysIndexMap.clone())?;
            assign_variant_field!(simEq => SimCode::SimEqSystem::SES_MIXED;
                cont = cont.clone(),
                indexMixedSystem = sysIndex.clone()
            );
            simEq.clone()
        },
        _ => {
            inEqn.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(outEqn)
}

pub fn addAlgebraicLoopsModelInfo(mut eqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut modelInfo: SimCode::ModelInfo) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, SimCode::ModelInfo, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>)> {
    let mut eqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = eqns;
    let mut modelInfo: SimCode::ModelInfo = modelInfo;
    let mut symJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut resEqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    for mut eqn in &*eqns.clone() {
        let mut eqn = eqn.clone();
        eqn = (::match_deref::match_deref! { match &(eqn.clone()) {
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { eqAttr, alternativeTearing: optNlSyst, nlSystem: nlSyst @ Deref @ SimCode::NonlinearSystem { .. } } => {
            let mut altNlSyst: Arc<SimCode::NonlinearSystem> = Arc::new(<SimCode::NonlinearSystem as ::std::default::Default>::default());
            let mut system: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut optNlSyst = (*optNlSyst).clone();
            let mut nlSyst = (*nlSyst).clone();
            (nlSyst, modelInfo, symJacs) = updateNonLinearSyst(nlSyst.clone(), modelInfo.clone(), symJacs.clone())?;
            if isSome(optNlSyst.clone()) {
                altNlSyst = Util::getOption(optNlSyst.clone())?;
                (altNlSyst, modelInfo, symJacs) = updateNonLinearSyst(altNlSyst.clone(), modelInfo.clone(), symJacs.clone())?;
                optNlSyst = Some(altNlSyst.clone());
            }
            system = Arc::new(SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: nlSyst.clone(), alternativeTearing: optNlSyst.clone(), eqAttr: eqAttr.clone() });
            modelInfo.nonLinearSystems = metamodelica::cons(system.clone(), modelInfo.nonLinearSystems.clone());
            system.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_LINEAR { eqAttr, alternativeTearing: optLinearSyst, lSystem: linearSyst @ Deref @ SimCode::LinearSystem { .. } } => {
            let mut altLinearSyst: Arc<SimCode::LinearSystem>;
            let mut system: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut optLinearSyst = (*optLinearSyst).clone();
            let mut linearSyst = (*linearSyst).clone();
            (linearSyst, modelInfo, symJacs) = updateLinearSyst(linearSyst.clone(), modelInfo.clone(), symJacs.clone())?;
            if isSome(optLinearSyst.clone()) {
                altLinearSyst = Util::getOption(optLinearSyst.clone())?;
                (altLinearSyst, modelInfo, symJacs) = updateLinearSyst(altLinearSyst.clone(), modelInfo.clone(), symJacs.clone())?;
                optLinearSyst = Some(altLinearSyst.clone());
            }
            system = Arc::new(SimCode::SimEqSystem::SES_LINEAR { lSystem: linearSyst.clone(), alternativeTearing: optLinearSyst.clone(), eqAttr: eqAttr.clone() });
            modelInfo.linearSystems = metamodelica::cons(system.clone(), modelInfo.linearSystems.clone());
            system.clone()
        },
        _ => {
            eqn.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
        resEqns = metamodelica::cons(eqn.clone(), resEqns.clone());
    }
    eqns = resEqns.clone().reverse();
    Ok((eqns, modelInfo, symJacs))
}

fn updateNonLinearSyst(mut inSyst: Arc<SimCode::NonlinearSystem>, mut modelInfo: SimCode::ModelInfo, mut allSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) -> Result<(Arc<SimCode::NonlinearSystem>, SimCode::ModelInfo, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>)> {
    let mut inSyst: Arc<SimCode::NonlinearSystem> = inSyst;
    let mut modelInfo: SimCode::ModelInfo = modelInfo;
    let mut allSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = allSymJacs;
    let mut varInfo: SimCode::VarInfo = modelInfo.varInfo.clone();
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut tmpSymJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
    let mut tmpSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let debug: bool = false;
    assign_field!(inSyst.indexNonLinearSystem = varInfo.numNonLinearSystems.clone());
    varInfo.numNonLinearSystems = varInfo.numNonLinearSystems.clone() + 1;
    modelInfo.varInfo = varInfo.clone();
    if isSome(inSyst.jacobianMatrix.clone()) {
        tmpSymJac = Util::getOption(inSyst.jacobianMatrix.clone())?;
        if !(tmpSymJac.columns.clone().is_empty()) {
            let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(addAlgebraicLoopsModelInfoSymJacs(list![tmpSymJac.clone()], modelInfo.clone())) {
                (Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Nil }, __pa1, __pa2) => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                _ => bail!("pattern mismatch"),
            } };
            tmpSymJac = __pa0.clone();
            modelInfo = __pa1.clone();
            tmpSymJacs = __pa2.clone();
            assign_field!(inSyst.jacobianMatrix = Some(tmpSymJac.clone()));
            allSymJacs = listAppend(tmpSymJacs.clone(), allSymJacs.clone());
            allSymJacs = metamodelica::cons(tmpSymJac.clone(), allSymJacs.clone());
        }
    }
    (eqs, modelInfo, tmpSymJacs) = addAlgebraicLoopsModelInfo(inSyst.eqs.clone(), modelInfo.clone())?;
    assign_field!(inSyst.eqs = eqs.clone());
    allSymJacs = listAppend(tmpSymJacs.clone(), allSymJacs.clone());
    if debug.clone() {
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Update nonlinear system ")); __mm_s.push_str(&*intString(inSyst.indexNonLinearSystem.clone())); __mm_s.push_str(&*literal!(" collected Jacobians: ")); __mm_s.push_str(&*intString((allSymJacs.clone().len() as i32))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    }
    Ok((inSyst, modelInfo, allSymJacs))
}

fn updateLinearSyst(mut inSyst: Arc<SimCode::LinearSystem>, mut modelInfo: SimCode::ModelInfo, mut allSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) -> Result<(Arc<SimCode::LinearSystem>, SimCode::ModelInfo, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>)> {
    let mut inSyst: Arc<SimCode::LinearSystem> = inSyst;
    let mut modelInfo: SimCode::ModelInfo = modelInfo;
    let mut allSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = allSymJacs;
    let mut varInfo: SimCode::VarInfo = modelInfo.varInfo.clone();
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut tmpSymJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
    let mut tmpSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let debug: bool = false;
    assign_field!(inSyst.indexLinearSystem = varInfo.numLinearSystems.clone());
    varInfo.numLinearSystems = varInfo.numLinearSystems.clone() + 1;
    modelInfo.varInfo = varInfo.clone();
    if isSome(inSyst.jacobianMatrix.clone()) {
        tmpSymJac = Util::getOption(inSyst.jacobianMatrix.clone())?;
        if !(tmpSymJac.columns.clone().is_empty()) {
            let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(addAlgebraicLoopsModelInfoSymJacs(list![tmpSymJac.clone()], modelInfo.clone())) {
                (Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Nil }, __pa1, __pa2) => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                _ => bail!("pattern mismatch"),
            } };
            tmpSymJac = __pa0.clone();
            modelInfo = __pa1.clone();
            tmpSymJacs = __pa2.clone();
            assign_field!(inSyst.jacobianMatrix = Some(tmpSymJac.clone()));
            allSymJacs = listAppend(tmpSymJacs.clone(), allSymJacs.clone());
            allSymJacs = metamodelica::cons(tmpSymJac.clone(), allSymJacs.clone());
        }
    }
    (eqs, modelInfo, tmpSymJacs) = addAlgebraicLoopsModelInfo(inSyst.residual.clone(), modelInfo.clone())?;
    assign_field!(inSyst.residual = eqs.clone());
    allSymJacs = listAppend(tmpSymJacs.clone(), allSymJacs.clone());
    if debug.clone() {
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Update linear system ")); __mm_s.push_str(&*intString(inSyst.indexLinearSystem.clone())); __mm_s.push_str(&*literal!(" collected Jacobians: ")); __mm_s.push_str(&*intString((allSymJacs.clone().len() as i32))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    }
    Ok((inSyst, modelInfo, allSymJacs))
}

pub fn addAlgebraicLoopsModelInfoSymJacs(mut symjacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>, mut modelInfo: SimCode::ModelInfo) -> (Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>, SimCode::ModelInfo, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) {
    let mut symjacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = symjacs;
    let mut modelInfo: SimCode::ModelInfo = modelInfo;
    let mut outSymJacsInSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut column: Arc<SimCode::JacobianColumn> = Arc::new(<SimCode::JacobianColumn as ::std::default::Default>::default());
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut varInfo: SimCode::VarInfo = <SimCode::VarInfo as ::std::default::Default>::default();
    let mut tmpSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut outSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let debug: bool = false;
    for mut symjac in &*symjacs.clone() {
        let mut symjac = symjac.clone();
        varInfo = modelInfo.varInfo.clone();
        assign_field!(symjac.jacobianIndex = varInfo.numJacobians.clone());
        varInfo.numJacobians = varInfo.numJacobians.clone() + 1;
        modelInfo.varInfo = varInfo.clone();
        if debug.clone() {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Collect Jacobian ")); __mm_s.push_str(&*symjac.matrixName.clone()); __mm_s.push_str(&*literal!(" and set index to: ")); __mm_s.push_str(&*intString(symjac.jacobianIndex.clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        }
        match '__try0: {
            let __pa1 = ::match_deref::match_deref! { match &(symjac.columns.clone()) {
                Deref @ metamodelica::List::Cons { head: __pa1, tail: Deref @ metamodelica::List::Nil } => __pa1.clone(),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            column = __pa1.clone();
            (eqs, modelInfo, tmpSymJacs) = unwrap_break_err!(addAlgebraicLoopsModelInfo(column.columnEqns.clone(), modelInfo.clone()), '__try0);
            tmpSymJacs = ({
        let mut __acc: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
        for mut a in (tmpSymJacs.clone()).into_iter().cloned() {
            let __x = rewriteJacPartIdx(a.clone(), modelInfo.nSubClocks.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
            outSymJacsInSymJacs = listAppend(tmpSymJacs.clone(), outSymJacsInSymJacs.clone());
            assign_field!(column.columnEqns = eqs.clone());
            assign_field!(symjac.columns = list![column.clone()]);
            outSymJacs = metamodelica::cons(symjac.clone(), outSymJacs.clone());
            Ok::<_, anyhow::Error>((column.clone(), eqs.clone(), modelInfo.clone(), outSymJacs.clone(), outSymJacsInSymJacs.clone(), symjac.clone(), tmpSymJacs.clone()))
        } {
            Ok((__try0_o0, __try0_o1, __try0_o2, __try0_o3, __try0_o4, __try0_o5, __try0_o6)) => {
                column = __try0_o0;
                eqs = __try0_o1;
                modelInfo = __try0_o2;
                outSymJacs = __try0_o3;
                outSymJacsInSymJacs = __try0_o4;
                symjac = __try0_o5;
                tmpSymJacs = __try0_o6;
            }
            Err(_) => {
                outSymJacs = metamodelica::cons(symjac.clone(), outSymJacs.clone());
                panic!("try/else: outputs not set in else branch");
            }
        }
    }
    symjacs = outSymJacs.clone().reverse();
    outSymJacsInSymJacs = outSymJacsInSymJacs.clone().reverse();
    (symjacs, modelInfo, outSymJacsInSymJacs)
}

pub fn addAlgebraicLoopsModelInfoStateSets(mut inSets: Arc<metamodelica::List<SimCode::StateSet>>, mut inModelInfo: SimCode::ModelInfo) -> Result<(Arc<metamodelica::List<SimCode::StateSet>>, SimCode::ModelInfo, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>)> {
    let mut outSets: Arc<metamodelica::List<SimCode::StateSet>> = metamodelica::nil();
    let mut modelInfo: SimCode::ModelInfo = inModelInfo.clone();
    let mut outSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut outSymJacsInternal: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut symJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
    let mut tmpSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    for mut set in &*inSets.clone() {
        let mut set = set.clone();
        let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(addAlgebraicLoopsModelInfoSymJacs(list![set.jacobianMatrix.clone()], modelInfo.clone())) {
            (Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Nil }, __pa1, __pa2) => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
            _ => bail!("pattern mismatch"),
        } };
        symJac = __pa0.clone();
        modelInfo = __pa1.clone();
        tmpSymJacs = __pa2.clone();
        outSymJacsInternal = listAppend(tmpSymJacs.clone(), outSymJacsInternal.clone());
        set.jacobianMatrix = symJac.clone();
        outSymJacs = metamodelica::cons(symJac.clone(), outSymJacs.clone());
        outSets = metamodelica::cons(set.clone(), outSets.clone());
    }
    outSets = outSets.clone().reverse();
    outSymJacs = outSymJacs.clone().reverse();
    outSymJacsInternal = outSymJacsInternal.clone().reverse();
    Ok((outSets, modelInfo, outSymJacs, outSymJacsInternal))
}

fn addAlgebraicLoopsClockPartitions(mut inColockPartition: Arc<metamodelica::List<SimCode::ClockedPartition>>, mut inModelInfo: SimCode::ModelInfo) -> Result<(Arc<metamodelica::List<SimCode::ClockedPartition>>, SimCode::ModelInfo, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>)> {
    let mut outColockPartition: Arc<metamodelica::List<SimCode::ClockedPartition>> = metamodelica::nil();
    let mut modelInfo: SimCode::ModelInfo = inModelInfo.clone();
    let mut outSymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut subPartitionTmp: SimCode::SubPartition = <SimCode::SubPartition as ::std::default::Default>::default();
    let mut clockPartitionTmp: SimCode::ClockedPartition = <SimCode::ClockedPartition as ::std::default::Default>::default();
    let mut subPartitionsTmp: Arc<metamodelica::List<SimCode::SubPartition>> = metamodelica::nil();
    let mut equations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut tmpJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut clockIndex: i32 = 0;
    for mut clockPartion in &*inColockPartition.clone() {
        let mut clockPartion = clockPartion.clone();
        subPartitionsTmp = metamodelica::nil();
        for mut subPartion in &*clockPartion.subPartitions.clone() {
            let mut subPartion = subPartion.clone();
            clockIndex = clockIndex.clone() + 1;
            (equations, modelInfo, tmpJacs) = addAlgebraicLoopsModelInfo(subPartion.equations.clone(), modelInfo.clone())?;
            tmpJacs = ({
        let mut __acc: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
        for mut a in (tmpJacs.clone()).into_iter().cloned() {
            let __x = rewriteJacPartIdx(a.clone(), clockIndex.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
            outSymJacs = listAppend(tmpJacs.clone(), outSymJacs.clone());
            subPartitionTmp = SimCode::SubPartition { vars: subPartion.vars.clone(), equations: equations.clone(), removedEquations: subPartion.removedEquations.clone(), subClock: subPartion.subClock.clone(), holdEvents: subPartion.holdEvents.clone() };
            subPartitionsTmp = metamodelica::cons(subPartitionTmp.clone(), subPartitionsTmp.clone());
        }
        clockPartitionTmp = SimCode::ClockedPartition { baseClock: clockPartion.baseClock.clone(), subPartitions: subPartitionsTmp.clone().reverse() };
        outColockPartition = metamodelica::cons(clockPartitionTmp.clone(), outColockPartition.clone());
    }
    outColockPartition = outColockPartition.clone().reverse();
    Ok((outColockPartition, modelInfo, outSymJacs))
}

fn rewriteJacPartIdx(mut inJac: Arc<SimCode::JacobianMatrix>, mut index: i32) -> Arc<SimCode::JacobianMatrix> {
    let mut outJac: Arc<SimCode::JacobianMatrix> = inJac.clone();
    assign_field!(outJac.partitionIndex = index.clone());
    outJac
}

pub fn jacobianColumnsAreEmpty(mut columns: Arc<metamodelica::List<Arc<SimCode::JacobianColumn>>>) -> bool {
    let mut b: bool = true;
    for mut col in &*columns.clone() {
        let mut col = col.clone();
        if !(col.columnEqns.clone().is_empty() && col.constantEqns.clone().is_empty()) {
            b = false;
            return b.clone();
        }
    }
    b
}

// =============================================================================
// section to create SimCode.Equations from BackendDAE.Equation
//
// =============================================================================
pub type CreateEquationsForSystemsFold = (i32, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping, i32);

/*uniqueEqIndex*/
/*odeEquations*/
/*algebraicEquations*/
/*allEquations*/
/*equationsForZeroCrossings*/
/*tempvars*/
/*eqSccMapping*/
/*eqBackendSimCodeMapping*/
/*backendSimCodeMapping*/
/*sccOffset*/
pub type CreateEquationsForSystemsArg = (Arc<BackendDAE::Shared>, Arc<metamodelica::List<BackendDAE::ZeroCrossing>>, bool);

fn createEquationsForSystems(mut inSysts: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>>, mut shared: Arc<BackendDAE::Shared>, mut iuniqueEqIndex: i32, mut inAllZeroCrossings: Arc<metamodelica::List<BackendDAE::ZeroCrossing>>, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut iSccOffset: i32, mut iBackendMapping: SimCode::BackendMapping, mut createAlgebraicEquations: bool) -> Result<(i32, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping, i32)> {
    let mut ouniqueEqIndex: i32 = 0;
    let mut oodeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut oalgebraicEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut olocalKnownVars: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut oallEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut oequationsForZeroCrossings: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut oeqSccMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut oeqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut obackendMapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    let mut oSccOffset: i32 = 0;
    let mut foldArg: CreateEquationsForSystemsFold = (0, metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), SimCode::BackendMapping::NO_MAPPING, 0);
    let mut arg: CreateEquationsForSystemsArg = (Arc::new(<BackendDAE::Shared as ::std::default::Default>::default()), metamodelica::nil(), false);
    match '__try0: {
        arg = (shared.clone(), inAllZeroCrossings.clone(), createAlgebraicEquations.clone());
        foldArg = (iuniqueEqIndex.clone(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), itempvars.clone(), metamodelica::nil(), metamodelica::nil(), iBackendMapping.clone(), iSccOffset.clone());
        foldArg = unwrap_break_err!(List::fold1(inSysts.clone(), (std::sync::Arc::new(createEquationsForSystems1) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, (Arc<BackendDAE::Shared>, Arc<metamodelica::List<BackendDAE::ZeroCrossing>>, bool), (i32, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping, i32)) -> Result<(i32, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping, i32)> + 'static>), arg.clone(), foldArg.clone()), '__try0);
        (ouniqueEqIndex, oodeEquations, oalgebraicEquations, oallEquations, oequationsForZeroCrossings, otempvars, oeqSccMapping, oeqBackendSimCodeMapping, obackendMapping, oSccOffset) = foldArg.clone();
        oequationsForZeroCrossings = Dangerous::listReverseInPlace(oequationsForZeroCrossings.clone());
        (ouniqueEqIndex, olocalKnownVars) = unwrap_break_err!(BackendVariable::traverseBackendDAEVars(shared.localKnownVars.clone(), (std::sync::Arc::new(traverseKnVarsToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (ouniqueEqIndex.clone(), metamodelica::nil())), '__try0);
        Ok::<_, anyhow::Error>((arg.clone(), foldArg.clone(), oSccOffset.clone(), oalgebraicEquations.clone(), oallEquations.clone(), obackendMapping.clone(), oeqBackendSimCodeMapping.clone(), oeqSccMapping.clone(), oequationsForZeroCrossings.clone(), olocalKnownVars.clone(), oodeEquations.clone(), otempvars.clone(), ouniqueEqIndex.clone()))
    } {
        Ok((__try0_o0, __try0_o1, __try0_o2, __try0_o3, __try0_o4, __try0_o5, __try0_o6, __try0_o7, __try0_o8, __try0_o9, __try0_o10, __try0_o11, __try0_o12)) => {
            arg = __try0_o0;
            foldArg = __try0_o1;
            oSccOffset = __try0_o2;
            oalgebraicEquations = __try0_o3;
            oallEquations = __try0_o4;
            obackendMapping = __try0_o5;
            oeqBackendSimCodeMapping = __try0_o6;
            oeqSccMapping = __try0_o7;
            oequationsForZeroCrossings = __try0_o8;
            olocalKnownVars = __try0_o9;
            oodeEquations = __try0_o10;
            otempvars = __try0_o11;
            ouniqueEqIndex = __try0_o12;
        }
        Err(__try0_err) => {
            Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("SimCodeUtil.createEquationsForSystems")); __mm_s.push_str(&*literal!(" failed")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
            return Err(__try0_err);
        }
    }
    Ok((ouniqueEqIndex, oodeEquations, oalgebraicEquations, olocalKnownVars, oallEquations, oequationsForZeroCrossings, otempvars, oeqSccMapping, oeqBackendSimCodeMapping, obackendMapping, oSccOffset))
}

fn createEquationsForSystems1(mut inSyst: Arc<BackendDAE::EqSystem>, mut inArg: CreateEquationsForSystemsArg, mut inFold: CreateEquationsForSystemsFold) -> Result<CreateEquationsForSystemsFold> {
    let mut outFold: CreateEquationsForSystemsFold = (0, metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), SimCode::BackendMapping::NO_MAPPING, 0);
    outFold = (::match_deref::match_deref! { match &(inSyst.matching.clone()) {
        Deref @ BackendDAE::Matching::MATCHING { comps, ass1, .. } => {
            let mut odeEquations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut algebraicEquations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut allEquations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut syst: Arc<BackendDAE::EqSystem> = Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default());
            let mut odeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
            let mut algebraicEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
            let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut equationsForZeroCrossings: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut equationsForZeroCrossings1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut uniqueEqIndex: i32 = 0;
            let mut sccOffset: i32 = 0;
            let mut stateeqnsmark: metamodelica::Array<i32> = Default::default();
            let mut zceqnsmarks: metamodelica::Array<i32> = Default::default();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut funcs: Arc<AvlTreePathFunction::Tree> = Arc::new(AvlTreePathFunction::Tree::EMPTY);
            let mut eqSccMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
            let mut eqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
            let mut backendMapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
            let mut zeroCrossings: Arc<metamodelica::List<BackendDAE::ZeroCrossing>> = metamodelica::nil();
            let mut shared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
            let mut createAlgebraicEquations: bool = false;
            if Flags::isSet(Flags::BLT_MATRIX_DUMP.clone())? {
                BackendDump::dumpEqSystemBLTmatrixHTML(inSyst.clone())?;
            }
            (shared, zeroCrossings, createAlgebraicEquations) = inArg.clone();
            (uniqueEqIndex, odeEquations, algebraicEquations, allEquations, equationsForZeroCrossings, tempvars, eqSccMapping, eqBackendSimCodeMapping, backendMapping, sccOffset) = inFold.clone();
            funcs = BackendDAEUtil::getFunctions(shared.clone())?;
            (syst, _, _) = BackendDAEUtil::getAdjacencyMatrixfromOption(inSyst.clone(), openmodelica_backend_types::BackendDAE::IndexType::ABSOLUTE, Some(funcs.clone()), BackendDAEUtil::isInitializationDAE(shared.clone()))?;
            stateeqnsmark = arrayCreate(BackendDAEUtil::equationArraySizeDAE(syst.clone()), 0);
            zceqnsmarks = arrayCreate(BackendDAEUtil::equationArraySizeDAE(syst.clone()), 0);
            stateeqnsmark = BackendDAEUtil::markStateEquations(syst.clone(), stateeqnsmark.clone(), ass1.clone())?;
            zceqnsmarks = BackendDAEUtil::markZeroCrossingEquations(syst.clone(), zeroCrossings.clone(), zceqnsmarks.clone(), ass1.clone())?;
            (odeEquations1, algebraicEquations1, allEquations1, equationsForZeroCrossings1, uniqueEqIndex, tempvars, eqSccMapping, eqBackendSimCodeMapping, backendMapping) = createEquationsForSystem(stateeqnsmark.clone(), zceqnsmarks.clone(), syst.clone(), shared.clone(), comps.clone(), uniqueEqIndex.clone(), tempvars.clone(), sccOffset.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), createAlgebraicEquations.clone())?;
            GCExt::free(stateeqnsmark.clone());
            GCExt::free(zceqnsmarks.clone());
            odeEquations = List::consOnTrue(!(odeEquations1.clone().is_empty()), odeEquations1.clone(), odeEquations.clone());
            algebraicEquations = List::consOnTrue(!(algebraicEquations1.clone().is_empty()), algebraicEquations1.clone(), algebraicEquations.clone());
            allEquations = List::append_reverse(allEquations1.clone(), allEquations.clone());
            equationsForZeroCrossings = List::append_reverse(equationsForZeroCrossings1.clone(), equationsForZeroCrossings.clone());
            (uniqueEqIndex.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsForZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), (comps.clone().len() as i32) + sccOffset.clone())
        },
        _ => {
            inFold.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(outFold)
}

pub type CreateEquationsForSystemFold = (i32, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping, i32);

/*uniqueEqIndex*/
/*odeEquations*/
/*algebraicEquations*/
/*allEquations*/
/*equationsForZeroCrossings*/
/*tempvars*/
/*eqSccMapping*/
/*eqBackendSimCodeMapping*/
/*backendSimCodeMapping*/
/*sccOffset*/
pub type CreateEquationsForSystemArg = (metamodelica::Array<i32>, metamodelica::Array<i32>, Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, bool);

/*stateeqnsmark*/
/*zceqnsmark*/
/*syst*/
/*shared*/
fn createEquationsForSystem(mut stateeqnsmark: metamodelica::Array<i32>, mut zceqnsmark: metamodelica::Array<i32>, mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut comps: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut iSccIndex: i32, mut ieqSccMapping: Arc<metamodelica::List<(i32, i32)>>, mut ieqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>>, mut iBackendMapping: SimCode::BackendMapping, mut createAlgebraicEquations: bool) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping)> {
    let mut outOdeEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut outAlgebraicEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut outAllEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut outEquationsforZeroCrossings: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut oeqSccMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut oeqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut oBackendMapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    let mut foldArg: CreateEquationsForSystemFold = (0, metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), SimCode::BackendMapping::NO_MAPPING, 0);
    let mut arg: CreateEquationsForSystemArg = (Default::default(), Default::default(), Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default()), Arc::new(<BackendDAE::Shared as ::std::default::Default>::default()), false);
    let mut odeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut algebraicEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut allEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut equationsforZeroCrossings: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    arg = (stateeqnsmark.clone(), zceqnsmark.clone(), syst.clone(), shared.clone(), createAlgebraicEquations.clone());
    foldArg = (iuniqueEqIndex.clone(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), itempvars.clone(), ieqSccMapping.clone(), ieqBackendSimCodeMapping.clone(), iBackendMapping.clone(), iSccIndex.clone());
    foldArg = List::fold2(comps.clone(), (std::sync::Arc::new(createEquationsForSystem1) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::StrongComponent>, (metamodelica::Array<i32>, metamodelica::Array<i32>, Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, bool), Option<i32>, (i32, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping, i32)) -> Result<(i32, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping, i32)> + 'static>), arg.clone(), partitionKindToClockIndex(syst.partitionKind.clone()), foldArg.clone())?;
    (ouniqueEqIndex, odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings, otempvars, oeqSccMapping, oeqBackendSimCodeMapping, oBackendMapping, _) = foldArg.clone();
    outOdeEquations = List::flattenReverse(odeEquations.clone())?;
    outAlgebraicEquations = List::flattenReverse(algebraicEquations.clone())?;
    outAllEquations = List::flattenReverse(allEquations.clone())?;
    outEquationsforZeroCrossings = List::flattenReverse(equationsforZeroCrossings.clone())?;
    Ok((outOdeEquations, outAlgebraicEquations, outAllEquations, outEquationsforZeroCrossings, ouniqueEqIndex, otempvars, oeqSccMapping, oeqBackendSimCodeMapping, oBackendMapping))
}

fn addEquationsToLists(mut inEq: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut stateeqnsmark: metamodelica::Array<i32>, mut zceqnsmark: metamodelica::Array<i32>, mut eqsIdx: Arc<metamodelica::List<i32>>, mut inOdeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, mut inAlgebraicEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, mut inAllEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, mut inEquationsforZeroCrossings: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>) -> (Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>) {
    let mut outOdeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut outAlgebraicEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut outAllEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut outEquationsforZeroCrossings: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut bdynamic: bool = false;
    let mut bzceqns: bool = false;
    bdynamic = BackendDAEUtil::blockIsDynamic(eqsIdx.clone(), stateeqnsmark.clone());
    bzceqns = BackendDAEUtil::blockIsDynamic(eqsIdx.clone(), zceqnsmark.clone());
    outOdeEquations = if (bdynamic.clone()) {metamodelica::cons(inEq.clone(), inOdeEquations.clone())} else {inOdeEquations.clone()};
    outAlgebraicEquations = if (!(bdynamic.clone())) {metamodelica::cons(inEq.clone(), inAlgebraicEquations.clone())} else {inAlgebraicEquations.clone()};
    outAllEquations = metamodelica::cons(inEq.clone(), inAllEquations.clone());
    outEquationsforZeroCrossings = if (bzceqns.clone()) {metamodelica::cons(inEq.clone(), inEquationsforZeroCrossings.clone())} else {inEquationsforZeroCrossings.clone()};
    (outOdeEquations, outAlgebraicEquations, outAllEquations, outEquationsforZeroCrossings)
}

fn createEquationsForSystem1(mut comp: Arc<BackendDAE::StrongComponent>, mut inArg: CreateEquationsForSystemArg, mut clockIndex: Option<i32>, mut inFold: CreateEquationsForSystemFold) -> Result<CreateEquationsForSystemFold> {
    let mut outFold: CreateEquationsForSystemFold = (0, metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), SimCode::BackendMapping::NO_MAPPING, 0);
    let mut stateeqnsmark: metamodelica::Array<i32> = Default::default();
    let mut zceqnsmark: metamodelica::Array<i32> = Default::default();
    let mut syst: Arc<BackendDAE::EqSystem> = Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default());
    let mut shared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
    let mut uniqueEqIndex: i32 = 0;
    let mut sccIndex: i32 = 0;
    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut eqSccMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut eqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut backendMapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    let mut odeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut algebraicEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut allEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut equationsforZeroCrossings: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut eqsIdx: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut varIdx: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
    let mut createAlgebraicEquations: bool = false;
    let mut bdynamic: bool = false;
    let mut skip: bool = false;
    let mut debug: bool = false;
    (stateeqnsmark, zceqnsmark, syst, shared, createAlgebraicEquations) = inArg.clone();
    (uniqueEqIndex, odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings, tempvars, eqSccMapping, eqBackendSimCodeMapping, backendMapping, sccIndex) = inFold.clone();
    (varlst, varIdx, eqnlst, eqsIdx) = BackendDAEUtil::getStrongComponentVarsAndEquations(comp.clone(), syst.orderedVars.clone(), syst.orderedEqs.clone())?;
    bdynamic = BackendDAEUtil::blockIsDynamic(eqsIdx.clone(), stateeqnsmark.clone());
    skip = false;
    if debug.clone() {
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Proceed component: ")); __mm_s.push_str(&*BackendDump::strongComponentString(comp.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        BackendDump::dumpEquationList(eqnlst.clone(), (literal!("Equations:")).clone())?;
        BackendDump::dumpVarList(varlst.clone(), (literal!("Variables:")).clone())?;
    }
    skip = List::all(eqnlst.clone(), (std::sync::Arc::new(fnptr!(BackendEquation::isWhenEquation, Arc<BackendDAE::Equation>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>) -> Result<bool> + 'static>))?;
    skip = skip.clone() || List::all(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::isVarDiscrete, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<bool> + 'static>))?;
    outFold = (::match_deref::match_deref! { match &(comp.clone()) {
        _ if (!(createAlgebraicEquations.clone() || bdynamic.clone()) || skip.clone() && !(createAlgebraicEquations.clone())) => {
            (uniqueEqIndex.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone())
        },
        Deref @ BackendDAE::StrongComponent::SINGLEEQUATION { var: vindex, eqn: index } => {
            let mut firstEqIndex: i32 = 0;
            let mut uniqueEqIndex1: i32 = 0;
            let mut firstSES: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (equations1, uniqueEqIndex1, tempvars) = createEquation(index.clone(), vindex.clone(), syst.clone(), shared.clone(), false, uniqueEqIndex.clone(), tempvars.clone(), metamodelica::nil())?;
            if !(equations1.clone().is_empty()) {
                firstSES = listHead(equations1.clone())?;
                firstEqIndex = if (isSimEqSys(firstSES.clone())) {uniqueEqIndex1.clone() - 1} else {uniqueEqIndex.clone()};
                eqSccMapping = appendSccIdxRange(firstEqIndex.clone(), uniqueEqIndex1.clone() - 1, sccIndex.clone(), eqSccMapping.clone());
                eqBackendSimCodeMapping = appendSccIdxRange(firstEqIndex.clone(), uniqueEqIndex1.clone() - 1, index.clone(), eqBackendSimCodeMapping.clone());
                backendMapping = setEqMapping(List::intRange2(firstEqIndex.clone(), uniqueEqIndex1.clone() - 1), list![index.clone()], backendMapping.clone())?;
            }
            if BackendEquation::isWhenEquation(BackendEquation::get(syst.orderedEqs.clone(), index.clone())?) {
                allEquations = metamodelica::cons(equations1.clone(), allEquations.clone());
            } else {
                (odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings) = addEquationsToLists(equations1.clone(), stateeqnsmark.clone(), zceqnsmark.clone(), list![index.clone()], odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone());
            }
            (uniqueEqIndex1.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone() + 1)
        },
        Deref @ BackendDAE::StrongComponent::SINGLEARRAY { eqn: e, .. } => {
            let mut uniqueEqIndex1: i32 = 0;
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), syst.orderedEqs.clone(), syst.orderedVars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, _, uniqueEqIndex1, tempvars) = createSingleArrayEqnCode(true, eqnlst.clone(), varlst.clone(), uniqueEqIndex.clone(), tempvars.clone(), shared.clone())?;
            eqSccMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, sccIndex.clone(), eqSccMapping.clone());
            eqBackendSimCodeMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, e.clone(), eqBackendSimCodeMapping.clone());
            (odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings) = addEquationsToLists(equations1.clone(), stateeqnsmark.clone(), zceqnsmark.clone(), list![e.clone()], odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone());
            (uniqueEqIndex1.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone() + 1)
        },
        Deref @ BackendDAE::StrongComponent::SINGLEALGORITHM { eqn: e, .. } => {
            let mut uniqueEqIndex1: i32 = 0;
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), syst.orderedEqs.clone(), syst.orderedVars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, uniqueEqIndex1) = createSingleAlgorithmCode(eqnlst.clone(), varlst.clone(), false, uniqueEqIndex.clone(), clockIndex.clone())?;
            eqSccMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, sccIndex.clone(), eqSccMapping.clone());
            eqBackendSimCodeMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, e.clone(), eqBackendSimCodeMapping.clone());
            (odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings) = addEquationsToLists(equations1.clone(), stateeqnsmark.clone(), zceqnsmark.clone(), list![e.clone()], odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone());
            (uniqueEqIndex1.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone() + 1)
        },
        Deref @ BackendDAE::StrongComponent::SINGLECOMPLEXEQUATION { eqn: e, .. } => {
            let mut uniqueEqIndex1: i32 = 0;
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), syst.orderedEqs.clone(), syst.orderedVars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, uniqueEqIndex1, tempvars) = createSingleComplexEqnCode(listHead(eqnlst.clone())?, varlst.clone(), uniqueEqIndex.clone(), tempvars.clone(), shared.info.clone(), true, shared.functionTree.clone(), clockIndex.clone())?;
            eqSccMapping = appendSccIdx(uniqueEqIndex1.clone() - 1, sccIndex.clone(), eqSccMapping.clone());
            eqBackendSimCodeMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, e.clone(), eqBackendSimCodeMapping.clone());
            (odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings) = addEquationsToLists(equations1.clone(), stateeqnsmark.clone(), zceqnsmark.clone(), list![e.clone()], odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone());
            (uniqueEqIndex1.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone() + 1)
        },
        Deref @ BackendDAE::StrongComponent::SINGLEWHENEQUATION { .. } => {
            let mut index: i32 = 0;
            let mut uniqueEqIndex1: i32 = 0;
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (eqnlst, varlst, index) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), syst.orderedEqs.clone(), syst.orderedVars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, uniqueEqIndex1, tempvars) = createSingleWhenEqnCode(listHead(eqnlst.clone())?, varlst.clone(), shared.clone(), uniqueEqIndex.clone(), tempvars.clone())?;
            eqSccMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, sccIndex.clone(), eqSccMapping.clone());
            eqBackendSimCodeMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, index.clone(), eqBackendSimCodeMapping.clone());
            allEquations = metamodelica::cons(equations1.clone(), allEquations.clone());
            (uniqueEqIndex1.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone() + 1)
        },
        Deref @ BackendDAE::StrongComponent::SINGLEIFEQUATION { eqn: e, .. } => {
            let mut index: i32 = 0;
            let mut uniqueEqIndex1: i32 = 0;
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (eqnlst, varlst, index) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), syst.orderedEqs.clone(), syst.orderedVars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, uniqueEqIndex1, tempvars) = createSingleIfEqnCode(listHead(eqnlst.clone())?, varlst.clone(), shared.clone(), true, uniqueEqIndex.clone(), tempvars.clone())?;
            eqSccMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, sccIndex.clone(), eqSccMapping.clone());
            eqBackendSimCodeMapping = appendSccIdxRange(uniqueEqIndex.clone(), uniqueEqIndex1.clone() - 1, index.clone(), eqBackendSimCodeMapping.clone());
            (odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings) = addEquationsToLists(equations1.clone(), stateeqnsmark.clone(), zceqnsmark.clone(), list![e.clone()], odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone());
            (uniqueEqIndex1.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone() + 1)
        },
        Deref @ BackendDAE::StrongComponent::EQUATIONSYSTEM { vars: Deref @ metamodelica::List::Cons { head: vindex, tail: Deref @ metamodelica::List::Nil }, eqns: Deref @ metamodelica::List::Cons { head: index, tail: Deref @ metamodelica::List::Nil }, .. } => {
            let mut firstEqIndex: i32 = 0;
            let mut uniqueEqIndex1: i32 = 0;
            let mut firstSES: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (equations1, uniqueEqIndex1, tempvars) = createEquation(index.clone(), vindex.clone(), syst.clone(), shared.clone(), false, uniqueEqIndex.clone(), tempvars.clone(), metamodelica::nil())?;
            if !(equations1.clone().is_empty()) {
                firstSES = listHead(equations1.clone())?;
                firstEqIndex = if (isSimEqSys(firstSES.clone())) {uniqueEqIndex1.clone() - 1} else {uniqueEqIndex.clone()};
                eqSccMapping = appendSccIdxRange(firstEqIndex.clone(), uniqueEqIndex1.clone() - 1, sccIndex.clone(), eqSccMapping.clone());
                eqBackendSimCodeMapping = appendSccIdxRange(firstEqIndex.clone(), uniqueEqIndex1.clone() - 1, index.clone(), eqBackendSimCodeMapping.clone());
                backendMapping = setEqMapping(List::intRange2(firstEqIndex.clone(), uniqueEqIndex1.clone() - 1), list![index.clone()], backendMapping.clone())?;
            }
            if BackendEquation::isWhenEquation(BackendEquation::get(syst.orderedEqs.clone(), index.clone())?) {
                allEquations = metamodelica::cons(equations1.clone(), allEquations.clone());
            } else {
                (odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings) = addEquationsToLists(equations1.clone(), stateeqnsmark.clone(), zceqnsmark.clone(), list![index.clone()], odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone());
            }
            (uniqueEqIndex1.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone() + 1)
        },
        _ => {
            let mut uniqueEqIndex1: i32 = 0;
            let mut eqnslst: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut noDiscEquations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (eqnslst, _) = BackendDAETransform::getEquationAndSolvedVarIndxes(comp.clone())?;
            (_, noDiscEquations1, uniqueEqIndex1, tempvars, eqSccMapping, backendMapping) = createOdeSystem(true && createAlgebraicEquations.clone(), false, syst.clone(), shared.clone(), comp.clone(), uniqueEqIndex.clone(), tempvars.clone(), sccIndex.clone(), eqSccMapping.clone(), backendMapping.clone())?;
            (odeEquations, algebraicEquations, allEquations, equationsforZeroCrossings) = addEquationsToLists(noDiscEquations1.clone(), stateeqnsmark.clone(), zceqnsmark.clone(), eqnslst.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone());
            (uniqueEqIndex1.clone(), odeEquations.clone(), algebraicEquations.clone(), allEquations.clone(), equationsforZeroCrossings.clone(), tempvars.clone(), eqSccMapping.clone(), eqBackendSimCodeMapping.clone(), backendMapping.clone(), sccIndex.clone() + 1)
        },
        _ => {
            let mut message: ArcStr = arcstr::literal!("");
            message = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("function createEquationsForSystem1 failed for component ")); __mm_s.push_str(&*BackendDump::strongComponentString(comp.clone())?); ArcStr::from(__mm_s) }).clone();
            Error::addInternalError((message.clone()).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(outFold)
}

fn appendSccIdx(mut iCurrentIdx: i32, mut iSccIdx: i32, mut iSccIdc: Arc<metamodelica::List<(i32, i32)>>) -> Arc<metamodelica::List<(i32, i32)>> {
    let mut oSccIdc: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    oSccIdc = metamodelica::cons((iCurrentIdx.clone(), iSccIdx.clone()), iSccIdc.clone());
    oSccIdc
}

fn appendSccIdxRange(mut iCurrentIdxStart: i32, mut iCurrentIdxStop: i32, mut iSccIdx: i32, mut iSccIdc: Arc<metamodelica::List<(i32, i32)>>) -> Arc<metamodelica::List<(i32, i32)>> {
    let mut oSccIdc: Arc<metamodelica::List<(i32, i32)>> = iSccIdc.clone();
    for mut i in (iCurrentIdxStart.clone()..=iCurrentIdxStop.clone()).rev() {
        oSccIdc = metamodelica::cons((i.clone(), iSccIdx.clone()), iSccIdc.clone());
    }
    oSccIdc
}

fn createEquations(mut includeWhen: bool, mut skipDiscInZc: bool, mut genDiscrete: bool, mut skipDiscInAlgorithm: bool, mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut comps: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut noDiscEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = iuniqueEqIndex.clone();
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = itempvars.clone();
    let mut accEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut accNoDiscEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    for mut comp in &*comps.clone() {
        let mut comp = comp.clone();
        (equations, noDiscEquations, ouniqueEqIndex, otempvars) = createEquationsWork(includeWhen.clone(), skipDiscInZc.clone(), genDiscrete.clone(), skipDiscInAlgorithm.clone(), syst.clone(), shared.clone(), comp.clone(), ouniqueEqIndex.clone(), otempvars.clone(), metamodelica::nil())?;
        accEquations = metamodelica::cons(equations.clone(), accEquations.clone());
        accNoDiscEquations = metamodelica::cons(noDiscEquations.clone(), accNoDiscEquations.clone());
    }
    equations = List::flattenReverse(accEquations.clone())?;
    noDiscEquations = List::flattenReverse(accNoDiscEquations.clone())?;
    Ok((equations, noDiscEquations, ouniqueEqIndex, otempvars))
}

fn createEquationsWork(mut includeWhen: bool, mut skipDiscInZc: bool, mut genDiscrete: bool, mut skipDiscInAlgorithm: bool, mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut comp: Arc<BackendDAE::StrongComponent>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut cons: Arc<metamodelica::List<Arc<DAE::Constraint>>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut noDiscEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equations, noDiscEquations, ouniqueEqIndex, otempvars) = (::match_deref::match_deref! { match &((includeWhen.clone(), skipDiscInZc.clone(), genDiscrete.clone(), syst.clone(), shared.clone(), comp.clone())) {
        (false, _, _, Deref @ BackendDAE::EqSystem { orderedEqs: eqns, .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEEQUATION { eqn: index, .. }) if (BackendEquation::isWhenEquation(BackendEquation::get(eqns.clone(), index.clone())?)) => {
            (metamodelica::nil(), metamodelica::nil(), iuniqueEqIndex.clone(), itempvars.clone())
        },
        (false, _, _, Deref @ BackendDAE::EqSystem { .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEWHENEQUATION { .. }) => {
            (metamodelica::nil(), metamodelica::nil(), iuniqueEqIndex.clone(), itempvars.clone())
        },
        (_, _, false, Deref @ BackendDAE::EqSystem { orderedVars: vars, .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEEQUATION { var: index, .. }) if (BackendVariable::isVarDiscrete(BackendVariable::getVarAt(vars.clone(), index.clone())?)) => {
            (metamodelica::nil(), metamodelica::nil(), iuniqueEqIndex.clone(), itempvars.clone())
        },
        (_, _, false, Deref @ BackendDAE::EqSystem { .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEWHENEQUATION { .. }) => {
            (metamodelica::nil(), metamodelica::nil(), iuniqueEqIndex.clone(), itempvars.clone())
        },
        (_, true, _, Deref @ BackendDAE::EqSystem { orderedVars: vars, .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEEQUATION { var: vindex, eqn: index }) if (BackendVariable::isVarDiscrete(BackendVariable::getVarAt(vars.clone(), vindex.clone())?) && listMember(index.clone(), zeroCrossingsEquations(syst.clone(), shared.clone())?)) => {
            (metamodelica::nil(), metamodelica::nil(), iuniqueEqIndex.clone(), itempvars.clone())
        },
        (_, _, _, _, _, Deref @ BackendDAE::StrongComponent::SINGLEEQUATION { var: vindex, eqn: index }) => {
            let mut uniqueEqIndex: i32 = 0;
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            if (Flags::isSet(Flags::TEARING_DUMP.clone())? || Flags::isSet(Flags::TEARING_DUMPVERBOSE.clone())?) && !(cons.clone().is_empty()) {
                println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Single Equation with constraint:\n")); __mm_s.push_str(&*BackendDump::equationString(BackendEquation::get(syst.orderedEqs.clone(), index.clone())?)?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Constraints:")); __mm_s.push_str(&*ExpressionDump::constraintDTlistToString(cons.clone(), (literal!("\n")).clone())?); __mm_s.push_str(&*literal!("\n\n")); ArcStr::from(__mm_s) }).clone());
            }
            (equations1, uniqueEqIndex, tempvars) = createEquation(index.clone(), vindex.clone(), syst.clone(), shared.clone(), skipDiscInAlgorithm.clone(), iuniqueEqIndex.clone(), itempvars.clone(), cons.clone())?;
            (equations1.clone(), equations1.clone(), uniqueEqIndex.clone(), tempvars.clone())
        },
        (_, _, _, Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEARRAY { .. }) => {
            let mut uniqueEqIndex: i32 = 0;
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut noDiscEquations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), eqns.clone(), vars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, noDiscEquations1, uniqueEqIndex, tempvars) = createSingleArrayEqnCode(genDiscrete.clone(), eqnlst.clone(), varlst.clone(), iuniqueEqIndex.clone(), itempvars.clone(), shared.clone())?;
            (equations1.clone(), noDiscEquations1.clone(), uniqueEqIndex.clone(), tempvars.clone())
        },
        (_, _, _, Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEALGORITHM { .. }) => {
            let mut uniqueEqIndex: i32 = 0;
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), eqns.clone(), vars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, uniqueEqIndex) = createSingleAlgorithmCode(eqnlst.clone(), varlst.clone(), skipDiscInAlgorithm.clone(), iuniqueEqIndex.clone(), partitionKindToClockIndex(syst.partitionKind.clone()))?;
            (equations1.clone(), equations1.clone(), uniqueEqIndex.clone(), itempvars.clone())
        },
        (_, _, _, Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, Deref @ BackendDAE::Shared { info: ei, .. }, Deref @ BackendDAE::StrongComponent::SINGLECOMPLEXEQUATION { .. }) => {
            let mut uniqueEqIndex: i32 = 0;
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), eqns.clone(), vars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, uniqueEqIndex, tempvars) = createSingleComplexEqnCode(listHead(eqnlst.clone())?, varlst.clone(), iuniqueEqIndex.clone(), itempvars.clone(), ei.clone(), genDiscrete.clone(), shared.functionTree.clone(), partitionKindToClockIndex(syst.partitionKind.clone()))?;
            (equations1.clone(), equations1.clone(), uniqueEqIndex.clone(), tempvars.clone())
        },
        (_, _, _, Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEWHENEQUATION { .. }) => {
            let mut uniqueEqIndex: i32 = 0;
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), eqns.clone(), vars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, uniqueEqIndex, tempvars) = createSingleWhenEqnCode(listHead(eqnlst.clone())?, varlst.clone(), shared.clone(), iuniqueEqIndex.clone(), itempvars.clone())?;
            (equations1.clone(), equations1.clone(), uniqueEqIndex.clone(), tempvars.clone())
        },
        (_, _, _, Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, _, Deref @ BackendDAE::StrongComponent::SINGLEIFEQUATION { .. }) => {
            let mut uniqueEqIndex: i32 = 0;
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), eqns.clone(), vars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (equations1, uniqueEqIndex, tempvars) = createSingleIfEqnCode(listHead(eqnlst.clone())?, varlst.clone(), shared.clone(), genDiscrete.clone(), iuniqueEqIndex.clone(), itempvars.clone())?;
            (equations1.clone(), equations1.clone(), uniqueEqIndex.clone(), tempvars.clone())
        },
        (_, _, _, _, _, _) => {
            let mut uniqueEqIndex: i32 = 0;
            let mut equations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut noDiscEquations1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            (equations1, noDiscEquations1, uniqueEqIndex, tempvars, _, _) = createOdeSystem(genDiscrete.clone(), skipDiscInAlgorithm.clone(), syst.clone(), shared.clone(), comp.clone(), iuniqueEqIndex.clone(), itempvars.clone(), 1, metamodelica::nil(), openmodelica_simcode_types::SimCode::BackendMapping::NO_MAPPING)?;
            (equations1.clone(), noDiscEquations1.clone(), uniqueEqIndex.clone(), tempvars.clone())
        },
        _ => {
            Error::addInternalError((literal!("createEquation failed")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((equations, noDiscEquations, ouniqueEqIndex, otempvars))
}

pub fn getSimCodeDAEModeDataEqns(mut inOptDaeMode: Option<SimCode::DaeModeData>) -> Result<Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>> {
    let mut daeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut daeModeData: SimCode::DaeModeData = <SimCode::DaeModeData as ::std::default::Default>::default();
    if isSome(inOptDaeMode.clone()) {
        daeModeData = Util::getOption(inOptDaeMode.clone())?;
        daeEquations = daeModeData.daeEquations.clone();
    } else {
        daeEquations = metamodelica::nil();
    }
    Ok(daeEquations)
}

pub fn sortSimVarsAndWriteIndex(mut inSimVar: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut crefToSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outSimVar: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut sorted: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    crefs = List::map(inSimVar.clone(), (std::sync::Arc::new(fnptr!(getSimVarCompRef, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
    sorted = getSimVars2Crefs(crefs.clone(), crefToSimVarHT.clone());
    sorted = List::sort(sorted.clone(), (std::sync::Arc::new(compareVarIndexGt) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, SimCodeVar::SimVar) -> Result<bool> + 'static>))?;
    (outSimVar, _) = rewriteIndex(sorted.clone(), 0);
    Ok(outSimVar)
}

// =============================================================================
// section for zeroCrossingsEquations
//
// =============================================================================
fn zeroCrossingsEquations(mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>) -> Result<Arc<metamodelica::List<i32>>> {
    let mut eqns: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut zeroCrossingLst: BackendDAE::ZeroCrossingSet = <BackendDAE::ZeroCrossingSet as ::std::default::Default>::default();
    let mut zcEqns: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    let mut wcEqns: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut orderedEqs: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
    let __pa0 = ::match_deref::match_deref! { match &(syst.clone()) {
        Deref @ BackendDAE::EqSystem { orderedEqs: __pa0, .. } => __pa0.clone(),
        _ => bail!("pattern mismatch"),
    } };
    orderedEqs = __pa0.clone();
    let __pa1 = ::match_deref::match_deref! { match &(shared.clone()) {
        Deref @ BackendDAE::Shared { eventInfo: BackendDAE::EventInfo { zeroCrossings: __pa1, .. }, .. } => __pa1.clone(),
        _ => bail!("pattern mismatch"),
    } };
    zeroCrossingLst = __pa1.clone();
    zcEqns = List::map(ZeroCrossings::toList(zeroCrossingLst.clone()), (std::sync::Arc::new(zeroCrossingEquations) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::ZeroCrossing) -> Result<Arc<metamodelica::List<i32>>> + 'static>))?;
    wcEqns = whenEquationsIndices(orderedEqs.clone())?;
    eqns = List::unionList(listAppend(zcEqns.clone(), list![wcEqns.clone()]))?;
    Ok(eqns)
}

fn zeroCrossingEquations(mut inZC: BackendDAE::ZeroCrossing) -> Result<Arc<metamodelica::List<i32>>> {
    let mut outLst: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let BackendDAE::ZERO_CROSSING { index: _, relation_: _, occurEquLst: __pa0, iter: _ } = (inZC.clone()) else { bail!("pattern mismatch") };
    outLst = __pa0.clone();
    Ok(outLst)
}

fn whenEquationsIndices(mut eqns: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>>) -> Result<Arc<metamodelica::List<i32>>> {
    let mut res: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut b: bool = false;
    let mut i: i32 = 1;
    let mut size: i32 = 0;
    size = BackendEquation::getNumberOfEquations(eqns.clone());
    res = metamodelica::nil();
    while i.clone() <= size.clone() {
        b = (::match_deref::match_deref! { match &(BackendEquation::get(eqns.clone(), i.clone())?) {
        Deref @ BackendDAE::Equation::WHEN_EQUATION { .. } => true,
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
        res = if (b.clone()) {metamodelica::cons(i.clone(), res.clone())} else {res.clone()};
        i = i.clone() + 1;
    }
    Ok(res)
}

fn updateZeroCrossEqnIndex(mut izeroCrossings: Arc<metamodelica::List<BackendDAE::ZeroCrossing>>, mut eqBackendSimCodeMapping: Arc<metamodelica::List<(i32, i32)>>, mut numEqnsinArray: i32) -> Result<Arc<metamodelica::List<BackendDAE::ZeroCrossing>>> {
    let mut ozeroCrossings: Arc<metamodelica::List<BackendDAE::ZeroCrossing>> = metamodelica::nil();
    let mut mappingArray: metamodelica::Array<i32> = Default::default();
    mappingArray = convertListMappingToArray(eqBackendSimCodeMapping.clone(), numEqnsinArray.clone())?;
    ozeroCrossings = updateZeroCrossEqnIndexHelp(izeroCrossings.clone(), mappingArray.clone(), metamodelica::nil())?;
    Ok(ozeroCrossings)
}

fn updateZeroCrossEqnIndexHelp(mut izeroCrossings: Arc<metamodelica::List<BackendDAE::ZeroCrossing>>, mut eqBackendSimCodeMappingArray: metamodelica::Array<i32>, mut iAccum: Arc<metamodelica::List<BackendDAE::ZeroCrossing>>) -> Result<Arc<metamodelica::List<BackendDAE::ZeroCrossing>>> {
    let mut ozeroCrossings: Arc<metamodelica::List<BackendDAE::ZeroCrossing>> = metamodelica::nil();
    ozeroCrossings = (::match_deref::match_deref! { match &(izeroCrossings.clone()) {
        Deref @ metamodelica::List::Nil => {
            Dangerous::listReverseInPlace(iAccum.clone())
        },
        Deref @ metamodelica::List::Cons { head: BackendDAE::ZeroCrossing { iter, occurEquLst, relation_: exp, .. }, tail: rest } => {
            let mut occurEquLst = (*occurEquLst).clone();
            occurEquLst = convertListIndx(occurEquLst.clone(), eqBackendSimCodeMappingArray.clone())?;
            ozeroCrossings = updateZeroCrossEqnIndexHelp(rest.clone(), eqBackendSimCodeMappingArray.clone(), metamodelica::cons(BackendDAE::ZeroCrossing { index: 0, relation_: exp.clone(), occurEquLst: occurEquLst.clone(), iter: iter.clone() }, iAccum.clone()))?;
            ozeroCrossings.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(ozeroCrossings)
}

fn convertListMappingToArray(mut iMapping: Arc<metamodelica::List<(i32, i32)>>, mut numOfBackendEqs: i32) -> Result<metamodelica::Array<i32>> {
    let mut outMapping: metamodelica::Array<i32> = Default::default();
    outMapping = arrayCreate(numOfBackendEqs.clone(), -1);
    outMapping = List::fold(iMapping.clone(), (std::sync::Arc::new(convertListMappingToArray1) as std::sync::Arc<dyn ::std::ops::Fn((i32, i32), metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> + 'static>), outMapping.clone())?;
    Ok(outMapping)
}

fn convertListMappingToArray1(mut iMapping: (i32, i32), mut iMappingArray: metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> {
    let mut outMappingArray: metamodelica::Array<i32> = Default::default();
    let mut simEqIdx: i32 = 0;
    let mut BackendEqnIdx: i32 = 0;
    (simEqIdx, BackendEqnIdx) = iMapping.clone();
    outMappingArray = {let _arr = iMappingArray.clone(); _arr.borrow_mut()[(BackendEqnIdx.clone()-1) as usize] = simEqIdx.clone(); _arr};
    Ok(outMappingArray)
}

fn convertListIndx(mut iIntList: Arc<metamodelica::List<i32>>, mut iMappingArray: metamodelica::Array<i32>) -> Result<Arc<metamodelica::List<i32>>> {
    let mut oIntList: Arc<metamodelica::List<i32>> = metamodelica::nil();
    oIntList = List::map1r(iIntList.clone(), (std::sync::Arc::new(arrayGet) as std::sync::Arc<dyn ::std::ops::Fn(_, i32) -> Result<_> + 'static>), iMappingArray.clone())?;
    Ok(oIntList)
}

fn addAssertEqn(mut asserts: Arc<metamodelica::List<Arc<DAE::Statement>>>, mut iequations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut iuniqueEqIndex: i32) -> (Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32) {
    let mut oequations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    (oequations, ouniqueEqIndex) = (::match_deref::match_deref! { match &(asserts.clone()) {
        Deref @ metamodelica::List::Nil => (iequations.clone(), iuniqueEqIndex.clone()),
        _ => (metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: asserts.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() }), iequations.clone()), iuniqueEqIndex.clone() + 1),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    (oequations, ouniqueEqIndex)
}

fn partitionKindToClockIndex(mut kind: BackendDAE::BaseClockPartitionKind) -> Option<i32> {
    let mut clockIndex: Option<i32> = None;
    clockIndex = (match kind.clone() {
        BackendDAE::BaseClockPartitionKind::CLOCKED_PARTITION { subPartIdx: mut index } => {
            Some(index.clone())
        },
        _ => {
            None
        },
    });
    clockIndex
}

fn createEquation(mut eqNum: i32, mut varNum: i32, mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut skipDiscInAlgorithm: bool, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut cons: Arc<metamodelica::List<Arc<DAE::Constraint>>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equation_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut vars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut eqns: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
    let mut eqn: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
    let mut v: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut partitionKind: BackendDAE::BaseClockPartitionKind = BackendDAE::BaseClockPartitionKind::CONTINUOUS_TIME_PARTITION;
    let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(syst.clone()) {
        Deref @ BackendDAE::EqSystem { partitionKind: __pa0, orderedEqs: __pa1, orderedVars: __pa2, .. } => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
        _ => bail!("pattern mismatch"),
    } };
    partitionKind = __pa0.clone();
    eqns = __pa1.clone();
    vars = __pa2.clone();
    eqn = BackendEquation::get(eqns.clone(), eqNum.clone())?;
    v = (::match_deref::match_deref! { match &(eqn.clone()) {
        Deref @ BackendDAE::Equation::WHEN_EQUATION { .. } => BackendVariable::makeVar(DAE::emptyCref().clone())?,
        _ => BackendVariable::getVarAt(vars.clone(), varNum.clone())?,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    (equation_, ouniqueEqIndex, otempvars) = createEquationImpl(eqn.clone(), v.clone(), vars.clone(), partitionKind.clone(), shared.clone(), skipDiscInAlgorithm.clone(), iuniqueEqIndex.clone(), itempvars.clone(), cons.clone())?;
    Ok((equation_, ouniqueEqIndex, otempvars))
}

fn createEquationImpl(mut eqn: Arc<BackendDAE::Equation>, mut v: BackendDAE::Var, mut vars: BackendDAE::Variables, mut partitionKind: BackendDAE::BaseClockPartitionKind, mut shared: Arc<BackendDAE::Shared>, mut skipDiscInAlgorithm: bool, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut cons: Arc<metamodelica::List<Arc<DAE::Constraint>>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equation_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equation_, ouniqueEqIndex, otempvars) = (::match_deref::match_deref! { match &(eqn.clone()) {
        Deref @ BackendDAE::Equation::ARRAY_EQUATION { attr: eqAttr, source, right: e2, left: Deref @ DAE::Exp::CREF { componentRef: cr, .. }, .. } => {
            let mut varexp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut simEqSys: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            varexp = Expression::crefExp(cr.clone())?;
            simEqSys = Arc::new(SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: iuniqueEqIndex.clone(), lhs: varexp.clone(), exp: e2.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
            (list![simEqSys.clone()], iuniqueEqIndex.clone() + 1, itempvars.clone())
        },
        Deref @ BackendDAE::Equation::FOR_EQUATION { attr: eqAttr, source, stop: cond, start, iter: varexp, .. } => {
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
            let mut solveCr: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut exp_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut iter: ArcStr = arcstr::literal!("");
            let mut asserts: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
            let mut simEqSys: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut funcs: Arc<AvlTreePathFunction::Tree> = Arc::new(AvlTreePathFunction::Tree::EMPTY);
            let mut solveEqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut b: bool = false;
            (b, e1, e2) = (::match_deref::match_deref! { match &(var_field!((*eqn).body, BackendDAE::Equation::FOR_EQUATION).clone()) {
        Deref @ BackendDAE::Equation::EQUATION { scalar: e2, exp: e1, .. } => (true, e1.clone(), e2.clone()),
        Deref @ BackendDAE::Equation::ARRAY_EQUATION { right: e2, left: e1, .. } => (true, e1.clone(), e2.clone()),
        _ => (false, Arc::new(DAE::Exp::BCONST { bool: false }), Arc::new(DAE::Exp::BCONST { bool: false })),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
            let __pa0 = ::match_deref::match_deref! { match &(varexp.clone()) {
                Deref @ DAE::Exp::CREF { componentRef: Deref @ DAE::ComponentRef::CREF_IDENT { ident: __pa0, .. }, .. } => __pa0.clone(),
                _ => bail!("pattern mismatch"),
            } };
            iter = __pa0.clone();
            var = v.clone();
            var.varName = ComponentReference::crefApplySubs(v.varName.clone(), list![Arc::new(DAE::Subscript::INDEX { exp: Arc::new(DAE::Exp::CREF { componentRef: Arc::new(DAE::ComponentRef::CREF_IDENT { ident: (iter.clone()).clone(), identType: DAE::T_INTEGER_DEFAULT().clone(), subscriptLst: metamodelica::nil() }), ty: DAE::T_INTEGER_DEFAULT().clone() }) })])?;
            if b.clone() {
                cr = var.varName.clone();
                let __pa2 = ::match_deref::match_deref! { match &(shared.clone()) {
                    Deref @ BackendDAE::Shared { functionTree: __pa2, .. } => __pa2.clone(),
                    _ => bail!("pattern mismatch"),
                } };
                funcs = __pa2.clone();
                if let Ok((__pa3, __pa4, __pa5, __pa6)) = ExpressionSolve::solve2(e1.clone(), e2.clone(), Expression::crefExp(cr.clone())?, Some(funcs.clone()), Some(iuniqueEqIndex.clone()), true, BackendDAEUtil::isSimulationDAE(shared.clone())) {
                    exp_ = __pa3.clone();
                    asserts = __pa4.clone();
                    solveEqns = __pa5.clone();
                    solveCr = __pa6.clone();
                } else {
                    Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("solving FOR_EQUATION body: ")); __mm_s.push_str(&*BackendDump::equationString(var_field!((*eqn).body, BackendDAE::Equation::FOR_EQUATION).clone())?); __mm_s.push_str(&*literal!("\nfor variable: ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cr.clone())?); __mm_s.push_str(&*literal!(".")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
                    bail!("fail");
                }
                simEqSys = Arc::new(SimCode::SimEqSystem::SES_FOR_LOOP { index: iuniqueEqIndex.clone(), iter: varexp.clone(), startIt: start.clone(), endIt: cond.clone(), cref: cr.clone(), exp: exp_.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
                otempvars = itempvars.clone();
            } else {
                (resEqs, _, otempvars) = createEquationImpl(var_field!((*eqn).body, BackendDAE::Equation::FOR_EQUATION).clone(), var.clone(), vars.clone(), partitionKind.clone(), shared.clone(), skipDiscInAlgorithm.clone(), iuniqueEqIndex.clone(), itempvars.clone(), cons.clone())?;
                simEqSys = Arc::new(SimCode::SimEqSystem::SES_FOR_EQUATION { index: iuniqueEqIndex.clone(), iter: varexp.clone(), startIt: start.clone(), endIt: cond.clone(), body: resEqs.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
            }
            (list![simEqSys.clone()], iuniqueEqIndex.clone() + 1, otempvars.clone())
        },
        Deref @ BackendDAE::Equation::SOLVED_EQUATION { attr: eqAttr, source, exp: e2, .. } => {
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut varexp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            cr = v.varName.clone();
            varexp = Expression::crefExp(cr.clone())?;
            varexp = if (BackendVariable::isStateVar(v.clone())) {Expression::expDer(varexp.clone())} else {varexp.clone()};
            (list![Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: iuniqueEqIndex.clone(), cref: cr.clone(), exp: e2.clone(), source: source.clone(), eqAttr: eqAttr.clone() })], iuniqueEqIndex.clone() + 1, itempvars.clone())
        },
        Deref @ BackendDAE::Equation::WHEN_EQUATION { attr: eqAttr, source, whenEquation, .. } => {
            let mut uniqueEqIndex: i32 = 0;
            let mut conditions: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut cond: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut elseWhen: Arc<BackendDAE::WhenEquation> = Arc::new(<BackendDAE::WhenEquation as ::std::default::Default>::default());
            let mut oelseWhen: Option<Arc<BackendDAE::WhenEquation>> = None;
            let mut elseWhenEquation: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut initialCall: bool = false;
            let mut whenStmtLst: Arc<metamodelica::List<BackendDAE::WhenOperator>> = metamodelica::nil();
            let mut oelseWhenSimEq: Option<Arc<SimCode::SimEqSystem>> = None;
            let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(whenEquation.clone()) {
                Deref @ BackendDAE::WhenEquation { condition: __pa0, whenStmtLst: __pa1, elsewhenPart: __pa2 } => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                _ => bail!("pattern mismatch"),
            } };
            cond = __pa0.clone();
            whenStmtLst = __pa1.clone();
            oelseWhen = __pa2.clone();
            if isSome(oelseWhen.clone()) {
                let __pa3 = ::match_deref::match_deref! { match &(oelseWhen.clone()) {
                    Some(__pa3) => __pa3.clone(),
                    _ => bail!("pattern mismatch"),
                } };
                elseWhen = __pa3.clone();
                (elseWhenEquation, uniqueEqIndex) = createElseWhenEquation(elseWhen.clone(), metamodelica::nil(), iuniqueEqIndex.clone() + 1, source.clone(), eqAttr.clone())?;
                oelseWhenSimEq = Some(elseWhenEquation.clone());
            } else {
                uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                oelseWhenSimEq = None;
            }
            (conditions, initialCall) = BackendDAEUtil::getConditionList(cond.clone())?;
            (list![Arc::new(SimCode::SimEqSystem::SES_WHEN { index: iuniqueEqIndex.clone(), conditions: conditions.clone(), initialCall: initialCall.clone(), whenStmtLst: whenStmtLst.clone(), elseWhen: oelseWhenSimEq.clone(), source: source.clone(), eqAttr: eqAttr.clone() })], uniqueEqIndex.clone() + 1, itempvars.clone())
        },
        Deref @ BackendDAE::Equation::EQUATION { attr: eqAttr, source, scalar: e2, exp: e1 } => {
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut uniqueEqIndex1: i32 = 0;
            let mut uniqueEqIndex: i32 = 0;
            let mut solveCr: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut varexp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut exp_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut asserts: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut homotopySupport: bool = false;
            let mut funcs: Arc<AvlTreePathFunction::Tree> = Arc::new(AvlTreePathFunction::Tree::EMPTY);
            let mut solveEqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut b: bool = false;
            let mut source = (*source).clone();
            cr = v.varName.clone();
            varexp = Expression::crefExp(cr.clone())?;
            varexp = if (BackendVariable::isStateVar(v.clone())) {Expression::expDer(varexp.clone())} else {varexp.clone()};
            let __pa0 = ::match_deref::match_deref! { match &(shared.clone()) {
                Deref @ BackendDAE::Shared { functionTree: __pa0, .. } => __pa0.clone(),
                _ => bail!("pattern mismatch"),
            } };
            funcs = __pa0.clone();
            b = true;
            if '__try1: {
                (exp_, asserts, solveEqns, solveCr) = unwrap_break_err!(ExpressionSolve::solve2(e1.clone(), e2.clone(), varexp.clone(), Some(funcs.clone()), Some(iuniqueEqIndex.clone()), true, BackendDAEUtil::isSimulationDAE(shared.clone())), '__try1);
                Ok::<(), anyhow::Error>(())
            }.is_err() {
                b = false;
            }
            if b.clone() {
                solveEqns = solveEqns.clone().reverse();
                solveCr = solveCr.clone().reverse();
                cr = if (BackendVariable::isStateVar(v.clone())) {ComponentReference::crefPrefixDer(cr.clone())} else {cr.clone()};
                source = ElementSource::addSymbolicTransformationSolve(true, source.clone(), cr.clone(), e1.clone(), e2.clone(), exp_.clone(), asserts.clone())?;
                (eqs, uniqueEqIndex1) = List::mapFold(solveEqns.clone(), (std::sync::Arc::new(makeSolved) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), iuniqueEqIndex.clone())?;
                if cons.clone().is_empty() {
                    (resEqs, uniqueEqIndex) = addAssertEqn(asserts.clone(), list![Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: uniqueEqIndex1.clone(), cref: cr.clone(), exp: exp_.clone(), source: source.clone(), eqAttr: eqAttr.clone() })], uniqueEqIndex1.clone() + 1);
                } else {
                    (resEqs, uniqueEqIndex) = addAssertEqn(asserts.clone(), list![Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { index: uniqueEqIndex1.clone(), cref: cr.clone(), exp: exp_.clone(), source: source.clone(), cons: cons.clone(), eqAttr: eqAttr.clone() })], uniqueEqIndex1.clone() + 1);
                }
                eqSystlst = listAppend(eqs.clone(), resEqs.clone());
                tempvars = createTempVarsforCrefs(List::map(solveCr.clone(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>))?, itempvars.clone())?;
            } else {
                let (__pa2, (__pa3, _), __pa4) = createNonlinearResidualEquations(list![eqn.clone()], (iuniqueEqIndex.clone(), 0), itempvars.clone(), shared.functionTree.clone())?;
                resEqs = __pa2.clone();
                uniqueEqIndex = __pa3.clone();
                tempvars = __pa4.clone();
                resEqs = fixNonlinearResidualIndices(resEqs.clone());
                cr = if (BackendVariable::isStateVar(v.clone())) {ComponentReference::crefPrefixDer(cr.clone())} else {cr.clone()};
                (_, homotopySupport) = BackendEquation::traverseExpsOfEquation(eqn.clone(), (std::sync::Arc::new(BackendDAEUtil::containsHomotopyCall) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, bool) -> Result<(Arc<DAE::Exp>, bool)> + 'static>), false)?;
                eqSystlst = list![Arc::new(SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Arc::new(SimCode::NonlinearSystem { index: uniqueEqIndex.clone(), eqs: resEqs.clone(), crefs: list![cr.clone()], indexNonLinearSystem: 0, nUnknowns: 1, jacobianMatrix: None, homotopySupport: homotopySupport.clone(), mixedSystem: false, tornSystem: false, clockIndex: partitionKindToClockIndex(partitionKind.clone()) }), alternativeTearing: None, eqAttr: eqAttr.clone() })];
                uniqueEqIndex = uniqueEqIndex.clone() + 1;
            }
            (eqSystlst.clone(), uniqueEqIndex.clone(), tempvars.clone())
        },
        Deref @ BackendDAE::Equation::ALGORITHM { attr: eqAttr, expand: crefExpand, source, alg, .. } => {
            let mut algStatements: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
            let mut varOutput: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut algStr: ArcStr = arcstr::literal!("");
            let mut message: ArcStr = arcstr::literal!("");
            let __pa0 = ::match_deref::match_deref! { match &(CheckModel::checkAndGetAlgorithmOutputs(alg.clone(), source.clone(), crefExpand.clone())?) {
                Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Nil } => __pa0.clone(),
                _ => bail!("pattern mismatch"),
            } };
            varOutput = __pa0.clone();
            let __pa2 = ::match_deref::match_deref! { match &(BackendDAEUtil::collateAlgorithm(alg.clone(), None)?) {
                Deref @ DAE::Algorithm { statementLst: __pa2 } => __pa2.clone(),
                _ => bail!("pattern mismatch"),
            } };
            algStatements = __pa2.clone();
            if ComponentReferenceBasics::crefEqualNoStringCompare(BackendVariable::varCref(v.clone())?, varOutput.clone())? {
                if skipDiscInAlgorithm.clone() {
                    algStatements = BackendDAEUtil::removeDiscreteAssignments(algStatements.clone(), vars.clone())?;
                }
            } else {
                if let Ok(__iflet3) = solveAlgorithmInverse(algStatements.clone(), list![v.clone()]) {
                    algStatements = __iflet3;
                } else {
                    algStr = (DAEDump::dumpAlgorithmsStr(list![Arc::new(DAE::Element::ALGORITHM { algorithm_: alg.clone(), source: source.clone() })])?).clone();
                    message = (ComponentReferenceBasics::printComponentRefStr(BackendVariable::varCref(v.clone())?)?).clone();
                    message = stringAppendList(list![(literal!("Inverse Algorithm needs to be solved for ")).clone(), (message.clone()).clone(), (literal!(" in\n")).clone(), (algStr.clone()).clone(), (literal!("This has not been implemented yet.\n")).clone()]);
                    Error::addInternalError((message.clone()).clone(), metamodelica::sourceInfo!())?;
                    bail!("fail");
                }
            }
            (list![Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: algStatements.clone(), eqAttr: eqAttr.clone() })], iuniqueEqIndex.clone() + 1, itempvars.clone())
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok((equation_, ouniqueEqIndex, otempvars))
}

fn replaceIFBrancheswithoutVar(mut inExp: Arc<DAE::Exp>, mut inTpl: (Arc<DAE::Exp>, Arc<DAE::Exp>)) -> Result<(Arc<DAE::Exp>, (Arc<DAE::Exp>, Arc<DAE::Exp>))> {
    let mut outExp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut outTpl: (Arc<DAE::Exp>, Arc<DAE::Exp>) = (Arc::new(<DAE::Exp as ::std::default::Default>::default()), Arc::new(<DAE::Exp as ::std::default::Default>::default()));
    (outExp, outTpl) = (::match_deref::match_deref! { match &((inExp.clone(), inTpl.clone())) {
        (Deref @ DAE::Exp::IFEXP { expCond: cond, expThen: e1, expElse: e2 }, (crexp, exp)) => {
            let mut b1: bool = false;
            let mut b2: bool = false;
            let mut e1 = (*e1).clone();
            let mut e2 = (*e2).clone();
            b1 = Expression::expContains(e1.clone(), crexp.clone())?;
            e1 = if (b1.clone()) {e1.clone()} else {exp.clone()};
            b2 = Expression::expContains(e2.clone(), crexp.clone())?;
            e2 = if (b2.clone()) {e2.clone()} else {exp.clone()};
            (if (b1.clone() && b2.clone()) {inExp.clone()} else {Arc::new(DAE::Exp::IFEXP { expCond: cond.clone(), expThen: e1.clone(), expElse: e2.clone() })}, inTpl.clone())
        },
        _ => {
            (inExp.clone(), inTpl.clone())
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((outExp, outTpl))
}

fn solveAlgorithmInverse(mut inStmts: Arc<metamodelica::List<Arc<DAE::Statement>>>, mut inSolveFor: Arc<metamodelica::List<BackendDAE::Var>>) -> Result<Arc<metamodelica::List<Arc<DAE::Statement>>>> {
    let mut outStmts: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
    outStmts = (::match_deref::match_deref! { match &((inStmts.clone(), inSolveFor.clone())) {
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_ASSIGN { source: source1, exp: e12, exp1: e11, .. }, tail: Deref @ metamodelica::List::Nil }, Deref @ metamodelica::List::Cons { head: v1 @ BackendDAE::Var { varName: cr1, .. }, tail: Deref @ metamodelica::List::Nil }) => {
            let mut varexp1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut solvedExp1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut tp1: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut asserts: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
            let mut source1 = (*source1).clone();
            let mut cr1 = (*cr1).clone();
            varexp1 = Expression::crefExp(cr1.clone())?;
            varexp1 = if (BackendVariable::isStateVar(v1.clone())) {Expression::expDer(varexp1.clone())} else {varexp1.clone()};
            (solvedExp1, asserts) = ExpressionSolve::solve(e11.clone(), e12.clone(), varexp1.clone(), None)?;
            cr1 = if (BackendVariable::isStateVar(v1.clone())) {ComponentReference::crefPrefixDer(cr1.clone())} else {cr1.clone()};
            source1 = ElementSource::addSymbolicTransformationSolve(true, source1.clone(), cr1.clone(), e11.clone(), e12.clone(), solvedExp1.clone(), asserts.clone())?;
            tp1 = Expression::r#typeof(varexp1.clone())?;
            list![Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp1.clone(), exp1: varexp1.clone(), exp: solvedExp1.clone(), source: source1.clone() })]
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_ASSIGN { source: source1, exp: e12, exp1: e11, .. }, tail: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_ASSIGN { source: source2, exp: e22, exp1: e21, .. }, tail: Deref @ metamodelica::List::Nil } }, Deref @ metamodelica::List::Cons { head: v1 @ BackendDAE::Var { varName: cr1, .. }, tail: Deref @ metamodelica::List::Cons { head: v2 @ BackendDAE::Var { varName: cr2, .. }, tail: Deref @ metamodelica::List::Nil } }) => {
            let mut varexp1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut solvedExp1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut tp1: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut varexp2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut solvedExp2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut tp2: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut asserts: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
            let mut source1 = (*source1).clone();
            let mut source2 = (*source2).clone();
            let mut cr1 = (*cr1).clone();
            let mut cr2 = (*cr2).clone();
            let false = (Expression::expHasCref(e12.clone(), cr2.clone())?) else { bail!("pattern mismatch") };
            let false = (Expression::expHasCref(e22.clone(), cr1.clone())?) else { bail!("pattern mismatch") };
            varexp1 = Expression::crefExp(cr1.clone())?;
            varexp1 = if (BackendVariable::isStateVar(v1.clone())) {Expression::expDer(varexp1.clone())} else {varexp1.clone()};
            (solvedExp1, asserts) = ExpressionSolve::solve(e11.clone(), e12.clone(), varexp1.clone(), None)?;
            cr1 = if (BackendVariable::isStateVar(v1.clone())) {ComponentReference::crefPrefixDer(cr1.clone())} else {cr1.clone()};
            source1 = ElementSource::addSymbolicTransformationSolve(true, source1.clone(), cr1.clone(), e11.clone(), e12.clone(), solvedExp1.clone(), asserts.clone())?;
            tp1 = Expression::r#typeof(varexp1.clone())?;
            varexp2 = Expression::crefExp(cr2.clone())?;
            varexp2 = if (BackendVariable::isStateVar(v2.clone())) {Expression::expDer(varexp2.clone())} else {varexp2.clone()};
            (solvedExp2, asserts) = ExpressionSolve::solve(e21.clone(), e22.clone(), varexp2.clone(), None)?;
            cr2 = if (BackendVariable::isStateVar(v2.clone())) {ComponentReference::crefPrefixDer(cr2.clone())} else {cr2.clone()};
            source2 = ElementSource::addSymbolicTransformationSolve(true, source2.clone(), cr2.clone(), e21.clone(), e22.clone(), solvedExp2.clone(), asserts.clone())?;
            tp2 = Expression::r#typeof(varexp2.clone())?;
            list![Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp1.clone(), exp1: varexp1.clone(), exp: solvedExp1.clone(), source: source1.clone() }), Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp2.clone(), exp1: varexp2.clone(), exp: solvedExp2.clone(), source: source2.clone() })]
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outStmts)
}

// =============================================================================
// Section for handling complex equations
//
// =============================================================================
fn createNonlinearResidualEquationsComplex(mut inExp: Arc<DAE::Exp>, mut inExp1: Arc<DAE::Exp>, mut source: Arc<DAE::ElementSource>, mut eqAttr: BackendDAE::EquationAttributes, mut idx_tpl: (i32, i32), mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, (i32, i32), Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut idx_tpl: (i32, i32) = idx_tpl;
    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars;
    let mut eq_idx: i32 = 0;
    let mut res_idx: i32 = 0;
    (eq_idx, res_idx) = idx_tpl.clone();
    (equations_, idx_tpl, tempvars) = 'mc: {
        let __mc_input = (inExp.clone(), inExp1.clone());
        if let Ok((__v, __wb0, __wb1, __wb2)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::CAST { ty: _, exp: e1 }, _) => {
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    let mut idx_tpl: (i32, i32) = idx_tpl.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    (equations_, idx_tpl, tempvars) = createNonlinearResidualEquationsComplex(e1.clone(), inExp1.clone(), source.clone(), eqAttr.clone(), idx_tpl.clone(), tempvars.clone())?;
                    Ok(((equations_.clone(), idx_tpl.clone(), tempvars.clone()), equations_.clone(), idx_tpl.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; idx_tpl = __wb1; tempvars = __wb2; break 'mc __v; }
        if let Ok((__v, __wb0, __wb1, __wb2)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ DAE::Exp::CAST { ty: _, exp: e2 }) => {
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    let mut idx_tpl: (i32, i32) = idx_tpl.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    (equations_, idx_tpl, tempvars) = createNonlinearResidualEquationsComplex(inExp.clone(), e2.clone(), source.clone(), eqAttr.clone(), idx_tpl.clone(), tempvars.clone())?;
                    Ok(((equations_.clone(), idx_tpl.clone(), tempvars.clone()), equations_.clone(), idx_tpl.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; idx_tpl = __wb1; tempvars = __wb2; break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::CREF { componentRef: cr, .. }, _) => {
                    let mut crtmp: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut etmp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
                    let mut varLst: Arc<metamodelica::List<Arc<DAE::Var>>> = metamodelica::nil();
                    let mut e1lst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut e2lst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut path: Arc<Absyn::Path> = Arc::new(<Absyn::Path as ::std::default::Default>::default());
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut eq_idx: i32 = eq_idx.clone();
                    let mut res_idx: i32 = res_idx.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    (e2_1, _) = Expression::extendArrExp(inExp1.clone(), false)?;
                    let (__pa2, __pa0, __pa1) = ::match_deref::match_deref! { match &(Expression::r#typeof(inExp.clone())?) {
                        __pa2 @ Deref @ DAE::Type::T_COMPLEX { complexClassType: ClassInf::State::RECORD { path: __pa0 }, varLst: __pa1, .. } => (__pa2.clone(), __pa0.clone(), __pa1.clone()),
                        _ => bail!("pattern mismatch"),
                    } };
                    path = __pa0.clone();
                    varLst = __pa1.clone();
                    tp = __pa2.clone();
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    crtmp = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(eq_idx.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    tempvars = createTempVars(varLst.clone(), crtmp.clone(), tempvars.clone())?;
                    e1lst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr.clone())?;
                    e2lst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), crtmp.clone())?;
                    exptl = List::zip(e1lst.clone(), e2lst.clone());
                    let (__pa3, (__pa4, __pa5)) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil())?;
                    eqSystlst = __pa3.clone();
                    eq_idx = __pa4.clone();
                    res_idx = __pa5.clone();
                    etmp = Expression::crefExp(crtmp.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: etmp.clone(), exp: e2_1.clone(), source: source.clone() });
                    eqSystlst = metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: eq_idx.clone(), statements: list![stms.clone()], eqAttr: eqAttr.clone() }), eqSystlst.clone());
                    Ok(((eqSystlst.clone(), (eq_idx.clone() + 1, res_idx.clone()), tempvars.clone()), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { tempvars = __wb0; break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ DAE::Exp::CREF { componentRef: cr, .. }) => {
                    let mut crtmp: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut etmp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
                    let mut varLst: Arc<metamodelica::List<Arc<DAE::Var>>> = metamodelica::nil();
                    let mut e1lst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut e2lst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut path: Arc<Absyn::Path> = Arc::new(<Absyn::Path as ::std::default::Default>::default());
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut eq_idx: i32 = eq_idx.clone();
                    let mut res_idx: i32 = res_idx.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    (e1_1, _) = Expression::extendArrExp(inExp.clone(), false)?;
                    let (__pa2, __pa0, __pa1) = ::match_deref::match_deref! { match &(Expression::r#typeof(inExp1.clone())?) {
                        __pa2 @ Deref @ DAE::Type::T_COMPLEX { complexClassType: ClassInf::State::RECORD { path: __pa0 }, varLst: __pa1, .. } => (__pa2.clone(), __pa0.clone(), __pa1.clone()),
                        _ => bail!("pattern mismatch"),
                    } };
                    path = __pa0.clone();
                    varLst = __pa1.clone();
                    tp = __pa2.clone();
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    crtmp = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(eq_idx.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    tempvars = createTempVars(varLst.clone(), crtmp.clone(), tempvars.clone())?;
                    e1lst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr.clone())?;
                    e2lst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), crtmp.clone())?;
                    exptl = List::zip(e1lst.clone(), e2lst.clone());
                    let (__pa3, (__pa4, __pa5)) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil())?;
                    eqSystlst = __pa3.clone();
                    eq_idx = __pa4.clone();
                    res_idx = __pa5.clone();
                    etmp = Expression::crefExp(crtmp.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: etmp.clone(), exp: e1_1.clone(), source: source.clone() });
                    eqSystlst = metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: eq_idx.clone(), statements: list![stms.clone()], eqAttr: eqAttr.clone() }), eqSystlst.clone());
                    Ok(((eqSystlst.clone(), (eq_idx.clone() + 1, res_idx.clone()), tempvars.clone()), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { tempvars = __wb0; break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: tp @ Deref @ DAE::Type::T_COMPLEX { complexClassType: ClassInf::State::RECORD { path: rpath }, varLst, .. }, .. }, expLst: e2lst, path }, _) => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut e1lst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut e2lst = (*e2lst).clone();
                    let mut eq_idx: i32 = eq_idx.clone();
                    let mut res_idx: i32 = res_idx.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    let true = (AbsynUtil::pathEqual(path.clone(), rpath.clone())) else { bail!("pattern mismatch") };
                    (e2_1, _) = Expression::extendArrExp(inExp1.clone(), false)?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(eq_idx.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    e1_1 = Expression::crefToExp(cr.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e1_1.clone(), exp: e2_1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: eq_idx.clone(), statements: list![stms.clone()], eqAttr: eqAttr.clone() });
                    eq_idx = eq_idx.clone() + 1;
                    e1lst = Expression::expandExpression(e1_1.clone(), true)?;
                    e2lst = List::mapFlat(e2lst.clone(), (std::sync::Arc::new({ let __pe_b1 = true; move |__pe_a0| Expression::expandExpression(__pe_a0, __pe_b1.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<Arc<metamodelica::List<Arc<DAE::Exp>>>> + 'static>))?;
                    exptl = List::zip(e1lst.clone(), e2lst.clone());
                    let (__pa0, (__pa1, __pa2)) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil())?;
                    eqSystlst = __pa0.clone();
                    eq_idx = __pa1.clone();
                    res_idx = __pa2.clone();
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), eqSystlst.clone());
                    tempvars = createTempVars(varLst.clone(), cr.clone(), tempvars.clone())?;
                    Ok(((eqSystlst.clone(), (eq_idx.clone(), res_idx.clone()), tempvars.clone()), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { tempvars = __wb0; break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::RECORD { ty: tp @ Deref @ DAE::Type::T_COMPLEX { varLst, .. }, exps: e2lst, path, .. }, _) => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut e1lst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut e2lst = (*e2lst).clone();
                    let mut eq_idx: i32 = eq_idx.clone();
                    let mut res_idx: i32 = res_idx.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    (e2_1, _) = Expression::extendArrExp(inExp1.clone(), false)?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(eq_idx.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    e1_1 = Expression::crefToExp(cr.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e1_1.clone(), exp: e2_1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: eq_idx.clone(), statements: list![stms.clone()], eqAttr: eqAttr.clone() });
                    eq_idx = eq_idx.clone() + 1;
                    e1lst = Expression::expandExpression(e1_1.clone(), true)?;
                    e2lst = List::mapFlat(e2lst.clone(), (std::sync::Arc::new({ let __pe_b1 = true; move |__pe_a0| Expression::expandExpression(__pe_a0, __pe_b1.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<Arc<metamodelica::List<Arc<DAE::Exp>>>> + 'static>))?;
                    exptl = List::zip(e1lst.clone(), e2lst.clone());
                    let (__pa0, (__pa1, __pa2)) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil())?;
                    eqSystlst = __pa0.clone();
                    eq_idx = __pa1.clone();
                    res_idx = __pa2.clone();
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), eqSystlst.clone());
                    tempvars = createTempVars(varLst.clone(), cr.clone(), tempvars.clone())?;
                    Ok(((eqSystlst.clone(), (eq_idx.clone(), res_idx.clone()), tempvars.clone()), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { tempvars = __wb0; break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: tp @ Deref @ DAE::Type::T_COMPLEX { complexClassType: ClassInf::State::RECORD { path: rpath }, varLst, .. }, .. }, expLst: e2lst, path }) => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut e1lst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut eq_idx: i32 = eq_idx.clone();
                    let mut res_idx: i32 = res_idx.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    let true = (AbsynUtil::pathEqual(path.clone(), rpath.clone())) else { bail!("pattern mismatch") };
                    (e1_1, _) = Expression::extendArrExp(inExp1.clone(), false)?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(eq_idx.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    e2_1 = Expression::crefExp(cr.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e2_1.clone(), exp: e1_1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: eq_idx.clone(), statements: list![stms.clone()], eqAttr: eqAttr.clone() });
                    eq_idx = eq_idx.clone() + 1;
                    e1lst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr.clone())?;
                    exptl = List::zip(e1lst.clone(), e2lst.clone());
                    let (__pa0, (__pa1, __pa2)) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil())?;
                    eqSystlst = __pa0.clone();
                    eq_idx = __pa1.clone();
                    res_idx = __pa2.clone();
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), eqSystlst.clone());
                    tempvars = createTempVars(varLst.clone(), cr.clone(), tempvars.clone())?;
                    Ok(((eqSystlst.clone(), (eq_idx.clone(), res_idx.clone()), tempvars.clone()), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { tempvars = __wb0; break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ DAE::Exp::RECORD { ty: tp @ Deref @ DAE::Type::T_COMPLEX { varLst, .. }, exps: e2lst, path, .. }) => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut e1lst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut eq_idx: i32 = eq_idx.clone();
                    let mut res_idx: i32 = res_idx.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    (e1_1, _) = Expression::extendArrExp(inExp1.clone(), false)?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(eq_idx.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    e2_1 = Expression::crefExp(cr.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e2_1.clone(), exp: e1_1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: eq_idx.clone(), statements: list![stms.clone()], eqAttr: eqAttr.clone() });
                    eq_idx = eq_idx.clone() + 1;
                    e1lst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr.clone())?;
                    exptl = List::zip(e1lst.clone(), e2lst.clone());
                    let (__pa0, (__pa1, __pa2)) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil())?;
                    eqSystlst = __pa0.clone();
                    eq_idx = __pa1.clone();
                    res_idx = __pa2.clone();
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), eqSystlst.clone());
                    tempvars = createTempVars(varLst.clone(), cr.clone(), tempvars.clone())?;
                    Ok(((eqSystlst.clone(), (eq_idx.clone(), res_idx.clone()), tempvars.clone()), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { tempvars = __wb0; break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::TUPLE { PR: expl }, Deref @ DAE::Exp::CALL { path, .. }) => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut crlst: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut crlstlst: Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>> = metamodelica::nil();
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
                    let mut crexplst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut expl = (*expl).clone();
                    let mut eq_idx: i32 = eq_idx.clone();
                    let mut res_idx: i32 = res_idx.clone();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars.clone();
                    tp = Expression::r#typeof(inExp.clone())?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(eq_idx.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    crexplst = List::map1(expl.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_TUPLE_ASSIGN { type_: tp.clone(), expExpLst: crexplst.clone(), exp: inExp1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: eq_idx.clone(), statements: list![stms.clone()], eqAttr: eqAttr.clone() });
                    eq_idx = eq_idx.clone() + 1;
                    expl = List::filterOnTrue(expl.clone(), (std::sync::Arc::new(fnptr!(Expression::isNotWild, Arc<DAE::Exp>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<bool> + 'static>))?;
                    expl = List::flatten(List::map1(expl.clone(), (std::sync::Arc::new(Expression::generateCrefsExpLstFromExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Option<Arc<DAE::ComponentRef>>) -> Result<Arc<metamodelica::List<Arc<DAE::Exp>>>> + 'static>), None)?)?;
                    crexplst = List::flatten(List::map1(expl.clone(), (std::sync::Arc::new(Expression::generateCrefsExpLstFromExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Option<Arc<DAE::ComponentRef>>) -> Result<Arc<metamodelica::List<Arc<DAE::Exp>>>> + 'static>), Some(cr.clone()))?)?;
                    crlst = List::map(crexplst.clone(), (std::sync::Arc::new(Expression::expCref) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    crlstlst = List::map1(crlst.clone(), (std::sync::Arc::new(ComponentReference::expandCref) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, bool) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> + 'static>), true)?;
                    crlst = List::flatten(crlstlst.clone())?;
                    crexplst = List::map(crlst.clone(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>))?;
                    crlst = List::map(expl.clone(), (std::sync::Arc::new(Expression::expCref) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    crlstlst = List::map1(crlst.clone(), (std::sync::Arc::new(ComponentReference::expandCref) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, bool) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> + 'static>), true)?;
                    crlst = List::flatten(crlstlst.clone())?;
                    expl = List::map(crlst.clone(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>))?;
                    exptl = List::zip(expl.clone(), crexplst.clone());
                    let (__pa0, (__pa1, __pa2)) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil())?;
                    eqSystlst = __pa0.clone();
                    eq_idx = __pa1.clone();
                    res_idx = __pa2.clone();
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), eqSystlst.clone());
                    tempvars = createTempVarsforCrefs(crexplst.clone().reverse(), tempvars.clone())?;
                    Ok(((eqSystlst.clone(), (eq_idx.clone(), res_idx.clone()), tempvars.clone()), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { tempvars = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut s: ArcStr = arcstr::literal!("");
                    let mut s1: ArcStr = arcstr::literal!("");
                    let mut s2: ArcStr = arcstr::literal!("");
                    s1 = (ExpressionBasics::printExpStr(inExp.clone())?).clone();
                    s2 = (ExpressionBasics::printExpStr(inExp1.clone())?).clone();
                    s = stringAppendList(list![(literal!("function createNonlinearResidualEquationsComplex failed for: ")).clone(), (s1.clone()).clone(), (literal!(" = ")).clone(), (s2.clone()).clone()]);
                    Error::addInternalError((s.clone()).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, idx_tpl, tempvars))
}

fn createNonlinearResidualEquationsSingleComplex(mut inExp: Arc<DAE::Exp>, mut inExp1: Arc<DAE::Exp>, mut source: Arc<DAE::ElementSource>, mut eqAttr: BackendDAE::EquationAttributes, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut eqCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut eqCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = eqCrefs;
    (equations_, ouniqueEqIndex, otempvars) = (::match_deref::match_deref! { match &((inExp.clone(), inExp1.clone())) {
        (Deref @ DAE::Exp::TUPLE { PR: expl }, Deref @ DAE::Exp::CALL { expLst: callExps, path, .. }) => {
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut callCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
            let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut crexplst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut lhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut rhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut lhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut rhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut eqSystlst_simpAss: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
            let mut uniqueEqIndex: i32 = 0;
            let mut ident: ArcStr = arcstr::literal!("");
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            (_, callCrefs) = Expression::traverseExpList(callExps.clone(), (std::sync::Arc::new(Expression::traversingComponentRefFinder) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> + 'static>), metamodelica::nil())?;
            tp = Expression::r#typeof(inExp.clone())?;
            ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
            cr = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
            (eqCrefs, crexplst, lhsExpLstRes, rhsExpLstRes, lhsExpLstAss, rhsExpLstAss) = createTmpCrefExpsForComplexEqnSys(expl.clone(), cr.clone(), eqCrefs.clone(), callCrefs.clone(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil(), metamodelica::nil())?;
            stms = Arc::new(DAE::Statement::STMT_TUPLE_ASSIGN { type_: tp.clone(), expExpLst: crexplst.clone(), exp: inExp1.clone(), source: source.clone() });
            simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![stms.clone()], eqAttr: eqAttr.clone() });
            uniqueEqIndex = iuniqueEqIndex.clone() + 1;
            exptl = List::zip(lhsExpLstAss.clone(), rhsExpLstAss.clone());
            (eqSystlst_simpAss, uniqueEqIndex) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_SIMPLE_ASSIGN) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), source.clone(), eqAttr.clone(), uniqueEqIndex.clone(), metamodelica::nil())?;
            exptl = List::zip(rhsExpLstRes.clone(), lhsExpLstRes.clone());
            let (__pa0, (__pa1, _)) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (uniqueEqIndex.clone(), 0), metamodelica::nil())?;
            eqSystlst = __pa0.clone();
            uniqueEqIndex = __pa1.clone();
            eqSystlst = metamodelica::cons(simeqn_complex.clone(), listAppend(eqSystlst_simpAss.clone(), eqSystlst.clone()));
            tempvars = createTempVarsforCrefs(rhsExpLstAss.clone().reverse(), itempvars.clone())?;
            tempvars = createTempVarsforCrefs(lhsExpLstRes.clone().reverse(), tempvars.clone())?;
            (eqSystlst.clone(), uniqueEqIndex.clone(), tempvars.clone())
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok((equations_, ouniqueEqIndex, otempvars, eqCrefs))
}

fn createTmpCrefExpsForComplexEqnSys(mut inExpLst: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut crPrefix: Arc<DAE::ComponentRef>, mut eqCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut callCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut lhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut rhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut lhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut rhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>>) -> Result<(Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::Exp>>>, Arc<metamodelica::List<Arc<DAE::Exp>>>, Arc<metamodelica::List<Arc<DAE::Exp>>>, Arc<metamodelica::List<Arc<DAE::Exp>>>, Arc<metamodelica::List<Arc<DAE::Exp>>>)> {
    let mut eqCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = eqCrefs;
    let mut outExpList: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
    let mut lhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>> = lhsExpLstRes;
    let mut rhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>> = rhsExpLstRes;
    let mut lhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>> = lhsExpLstAss;
    let mut rhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>> = rhsExpLstAss;
    let mut newExp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    for mut e in &*inExpLst.clone() {
        let mut e = e.clone();
        (eqCrefs, newExp, lhsExpLstRes, rhsExpLstRes, lhsExpLstAss, rhsExpLstAss) = createTmpCrefExpsForComplexEqnSys_work(e.clone(), crPrefix.clone(), eqCrefs.clone(), callCrefs.clone(), lhsExpLstRes.clone(), rhsExpLstRes.clone(), lhsExpLstAss.clone(), rhsExpLstAss.clone())?;
        outExpList = metamodelica::cons(newExp.clone(), outExpList.clone());
    }
    outExpList = Dangerous::listReverseInPlace(outExpList.clone());
    Ok((eqCrefs, outExpList, lhsExpLstRes, rhsExpLstRes, lhsExpLstAss, rhsExpLstAss))
}

fn createTmpCrefExpsForComplexEqnSys_work(mut inExp: Arc<DAE::Exp>, mut inCrefPrefix: Arc<DAE::ComponentRef>, mut eqCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut callCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut lhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut rhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut lhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut rhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>>) -> Result<(Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>, Arc<metamodelica::List<Arc<DAE::Exp>>>, Arc<metamodelica::List<Arc<DAE::Exp>>>, Arc<metamodelica::List<Arc<DAE::Exp>>>)> {
    let mut eqCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = eqCrefs;
    let mut outCrefExp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut lhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>> = lhsExpLstRes;
    let mut rhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>> = rhsExpLstRes;
    let mut lhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>> = lhsExpLstAss;
    let mut rhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>> = rhsExpLstAss;
    outCrefExp = ({
        let mut tmp_lhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
        let mut tmp_rhsExpLstRes: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
        let mut tmp_lhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
        let mut tmp_rhsExpLstAss: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
        let mut createTmpVar: bool = false;
        (::match_deref::match_deref! { match &(inExp.clone()) {
        Deref @ DAE::Exp::CREF { componentRef: Deref @ DAE::ComponentRef::WILD { .. }, .. } => {
            inExp.clone()
        },
        Deref @ DAE::Exp::CREF { componentRef: cr, .. } if (Expression::isArrayType(ComponentReference::crefTypeFull(cr.clone())?)) => {
            let mut name: ArcStr = arcstr::literal!("");
            let mut ty: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut c1: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut crlist: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut tmp_exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut isEqCref: bool = false;
            let mut cr = (*cr).clone();
            crlist = ComponentReference::expandCref(cr.clone(), true)?;
            for mut c in &*crlist.clone() {
                let mut c = c.clone();
                name = (ComponentReference::crefModelicaStr(c.clone())).clone();
                ty = ComponentReference::crefTypeFull(c.clone())?;
                c1 = ComponentReference::crefPrependIdent(inCrefPrefix.clone(), (name.clone()).clone(), ComponentReferenceBasics::crefSubs(c.clone())?, ty.clone())?;
                tmp_exp = Expression::makeCrefExp(c1.clone(), ty.clone())?;
                exp = Expression::makeCrefExp(c.clone(), ty.clone())?;
                isEqCref = List::isMemberOnTrue(c.clone(), eqCrefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?;
                if isEqCref.clone() {
                    tmp_lhsExpLstAss = metamodelica::cons(exp.clone(), tmp_lhsExpLstAss.clone());
                    tmp_rhsExpLstAss = metamodelica::cons(tmp_exp.clone(), tmp_rhsExpLstAss.clone());
                    (eqCrefs, _) = List::deleteMemberOnTrue(c.clone(), eqCrefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?;
                } else {
                    tmp_lhsExpLstRes = metamodelica::cons(tmp_exp.clone(), tmp_lhsExpLstRes.clone());
                    tmp_rhsExpLstRes = metamodelica::cons(exp.clone(), tmp_rhsExpLstRes.clone());
                }
                createTmpVar = createTmpVar.clone() || !(isEqCref.clone());
            }
            if createTmpVar.clone() {
                name = (ComponentReference::crefModelicaStr(cr.clone())).clone();
                ty = ComponentReference::crefTypeFull(cr.clone())?;
                cr = ComponentReference::crefPrependIdent(inCrefPrefix.clone(), (name.clone()).clone(), ComponentReferenceBasics::crefSubs(cr.clone())?, ty.clone())?;
                e = Expression::makeCrefExp(cr.clone(), ty.clone())?;
                lhsExpLstAss = listAppend(tmp_lhsExpLstAss.clone(), lhsExpLstAss.clone());
                rhsExpLstAss = listAppend(tmp_rhsExpLstAss.clone(), rhsExpLstAss.clone());
                lhsExpLstRes = listAppend(tmp_lhsExpLstRes.clone(), lhsExpLstRes.clone());
                rhsExpLstRes = listAppend(tmp_rhsExpLstRes.clone(), rhsExpLstRes.clone());
            } else {
                e = inExp.clone();
            }
            e.clone()
        },
        Deref @ DAE::Exp::CREF { componentRef: cr, .. } => {
            let mut name: ArcStr = arcstr::literal!("");
            let mut ty: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut cr = (*cr).clone();
            if !(List::isMemberOnTrue(cr.clone(), eqCrefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?) || List::isMemberOnTrue(cr.clone(), callCrefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))? {
                name = (ComponentReference::crefModelicaStr(cr.clone())).clone();
                ty = ComponentReference::crefTypeFull(cr.clone())?;
                cr = ComponentReference::crefPrependIdent(inCrefPrefix.clone(), (name.clone()).clone(), ComponentReferenceBasics::crefSubs(cr.clone())?, ty.clone())?;
                e = Expression::makeCrefExp(cr.clone(), ty.clone())?;
                lhsExpLstRes = metamodelica::cons(e.clone(), lhsExpLstRes.clone());
                rhsExpLstRes = metamodelica::cons(inExp.clone(), rhsExpLstRes.clone());
            } else {
                e = inExp.clone();
                (eqCrefs, _) = List::deleteMemberOnTrue(cr.clone(), eqCrefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?;
            }
            e.clone()
        },
        _ => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("SimCodeUtil.createTmpCrefExpsForComplexEqnSys_work: fail for")); __mm_s.push_str(&*ExpressionBasics::printExpStr(inExp.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
    });
    Ok((eqCrefs, outCrefExp, lhsExpLstRes, rhsExpLstRes, lhsExpLstAss, rhsExpLstAss))
}

fn createArrayTempVar(mut name: Arc<DAE::ComponentRef>, mut dims: Arc<metamodelica::List<i32>>, mut inTmpCrefsLst: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    otempvars = (::match_deref::match_deref! { match &(inTmpCrefsLst.clone()) {
        Deref @ metamodelica::List::Nil => {
            itempvars.clone()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::CREF { componentRef: cr, ty }, tail: rest } => {
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut var: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut slst: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            slst = List::map(dims.clone(), (std::sync::Arc::new(fnptr!(intString, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))?;
            if FMI::isFMIVersion20((FMI::getFMIVersionString()?).clone())? {
                var = SimCodeVar::SimVar { name: cr.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: 0, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: ty.clone(), isDiscrete: false, arrayCref: Some(name.clone()), aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL), variable_index: None, fmi_index: None, numArrayElement: slst.clone(), isValueChangeable: false, isProtected: true, hideResult: Some(true), isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(cr.clone()), relativeQuantity: false };
            } else {
                var = SimCodeVar::SimVar { name: cr.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: 0, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: ty.clone(), isDiscrete: false, arrayCref: Some(name.clone()), aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::NONECAUS), variable_index: None, fmi_index: None, numArrayElement: slst.clone(), isValueChangeable: false, isProtected: true, hideResult: Some(true), isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(cr.clone()), relativeQuantity: false };
            }
            tempvars = createTempVarsforCrefs(rest.clone(), list![var.clone()])?;
            List::append_reverse(tempvars.clone(), itempvars.clone())
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(otempvars)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn createTempVarsforCrefs(mut inTmpCrefsLst: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    otempvars = (::match_deref::match_deref! { match &(inTmpCrefsLst.clone()) {
        Deref @ metamodelica::List::Nil => {
            itempvars.clone()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::ARRAY { array: expl, .. }, tail: rest } => {
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            tempvars = createTempVarsforCrefs(expl.clone(), itempvars.clone())?;
            createTempVarsforCrefs(rest.clone(), tempvars.clone())?
        },
        Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::TUPLE { PR: expl }, tail: rest } => {
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            tempvars = createTempVarsforCrefs(expl.clone(), itempvars.clone())?;
            createTempVarsforCrefs(rest.clone(), tempvars.clone())?
        },
        Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::CREF { componentRef: cr, ty }, tail: rest } => {
            let mut var: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut arrayCref: Option<Arc<DAE::ComponentRef>> = None;
            let mut numArrayElement: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            let mut inst_dims: Arc<metamodelica::List<Arc<DAE::Dimension>>> = metamodelica::nil();
            arrayCref = ComponentReference::getArrayCref(cr.clone())?;
            inst_dims = ComponentReferenceBasics::crefDims(cr.clone())?;
            numArrayElement = List::map(inst_dims.clone(), (std::sync::Arc::new(ExpressionBasics::dimensionString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Dimension>) -> Result<ArcStr> + 'static>))?;
            if FMI::isFMIVersion20((FMI::getFMIVersionString()?).clone())? {
                var = SimCodeVar::SimVar { name: cr.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: 0, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: ty.clone(), isDiscrete: false, arrayCref: arrayCref.clone(), aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: false, isProtected: true, hideResult: Some(true), isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(cr.clone()), relativeQuantity: false };
            } else {
                var = SimCodeVar::SimVar { name: cr.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: 0, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: ty.clone(), isDiscrete: false, arrayCref: arrayCref.clone(), aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::NONECAUS), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: false, isProtected: true, hideResult: Some(true), isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(cr.clone()), relativeQuantity: false };
            }
            createTempVarsforCrefs(rest.clone(), metamodelica::cons(var.clone(), itempvars.clone()))?
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(otempvars)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn createTempVars(mut varLst: Arc<metamodelica::List<Arc<DAE::Var>>>, mut inCrefPrefix: Arc<DAE::ComponentRef>, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    otempvars = (::match_deref::match_deref! { match &(varLst.clone()) {
        Deref @ metamodelica::List::Nil => {
            itempvars.clone()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ DAE::Var { ty: ty @ Deref @ DAE::Type::T_COMPLEX { complexClassType: ClassInf::State::RECORD { path: _ }, .. }, name, .. }, tail: rest } => {
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            cr = ComponentReference::crefPrependIdent(inCrefPrefix.clone(), (name.clone()).clone(), metamodelica::nil(), ty.clone())?;
            createTempVars(rest.clone(), cr.clone(), itempvars.clone())?
        },
        Deref @ metamodelica::List::Cons { head: Deref @ DAE::Var { ty, name, .. }, tail: rest } => {
            let mut ttmpvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut arraycref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut numArrayElement: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            let mut crlst: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut var: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut ty = (*ty).clone();
            cr = ComponentReference::crefPrependIdent(inCrefPrefix.clone(), (name.clone()).clone(), metamodelica::nil(), ty.clone())?;
            let (__pa0, __pa1) = ::match_deref::match_deref! { match &(ComponentReference::expandCref(cr.clone(), true)?) {
                Deref @ metamodelica::List::Cons { head: __pa0, tail: __pa1 } => (__pa0.clone(), __pa1.clone()),
                _ => bail!("pattern mismatch"),
            } };
            cr = __pa0.clone();
            crlst = __pa1.clone();
            arraycref = ComponentReference::crefStripSubs(cr.clone())?;
            numArrayElement = List::map(ComponentReferenceBasics::crefDims(cr.clone())?, (std::sync::Arc::new(ExpressionBasics::dimensionString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Dimension>) -> Result<ArcStr> + 'static>))?;
            ty = ComponentReference::crefTypeFull(cr.clone())?;
            if FMI::isFMIVersion20((FMI::getFMIVersionString()?).clone())? {
                var = SimCodeVar::SimVar { name: cr.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: 0, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: ty.clone(), isDiscrete: false, arrayCref: Some(arraycref.clone()), aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: false, isProtected: true, hideResult: Some(true), isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(cr.clone()), relativeQuantity: false };
            } else {
                var = SimCodeVar::SimVar { name: cr.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: 0, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: ty.clone(), isDiscrete: false, arrayCref: Some(arraycref.clone()), aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::NONECAUS), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: false, isProtected: true, hideResult: Some(true), isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(cr.clone()), relativeQuantity: false };
            }
            ttmpvars = list![var.clone()];
            for mut cr in &*crlst.clone() {
                let mut cr = cr.clone();
                ty = ComponentReference::crefTypeFull(cr.clone())?;
                if FMI::isFMIVersion20((FMI::getFMIVersionString()?).clone())? {
                    var = SimCodeVar::SimVar { name: cr.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: 0, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: ty.clone(), isDiscrete: false, arrayCref: None, aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: false, isProtected: true, hideResult: Some(true), isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(cr.clone()), relativeQuantity: false };
                } else {
                    var = SimCodeVar::SimVar { name: cr.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: 0, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: ty.clone(), isDiscrete: false, arrayCref: None, aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::NONECAUS), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: false, isProtected: true, hideResult: Some(true), isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(cr.clone()), relativeQuantity: false };
                }
                ttmpvars = metamodelica::cons(var.clone(), ttmpvars.clone());
            }
            ttmpvars = Dangerous::listReverseInPlace(ttmpvars.clone());
            ttmpvars = listAppend(itempvars.clone(), ttmpvars.clone());
            createTempVars(rest.clone(), inCrefPrefix.clone(), ttmpvars.clone())?
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(otempvars)
}

pub fn createNonlinearResidualEquations(mut eqs: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, mut idx_tpl: (i32, i32), mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut funcTree: Arc<AvlTreePathFunction::Tree>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, (i32, i32), Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut eqSystems: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut idx_tpl: (i32, i32) = idx_tpl;
    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tempvars;
    let mut eq_idx: i32 = 0;
    let mut res_idx: i32 = 0;
    match '__try0: {
        for mut eq in &*eqs.clone().reverse() {
            let mut eq = eq.clone();
            (eq_idx, res_idx) = idx_tpl.clone();
            eqSystems = (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ BackendDAE::Equation::EQUATION { attr: eqAttr, source, scalar: e2, exp: e1 } => {
            let mut res_exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            res_exp = unwrap_break_err!(Expression::createResidualExp(e1.clone(), e2.clone()), '__try0);
            res_exp = unwrap_break_err!(Expression::replaceDerOpInExp(res_exp.clone()), '__try0);
            idx_tpl = (eq_idx.clone() + 1, res_idx.clone() + 1);
            metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_RESIDUAL { index: eq_idx.clone(), res_index: res_idx.clone(), exp: res_exp.clone(), source: source.clone(), eqAttr: eqAttr.clone() }), eqSystems.clone())
        },
        Deref @ BackendDAE::Equation::RESIDUAL_EQUATION { attr: eqAttr, source, exp: e } => {
            let mut res_exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (res_exp, _) = unwrap_break_err!(ExpressionSimplify::simplify(e.clone()), '__try0);
            res_exp = unwrap_break_err!(Expression::replaceDerOpInExp(res_exp.clone()), '__try0);
            idx_tpl = (eq_idx.clone() + 1, res_idx.clone() + 1);
            metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_RESIDUAL { index: eq_idx.clone(), res_index: res_idx.clone(), exp: res_exp.clone(), source: source.clone(), eqAttr: eqAttr.clone() }), eqSystems.clone())
        },
        ifEq @ Deref @ BackendDAE::Equation::IF_EQUATION { .. } => {
            let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut resEqs: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            resEqs = unwrap_break_err!(BackendEquation::equationToScalarResidualForm(ifEq.clone(), funcTree.clone()), '__try0);
            (eqSystlst, idx_tpl, tempvars) = unwrap_break_err!(createNonlinearResidualEquations(resEqs.clone(), (eq_idx.clone() - 1, res_idx.clone()), tempvars.clone(), funcTree.clone()), '__try0);
            eqSystlst.clone()
        },
        Deref @ BackendDAE::Equation::ARRAY_EQUATION { attr: eqAttr, source, right: e2, left: e1, dimSize: ds, .. } => {
            let mut res_exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut lhse: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut explst1: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut left: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut crefstmp: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut ty: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            ty = unwrap_break_err!(Expression::r#typeof(e1.clone()), '__try0);
            left = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*intString(eq_idx.clone())); ArcStr::from(__mm_s) }).clone(), ty.clone(), metamodelica::nil());
            lhse = Arc::new(DAE::Exp::CREF { componentRef: left.clone(), ty: ty.clone() });
            res_exp = unwrap_break_err!(Expression::createResidualExp(e1.clone(), e2.clone()), '__try0);
            res_exp = unwrap_break_err!(Expression::replaceDerOpInExp(res_exp.clone()), '__try0);
            crefstmp = unwrap_break_err!(ComponentReference::expandCref(left.clone(), false), '__try0);
            explst1 = unwrap_break_err!(List::map(crefstmp.clone(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>)), '__try0);
            let (__pa0, (__pa1, __pa2)) = unwrap_break_err!(List::map2Fold(explst1.clone(), (std::sync::Arc::new(fnptr!(makeSES_RESIDUAL, Arc<DAE::Exp>, Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32))) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil()), '__try0);
            eqSystlst = __pa0.clone();
            eq_idx = __pa1.clone();
            res_idx = __pa2.clone();
            eqSystlst = metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: eq_idx.clone(), lhs: lhse.clone(), exp: res_exp.clone(), source: source.clone(), eqAttr: eqAttr.clone() }), eqSystlst.clone());
            tempvars = unwrap_break_err!(createArrayTempVar(left.clone(), ds.clone(), explst1.clone(), tempvars.clone()), '__try0);
            idx_tpl = (eq_idx.clone() + 1, res_idx.clone());
            listAppend(eqSystlst.clone(), eqSystems.clone())
        },
        Deref @ BackendDAE::Equation::COMPLEX_EQUATION { attr: eqAttr, source, right: e2, left: e1, .. } => {
            let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut e2 = (*e2).clone();
            let mut e1 = (*e1).clone();
            (e1, _) = unwrap_break_err!(ExpressionSimplify::simplify(e1.clone()), '__try0);
            e1 = unwrap_break_err!(Expression::replaceDerOpInExp(e1.clone()), '__try0);
            (e2, _) = unwrap_break_err!(ExpressionSimplify::simplify(e2.clone()), '__try0);
            e2 = unwrap_break_err!(Expression::replaceDerOpInExp(e2.clone()), '__try0);
            (eqSystlst, idx_tpl, tempvars) = unwrap_break_err!(createNonlinearResidualEquationsComplex(e1.clone(), e2.clone(), source.clone(), eqAttr.clone(), idx_tpl.clone(), tempvars.clone()), '__try0);
            listAppend(eqSystlst.clone(), eqSystems.clone())
        },
        Deref @ BackendDAE::Equation::WHEN_EQUATION { whenEquation: Deref @ BackendDAE::WhenEquation { .. }, .. } => {
            unwrap_break_err!(Error::addSourceMessage(Error::UNSUPPORTED_LANGUAGE_FEATURE.clone(), list![(literal!("non-linear equations within when-equations")).clone(), (literal!("Perform non-linear operations outside the when-equation (this is slower, but works)")).clone()], unwrap_break_err!(BackendEquation::equationInfo(eq.clone()), '__try0)), '__try0);
            break '__try0 Err::<_, _>(anyhow::anyhow!("fail"))
        },
        Deref @ BackendDAE::Equation::ALGORITHM { attr: eqAttr, expand: crefExpand, source, alg: Deref @ DAE::Algorithm { statementLst: algStatements }, .. } => {
            let mut explst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut explst1: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
            let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut crefstmp: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut repl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
            let mut algStatements = (*algStatements).clone();
            crefs = unwrap_break_err!(CheckModel::checkAndGetAlgorithmOutputs(Arc::new(DAE::Algorithm { statementLst: algStatements.clone() }), source.clone(), crefExpand.clone()), '__try0);
            (crefstmp, repl) = unwrap_break_err!(createTmpCrefs(crefs.clone(), eq_idx.clone(), metamodelica::nil(), BackendVarTransform::emptyReplacements()), '__try0);
            explst = unwrap_break_err!(List::map(crefs.clone(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>)), '__try0);
            explst = unwrap_break_err!(List::map(explst.clone(), (std::sync::Arc::new(Expression::replaceDerOpInExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<Arc<DAE::Exp>> + 'static>)), '__try0);
            (algStatements, _) = unwrap_break_err!(BackendVarTransform::replaceStatementLst(algStatements.clone(), repl.clone(), Some((std::sync::Arc::new(fnptr!(BackendVarTransform::skipPreOperator, Arc<DAE::Exp>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<bool> + 'static>)), metamodelica::nil(), false), '__try0);
            explst1 = unwrap_break_err!(List::map(crefstmp.clone(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>)), '__try0);
            explst1 = unwrap_break_err!(List::map(explst1.clone(), (std::sync::Arc::new(Expression::replaceDerOpInExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<Arc<DAE::Exp>> + 'static>)), '__try0);
            tempvars = unwrap_break_err!(createTempVarsforCrefs(explst1.clone(), tempvars.clone()), '__try0);
            exptl = List::zip(explst.clone(), explst1.clone());
            let (__pa0, (__pa1, __pa2)) = unwrap_break_err!(List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_RESIDUAL1) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> + 'static>), source.clone(), eqAttr.clone(), (eq_idx.clone(), res_idx.clone()), metamodelica::nil()), '__try0);
            eqSystlst = __pa0.clone();
            eq_idx = __pa1.clone();
            res_idx = __pa2.clone();
            eqSystlst = metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: eq_idx.clone(), statements: algStatements.clone(), eqAttr: eqAttr.clone() }), eqSystlst.clone());
            idx_tpl = (eq_idx.clone() + 1, res_idx.clone());
            listAppend(eqSystlst.clone(), eqSystems.clone())
        },
        _ => bail!("match: no arm matched"),
    } });
        }
        Ok::<(), anyhow::Error>(())
    } {
        Ok(()) => {}
        Err(__try0_err) => {
            Error::addMessage(Error::INTERNAL_ERROR.clone(), list![(literal!("function createNonlinearResidualEquations failed")).clone()])?;
            return Err(__try0_err);
        }
    }
    Ok((eqSystems, idx_tpl, tempvars))
}

pub fn fixNonlinearResidualIndices(mut inSysts: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> {
    let mut outSysts: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut index: i32 = 0;
    for mut syst in &*inSysts.clone() {
        let mut syst = syst.clone();
        syst = (::match_deref::match_deref! { match &(syst.clone()) {
        Deref @ SimCode::SimEqSystem::SES_RESIDUAL { .. } => {
            assign_variant_field!(syst => SimCode::SimEqSystem::SES_RESIDUAL; res_index = index.clone());
            index = index.clone() + 1;
            syst.clone()
        },
        _ => syst.clone(),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
        outSysts = metamodelica::cons(syst.clone(), outSysts.clone());
    }
    outSysts = outSysts.clone().reverse();
    outSysts
}

pub fn dimsToAllIndexes(mut inDims: Arc<metamodelica::List<Arc<DAE::Dimension>>>) -> Result<Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>> {
    let mut outIndexes: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    let mut ilst: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut lstlst: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    ilst = Expression::dimensionsSizes(inDims.clone())?;
    lstlst = List::map(ilst.clone(), (std::sync::Arc::new(fnptr!(List::intRange, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<Arc<metamodelica::List<i32>>> + 'static>))?;
    outIndexes = dimsToAllIndexes1(lstlst.clone())?;
    Ok(outIndexes)
}

fn dimsToAllIndexes1(mut inDims: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>) -> Result<Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>> {
    let mut oAllIndex: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    oAllIndex = (::match_deref::match_deref! { match &(inDims.clone()) {
        Deref @ metamodelica::List::Cons { head: dims, tail: Deref @ metamodelica::List::Nil } => {
            let mut indxes: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
            indxes = List::map(dims.clone(), std::sync::Arc::new(fnptr!(List::create, _)))?;
            indxes.clone()
        },
        Deref @ metamodelica::List::Cons { head: dims, tail: rest } => {
            let mut indxes: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
            indxes = dimsToAllIndexes1(rest.clone())?;
            indxes = List::fold1(dims.clone(), (std::sync::Arc::new(dimsToAllIndexes2) as std::sync::Arc<dyn ::std::ops::Fn(i32, Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>, Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>) -> Result<Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>> + 'static>), indxes.clone(), metamodelica::nil())?;
            indxes.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(oAllIndex)
}

fn dimsToAllIndexes2(mut i: i32, mut iIndex: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>, mut iAllIndex: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>) -> Result<Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>> {
    let mut oAllIndex: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    oAllIndex = List::map1(iIndex.clone(), std::sync::Arc::new(fnptr!(List::consr, _, _)), i.clone())?;
    oAllIndex = listAppend(iAllIndex.clone(), oAllIndex.clone());
    Ok(oAllIndex)
}

fn createTmpCrefs(mut inCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut iuniqueEqIndex: i32, mut inCrefsAcc: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut iRepl: BackendVarTransform::VariableReplacements) -> Result<(Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, BackendVarTransform::VariableReplacements)> {
    let mut outCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut oRepl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
    (outCrefs, oRepl) = (::match_deref::match_deref! { match &(inCrefs.clone()) {
        Deref @ metamodelica::List::Nil => {
            (inCrefsAcc.clone().reverse(), iRepl.clone())
        },
        Deref @ metamodelica::List::Cons { head: cref, tail: rest } => {
            let mut crtmp: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut result: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut ident: ArcStr = arcstr::literal!("");
            let mut repl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
            ident = (ComponentReferenceBasics::printComponentRefStr(cref.clone())?).clone();
            tp = Types::arrayElementType(ComponentReference::crefLastType(cref.clone())?);
            crtmp = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*literal!("_")); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
            repl = BackendVarTransform::addReplacement(iRepl.clone(), cref.clone(), Arc::new(DAE::Exp::CREF { componentRef: crtmp.clone(), ty: tp.clone() }), Some((std::sync::Arc::new(fnptr!(BackendVarTransform::skipPreOperator, Arc<DAE::Exp>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<bool> + 'static>)))?;
            (result, repl) = createTmpCrefs(rest.clone(), iuniqueEqIndex.clone(), metamodelica::cons(crtmp.clone(), inCrefsAcc.clone()), repl.clone())?;
            (result.clone(), repl.clone())
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((outCrefs, oRepl))
}

fn makeSES_RESIDUAL(mut inExp: Arc<DAE::Exp>, mut source: Arc<DAE::ElementSource>, mut eqAttr: BackendDAE::EquationAttributes, mut idx_tpl: (i32, i32)) -> (Arc<SimCode::SimEqSystem>, (i32, i32)) {
    let mut outSimEqn: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut idx_tpl: (i32, i32) = idx_tpl;
    let mut eq_idx: i32 = 0;
    let mut res_idx: i32 = 0;
    (eq_idx, res_idx) = idx_tpl.clone();
    outSimEqn = Arc::new(SimCode::SimEqSystem::SES_RESIDUAL { index: eq_idx.clone(), res_index: res_idx.clone(), exp: inExp.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
    idx_tpl = (eq_idx.clone() + 1, res_idx.clone() + 1);
    (outSimEqn, idx_tpl)
}

fn makeSES_RESIDUAL1(mut inTpl: (Arc<DAE::Exp>, Arc<DAE::Exp>), mut source: Arc<DAE::ElementSource>, mut eqAttr: BackendDAE::EquationAttributes, mut idx_tpl: (i32, i32)) -> Result<(Arc<SimCode::SimEqSystem>, (i32, i32))> {
    let mut outSimEqn: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut idx_tpl: (i32, i32) = idx_tpl;
    let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut eq_idx: i32 = 0;
    let mut res_idx: i32 = 0;
    (e1, e2) = inTpl.clone();
    (eq_idx, res_idx) = idx_tpl.clone();
    e = Expression::createResidualExp(e1.clone(), e2.clone())?;
    outSimEqn = Arc::new(SimCode::SimEqSystem::SES_RESIDUAL { index: eq_idx.clone(), res_index: res_idx.clone(), exp: e.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
    idx_tpl = (eq_idx.clone() + 1, res_idx.clone() + 1);
    Ok((outSimEqn, idx_tpl))
}

fn makeSES_SIMPLE_ASSIGN(mut inTpl: (Arc<DAE::Exp>, Arc<DAE::Exp>), mut source: Arc<DAE::ElementSource>, mut eqAttr: BackendDAE::EquationAttributes, mut iuniqueEqIndex: i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> {
    let mut outSimEqn: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut ouniqueEqIndex: i32 = 0;
    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut msg: ArcStr = arcstr::literal!("");
    (cr, e) = (::match_deref::match_deref! { match &(inTpl.clone()) {
        (Deref @ DAE::Exp::CREF { componentRef: cr, ty: _ }, e) => (cr.clone(), e.clone()),
        (Deref @ DAE::Exp::UNARY { operator: DAE::Operator::UMINUS { ty: _ }, exp: Deref @ DAE::Exp::CREF { componentRef: cr, ty: _ } }, e) => (cr.clone(), Expression::negate(e.clone())?),
        _ => {
            msg = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("SimCodeUtil.makeSES_SIMPLE_ASSIGN failed for: ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(Util::tuple21(inTpl.clone()))?); __mm_s.push_str(&*literal!(" = ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(Util::tuple22(inTpl.clone()))?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
            Error::addCompilerWarning((msg.clone()).clone())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    outSimEqn = Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: iuniqueEqIndex.clone(), cref: cr.clone(), exp: e.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
    ouniqueEqIndex = iuniqueEqIndex.clone() + 1;
    Ok((outSimEqn, ouniqueEqIndex))
}

fn makeSES_SIMPLE_ASSIGNwithArray(mut inTpl: (Arc<DAE::Exp>, Arc<DAE::Exp>), mut source: Arc<DAE::ElementSource>, mut eqAttr: BackendDAE::EquationAttributes, mut iuniqueEqIndex: i32) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32)> {
    let mut outSimEqn: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = iuniqueEqIndex.clone();
    let mut eqn: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    (outSimEqn, ouniqueEqIndex) = (::match_deref::match_deref! { match &(inTpl.clone()) {
        (left @ Deref @ DAE::Exp::ARRAY { .. }, right @ Deref @ DAE::Exp::CREF { .. }) => {
            let mut elems: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            if '__try0: {
                let __pa1 = ::match_deref::match_deref! { match &(unwrap_break_err!(Static::crefVectorize(true, right.clone(), var_field!((**right).ty, DAE::Exp::CREF).clone(), None, Types::arrayElementType(var_field!((**right).ty, DAE::Exp::CREF).clone())), '__try0)) {
                    Deref @ DAE::Exp::ARRAY { array: __pa1, .. } => __pa1.clone(),
                    _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
                } };
                elems = __pa1.clone();
                for mut tpl in &*List::zip(var_field!((**left).array, DAE::Exp::ARRAY).clone(), elems.clone()) {
                    let mut tpl = tpl.clone();
                    (eqn, ouniqueEqIndex) = unwrap_break_err!(makeSES_SIMPLE_ASSIGN(tpl.clone(), source.clone(), eqAttr.clone(), ouniqueEqIndex.clone()), '__try0);
                    outSimEqn = metamodelica::cons(eqn.clone(), outSimEqn.clone());
                }
                outSimEqn = outSimEqn.clone().reverse();
                Ok::<(), anyhow::Error>(())
            }.is_err() {
                Error::assertion(false, ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("SimCodeUtil.makeSES_SIMPLE_ASSIGNwithArray")); __mm_s.push_str(&*literal!(" failed because expression ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(right.clone())?); __mm_s.push_str(&*literal!(" could not be scalarized.")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
            }
            (outSimEqn.clone(), ouniqueEqIndex.clone())
        },
        _ => {
            (eqn, ouniqueEqIndex) = makeSES_SIMPLE_ASSIGN(inTpl.clone(), source.clone(), eqAttr.clone(), ouniqueEqIndex.clone())?;
            (list![eqn.clone()], ouniqueEqIndex.clone())
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((outSimEqn, ouniqueEqIndex))
}

fn makeSolved(mut eq: Arc<BackendDAE::Equation>, mut uniqueEqIndex: i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> {
    let mut outSimEqn: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut uniqueEqIndex: i32 = uniqueEqIndex;
    outSimEqn = (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ BackendDAE::Equation::SOLVED_EQUATION { .. } => {
            Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: uniqueEqIndex.clone(), cref: var_field!((*eq).componentRef, BackendDAE::Equation::SOLVED_EQUATION).clone(), exp: var_field!((*eq).exp, BackendDAE::Equation::SOLVED_EQUATION).clone(), source: var_field!((*eq).source, BackendDAE::Equation::SOLVED_EQUATION).clone(), eqAttr: var_field!((*eq).attr, BackendDAE::Equation::SOLVED_EQUATION).clone() })
        },
        Deref @ BackendDAE::Equation::IF_EQUATION { .. } => {
            let mut ifbranches: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)>> = metamodelica::nil();
            let mut elsebranch: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            (ifbranches, uniqueEqIndex) = List::threadMapFold(var_field!((*eq).conditions, BackendDAE::Equation::IF_EQUATION).clone(), var_field!((*eq).eqnstrue, BackendDAE::Equation::IF_EQUATION).clone(), (std::sync::Arc::new(makeSolvedIfBranch) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, i32) -> Result<((Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>), i32)> + 'static>), uniqueEqIndex.clone())?;
            (elsebranch, uniqueEqIndex) = List::mapFold(var_field!((*eq).eqnsfalse, BackendDAE::Equation::IF_EQUATION).clone(), (std::sync::Arc::new(makeSolved) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone())?;
            Arc::new(SimCode::SimEqSystem::SES_IFEQUATION { index: uniqueEqIndex.clone(), ifbranches: ifbranches.clone(), elsebranch: elsebranch.clone(), source: var_field!((*eq).source, BackendDAE::Equation::IF_EQUATION).clone(), eqAttr: var_field!((*eq).attr, BackendDAE::Equation::IF_EQUATION).clone() })
        },
        Deref @ BackendDAE::Equation::ALGORITHM { alg: Deref @ DAE::Algorithm { statementLst: stmts }, .. } => {
            Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: uniqueEqIndex.clone(), statements: stmts.clone(), eqAttr: var_field!((*eq).attr, BackendDAE::Equation::ALGORITHM).clone() })
        },
        _ => {
            Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("SimCodeUtil.makeSolved")); __mm_s.push_str(&*literal!(" failed!")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    uniqueEqIndex = uniqueEqIndex.clone() + 1;
    Ok((outSimEqn, uniqueEqIndex))
}

fn makeSolvedIfBranch(mut cond: Arc<DAE::Exp>, mut ifbranch: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, mut uniqueEqIndex: i32) -> Result<((Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>), i32)> {
    let mut result: (Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (Arc::new(<DAE::Exp as ::std::default::Default>::default()), metamodelica::nil());
    let mut uniqueEqIndex: i32 = uniqueEqIndex;
    let mut lst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    (lst, uniqueEqIndex) = List::mapFold(ifbranch.clone(), (std::sync::Arc::new(makeSolved) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone())?;
    result = (cond.clone(), lst.clone());
    Ok((result, uniqueEqIndex))
}

fn createOdeSystem(mut genDiscrete: bool, mut skipDiscInAlgorithm: bool, mut isyst: Arc<BackendDAE::EqSystem>, mut ishared: Arc<BackendDAE::Shared>, mut inComp: Arc<BackendDAE::StrongComponent>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut isccIndex: i32, mut ieqSccMapping: Arc<metamodelica::List<(i32, i32)>>, mut iBackendMapping: SimCode::BackendMapping) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(i32, i32)>>, SimCode::BackendMapping)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut noDiscequations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut oeqSccMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut oBackendMapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    (equations_, noDiscequations_, ouniqueEqIndex, otempvars, oeqSccMapping, oBackendMapping) = 'mc: {
        let __mc_input = (isyst.clone(), ishared.clone(), inComp.clone());
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, Deref @ BackendDAE::Shared { info: ei, functionTree: funcs, globalKnownVars, .. }, comp @ Deref @ BackendDAE::StrongComponent::EQUATIONSYSTEM { mixedSystem, jacType: jac_tp, jac: jacobian, eqns: eqIdcs, .. }) => {
                    let mut eqn_lst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut var_lst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
                    let mut var_lst_1: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
                    let mut vars_1: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut eqns_1: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut uniqueEqIndexMapping: i32 = 0;
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut tmpEqSccMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
                    let mut tmpBackendMapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
                    let mut partOfJac: bool = false;
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        Debug::trace((literal!("function createOdeSystem create continuous system.\n")).clone())?;
                    }
                    if Flags::isSet(Flags::GRAPHML.clone())? {
                        BackendDump::dumpBipartiteGraphStrongComponent1(inComp.clone(), BackendEquation::equationList(eqns.clone())?, BackendVariable::varList(vars.clone())?, Some(BackendDAEUtil::getFunctions(ishared.clone())?), ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("BIPARITEGRPAH_LS_")); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone())?;
                    }
                    (eqn_lst, var_lst, _) = BackendDAETransform::getEquationAndSolvedVar(comp.clone(), eqns.clone(), vars.clone())?;
                    eqn_lst = BackendEquation::replaceDerOpInEquationList(eqn_lst.clone())?;
                    var_lst_1 = List::map(var_lst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
                    vars_1 = BackendVariable::listVar1(var_lst_1.clone())?;
                    eqns_1 = BackendEquation::listEquation(eqn_lst.clone())?;
                    partOfJac = BackendDAEUtil::isJacobianDAE(ishared.clone());
                    (equations_, uniqueEqIndex, tempvars) = createOdeSystem2(false, vars_1.clone(), globalKnownVars.clone(), eqns_1.clone(), jacobian.clone(), jac_tp.clone(), funcs.clone(), vars.clone(), iuniqueEqIndex.clone(), ei.clone(), mixedSystem.clone(), partOfJac.clone(), itempvars.clone(), partitionKindToClockIndex(isyst.partitionKind.clone()))?;
                    uniqueEqIndexMapping = uniqueEqIndex.clone() - 1;
                    tmpEqSccMapping = appendSccIdxRange(uniqueEqIndexMapping.clone(), uniqueEqIndex.clone() - 1, isccIndex.clone(), ieqSccMapping.clone());
                    tmpBackendMapping = setEqMapping(List::intRange2(uniqueEqIndexMapping.clone(), uniqueEqIndex.clone() - 1), eqIdcs.clone(), iBackendMapping.clone())?;
                    Ok(((equations_.clone(), equations_.clone(), uniqueEqIndex.clone(), tempvars.clone(), tmpEqSccMapping.clone(), tmpBackendMapping.clone()), equations_.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, _, Deref @ BackendDAE::StrongComponent::TORNSYSTEM { mixedSystem, linear: b, casualTearingSet, strictTearingSet }) => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut tmpEqSccMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
                    let mut tmpBackendMapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    if Flags::isSet(Flags::GRAPHML.clone())? {
                        BackendDump::dumpBipartiteGraphStrongComponent1(inComp.clone(), BackendEquation::equationList(eqns.clone())?, BackendVariable::varList(vars.clone())?, Some(BackendDAEUtil::getFunctions(ishared.clone())?), ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("BIPARITEGRPAH_TS_")); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone())?;
                    }
                    (equations_, uniqueEqIndex, tempvars) = createTornSystem(b.clone(), skipDiscInAlgorithm.clone(), genDiscrete.clone(), strictTearingSet.clone(), casualTearingSet.clone(), isyst.clone(), ishared.clone(), iuniqueEqIndex.clone(), mixedSystem.clone(), itempvars.clone())?;
                    tmpEqSccMapping = appendSccIdx(uniqueEqIndex.clone() - 1, isccIndex.clone(), ieqSccMapping.clone());
                    tmpBackendMapping = iBackendMapping.clone();
                    Ok(((equations_.clone(), equations_.clone(), uniqueEqIndex.clone(), tempvars.clone(), tmpEqSccMapping.clone(), tmpBackendMapping.clone()), equations_.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut msg: ArcStr = arcstr::literal!("");
                    msg = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("function createOdeSystem failed for component ")); __mm_s.push_str(&*BackendDump::strongComponentString(inComp.clone())?); ArcStr::from(__mm_s) }).clone();
                    Error::addInternalError((msg.clone()).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, noDiscequations_, ouniqueEqIndex, otempvars, oeqSccMapping, oBackendMapping))
}

fn createOdeSystem2(mut mixedEvent: bool, mut inVars: BackendDAE::Variables, mut inKnVars: BackendDAE::Variables, mut inEquationArray: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>>, mut inJacobian: Arc<BackendDAE::Jacobian>, mut inJacobianType: BackendDAE::JacobianType, mut inFuncs: Arc<AvlTreePathFunction::Tree>, mut inAllVars: BackendDAE::Variables, mut iuniqueEqIndex: i32, mut iei: BackendDAE::ExtraInfo, mut mixedSystem: bool, mut partOfJac: bool, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut clockIndex: Option<i32>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equations_, ouniqueEqIndex, otempvars) = 'mc: {
        let __mc_input = (inJacobian.clone(), inJacobianType.clone());
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Jacobian::FULL_JACOBIAN { jacobian: Some(jac) }, BackendDAE::JacobianType::JAC_CONSTANT { .. }) => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut simVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut beqs: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut linInfo: i32 = 0;
                    let mut jacVals: Arc<metamodelica::List<Arc<metamodelica::List<metamodelica::Real>>>> = metamodelica::nil();
                    let mut rhsVals: Arc<metamodelica::List<metamodelica::Real>> = metamodelica::nil();
                    let mut solvedVals: Arc<metamodelica::List<metamodelica::Real>> = metamodelica::nil();
                    let mut sources: Arc<metamodelica::List<Arc<DAE::ElementSource>>> = metamodelica::nil();
                    let mut names: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        Debug::trace((literal!("function createOdeSystem2 create linear system(const jacobian).\n")).clone())?;
                    }
                    (simVars, _) = BackendVariable::traverseBackendDAEVars(inVars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), inKnVars.clone()))?;
                    simVars = simVars.clone().reverse();
                    (beqs, sources) = BackendDAEUtil::getEqnSysRhs(inEquationArray.clone(), inVars.clone(), Some(inFuncs.clone()))?;
                    beqs = beqs.clone().reverse();
                    rhsVals = ValuesUtil::valueReals(List::map(beqs.clone(), (std::sync::Arc::new(Ceval::cevalSimple) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<Arc<Values::Value>> + 'static>))?)?;
                    jacVals = SymbolicJacobian::evaluateConstantJacobian((simVars.clone().len() as i32), jac.clone())?;
                    (solvedVals, linInfo) = System::dgesv(jacVals.clone(), rhsVals.clone())?;
                    names = ({
        let mut __acc: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
        for mut v in (simVars.clone()).into_iter().cloned() {
                    let __x = v.name.clone();
                    __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                    checkLinearSystem(linInfo.clone(), names.clone(), jacVals.clone(), rhsVals.clone())?;
                    sources = List::map1(sources.clone(), (std::sync::Arc::new(ElementSource::addSymbolicTransformation) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ElementSource>, Arc<DAE::SymbolicOperation>) -> Result<Arc<DAE::ElementSource>> + 'static>), Arc::new(DAE::SymbolicOperation::LINEAR_SOLVED { vars: names.clone(), jac: jacVals.clone(), rhs: rhsVals.clone(), result: solvedVals.clone() }))?;
                    (equations_, uniqueEqIndex) = List::thread3MapFold(simVars.clone(), solvedVals.clone(), sources.clone(), (std::sync::Arc::new(generateSolvedEquation) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, metamodelica::Real, Arc<DAE::ElementSource>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), iuniqueEqIndex.clone())?;
                    Ok(((equations_.clone(), uniqueEqIndex.clone(), itempvars.clone()), equations_.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Jacobian::FULL_JACOBIAN { jacobian: Some(jac) }, BackendDAE::JacobianType::JAC_LINEAR { .. }) => {
                    let mut simVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut beqs: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut simJac: Arc<metamodelica::List<(i32, i32, Arc<SimCode::SimEqSystem>)>> = metamodelica::nil();
                    let mut sources: Arc<metamodelica::List<Arc<DAE::ElementSource>>> = metamodelica::nil();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        Debug::trace((literal!("function createOdeSystem2 create linear system with jacobian.\n")).clone())?;
                    }
                    (simVars, _) = BackendVariable::traverseBackendDAEVars(inVars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), inKnVars.clone()))?;
                    simVars = simVars.clone().reverse();
                    (beqs, sources) = BackendDAEUtil::getEqnSysRhs(inEquationArray.clone(), inVars.clone(), Some(inFuncs.clone()))?;
                    beqs = beqs.clone().reverse();
                    simJac = List::map1(jac.clone(), (std::sync::Arc::new(jacToSimjac) as std::sync::Arc<dyn ::std::ops::Fn((i32, i32, Arc<BackendDAE::Equation>), BackendDAE::Variables) -> Result<(i32, i32, Arc<SimCode::SimEqSystem>)> + 'static>), inVars.clone())?;
                    if Config::simCodeTarget()? == literal!("Cpp") {
                        simJac = List::sort(simJac.clone(), (std::sync::Arc::new(fnptr!(simJacCSRToCSC, (i32, i32, Arc<SimCode::SimEqSystem>), (i32, i32, Arc<SimCode::SimEqSystem>))) as std::sync::Arc<dyn ::std::ops::Fn((i32, i32, Arc<SimCode::SimEqSystem>), (i32, i32, Arc<SimCode::SimEqSystem>)) -> Result<bool> + 'static>))?;
                    }
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_LINEAR { lSystem: Arc::new(SimCode::LinearSystem { index: iuniqueEqIndex.clone(), partOfMixed: mixedEvent.clone(), tornSystem: false, vars: simVars.clone(), beqs: beqs.clone(), simJac: simJac.clone(), residual: metamodelica::nil(), jacobianMatrix: None, sources: sources.clone(), indexLinearSystem: 0, nUnknowns: inVars.numberOfVars.clone(), partOfJac: partOfJac.clone() }), alternativeTearing: None, eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() })], iuniqueEqIndex.clone() + 1, itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, BackendDAE::JacobianType::JAC_GENERIC { .. }) => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut eqn_lst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut jacobianMatrix: Option<Arc<SimCode::JacobianMatrix>> = None;
                    let mut homotopySupport: bool = false;
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        Debug::trace((literal!("function createOdeSystem2 create non-linear system with jacobian.")).clone())?;
                    }
                    eqn_lst = BackendEquation::equationList(inEquationArray.clone())?;
                    crefs = BackendVariable::getAllCrefFromVariables(inVars.clone())?;
                    let (__pa0, (__pa1, _), __pa2) = createNonlinearResidualEquations(eqn_lst.clone(), (iuniqueEqIndex.clone(), 0), itempvars.clone(), inFuncs.clone())?;
                    resEqs = __pa0.clone();
                    uniqueEqIndex = __pa1.clone();
                    tempvars = __pa2.clone();
                    resEqs = fixNonlinearResidualIndices(resEqs.clone());
                    (jacobianMatrix, uniqueEqIndex, tempvars) = createSymbolicSimulationJacobian(inJacobian.clone(), uniqueEqIndex.clone(), tempvars.clone(), true)?;
                    (_, homotopySupport) = BackendEquation::traverseExpsOfEquationList(eqn_lst.clone(), (std::sync::Arc::new(BackendDAEUtil::containsHomotopyCall) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, bool) -> Result<(Arc<DAE::Exp>, bool)> + 'static>), false)?;
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Arc::new(SimCode::NonlinearSystem { index: uniqueEqIndex.clone(), eqs: resEqs.clone(), crefs: crefs.clone(), indexNonLinearSystem: 0, nUnknowns: inVars.numberOfVars.clone() + (tempvars.clone().len() as i32) - (itempvars.clone().len() as i32), jacobianMatrix: jacobianMatrix.clone(), homotopySupport: homotopySupport.clone(), mixedSystem: mixedSystem.clone(), tornSystem: false, clockIndex: clockIndex.clone() }), alternativeTearing: None, eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() })], uniqueEqIndex.clone() + 1, tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, _) => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut eqn_lst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut homotopySupport: bool = false;
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        Debug::trace((literal!("function createOdeSystem2 create non-linear system without jacobian.")).clone())?;
                    }
                    eqn_lst = BackendEquation::equationList(inEquationArray.clone())?;
                    crefs = BackendVariable::getAllCrefFromVariables(inVars.clone())?;
                    let (__pa0, (__pa1, _), __pa2) = createNonlinearResidualEquations(eqn_lst.clone(), (iuniqueEqIndex.clone(), 0), itempvars.clone(), inFuncs.clone())?;
                    resEqs = __pa0.clone();
                    uniqueEqIndex = __pa1.clone();
                    tempvars = __pa2.clone();
                    resEqs = fixNonlinearResidualIndices(resEqs.clone());
                    (_, homotopySupport) = BackendEquation::traverseExpsOfEquationList(eqn_lst.clone(), (std::sync::Arc::new(BackendDAEUtil::containsHomotopyCall) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, bool) -> Result<(Arc<DAE::Exp>, bool)> + 'static>), false)?;
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Arc::new(SimCode::NonlinearSystem { index: uniqueEqIndex.clone(), eqs: resEqs.clone(), crefs: crefs.clone(), indexNonLinearSystem: 0, nUnknowns: inVars.numberOfVars.clone() + (tempvars.clone().len() as i32) - (itempvars.clone().len() as i32), jacobianMatrix: None, homotopySupport: homotopySupport.clone(), mixedSystem: mixedSystem.clone(), tornSystem: false, clockIndex: clockIndex.clone() }), alternativeTearing: None, eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() })], uniqueEqIndex.clone() + 1, tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut r#str: ArcStr = arcstr::literal!("");
                    r#str = (BackendDump::jacobianTypeStr(inJacobianType.clone())?).clone();
                    r#str = stringAppendList(list![(literal!("createOdeSystem2 failed for ")).clone(), (r#str.clone()).clone()]);
                    Error::addInternalError((r#str.clone()).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, ouniqueEqIndex, otempvars))
}

fn checkLinearSystem(mut info: i32, mut vars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut jac: Arc<metamodelica::List<Arc<metamodelica::List<metamodelica::Real>>>>, mut rhs: Arc<metamodelica::List<metamodelica::Real>>) -> Result<()> {
    let () = 'mc: {
        let __mc_input = info.clone();
        if let Ok(__v) = (|| -> Result<_> {
            let 0 = __mc_input.clone() else { bail!("nomatch") };
            Ok(())
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let _ = __mc_input.clone() else { bail!("nomatch") };
            let mut infoStr: ArcStr = arcstr::literal!("");
            let mut syst: ArcStr = arcstr::literal!("");
            let mut varnames: ArcStr = arcstr::literal!("");
            let mut varname: ArcStr = arcstr::literal!("");
            let mut rhsStr: ArcStr = arcstr::literal!("");
            let mut jacStr: ArcStr = arcstr::literal!("");
            let true = (info.clone() > 0) else { bail!("pattern mismatch") };
            varname = (ComponentReferenceBasics::printComponentRefStr((vars.clone()).get(info.clone())?)?).clone();
            infoStr = (intString(info.clone())).clone();
            varnames = stringDelimitList(List::map(vars.clone(), (std::sync::Arc::new(ComponentReferenceBasics::printComponentRefStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))?, (literal!(" ;\n  ")).clone());
            rhsStr = stringDelimitList(List::map(rhs.clone(), (std::sync::Arc::new(fnptr!(realString, metamodelica::Real)) as std::sync::Arc<dyn ::std::ops::Fn(metamodelica::Real) -> Result<ArcStr> + 'static>))?, (literal!(" ;\n  ")).clone());
            jacStr = stringDelimitList(List::map1(List::mapList(jac.clone(), (std::sync::Arc::new(fnptr!(realString, metamodelica::Real)) as std::sync::Arc<dyn ::std::ops::Fn(metamodelica::Real) -> Result<ArcStr> + 'static>))?, (std::sync::Arc::new(fnptr!(stringDelimitList, Arc<metamodelica::List<ArcStr>>, ArcStr)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<ArcStr>>, ArcStr) -> Result<ArcStr> + 'static>), (literal!(" , ")).clone())?, (literal!(" ;\n  ")).clone());
            syst = stringAppendList(list![(literal!("\n[\n  ")).clone(), (jacStr.clone()).clone(), (literal!("\n]\n  *\n[\n  ")).clone(), (varnames.clone()).clone(), (literal!("\n]\n  =\n[\n  ")).clone(), (rhsStr.clone()).clone(), (literal!("\n]")).clone()]);
            Error::addMessage(Error::LINEAR_SYSTEM_SINGULAR.clone(), list![(syst.clone()).clone(), (infoStr.clone()).clone(), (varname.clone()).clone()])?;
            Ok(bail!("fail"))
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let _ = __mc_input.clone() else { bail!("nomatch") };
            let mut syst: ArcStr = arcstr::literal!("");
            let mut varnames: ArcStr = arcstr::literal!("");
            let mut rhsStr: ArcStr = arcstr::literal!("");
            let mut jacStr: ArcStr = arcstr::literal!("");
            let true = (info.clone() < 0) else { bail!("pattern mismatch") };
            varnames = stringDelimitList(List::map(vars.clone(), (std::sync::Arc::new(ComponentReferenceBasics::printComponentRefStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))?, (literal!(" ;\n  ")).clone());
            rhsStr = stringDelimitList(List::map(rhs.clone(), (std::sync::Arc::new(fnptr!(realString, metamodelica::Real)) as std::sync::Arc<dyn ::std::ops::Fn(metamodelica::Real) -> Result<ArcStr> + 'static>))?, (literal!(" ; ")).clone());
            jacStr = stringDelimitList(List::map1(List::mapList(jac.clone(), (std::sync::Arc::new(fnptr!(realString, metamodelica::Real)) as std::sync::Arc<dyn ::std::ops::Fn(metamodelica::Real) -> Result<ArcStr> + 'static>))?, (std::sync::Arc::new(fnptr!(stringDelimitList, Arc<metamodelica::List<ArcStr>>, ArcStr)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<ArcStr>>, ArcStr) -> Result<ArcStr> + 'static>), (literal!(" , ")).clone())?, (literal!(" ; ")).clone());
            syst = stringAppendList(list![(literal!("[")).clone(), (jacStr.clone()).clone(), (literal!("] * [")).clone(), (varnames.clone()).clone(), (literal!("] = [")).clone(), (rhsStr.clone()).clone(), (literal!("]")).clone()]);
            Error::addMessage(Error::LINEAR_SYSTEM_INVALID.clone(), list![(literal!("LAPACK/dgesv")).clone(), (syst.clone()).clone()])?;
            Ok(bail!("fail"))
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(())
}

fn generateSolvedEquation(mut var: SimCodeVar::SimVar, mut val: metamodelica::Real, mut source: Arc<DAE::ElementSource>, mut iuniqueEqIndex: i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> {
    let mut eq: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut ouniqueEqIndex: i32 = 0;
    let mut name: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let SimCodeVar::SIMVAR { name: __pa0, .. } = (var.clone()) else { bail!("pattern mismatch") };
    name = __pa0.clone();
    eq = Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: iuniqueEqIndex.clone(), cref: name.clone(), exp: Arc::new(DAE::Exp::RCONST { real: val.clone() }), source: source.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
    ouniqueEqIndex = iuniqueEqIndex.clone() + 1;
    Ok((eq, ouniqueEqIndex))
}

fn createTornSystem(mut linear: bool, mut skipDiscInAlgorithm: bool, mut genDiscrete: bool, mut strictTearingSet: BackendDAE::TearingSet, mut casualTearingSet: Option<BackendDAE::TearingSet>, mut isyst: Arc<BackendDAE::EqSystem>, mut ishared: Arc<BackendDAE::Shared>, mut iuniqueEqIndex: i32, mut mixedSystem: bool, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equations_, ouniqueEqIndex, otempvars) = (::match_deref::match_deref! { match &((linear.clone(), isyst.clone(), ishared.clone())) {
        (true, Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, Deref @ BackendDAE::Shared { globalKnownVars, .. }) => {
            let mut tvars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut reqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut tempvars2: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut simVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut simequations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut uniqueEqIndex: i32 = 0;
            let mut nInnerVars: i32 = 0;
            let mut jacobianMatrix: Option<Arc<SimCode::JacobianMatrix>> = None;
            let mut tearingVars: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut residualEqns: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut innerEquations: Arc<metamodelica::List<BackendDAE::InnerEquation>> = metamodelica::nil();
            let mut inJacobian: Arc<BackendDAE::Jacobian> = Arc::new(BackendDAE::Jacobian::EMPTY_JACOBIAN);
            let mut lSystem: Arc<SimCode::LinearSystem>;
            let mut alternativeTearingL: Option<Arc<SimCode::LinearSystem>> = None;
            let mut partOfJac: bool = false;
            let BackendDAE::TEARINGSET { jac: __pa0, innerEquations: __pa1, residualequations: __pa2, tearingvars: __pa3 } = (strictTearingSet.clone()) else { bail!("pattern mismatch") };
            inJacobian = __pa0.clone();
            innerEquations = __pa1.clone();
            residualEqns = __pa2.clone();
            tearingVars = __pa3.clone();
            if !(SymbolicJacobian::isJacobianGeneric(inJacobian.clone())) {
                Error::addMessage(Error::NO_JACONIAN_TORNLINEAR_SYSTEM.clone(), metamodelica::nil())?;
                bail!("fail");
            }
            tvars = List::map1r(tearingVars.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), vars.clone())?;
            tvars = List::map(tvars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (simVars, _) = List::fold(tvars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvarFold) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)> + 'static>), (metamodelica::nil(), globalKnownVars.clone()))?;
            simVars = simVars.clone().reverse();
            reqns = BackendEquation::getList(residualEqns.clone(), eqns.clone())?;
            reqns = BackendEquation::replaceDerOpInEquationList(reqns.clone())?;
            (simequations, uniqueEqIndex, tempvars, nInnerVars, _) = createTornSystemInnerEqns(innerEquations.clone(), skipDiscInAlgorithm.clone(), genDiscrete.clone(), isyst.clone(), ishared.clone(), iuniqueEqIndex.clone(), itempvars.clone(), metamodelica::nil())?;
            let (__pa4, (__pa5, _), __pa6) = createNonlinearResidualEquations(reqns.clone(), (uniqueEqIndex.clone(), 0), tempvars.clone(), ishared.functionTree.clone())?;
            resEqs = __pa4.clone();
            uniqueEqIndex = __pa5.clone();
            tempvars = __pa6.clone();
            resEqs = fixNonlinearResidualIndices(resEqs.clone());
            eqs = listAppend(simequations.clone(), resEqs.clone());
            (jacobianMatrix, uniqueEqIndex, tempvars) = createSymbolicSimulationJacobian(inJacobian.clone(), uniqueEqIndex.clone(), tempvars.clone(), false)?;
            partOfJac = BackendDAEUtil::isJacobianDAE(ishared.clone());
            lSystem = Arc::new(SimCode::LinearSystem { index: uniqueEqIndex.clone(), partOfMixed: false, tornSystem: true, vars: simVars.clone(), beqs: metamodelica::nil(), simJac: metamodelica::nil(), residual: eqs.clone(), jacobianMatrix: jacobianMatrix.clone(), sources: metamodelica::nil(), indexLinearSystem: 0, nUnknowns: (tvars.clone().len() as i32) + nInnerVars.clone() + (tempvars.clone().len() as i32) - (itempvars.clone().len() as i32), partOfJac: partOfJac.clone() });
            tempvars2 = tempvars.clone();
            if isSome(casualTearingSet.clone()) {
                let (__pa7, __pa8, __pa9, __pa10) = ::match_deref::match_deref! { match &(casualTearingSet.clone()) {
                    Some(BackendDAE::TearingSet { jac: __pa7, innerEquations: __pa8, residualequations: __pa9, tearingvars: __pa10 }) => (__pa7.clone(), __pa8.clone(), __pa9.clone(), __pa10.clone()),
                    _ => bail!("pattern mismatch"),
                } };
                inJacobian = __pa7.clone();
                innerEquations = __pa8.clone();
                residualEqns = __pa9.clone();
                tearingVars = __pa10.clone();
                tvars = List::map1r(tearingVars.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), vars.clone())?;
                tvars = List::map(tvars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
                (simVars, _) = List::fold(tvars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvarFold) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)> + 'static>), (metamodelica::nil(), globalKnownVars.clone()))?;
                simVars = simVars.clone().reverse();
                reqns = BackendEquation::getList(residualEqns.clone(), eqns.clone())?;
                reqns = BackendEquation::replaceDerOpInEquationList(reqns.clone())?;
                (simequations, uniqueEqIndex, tempvars2, nInnerVars, _) = createTornSystemInnerEqns(innerEquations.clone(), skipDiscInAlgorithm.clone(), genDiscrete.clone(), isyst.clone(), ishared.clone(), uniqueEqIndex.clone() + 1, tempvars.clone(), metamodelica::nil())?;
                let (__pa11, (__pa12, _), __pa13) = createNonlinearResidualEquations(reqns.clone(), (uniqueEqIndex.clone(), 0), tempvars2.clone(), ishared.functionTree.clone())?;
                resEqs = __pa11.clone();
                uniqueEqIndex = __pa12.clone();
                tempvars2 = __pa13.clone();
                resEqs = fixNonlinearResidualIndices(resEqs.clone());
                eqs = listAppend(simequations.clone(), resEqs.clone());
                (jacobianMatrix, uniqueEqIndex, tempvars2) = createSymbolicSimulationJacobian(inJacobian.clone(), uniqueEqIndex.clone(), tempvars2.clone(), false)?;
                alternativeTearingL = Some(Arc::new(SimCode::LinearSystem { index: uniqueEqIndex.clone(), partOfMixed: false, tornSystem: true, vars: simVars.clone(), beqs: metamodelica::nil(), simJac: metamodelica::nil(), residual: eqs.clone(), jacobianMatrix: jacobianMatrix.clone(), sources: metamodelica::nil(), indexLinearSystem: 0, nUnknowns: (tvars.clone().len() as i32) + nInnerVars.clone() + (tempvars2.clone().len() as i32) - (tempvars.clone().len() as i32), partOfJac: partOfJac.clone() }));
            } else {
                alternativeTearingL = None;
            }
            (list![Arc::new(SimCode::SimEqSystem::SES_LINEAR { lSystem: lSystem.clone(), alternativeTearing: alternativeTearingL.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() })], uniqueEqIndex.clone() + 1, tempvars2.clone())
        },
        (false, Deref @ BackendDAE::EqSystem { orderedEqs: eqns, orderedVars: vars, .. }, _) => {
            let mut tvars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut reqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut tempvars2: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut simequations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut uniqueEqIndex: i32 = 0;
            let mut nInnerVars: i32 = 0;
            let mut tcrs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut jacobianMatrix: Option<Arc<SimCode::JacobianMatrix>> = None;
            let mut tearingVars: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut residualEqns: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut homotopySupport: bool = false;
            let mut innerEquations: Arc<metamodelica::List<BackendDAE::InnerEquation>> = metamodelica::nil();
            let mut inJacobian: Arc<BackendDAE::Jacobian> = Arc::new(BackendDAE::Jacobian::EMPTY_JACOBIAN);
            let mut nlSystem: Arc<SimCode::NonlinearSystem> = Arc::new(<SimCode::NonlinearSystem as ::std::default::Default>::default());
            let mut alternativeTearingNl: Option<Arc<SimCode::NonlinearSystem>> = None;
            let mut clockIndex: Option<i32> = None;
            let BackendDAE::TEARINGSET { jac: __pa0, innerEquations: __pa1, residualequations: __pa2, tearingvars: __pa3 } = (strictTearingSet.clone()) else { bail!("pattern mismatch") };
            inJacobian = __pa0.clone();
            innerEquations = __pa1.clone();
            residualEqns = __pa2.clone();
            tearingVars = __pa3.clone();
            tvars = List::map1r(tearingVars.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), vars.clone())?;
            tvars = List::map(tvars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            reqns = BackendEquation::getList(residualEqns.clone(), eqns.clone())?;
            reqns = BackendEquation::replaceDerOpInEquationList(reqns.clone())?;
            tcrs = List::map(tvars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
            (simequations, uniqueEqIndex, tempvars, nInnerVars, homotopySupport) = createTornSystemInnerEqns(innerEquations.clone(), skipDiscInAlgorithm.clone(), genDiscrete.clone(), isyst.clone(), ishared.clone(), iuniqueEqIndex.clone(), itempvars.clone(), metamodelica::nil())?;
            let (__pa4, (__pa5, _), __pa6) = createNonlinearResidualEquations(reqns.clone(), (uniqueEqIndex.clone(), 0), tempvars.clone(), ishared.functionTree.clone())?;
            resEqs = __pa4.clone();
            uniqueEqIndex = __pa5.clone();
            tempvars = __pa6.clone();
            resEqs = fixNonlinearResidualIndices(resEqs.clone());
            eqs = listAppend(simequations.clone(), resEqs.clone());
            (jacobianMatrix, uniqueEqIndex, tempvars) = createSymbolicSimulationJacobian(inJacobian.clone(), uniqueEqIndex.clone(), tempvars.clone(), true)?;
            if !(homotopySupport.clone()) {
                (_, homotopySupport) = BackendEquation::traverseExpsOfEquationList(reqns.clone(), (std::sync::Arc::new(BackendDAEUtil::containsHomotopyCall) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, bool) -> Result<(Arc<DAE::Exp>, bool)> + 'static>), false)?;
            }
            clockIndex = partitionKindToClockIndex(isyst.partitionKind.clone());
            nlSystem = Arc::new(SimCode::NonlinearSystem { index: uniqueEqIndex.clone(), eqs: eqs.clone(), crefs: tcrs.clone(), indexNonLinearSystem: 0, nUnknowns: (tvars.clone().len() as i32) + nInnerVars.clone() + (tempvars.clone().len() as i32) - (itempvars.clone().len() as i32), jacobianMatrix: jacobianMatrix.clone(), homotopySupport: homotopySupport.clone(), mixedSystem: mixedSystem.clone(), tornSystem: true, clockIndex: clockIndex.clone() });
            tempvars2 = tempvars.clone();
            if isSome(casualTearingSet.clone()) {
                let (__pa7, __pa8, __pa9, __pa10) = ::match_deref::match_deref! { match &(casualTearingSet.clone()) {
                    Some(BackendDAE::TearingSet { jac: __pa7, innerEquations: __pa8, residualequations: __pa9, tearingvars: __pa10 }) => (__pa7.clone(), __pa8.clone(), __pa9.clone(), __pa10.clone()),
                    _ => bail!("pattern mismatch"),
                } };
                inJacobian = __pa7.clone();
                innerEquations = __pa8.clone();
                residualEqns = __pa9.clone();
                tearingVars = __pa10.clone();
                tvars = List::map1r(tearingVars.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), vars.clone())?;
                tvars = List::map(tvars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
                reqns = BackendEquation::getList(residualEqns.clone(), eqns.clone())?;
                reqns = BackendEquation::replaceDerOpInEquationList(reqns.clone())?;
                tcrs = List::map(tvars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                (simequations, uniqueEqIndex, tempvars2, nInnerVars, homotopySupport) = createTornSystemInnerEqns(innerEquations.clone(), skipDiscInAlgorithm.clone(), genDiscrete.clone(), isyst.clone(), ishared.clone(), uniqueEqIndex.clone() + 1, tempvars.clone(), metamodelica::nil())?;
                let (__pa11, (__pa12, _), __pa13) = createNonlinearResidualEquations(reqns.clone(), (uniqueEqIndex.clone(), 0), tempvars2.clone(), ishared.functionTree.clone())?;
                resEqs = __pa11.clone();
                uniqueEqIndex = __pa12.clone();
                tempvars2 = __pa13.clone();
                resEqs = fixNonlinearResidualIndices(resEqs.clone());
                eqs = listAppend(simequations.clone(), resEqs.clone());
                (jacobianMatrix, uniqueEqIndex, tempvars2) = createSymbolicSimulationJacobian(inJacobian.clone(), uniqueEqIndex.clone(), tempvars2.clone(), false)?;
                if !(homotopySupport.clone()) {
                    (_, homotopySupport) = BackendEquation::traverseExpsOfEquationList(reqns.clone(), (std::sync::Arc::new(BackendDAEUtil::containsHomotopyCall) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, bool) -> Result<(Arc<DAE::Exp>, bool)> + 'static>), false)?;
                }
                alternativeTearingNl = Some(Arc::new(SimCode::NonlinearSystem { index: uniqueEqIndex.clone(), eqs: eqs.clone(), crefs: tcrs.clone(), indexNonLinearSystem: 0, nUnknowns: (tvars.clone().len() as i32) + nInnerVars.clone() + (tempvars2.clone().len() as i32) - (tempvars.clone().len() as i32), jacobianMatrix: jacobianMatrix.clone(), homotopySupport: homotopySupport.clone(), mixedSystem: mixedSystem.clone(), tornSystem: true, clockIndex: clockIndex.clone() }));
            } else {
                alternativeTearingNl = None;
            }
            (list![Arc::new(SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: nlSystem.clone(), alternativeTearing: alternativeTearingNl.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() })], uniqueEqIndex.clone() + 1, tempvars2.clone())
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok((equations_, ouniqueEqIndex, otempvars))
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn solveInnerEquations(mut innerEquations: Arc<metamodelica::List<BackendDAE::InnerEquation>>, mut inEqns: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>>, mut inVars: BackendDAE::Variables, mut ishared: Arc<BackendDAE::Shared>, mut inRepl: BackendVarTransform::VariableReplacements) -> Result<BackendVarTransform::VariableReplacements> {
    let mut outRepl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
    outRepl = (::match_deref::match_deref! { match &(innerEquations.clone()) {
        Deref @ metamodelica::List::Nil => {
            inRepl.clone()
        },
        Deref @ metamodelica::List::Cons { head: BackendDAE::InnerEquation::INNEREQUATION { vars: Deref @ metamodelica::List::Cons { head: v, tail: Deref @ metamodelica::List::Nil }, eqn: e }, tail: rest } => {
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut varexp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut expr: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut dcr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut repl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
            let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
            let mut funcs: Arc<AvlTreePathFunction::Tree> = Arc::new(AvlTreePathFunction::Tree::EMPTY);
            let (__pa0, __pa1) = ::match_deref::match_deref! { match &(BackendEquation::get(inEqns.clone(), e.clone())?) {
                Deref @ BackendDAE::Equation::EQUATION { scalar: __pa0, exp: __pa1, .. } => (__pa0.clone(), __pa1.clone()),
                _ => bail!("pattern mismatch"),
            } };
            e2 = __pa0.clone();
            e1 = __pa1.clone();
            let ref __pa3 @ BackendDAE::VAR { varName: ref __pa2, .. } = (BackendVariable::getVarAt(inVars.clone(), v.clone())?) else { bail!("pattern mismatch") };
            cr = __pa2.clone();
            var = __pa3.clone();
            varexp = Expression::crefExp(cr.clone())?;
            varexp = if (BackendVariable::isStateVar(var.clone())) {Expression::expDer(varexp.clone())} else {varexp.clone()};
            let __pa4 = ::match_deref::match_deref! { match &(ishared.clone()) {
                Deref @ BackendDAE::Shared { functionTree: __pa4, .. } => __pa4.clone(),
                _ => bail!("pattern mismatch"),
            } };
            funcs = __pa4.clone();
            let __pa5 = ::match_deref::match_deref! { match &(ExpressionSolve::solve2(e1.clone(), e2.clone(), varexp.clone(), Some(funcs.clone()), None, true, BackendDAEUtil::isSimulationDAE(ishared.clone()))?) {
                (__pa5, Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil) => __pa5.clone(),
                _ => bail!("pattern mismatch"),
            } };
            expr = __pa5.clone();
            dcr = if (BackendVariable::isStateVar(var.clone())) {ComponentReference::crefPrefixDer(cr.clone())} else {cr.clone()};
            repl = BackendVarTransform::addReplacement(inRepl.clone(), dcr.clone(), expr.clone(), Some((std::sync::Arc::new(fnptr!(BackendVarTransform::skipPreOperator, Arc<DAE::Exp>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<bool> + 'static>)))?;
            repl = if (BackendVariable::isStateVar(var.clone())) {BackendVarTransform::addDerConstRepl(cr.clone(), expr.clone(), repl.clone())?} else {repl.clone()};
            solveInnerEquations(rest.clone(), inEqns.clone(), inVars.clone(), ishared.clone(), repl.clone())?
        },
        Deref @ metamodelica::List::Cons { head: BackendDAE::InnerEquation::INNEREQUATION { vars: vlst, eqn: e }, tail: rest } => {
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut repl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut ds: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut explst1: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut explst2: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut subslst: Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::Subscript>>>>> = metamodelica::nil();
            let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(BackendEquation::get(inEqns.clone(), e.clone())?) {
                Deref @ BackendDAE::Equation::ARRAY_EQUATION { right: __pa0, left: __pa1, dimSize: __pa2, .. } => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                _ => bail!("pattern mismatch"),
            } };
            e2 = __pa0.clone();
            e1 = __pa1.clone();
            ds = __pa2.clone();
            varlst = List::map1r(vlst.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), inVars.clone())?;
            subslst = Expression::dimensionSizesSubscripts(ds.clone())?;
            subslst = Expression::rangesToSubscripts(subslst.clone())?;
            explst1 = List::map1r(subslst.clone(), (std::sync::Arc::new(Expression::applyExpSubscripts) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Subscript>>>) -> Result<Arc<DAE::Exp>> + 'static>), e1.clone())?;
            explst1 = ExpressionSimplify::simplifyList(explst1.clone())?;
            explst2 = List::map1r(subslst.clone(), (std::sync::Arc::new(Expression::applyExpSubscripts) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Subscript>>>) -> Result<Arc<DAE::Exp>> + 'static>), e2.clone())?;
            explst2 = ExpressionSimplify::simplifyList(explst2.clone())?;
            repl = solveInnerEquations1(explst1.clone(), explst2.clone(), varlst.clone(), inVars.clone(), ishared.clone(), inRepl.clone())?;
            solveInnerEquations(rest.clone(), inEqns.clone(), inVars.clone(), ishared.clone(), repl.clone())?
        },
        Deref @ metamodelica::List::Cons { head: BackendDAE::InnerEquation::INNEREQUATIONCONSTRAINTS { vars: Deref @ metamodelica::List::Cons { head: v, tail: Deref @ metamodelica::List::Nil }, eqn: e, .. }, tail: rest } => {
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut varexp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut expr: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut dcr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut repl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
            let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
            let mut funcs: Arc<AvlTreePathFunction::Tree> = Arc::new(AvlTreePathFunction::Tree::EMPTY);
            let (__pa0, __pa1) = ::match_deref::match_deref! { match &(BackendEquation::get(inEqns.clone(), e.clone())?) {
                Deref @ BackendDAE::Equation::EQUATION { scalar: __pa0, exp: __pa1, .. } => (__pa0.clone(), __pa1.clone()),
                _ => bail!("pattern mismatch"),
            } };
            e2 = __pa0.clone();
            e1 = __pa1.clone();
            let ref __pa3 @ BackendDAE::VAR { varName: ref __pa2, .. } = (BackendVariable::getVarAt(inVars.clone(), v.clone())?) else { bail!("pattern mismatch") };
            cr = __pa2.clone();
            var = __pa3.clone();
            varexp = Expression::crefExp(cr.clone())?;
            varexp = if (BackendVariable::isStateVar(var.clone())) {Expression::expDer(varexp.clone())} else {varexp.clone()};
            let __pa4 = ::match_deref::match_deref! { match &(ishared.clone()) {
                Deref @ BackendDAE::Shared { functionTree: __pa4, .. } => __pa4.clone(),
                _ => bail!("pattern mismatch"),
            } };
            funcs = __pa4.clone();
            let __pa5 = ::match_deref::match_deref! { match &(ExpressionSolve::solve2(e1.clone(), e2.clone(), varexp.clone(), Some(funcs.clone()), None, true, BackendDAEUtil::isSimulationDAE(ishared.clone()))?) {
                (__pa5, Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil) => __pa5.clone(),
                _ => bail!("pattern mismatch"),
            } };
            expr = __pa5.clone();
            dcr = if (BackendVariable::isStateVar(var.clone())) {ComponentReference::crefPrefixDer(cr.clone())} else {cr.clone()};
            repl = BackendVarTransform::addReplacement(inRepl.clone(), dcr.clone(), expr.clone(), Some((std::sync::Arc::new(fnptr!(BackendVarTransform::skipPreOperator, Arc<DAE::Exp>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<bool> + 'static>)))?;
            repl = if (BackendVariable::isStateVar(var.clone())) {BackendVarTransform::addDerConstRepl(cr.clone(), expr.clone(), repl.clone())?} else {repl.clone()};
            solveInnerEquations(rest.clone(), inEqns.clone(), inVars.clone(), ishared.clone(), repl.clone())?
        },
        Deref @ metamodelica::List::Cons { head: BackendDAE::InnerEquation::INNEREQUATIONCONSTRAINTS { vars: vlst, eqn: e, .. }, tail: rest } => {
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut repl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut ds: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut explst1: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut explst2: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
            let mut subslst: Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::Subscript>>>>> = metamodelica::nil();
            let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(BackendEquation::get(inEqns.clone(), e.clone())?) {
                Deref @ BackendDAE::Equation::ARRAY_EQUATION { right: __pa0, left: __pa1, dimSize: __pa2, .. } => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                _ => bail!("pattern mismatch"),
            } };
            e2 = __pa0.clone();
            e1 = __pa1.clone();
            ds = __pa2.clone();
            varlst = List::map1r(vlst.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), inVars.clone())?;
            subslst = Expression::dimensionSizesSubscripts(ds.clone())?;
            subslst = Expression::rangesToSubscripts(subslst.clone())?;
            explst1 = List::map1r(subslst.clone(), (std::sync::Arc::new(Expression::applyExpSubscripts) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Subscript>>>) -> Result<Arc<DAE::Exp>> + 'static>), e1.clone())?;
            explst1 = ExpressionSimplify::simplifyList(explst1.clone())?;
            explst2 = List::map1r(subslst.clone(), (std::sync::Arc::new(Expression::applyExpSubscripts) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Subscript>>>) -> Result<Arc<DAE::Exp>> + 'static>), e2.clone())?;
            explst2 = ExpressionSimplify::simplifyList(explst2.clone())?;
            repl = solveInnerEquations1(explst1.clone(), explst2.clone(), varlst.clone(), inVars.clone(), ishared.clone(), inRepl.clone())?;
            solveInnerEquations(rest.clone(), inEqns.clone(), inVars.clone(), ishared.clone(), repl.clone())?
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outRepl)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn solveInnerEquations1(mut iExps1: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut iExps2: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut iVars: Arc<metamodelica::List<BackendDAE::Var>>, mut inVars: BackendDAE::Variables, mut ishared: Arc<BackendDAE::Shared>, mut inRepl: BackendVarTransform::VariableReplacements) -> Result<BackendVarTransform::VariableReplacements> {
    let mut outRepl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
    outRepl = (::match_deref::match_deref! { match &((iExps1.clone(), iExps2.clone(), iVars.clone())) {
        (Deref @ metamodelica::List::Nil, _, _) => {
            inRepl.clone()
        },
        (Deref @ metamodelica::List::Cons { head: e1, tail: explst1 }, Deref @ metamodelica::List::Cons { head: e2, tail: explst2 }, Deref @ metamodelica::List::Cons { head: var @ BackendDAE::Var { varName: cr, .. }, tail: rest }) => {
            let mut varexp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut expr: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut dcr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut repl: BackendVarTransform::VariableReplacements = <BackendVarTransform::VariableReplacements as ::std::default::Default>::default();
            let mut funcs: Arc<AvlTreePathFunction::Tree> = Arc::new(AvlTreePathFunction::Tree::EMPTY);
            varexp = Expression::crefExp(cr.clone())?;
            varexp = if (BackendVariable::isStateVar(var.clone())) {Expression::expDer(varexp.clone())} else {varexp.clone()};
            let __pa0 = ::match_deref::match_deref! { match &(ishared.clone()) {
                Deref @ BackendDAE::Shared { functionTree: __pa0, .. } => __pa0.clone(),
                _ => bail!("pattern mismatch"),
            } };
            funcs = __pa0.clone();
            let __pa1 = ::match_deref::match_deref! { match &(ExpressionSolve::solve2(e1.clone(), e2.clone(), varexp.clone(), Some(funcs.clone()), None, true, BackendDAEUtil::isSimulationDAE(ishared.clone()))?) {
                (__pa1, Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil) => __pa1.clone(),
                _ => bail!("pattern mismatch"),
            } };
            expr = __pa1.clone();
            dcr = if (BackendVariable::isStateVar(var.clone())) {ComponentReference::crefPrefixDer(cr.clone())} else {cr.clone()};
            repl = BackendVarTransform::addReplacement(inRepl.clone(), dcr.clone(), expr.clone(), Some((std::sync::Arc::new(fnptr!(BackendVarTransform::skipPreOperator, Arc<DAE::Exp>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<bool> + 'static>)))?;
            repl = if (BackendVariable::isStateVar(var.clone())) {BackendVarTransform::addDerConstRepl(cr.clone(), expr.clone(), repl.clone())?} else {repl.clone()};
            solveInnerEquations1(explst1.clone(), explst2.clone(), rest.clone(), inVars.clone(), ishared.clone(), repl.clone())?
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outRepl)
}

fn createTornSystemInnerEqns(mut innerEquations: Arc<metamodelica::List<BackendDAE::InnerEquation>>, mut skipDiscInAlgorithm: bool, mut genDiscrete: bool, mut isyst: Arc<BackendDAE::EqSystem>, mut ishared: Arc<BackendDAE::Shared>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut isimequations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32, bool)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = iuniqueEqIndex.clone();
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = itempvars.clone();
    let mut nVars: i32 = 0;
    let mut homotopySupport: bool = false;
    let mut eqns: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
    let mut eqnindx: i32 = 0;
    let mut vars: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut eqn: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
    let mut comp: Arc<BackendDAE::StrongComponent> = Arc::new(<BackendDAE::StrongComponent as ::std::default::Default>::default());
    let mut simequations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut equations: DoubleEnded::MutableList<Arc<SimCode::SimEqSystem>> = <DoubleEnded::MutableList<Arc<SimCode::SimEqSystem>> as ::std::default::Default>::default();
    let mut cons: Arc<metamodelica::List<Arc<DAE::Constraint>>> = metamodelica::nil();
    if innerEquations.clone().is_empty() {
        equations_ = isimequations.clone();
        return Ok((equations_.clone(), ouniqueEqIndex.clone(), otempvars.clone(), nVars.clone(), homotopySupport.clone()));
    }
    let __pa0 = ::match_deref::match_deref! { match &(isyst.clone()) {
        Deref @ BackendDAE::EqSystem { orderedEqs: __pa0, .. } => __pa0.clone(),
        _ => bail!("pattern mismatch"),
    } };
    eqns = __pa0.clone();
    equations = DoubleEnded::fromList(isimequations.clone())?;
    for mut eq in &*innerEquations.clone() {
        let mut eq = eq.clone();
        (eqnindx, vars, cons) = BackendDAEUtil::getEqnAndVarsFromInnerEquation(eq.clone())?;
        nVars = nVars.clone() + (vars.clone().len() as i32);
        eqn = BackendEquation::get(eqns.clone(), eqnindx.clone())?;
        if !(homotopySupport.clone()) {
            (_, homotopySupport) = BackendEquation::traverseExpsOfEquation(eqn.clone(), (std::sync::Arc::new(BackendDAEUtil::containsHomotopyCall) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, bool) -> Result<(Arc<DAE::Exp>, bool)> + 'static>), false)?;
        }
        comp = createTornSystemInnerEqns1(eqn.clone(), eqnindx.clone(), vars.clone())?;
        (simequations, _, ouniqueEqIndex, otempvars) = createEquationsWork(genDiscrete.clone(), false, genDiscrete.clone(), skipDiscInAlgorithm.clone(), isyst.clone(), ishared.clone(), comp.clone(), ouniqueEqIndex.clone(), otempvars.clone(), cons.clone())?;
        DoubleEnded::push_list_back(equations.clone(), simequations.clone());
    }
    equations_ = DoubleEnded::toListAndClear(equations.clone(), metamodelica::nil());
    Ok((equations_, ouniqueEqIndex, otempvars, nVars, homotopySupport))
}

fn createTornSystemInnerEqns1(mut eqn: Arc<BackendDAE::Equation>, mut eqnindx: i32, mut varindx: Arc<metamodelica::List<i32>>) -> Result<Arc<BackendDAE::StrongComponent>> {
    let mut ocomp: Arc<BackendDAE::StrongComponent> = Arc::new(<BackendDAE::StrongComponent as ::std::default::Default>::default());
    ocomp = (::match_deref::match_deref! { match &((eqn.clone(), varindx.clone())) {
        (Deref @ BackendDAE::Equation::EQUATION { .. }, Deref @ metamodelica::List::Cons { head: v, tail: Deref @ metamodelica::List::Nil }) => {
            Arc::new(BackendDAE::StrongComponent::SINGLEEQUATION { eqn: eqnindx.clone(), var: v.clone() })
        },
        (Deref @ BackendDAE::Equation::RESIDUAL_EQUATION { .. }, Deref @ metamodelica::List::Cons { head: v, tail: Deref @ metamodelica::List::Nil }) => {
            Arc::new(BackendDAE::StrongComponent::SINGLEEQUATION { eqn: eqnindx.clone(), var: v.clone() })
        },
        (Deref @ BackendDAE::Equation::SOLVED_EQUATION { .. }, Deref @ metamodelica::List::Cons { head: v, tail: Deref @ metamodelica::List::Nil }) => {
            Arc::new(BackendDAE::StrongComponent::SINGLEEQUATION { eqn: eqnindx.clone(), var: v.clone() })
        },
        (Deref @ BackendDAE::Equation::ARRAY_EQUATION { .. }, _) => {
            Arc::new(BackendDAE::StrongComponent::SINGLEARRAY { eqn: eqnindx.clone(), vars: varindx.clone() })
        },
        (Deref @ BackendDAE::Equation::IF_EQUATION { .. }, _) => {
            Arc::new(BackendDAE::StrongComponent::SINGLEIFEQUATION { eqn: eqnindx.clone(), vars: varindx.clone() })
        },
        (Deref @ BackendDAE::Equation::ALGORITHM { .. }, _) => {
            Arc::new(BackendDAE::StrongComponent::SINGLEALGORITHM { eqn: eqnindx.clone(), vars: varindx.clone() })
        },
        (Deref @ BackendDAE::Equation::COMPLEX_EQUATION { .. }, _) => {
            Arc::new(BackendDAE::StrongComponent::SINGLECOMPLEXEQUATION { eqn: eqnindx.clone(), vars: varindx.clone() })
        },
        (Deref @ BackendDAE::Equation::WHEN_EQUATION { .. }, _) => {
            Arc::new(BackendDAE::StrongComponent::SINGLEWHENEQUATION { eqn: eqnindx.clone(), vars: varindx.clone() })
        },
        _ => {
            println!("{}", (literal!("SimCodeUtil.createTornSystemInnerEqns1 failed for\n")).clone());
            BackendDump::printEquationList(list![eqn.clone()])?;
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Eqn: ")); __mm_s.push_str(&*intString(eqnindx.clone())); __mm_s.push_str(&*literal!(" Vars: ")); __mm_s.push_str(&*stringDelimitList(List::map(varindx.clone(), (std::sync::Arc::new(fnptr!(intString, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))?, (literal!(", ")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(ocomp)
}

// =============================================================================
// section to create equations for omsi functions
//
// =============================================================================
fn createAllEquationOMSI(mut constSysts: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>>, mut shared: Arc<BackendDAE::Shared>, mut inZeroCrossings: Arc<metamodelica::List<BackendDAE::ZeroCrossing>>, mut uniqueEqIndex: i32) -> Result<(Arc<SimCode::OMSIFunction>, i32)> {
    let mut omsiAllEquations: Arc<SimCode::OMSIFunction> = SimCode::emptyOMSIFunction().clone();
    let mut uniqueEqIndex: i32 = uniqueEqIndex;
    let mut components: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>> = metamodelica::nil();
    let mut newAllEquations: Arc<SimCode::OMSIFunction> = Arc::new(<SimCode::OMSIFunction as ::std::default::Default>::default());
    assign_field!(omsiAllEquations.context = SimCodeFunction::Context::OMSI_CONTEXT { hashTable: Some(HashTableCrefSimVar::emptyHashTableSized(1013)) });
    for mut constSyst in &*constSysts.clone() {
        let mut constSyst = constSyst.clone();
        match '__try0: {
            let __pa1 = ::match_deref::match_deref! { match &(constSyst.matching.clone()) {
                Deref @ BackendDAE::Matching::MATCHING { comps: __pa1, .. } => __pa1.clone(),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            components = __pa1.clone();
            Ok::<_, anyhow::Error>((components.clone(),))
        } {
            Ok((__try0_o0,)) => {
                components = __try0_o0;
            }
            Err(__try0_err) => {
                Error::addInternalError((literal!("The matching information is missing in function createAllEquationOMSI!")).clone(), metamodelica::sourceInfo!())?;
                return Err(__try0_err);
            }
        }
        (newAllEquations, uniqueEqIndex) = generateEquationsForComponents(components.clone(), constSyst.clone(), shared.clone(), uniqueEqIndex.clone())?;
        omsiAllEquations = appendOMSIFunction(omsiAllEquations.clone(), newAllEquations.clone())?;
    }
    Ok((omsiAllEquations, uniqueEqIndex))
}

fn generateEquationsForComponents(mut components: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>>, mut constSyst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut uniqueEqIndex: i32) -> Result<(Arc<SimCode::OMSIFunction>, i32)> {
    let mut omsiFuncEquations: Arc<SimCode::OMSIFunction> = Arc::new(<SimCode::OMSIFunction as ::std::default::Default>::default());
    let mut uniqueEqIndex: i32 = uniqueEqIndex;
    let mut equations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut inputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut outputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut innerVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut hashTable: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
    let mut tmpEqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut tmpInputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut tmpOutputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut tmpInnerVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut tempVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut nAlgebraicSystems: i32 = 0;
    let mut index: i32 = 0;
    let mut nAllVars: i32 = 0;
    let mut debug: bool = false;
    let mut clockIndex: Option<i32> = None;
    clockIndex = partitionKindToClockIndex(constSyst.partitionKind.clone());
    for mut component in &*components.clone() {
        let mut component = component.clone();
        tmpEqns = metamodelica::nil();
        tmpInputVars = metamodelica::nil();
        tmpOutputVars = metamodelica::nil();
        tmpInnerVars = metamodelica::nil();
        let () = (::match_deref::match_deref! { match &(component.clone()) {
        Deref @ BackendDAE::StrongComponent::SINGLEEQUATION { .. } => {
            let mut eqn: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
            let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
            let (__pa0, __pa1) = ::match_deref::match_deref! { match &(BackendDAETransform::getEquationAndSolvedVar(component.clone(), constSyst.orderedEqs.clone(), constSyst.orderedVars.clone())?) {
                (Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Nil }, Deref @ metamodelica::List::Cons { head: __pa1, tail: Deref @ metamodelica::List::Nil }, _) => (__pa0.clone(), __pa1.clone()),
                _ => bail!("pattern mismatch"),
            } };
            eqn = __pa0.clone();
            var = __pa1.clone();
            (tmpEqns, tmpInputVars, tmpOutputVars, tmpInnerVars, uniqueEqIndex) = generateSingleEquation(eqn.clone(), var.clone(), shared.functionTree.clone(), shared.timeInterval.clone(), uniqueEqIndex.clone())?;
            ()
        },
        Deref @ BackendDAE::StrongComponent::SINGLEWHENEQUATION { .. } => {
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(component.clone(), constSyst.orderedEqs.clone(), constSyst.orderedVars.clone())?;
            (tmpEqns, tmpInputVars, tmpOutputVars, tmpInnerVars, uniqueEqIndex) = generateSingleEquation(listHead(eqnlst.clone())?, listHead(varlst.clone())?, shared.functionTree.clone(), shared.timeInterval.clone(), uniqueEqIndex.clone())?;
            ()
        },
        Deref @ BackendDAE::StrongComponent::SINGLECOMPLEXEQUATION { .. } => {
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(component.clone(), constSyst.orderedEqs.clone(), constSyst.orderedVars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (tmpEqns, uniqueEqIndex, _) = createSingleComplexEqnCode(listHead(eqnlst.clone())?, varlst.clone(), uniqueEqIndex.clone(), metamodelica::nil(), shared.info.clone(), true, shared.functionTree.clone(), clockIndex.clone())?;
            ()
        },
        Deref @ BackendDAE::StrongComponent::SINGLEALGORITHM { .. } => {
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(component.clone(), constSyst.orderedEqs.clone(), constSyst.orderedVars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (tmpEqns, uniqueEqIndex) = createSingleAlgorithmCode(eqnlst.clone(), varlst.clone(), false, uniqueEqIndex.clone(), clockIndex.clone())?;
            ()
        },
        Deref @ BackendDAE::StrongComponent::SINGLEARRAY { .. } => {
            let mut varlst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut eqnlst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            (eqnlst, varlst, _) = BackendDAETransform::getEquationAndSolvedVar(component.clone(), constSyst.orderedEqs.clone(), constSyst.orderedVars.clone())?;
            varlst = List::map(varlst.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (tmpEqns, _, uniqueEqIndex, _) = createSingleArrayEqnCode(true, eqnlst.clone(), varlst.clone(), uniqueEqIndex.clone(), metamodelica::nil(), shared.clone())?;
            ()
        },
        Deref @ BackendDAE::StrongComponent::TORNSYSTEM { mixedSystem, linear, strictTearingSet: BackendDAE::TearingSet { jac: jacobian, innerEquations, residualequations: residualEqns, tearingvars: tearingVars }, .. } => {
            let mut tvars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut loopIterationVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut loopSolvedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut reqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut algSystem: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut simequations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut omsiFunction: Arc<SimCode::OMSIFunction> = Arc::new(<SimCode::OMSIFunction as ::std::default::Default>::default());
            let mut derivativeMatrix: Option<Arc<SimCode::DerivativeMatrix>> = None;
            let mut algEqIndex: i32 = 0;
            if !(SymbolicJacobian::isJacobianGeneric(jacobian.clone())) && linear.clone() {
                Error::addMessage(Error::NO_JACONIAN_TORNLINEAR_SYSTEM.clone(), metamodelica::nil())?;
                bail!("fail");
            }
            algEqIndex = uniqueEqIndex.clone();
            uniqueEqIndex = uniqueEqIndex.clone() + 1;
            tvars = List::map1r(tearingVars.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), constSyst.orderedVars.clone())?;
            tvars = List::map(tvars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            tvars = BackendVariable::setVarsKind(tvars.clone(), openmodelica_backend_types::BackendDAE::VarKind::LOOP_ITERATION)?;
            (loopIterationVars, _) = List::fold(tvars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvarFold) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)> + 'static>), (metamodelica::nil(), BackendVariable::emptyVars(0)))?;
            loopIterationVars = loopIterationVars.clone().reverse();
            (simequations, loopSolvedVars, uniqueEqIndex) = generateInnerEqns(innerEquations.clone(), constSyst.clone(), shared.clone(), uniqueEqIndex.clone())?;
            reqns = BackendEquation::getList(residualEqns.clone(), constSyst.orderedEqs.clone())?;
            reqns = BackendEquation::replaceDerOpInEquationList(reqns.clone())?;
            let (__pa0, (__pa1, _), __pa2) = createNonlinearResidualEquations(reqns.clone(), (uniqueEqIndex.clone(), 0), metamodelica::nil(), shared.functionTree.clone())?;
            resEqs = __pa0.clone();
            uniqueEqIndex = __pa1.clone();
            tempVars = __pa2.clone();
            resEqs = fixNonlinearResidualIndices(resEqs.clone());
            eqs = listAppend(simequations.clone(), resEqs.clone());
            (loopIterationVars, index) = rewriteIndex(loopIterationVars.clone(), 0);
            (loopSolvedVars, index) = rewriteIndex(loopSolvedVars.clone(), index.clone());
            nAllVars = (loopIterationVars.clone().len() as i32) + (loopSolvedVars.clone().len() as i32) + (tempVars.clone().len() as i32);
            hashTable = fillLocalHashTable(list![loopIterationVars.clone(), loopSolvedVars.clone(), tempVars.clone()], nAllVars.clone())?;
            if debug.clone() {
                println!("{}", (literal!("Function SimCodeUtil.generateEquationsForComponentsAlgSystem:\n")).clone());
                dumpVarLst(loopIterationVars.clone(), (literal!("AlgSystem loopIterationVars")).clone())?;
                dumpVarLst(loopSolvedVars.clone(), (literal!("AlgSystem loopSolvedVars")).clone())?;
            }
            tmpOutputVars = listAppend(loopIterationVars.clone(), loopSolvedVars.clone());
            omsiFunction = Arc::new(SimCode::OMSIFunction { nAlgebraicSystems: 0, context: SimCodeFunction::Context::OMSI_CONTEXT { hashTable: Some(hashTable.clone()) }, nAllVars: nAllVars.clone(), innerVars: tempVars.clone(), outputVars: tmpOutputVars.clone(), inputVars: metamodelica::nil(), equations: eqs.clone() });
            (derivativeMatrix, uniqueEqIndex) = createDerivativeMatrix(jacobian.clone(), uniqueEqIndex.clone())?;
            algSystem = Arc::new(SimCode::SimEqSystem::SES_ALGEBRAIC_SYSTEM { eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone(), sources: metamodelica::nil(), zeroCrossingConditions: metamodelica::nil(), matrix: derivativeMatrix.clone(), residual: omsiFunction.clone(), linearSystem: linear.clone(), tornSystem: true, partOfMixed: mixedSystem.clone(), dim_n: (tvars.clone().len() as i32), algSysIndex: nAlgebraicSystems.clone(), index: algEqIndex.clone() });
            nAlgebraicSystems = nAlgebraicSystems.clone() + 1;
            tmpEqns = list![algSystem.clone()];
            ()
        },
        Deref @ BackendDAE::StrongComponent::EQUATIONSYSTEM { mixedSystem, jac: jacobian, vars: variables, eqns, .. } => {
            let mut tvars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut loopSolvedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut reqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut algSystem: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut omsiFunction: Arc<SimCode::OMSIFunction> = Arc::new(<SimCode::OMSIFunction as ::std::default::Default>::default());
            let mut derivativeMatrix: Option<Arc<SimCode::DerivativeMatrix>> = None;
            let mut algEqIndex: i32 = 0;
            algEqIndex = uniqueEqIndex.clone();
            uniqueEqIndex = uniqueEqIndex.clone() + 1;
            tvars = List::map1r(variables.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), constSyst.orderedVars.clone())?;
            tvars = List::map(tvars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::transformXToXd, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<BackendDAE::Var> + 'static>))?;
            (loopSolvedVars, _) = List::fold(tvars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvarFold) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)> + 'static>), (metamodelica::nil(), BackendVariable::emptyVars(0)))?;
            loopSolvedVars = loopSolvedVars.clone().reverse();
            reqns = BackendEquation::getList(eqns.clone(), constSyst.orderedEqs.clone())?;
            reqns = BackendEquation::replaceDerOpInEquationList(reqns.clone())?;
            let (__pa0, (__pa1, _), __pa2) = createNonlinearResidualEquations(reqns.clone(), (uniqueEqIndex.clone(), 0), metamodelica::nil(), shared.functionTree.clone())?;
            resEqs = __pa0.clone();
            uniqueEqIndex = __pa1.clone();
            tempVars = __pa2.clone();
            resEqs = fixNonlinearResidualIndices(resEqs.clone());
            (loopSolvedVars, index) = rewriteIndex(loopSolvedVars.clone(), 0);
            nAllVars = (loopSolvedVars.clone().len() as i32) + (tempVars.clone().len() as i32);
            hashTable = fillLocalHashTable(list![loopSolvedVars.clone(), tempVars.clone()], nAllVars.clone())?;
            omsiFunction = Arc::new(SimCode::OMSIFunction { nAlgebraicSystems: 0, context: SimCodeFunction::Context::OMSI_CONTEXT { hashTable: Some(hashTable.clone()) }, nAllVars: nAllVars.clone(), innerVars: tempVars.clone(), outputVars: loopSolvedVars.clone(), inputVars: metamodelica::nil(), equations: resEqs.clone() });
            (derivativeMatrix, uniqueEqIndex) = createDerivativeMatrix(jacobian.clone(), uniqueEqIndex.clone())?;
            algSystem = Arc::new(SimCode::SimEqSystem::SES_ALGEBRAIC_SYSTEM { eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone(), sources: metamodelica::nil(), zeroCrossingConditions: metamodelica::nil(), matrix: derivativeMatrix.clone(), residual: omsiFunction.clone(), linearSystem: false, tornSystem: false, partOfMixed: mixedSystem.clone(), dim_n: (tvars.clone().len() as i32), algSysIndex: nAlgebraicSystems.clone(), index: algEqIndex.clone() });
            nAlgebraicSystems = nAlgebraicSystems.clone() + 1;
            tmpEqns = list![algSystem.clone()];
            if debug.clone() {
                dumpOMSIFunc(omsiFunction.clone(), (literal!("\nEquation system omsiFunction")).clone());
            }
            ()
        },
        _ => {
            Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!(" - case for component ")); __mm_s.push_str(&*BackendDump::printComponent(component.clone(), None)?); __mm_s.push_str(&*literal!(" not implemented in SimCodeUtil.createAllEquationOMSI")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
            bail!("fail");
            ()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
        equations = listAppend(tmpEqns.clone(), equations.clone());
        inputVars = listAppend(tmpInputVars.clone(), inputVars.clone());
        outputVars = listAppend(tmpOutputVars.clone(), outputVars.clone());
        innerVars = listAppend(tmpInnerVars.clone(), innerVars.clone());
    }
    (inputVars, index) = rewriteIndex(inputVars.clone(), 0);
    (innerVars, index) = rewriteIndex(innerVars.clone(), index.clone());
    (outputVars, index) = rewriteIndex(outputVars.clone(), index.clone());
    omsiFuncEquations = Arc::new(SimCode::OMSIFunction { nAlgebraicSystems: nAlgebraicSystems.clone(), context: SimCodeFunction::Context::OMSI_CONTEXT { hashTable: None }, nAllVars: nAllVars.clone(), innerVars: innerVars.clone(), outputVars: outputVars.clone(), inputVars: inputVars.clone(), equations: equations.clone().reverse() });
    if debug.clone() {
        println!("{}", (literal!("Function SimCodeUtil.generateEquationsForComponentsAlgSystem:\n")).clone());
        dumpVarLst(inputVars.clone(), (literal!("InputVars")).clone())?;
        dumpVarLst(innerVars.clone(), (literal!("InnerVars")).clone())?;
        dumpVarLst(outputVars.clone(), (literal!("OutputVars")).clone())?;
    }
    Ok((omsiFuncEquations, uniqueEqIndex))
}

fn generateSingleEquation(mut eqn: Arc<BackendDAE::Equation>, mut var: BackendDAE::Var, mut funcTree: Arc<AvlTreePathFunction::Tree>, mut timeInterval: Option<Arc<DAE::Exp>>, mut uniqueEqIndex: i32) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32)> {
    let mut equations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut inputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut outputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut innerVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut uniqueEqIndex: i32 = uniqueEqIndex;
    let debug: bool = false;
    let () = (::match_deref::match_deref! { match &(eqn.clone()) {
        Deref @ BackendDAE::Equation::EQUATION { attr: eqAttr, source, scalar: rhs, exp: lhs } => {
            let mut resolvedExp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut varExp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut tmpSimEqLst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut newSimVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut solveEqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut asserts: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
            let mut solveCr: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut source = (*source).clone();
            cr = var.varName.clone();
            varExp = Expression::crefToExp(cr.clone())?;
            if BackendVariable::isStateVar(var.clone()) {
                varExp = Expression::expDer(varExp.clone());
                cr = ComponentReference::crefPrefixDer(cr.clone());
            }
            match '__try0: {
                (resolvedExp, asserts, solveEqns, solveCr) = unwrap_break_err!(ExpressionSolve::solve2(lhs.clone(), rhs.clone(), varExp.clone(), Some(funcTree.clone()), Some(uniqueEqIndex.clone()), true, true), '__try0);
                (eqs, uniqueEqIndex) = unwrap_break_err!(List::mapFold(solveEqns.clone().reverse(), (std::sync::Arc::new(makeSolved) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone()), '__try0);
                innerVars = unwrap_break_err!(createTempVarsforCrefs(unwrap_break_err!(List::map(solveCr.clone().reverse(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>)), '__try0), metamodelica::nil()), '__try0);
                source = unwrap_break_err!(ElementSource::addSymbolicTransformationSolve(true, source.clone(), cr.clone(), lhs.clone(), rhs.clone(), resolvedExp.clone(), asserts.clone()), '__try0);
                (tmpSimEqLst, uniqueEqIndex) = addAssertEqn(asserts.clone(), list![Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: uniqueEqIndex.clone(), cref: cr.clone(), exp: resolvedExp.clone(), source: source.clone(), eqAttr: eqAttr.clone() })], uniqueEqIndex.clone() + 1);
                equations = listAppend(eqs.clone(), tmpSimEqLst.clone());
                newSimVar = unwrap_break_err!(dlowvarToSimvar(var.clone(), None, BackendVariable::emptyVars(0), None), '__try0);
                if debug.clone() {
                    println!("{}", (literal!("generateSingleEquation:\n")).clone());
                    unwrap_break_err!(dumpSimEqSystemLst(tmpSimEqLst.clone(), (literal!("\n")).clone()), '__try0);
                    unwrap_break_err!(dumpVarLst(list![newSimVar.clone()], (literal!("newSimVar")).clone()), '__try0);
                }
                if BackendVariable::isStateVar(var.clone()) {
                    outputVars = listAppend(list![unwrap_break_err!(derVarFromStateVar(newSimVar.clone(), timeInterval.clone(), None), '__try0)], outputVars.clone());
                    inputVars = listAppend(list![newSimVar.clone()], inputVars.clone());
                } else {
                    outputVars = listAppend(list![newSimVar.clone()], outputVars.clone());
                }
                Ok::<_, anyhow::Error>((asserts.clone(), eqs.clone(), equations.clone(), innerVars.clone(), newSimVar.clone(), outputVars.clone(), resolvedExp.clone(), solveCr.clone(), solveEqns.clone(), source.clone(), tmpSimEqLst.clone(), uniqueEqIndex.clone()))
            } {
                Ok((__try0_o0, __try0_o1, __try0_o2, __try0_o3, __try0_o4, __try0_o5, __try0_o6, __try0_o7, __try0_o8, __try0_o9, __try0_o10, __try0_o11)) => {
                    asserts = __try0_o0;
                    eqs = __try0_o1;
                    equations = __try0_o2;
                    innerVars = __try0_o3;
                    newSimVar = __try0_o4;
                    outputVars = __try0_o5;
                    resolvedExp = __try0_o6;
                    solveCr = __try0_o7;
                    solveEqns = __try0_o8;
                    source = __try0_o9;
                    tmpSimEqLst = __try0_o10;
                    uniqueEqIndex = __try0_o11;
                }
                Err(__try0_err) => {
                    Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("- ")); __mm_s.push_str(&*BackendDump::equationString(eqn.clone())?); __mm_s.push_str(&*literal!(" could not resolved for ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cr.clone())?); __mm_s.push_str(&*literal!(" in SimCodeUtil.generateSingleEquation")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
                    return Err(__try0_err);
                }
            }
            ()
        },
        Deref @ BackendDAE::Equation::WHEN_EQUATION { attr: eqAttr, source, whenEquation, .. } => {
            let mut cond: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut whenStmtLst: Arc<metamodelica::List<BackendDAE::WhenOperator>> = metamodelica::nil();
            let mut oelseWhen: Option<Arc<BackendDAE::WhenEquation>> = None;
            let mut tmpSimEqLst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut newSimVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut conditions: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut initialCall: bool = false;
            let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(whenEquation.clone()) {
                Deref @ BackendDAE::WhenEquation { condition: __pa0, whenStmtLst: __pa1, elsewhenPart: __pa2 } => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                _ => bail!("pattern mismatch"),
            } };
            cond = __pa0.clone();
            whenStmtLst = __pa1.clone();
            oelseWhen = __pa2.clone();
            if isSome(oelseWhen.clone()) {
                Error::addInternalError((literal!("Else when equation not implemented in SimCodeUtil.generateSingleEquation")).clone(), metamodelica::sourceInfo!())?;
                bail!("fail");
            }
            (conditions, initialCall) = BackendDAEUtil::getConditionList(cond.clone())?;
            tmpSimEqLst = list![Arc::new(SimCode::SimEqSystem::SES_WHEN { index: uniqueEqIndex.clone(), conditions: conditions.clone(), initialCall: initialCall.clone(), whenStmtLst: whenStmtLst.clone(), elseWhen: None, source: source.clone(), eqAttr: eqAttr.clone() })];
            uniqueEqIndex = uniqueEqIndex.clone() + 1;
            newSimVar = dlowvarToSimvar(var.clone(), None, BackendVariable::emptyVars(0), None)?;
            if debug.clone() {
                println!("{}", (literal!("generateWhenEquation:\n")).clone());
                dumpSimEqSystemLst(tmpSimEqLst.clone(), (literal!("\n")).clone())?;
                dumpVarLst(list![newSimVar.clone()], (literal!("newSimVar")).clone())?;
            }
            equations = listAppend(equations.clone(), tmpSimEqLst.clone());
            outputVars = listAppend(list![newSimVar.clone()], outputVars.clone());
            ()
        },
        _ => {
            let mut r#str: ArcStr = arcstr::literal!("");
            r#str = (BackendDump::equationString(eqn.clone())?).clone();
            Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("- ")); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!(" not implemented SimCodeUtil.generateSingleEquation")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
            bail!("fail");
            ()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((equations, inputVars, outputVars, innerVars, uniqueEqIndex))
}

fn generateInnerEqns(mut innerEquations: Arc<metamodelica::List<BackendDAE::InnerEquation>>, mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut uniqueEqIndex: i32) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32)> {
    let mut equations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut outputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut uniqueEqIndex: i32 = uniqueEqIndex;
    let mut eqnindx: i32 = 0;
    let mut vars: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut tmpOutputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut tmpVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut eqn: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
    let mut comp: Arc<BackendDAE::StrongComponent> = Arc::new(<BackendDAE::StrongComponent as ::std::default::Default>::default());
    let mut dblLstEqns: DoubleEnded::MutableList<Arc<SimCode::SimEqSystem>> = <DoubleEnded::MutableList<Arc<SimCode::SimEqSystem>> as ::std::default::Default>::default();
    let mut omsiFuncEquations: Arc<SimCode::OMSIFunction> = Arc::new(<SimCode::OMSIFunction as ::std::default::Default>::default());
    dblLstEqns = DoubleEnded::fromList(equations.clone())?;
    for mut eq in &*innerEquations.clone() {
        let mut eq = eq.clone();
        (eqnindx, vars, _) = BackendDAEUtil::getEqnAndVarsFromInnerEquation(eq.clone())?;
        tmpVars = List::map1r(vars.clone(), (std::sync::Arc::new(BackendVariable::getVarAt) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, i32) -> Result<BackendDAE::Var> + 'static>), syst.orderedVars.clone())?;
        tmpVars = BackendVariable::setVarsKind(tmpVars.clone(), openmodelica_backend_types::BackendDAE::VarKind::LOOP_SOLVED)?;
        (tmpOutputVars, _) = List::fold(tmpVars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvarFold) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)> + 'static>), (metamodelica::nil(), BackendVariable::emptyVars(0)))?;
        outputVars = List::append_reverse(tmpOutputVars.clone(), outputVars.clone());
        eqn = BackendEquation::get(syst.orderedEqs.clone(), eqnindx.clone())?;
        comp = createTornSystemInnerEqns1(eqn.clone(), eqnindx.clone(), vars.clone())?;
        (omsiFuncEquations, uniqueEqIndex) = generateEquationsForComponents(list![comp.clone()], syst.clone(), shared.clone(), uniqueEqIndex.clone())?;
        DoubleEnded::push_list_back(dblLstEqns.clone(), omsiFuncEquations.equations.clone());
    }
    outputVars = Dangerous::listReverseInPlace(outputVars.clone());
    equations = DoubleEnded::toListAndClear(dblLstEqns.clone(), metamodelica::nil());
    Ok((equations, outputVars, uniqueEqIndex))
}

fn appendOMSIFunction(mut omsiFunction_1: Arc<SimCode::OMSIFunction>, mut omsiFunction_2: Arc<SimCode::OMSIFunction>) -> Result<Arc<SimCode::OMSIFunction>> {
    let mut omsiFunction_1: Arc<SimCode::OMSIFunction> = omsiFunction_1;
    assign_field!(
        omsiFunction_1.equations = listAppend(omsiFunction_1.equations.clone(), omsiFunction_2.equations.clone()),
        omsiFunction_1.inputVars = listAppend(omsiFunction_1.inputVars.clone(), omsiFunction_2.inputVars.clone()),
        omsiFunction_1.outputVars = listAppend(omsiFunction_1.outputVars.clone(), omsiFunction_2.outputVars.clone()),
        omsiFunction_1.innerVars = listAppend(omsiFunction_1.innerVars.clone(), omsiFunction_2.innerVars.clone()),
        omsiFunction_1.nAllVars = omsiFunction_1.nAllVars.clone() + omsiFunction_2.nAllVars.clone(),
        omsiFunction_1.context = (match omsiFunction_1.context.clone() {
        SimCodeFunction::Context::OMSI_CONTEXT { hashTable: Some(mut hashTable) } => {
            hashTable = List::fold(omsiFunction_2.inputVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), hashTable.clone())?;
            for mut simVar in &*omsiFunction_2.outputVars.clone() {
                let mut simVar = simVar.clone();
                hashTable = HashTableCrefSimVar::addSimVarToHashTable(simVar.clone(), hashTable.clone())?;
            }
            for mut simVar in &*omsiFunction_2.innerVars.clone() {
                let mut simVar = simVar.clone();
                hashTable = HashTableCrefSimVar::addSimVarToHashTable(simVar.clone(), hashTable.clone())?;
            }
            SimCodeFunction::Context::OMSI_CONTEXT { hashTable: Some(hashTable.clone()) }
        },
        _ => bail!("match: no arm matched"),
    }),
        omsiFunction_1.nAlgebraicSystems = omsiFunction_1.nAlgebraicSystems.clone() + omsiFunction_2.nAlgebraicSystems.clone()
    );
    Ok(omsiFunction_1)
}

fn fillLocalHashTable(mut varListList: Arc<metamodelica::List<Arc<metamodelica::List<SimCodeVar::SimVar>>>>, mut numberOfElements: i32) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> {
    let mut hashTable: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
    let mut sizeHT: i32 = 0;
    sizeHT = std::cmp::max(1013, Util::nextPrime(numberOfElements.clone() * 2));
    hashTable = HashTableCrefSimVar::emptyHashTableSized(sizeHT.clone());
    for mut simVarList in &*varListList.clone() {
        let mut simVarList = simVarList.clone();
        hashTable = List::fold(simVarList.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), hashTable.clone())?;
    }
    Ok(hashTable)
}

// =============================================================================
// section to create state set equations
//
// =============================================================================
pub fn createStateSets(mut inDAE: Arc<BackendDAE::BackendDAE>, mut iEquations: Arc<metamodelica::List<SimCode::StateSet>>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<BackendDAE::BackendDAE>, Arc<metamodelica::List<SimCode::StateSet>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32)> {
    let mut outDAE: Arc<BackendDAE::BackendDAE> = Arc::new(<BackendDAE::BackendDAE as ::std::default::Default>::default());
    let mut oEquations: Arc<metamodelica::List<SimCode::StateSet>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut numStateSets: i32 = 0;
    if Flags::getConfigString(Flags::INDEX_REDUCTION_METHOD.clone())? == literal!("dummyDerivatives") {
        outDAE = inDAE.clone();
        oEquations = iEquations.clone();
        ouniqueEqIndex = iuniqueEqIndex.clone();
        otempvars = itempvars.clone();
        numStateSets = 0;
    } else {
        let (__pa0, (__pa1, __pa2, __pa3, __pa4)) = BackendDAEUtil::mapEqSystemAndFold(inDAE.clone(), (std::sync::Arc::new(createStateSetsSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, (Arc<metamodelica::List<SimCode::StateSet>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32)) -> Result<(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, (Arc<metamodelica::List<SimCode::StateSet>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32))> + 'static>), (iEquations.clone(), iuniqueEqIndex.clone(), itempvars.clone(), 0))?;
        outDAE = __pa0.clone();
        oEquations = __pa1.clone();
        ouniqueEqIndex = __pa2.clone();
        otempvars = __pa3.clone();
        numStateSets = __pa4.clone();
    }
    Ok((outDAE, oEquations, ouniqueEqIndex, otempvars, numStateSets))
}

fn createStateSetsSystem(mut isyst: Arc<BackendDAE::EqSystem>, mut inShared: Arc<BackendDAE::Shared>, mut inTpl: (Arc<metamodelica::List<SimCode::StateSet>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32)) -> Result<(Arc<BackendDAE::EqSystem>, Arc<BackendDAE::Shared>, (Arc<metamodelica::List<SimCode::StateSet>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32))> {
    let mut osyst: Arc<BackendDAE::EqSystem> = Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default());
    let mut outShared: Arc<BackendDAE::Shared> = inShared.clone();
    let mut outTpl: (Arc<metamodelica::List<SimCode::StateSet>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32) = (metamodelica::nil(), 0, metamodelica::nil(), 0);
    (osyst, outTpl) = (::match_deref::match_deref! { match &((isyst.clone(), inTpl.clone())) {
        (Deref @ BackendDAE::EqSystem { stateSets: Deref @ metamodelica::List::Nil, .. }, _) => {
            (isyst.clone(), inTpl.clone())
        },
        (syst @ Deref @ BackendDAE::EqSystem { stateSets, matching: Deref @ BackendDAE::Matching::MATCHING { comps, .. }, orderedEqs: eqns, orderedVars: vars, .. }, (equations, uniqueEqIndex, tempvars, numStateSets)) => {
            let mut equations = (*equations).clone();
            let mut uniqueEqIndex = (*uniqueEqIndex).clone();
            let mut tempvars = (*tempvars).clone();
            let mut numStateSets = (*numStateSets).clone();
            (equations, uniqueEqIndex, tempvars, numStateSets) = createStateSetsSets(stateSets.clone(), vars.clone(), eqns.clone(), comps.clone(), equations.clone(), uniqueEqIndex.clone(), tempvars.clone(), numStateSets.clone())?;
            (syst.clone(), (equations.clone(), uniqueEqIndex.clone(), tempvars.clone(), numStateSets.clone()))
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok((osyst, outShared, outTpl))
}

fn createStateSetsSets(mut iStateSets: Arc<metamodelica::List<BackendDAE::StateSet>>, mut iVars: BackendDAE::Variables, mut iEqns: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>>, mut comps: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>>, mut iEquations: Arc<metamodelica::List<SimCode::StateSet>>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut iNumStateSets: i32) -> Result<(Arc<metamodelica::List<SimCode::StateSet>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>, i32)> {
    let mut oEquations: Arc<metamodelica::List<SimCode::StateSet>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut oNumStateSets: i32 = 0;
    (oEquations, ouniqueEqIndex, otempvars, oNumStateSets) = 'mc: {
        let __mc_input = iStateSets.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Nil => {
                    Ok((iEquations.clone(), iuniqueEqIndex.clone(), itempvars.clone(), iNumStateSets.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: BackendDAE::StateSet { jacobian, statescandidates: statevars, varA: aVars, crA, state: crset, rang, .. }, tail: sets } => {
                    let mut numStateSets: i32 = 0;
                    let mut nCandidates: i32 = 0;
                    let mut crstates: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut jacobianMatrix: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
                    let mut simequations: Arc<metamodelica::List<SimCode::StateSet>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut simCodeAVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut crA = (*crA).clone();
                    crstates = List::map(statevars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    simCodeAVars = List::map3(aVars.clone(), (std::sync::Arc::new(dlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, Option<BackendDAE::Variables>, BackendDAE::Variables, Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>) -> Result<SimCodeVar::SimVar> + 'static>), None, iVars.clone(), None)?;
                    crA = ComponentReference::subscriptCrefWithInt(crA.clone(), 1)?;
                    crA = if (intGt((crset.clone().len() as i32), 1)) {ComponentReference::subscriptCrefWithInt(crA.clone(), 1)?} else {crA.clone()};
                    nCandidates = (statevars.clone().len() as i32);
                    let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(createSymbolicSimulationJacobian(jacobian.clone(), iuniqueEqIndex.clone(), listAppend(itempvars.clone(), simCodeAVars.clone()), false)?) {
                        (Some(__pa0), __pa1, __pa2) => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                        _ => bail!("pattern mismatch"),
                    } };
                    jacobianMatrix = __pa0.clone();
                    uniqueEqIndex = __pa1.clone();
                    tempvars = __pa2.clone();
                    (simequations, uniqueEqIndex, tempvars, numStateSets) = createStateSetsSets(sets.clone(), iVars.clone(), iEqns.clone(), comps.clone(), metamodelica::cons(SimCode::StateSet { index: iuniqueEqIndex.clone(), nCandidates: nCandidates.clone(), nStates: rang.clone(), states: crset.clone(), statescandidates: crstates.clone(), crA: crA.clone(), jacobianMatrix: jacobianMatrix.clone() }, iEquations.clone()), uniqueEqIndex.clone(), tempvars.clone(), iNumStateSets.clone() + 1)?;
                    Ok((simequations.clone(), uniqueEqIndex.clone(), tempvars.clone(), numStateSets.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut errorMessage: ArcStr = arcstr::literal!("");
                    errorMessage = (literal!("function createStateSetsSets failed.")).clone();
                    Error::addInternalError((errorMessage.clone()).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((oEquations, ouniqueEqIndex, otempvars, oNumStateSets))
}

// =============================================================================
// section to create SimCode symbolic jacobian from BackendDAE.Equations
//
// =============================================================================
pub fn createSymbolicSimulationJacobian(mut inJacobian: Arc<BackendDAE::Jacobian>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut detectNonlinearPattern: bool) -> Result<(Option<Arc<SimCode::JacobianMatrix>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut res: Option<Arc<SimCode::JacobianMatrix>> = None;
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (res, ouniqueEqIndex, otempvars) = ({
        let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
        let mut constantEqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
        'mc: {
        let __mc_input = (inJacobian.clone(), iuniqueEqIndex.clone(), itempvars.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Jacobian::EMPTY_JACOBIAN { .. }, _, _) => {
                    Ok((None, iuniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Jacobian::FULL_JACOBIAN { jacobian: _ }, _, _) => {
                    Ok((None, iuniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Jacobian::GENERIC_JACOBIAN { jacobian: None, sparsePattern: pattern @ (sparsepatternComRefs, sparsepatternComRefsT, (independentComRefs, dependentVarsComRefs), _), coloring: sparseColoring, nonlinearPattern: (nonlinearpatternComRefs, nonlinearpatternComRefsT, _, _) }, _, _) => {
                    let mut coloring: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
                    let mut sparseInts: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut sparseIntsT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut nonlinearPat: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut nonlinearPatT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut maxColor: i32 = 0;
                    let mut varsSeedIndex: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut indexVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("create sparse pattern for algebraic loop time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        BackendDump::dumpSparsityPattern(pattern.clone(), (literal!("---+++ SparsePattern +++---")).clone())?;
                    }
                    seedVars = ({
        let mut __acc: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
        for mut cr in (independentComRefs.clone()).into_iter().cloned() {
                    let __x = makeTmpRealSimCodeVar(cr.clone(), openmodelica_backend_types::BackendDAE::VarKind::SEED_VAR)?;
                    __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                    indexVars = ({
        let mut __acc: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
        for mut cr in (dependentVarsComRefs.clone()).into_iter().cloned() {
                    let __x = makeTmpRealSimCodeVar(cr.clone(), openmodelica_backend_types::BackendDAE::VarKind::VARIABLE)?;
                    __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                    (seedVars, _) = rewriteIndex(seedVars.clone(), 0);
                    (indexVars, _) = rewriteIndex(indexVars.clone(), 0);
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", (literal!("\n---+++ seedVars variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), seedVars.clone())?).clone());
                        println!("{}", (literal!("\n---+++ indexVars variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), indexVars.clone())?).clone());
                    }
                    varsSeedIndex = listAppend(seedVars.clone(), indexVars.clone());
                    sparseInts = sortSparsePattern(varsSeedIndex.clone(), sparsepatternComRefs.clone(), false)?;
                    sparseIntsT = sortSparsePattern(varsSeedIndex.clone(), sparsepatternComRefsT.clone(), false)?;
                    nonlinearPat = sortSparsePattern(varsSeedIndex.clone(), nonlinearpatternComRefs.clone(), false)?;
                    nonlinearPatT = sortSparsePattern(varsSeedIndex.clone(), nonlinearpatternComRefsT.clone(), false)?;
                    coloring = sortColoring(seedVars.clone(), sparseColoring.clone())?;
                    maxColor = (sparseColoring.clone().len() as i32);
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("created sparse pattern for algebraic loop time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    Ok((Some(Arc::new(SimCode::JacobianMatrix { columns: metamodelica::nil(), seedVars: metamodelica::nil(), matrixName: (literal!("")).clone(), sparsity: sparseInts.clone(), sparsityT: sparseIntsT.clone(), nonlinear: nonlinearPat.clone(), nonlinearT: nonlinearPatT.clone(), coloredCols: coloring.clone(), coloredRows: metamodelica::nil(), maxColorCols: maxColor.clone(), jacobianIndex: -1, partitionIndex: 0, generic_loop_calls: metamodelica::nil(), crefsHT: None, isAdjoint: false })), iuniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Jacobian::GENERIC_JACOBIAN { jacobian: Some((Deref @ BackendDAE::BackendDAE { shared, eqs: systs }, name, independentVarsLst, residualVarsLst, dependentVarsLst, _)), sparsePattern: (sparsepatternComRefs, sparsepatternComRefsT, _, _), coloring: sparseColoring, nonlinearPattern: (nonlinearpatternComRefs, nonlinearpatternComRefsT, _, _) }, uniqueEqIndex, tempvars) => {
                    let mut emptyVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut independentVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut residualVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut systvars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut allVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
                    let mut x: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut coloring: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
                    let mut sparseInts: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut sparseIntsT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut nonlinearPat: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut nonlinearPatT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut dummyVar: ArcStr = arcstr::literal!("");
                    let mut maxColor: i32 = 0;
                    let mut nRows: i32 = 0;
                    let mut columnVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut varsSeedIndex: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut indexVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut crefToSimVarHTJacobian: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
                    let mut uniqueEqIndex = (*uniqueEqIndex).clone();
                    let mut tempvars = (*tempvars).clone();
                    dummyVar = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("dummyVar")); __mm_s.push_str(&*name.clone()); ArcStr::from(__mm_s) }).clone();
                    x = Arc::new(DAE::ComponentRef::CREF_IDENT { ident: (dummyVar.clone()).clone(), identType: DAE::T_REAL_DEFAULT().clone(), subscriptLst: metamodelica::nil() });
                    emptyVars = BackendVariable::emptyVars(BaseHashTable::bigBucketSize.clone());
                    residualVars = BackendVariable::listVar1(residualVarsLst.clone())?;
                    independentVars = BackendVariable::listVar1(independentVarsLst.clone())?;
                    (allVars, _) = BackendVariable::traverseBackendDAEVars(BackendVariable::listVar1(BackendVariable::equationSystemsVarsLst(systs.clone())?)?, (std::sync::Arc::new(getFurtherVars) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>))> + 'static>), (metamodelica::nil(), x.clone()))?;
                    systvars = BackendVariable::listVar1(allVars.clone())?;
                    (columnVars, _) = BackendVariable::traverseBackendDAEVars(systvars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone()))?;
                    columnVars = List::map1(columnVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarKind, SimCodeVar::SimVar, BackendDAE::VarKind)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::JAC_TMP_VAR)?;
                    columnVars = List::map1(columnVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarMatrixName, SimCodeVar::SimVar, Option<ArcStr>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Option<ArcStr>) -> Result<SimCodeVar::SimVar> + 'static>), Some((name.clone()).clone()))?;
                    (columnVars, _) = rewriteIndex(columnVars.clone(), 0);
                    columnVars = createAllDiffedSimVars(dependentVarsLst.clone(), x.clone(), residualVars.clone(), 0, (columnVars.clone().len() as i32), (name.clone()).clone(), columnVars.clone())?;
                    columnVars = columnVars.clone().reverse();
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", (literal!("\n---+++ all column variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), columnVars.clone())?).clone());
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> create all SimCode vars for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    (seedVars, _) = BackendVariable::traverseBackendDAEVars(independentVars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone()))?;
                    (indexVars, _) = BackendVariable::traverseBackendDAEVars(residualVars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone()))?;
                    (seedVars, _) = rewriteIndex(seedVars.clone().reverse(), 0);
                    (indexVars, _) = rewriteIndex(indexVars.clone().reverse(), 0);
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", (literal!("\n---+++ seedVars variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), seedVars.clone())?).clone());
                        println!("{}", (literal!("\n---+++ indexVars variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), indexVars.clone())?).clone());
                    }
                    varsSeedIndex = listAppend(seedVars.clone(), indexVars.clone());
                    sparseInts = sortSparsePattern(varsSeedIndex.clone(), sparsepatternComRefs.clone(), false)?;
                    sparseIntsT = sortSparsePattern(varsSeedIndex.clone(), sparsepatternComRefsT.clone(), false)?;
                    if detectNonlinearPattern.clone() {
                        nonlinearPat = sortSparsePattern(varsSeedIndex.clone(), nonlinearpatternComRefs.clone(), false)?;
                        nonlinearPatT = sortSparsePattern(varsSeedIndex.clone(), nonlinearpatternComRefsT.clone(), false)?;
                    } else {
                        nonlinearPat = metamodelica::nil();
                        nonlinearPatT = metamodelica::nil();
                    }
                    coloring = sortColoring(varsSeedIndex.clone(), sparseColoring.clone())?;
                    maxColor = (sparseColoring.clone().len() as i32);
                    nRows = (residualVarsLst.clone().len() as i32);
                    seedVars = replaceSeedVarsName(seedVars.clone(), (name.clone()).clone())?;
                    seedVars = List::map1(seedVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarKind, SimCodeVar::SimVar, BackendDAE::VarKind)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::SEED_VAR)?;
                    seedVars = List::map1(seedVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarMatrixName, SimCodeVar::SimVar, Option<ArcStr>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Option<ArcStr>) -> Result<SimCodeVar::SimVar> + 'static>), Some((name.clone()).clone()))?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> transformed to SimCode for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    crefToSimVarHTJacobian = HashTableCrefSimVar::emptyHashTableSized((seedVars.clone().len() as i32) + (columnVars.clone().len() as i32));
                    crefToSimVarHTJacobian = List::fold(seedVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), crefToSimVarHTJacobian.clone())?;
                    crefToSimVarHTJacobian = List::fold(columnVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), crefToSimVarHTJacobian.clone())?;
                    (allEquations, constantEqns, uniqueEqIndex, tempvars) = getSimEqSystemForJacobians(systs.clone(), shared.clone(), uniqueEqIndex.clone(), tempvars.clone())?;
                    Ok((Some(Arc::new(SimCode::JacobianMatrix { columns: list![Arc::new(SimCode::JacobianColumn { columnEqns: allEquations.clone(), columnVars: columnVars.clone(), numberOfResultVars: nRows.clone(), constantEqns: constantEqns.clone() })], seedVars: seedVars.clone(), matrixName: (name.clone()).clone(), sparsity: sparseInts.clone(), sparsityT: sparseIntsT.clone(), nonlinear: nonlinearPat.clone(), nonlinearT: nonlinearPatT.clone(), coloredCols: coloring.clone(), coloredRows: metamodelica::nil(), maxColorCols: maxColor.clone(), jacobianIndex: -1, partitionIndex: 0, generic_loop_calls: metamodelica::nil(), crefsHT: Some(crefToSimVarHTJacobian.clone()), isAdjoint: false })), uniqueEqIndex.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut errorMessage: ArcStr = arcstr::literal!("");
                    if Flags::isSet(Flags::JAC_DUMP.clone())? {
                        errorMessage = (literal!("function createSymbolicSimulationJacobian failed.")).clone();
                        Error::addInternalError((errorMessage.clone()).clone(), metamodelica::sourceInfo!())?;
                    }
                    Ok((None, iuniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    }
    });
    Ok((res, ouniqueEqIndex, otempvars))
}

pub fn syncDAEandSimJac(mut symJac: Arc<SimCode::JacobianMatrix>, mut daeJac: Arc<SimCode::JacobianMatrix>) -> Result<Arc<SimCode::JacobianMatrix>> {
    let mut daeJac: Arc<SimCode::JacobianMatrix> = daeJac;
    if symJac.matrixName.clone() == literal!("A") {
        assign_field!(
            daeJac.columns = ({
        let mut __acc: Arc<metamodelica::List<Arc<SimCode::JacobianColumn>>> = metamodelica::nil();
        for mut col in (symJac.columns.clone()).into_iter().cloned() {
            let __x = updateSimVarIndex(col.clone(), daeJac.crefsHT.clone())?;
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }),
            daeJac.jacobianIndex = symJac.jacobianIndex.clone()
        );
    } else {
        daeJac = symJac.clone();
    }
    Ok(daeJac)
}

pub fn updateSimVarIndex(mut column: Arc<SimCode::JacobianColumn>, mut crefsHT: Option<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))>) -> Result<Arc<SimCode::JacobianColumn>> {
    let mut column: Arc<SimCode::JacobianColumn> = column;
    column = (match crefsHT.clone() {
        Some(mut table) => {
            assign_field!(column.columnVars = ({
        let mut __acc: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
        for mut var in (column.columnVars.clone()).into_iter().cloned() {
            let __x = BaseHashTable::getOrDefault(var.name.clone(), table.clone(), var.clone())?;
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }));
            column.clone()
        },
        _ => {
            column.clone()
        },
    });
    Ok(column)
}

fn getFurtherVars(mut v: BackendDAE::Var, mut inTpl: (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>))> {
    let mut outVar: BackendDAE::Var = v.clone();
    let mut outTpl: (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>) = inTpl.clone();
    let mut diffCref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut vars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut b: bool = false;
    (vars, diffCref) = inTpl.clone();
    b = ComponentReferenceBasics::crefLastIdentEqual(BackendVariable::varCref(v.clone())?, diffCref.clone())?;
    if !(b.clone()) {
        vars = metamodelica::cons(v.clone(), vars.clone());
        outTpl = (vars.clone(), diffCref.clone());
    }
    Ok((outVar, outTpl))
}

fn getSimEqSystemForJacobians(mut inSysts: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>>, mut inShared: Arc<BackendDAE::Shared>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut constantEqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut uniqueEqIndex: i32 = iuniqueEqIndex.clone();
    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = itempvars.clone();
    let mut comps: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>> = metamodelica::nil();
    let mut rest: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let mut syst: Arc<BackendDAE::EqSystem> = Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default());
    let mut accEqns: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let (__pa0, __pa1) = ::match_deref::match_deref! { match &(inSysts.clone()) {
        Deref @ metamodelica::List::Cons { head: __pa0, tail: __pa1 } => (__pa0.clone(), __pa1.clone()),
        _ => bail!("pattern mismatch"),
    } };
    syst = __pa0.clone();
    rest = __pa1.clone();
    let __pa2 = ::match_deref::match_deref! { match &(syst.clone()) {
        Deref @ BackendDAE::EqSystem { matching: Deref @ BackendDAE::Matching::MATCHING { comps: __pa2, .. }, .. } => __pa2.clone(),
        _ => bail!("pattern mismatch"),
    } };
    comps = __pa2.clone();
    (allEquations, _, uniqueEqIndex, tempvars) = createEquations(false, false, true, false, syst.clone(), inShared.clone(), comps.clone(), uniqueEqIndex.clone(), tempvars.clone())?;
    for mut eq in &*rest.clone() {
        let mut eq = eq.clone();
        let __pa4 = ::match_deref::match_deref! { match &(eq.clone()) {
            Deref @ BackendDAE::EqSystem { matching: Deref @ BackendDAE::Matching::MATCHING { comps: __pa4, .. }, .. } => __pa4.clone(),
            _ => bail!("pattern mismatch"),
        } };
        comps = __pa4.clone();
        (constantEqns, _, uniqueEqIndex, tempvars) = createEquations(false, false, true, false, eq.clone(), inShared.clone(), comps.clone(), uniqueEqIndex.clone(), tempvars.clone())?;
        accEqns = metamodelica::cons(constantEqns.clone(), accEqns.clone());
    }
    constantEqns = List::flattenReverse(accEqns.clone())?;
    Ok((allEquations, constantEqns, uniqueEqIndex, tempvars))
}

pub fn createJacobianLinearCode(mut inSymjacs: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>>, mut inModelInfo: SimCode::ModelInfo, mut iuniqueEqIndex: i32, mut shared: Arc<BackendDAE::Shared>) -> Result<(Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>, i32)> {
    let mut res: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    (res, ouniqueEqIndex) = (match iuniqueEqIndex.clone() {
        _ => {
            let mut crefSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
            let mut matrixnames: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            let mut jacH: Option<Arc<BackendDAE::Jacobian>> = None;
            crefSimVarHT = createCrefToSimVarHT(inModelInfo.clone())?;
            if isSome(shared.dataReconciliationData.clone()) {
                let BackendDAE::DATA_RECON { symbolicJacobian: _, setcVars: _, datareconinputs: _, setBVars: _, symbolicJacobianH: __pa0, .. } = (Util::getOption(shared.dataReconciliationData.clone())?) else { bail!("pattern mismatch") };
                jacH = __pa0.clone();
                if isSome(jacH.clone()) {
                    matrixnames = list![(literal!("A")).clone(), (literal!("B")).clone(), (literal!("C")).clone(), (literal!("D")).clone(), (literal!("ADJ")).clone()];
                } else {
                    matrixnames = list![(literal!("A")).clone(), (literal!("B")).clone(), (literal!("C")).clone(), (literal!("D")).clone(), (literal!("H")).clone(), (literal!("ADJ")).clone()];
                }
            } else {
                matrixnames = list![(literal!("A")).clone(), (literal!("B")).clone(), (literal!("C")).clone(), (literal!("D")).clone(), (literal!("F")).clone(), (literal!("H")).clone(), (literal!("ADJ")).clone()];
            }
            (res, ouniqueEqIndex) = createSymbolicJacobianssSimCode(inSymjacs.clone(), crefSimVarHT.clone(), iuniqueEqIndex.clone(), matrixnames.clone(), metamodelica::nil())?;
            (res.clone(), ouniqueEqIndex.clone())
        },
    });
    Ok((res, ouniqueEqIndex))
}

fn checkForEmptyBDAE(mut inBDAE: Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>) -> bool {
    let mut result: bool = false;
    result = (::match_deref::match_deref! { match &(inBDAE.clone()) {
        None => true,
        Some((_, _, Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil, _)) => true,
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    result
}

pub fn createSymbolicJacobianssSimCode(mut inSymJacobians: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>>, mut inSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>)), mut iuniqueEqIndex: i32, mut inNames: Arc<metamodelica::List<ArcStr>>, mut inJacobianMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>, i32)> {
    let mut outJacobianMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    (outJacobianMatrices, ouniqueEqIndex) = ({
        let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
        let mut constantEqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
        'mc: {
        let __mc_input = (inSymJacobians.clone(), iuniqueEqIndex.clone(), inNames.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, _, Deref @ metamodelica::List::Nil) => {
                    Ok((inJacobianMatrices.clone(), iuniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Nil, _, Deref @ metamodelica::List::Cons { head: name, tail: restnames }) => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut linearModelMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
                    let mut tmpJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
                    tmpJac = SimCode::emptyJacobian().clone();
                    assign_field!(tmpJac.matrixName = name.clone());
                    linearModelMatrices = metamodelica::cons(tmpJac.clone(), inJacobianMatrices.clone());
                    (linearModelMatrices, uniqueEqIndex) = createSymbolicJacobianssSimCode(metamodelica::nil(), inSimVarHT.clone(), iuniqueEqIndex.clone(), restnames.clone(), linearModelMatrices.clone())?;
                    Ok((linearModelMatrices.clone(), uniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: (None, (Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil, (Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil), _), Deref @ metamodelica::List::Nil, _), tail: rest }, _, Deref @ metamodelica::List::Cons { head: name, tail: restnames }) => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut linearModelMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
                    let mut tmpJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
                    tmpJac = SimCode::emptyJacobian().clone();
                    assign_field!(tmpJac.matrixName = name.clone());
                    linearModelMatrices = metamodelica::cons(tmpJac.clone(), inJacobianMatrices.clone());
                    (linearModelMatrices, uniqueEqIndex) = createSymbolicJacobianssSimCode(rest.clone(), inSimVarHT.clone(), iuniqueEqIndex.clone(), restnames.clone(), linearModelMatrices.clone())?;
                    Ok((linearModelMatrices.clone(), uniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: (optionBDAE, (sparsepattern, sparsepatternT, (diffCompRefs, diffedCompRefs), _), colsColors, _), tail: rest }, _, Deref @ metamodelica::List::Cons { head: name, tail: restnames }) => {
                    if !((checkForEmptyBDAE(optionBDAE.clone()))) { bail!("guard") }
                    let mut uniqueEqIndex: i32 = 0;
                    let mut nRows: i32 = 0;
                    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut indexVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut seedIndexVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut maxColor: i32 = 0;
                    let mut linearModelMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
                    let mut sparseInts: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut sparseIntsT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut nonlinearPat: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut nonlinearPatT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut coloring: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
                    let mut tmpJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", (literal!("Start sparse pattern without analytical Jacobians\n")).clone());
                    }
                    seedVars = getSimVars2Crefs(diffCompRefs.clone(), inSimVarHT.clone());
                    seedVars = List::sort(seedVars.clone(), (std::sync::Arc::new(compareVarIndexGt) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, SimCodeVar::SimVar) -> Result<bool> + 'static>))?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("diffCrefs: ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefListStr(diffCompRefs.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        println!("{}", (literal!("\n---+++  seedVars +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), seedVars.clone())?).clone());
                    }
                    indexVars = getSimVars2Crefs(diffedCompRefs.clone(), inSimVarHT.clone());
                    indexVars = List::sort(indexVars.clone(), (std::sync::Arc::new(compareVarIndexGt) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, SimCodeVar::SimVar) -> Result<bool> + 'static>))?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("diffedCrefs: ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefListStr(diffedCompRefs.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        println!("{}", (literal!("\n---+++  indexVars +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), indexVars.clone())?).clone());
                        println!("{}", (literal!("\n---+++  sparse pattern vars +++---\n")).clone());
                        dumpSparsePattern(sparsepattern.clone())?;
                        println!("{}", (literal!("\n---+++  sparse pattern transpose +++---\n")).clone());
                        dumpSparsePattern(sparsepatternT.clone())?;
                    }
                    (seedVars, _) = rewriteIndex(seedVars.clone(), 0);
                    (indexVars, _) = rewriteIndex(indexVars.clone(), 0);
                    seedIndexVars = listAppend(seedVars.clone(), indexVars.clone());
                    sparseInts = sortSparsePattern(seedIndexVars.clone(), sparsepattern.clone(), false)?;
                    sparseIntsT = sortSparsePattern(seedIndexVars.clone(), sparsepatternT.clone(), false)?;
                    nonlinearPat = metamodelica::nil();
                    nonlinearPatT = metamodelica::nil();
                    maxColor = (colsColors.clone().len() as i32);
                    nRows = (diffedCompRefs.clone().len() as i32);
                    coloring = sortColoring(seedVars.clone(), colsColors.clone())?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> transformed to SimCode for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        println!("{}", (literal!("\n---+++  sparse pattern vars +++---\n")).clone());
                        dumpSparsePatternInt(sparseInts.clone())?;
                        println!("{}", (literal!("\n---+++  sparse pattern transpose +++---\n")).clone());
                        dumpSparsePatternInt(sparseIntsT.clone())?;
                    }
                    seedVars = replaceSeedVarsName(seedVars.clone(), (name.clone()).clone())?;
                    seedVars = List::map1(seedVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarKind, SimCodeVar::SimVar, BackendDAE::VarKind)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::SEED_VAR)?;
                    seedVars = List::map1(seedVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarMatrixName, SimCodeVar::SimVar, Option<ArcStr>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Option<ArcStr>) -> Result<SimCodeVar::SimVar> + 'static>), Some((name.clone()).clone()))?;
                    tmpJac = Arc::new(SimCode::JacobianMatrix { columns: list![Arc::new(SimCode::JacobianColumn { columnEqns: metamodelica::nil(), columnVars: metamodelica::nil(), numberOfResultVars: nRows.clone(), constantEqns: metamodelica::nil() })], seedVars: seedVars.clone(), matrixName: (name.clone()).clone(), sparsity: sparseInts.clone(), sparsityT: sparseIntsT.clone(), nonlinear: nonlinearPat.clone(), nonlinearT: nonlinearPatT.clone(), coloredCols: coloring.clone(), coloredRows: metamodelica::nil(), maxColorCols: maxColor.clone(), jacobianIndex: -1, partitionIndex: 0, generic_loop_calls: metamodelica::nil(), crefsHT: None, isAdjoint: false });
                    linearModelMatrices = metamodelica::cons(tmpJac.clone(), inJacobianMatrices.clone());
                    (linearModelMatrices, uniqueEqIndex) = createSymbolicJacobianssSimCode(rest.clone(), inSimVarHT.clone(), iuniqueEqIndex.clone(), restnames.clone(), linearModelMatrices.clone())?;
                    Ok((linearModelMatrices.clone(), uniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: (Some((Deref @ BackendDAE::BackendDAE { shared, eqs: systs }, name, _, diffedVars, alldiffedVars, _)), (sparsepattern, sparsepatternT, (diffCompRefs, diffedCompRefs), _), colsColors, (nonlinearpattern, nonlinearpatternT, _, _)), tail: rest }, uniqueEqIndex, Deref @ metamodelica::List::Cons { head: _, tail: restnames }) => {
                    let mut vars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut empty: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut systvars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut emptyVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut x: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut allVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
                    let mut allCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut nRows: i32 = 0;
                    let mut dummyVar: ArcStr = arcstr::literal!("");
                    let mut columnVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut otherColumnVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut indexVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut seedIndexVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut maxColor: i32 = 0;
                    let mut linearModelMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
                    let mut sparseInts: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut sparseIntsT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut nonlinearPat: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut nonlinearPatT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut coloring: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
                    let mut tmpJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
                    let mut crefToSimVarHTJacobian: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
                    let mut alldiffedVars = (*alldiffedVars).clone();
                    let mut uniqueEqIndex = (*uniqueEqIndex).clone();
                    dummyVar = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("dummyVar")); __mm_s.push_str(&*name.clone()); ArcStr::from(__mm_s) }).clone();
                    x = Arc::new(DAE::ComponentRef::CREF_IDENT { ident: (dummyVar.clone()).clone(), identType: DAE::T_REAL_DEFAULT().clone(), subscriptLst: metamodelica::nil() });
                    emptyVars = BackendVariable::emptyVars(BaseHashTable::bigBucketSize.clone());
                    (allVars, _) = BackendVariable::traverseBackendDAEVars(BackendVariable::listVar1(BackendVariable::equationSystemsVarsLst(systs.clone())?)?, (std::sync::Arc::new(getFurtherVars) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>))> + 'static>), (metamodelica::nil(), x.clone()))?;
                    systvars = BackendVariable::listVar1(allVars.clone())?;
                    (otherColumnVars, _) = BackendVariable::traverseBackendDAEVars(systvars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone()))?;
                    otherColumnVars = List::map1(otherColumnVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarKind, SimCodeVar::SimVar, BackendDAE::VarKind)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::JAC_TMP_VAR)?;
                    otherColumnVars = List::map1(otherColumnVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarMatrixName, SimCodeVar::SimVar, Option<ArcStr>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Option<ArcStr>) -> Result<SimCodeVar::SimVar> + 'static>), Some((name.clone()).clone()))?;
                    (otherColumnVars, _) = rewriteIndex(otherColumnVars.clone(), 0);
                    empty = BackendVariable::listVar1(alldiffedVars.clone())?;
                    allCrefs = List::map(alldiffedVars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    columnVars = getSimVars2Crefs(allCrefs.clone(), inSimVarHT.clone());
                    columnVars = List::sort(columnVars.clone(), (std::sync::Arc::new(compareVarIndexGt) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, SimCodeVar::SimVar) -> Result<bool> + 'static>))?;
                    alldiffedVars = ({
        let mut __acc: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
        for mut v in (columnVars.clone()).into_iter().cloned() {
                    let __x = sortBackVarWithSimVarsOrder(v.clone(), empty.clone())?;
                    __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                    vars = BackendVariable::listVar1(diffedVars.clone())?;
                    columnVars = createAllDiffedSimVars(alldiffedVars.clone(), x.clone(), vars.clone(), 0, (otherColumnVars.clone().len() as i32), (name.clone()).clone(), otherColumnVars.clone())?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", (literal!("\n---+++ second columnVars +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), columnVars.clone())?).clone());
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> create column variables for matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> create all SimCode vars for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        println!("{}", (literal!("\n---+++  columnVars +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), columnVars.clone())?).clone());
                    }
                    seedVars = getSimVars2Crefs(diffCompRefs.clone(), inSimVarHT.clone());
                    seedVars = List::sort(seedVars.clone(), (std::sync::Arc::new(compareVarIndexGt) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, SimCodeVar::SimVar) -> Result<bool> + 'static>))?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("diffCrefs: ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefListStr(diffCompRefs.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        println!("{}", (literal!("\n---+++  seedVars +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), seedVars.clone())?).clone());
                    }
                    indexVars = getSimVars2Crefs(diffedCompRefs.clone(), inSimVarHT.clone());
                    indexVars = List::sort(indexVars.clone(), (std::sync::Arc::new(compareVarIndexGt) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, SimCodeVar::SimVar) -> Result<bool> + 'static>))?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("diffedCrefs: ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefListStr(diffedCompRefs.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        println!("{}", (literal!("\n---+++  indexVars +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), indexVars.clone())?).clone());
                        println!("{}", (literal!("\n---+++  sparse pattern vars +++---\n")).clone());
                        dumpSparsePattern(sparsepattern.clone())?;
                        println!("{}", (literal!("\n---+++  sparse pattern transpose +++---\n")).clone());
                        dumpSparsePattern(sparsepatternT.clone())?;
                    }
                    (seedVars, _) = rewriteIndex(seedVars.clone(), 0);
                    (indexVars, _) = rewriteIndex(indexVars.clone(), 0);
                    seedIndexVars = listAppend(seedVars.clone(), indexVars.clone());
                    sparseIntsT = sortSparsePattern(seedIndexVars.clone(), sparsepatternT.clone(), false)?;
                    sparseInts = sortSparsePattern(seedIndexVars.clone(), sparsepattern.clone(), false)?;
                    nonlinearPat = sortSparsePattern(seedIndexVars.clone(), nonlinearpattern.clone(), false)?;
                    nonlinearPatT = sortSparsePattern(seedIndexVars.clone(), nonlinearpatternT.clone(), false)?;
                    maxColor = (colsColors.clone().len() as i32);
                    nRows = (diffedVars.clone().len() as i32);
                    coloring = sortColoring(seedVars.clone(), colsColors.clone())?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> transformed to SimCode for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        println!("{}", (literal!("\n---+++  sparse pattern vars +++---\n")).clone());
                        dumpSparsePatternInt(sparseInts.clone())?;
                        println!("{}", (literal!("\n---+++  sparse pattern transpose +++---\n")).clone());
                        dumpSparsePatternInt(sparseIntsT.clone())?;
                    }
                    seedVars = replaceSeedVarsName(seedVars.clone(), (name.clone()).clone())?;
                    seedVars = List::map1(seedVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarKind, SimCodeVar::SimVar, BackendDAE::VarKind)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::SEED_VAR)?;
                    seedVars = List::map1(seedVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarMatrixName, SimCodeVar::SimVar, Option<ArcStr>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Option<ArcStr>) -> Result<SimCodeVar::SimVar> + 'static>), Some((name.clone()).clone()))?;
                    crefToSimVarHTJacobian = HashTableCrefSimVar::emptyHashTableSized((seedVars.clone().len() as i32) + (columnVars.clone().len() as i32));
                    crefToSimVarHTJacobian = List::fold(seedVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), crefToSimVarHTJacobian.clone())?;
                    crefToSimVarHTJacobian = List::fold(columnVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), crefToSimVarHTJacobian.clone())?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> creating SimCode equations for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    (allEquations, constantEqns, uniqueEqIndex, _) = getSimEqSystemForJacobians(systs.clone(), shared.clone(), uniqueEqIndex.clone(), metamodelica::nil())?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> created all SimCode equations for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    tmpJac = Arc::new(SimCode::JacobianMatrix { columns: list![Arc::new(SimCode::JacobianColumn { columnEqns: allEquations.clone(), columnVars: columnVars.clone(), numberOfResultVars: nRows.clone(), constantEqns: constantEqns.clone() })], seedVars: seedVars.clone(), matrixName: (name.clone()).clone(), sparsity: sparseInts.clone(), sparsityT: sparseIntsT.clone(), nonlinear: nonlinearPat.clone(), nonlinearT: nonlinearPatT.clone(), coloredCols: coloring.clone(), coloredRows: metamodelica::nil(), maxColorCols: maxColor.clone(), jacobianIndex: -1, partitionIndex: 0, generic_loop_calls: metamodelica::nil(), crefsHT: Some(crefToSimVarHTJacobian.clone()), isAdjoint: false });
                    linearModelMatrices = metamodelica::cons(tmpJac.clone(), inJacobianMatrices.clone());
                    (linearModelMatrices, uniqueEqIndex) = createSymbolicJacobianssSimCode(rest.clone(), inSimVarHT.clone(), uniqueEqIndex.clone(), restnames.clone(), linearModelMatrices.clone())?;
                    Ok((linearModelMatrices.clone(), uniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("Generation of symbolic matrix SimCode (SimCodeUtil.createSymbolicJacobianssSimCode) failed")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    }
    });
    Ok((outJacobianMatrices, ouniqueEqIndex))
}

pub fn getSimVars2Crefs(mut inCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut inSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Arc<metamodelica::List<SimCodeVar::SimVar>> {
    let mut outSimVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    for mut cref in &*inCrefs.clone() {
        let mut cref = cref.clone();
        if '__try0: {
            outSimVars = metamodelica::cons(unwrap_break_err!(BaseHashTable::get(cref.clone(), inSimVarHT.clone()), '__try0), outSimVars.clone());
            Ok::<(), anyhow::Error>(())
        }.is_err() {
        }
    }
    outSimVars
}

fn replaceSeedVarsName(mut inVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut inMatrixName: ArcStr) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outSimVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = ({
        let mut __acc: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
        for mut v in (inVars.clone()).into_iter().cloned() {
            let __x = replaceSimVarName(Differentiate::createSeedCrefName(v.name.clone(), (inMatrixName.clone()).clone())?, v.clone())?;
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
    Ok(outSimVars)
}

fn sortBackVarWithSimVarsOrder(mut var: SimCodeVar::SimVar, mut vars: BackendDAE::Variables) -> Result<BackendDAE::Var> {
    let (mut outVar, _): (BackendDAE::Var, i32) = BackendVariable::getVarSingle(var.name.clone(), vars.clone())?;
    Ok(outVar)
}

fn createJacSimVarsColumn(mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut inCref: Arc<DAE::ComponentRef>, mut inAllVars: BackendDAE::Variables, mut inResIndex: i32, mut inTmpIndex: i32, mut inMatrixName: ArcStr, mut tmpVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut resVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut tmpVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = tmpVars;
    let mut resVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = resVars;
    (tmpVars, resVars) = ({
        let mut resIndex: i32 = inResIndex.clone();
        let mut tmpIndex: i32 = inTmpIndex.clone();
        (::match_deref::match_deref! { match &(inVars.clone()) {
        Deref @ metamodelica::List::Nil => {
            (tmpVars.clone().reverse(), resVars.clone().reverse())
        },
        Deref @ metamodelica::List::Cons { head: v @ BackendDAE::Var { varKind: varkind, varName: currVar, .. }, tail: restVar } => {
            let mut v1: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
            let mut simVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut derivedCref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut currVar = (*currVar).clone();
            match '__try0: {
                unwrap_break_err!(BackendVariable::getVarSingle(currVar.clone(), inAllVars.clone()), '__try0);
                currVar = (match varkind.clone() {
        BackendDAE::VarKind::STATE { .. } => ComponentReference::crefPrefixDer(currVar.clone()),
        _ => currVar.clone(),
    });
                derivedCref = unwrap_break_err!(ComponentReference::createDifferentiatedCrefName(currVar.clone(), inCref.clone(), (inMatrixName.clone()).clone()), '__try0);
                v1 = BackendVariable::copyVarNewName(derivedCref.clone(), v.clone());
                v1 = unwrap_break_err!(BackendVariable::setVarKind(v1.clone(), openmodelica_backend_types::BackendDAE::VarKind::JAC_VAR), '__try0);
                simVar = unwrap_break_err!(dlowvarToSimvar(v1.clone(), None, inAllVars.clone(), None), '__try0);
                simVar.index = inResIndex.clone();
                resIndex = inResIndex.clone() + 1;
                simVar.matrixName = Some((inMatrixName.clone()).clone());
                resVars = metamodelica::cons(simVar.clone(), resVars.clone());
                Ok::<_, anyhow::Error>((currVar.clone(), derivedCref.clone(), resIndex.clone(), resVars.clone(), simVar.clone(), v1.clone()))
            } {
                Ok((__try0_o0, __try0_o1, __try0_o2, __try0_o3, __try0_o4, __try0_o5)) => {
                    currVar = __try0_o0;
                    derivedCref = __try0_o1;
                    resIndex = __try0_o2;
                    resVars = __try0_o3;
                    simVar = __try0_o4;
                    v1 = __try0_o5;
                }
                Err(__try0_err) => {
                    currVar = (match varkind.clone() {
        BackendDAE::VarKind::STATE { .. } => ComponentReference::crefPrefixDer(currVar.clone()),
        _ => currVar.clone(),
    });
                    derivedCref = ComponentReference::createDifferentiatedCrefName(currVar.clone(), inCref.clone(), (inMatrixName.clone()).clone())?;
                    v1 = BackendVariable::copyVarNewName(derivedCref.clone(), v.clone());
                    v1 = BackendVariable::setVarKind(v1.clone(), openmodelica_backend_types::BackendDAE::VarKind::JAC_TMP_VAR)?;
                    simVar = dlowvarToSimvar(v1.clone(), None, inAllVars.clone(), None)?;
                    simVar.index = inTmpIndex.clone();
                    simVar.matrixName = Some((inMatrixName.clone()).clone());
                    tmpIndex = inTmpIndex.clone() + 1;
                    tmpVars = metamodelica::cons(simVar.clone(), tmpVars.clone());
                    return Err(__try0_err);
                }
            }
            createJacSimVarsColumn(restVar.clone(), inCref.clone(), inAllVars.clone(), resIndex.clone(), tmpIndex.clone(), (inMatrixName.clone()).clone(), tmpVars.clone(), resVars.clone())?
        },
        _ => {
            Error::addInternalError((literal!("function createAllDiffedSimVars failed")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
    });
    Ok((tmpVars, resVars))
}

fn createAllDiffedSimVars(mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut inCref: Arc<DAE::ComponentRef>, mut inAllVars: BackendDAE::Variables, mut inResIndex: i32, mut inTmpIndex: i32, mut inMatrixName: ArcStr, mut iVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut lst1: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (outVars, lst1) = createJacSimVarsColumn(inVars.clone(), inCref.clone(), inAllVars.clone(), inResIndex.clone(), inTmpIndex.clone(), (inMatrixName.clone()).clone(), iVars.clone(), metamodelica::nil())?;
    outVars = listAppend(lst1.clone(), outVars.clone());
    Ok(outVars)
}

pub fn collectAllJacobianEquations(mut inJacobianMatrix: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut outEqn: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut column: Arc<metamodelica::List<Arc<SimCode::JacobianColumn>>> = metamodelica::nil();
    let mut tmp: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut constantEqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    for mut m in &*inJacobianMatrix.clone() {
        let mut m = m.clone();
        let __pa0 = ::match_deref::match_deref! { match &(m.clone()) {
            Deref @ SimCode::JacobianMatrix { columns: __pa0, .. } => __pa0.clone(),
            _ => bail!("pattern mismatch"),
        } };
        column = __pa0.clone();
        for mut c in &*column.clone() {
            let mut c = c.clone();
            let (__pa1, __pa2) = ::match_deref::match_deref! { match &(c.clone()) {
                Deref @ SimCode::JacobianColumn { constantEqns: __pa1, columnEqns: __pa2, .. } => (__pa1.clone(), __pa2.clone()),
                _ => bail!("pattern mismatch"),
            } };
            constantEqns = __pa1.clone();
            tmp = __pa2.clone();
            outEqn = listAppend(tmp.clone(), outEqn.clone());
            outEqn = listAppend(constantEqns.clone(), outEqn.clone());
        }
    }
    Ok(outEqn)
}

pub fn collectAllJacobianVars(mut inJacobianMatrix: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut column: Arc<metamodelica::List<Arc<SimCode::JacobianColumn>>> = metamodelica::nil();
    let mut tmp: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    for mut m in &*inJacobianMatrix.clone() {
        let mut m = m.clone();
        let __pa0 = ::match_deref::match_deref! { match &(m.clone()) {
            Deref @ SimCode::JacobianMatrix { columns: __pa0, .. } => __pa0.clone(),
            _ => bail!("pattern mismatch"),
        } };
        column = __pa0.clone();
        for mut c in &*column.clone() {
            let mut c = c.clone();
            let __pa1 = ::match_deref::match_deref! { match &(c.clone()) {
                Deref @ SimCode::JacobianColumn { columnVars: __pa1, .. } => __pa1.clone(),
                _ => bail!("pattern mismatch"),
            } };
            tmp = __pa1.clone();
            outVars = listAppend(tmp.clone(), outVars.clone());
        }
    }
    Ok(outVars)
}

pub fn collectAllSeedVars(mut inJacobianMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    for mut m in &*inJacobianMatrices.clone() {
        let mut m = m.clone();
        let __pa0 = ::match_deref::match_deref! { match &(m.clone()) {
            Deref @ SimCode::JacobianMatrix { seedVars: __pa0, .. } => __pa0.clone(),
            _ => bail!("pattern mismatch"),
        } };
        seedVars = __pa0.clone();
        outVars = listAppend(seedVars.clone(), outVars.clone());
    }
    outVars = List::map1(outVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarKind, SimCodeVar::SimVar, BackendDAE::VarKind)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::SEED_VAR)?;
    Ok(outVars)
}

fn setSimVarKind(mut simVar: SimCodeVar::SimVar, mut varKind: BackendDAE::VarKind) -> SimCodeVar::SimVar {
    let mut simVar: SimCodeVar::SimVar = simVar;
    simVar.varKind = varKind.clone();
    simVar
}

fn setSimVarMatrixName(mut simVar: SimCodeVar::SimVar, mut optName: Option<ArcStr>) -> SimCodeVar::SimVar {
    let mut simVar: SimCodeVar::SimVar = simVar;
    simVar.matrixName = optName.clone();
    simVar
}

fn makeTmpRealSimCodeVar(mut inName: Arc<DAE::ComponentRef>, mut inVarKind: BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> {
    let mut outSimVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    if FMI::isFMIVersion20((FMI::getFMIVersionString()?).clone())? {
        outSimVar = SimCodeVar::SimVar { name: inName.clone(), varKind: inVarKind.clone(), comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: -1, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: DAE::T_REAL_DEFAULT().clone(), isDiscrete: false, arrayCref: None, aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL), variable_index: None, fmi_index: None, numArrayElement: metamodelica::nil(), isValueChangeable: false, isProtected: false, hideResult: None, isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(inName.clone()), relativeQuantity: false };
    } else {
        outSimVar = SimCodeVar::SimVar { name: inName.clone(), varKind: inVarKind.clone(), comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: -1, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: DAE::T_REAL_DEFAULT().clone(), isDiscrete: false, arrayCref: None, aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::NONECAUS), variable_index: None, fmi_index: None, numArrayElement: metamodelica::nil(), isValueChangeable: false, isProtected: false, hideResult: None, isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(inName.clone()), relativeQuantity: false };
    }
    Ok(outSimVar)
}

fn sortInitialUnknowsSimVars(mut inSimVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>> {
    let mut sortedCrefs: Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut unsortedCrefs: Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    let mut index: i32 = 0;
    for mut var in &*inSimVars.clone() {
        let mut var = var.clone();
        if isRealInput(var.clone()) {
            unsortedCrefs = metamodelica::cons((getVariableFMIIndex(var.clone()), var.name.clone()), unsortedCrefs.clone());
        }
    }
    index = 0;
    for mut i in &*List::sort(unsortedCrefs.clone(), std::sync::Arc::new(fnptr!(Util::compareTupleIntGt, _, _))).unwrap() {
        let mut i = i.clone();
        (_, cref) = i.clone();
        sortedCrefs = metamodelica::cons((index.clone(), cref.clone()), sortedCrefs.clone());
        index = index.clone() + 1;
    }
    sortedCrefs = sortedCrefs.clone().reverse();
    sortedCrefs
}

fn dumpSortedInitialUnknownCrefs(mut sortedCrefs: Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>>) -> Result<()> {
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut index: i32 = 0;
    for mut i in &*sortedCrefs.clone() {
        let mut i = i.clone();
        (index, cref) = i.clone();
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nindex :")); __mm_s.push_str(&*intString(index.clone())); __mm_s.push_str(&*literal!("=>")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cref.clone())?); ArcStr::from(__mm_s) }).clone());
    }
    println!("{}", (literal!("\n*********")).clone());
    Ok(())
}

fn sortSparsePattern(mut inSimVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut inSparsePattern: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, mut useFMIIndex: bool) -> Result<Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>>> {
    let mut outSparse: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr));
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut size: i32 = 0;
    let mut i: i32 = 0;
    let mut j: i32 = 0;
    let mut intLst: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    size = (inSimVars.clone().len() as i32);
    if size.clone() > 0 {
        ht = HashTable::emptyHashTableSized(size.clone());
        for mut var in &*inSimVars.clone() {
            let mut var = var.clone();
            if !(useFMIIndex.clone()) {
                let SimCodeVar::SIMVAR { index: __pa0, name: __pa1, .. } = (var.clone()) else { bail!("pattern mismatch") };
                i = __pa0.clone();
                cref = __pa1.clone();
            } else {
                let SimCodeVar::SIMVAR { name: __pa2, .. } = (var.clone()) else { bail!("pattern mismatch") };
                cref = __pa2.clone();
                i = getVariableFMIIndex(var.clone());
            }
            ht = BaseHashTable::add((cref.clone(), i.clone()), ht.clone())?;
        }
        for mut tpl in &*inSparsePattern.clone() {
            let mut tpl = tpl.clone();
            (cref, crefs) = tpl.clone();
            i = BaseHashTable::get(cref.clone(), ht.clone())?;
            if !(useFMIIndex.clone()) || i.clone() > 0 {
                intLst = metamodelica::nil();
                for mut cr in &*crefs.clone() {
                    let mut cr = cr.clone();
                    j = BaseHashTable::get(cr.clone(), ht.clone())?;
                    if !(useFMIIndex.clone()) || j.clone() > 0 {
                        intLst = metamodelica::cons(j.clone(), intLst.clone());
                    }
                }
                intLst = List::heapSortIntList(intLst.clone());
                outSparse = metamodelica::cons((i.clone(), intLst.clone()), outSparse.clone());
            }
        }
        outSparse = List::sort(outSparse.clone(), std::sync::Arc::new(fnptr!(Util::compareTupleIntGt, _, _)))?;
    }
    Ok(outSparse)
}

fn sortColoring(mut inSimVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut inColoring: Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>) -> Result<Arc<metamodelica::List<Arc<metamodelica::List<i32>>>>> {
    let mut outColoring: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, i32)>>), i32, (HashTable::FuncHashCref, HashTable::FuncCrefEqual, HashTable::FuncCrefStr, HashTable::FuncExpStr));
    let mut size: i32 = 0;
    let mut i: i32 = 0;
    let mut j: i32 = 0;
    let mut intLst: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    size = (inSimVars.clone().len() as i32);
    if size.clone() > 0 {
        ht = HashTable::emptyHashTableSized(size.clone());
        for mut var in &*inSimVars.clone() {
            let mut var = var.clone();
            let SimCodeVar::SIMVAR { index: __pa0, name: __pa1, .. } = (var.clone()) else { bail!("pattern mismatch") };
            i = __pa0.clone();
            cref = __pa1.clone();
            ht = BaseHashTable::add((cref.clone(), i.clone()), ht.clone())?;
        }
        for mut crefs in &*inColoring.clone() {
            let mut crefs = crefs.clone();
            intLst = metamodelica::nil();
            for mut cr in &*crefs.clone() {
                let mut cr = cr.clone();
                j = BaseHashTable::get(cr.clone(), ht.clone())?;
                intLst = metamodelica::cons(j.clone(), intLst.clone());
            }
            outColoring = metamodelica::cons(intLst.clone(), outColoring.clone());
        }
    }
    Ok(outColoring)
}

fn dumpSparsePatternInt(mut sparsePattern: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>>) -> Result<()> {
    let mut i: i32 = 0;
    let mut lst: Arc<metamodelica::List<i32>> = metamodelica::nil();
    for mut tpl in &*sparsePattern.clone() {
        let mut tpl = tpl.clone();
        (i, lst) = tpl.clone();
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Row   ")); __mm_s.push_str(&*intString(i.clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Cols: ")); __mm_s.push_str(&*stringDelimitList(List::map(lst.clone(), (std::sync::Arc::new(fnptr!(intString, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))?, (literal!(" ")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    }
    Ok(())
}

fn dumpSparsePattern(mut sparsePattern: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>) -> Result<()> {
    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    for mut tpl in &*sparsePattern.clone() {
        let mut tpl = tpl.clone();
        (cr, crefs) = tpl.clone();
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Row   ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cr.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Cols: ")); __mm_s.push_str(&*stringDelimitList(List::map(crefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::printComponentRefStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))?, (literal!(" ")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    }
    Ok(())
}

fn createDerivativeMatrix(mut inJacobian: Arc<BackendDAE::Jacobian>, mut iuniqueEqIndex: i32) -> Result<(Option<Arc<SimCode::DerivativeMatrix>>, i32)> {
    let mut res: Option<Arc<SimCode::DerivativeMatrix>> = None;
    let mut ouniqueEqIndex: i32 = 0;
    let mut debug: bool = false;
    (res, ouniqueEqIndex) = 'mc: {
        let __mc_input = inJacobian.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Jacobian::EMPTY_JACOBIAN { .. } => {
                    Ok((None, iuniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Jacobian::FULL_JACOBIAN { jacobian: _ } => {
                    Ok((None, iuniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Jacobian::GENERIC_JACOBIAN { jacobian: None, sparsePattern: pattern @ (sparsepatternComRefs, sparsepatternComRefsT, (independentComRefs, dependentVarsComRefs), _), coloring: sparseColoring, .. } => {
                    let mut coloring: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
                    let mut sparseInts: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut sparseIntsT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut maxColor: i32 = 0;
                    let mut index: i32 = 0;
                    let mut varsSeedIndex: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut indexVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("create sparse pattern for algebraic loop time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                        BackendDump::dumpSparsityPattern(pattern.clone(), (literal!("---+++ SparsePattern +++---")).clone())?;
                    }
                    seedVars = ({
        let mut __acc: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
        for mut cr in (independentComRefs.clone()).into_iter().cloned() {
                    let __x = makeTmpRealSimCodeVar(cr.clone(), openmodelica_backend_types::BackendDAE::VarKind::SEED_VAR)?;
                    __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                    indexVars = ({
        let mut __acc: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
        for mut cr in (dependentVarsComRefs.clone()).into_iter().cloned() {
                    let __x = makeTmpRealSimCodeVar(cr.clone(), openmodelica_backend_types::BackendDAE::VarKind::VARIABLE)?;
                    __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                    (seedVars, index) = rewriteIndex(seedVars.clone(), 0);
                    (indexVars, index) = rewriteIndex(indexVars.clone(), index.clone());
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", (literal!("\n---+++ seedVars variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), seedVars.clone())?).clone());
                        println!("{}", (literal!("\n---+++ indexVars variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), indexVars.clone())?).clone());
                    }
                    varsSeedIndex = listAppend(seedVars.clone(), indexVars.clone());
                    sparseInts = sortSparsePattern(varsSeedIndex.clone(), sparsepatternComRefs.clone(), false)?;
                    sparseIntsT = sortSparsePattern(varsSeedIndex.clone(), sparsepatternComRefsT.clone(), false)?;
                    coloring = sortColoring(seedVars.clone(), sparseColoring.clone())?;
                    maxColor = (sparseColoring.clone().len() as i32);
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("created sparse pattern for algebraic loop time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    Ok((Some(Arc::new(SimCode::DerivativeMatrix { columns: metamodelica::nil(), matrixName: (literal!("")).clone(), sparsity: sparseInts.clone(), sparsityT: sparseIntsT.clone(), coloredCols: coloring.clone(), maxColorCols: maxColor.clone() })), iuniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Jacobian::GENERIC_JACOBIAN { jacobian: Some((Deref @ BackendDAE::BackendDAE { shared, eqs: Deref @ metamodelica::List::Cons { head: syst @ Deref @ BackendDAE::EqSystem { matching: Deref @ BackendDAE::Matching::MATCHING { comps, .. }, .. }, tail: Deref @ metamodelica::List::Nil } }, name, independentVarsLst, residualVarsLst, dependentVarsLst, _)), sparsePattern: (sparsepatternComRefs, sparsepatternComRefsT, _, _), coloring: sparseColoring, .. } => {
                    let mut emptyVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut independentVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut residualVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut systvars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut allVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
                    let mut x: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut coloring: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
                    let mut sparseInts: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut sparseIntsT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut dummyVar: ArcStr = arcstr::literal!("");
                    let mut maxColor: i32 = 0;
                    let mut uniqueEqIndex: i32 = 0;
                    let mut index: i32 = 0;
                    let mut nAllVars: i32 = 0;
                    let mut columnVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut innerVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut varsSeedIndex: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut indexVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut hashTable: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
                    let mut outRes: Option<Arc<SimCode::DerivativeMatrix>> = None;
                    let mut omsiJacFunction: Arc<SimCode::OMSIFunction> = Arc::new(<SimCode::OMSIFunction as ::std::default::Default>::default());
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> creating SimCode equations for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    (omsiJacFunction, uniqueEqIndex) = generateEquationsForComponents(comps.clone(), syst.clone(), shared.clone(), iuniqueEqIndex.clone())?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> created all SimCode equations for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    dummyVar = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("dummyVar")); __mm_s.push_str(&*name.clone()); ArcStr::from(__mm_s) }).clone();
                    x = Arc::new(DAE::ComponentRef::CREF_IDENT { ident: (dummyVar.clone()).clone(), identType: DAE::T_REAL_DEFAULT().clone(), subscriptLst: metamodelica::nil() });
                    emptyVars = BackendVariable::emptyVars(BaseHashTable::bigBucketSize.clone());
                    residualVars = BackendVariable::listVar1(residualVarsLst.clone())?;
                    independentVars = BackendVariable::listVar1(independentVarsLst.clone())?;
                    (allVars, _) = BackendVariable::traverseBackendDAEVars(syst.orderedVars.clone(), (std::sync::Arc::new(getFurtherVars) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<BackendDAE::Var>>, Arc<DAE::ComponentRef>))> + 'static>), (metamodelica::nil(), x.clone()))?;
                    systvars = BackendVariable::listVar1(allVars.clone())?;
                    (columnVars, _) = BackendVariable::traverseBackendDAEVars(systvars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone()))?;
                    columnVars = List::map1(columnVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarKind, SimCodeVar::SimVar, BackendDAE::VarKind)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::JAC_TMP_VAR)?;
                    columnVars = List::map1(columnVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarMatrixName, SimCodeVar::SimVar, Option<ArcStr>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Option<ArcStr>) -> Result<SimCodeVar::SimVar> + 'static>), Some((name.clone()).clone()))?;
                    (innerVars, _) = rewriteIndex(columnVars.clone(), 0);
                    (innerVars, columnVars) = createJacSimVarsColumn(dependentVarsLst.clone(), x.clone(), residualVars.clone(), 0, (innerVars.clone().len() as i32), (name.clone()).clone(), innerVars.clone(), metamodelica::nil())?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", (literal!("\n---+++ all column variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), columnVars.clone())?).clone());
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> create all SimCode vars for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    (seedVars, _) = BackendVariable::traverseBackendDAEVars(independentVars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone()))?;
                    (indexVars, _) = BackendVariable::traverseBackendDAEVars(residualVars.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone()))?;
                    (seedVars, _) = rewriteIndex(seedVars.clone().reverse(), 0);
                    (indexVars, _) = rewriteIndex(indexVars.clone().reverse(), 0);
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", (literal!("\n---+++ seedVars variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), seedVars.clone())?).clone());
                        println!("{}", (literal!("\n---+++ indexVars variables +++---\n")).clone());
                        println!("{}", (Tpl::tplString((std::sync::Arc::new(SimCodeDump::dumpVarsShort) as std::sync::Arc<dyn ::std::ops::Fn(Tpl::Text, Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Tpl::Text> + 'static>), indexVars.clone())?).clone());
                    }
                    varsSeedIndex = listAppend(seedVars.clone(), indexVars.clone());
                    sparseInts = sortSparsePattern(varsSeedIndex.clone(), sparsepatternComRefs.clone(), false)?;
                    sparseIntsT = sortSparsePattern(varsSeedIndex.clone(), sparsepatternComRefsT.clone(), false)?;
                    coloring = sortColoring(varsSeedIndex.clone(), sparseColoring.clone())?;
                    maxColor = (sparseColoring.clone().len() as i32);
                    seedVars = replaceSeedVarsName(seedVars.clone(), (name.clone()).clone())?;
                    seedVars = List::map1(seedVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarKind, SimCodeVar::SimVar, BackendDAE::VarKind)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, BackendDAE::VarKind) -> Result<SimCodeVar::SimVar> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::SEED_VAR)?;
                    seedVars = List::map1(seedVars.clone(), (std::sync::Arc::new(fnptr!(setSimVarMatrixName, SimCodeVar::SimVar, Option<ArcStr>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Option<ArcStr>) -> Result<SimCodeVar::SimVar> + 'static>), Some((name.clone()).clone()))?;
                    if Flags::isSet(Flags::JAC_DUMP2.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("analytical Jacobians -> transformed to SimCode for Matrix ")); __mm_s.push_str(&*name.clone()); __mm_s.push_str(&*literal!(" time: ")); __mm_s.push_str(&*realString(clock())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    (columnVars, index) = rewriteIndex(columnVars.clone(), 0);
                    (innerVars, index) = rewriteIndex(innerVars.clone(), index.clone());
                    (seedVars, index) = rewriteIndex(seedVars.clone(), index.clone());
                    nAllVars = (seedVars.clone().len() as i32) + (innerVars.clone().len() as i32) + (indexVars.clone().len() as i32);
                    hashTable = fillLocalHashTable(list![seedVars.clone(), innerVars.clone(), columnVars.clone()], nAllVars.clone())?;
                    assign_field!(
                        omsiJacFunction.inputVars = seedVars.clone(),
                        omsiJacFunction.innerVars = innerVars.clone(),
                        omsiJacFunction.outputVars = columnVars.clone(),
                        omsiJacFunction.nAllVars = nAllVars.clone(),
                        omsiJacFunction.context = SimCodeFunction::Context::JACOBIAN_CONTEXT { name: (name.clone()).clone(), jacHT: Some(hashTable.clone()) }
                    );
                    if debug.clone() {
                        dumpOMSIFunc(omsiJacFunction.clone(), (literal!("\nJacobian OMSIFunction")).clone());
                        println!("{}", (literal!("\nLocal jacobian hash table:\n")).clone());
                        BaseHashTable::dumpHashTableStatistics(hashTable.clone());
                    }
                    outRes = Some(Arc::new(SimCode::DerivativeMatrix { maxColorCols: maxColor.clone(), coloredCols: coloring.clone(), sparsityT: sparseIntsT.clone(), sparsity: sparseInts.clone(), matrixName: (name.clone()).clone(), columns: list![omsiJacFunction.clone()] }));
                    Ok((outRes.clone(), uniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut errorMessage: ArcStr = arcstr::literal!("");
                    if Flags::isSet(Flags::JAC_DUMP.clone())? {
                        errorMessage = (literal!("function createSymbolicSimulationJacobian failed.")).clone();
                        Error::addInternalError((errorMessage.clone()).clone(), metamodelica::sourceInfo!())?;
                    }
                    Ok((None, iuniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((res, ouniqueEqIndex))
}

// =============================================================================
// section with unsorted function
//
// TODO: clean up this section ;)
// =============================================================================
fn isSimEqSys(mut simEqSysIn: Arc<SimCode::SimEqSystem>) -> bool {
    let mut isEqSys: bool = false;
    isEqSys = (::match_deref::match_deref! { match &(simEqSysIn.clone()) {
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { .. } => true,
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    isEqSys
}

fn collectDelayExpressions(mut e: Arc<DAE::Exp>, mut acc: Arc<metamodelica::List<Arc<DAE::Exp>>>) -> (Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>) {
    let mut outExp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut outAcc: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
    (outExp, outAcc) = (::match_deref::match_deref! { match &(e.clone()) {
        Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "delay" }, .. } => (e.clone(), metamodelica::cons(e.clone(), acc.clone())),
        _ => (e.clone(), acc.clone()),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    (outExp, outAcc)
}

fn updateDelayExpressions(mut e: Arc<DAE::Exp>, mut acc: (Arc<metamodelica::List<Arc<DAE::Exp>>>, i32)) -> (Arc<DAE::Exp>, (Arc<metamodelica::List<Arc<DAE::Exp>>>, i32)) {
    let mut e: Arc<DAE::Exp> = e;
    let mut acc: (Arc<metamodelica::List<Arc<DAE::Exp>>>, i32) = acc;
    (e, acc) = (::match_deref::match_deref! { match &((e.clone(), acc.clone())) {
        (Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::ICONST { integer: i }, tail: rest }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "delay" }, .. }, (lst, index)) if (i.clone() < 0) => {
            let mut res: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            res = Arc::new(DAE::Exp::CALL { path: Arc::new(Absyn::Path::IDENT { name: (literal!("delay")).clone() }), expLst: metamodelica::cons(Arc::new(DAE::Exp::ICONST { integer: index.clone() }), rest.clone()), attr: var_field!((*e).attr, DAE::Exp::CALL).clone() });
            (res.clone(), (metamodelica::cons(res.clone(), lst.clone()), index.clone() + 1))
        },
        _ => {
            (e.clone(), acc.clone())
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    (e, acc)
}

pub fn extractDelayedExpressions(mut dlow: Arc<BackendDAE::BackendDAE>) -> Result<(Arc<metamodelica::List<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))>>, i32)> {
    let mut delayedExps: Arc<metamodelica::List<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))>> = metamodelica::nil();
    let mut maxDelayedExpIndex: i32 = 0;
    (delayedExps, maxDelayedExpIndex) = 'mc: {
        let __mc_input = dlow.clone();
        if let Ok((__v, __wb0, __wb1)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut exps: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut delayedExps: Arc<metamodelica::List<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))>> = delayedExps.clone();
                    let mut maxDelayedExpIndex: i32 = maxDelayedExpIndex.clone();
                    (_, exps) = BackendDAEUtil::traverseBackendDAEExps(dlow.clone(), (std::sync::Arc::new(Expression::traverseSubexpressionsHelper) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, _) -> Result<_> + 'static>), ((std::sync::Arc::new(fnptr!(collectDelayExpressions, Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>) -> Result<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>)> + 'static>), metamodelica::nil()))?;
                    delayedExps = List::map(exps.clone(), (std::sync::Arc::new(extractIdAndExpFromDelayExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))> + 'static>))?;
                    maxDelayedExpIndex = List::applyAndFold(delayedExps.clone(), (std::sync::Arc::new(fnptr!(intMax, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), std::sync::Arc::new(fnptr!(Util::tuple21, _)), -1)?;
                    Ok(((delayedExps.clone(), maxDelayedExpIndex.clone() + 1), delayedExps.clone(), maxDelayedExpIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { delayedExps = __wb0; maxDelayedExpIndex = __wb1; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("function extractDelayedExpressions failed")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((delayedExps, maxDelayedExpIndex))
}

pub fn extractJacobianDelayedExpressions(mut delayedExps: Arc<metamodelica::List<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))>>, mut maxDelayedExpIndex: i32, mut SymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) -> Result<(Arc<metamodelica::List<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))>>, i32, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>)> {
    let mut delayedExps: Arc<metamodelica::List<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))>> = delayedExps;
    let mut maxDelayedExpIndex: i32 = maxDelayedExpIndex;
    let mut SymJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = SymJacs;
    let mut tmpJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut tmpCols: Arc<metamodelica::List<Arc<SimCode::JacobianColumn>>> = metamodelica::nil();
    let mut tmpSysts: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut new_delayedExps: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
    for mut jac in &*SymJacs.clone().reverse() {
        let mut jac = jac.clone();
        tmpCols = metamodelica::nil();
        for mut col in &*jac.columns.clone().reverse() {
            let mut col = col.clone();
            let (__pa0, (_, (__pa1, __pa2))) = traverseExpsEqSystems(col.columnEqns.clone(), (std::sync::Arc::new(Expression::traverseSubexpressionsHelper) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, _) -> Result<_> + 'static>), ((std::sync::Arc::new(fnptr!(updateDelayExpressions, Arc<DAE::Exp>, (Arc<metamodelica::List<Arc<DAE::Exp>>>, i32))) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (Arc<metamodelica::List<Arc<DAE::Exp>>>, i32)) -> Result<(Arc<DAE::Exp>, (Arc<metamodelica::List<Arc<DAE::Exp>>>, i32))> + 'static>), (metamodelica::nil(), maxDelayedExpIndex.clone())), metamodelica::nil())?;
            tmpSysts = __pa0.clone();
            new_delayedExps = __pa1.clone();
            maxDelayedExpIndex = __pa2.clone();
            delayedExps = listAppend(List::map(new_delayedExps.clone(), (std::sync::Arc::new(extractIdAndExpFromDelayExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))> + 'static>))?, delayedExps.clone());
            assign_field!(col.columnEqns = tmpSysts.clone());
            tmpCols = metamodelica::cons(col.clone(), tmpCols.clone());
        }
        assign_field!(jac.columns = tmpCols.clone());
        tmpJacs = metamodelica::cons(jac.clone(), tmpJacs.clone());
    }
    SymJacs = tmpJacs.clone();
    Ok((delayedExps, maxDelayedExpIndex, SymJacs))
}

pub fn extractIdAndExpFromDelayExp(mut delayCallExp: Arc<DAE::Exp>) -> Result<(i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>))> {
    let mut delayedExp: (i32, (Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::Exp>)) = (0, (Arc::new(<DAE::Exp as ::std::default::Default>::default()), Arc::new(<DAE::Exp as ::std::default::Default>::default()), Arc::new(<DAE::Exp as ::std::default::Default>::default())));
    delayedExp = (::match_deref::match_deref! { match &(delayCallExp.clone()) {
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::ICONST { integer: i }, tail: Deref @ metamodelica::List::Cons { head: e, tail: Deref @ metamodelica::List::Cons { head: delay, tail: Deref @ metamodelica::List::Cons { head: delayMax, tail: Deref @ metamodelica::List::Nil } } } }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "delay" }, .. } => {
            (i.clone(), (e.clone(), delay.clone(), delayMax.clone()))
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(delayedExp)
}

pub fn extractSpatialDistributionInfo(mut dlow: Arc<BackendDAE::BackendDAE>) -> Result<SimCode::SpatialDistributionInfo> {
    let mut spatialInfo: SimCode::SpatialDistributionInfo = <SimCode::SpatialDistributionInfo as ::std::default::Default>::default();
    let mut spatial_lst: Arc<metamodelica::List<SimCode::SpatialDistribution>> = metamodelica::nil();
    let mut maxIndex_ptr: Mutable::Mutable<i32> = Mutable::create(-1);
    (_, spatial_lst) = BackendDAEUtil::traverseBackendDAEExps(dlow.clone(), (std::sync::Arc::new(Expression::traverseSubexpressionsHelper) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, _) -> Result<_> + 'static>), ((std::sync::Arc::new({ let __pe_b2 = maxIndex_ptr.clone(); move |__pe_a0, __pe_a1| extractSpatialDistributionInfoExp(__pe_a0, __pe_a1, __pe_b2.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<SimCode::SpatialDistribution>>) -> Result<(Arc<DAE::Exp>, Arc<metamodelica::List<SimCode::SpatialDistribution>>)> + 'static>), metamodelica::nil()))?;
    spatialInfo = SimCode::SpatialDistributionInfo { spatialDistributions: spatial_lst.clone(), maxIndex: Mutable::access(maxIndex_ptr.clone()) };
    Ok(spatialInfo)
}

pub fn extractSpatialDistributionInfoExp(mut callExp: Arc<DAE::Exp>, mut spatialInfo: Arc<metamodelica::List<SimCode::SpatialDistribution>>, mut maxIndex_ptr: Mutable::Mutable<i32>) -> Result<(Arc<DAE::Exp>, Arc<metamodelica::List<SimCode::SpatialDistribution>>)> {
    let mut callExp: Arc<DAE::Exp> = callExp;
    let mut spatialInfo: Arc<metamodelica::List<SimCode::SpatialDistribution>> = spatialInfo;
    spatialInfo = (::match_deref::match_deref! { match &(callExp.clone()) {
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::ICONST { integer: i }, tail: Deref @ metamodelica::List::Cons { head: in0, tail: Deref @ metamodelica::List::Cons { head: in1, tail: Deref @ metamodelica::List::Cons { head: pos, tail: Deref @ metamodelica::List::Cons { head: dir, tail: Deref @ metamodelica::List::Cons { head: initPnts, tail: Deref @ metamodelica::List::Cons { head: initVals, tail: Deref @ metamodelica::List::Nil } } } } } } }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "spatialDistribution" }, .. } => {
            let mut initSize: i32 = 0;
            if i.clone() > Mutable::access(maxIndex_ptr.clone()) {
                Mutable::update(maxIndex_ptr.clone(), i.clone());
            }
            if !(Expression::sizeOf(Expression::r#typeof(initPnts.clone())?)? == Expression::sizeOf(Expression::r#typeof(initVals.clone())?)?) {
                Error::addInternalError((literal!("function extractDelayedExpressions failed: initialPoints and initialValues of spatialDistribution are not of the same size.")).clone(), metamodelica::sourceInfo!())?;
            }
            initSize = Expression::sizeOf(Expression::r#typeof(initPnts.clone())?)?;
            metamodelica::cons(SimCode::SpatialDistribution { index: i.clone(), in0: in0.clone(), in1: in1.clone(), pos: pos.clone(), dir: dir.clone(), initPnts: initPnts.clone(), initVals: initVals.clone(), initSize: initSize.clone() }, spatialInfo.clone())
        },
        _ => {
            spatialInfo.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((callExp, spatialInfo))
}

pub fn createExtObjInfo(mut shared: Arc<BackendDAE::Shared>) -> Result<SimCode::ExtObjInfo> {
    let mut extObjInfo: SimCode::ExtObjInfo = <SimCode::ExtObjInfo as ::std::default::Default>::default();
    let mut evars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut evarLst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut aliases: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    let mut simvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let __pa0 = ::match_deref::match_deref! { match &(shared.clone()) {
        Deref @ BackendDAE::Shared { externalObjects: __pa0, .. } => __pa0.clone(),
        _ => bail!("pattern mismatch"),
    } };
    evars = __pa0.clone();
    evarLst = BackendVariable::varList(evars.clone())?;
    evarLst = orderExtVars(evarLst.clone(), BackendDAEUtil::isInitializationDAE(shared.clone()));
    (simvars, aliases) = extractExtObjInfo2(evarLst.clone(), evars.clone())?;
    extObjInfo = SimCode::ExtObjInfo { vars: simvars.clone(), aliases: aliases.clone() };
    Ok(extObjInfo)
}

fn orderExtVars(mut varLstIn: Arc<metamodelica::List<BackendDAE::Var>>, mut isInitial: bool) -> Arc<metamodelica::List<BackendDAE::Var>> {
    let mut varLstOut: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut order: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut ass1: metamodelica::Array<i32> = Default::default();
    let mut ass2: metamodelica::Array<i32> = Default::default();
    let mut comps: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    let mut varsWithBind: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut varsWithoutBind: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    match '__try0: {
        (varsWithBind, varsWithoutBind) = unwrap_break_err!(List::separateOnTrue(varLstIn.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::varHasBindExp, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<bool> + 'static>)), '__try0);
        (comps, ass1, ass2) = unwrap_break_err!(BackendDAEUtil::causalizeVarBindSystem(varsWithBind.clone(), isInitial.clone()), '__try0);
        order = unwrap_break_err!(List::map1(unwrap_break_err!(List::flatten(comps.clone()), '__try0), (std::sync::Arc::new(Array::getIndexFirst) as std::sync::Arc<dyn ::std::ops::Fn(i32, _) -> Result<_> + 'static>), ass1.clone()), '__try0);
        varsWithBind = unwrap_break_err!(List::map1(order.clone(), std::sync::Arc::new(fnptr!(List::getIndexFirst, i32, _)), varsWithBind.clone()), '__try0);
        varLstOut = listAppend(varsWithoutBind.clone(), varsWithBind.clone());
        Ok::<_, anyhow::Error>((varLstOut.clone(),))
    } {
        Ok((__try0_o0,)) => {
            varLstOut = __try0_o0;
        }
        Err(_) => {
            varLstOut = varLstIn.clone();
        }
    }
    varLstOut
}

fn extractExtObjInfo2(mut varLst: Arc<metamodelica::List<BackendDAE::Var>>, mut evars: BackendDAE::Variables) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>)>>)> {
    let mut vars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut aliases: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    for mut bv in &*varLst.clone() {
        let mut bv = bv.clone();
        let () = (::match_deref::match_deref! { match &(bv.clone()) {
        BackendDAE::Var { varKind: BackendDAE::VarKind::EXTOBJ { fullClassName: _ }, bindExp: Some(Deref @ DAE::Exp::CREF { componentRef: cr, ty: _ }), varName: name, .. } => {
            aliases = metamodelica::cons((name.clone(), cr.clone()), aliases.clone());
            ()
        },
        _ => {
            let mut sv: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            sv = dlowvarToSimvar(bv.clone(), None, evars.clone(), None)?;
            vars = metamodelica::cons(sv.clone(), vars.clone());
            ()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    }
    vars = Dangerous::listReverseInPlace(vars.clone());
    aliases = Dangerous::listReverseInPlace(aliases.clone());
    Ok((vars, aliases))
}

fn createAlgorithmAndEquationAsserts(mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut acc: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> {
    let mut algorithmAndEquationAsserts: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    algorithmAndEquationAsserts = 'mc: {
        let __mc_input = (syst.clone(), shared.clone(), acc.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::EqSystem { orderedVars: vars, .. }, Deref @ BackendDAE::Shared { .. }, (uniqueEqIndex, simeqns)) => {
                    let mut res: Arc<metamodelica::List<Arc<DAE::Algorithm>>> = metamodelica::nil();
                    let mut result: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut uniqueEqIndex = (*uniqueEqIndex).clone();
                    res = BackendVariable::traverseBackendDAEVars(vars.clone(), (std::sync::Arc::new(BackendVariable::getMinMaxAsserts) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, Arc<metamodelica::List<Arc<DAE::Algorithm>>>) -> Result<(BackendDAE::Var, Arc<metamodelica::List<Arc<DAE::Algorithm>>>)> + 'static>), metamodelica::nil())?;
                    (result, uniqueEqIndex) = List::mapFold(res.clone(), (std::sync::Arc::new(dlowAlgToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Algorithm>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone())?;
                    Ok((uniqueEqIndex.clone(), listAppend(result.clone(), simeqns.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("function createAlgorithmAndEquationAsserts failed")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(algorithmAndEquationAsserts)
}

pub fn traversedlowEqToSimEqSystem(mut inEq: Arc<BackendDAE::Equation>, mut inTpl: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> {
    let mut outEq: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
    let mut outTpl: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    (outEq, outTpl) = 'mc: {
        let __mc_input = (inEq.clone(), inTpl.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (e, (uniqueEqIndex, seqnlst)) => {
                    let mut se: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut uniqueEqIndex = (*uniqueEqIndex).clone();
                    (se, uniqueEqIndex) = dlowEqToSimEqSystem(e.clone(), uniqueEqIndex.clone())?;
                    Ok((e.clone(), (uniqueEqIndex.clone(), metamodelica::cons(se.clone(), seqnlst.clone()))))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((inEq.clone(), inTpl.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outEq, outTpl))
}

pub fn extractDiscreteModelVars(mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut acc: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> {
    let mut discreteModelVars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    discreteModelVars = 'mc: {
        let __mc_input = syst.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::EqSystem { orderedVars: v, .. } => {
                    let mut vLst2: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    vLst2 = BackendVariable::traverseBackendDAEVars(v.clone(), (std::sync::Arc::new(traversingisVarDiscreteCrefFinder) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<(BackendDAE::Var, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> + 'static>), acc.clone())?;
                    Ok(vLst2.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("function extractDiscreteModelVars failed")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(discreteModelVars)
}

fn traversingisVarDiscreteCrefFinder(mut inVar: BackendDAE::Var, mut inTpl: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<(BackendDAE::Var, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outTpl: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    (outVar, outTpl) = 'mc: {
        let __mc_input = (inVar.clone(), inTpl.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (v, cr_lst) => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let true = (BackendVariable::isVarDiscrete(v.clone())) else { bail!("pattern mismatch") };
                    cr = BackendVariable::varCref(v.clone())?;
                    Ok((v.clone(), metamodelica::cons(cr.clone(), cr_lst.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((inVar.clone(), inTpl.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outVar, outTpl))
}

fn jacToSimjac(mut jac: (i32, i32, Arc<BackendDAE::Equation>), mut v: BackendDAE::Variables) -> Result<(i32, i32, Arc<SimCode::SimEqSystem>)> {
    let mut simJac: (i32, i32, Arc<SimCode::SimEqSystem>) = (0, 0, Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default()));
    simJac = (::match_deref::match_deref! { match &(jac.clone()) {
        (row, col, Deref @ BackendDAE::Equation::RESIDUAL_EQUATION { attr: eqAttr, source, exp: e }) => {
            (row.clone() - 1, col.clone() - 1, Arc::new(SimCode::SimEqSystem::SES_RESIDUAL { index: 0, res_index: 0, exp: e.clone(), source: source.clone(), eqAttr: eqAttr.clone() }))
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(simJac)
}

fn simJacCSRToCSC(mut e1: (i32, i32, Arc<SimCode::SimEqSystem>), mut e2: (i32, i32, Arc<SimCode::SimEqSystem>)) -> bool {
    let mut isGt: bool = false;
    let mut r1: i32 = 0;
    let mut r2: i32 = 0;
    let mut c1: i32 = 0;
    let mut c2: i32 = 0;
    (r1, c1, _) = e1.clone();
    (r2, c2, _) = e2.clone();
    if intNe(c1.clone(), c2.clone()) {
        isGt = intGt(c1.clone(), c2.clone());
    } else {
        isGt = intGt(r1.clone(), r2.clone());
    }
    isGt
}

fn createSingleWhenEqnCode(mut inEquation: Arc<BackendDAE::Equation>, mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut shared: Arc<BackendDAE::Shared>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equations_, ouniqueEqIndex, otempvars) = 'mc: {
        let __mc_input = inEquation.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Equation::WHEN_EQUATION { attr: eqAttr, source, whenEquation: Deref @ BackendDAE::WhenEquation { elsewhenPart: oelseWhen, whenStmtLst, condition: cond }, .. } => {
                    let mut left: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut elseWhen: Arc<BackendDAE::WhenEquation> = Arc::new(<BackendDAE::WhenEquation as ::std::default::Default>::default());
                    let mut conditions: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut initialCall: bool = false;
                    let mut uniqueEqIndex: i32 = 0;
                    let mut simElseWhenEq: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut osimElseWhenEq: Option<Arc<SimCode::SimEqSystem>> = None;
                    for mut stmt in &*whenStmtLst.clone() {
                        let mut stmt = stmt.clone();
                        let () = (::match_deref::match_deref! { match &(stmt.clone()) {
        BackendDAE::WhenOperator::ASSIGN { left: Deref @ DAE::Exp::CREF { componentRef: left, .. }, .. } => {
                    crefs = List::map(inVars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    let true = (List::all(crefs.clone(), (std::sync::Arc::new({ let __pe_b0 = left.clone(); move |__pe_a1| ComponentReferenceBasics::crefPrefixOf(__pe_b0.clone(), __pe_a1) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    ()
        },
        _ => (),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
                    }
                    if isSome(oelseWhen.clone()) {
                        let __pa0 = ::match_deref::match_deref! { match &(oelseWhen.clone()) {
                            Some(__pa0) => __pa0.clone(),
                            _ => bail!("pattern mismatch"),
                        } };
                        elseWhen = __pa0.clone();
                        (simElseWhenEq, uniqueEqIndex) = createElseWhenEquation(elseWhen.clone(), inVars.clone(), iuniqueEqIndex.clone() + 1, source.clone(), eqAttr.clone())?;
                        osimElseWhenEq = Some(simElseWhenEq.clone());
                    } else {
                        uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                        osimElseWhenEq = None;
                    }
                    (conditions, initialCall) = BackendDAEUtil::getConditionList(cond.clone())?;
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_WHEN { index: iuniqueEqIndex.clone(), conditions: conditions.clone(), initialCall: initialCall.clone(), whenStmtLst: whenStmtLst.clone(), elseWhen: osimElseWhenEq.clone(), source: source.clone(), eqAttr: eqAttr.clone() })], uniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("function createSingleWhenEqnCode failed. When equations currently only supported on form v = ...")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, ouniqueEqIndex, otempvars))
}

fn createElseWhenEquation(mut inElseWhenEquation: Arc<BackendDAE::WhenEquation>, mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut iuniqueEqIndex: i32, mut inElementSource: Arc<DAE::ElementSource>, mut inEqAttr: BackendDAE::EquationAttributes) -> Result<(Arc<SimCode::SimEqSystem>, i32)> {
    let mut outSimEqSystem: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut ouniqueEqIndex: i32 = 0;
    (outSimEqSystem, ouniqueEqIndex) = (::match_deref::match_deref! { match &(inElseWhenEquation.clone()) {
        Deref @ BackendDAE::WhenEquation { elsewhenPart: oelseWhenEquation, whenStmtLst, condition: cond } => {
            let mut left: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut elseWhenEquation: Arc<BackendDAE::WhenEquation> = Arc::new(<BackendDAE::WhenEquation as ::std::default::Default>::default());
            let mut simElseWhenEq: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut osimElseWhenEq: Option<Arc<SimCode::SimEqSystem>> = None;
            let mut conditions: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut initialCall: bool = false;
            let mut uniqueEqIndex: i32 = 0;
            let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            for mut stmt in &*whenStmtLst.clone() {
                let mut stmt = stmt.clone();
                let () = (::match_deref::match_deref! { match &(stmt.clone()) {
        BackendDAE::WhenOperator::ASSIGN { left: Deref @ DAE::Exp::CREF { componentRef: left, .. }, .. } => {
            crefs = List::map(inVars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
            let true = (List::all(crefs.clone(), (std::sync::Arc::new({ let __pe_b0 = left.clone(); move |__pe_a1| ComponentReferenceBasics::crefPrefixOf(__pe_b0.clone(), __pe_a1) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
            ()
        },
        _ => (),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
            }
            if isSome(oelseWhenEquation.clone()) {
                let __pa0 = ::match_deref::match_deref! { match &(oelseWhenEquation.clone()) {
                    Some(__pa0) => __pa0.clone(),
                    _ => bail!("pattern mismatch"),
                } };
                elseWhenEquation = __pa0.clone();
                (simElseWhenEq, uniqueEqIndex) = createElseWhenEquation(elseWhenEquation.clone(), inVars.clone(), iuniqueEqIndex.clone() + 1, inElementSource.clone(), inEqAttr.clone())?;
                osimElseWhenEq = Some(simElseWhenEq.clone());
            } else {
                uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                osimElseWhenEq = None;
            }
            (conditions, initialCall) = BackendDAEUtil::getConditionList(cond.clone())?;
            (Arc::new(SimCode::SimEqSystem::SES_WHEN { index: iuniqueEqIndex.clone(), conditions: conditions.clone(), initialCall: initialCall.clone(), whenStmtLst: whenStmtLst.clone(), elseWhen: osimElseWhenEq.clone(), source: inElementSource.clone(), eqAttr: inEqAttr.clone() }), uniqueEqIndex.clone())
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((outSimEqSystem, ouniqueEqIndex))
}

fn createSingleIfEqnCode(mut inEquation: Arc<BackendDAE::Equation>, mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut shared: Arc<BackendDAE::Shared>, mut genDiscrete: bool, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equations_, ouniqueEqIndex, otempvars) = 'mc: {
        let __mc_input = (inEquation.clone(), shared.clone());
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Equation::IF_EQUATION { attr: eqAttr, source: source_, eqnsfalse: elseqns, eqnstrue: eqnsLst, conditions }, Deref @ BackendDAE::Shared { info: ei, .. }) => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut ifbranches: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)>> = metamodelica::nil();
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    (ifbranches, uniqueEqIndex, tempvars) = createEquationsIfBranch(conditions.clone(), eqnsLst.clone(), inVars.clone(), shared.clone(), genDiscrete.clone(), iuniqueEqIndex.clone(), itempvars.clone())?;
                    (equations_, uniqueEqIndex, tempvars) = createEquationsfromList(elseqns.clone(), inVars.clone(), uniqueEqIndex.clone(), tempvars.clone(), ei.clone(), genDiscrete.clone(), false, false, false)?;
                    Ok(((list![Arc::new(SimCode::SimEqSystem::SES_IFEQUATION { index: uniqueEqIndex.clone(), ifbranches: ifbranches.clone(), elsebranch: equations_.clone(), source: source_.clone(), eqAttr: eqAttr.clone() })], uniqueEqIndex.clone() + 1, tempvars.clone()), equations_.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("SimCodeUtil.createSingleIfEqnCode failed.")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, ouniqueEqIndex, otempvars))
}

fn createEquationsIfBranch(mut inConditions: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut inEquationsLst: Arc<metamodelica::List<Arc<metamodelica::List<Arc<BackendDAE::Equation>>>>>, mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut shared: Arc<BackendDAE::Shared>, mut genDiscrete: bool, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut outEquations: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (outEquations, ouniqueEqIndex, otempvars) = 'mc: {
        let __mc_input = (inConditions.clone(), inEquationsLst.clone(), shared.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil, _) => {
                    Ok((metamodelica::nil(), iuniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: condition, tail: conditionList }, Deref @ metamodelica::List::Cons { head: eqns, tail: eqnsLst }, Deref @ BackendDAE::Shared { info: ei, .. }) => {
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut ifbranch: (Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (Arc::new(<DAE::Exp as ::std::default::Default>::default()), metamodelica::nil());
                    let mut ifbranches: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)>> = metamodelica::nil();
                    (equations_, uniqueEqIndex, tempvars) = createEquationsfromList(eqns.clone(), inVars.clone(), iuniqueEqIndex.clone(), itempvars.clone(), ei.clone(), genDiscrete.clone(), false, false, false)?;
                    ifbranch = (condition.clone(), equations_.clone());
                    (ifbranches, uniqueEqIndex, tempvars) = createEquationsIfBranch(conditionList.clone(), eqnsLst.clone(), inVars.clone(), shared.clone(), genDiscrete.clone(), uniqueEqIndex.clone(), tempvars.clone())?;
                    ifbranches = metamodelica::cons(ifbranch.clone(), ifbranches.clone());
                    Ok((ifbranches.clone(), uniqueEqIndex.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("SimCodeUtil.createEquationfromList failed.")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outEquations, ouniqueEqIndex, otempvars))
}

pub fn createEquationsfromBackendDAE(mut inBDAE: Arc<BackendDAE::BackendDAE>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut genDiscrete: bool, mut includeWhen: bool, mut skipDiscInZc: bool, mut skipDiscInAlgorithm: bool) -> Result<(Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut outEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut uniqueEqIndex: i32 = iuniqueEqIndex.clone();
    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = itempvars.clone();
    let mut comps: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>> = metamodelica::nil();
    let mut simEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut zeroVarEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    for mut syst in &*inBDAE.eqs.clone() {
        let mut syst = syst.clone();
        comps = BackendDAEUtil::getCompsOfMatching(syst.matching.clone());
        (simEqs, _, uniqueEqIndex, tempvars) = createEquations(includeWhen.clone(), skipDiscInZc.clone(), genDiscrete.clone(), skipDiscInAlgorithm.clone(), syst.clone(), inBDAE.shared.clone(), comps.clone(), uniqueEqIndex.clone(), tempvars.clone())?;
        (uniqueEqIndex, zeroVarEquations) = BackendEquation::traverseEquationArray(syst.removedEqs.clone(), (std::sync::Arc::new(traversedlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (uniqueEqIndex.clone(), metamodelica::nil()))?;
        outEquations = metamodelica::cons(listAppend(simEqs.clone(), zeroVarEquations.clone()), outEquations.clone());
    }
    outEquations = Dangerous::listReverseInPlace(outEquations.clone());
    Ok((outEquations, uniqueEqIndex, tempvars))
}

pub fn createEquationsfromList(mut inEquations: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut iextra: BackendDAE::ExtraInfo, mut genDiscrete: bool, mut includeWhen: bool, mut skipDiscInZc: bool, mut skipDiscInAlgorithm: bool) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equations_, ouniqueEqIndex, otempvars) = 'mc: {
        let __mc_input = inEquations.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Nil => {
                    Ok((metamodelica::nil(), iuniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut vars1: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut eqns_1: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
                    let mut subsystem_dae: Arc<BackendDAE::BackendDAE> = Arc::new(<BackendDAE::BackendDAE as ::std::default::Default>::default());
                    let mut comps: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>> = metamodelica::nil();
                    let mut syst: Arc<BackendDAE::EqSystem> = Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default());
                    let mut shared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
                    let mut uniqueEqIndex: i32 = 0;
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    eqns_1 = BackendEquation::listEquation(inEquations.clone())?;
                    vars1 = BackendVariable::listVar1(inVars.clone())?;
                    syst = BackendDAEUtil::createEqSystem(vars1.clone(), eqns_1.clone(), metamodelica::nil(), openmodelica_backend_types::BackendDAE::BaseClockPartitionKind::UNKNOWN_PARTITION, BackendEquation::emptyEqns());
                    shared = BackendDAEUtil::createEmptyShared(openmodelica_backend_types::BackendDAE::BackendDAEType::ARRAYSYSTEM, iextra.clone(), FCore::emptyCache(), FGraph::empty())?;
                    subsystem_dae = Arc::new(BackendDAE::BackendDAE { eqs: list![syst.clone()], shared: shared.clone() });
                    let (__pa1, __pa0, __pa2) = ::match_deref::match_deref! { match &(BackendDAEUtil::transformBackendDAE(subsystem_dae.clone(), Some((openmodelica_backend_types::BackendDAE::IndexReduction::NO_INDEX_REDUCTION, openmodelica_backend_types::BackendDAE::EquationConstraints::ALLOW_UNDERCONSTRAINED)), None, None)?) {
                        Deref @ BackendDAE::BackendDAE { eqs: Deref @ metamodelica::List::Cons { head: __pa1 @ Deref @ BackendDAE::EqSystem { matching: Deref @ BackendDAE::Matching::MATCHING { comps: __pa0, .. }, .. }, tail: Deref @ metamodelica::List::Nil }, shared: __pa2 } => (__pa1.clone(), __pa0.clone(), __pa2.clone()),
                        _ => bail!("pattern mismatch"),
                    } };
                    comps = __pa0.clone();
                    syst = __pa1.clone();
                    shared = __pa2.clone();
                    (equations_, _, uniqueEqIndex, tempvars) = createEquations(includeWhen.clone(), skipDiscInZc.clone(), genDiscrete.clone(), skipDiscInAlgorithm.clone(), syst.clone(), shared.clone(), comps.clone(), iuniqueEqIndex.clone(), itempvars.clone())?;
                    Ok(((equations_.clone(), uniqueEqIndex.clone(), tempvars.clone()), equations_.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("SimCodeUtil.createEquationfromList failed.")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, ouniqueEqIndex, otempvars))
}

fn createSingleComplexEqnCode(mut inEquation: Arc<BackendDAE::Equation>, mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut iextra: BackendDAE::ExtraInfo, mut genDiscrete: bool, mut funcTree: Arc<AvlTreePathFunction::Tree>, mut clockIndex: Option<i32>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equations_, ouniqueEqIndex, otempvars) = 'mc: {
        let __mc_input = inEquation.clone();
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Equation::COMPLEX_EQUATION { attr: eqAttr, source, right: e2, left: e1, .. } => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut e2 = (*e2).clone();
                    let mut e1 = (*e1).clone();
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    crefs = List::map(inVars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    e1 = Expression::replaceDerOpInExp(e1.clone())?;
                    e2 = Expression::replaceDerOpInExp(e2.clone())?;
                    (equations_, uniqueEqIndex, tempvars) = createSingleComplexEqnCode2(crefs.clone(), e1.clone(), e2.clone(), iuniqueEqIndex.clone(), itempvars.clone(), source.clone(), eqAttr.clone(), iextra.clone(), genDiscrete.clone(), inVars.clone())?;
                    Ok(((equations_.clone(), uniqueEqIndex.clone(), tempvars.clone()), equations_.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Equation::COMPLEX_EQUATION { attr: eqAttr, source, right: e2, left: e1, .. } => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut homotopySupport: bool = false;
                    let mut e2 = (*e2).clone();
                    let mut e1 = (*e1).clone();
                    crefs = List::map(inVars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    let true = (List::mapMapBoolAnd(crefs.clone(), (std::sync::Arc::new(ComponentReference::crefLastType) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Type>> + 'static>), (std::sync::Arc::new(Types::isRealOrSubTypeReal) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Type>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    (e1, _) = ExpressionSimplify::simplify(e1.clone())?;
                    e1 = Expression::replaceDerOpInExp(e1.clone())?;
                    (e2, _) = ExpressionSimplify::simplify(e2.clone())?;
                    e2 = Expression::replaceDerOpInExp(e2.clone())?;
                    (resEqs, uniqueEqIndex, tempvars, crefs) = createNonlinearResidualEquationsSingleComplex(e1.clone(), e2.clone(), source.clone(), eqAttr.clone(), iuniqueEqIndex.clone(), itempvars.clone(), crefs.clone())?;
                    resEqs = fixNonlinearResidualIndices(resEqs.clone());
                    (_, homotopySupport) = BackendEquation::traverseExpsOfEquation(inEquation.clone(), (std::sync::Arc::new(BackendDAEUtil::containsHomotopyCall) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, bool) -> Result<(Arc<DAE::Exp>, bool)> + 'static>), false)?;
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Arc::new(SimCode::NonlinearSystem { index: uniqueEqIndex.clone(), eqs: resEqs.clone(), crefs: crefs.clone(), indexNonLinearSystem: 0, nUnknowns: (inVars.clone().len() as i32) + (tempvars.clone().len() as i32) - (itempvars.clone().len() as i32), jacobianMatrix: None, homotopySupport: homotopySupport.clone(), mixedSystem: false, tornSystem: false, clockIndex: clockIndex.clone() }), alternativeTearing: None, eqAttr: eqAttr.clone() })], uniqueEqIndex.clone() + 1, tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Equation::COMPLEX_EQUATION { attr: eqAttr, .. } => {
                    let mut uniqueEqIndex: i32 = 0;
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut resEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut homotopySupport: bool = false;
                    crefs = List::map(inVars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    let true = (List::mapMapBoolAnd(crefs.clone(), (std::sync::Arc::new(ComponentReference::crefLastType) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Type>> + 'static>), (std::sync::Arc::new(Types::isRealOrSubTypeReal) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Type>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    let (__pa0, (__pa1, _), __pa2) = createNonlinearResidualEquations(list![inEquation.clone()], (iuniqueEqIndex.clone(), 0), itempvars.clone(), funcTree.clone())?;
                    resEqs = __pa0.clone();
                    uniqueEqIndex = __pa1.clone();
                    tempvars = __pa2.clone();
                    resEqs = fixNonlinearResidualIndices(resEqs.clone());
                    (_, homotopySupport) = BackendEquation::traverseExpsOfEquation(inEquation.clone(), (std::sync::Arc::new(BackendDAEUtil::containsHomotopyCall) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, bool) -> Result<(Arc<DAE::Exp>, bool)> + 'static>), false)?;
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Arc::new(SimCode::NonlinearSystem { index: uniqueEqIndex.clone(), eqs: resEqs.clone(), crefs: crefs.clone(), indexNonLinearSystem: 0, nUnknowns: (inVars.clone().len() as i32) + (tempvars.clone().len() as i32) - (itempvars.clone().len() as i32), jacobianMatrix: None, homotopySupport: homotopySupport.clone(), mixedSystem: false, tornSystem: false, clockIndex: clockIndex.clone() }), alternativeTearing: None, eqAttr: eqAttr.clone() })], uniqueEqIndex.clone() + 1, tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Equation::COMPLEX_EQUATION { right: e2, left: e1, .. } => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut s: ArcStr = arcstr::literal!("");
                    let mut s1: ArcStr = arcstr::literal!("");
                    let mut s2: ArcStr = arcstr::literal!("");
                    let mut s3: ArcStr = arcstr::literal!("");
                    crefs = List::map(inVars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    let false = (List::mapMapBoolAnd(crefs.clone(), (std::sync::Arc::new(ComponentReference::crefLastType) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Type>> + 'static>), (std::sync::Arc::new(Types::isRealOrSubTypeReal) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Type>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    s1 = (ExpressionBasics::printExpStr(e1.clone())?).clone();
                    s2 = (ExpressionBasics::printExpStr(e2.clone())?).clone();
                    s3 = (ComponentReferenceBasics::printComponentRefListStr(crefs.clone())?).clone();
                    s = stringAppendList(list![(literal!("No support of solving not real variables with a non-linear solver. Equation:\n")).clone(), (s1.clone()).clone(), (literal!(" = ")).clone(), (s2.clone()).clone(), (literal!(" solve for ")).clone(), (s3.clone()).clone()]);
                    Error::addInternalError((s.clone()).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::Equation::COMPLEX_EQUATION { right: e2, left: e1, .. } => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut s: ArcStr = arcstr::literal!("");
                    let mut s1: ArcStr = arcstr::literal!("");
                    let mut s2: ArcStr = arcstr::literal!("");
                    let mut s3: ArcStr = arcstr::literal!("");
                    crefs = List::map(inVars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    let true = (List::mapMapBoolAnd(crefs.clone(), (std::sync::Arc::new(ComponentReference::crefLastType) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Type>> + 'static>), (std::sync::Arc::new(Types::isRealOrSubTypeReal) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Type>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    s1 = (ExpressionBasics::printExpStr(e1.clone())?).clone();
                    s2 = (ExpressionBasics::printExpStr(e2.clone())?).clone();
                    s3 = (ComponentReferenceBasics::printComponentRefListStr(crefs.clone())?).clone();
                    s = stringAppendList(list![(literal!("complex equations currently only supported on form v = functioncall(...). Equation: ")).clone(), (s1.clone()).clone(), (literal!(" = ")).clone(), (s2.clone()).clone(), (literal!(" solve for ")).clone(), (s3.clone()).clone()]);
                    Error::addInternalError((s.clone()).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, ouniqueEqIndex, otempvars))
}

// TODO: are the cases really correct?
// mahge: These should be revised or removed!!
fn createSingleComplexEqnCode2(mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut inExp3: Arc<DAE::Exp>, mut inExp4: Arc<DAE::Exp>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut source: Arc<DAE::ElementSource>, mut eqKind: BackendDAE::EquationAttributes, mut iextra: BackendDAE::ExtraInfo, mut genDiscrete: bool, mut inVars: Arc<metamodelica::List<BackendDAE::Var>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    (equations_, ouniqueEqIndex, otempvars) = 'mc: {
        let __mc_input = (inExp3.clone(), inExp4.clone());
        if let Ok((__v, __wb0, __wb1, __wb2)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::CAST { exp: e1, .. }, _) => {
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = otempvars.clone();
                    let mut ouniqueEqIndex: i32 = ouniqueEqIndex.clone();
                    (equations_, ouniqueEqIndex, otempvars) = createSingleComplexEqnCode2(crefs.clone(), e1.clone(), inExp4.clone(), iuniqueEqIndex.clone(), itempvars.clone(), source.clone(), eqKind.clone(), iextra.clone(), genDiscrete.clone(), inVars.clone())?;
                    Ok(((equations_.clone(), ouniqueEqIndex.clone(), otempvars.clone()), equations_.clone(), otempvars.clone(), ouniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; otempvars = __wb1; ouniqueEqIndex = __wb2; break 'mc __v; }
        if let Ok((__v, __wb0, __wb1, __wb2)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ DAE::Exp::CAST { exp: e1, .. }) => {
                    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = equations_.clone();
                    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = otempvars.clone();
                    let mut ouniqueEqIndex: i32 = ouniqueEqIndex.clone();
                    (equations_, ouniqueEqIndex, otempvars) = createSingleComplexEqnCode2(crefs.clone(), inExp3.clone(), e1.clone(), iuniqueEqIndex.clone(), itempvars.clone(), source.clone(), eqKind.clone(), iextra.clone(), genDiscrete.clone(), inVars.clone())?;
                    Ok(((equations_.clone(), ouniqueEqIndex.clone(), otempvars.clone()), equations_.clone(), otempvars.clone(), ouniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { equations_ = __wb0; otempvars = __wb1; ouniqueEqIndex = __wb2; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (e1 @ Deref @ DAE::Exp::CREF { componentRef: cr2, .. }, e2) => {
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
                    let true = (List::all(crefs.clone(), (std::sync::Arc::new({ let __pe_b0 = cr2.clone(); move |__pe_a1| ComponentReferenceBasics::crefPrefixOf(__pe_b0.clone(), __pe_a1) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    (e2_1, _) = Expression::extendArrExp(e2.clone(), false)?;
                    tp = Expression::r#typeof(e1.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e1.clone(), exp: e2_1.clone(), source: source.clone() });
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![stms.clone()], eqAttr: eqKind.clone() })], iuniqueEqIndex.clone() + 1, itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (e1, e2 @ Deref @ DAE::Exp::CREF { componentRef: cr2, .. }) => {
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
                    let true = (List::all(crefs.clone(), (std::sync::Arc::new({ let __pe_b0 = cr2.clone(); move |__pe_a1| ComponentReferenceBasics::crefPrefixOf(__pe_b0.clone(), __pe_a1) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    (e1_1, _) = Expression::extendArrExp(e1.clone(), false)?;
                    tp = Expression::r#typeof(e2.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e2.clone(), exp: e1_1.clone(), source: source.clone() });
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![stms.clone()], eqAttr: eqKind.clone() })], iuniqueEqIndex.clone() + 1, itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: tp @ Deref @ DAE::Type::T_COMPLEX { varLst, complexClassType: ClassInf::State::RECORD { path: rpath }, .. }, .. }, expLst, path }, e2) => {
                    let mut cr1: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut crexplst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut eqSystlst_nonflat: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    let mut expLst = (*expLst).clone();
                    let true = (AbsynUtil::pathEqual(path.clone(), rpath.clone())) else { bail!("pattern mismatch") };
                    ht = HashSet::emptyHashSet();
                    ht = List::fold(crefs.clone(), (std::sync::Arc::new(BaseHashSet::add) as std::sync::Arc<dyn ::std::ops::Fn(_, _) -> Result<_> + 'static>), ht.clone())?;
                    (expLst, _) = Expression::traverseExpList(expLst.clone(), (std::sync::Arc::new({ let __pe_b1 = true; move |__pe_a0, __pe_a2| Expression::expandCrefs(__pe_a0, __pe_b1.clone(), __pe_a2) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, i32) -> Result<(Arc<DAE::Exp>, i32)> + 'static>), 0)?;
                    List::foldAllValue(expLst.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, ht.clone())?;
                    (e2_1, _) = Expression::extendArrExp(e2.clone(), false)?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr1 = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    e1_1 = Expression::crefToExp(cr1.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e1_1.clone(), exp: e2_1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![stms.clone()], eqAttr: eqKind.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    crexplst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr1.clone())?;
                    exptl = List::zip(expLst.clone(), crexplst.clone());
                    (eqSystlst_nonflat, uniqueEqIndex) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_SIMPLE_ASSIGNwithArray) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, i32) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32)> + 'static>), source.clone(), eqKind.clone(), uniqueEqIndex.clone(), metamodelica::nil())?;
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), List::flatten(eqSystlst_nonflat.clone())?);
                    tempvars = createTempVars(varLst.clone(), cr1.clone(), itempvars.clone())?;
                    Ok((eqSystlst.clone(), uniqueEqIndex.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::RECORD { ty: tp @ Deref @ DAE::Type::T_COMPLEX { varLst, .. }, exps: expLst, path, .. }, e2) => {
                    let mut cr1: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut crexplst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut eqSystlst_nonflat: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    let mut expLst = (*expLst).clone();
                    ht = HashSet::emptyHashSet();
                    ht = List::fold(crefs.clone(), (std::sync::Arc::new(BaseHashSet::add) as std::sync::Arc<dyn ::std::ops::Fn(_, _) -> Result<_> + 'static>), ht.clone())?;
                    (expLst, _) = Expression::traverseExpList(expLst.clone(), (std::sync::Arc::new({ let __pe_b1 = true; move |__pe_a0, __pe_a2| Expression::expandCrefs(__pe_a0, __pe_b1.clone(), __pe_a2) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, i32) -> Result<(Arc<DAE::Exp>, i32)> + 'static>), 0)?;
                    List::foldAllValue(expLst.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, ht.clone())?;
                    (e2_1, _) = Expression::extendArrExp(e2.clone(), false)?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr1 = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    e1_1 = Expression::crefToExp(cr1.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e1_1.clone(), exp: e2_1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![stms.clone()], eqAttr: eqKind.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    crexplst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr1.clone())?;
                    exptl = List::zip(expLst.clone(), crexplst.clone());
                    (eqSystlst_nonflat, uniqueEqIndex) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_SIMPLE_ASSIGNwithArray) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, i32) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32)> + 'static>), source.clone(), eqKind.clone(), uniqueEqIndex.clone(), metamodelica::nil())?;
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), List::flatten(eqSystlst_nonflat.clone())?);
                    tempvars = createTempVars(varLst.clone(), cr1.clone(), itempvars.clone())?;
                    Ok((eqSystlst.clone(), uniqueEqIndex.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (e1, Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: tp @ Deref @ DAE::Type::T_COMPLEX { varLst, complexClassType: ClassInf::State::RECORD { path: rpath }, .. }, .. }, expLst, path }) => {
                    let mut cr1: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut crexplst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut eqSystlst_nonflat: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    let mut expLst = (*expLst).clone();
                    let true = (AbsynUtil::pathEqual(path.clone(), rpath.clone())) else { bail!("pattern mismatch") };
                    ht = HashSet::emptyHashSet();
                    ht = List::fold(crefs.clone(), (std::sync::Arc::new(BaseHashSet::add) as std::sync::Arc<dyn ::std::ops::Fn(_, _) -> Result<_> + 'static>), ht.clone())?;
                    (expLst, _) = Expression::traverseExpList(expLst.clone(), (std::sync::Arc::new({ let __pe_b1 = true; move |__pe_a0, __pe_a2| Expression::expandCrefs(__pe_a0, __pe_b1.clone(), __pe_a2) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, i32) -> Result<(Arc<DAE::Exp>, i32)> + 'static>), 0)?;
                    List::foldAllValue(expLst.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, ht.clone())?;
                    (e1_1, _) = Expression::extendArrExp(e1.clone(), false)?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr1 = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    e2_1 = Expression::crefExp(cr1.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e2_1.clone(), exp: e1_1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![stms.clone()], eqAttr: eqKind.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    crexplst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr1.clone())?;
                    exptl = List::zip(expLst.clone(), crexplst.clone());
                    (eqSystlst_nonflat, uniqueEqIndex) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_SIMPLE_ASSIGNwithArray) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, i32) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32)> + 'static>), source.clone(), eqKind.clone(), uniqueEqIndex.clone(), metamodelica::nil())?;
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), List::flatten(eqSystlst_nonflat.clone())?);
                    tempvars = createTempVars(varLst.clone(), cr1.clone(), itempvars.clone())?;
                    Ok((eqSystlst.clone(), uniqueEqIndex.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (e1, Deref @ DAE::Exp::RECORD { ty: tp @ Deref @ DAE::Type::T_COMPLEX { varLst, .. }, exps: expLst, path, .. }) => {
                    let mut cr1: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut e1_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2_1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut stms: Arc<DAE::Statement> = Arc::new(<DAE::Statement as ::std::default::Default>::default());
                    let mut crexplst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut ident: ArcStr = arcstr::literal!("");
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    let mut simeqn_complex: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut eqSystlst_nonflat: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    let mut expLst = (*expLst).clone();
                    ht = HashSet::emptyHashSet();
                    ht = List::fold(crefs.clone(), (std::sync::Arc::new(BaseHashSet::add) as std::sync::Arc<dyn ::std::ops::Fn(_, _) -> Result<_> + 'static>), ht.clone())?;
                    (expLst, _) = Expression::traverseExpList(expLst.clone(), (std::sync::Arc::new({ let __pe_b1 = true; move |__pe_a0, __pe_a2| Expression::expandCrefs(__pe_a0, __pe_b1.clone(), __pe_a2) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, i32) -> Result<(Arc<DAE::Exp>, i32)> + 'static>), 0)?;
                    List::foldAllValue(expLst.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, ht.clone())?;
                    (e1_1, _) = Expression::extendArrExp(e1.clone(), false)?;
                    ident = (AbsynUtil::pathStringUnquoteReplaceDot(path.clone(), (literal!("_")).clone())?).clone();
                    cr1 = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*ident.clone()); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone(), tp.clone(), metamodelica::nil());
                    e2_1 = Expression::crefExp(cr1.clone())?;
                    stms = Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: e2_1.clone(), exp: e1_1.clone(), source: source.clone() });
                    simeqn_complex = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![stms.clone()], eqAttr: eqKind.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    crexplst = List::map1(varLst.clone(), (std::sync::Arc::new(Expression::generateCrefsExpFromExpVar) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Var>, Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>), cr1.clone())?;
                    exptl = List::zip(expLst.clone(), crexplst.clone());
                    (eqSystlst_nonflat, uniqueEqIndex) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_SIMPLE_ASSIGNwithArray) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, i32) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32)> + 'static>), source.clone(), eqKind.clone(), uniqueEqIndex.clone(), metamodelica::nil())?;
                    eqSystlst = metamodelica::cons(simeqn_complex.clone(), List::flatten(eqSystlst_nonflat.clone())?);
                    tempvars = createTempVars(varLst.clone(), cr1.clone(), itempvars.clone())?;
                    Ok((eqSystlst.clone(), uniqueEqIndex.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (e1 @ Deref @ DAE::Exp::TUPLE { PR: expl }, e2 @ Deref @ DAE::Exp::CALL { .. }) => {
                    let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    tp = Expression::r#typeof(e1.clone())?;
                    ht = HashSet::emptyHashSet();
                    ht = List::fold(crefs.clone(), (std::sync::Arc::new(BaseHashSet::add) as std::sync::Arc<dyn ::std::ops::Fn(_, _) -> Result<_> + 'static>), ht.clone())?;
                    let false = (Expression::expHasCrefsNoPreOrStart(e2.clone(), crefs.clone())?) else { bail!("pattern mismatch") };
                    List::foldAllValue(expl.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, ht.clone())?;
                    eqSystlst = list![Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![Arc::new(DAE::Statement::STMT_TUPLE_ASSIGN { type_: tp.clone(), expExpLst: expl.clone(), exp: e2.clone(), source: source.clone() })], eqAttr: eqKind.clone() })];
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    Ok((eqSystlst.clone(), uniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::TUPLE { PR: expl }, Deref @ DAE::Exp::TUPLE { PR: expl1 }) => {
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    let mut eqnLst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    ht = HashSet::emptyHashSet();
                    ht = List::fold(crefs.clone(), (std::sync::Arc::new(BaseHashSet::add) as std::sync::Arc<dyn ::std::ops::Fn(_, _) -> Result<_> + 'static>), ht.clone())?;
                    List::foldAllValue(expl.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, ht.clone())?;
                    eqnLst = List::threadMap2(expl.clone(), expl1.clone(), (std::sync::Arc::new(BackendEquation::generateEquation) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::ElementSource>, BackendDAE::EquationAttributes) -> Result<Arc<BackendDAE::Equation>> + 'static>), source.clone(), eqKind.clone())?;
                    (eqSystlst, uniqueEqIndex, tempvars) = createEquationsfromList(eqnLst.clone(), inVars.clone(), iuniqueEqIndex.clone(), itempvars.clone(), iextra.clone(), genDiscrete.clone(), false, false, false)?;
                    Ok((eqSystlst.clone(), uniqueEqIndex.clone(), tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::TUPLE { PR: expl1 }, Deref @ DAE::Exp::TUPLE { PR: expl }) => {
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    let mut eqnLst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    ht = HashSet::emptyHashSet();
                    ht = List::fold(crefs.clone(), (std::sync::Arc::new(BaseHashSet::add) as std::sync::Arc<dyn ::std::ops::Fn(_, _) -> Result<_> + 'static>), ht.clone())?;
                    List::foldAllValue(expl.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, ht.clone())?;
                    eqnLst = List::threadMap2(expl.clone(), expl1.clone(), (std::sync::Arc::new(BackendEquation::generateEquation) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>, Arc<DAE::ElementSource>, BackendDAE::EquationAttributes) -> Result<Arc<BackendDAE::Equation>> + 'static>), source.clone(), eqKind.clone())?;
                    (eqSystlst, uniqueEqIndex, _) = createEquationsfromList(eqnLst.clone(), inVars.clone(), iuniqueEqIndex.clone(), itempvars.clone(), iextra.clone(), genDiscrete.clone(), false, false, false)?;
                    Ok((eqSystlst.clone(), uniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (e1, e2) => {
                    let mut s: ArcStr = arcstr::literal!("");
                    let mut s1: ArcStr = arcstr::literal!("");
                    let mut s2: ArcStr = arcstr::literal!("");
                    let mut s3: ArcStr = arcstr::literal!("");
                    let true = (Flags::isSet(Flags::FAILTRACE.clone())?) else { bail!("pattern mismatch") };
                    s1 = (ExpressionBasics::printExpStr(e1.clone())?).clone();
                    s2 = (ExpressionBasics::printExpStr(e2.clone())?).clone();
                    s3 = (ComponentReferenceBasics::printComponentRefListStr(crefs.clone())?).clone();
                    s = stringAppendList(list![(literal!("function createSingleComplexEqnCode2 failed for: ")).clone(), (s1.clone()).clone(), (literal!(" = ")).clone(), (s2.clone()).clone(), (literal!(" solve for ")).clone(), (s3.clone()).clone()]);
                    Debug::traceln((s.clone()).clone())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, ouniqueEqIndex, otempvars))
}

fn createSingleComplexEqnCode3(mut inExp: Arc<DAE::Exp>, mut iht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> {
    let mut outB: bool = false;
    let mut oht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
    (outB, oht) = 'mc: {
        let __mc_input = inExp.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CREF { componentRef: cr, .. } => {
                    if !((BaseHashSet::has(cr.clone(), iht.clone())?)) { bail!("guard") }
                    let mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    ht = BaseHashSet::delete(cr.clone(), iht.clone())?;
                    Ok((true, ht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CREF { componentRef: cr, .. } => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut expLst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    crefs = ComponentReference::expandCref(cr.clone(), true)?;
                    let false = (list![cr.clone()] == crefs.clone()) else { bail!("pattern mismatch") };
                    expLst = List::map(crefs.clone(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>))?;
                    List::foldAllValue(expLst.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, iht.clone())?;
                    Ok((true, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::RCONST { real: _ } => {
                    Ok((true, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::ICONST { integer: _ } => {
                    Ok((true, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::BCONST { bool: _ } => {
                    Ok((true, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CREF { componentRef: Deref @ DAE::ComponentRef::WILD { .. }, .. } => {
                    Ok((true, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CALL { expLst, .. } => {
                    List::foldAllValue(expLst.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, iht.clone())?;
                    Ok((true, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::RECORD { exps: expLst, .. } => {
                    List::foldAllValue(expLst.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, iht.clone())?;
                    Ok((true, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::ARRAY { array: expLst, .. } => {
                    List::foldAllValue(expLst.clone(), (std::sync::Arc::new(createSingleComplexEqnCode3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(bool, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), true, iht.clone())?;
                    Ok((true, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((false, iht.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outB, oht))
}

fn createSingleArrayEqnCode(mut genDiscrete: bool, mut inEquations: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, mut inVars: Arc<metamodelica::List<BackendDAE::Var>>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut shared: Arc<BackendDAE::Shared>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut noDiscequations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut inEquation: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
    inEquation = (::match_deref::match_deref! { match &(listHead(inEquations.clone())?) {
        inEquation @ Deref @ BackendDAE::Equation::ARRAY_EQUATION { dimSize: _, .. } => {
            let mut inEquation = (*inEquation).clone();
            assign_variant_field!(inEquation => BackendDAE::Equation::ARRAY_EQUATION;
                left = Expression::replaceDerOpInExp(var_field!((*inEquation).left, BackendDAE::Equation::ARRAY_EQUATION).clone())?,
                right = Expression::replaceDerOpInExp(var_field!((*inEquation).right, BackendDAE::Equation::ARRAY_EQUATION).clone())?
            );
            inEquation.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    (equations_, noDiscequations, ouniqueEqIndex, otempvars) = 'mc: {
        let __mc_input = (inEquation.clone(), inVars.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Equation::ARRAY_EQUATION { attr: eqAttr, source, right: rhse @ Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "$stateSelectionSet" }, .. }, .. }, _) => {
                    let mut equation_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut uniqueEqIndex: i32 = 0;
                    equation_ = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![Arc::new(DAE::Statement::STMT_NORETCALL { exp: rhse.clone(), source: source.clone() })], eqAttr: eqAttr.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    Ok((list![equation_.clone()], list![equation_.clone()], uniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Equation::ARRAY_EQUATION { attr: eqAttr, source, right: rhse @ Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "$initialStateSelect" }, .. }, .. }, _) => {
                    let mut equation_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut uniqueEqIndex: i32 = 0;
                    equation_ = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: list![Arc::new(DAE::Statement::STMT_NORETCALL { exp: rhse.clone(), source: source.clone() })], eqAttr: eqAttr.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    Ok((list![equation_.clone()], list![equation_.clone()], uniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Equation::ARRAY_EQUATION { attr: eqAttr, source, right: e2, left: e1, dimSize: ds, .. }, _) => {
                    if !((Expression::isMatrix(e1.clone()) || Expression::isArray(e1.clone()))) { bail!("guard") }
                    let mut lhse: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut expLst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut expLstTmp: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut left: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut eqSystlst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut uniqueEqIndex: i32 = 0;
                    let mut dims: Arc<metamodelica::List<Arc<DAE::Dimension>>> = metamodelica::nil();
                    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
                    let mut crefstmp: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut ty: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
                    let mut basety: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
                    let mut exptl: Arc<metamodelica::List<(Arc<DAE::Exp>, Arc<DAE::Exp>)>> = metamodelica::nil();
                    expLst = Expression::flattenArrayExpToList(e1.clone())?;
                    ty = Expression::r#typeof(e1.clone())?;
                    (basety, dims) = TypesDump::flattenArrayType(ty.clone());
                    ty = Arc::new(DAE::Type::T_ARRAY { ty: basety.clone(), dims: dims.clone() });
                    left = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("$TMP_")); __mm_s.push_str(&*intString(iuniqueEqIndex.clone())); ArcStr::from(__mm_s) }).clone(), ty.clone(), metamodelica::nil());
                    lhse = Arc::new(DAE::Exp::CREF { componentRef: left.clone(), ty: ty.clone() });
                    crefstmp = ComponentReference::expandCref(left.clone(), false)?;
                    expLstTmp = List::map(crefstmp.clone(), (std::sync::Arc::new(Expression::crefExp) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::Exp>> + 'static>))?;
                    tempvars = createArrayTempVar(left.clone(), ds.clone(), expLstTmp.clone(), itempvars.clone())?;
                    exptl = List::zip(expLst.clone(), expLstTmp.clone());
                    (eqSystlst, uniqueEqIndex) = List::map2Fold(exptl.clone(), (std::sync::Arc::new(makeSES_SIMPLE_ASSIGN) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>), Arc<DAE::ElementSource>, BackendDAE::EquationAttributes, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), source.clone(), eqAttr.clone(), iuniqueEqIndex.clone(), metamodelica::nil())?;
                    eqSystlst = metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: uniqueEqIndex.clone(), lhs: lhse.clone(), exp: e2.clone(), source: source.clone(), eqAttr: eqAttr.clone() }), eqSystlst.clone());
                    Ok((eqSystlst.clone(), eqSystlst.clone(), uniqueEqIndex.clone() + 1, tempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Equation::ARRAY_EQUATION { attr: eqAttr, source, right: e2, left: e1 @ Deref @ DAE::Exp::CREF { componentRef: cr_1, ty: _ }, .. }, Deref @ metamodelica::List::Cons { head: BackendDAE::Var { varName: cr, .. }, tail: _ }) => {
                    if !((ComponentReferenceBasics::crefEqual(cr_1.clone(), ComponentReferenceBasics::crefStripLastSubs(cr.clone())?)?)) { bail!("guard") }
                    let mut equation_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut uniqueEqIndex: i32 = 0;
                    let mut e2 = (*e2).clone();
                    let mut e1 = (*e1).clone();
                    (e1, _) = BackendDAEUtil::collateArrExp(e1.clone(), None)?;
                    (e2, _) = BackendDAEUtil::collateArrExp(e2.clone(), None)?;
                    equation_ = Arc::new(SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: iuniqueEqIndex.clone(), lhs: e1.clone(), exp: e2.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    Ok((list![equation_.clone()], list![equation_.clone()], uniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Equation::ARRAY_EQUATION { attr: eqAttr, source, right: e2 @ Deref @ DAE::Exp::CREF { componentRef: cr_1, ty: _ }, left: e1, .. }, Deref @ metamodelica::List::Cons { head: BackendDAE::Var { varName: cr, .. }, tail: _ }) => {
                    if !((ComponentReferenceBasics::crefEqual(cr_1.clone(), ComponentReferenceBasics::crefStripLastSubs(cr.clone())?)?)) { bail!("guard") }
                    let mut equation_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut uniqueEqIndex: i32 = 0;
                    let mut e2 = (*e2).clone();
                    let mut e1 = (*e1).clone();
                    (e1, _) = BackendDAEUtil::collateArrExp(e1.clone(), None)?;
                    (e2, _) = BackendDAEUtil::collateArrExp(e2.clone(), None)?;
                    equation_ = Arc::new(SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: iuniqueEqIndex.clone(), lhs: e2.clone(), exp: e1.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    Ok((list![equation_.clone()], list![equation_.clone()], uniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::Equation::ARRAY_EQUATION { attr: eqAttr, source, right: rhse, left: lhse, .. }, Deref @ metamodelica::List::Cons { head: BackendDAE::Var { varName: cr, .. }, tail: _ }) => {
                    let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut funcs: Arc<AvlTreePathFunction::Tree> = Arc::new(AvlTreePathFunction::Tree::EMPTY);
                    let mut equation_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
                    let mut uniqueEqIndex: i32 = 0;
                    let mut rhse = (*rhse).clone();
                    let mut lhse = (*lhse).clone();
                    (lhse, _) = BackendDAEUtil::collateArrExp(lhse.clone(), None)?;
                    (rhse, _) = BackendDAEUtil::collateArrExp(rhse.clone(), None)?;
                    let __pa0 = ::match_deref::match_deref! { match &(shared.clone()) {
                        Deref @ BackendDAE::Shared { functionTree: __pa0, .. } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    funcs = __pa0.clone();
                    e1 = Expression::crefExp(cr.clone())?;
                    (e2, _, _, _) = ExpressionSolve::solve2(lhse.clone(), rhse.clone(), e1.clone(), Some(funcs.clone()), Some(iuniqueEqIndex.clone()), true, BackendDAEUtil::isSimulationDAE(shared.clone()))?;
                    equation_ = Arc::new(SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: iuniqueEqIndex.clone(), lhs: e1.clone(), exp: e2.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
                    uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    Ok((list![equation_.clone()], list![equation_.clone()], uniqueEqIndex.clone(), itempvars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let mut r#str: ArcStr = arcstr::literal!("");
                    let __pa0 = ::match_deref::match_deref! { match &(inVars.clone()) {
                        Deref @ metamodelica::List::Cons { head: BackendDAE::Var { varName: __pa0, .. }, tail: _ } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    cr = __pa0.clone();
                    r#str = (BackendDump::dumpEqnsStr(inEquations.clone())?).clone();
                    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("solving array equation: ")); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("\nfor variable: ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cr.clone())?); __mm_s.push_str(&*literal!(".")); ArcStr::from(__mm_s) }).clone();
                    Error::addInternalError((r#str.clone()).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((equations_, noDiscequations, ouniqueEqIndex, otempvars))
}

fn createSingleAlgorithmCode(mut eqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, mut vars: Arc<metamodelica::List<BackendDAE::Var>>, mut skipDiscinAlgorithm: bool, mut iuniqueEqIndex: i32, mut clockIndex: Option<i32>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32)> {
    let mut equations_: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = 0;
    (equations_, ouniqueEqIndex) = ({
        let mut debug: bool = false;
        'mc: {
        let __mc_input = (eqns.clone(), skipDiscinAlgorithm.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::ALGORITHM { attr: eqAttr, expand: crefExpand, source, alg, .. }, tail: _ }, false) => {
                    let mut solvedVars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut algStatements: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
                    solvedVars = List::map(vars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    let true = (CheckModel::isCrefListAlgorithmOutput(solvedVars.clone(), alg.clone(), source.clone(), crefExpand.clone())?) else { bail!("pattern mismatch") };
                    let __pa0 = ::match_deref::match_deref! { match &(BackendDAEUtil::collateAlgorithm(alg.clone(), None)?) {
                        Deref @ DAE::Algorithm { statementLst: __pa0 } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    algStatements = __pa0.clone();
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: algStatements.clone(), eqAttr: eqAttr.clone() })], iuniqueEqIndex.clone() + 1))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::ALGORITHM { attr: eqAttr, expand: crefExpand, source, alg, .. }, tail: _ }, true) => {
                    let mut solvedVars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut algStatements: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
                    solvedVars = List::map(vars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    let true = (CheckModel::isCrefListAlgorithmOutput(solvedVars.clone(), alg.clone(), source.clone(), crefExpand.clone())?) else { bail!("pattern mismatch") };
                    let __pa0 = ::match_deref::match_deref! { match &(BackendDAEUtil::collateAlgorithm(alg.clone(), None)?) {
                        Deref @ DAE::Algorithm { statementLst: __pa0 } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    algStatements = __pa0.clone();
                    algStatements = BackendDAEUtil::removeDiscreteAssignments(algStatements.clone(), BackendVariable::listVar1(vars.clone())?)?;
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: algStatements.clone(), eqAttr: eqAttr.clone() })], iuniqueEqIndex.clone() + 1))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::ALGORITHM { attr: eqAttr, alg, .. }, tail: _ }, false) => {
                    let mut algStatements: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
                    let __pa0 = ::match_deref::match_deref! { match &(BackendDAEUtil::collateAlgorithm(alg.clone(), None)?) {
                        Deref @ DAE::Algorithm { statementLst: __pa0 } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    algStatements = __pa0.clone();
                    algStatements = solveAlgorithmInverse(algStatements.clone(), vars.clone())?;
                    Ok((list![Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: algStatements.clone(), eqAttr: eqAttr.clone() })], iuniqueEqIndex.clone() + 1))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::ALGORITHM { attr: eqAttr, expand: crefExpand, source, alg: alg @ Deref @ DAE::Algorithm { statementLst: algStatements }, .. }, tail: _ }, _) => {
                    let mut solvedVars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut algOutVars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut knownOutputCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut result: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut discreteVarsExp: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut alg = (*alg).clone();
                    let mut algStatements = (*algStatements).clone();
                    let mut ouniqueEqIndex: i32 = ouniqueEqIndex.clone();
                    if debug.clone() {
                        println!("{}", (literal!("createSingleAlgorithmCode ->\n")).clone());
                        BackendDump::dumpAlgorithms(list![Arc::new(DAE::Algorithm { statementLst: algStatements.clone() })], 0)?;
                    }
                    solvedVars = List::map(vars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    solvedVars = List::unionList(List::map1(solvedVars.clone(), (std::sync::Arc::new(ComponentReference::expandCref) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, bool) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> + 'static>), true)?)?;
                    if debug.clone() {
                        BackendDump::debugStrCrefLstStr((literal!("solvedVars : ")).clone(), solvedVars.clone(), (literal!(", ")).clone(), (literal!("\n")).clone())?;
                    }
                    algOutVars = CheckModel::checkAndGetAlgorithmOutputs(alg.clone(), source.clone(), crefExpand.clone())?;
                    algOutVars = List::unionList(List::map1(algOutVars.clone(), (std::sync::Arc::new(ComponentReference::expandCref) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, bool) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> + 'static>), true)?)?;
                    knownOutputCrefs = List::setDifference(algOutVars.clone(), solvedVars.clone())?;
                    solvedVars = List::map(List::filterOnTrue(vars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::isVarNonDiscrete, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<bool> + 'static>))?, (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    discreteVarsExp = ({
        let mut __acc: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
        for mut v in (List::filterOnTrue(vars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::isVarDiscrete, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<bool> + 'static>))?).into_iter().cloned() {
                    let __x = Expression::crefToExp(BackendVariable::varCref(v.clone())?)?;
                    __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                    algStatements = BackendDAEOptimize::expandAlgorithmStmts(algStatements.clone(), discreteVarsExp.clone(), BackendVariable::listVar(vars.clone())?, true)?;
                    if debug.clone() {
                        BackendDump::debugStrCrefLstStr((literal!("algOutVars : ")).clone(), algOutVars.clone(), (literal!(", ")).clone(), (literal!("\n")).clone())?;
                        BackendDump::debugStrCrefLstStr((literal!("filtered solvedVars: ")).clone(), solvedVars.clone(), (literal!(", ")).clone(), (literal!("\n")).clone())?;
                        BackendDump::debugStrCrefLstStr((literal!("knownOutputCrefs : ")).clone(), knownOutputCrefs.clone(), (literal!(", ")).clone(), (literal!("\n")).clone())?;
                    }
                    alg = Arc::new(DAE::Algorithm { statementLst: algStatements.clone() });
                    let __pa0 = ::match_deref::match_deref! { match &(BackendDAEUtil::collateAlgorithm(alg.clone(), None)?) {
                        Deref @ DAE::Algorithm { statementLst: __pa0 } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    algStatements = __pa0.clone();
                    if !(solvedVars.clone().is_empty()) {
                        result = list![Arc::new(SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Arc::new(SimCode::NonlinearSystem { index: iuniqueEqIndex.clone() + 1, eqs: list![Arc::new(SimCode::SimEqSystem::SES_INVERSE_ALGORITHM { index: iuniqueEqIndex.clone(), statements: algStatements.clone(), knownOutputCrefs: knownOutputCrefs.clone(), insideNonLinearSystem: true, eqAttr: eqAttr.clone() })], crefs: solvedVars.clone(), indexNonLinearSystem: 0, nUnknowns: (vars.clone().len() as i32), jacobianMatrix: None, homotopySupport: false, mixedSystem: false, tornSystem: false, clockIndex: clockIndex.clone() }), alternativeTearing: None, eqAttr: eqAttr.clone() })];
                        ouniqueEqIndex = iuniqueEqIndex.clone() + 2;
                    } else {
                        result = list![Arc::new(SimCode::SimEqSystem::SES_INVERSE_ALGORITHM { index: iuniqueEqIndex.clone(), statements: algStatements.clone(), knownOutputCrefs: knownOutputCrefs.clone(), insideNonLinearSystem: false, eqAttr: eqAttr.clone() })];
                        ouniqueEqIndex = iuniqueEqIndex.clone() + 1;
                    }
                    Ok(((result.clone(), ouniqueEqIndex.clone()), ouniqueEqIndex.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { ouniqueEqIndex = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::ALGORITHM { expand: crefExpand, source, alg, .. }, tail: _ }, _) => {
                    let mut solvedVars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut crefsStr: ArcStr = arcstr::literal!("");
                    let mut algStr: ArcStr = arcstr::literal!("");
                    solvedVars = List::map(vars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
                    let false = (CheckModel::isCrefListAlgorithmOutput(solvedVars.clone(), alg.clone(), source.clone(), crefExpand.clone())?) else { bail!("pattern mismatch") };
                    crefsStr = (ComponentReferenceBasics::printComponentRefListStr(solvedVars.clone())?).clone();
                    algStr = (DAEDump::dumpAlgorithmsStr(list![Arc::new(DAE::Element::ALGORITHM { algorithm_: alg.clone(), source: source.clone() })])?).clone();
                    Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Inverse Algorithm needs to be solved for ")); __mm_s.push_str(&*crefsStr.clone()); __mm_s.push_str(&*literal!(" in\n")); __mm_s.push_str(&*algStr.clone()); __mm_s.push_str(&*literal!("Discrete variables are not supported yet.")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("function createSingleAlgorithmCode failed")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    }
    });
    Ok((equations_, ouniqueEqIndex))
}

pub fn createInitialEquations(mut inInitDAE: Arc<BackendDAE::BackendDAE>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut outInitialEqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = iuniqueEqIndex.clone();
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = itempvars.clone();
    let mut removedEqs: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut uniqueEqIndex: i32 = 0;
    let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut knownVarEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut solvedEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut removedEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut aliasEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut systs: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let mut shared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
    let mut globalKnownVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut aliasVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let (__pa0, __pa3, __pa1, __pa2) = ::match_deref::match_deref! { match &(inInitDAE.clone()) {
        Deref @ BackendDAE::BackendDAE { eqs: __pa0, shared: __pa3 @ Deref @ BackendDAE::Shared { aliasVars: __pa1, globalKnownVars: __pa2, .. } } => (__pa0.clone(), __pa3.clone(), __pa1.clone(), __pa2.clone()),
        _ => bail!("pattern mismatch"),
    } };
    systs = __pa0.clone();
    aliasVars = __pa1.clone();
    globalKnownVars = __pa2.clone();
    shared = __pa3.clone();
    removedEqs = BackendDAEUtil::collapseRemovedEqs(inInitDAE.clone())?;
    (uniqueEqIndex, knownVarEquations) = BackendVariable::traverseBackendDAEVars(globalKnownVars.clone(), (std::sync::Arc::new(traverseKnVarsToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (iuniqueEqIndex.clone(), metamodelica::nil()))?;
    (uniqueEqIndex, _, _, _, solvedEquations, _, tempvars, _, _, _, _) = createEquationsForSystems(systs.clone(), shared.clone(), uniqueEqIndex.clone(), metamodelica::nil(), itempvars.clone(), 0, openmodelica_simcode_types::SimCode::BackendMapping::NO_MAPPING, true)?;
    (uniqueEqIndex, removedEquations) = BackendEquation::traverseEquationArray(removedEqs.clone(), (std::sync::Arc::new(traversedlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(Arc<BackendDAE::Equation>, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (uniqueEqIndex.clone(), metamodelica::nil()))?;
    (uniqueEqIndex, aliasEquations) = BackendVariable::traverseBackendDAEVars(aliasVars.clone(), (std::sync::Arc::new(traverseAliasVarsToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (uniqueEqIndex.clone(), metamodelica::nil()))?;
    allEquations = Dangerous::listReverseInPlace(aliasEquations.clone());
    allEquations = listAppend(removedEquations.clone(), allEquations.clone());
    allEquations = List::append_reverse(solvedEquations.clone(), allEquations.clone());
    allEquations = listAppend(knownVarEquations.clone(), allEquations.clone());
    outInitialEqns = allEquations.clone();
    ouniqueEqIndex = uniqueEqIndex.clone();
    otempvars = tempvars.clone();
    Ok((outInitialEqns, ouniqueEqIndex, otempvars))
}

pub fn createInitialEquations_lambda0(mut inInitDAE: Arc<BackendDAE::BackendDAE>, mut iuniqueEqIndex: i32, mut itempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32, Arc<metamodelica::List<SimCodeVar::SimVar>>)> {
    let mut outInitialEqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut ouniqueEqIndex: i32 = iuniqueEqIndex.clone();
    let mut otempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = itempvars.clone();
    let mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut uniqueEqIndex: i32 = 0;
    let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut knownEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut solvedEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut aliasEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut systs: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let mut shared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
    let mut globalKnownVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut aliasVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let (__pa0, __pa3, __pa1, __pa2) = ::match_deref::match_deref! { match &(inInitDAE.clone()) {
        Deref @ BackendDAE::BackendDAE { eqs: __pa0, shared: __pa3 @ Deref @ BackendDAE::Shared { aliasVars: __pa1, globalKnownVars: __pa2, .. } } => (__pa0.clone(), __pa3.clone(), __pa1.clone(), __pa2.clone()),
        _ => bail!("pattern mismatch"),
    } };
    systs = __pa0.clone();
    aliasVars = __pa1.clone();
    globalKnownVars = __pa2.clone();
    shared = __pa3.clone();
    (uniqueEqIndex, knownEquations) = BackendVariable::traverseBackendDAEVars(globalKnownVars.clone(), (std::sync::Arc::new(traverseKnVarsToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (iuniqueEqIndex.clone(), metamodelica::nil()))?;
    (uniqueEqIndex, _, _, _, solvedEquations, _, tempvars, _, _, _, _) = createEquationsForSystems(systs.clone(), shared.clone(), uniqueEqIndex.clone(), metamodelica::nil(), itempvars.clone(), 0, openmodelica_simcode_types::SimCode::BackendMapping::NO_MAPPING, true)?;
    (uniqueEqIndex, aliasEquations) = BackendVariable::traverseBackendDAEVars(aliasVars.clone(), (std::sync::Arc::new(traverseAliasVarsToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> + 'static>), (uniqueEqIndex.clone(), metamodelica::nil()))?;
    allEquations = List::append_reverse(solvedEquations.clone(), aliasEquations.clone());
    allEquations = listAppend(knownEquations.clone(), allEquations.clone());
    outInitialEqns = allEquations.clone();
    ouniqueEqIndex = uniqueEqIndex.clone();
    otempvars = tempvars.clone();
    Ok((outInitialEqns, ouniqueEqIndex, otempvars))
}

fn traverseKnVarsToSimEqSystem(mut inVar: BackendDAE::Var, mut inTpl: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outTpl: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    (outVar, outTpl) = 'mc: {
        let __mc_input = (inVar.clone(), inTpl.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (v @ BackendDAE::Var { source, bindExp: Some(exp), varName: cr, .. }, (uniqueEqIndex, eqns)) => {
                    let false = (BackendVariable::isVarOnTopLevelAndInput(v.clone())) else { bail!("pattern mismatch") };
                    Ok((v.clone(), (uniqueEqIndex.clone() + 1, metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: uniqueEqIndex.clone(), cref: cr.clone(), exp: exp.clone(), source: source.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() }), eqns.clone()))))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((inVar.clone(), inTpl.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outVar, outTpl))
}

fn traverseAliasVarsToSimEqSystem(mut inVar: BackendDAE::Var, mut inTpl: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>))> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outTpl: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    (outVar, outTpl) = (::match_deref::match_deref! { match &((inVar.clone(), inTpl.clone())) {
        (v @ BackendDAE::Var { source, bindExp: Some(exp), varName: cr, .. }, (uniqueEqIndex, eqns)) => {
            (v.clone(), (uniqueEqIndex.clone() + 1, metamodelica::cons(Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: uniqueEqIndex.clone(), cref: cr.clone(), exp: exp.clone(), source: source.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() }), eqns.clone())))
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok((outVar, outTpl))
}

fn dlowEqToSimEqSystem(mut inEquation: Arc<BackendDAE::Equation>, mut iuniqueEqIndex: i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> {
    let mut outEquation: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut ouniqueEqIndex: i32 = 0;
    (outEquation, ouniqueEqIndex) = (::match_deref::match_deref! { match &(inEquation.clone()) {
        Deref @ BackendDAE::Equation::SOLVED_EQUATION { attr: eqAttr, source, exp: exp_, componentRef: cr } => {
            (Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: iuniqueEqIndex.clone(), cref: cr.clone(), exp: exp_.clone(), source: source.clone(), eqAttr: eqAttr.clone() }), iuniqueEqIndex.clone() + 1)
        },
        Deref @ BackendDAE::Equation::RESIDUAL_EQUATION { attr: eqAttr, source, exp: exp_ } => {
            (Arc::new(SimCode::SimEqSystem::SES_RESIDUAL { index: iuniqueEqIndex.clone(), res_index: 0, exp: exp_.clone(), source: source.clone(), eqAttr: eqAttr.clone() }), iuniqueEqIndex.clone() + 1)
        },
        Deref @ BackendDAE::Equation::ALGORITHM { attr: eqAttr, alg, .. } => {
            let mut algStatements: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
            let __pa0 = ::match_deref::match_deref! { match &(BackendDAEUtil::collateAlgorithm(alg.clone(), None)?) {
                Deref @ DAE::Algorithm { statementLst: __pa0 } => __pa0.clone(),
                _ => bail!("pattern mismatch"),
            } };
            algStatements = __pa0.clone();
            (Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: algStatements.clone(), eqAttr: eqAttr.clone() }), iuniqueEqIndex.clone() + 1)
        },
        Deref @ BackendDAE::Equation::WHEN_EQUATION { attr: eqAttr, source, whenEquation, .. } => {
            let mut cond: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut elseWhen: Arc<BackendDAE::WhenEquation> = Arc::new(<BackendDAE::WhenEquation as ::std::default::Default>::default());
            let mut whenStmtLst: Arc<metamodelica::List<BackendDAE::WhenOperator>> = metamodelica::nil();
            let mut oelseWhen: Option<Arc<BackendDAE::WhenEquation>> = None;
            let mut conditions: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut initialCall: bool = false;
            let mut elseWhenEquation: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut oelseWhenSimEq: Option<Arc<SimCode::SimEqSystem>> = None;
            let mut uniqueEqIndex: i32 = 0;
            let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(whenEquation.clone()) {
                Deref @ BackendDAE::WhenEquation { condition: __pa0, whenStmtLst: __pa1, elsewhenPart: __pa2 } => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                _ => bail!("pattern mismatch"),
            } };
            cond = __pa0.clone();
            whenStmtLst = __pa1.clone();
            oelseWhen = __pa2.clone();
            if isSome(oelseWhen.clone()) {
                let __pa3 = ::match_deref::match_deref! { match &(oelseWhen.clone()) {
                    Some(__pa3) => __pa3.clone(),
                    _ => bail!("pattern mismatch"),
                } };
                elseWhen = __pa3.clone();
                (elseWhenEquation, uniqueEqIndex) = createElseWhenEquation(elseWhen.clone(), metamodelica::nil(), iuniqueEqIndex.clone() + 1, source.clone(), eqAttr.clone())?;
                oelseWhenSimEq = Some(elseWhenEquation.clone());
            } else {
                uniqueEqIndex = iuniqueEqIndex.clone() + 1;
                oelseWhenSimEq = None;
            }
            (conditions, initialCall) = BackendDAEUtil::getConditionList(cond.clone())?;
            (Arc::new(SimCode::SimEqSystem::SES_WHEN { index: iuniqueEqIndex.clone(), conditions: conditions.clone(), initialCall: initialCall.clone(), whenStmtLst: whenStmtLst.clone(), elseWhen: oelseWhenSimEq.clone(), source: source.clone(), eqAttr: eqAttr.clone() }), uniqueEqIndex.clone())
        },
        _ => {
            if Flags::isSet(Flags::FAILTRACE.clone())? {
                Error::addInternalError((literal!("function dlowEqToSimEqSystem failed.")).clone(), metamodelica::sourceInfo!())?;
            }
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((outEquation, ouniqueEqIndex))
}

fn dlowAlgToSimEqSystem(mut inAlg: Arc<DAE::Algorithm>, mut iuniqueEqIndex: i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> {
    let mut outEquation: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut ouniqueEqIndex: i32 = 0;
    let mut algStatements: Arc<metamodelica::List<Arc<DAE::Statement>>> = metamodelica::nil();
    let __pa0 = ::match_deref::match_deref! { match &(BackendDAEUtil::collateAlgorithm(inAlg.clone(), None)?) {
        Deref @ DAE::Algorithm { statementLst: __pa0 } => __pa0.clone(),
        _ => bail!("pattern mismatch"),
    } };
    algStatements = __pa0.clone();
    outEquation = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: iuniqueEqIndex.clone(), statements: algStatements.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
    ouniqueEqIndex = iuniqueEqIndex.clone() + 1;
    Ok((outEquation, ouniqueEqIndex))
}

pub fn createStartValueEquations(mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut acc: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, BackendDAE::Variables)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, BackendDAE::Variables)> {
    let mut startValueEquations: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, BackendDAE::Variables) = (0, metamodelica::nil(), <BackendDAE::Variables as ::std::default::Default>::default());
    startValueEquations = 'mc: {
        let __mc_input = (syst.clone(), shared.clone(), acc.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ BackendDAE::EqSystem { orderedVars: vars, .. }, Deref @ BackendDAE::Shared { aliasVars: av, .. }, (uniqueEqIndex, simeqns, globalKnownVars)) => {
                    let mut startValueEquationsTmp2: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut simeqns1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let mut uniqueEqIndex = (*uniqueEqIndex).clone();
                    (startValueEquationsTmp2, _, _) = BackendVariable::traverseBackendDAEVars(vars.clone(), (std::sync::Arc::new(createInitialAssignmentsFromStart) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), av.clone(), globalKnownVars.clone()))?;
                    startValueEquationsTmp2 = startValueEquationsTmp2.clone().reverse();
                    (simeqns1, uniqueEqIndex) = List::mapFold(startValueEquationsTmp2.clone(), (std::sync::Arc::new(dlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone())?;
                    Ok((uniqueEqIndex.clone(), listAppend(simeqns1.clone(), simeqns.clone()), globalKnownVars.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Error::addInternalError((literal!("function createStartValueEquations failed")).clone(), metamodelica::sourceInfo!())?;
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(startValueEquations)
}

pub fn createValueEquationsShared(mut shared: Arc<BackendDAE::Shared>, mut valueFunction: Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> + 'static>, mut acc: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> {
    pub type Function = std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> + 'static>;

    let mut maxValueEquations: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    maxValueEquations = (::match_deref::match_deref! { match &((shared.clone(), acc.clone())) {
        (Deref @ BackendDAE::Shared { .. }, (uniqueEqIndex, simeqns)) => {
            let mut maxValueEquationsTmp: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut simeqns1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut uniqueEqIndex = (*uniqueEqIndex).clone();
            (maxValueEquationsTmp, _) = BackendVariable::traverseBackendDAEVars(shared.globalKnownVars.clone(), valueFunction.clone(), (metamodelica::nil(), shared.aliasVars.clone()))?;
            maxValueEquationsTmp = maxValueEquationsTmp.clone().reverse();
            (simeqns1, uniqueEqIndex) = List::mapFold(maxValueEquationsTmp.clone(), (std::sync::Arc::new(dlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone())?;
            (uniqueEqIndex.clone(), listAppend(simeqns1.clone(), simeqns.clone()))
        },
        _ => {
            Error::addInternalError((literal!("function createValueEquationsShared failed")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(maxValueEquations)
}

pub fn createNominalValueEquations(mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut acc: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> {
    let mut nominalValueEquations: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    nominalValueEquations = (::match_deref::match_deref! { match &((syst.clone(), shared.clone(), acc.clone())) {
        (Deref @ BackendDAE::EqSystem { .. }, Deref @ BackendDAE::Shared { .. }, (uniqueEqIndex, simeqns)) => {
            let mut nominalValueEquationsTmp: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut simeqns1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut uniqueEqIndex = (*uniqueEqIndex).clone();
            (nominalValueEquationsTmp, _) = BackendVariable::traverseBackendDAEVars(syst.orderedVars.clone(), (std::sync::Arc::new(createInitialAssignmentsFromNominal) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), shared.aliasVars.clone()))?;
            nominalValueEquationsTmp = nominalValueEquationsTmp.clone().reverse();
            (simeqns1, uniqueEqIndex) = List::mapFold(nominalValueEquationsTmp.clone(), (std::sync::Arc::new(dlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone())?;
            (uniqueEqIndex.clone(), listAppend(simeqns1.clone(), simeqns.clone()))
        },
        _ => {
            Error::addInternalError((literal!("function createNominalValueEquations failed")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(nominalValueEquations)
}

pub fn createMinValueEquations(mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut acc: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> {
    let mut minValueEquations: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    minValueEquations = (::match_deref::match_deref! { match &((syst.clone(), shared.clone(), acc.clone())) {
        (Deref @ BackendDAE::EqSystem { .. }, Deref @ BackendDAE::Shared { .. }, (uniqueEqIndex, simeqns)) => {
            let mut minValueEquationsTmp: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut simeqns1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut uniqueEqIndex = (*uniqueEqIndex).clone();
            (minValueEquationsTmp, _) = BackendVariable::traverseBackendDAEVars(syst.orderedVars.clone(), (std::sync::Arc::new(createInitialAssignmentsFromMin) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), shared.aliasVars.clone()))?;
            minValueEquationsTmp = minValueEquationsTmp.clone().reverse();
            (simeqns1, uniqueEqIndex) = List::mapFold(minValueEquationsTmp.clone(), (std::sync::Arc::new(dlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone())?;
            (uniqueEqIndex.clone(), listAppend(simeqns1.clone(), simeqns.clone()))
        },
        _ => {
            Error::addInternalError((literal!("function createMinValueEquations failed")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(minValueEquations)
}

pub fn createMaxValueEquations(mut syst: Arc<BackendDAE::EqSystem>, mut shared: Arc<BackendDAE::Shared>, mut acc: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> {
    let mut maxValueEquations: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    maxValueEquations = (::match_deref::match_deref! { match &((syst.clone(), shared.clone(), acc.clone())) {
        (Deref @ BackendDAE::EqSystem { .. }, Deref @ BackendDAE::Shared { .. }, (uniqueEqIndex, simeqns)) => {
            let mut maxValueEquationsTmp: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
            let mut simeqns1: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut uniqueEqIndex = (*uniqueEqIndex).clone();
            (maxValueEquationsTmp, _) = BackendVariable::traverseBackendDAEVars(syst.orderedVars.clone(), (std::sync::Arc::new(createInitialAssignmentsFromMax) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), shared.aliasVars.clone()))?;
            maxValueEquationsTmp = maxValueEquationsTmp.clone().reverse();
            (simeqns1, uniqueEqIndex) = List::mapFold(maxValueEquationsTmp.clone(), (std::sync::Arc::new(dlowEqToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::Equation>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), uniqueEqIndex.clone())?;
            (uniqueEqIndex.clone(), listAppend(simeqns1.clone(), simeqns.clone()))
        },
        _ => {
            Error::addInternalError((literal!("function createMaxValueEquations failed")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(maxValueEquations)
}

fn makeSolved_fromStartValue(mut inVar: BackendDAE::Var, mut inUniqueEqIndex: i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> {
    let mut outSimEqn: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut outUniqueEqIndex: i32 = 0;
    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut varExp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut source: Arc<DAE::ElementSource> = Arc::new(<DAE::ElementSource as ::std::default::Default>::default());
    e = BackendVariable::varBindExpStartValueNoFail(inVar.clone())?;
    source = BackendVariable::getVarSource(inVar.clone());
    if Types::isArray(inVar.varType.clone()) {
        varExp = BackendVariable::varExp(inVar.clone())?;
        outSimEqn = Arc::new(SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: inUniqueEqIndex.clone(), lhs: varExp.clone(), exp: e.clone(), source: source.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
    } else {
        cr = BackendVariable::varCref(inVar.clone())?;
        outSimEqn = Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: inUniqueEqIndex.clone(), cref: cr.clone(), exp: e.clone(), source: source.clone(), eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
    }
    outUniqueEqIndex = inUniqueEqIndex.clone() + 1;
    Ok((outSimEqn, outUniqueEqIndex))
}

pub fn createParameterEquations(mut inUniqueEqIndex: i32, mut acc: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut globalKnownVars: BackendDAE::Variables) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, i32)> {
    let mut outUniqueEqIndex: i32 = inUniqueEqIndex.clone();
    let mut outParameterEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut nFixedParameters: i32 = 0;
    let mut simvarasserts: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut varasserts: Arc<metamodelica::List<Arc<DAE::Algorithm>>> = metamodelica::nil();
    (outUniqueEqIndex, outParameterEquations, varasserts, nFixedParameters, _) = BackendVariable::traverseBackendDAEVars(globalKnownVars.clone(), (std::sync::Arc::new(createSimEqsForGlobalKnownVars) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<DAE::Algorithm>>>, i32, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::Exp>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>)))) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<DAE::Algorithm>>>, i32, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::Exp>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>))))> + 'static>), (outUniqueEqIndex.clone(), outParameterEquations.clone(), metamodelica::nil(), 0, HashSetExp::emptyHashSetSized(Util::nextPrime(globalKnownVars.numberOfVars.clone()))))?;
    if Flags::isSet(Flags::PARAM_DLOW_DUMP.clone())? {
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nparameters in order (")); __mm_s.push_str(&*intString((outParameterEquations.clone().len() as i32))); __mm_s.push_str(&*literal!(")\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        dumpSimEqSystemLst(outParameterEquations.clone().reverse(), (literal!("\n")).clone())?;
        println!("{}", (literal!("\n")).clone());
    }
    varasserts = metamodelica::Dangerous::listReverseInPlace(varasserts.clone());
    (simvarasserts, outUniqueEqIndex) = List::mapFold(varasserts.clone(), (std::sync::Arc::new(dlowAlgToSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Algorithm>, i32) -> Result<(Arc<SimCode::SimEqSystem>, i32)> + 'static>), outUniqueEqIndex.clone())?;
    outParameterEquations = List::append_reverse(simvarasserts.clone(), outParameterEquations.clone());
    outParameterEquations = List::append_reverse(acc.clone(), outParameterEquations.clone());
    outParameterEquations = outParameterEquations.clone().reverse();
    Ok((outUniqueEqIndex, outParameterEquations, nFixedParameters))
}

fn createSimEqsForGlobalKnownVars(mut globalKnownVar: BackendDAE::Var, mut inTuple: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<DAE::Algorithm>>>, i32, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::Exp>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>)))) -> Result<(BackendDAE::Var, (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<DAE::Algorithm>>>, i32, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::Exp>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<DAE::Exp>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>))))> {
    let mut globalKnownVar: BackendDAE::Var = globalKnownVar;
    let mut outTuple: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, Arc<metamodelica::List<Arc<DAE::Algorithm>>>, i32, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::Exp>>>), i32, i32, (HashSetExp::FuncHashCref, HashSetExp::FuncCrefEqual, HashSetExp::FuncCrefStr)));
    let mut uniqueEqIndex: i32 = 0;
    let mut nFixedParameters: i32 = 0;
    let mut simEq: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut parameterEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut varasserts: Arc<metamodelica::List<Arc<DAE::Algorithm>>> = metamodelica::nil();
    let mut varasserts2: Arc<metamodelica::List<Arc<DAE::Algorithm>>> = metamodelica::nil();
    let mut tplExpHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::Exp>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::Exp>>>), i32, i32, (HashSetExp::FuncHashCref, HashSetExp::FuncCrefEqual, HashSetExp::FuncCrefStr));
    (uniqueEqIndex, parameterEquations, varasserts, nFixedParameters, tplExpHT) = inTuple.clone();
    if BackendVariable::isParam(globalKnownVar.clone()) && BackendVariable::varFixed(globalKnownVar.clone()) {
        varasserts2 = createVarAsserts(globalKnownVar.clone())?;
        varasserts = List::append_reverse(varasserts2.clone(), varasserts.clone());
        nFixedParameters = nFixedParameters.clone() + 1;
    }
    if BackendVariable::isParam(globalKnownVar.clone()) && (BackendVariable::varFixed(globalKnownVar.clone()) && (BackendVariable::isFinalOrProtectedVar(globalKnownVar.clone()) || BackendVariable::varHasNonConstantBindExpOrStartValue(globalKnownVar.clone())?) || BackendVariable::varHasStartValue(globalKnownVar.clone())? && !(BackendVariable::varHasConstantStartExp(globalKnownVar.clone()))) || BackendVariable::isVarAlg(globalKnownVar.clone()) && !(BackendVariable::isInput(globalKnownVar.clone())) && BackendVariable::varFixed(globalKnownVar.clone()) || BackendVariable::isExtObj(globalKnownVar.clone()) && BackendVariable::varHasBindExp(globalKnownVar.clone()) && BackendVariable::varFixed(globalKnownVar.clone()) {
        let () = (::match_deref::match_deref! { match &(globalKnownVar.clone()) {
        BackendDAE::Var { tplExp: Some(tplExp @ Deref @ DAE::Exp::TUPLE { PR: expl }), .. } => {
            let mut call: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut source: Arc<DAE::ElementSource> = Arc::new(<DAE::ElementSource as ::std::default::Default>::default());
            let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            if BaseHashSet::has(tplExp.clone(), tplExpHT.clone())? {
                outTuple = (uniqueEqIndex.clone(), parameterEquations.clone(), varasserts.clone(), nFixedParameters.clone(), tplExpHT.clone());
                return Ok((globalKnownVar.clone(), outTuple.clone()));
            } else {
                tp = Expression::r#typeof(tplExp.clone())?;
                let __pa0 = ::match_deref::match_deref! { match &(globalKnownVar.bindExp.clone()) {
                    Some(__pa0) => __pa0.clone(),
                    _ => bail!("pattern mismatch"),
                } };
                call = __pa0.clone();
                source = BackendVariable::getVarSource(globalKnownVar.clone());
                simEq = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: uniqueEqIndex.clone(), statements: list![Arc::new(DAE::Statement::STMT_TUPLE_ASSIGN { type_: tp.clone(), expExpLst: expl.clone(), exp: call.clone(), source: source.clone() })], eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
                uniqueEqIndex = uniqueEqIndex.clone() + 1;
                tplExpHT = BaseHashSet::add(tplExp.clone(), tplExpHT.clone())?;
            }
            ()
        },
        BackendDAE::Var { tplExp: Some(rec), .. } => {
            let mut call: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut source: Arc<DAE::ElementSource> = Arc::new(<DAE::ElementSource as ::std::default::Default>::default());
            let mut tp: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            if BaseHashSet::has(rec.clone(), tplExpHT.clone())? {
                outTuple = (uniqueEqIndex.clone(), parameterEquations.clone(), varasserts.clone(), nFixedParameters.clone(), tplExpHT.clone());
                return Ok((globalKnownVar.clone(), outTuple.clone()));
            } else {
                tp = Expression::r#typeof(rec.clone())?;
                let __pa0 = ::match_deref::match_deref! { match &(globalKnownVar.bindExp.clone()) {
                    Some(__pa0) => __pa0.clone(),
                    _ => bail!("pattern mismatch"),
                } };
                call = __pa0.clone();
                source = BackendVariable::getVarSource(globalKnownVar.clone());
                simEq = Arc::new(SimCode::SimEqSystem::SES_ALGORITHM { index: uniqueEqIndex.clone(), statements: list![Arc::new(DAE::Statement::STMT_ASSIGN { type_: tp.clone(), exp1: rec.clone(), exp: call.clone(), source: source.clone() })], eqAttr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
                uniqueEqIndex = uniqueEqIndex.clone() + 1;
                tplExpHT = BaseHashSet::add(rec.clone(), tplExpHT.clone())?;
            }
            ()
        },
        _ => {
            (simEq, uniqueEqIndex) = makeSolved_fromStartValue(globalKnownVar.clone(), uniqueEqIndex.clone())?;
            ()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
        parameterEquations = metamodelica::cons(simEq.clone(), parameterEquations.clone());
    }
    outTuple = (uniqueEqIndex.clone(), parameterEquations.clone(), varasserts.clone(), nFixedParameters.clone(), tplExpHT.clone());
    Ok((globalKnownVar, outTuple))
}

fn createInitialAssignmentsFromStart(mut inVar: BackendDAE::Var, mut inTpl: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables, BackendDAE::Variables))> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outTpl: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables, BackendDAE::Variables) = (metamodelica::nil(), <BackendDAE::Variables as ::std::default::Default>::default(), <BackendDAE::Variables as ::std::default::Default>::default());
    (outVar, outTpl) = 'mc: {
        let __mc_input = (inVar.clone(), inTpl.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (var @ BackendDAE::Var { source, varName: cref, .. }, (eqns, av, globalKnownVars)) => {
                    let mut initialEquation: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
                    let mut startExp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut parameters: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    startExp = BackendVariable::varStartValueFail(var.clone())?;
                    parameters = Expression::getAllCrefs(startExp.clone())?;
                    let true = (BackendVariable::areAllCrefsPrimaryParameters(parameters.clone(), globalKnownVars.clone())) else { bail!("pattern mismatch") };
                    let false = (Expression::isConst(startExp.clone())?) else { bail!("pattern mismatch") };
                    let SimCodeVar::NOALIAS { .. } = (getAliasVar(var.clone(), Some(av.clone()))?) else { bail!("pattern mismatch") };
                    initialEquation = Arc::new(BackendDAE::Equation::SOLVED_EQUATION { componentRef: ComponentReference::crefPrefixStart(cref.clone()), exp: startExp.clone(), source: source.clone(), attr: BackendDAE::EQ_ATTR_DEFAULT_INITIAL.clone() });
                    Ok((var.clone(), (metamodelica::cons(initialEquation.clone(), eqns.clone()), av.clone(), globalKnownVars.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((inVar.clone(), inTpl.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outVar, outTpl))
}

pub fn createInitialAssignmentsFromNominal(mut inVar: BackendDAE::Var, mut inTpl: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outTpl: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables) = (metamodelica::nil(), <BackendDAE::Variables as ::std::default::Default>::default());
    (outVar, outTpl) = 'mc: {
        let __mc_input = (inVar.clone(), inTpl.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (var @ BackendDAE::Var { source, varName: name, .. }, (eqns, av)) => {
                    let mut initialEquation: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
                    let mut nominalv: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    nominalv = BackendVariable::varNominalValueFail(var.clone())?;
                    let false = (Expression::isConst(nominalv.clone())?) else { bail!("pattern mismatch") };
                    let SimCodeVar::NOALIAS { .. } = (getAliasVar(var.clone(), Some(av.clone()))?) else { bail!("pattern mismatch") };
                    initialEquation = Arc::new(BackendDAE::Equation::SOLVED_EQUATION { componentRef: name.clone(), exp: nominalv.clone(), source: source.clone(), attr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
                    Ok((var.clone(), (metamodelica::cons(initialEquation.clone(), eqns.clone()), av.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((inVar.clone(), inTpl.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outVar, outTpl))
}

pub fn createInitialAssignmentsFromMin(mut inVar: BackendDAE::Var, mut inTpl: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outTpl: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables) = (metamodelica::nil(), <BackendDAE::Variables as ::std::default::Default>::default());
    (outVar, outTpl) = 'mc: {
        let __mc_input = (inVar.clone(), inTpl.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (var @ BackendDAE::Var { source, varName: name, .. }, (eqns, av)) => {
                    let mut initialEquation: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
                    let mut minv: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    minv = BackendVariable::varMinValueFail(var.clone())?;
                    let false = (Expression::isConst(minv.clone())?) else { bail!("pattern mismatch") };
                    let SimCodeVar::NOALIAS { .. } = (getAliasVar(var.clone(), Some(av.clone()))?) else { bail!("pattern mismatch") };
                    initialEquation = Arc::new(BackendDAE::Equation::SOLVED_EQUATION { componentRef: name.clone(), exp: minv.clone(), source: source.clone(), attr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
                    Ok((var.clone(), (metamodelica::cons(initialEquation.clone(), eqns.clone()), av.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((inVar.clone(), inTpl.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outVar, outTpl))
}

pub fn createInitialAssignmentsFromMax(mut inVar: BackendDAE::Var, mut inTpl: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables))> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outTpl: (Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, BackendDAE::Variables) = (metamodelica::nil(), <BackendDAE::Variables as ::std::default::Default>::default());
    (outVar, outTpl) = 'mc: {
        let __mc_input = (inVar.clone(), inTpl.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (var @ BackendDAE::Var { source, varName: name, .. }, (eqns, av)) => {
                    let mut initialEquation: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
                    let mut maxv: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    maxv = BackendVariable::varMaxValueFail(var.clone())?;
                    let false = (Expression::isConst(maxv.clone())?) else { bail!("pattern mismatch") };
                    let SimCodeVar::NOALIAS { .. } = (getAliasVar(var.clone(), Some(av.clone()))?) else { bail!("pattern mismatch") };
                    initialEquation = Arc::new(BackendDAE::Equation::SOLVED_EQUATION { componentRef: name.clone(), exp: maxv.clone(), source: source.clone(), attr: BackendDAE::EQ_ATTR_DEFAULT_UNKNOWN.clone() });
                    Ok((var.clone(), (metamodelica::cons(initialEquation.clone(), eqns.clone()), av.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((inVar.clone(), inTpl.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outVar, outTpl))
}

fn createVarAsserts(mut inVar: BackendDAE::Var) -> Result<Arc<metamodelica::List<Arc<DAE::Algorithm>>>> {
    let mut outAlgs: Arc<metamodelica::List<Arc<DAE::Algorithm>>> = metamodelica::nil();
    (_, outAlgs) = BackendVariable::getMinMaxAsserts(inVar.clone(), metamodelica::nil())?;
    Ok(outAlgs)
}

pub fn createModelInfo(mut class_: Arc<Absyn::Path>, mut program: Absyn::Program, mut dlow: Arc<BackendDAE::BackendDAE>, mut inInitDAE: Arc<BackendDAE::BackendDAE>, mut functions: Arc<metamodelica::List<Arc<SimCodeFunction::Function::Function>>>, mut labels: Arc<metamodelica::List<ArcStr>>, mut numStateSets: i32, mut numSpatialDistributions: i32, mut fileDir: ArcStr, mut nSubClock: i32, mut tempVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<SimCode::ModelInfo> {
    let mut modelInfo: SimCode::ModelInfo = <SimCode::ModelInfo as ::std::default::Default>::default();
    let mut description: ArcStr = arcstr::literal!("");
    let mut directory: ArcStr = arcstr::literal!("");
    let mut version: ArcStr = arcstr::literal!("");
    let mut author: ArcStr = arcstr::literal!("");
    let mut license: ArcStr = arcstr::literal!("");
    let mut copyright: ArcStr = arcstr::literal!("");
    let mut fileName: ArcStr = arcstr::literal!("");
    let mut varInfo: SimCode::VarInfo = <SimCode::VarInfo as ::std::default::Default>::default();
    let mut vars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    let mut nx: i32 = 0;
    let mut ny: i32 = 0;
    let mut ndy: i32 = 0;
    let mut np: i32 = 0;
    let mut na: i32 = 0;
    let mut next: i32 = 0;
    let mut numOutVars: i32 = 0;
    let mut numInVars: i32 = 0;
    let mut ny_int: i32 = 0;
    let mut np_int: i32 = 0;
    let mut na_int: i32 = 0;
    let mut ny_bool: i32 = 0;
    let mut np_bool: i32 = 0;
    let mut numOptimizeConstraints: i32 = 0;
    let mut numOptimizeFinalConstraints: i32 = 0;
    let mut numRealInputVars: i32 = 0;
    let mut na_bool: i32 = 0;
    let mut ny_string: i32 = 0;
    let mut np_string: i32 = 0;
    let mut na_string: i32 = 0;
    let mut hasLargeEqSystems: bool = false;
    let mut unitDefinitions: Arc<metamodelica::List<SimCode::UnitDefinition>> = metamodelica::nil();
    let debug: bool = false;
    match '__try0: {
        directory = (System::trim((fileDir.clone()).clone(), (literal!("\"")).clone())).clone();
        version = (System::trim((unwrap_break_err!(ProgramUtil::getNamedAnnotationExp(class_.clone(), program.clone(), Arc::new(Absyn::Path::IDENT { name: (literal!("version")).clone() }), Some((literal!("")).clone()), (std::sync::Arc::new(ProgramUtil::getDefaultComponentPrefixesModStr) as std::sync::Arc<dyn ::std::ops::Fn(Option<Arc<Absyn::Modification>>) -> Result<ArcStr> + 'static>)), '__try0)).clone(), (literal!("\"")).clone())).clone();
        author = (System::trim((unwrap_break_err!(ProgramUtil::getNamedAnnotationExp(class_.clone(), program.clone(), Arc::new(Absyn::Path::IDENT { name: (literal!("__OpenModelica_author")).clone() }), Some((literal!("")).clone()), (std::sync::Arc::new(ProgramUtil::getDefaultComponentPrefixesModStr) as std::sync::Arc<dyn ::std::ops::Fn(Option<Arc<Absyn::Modification>>) -> Result<ArcStr> + 'static>)), '__try0)).clone(), (literal!("\"")).clone())).clone();
        license = (System::trim((unwrap_break_err!(ProgramUtil::getNamedAnnotationExp(class_.clone(), program.clone(), Arc::new(Absyn::Path::IDENT { name: (literal!("__OpenModelica_license")).clone() }), Some((literal!("")).clone()), (std::sync::Arc::new(ProgramUtil::getDefaultComponentPrefixesModStr) as std::sync::Arc<dyn ::std::ops::Fn(Option<Arc<Absyn::Modification>>) -> Result<ArcStr> + 'static>)), '__try0)).clone(), (literal!("\"")).clone())).clone();
        copyright = (System::trim((unwrap_break_err!(ProgramUtil::getNamedAnnotationExp(class_.clone(), program.clone(), Arc::new(Absyn::Path::IDENT { name: (literal!("__OpenModelica_copyright")).clone() }), Some((literal!("")).clone()), (std::sync::Arc::new(ProgramUtil::getDefaultComponentPrefixesModStr) as std::sync::Arc<dyn ::std::ops::Fn(Option<Arc<Absyn::Modification>>) -> Result<ArcStr> + 'static>)), '__try0)).clone(), (literal!("\"")).clone())).clone();
        fileName = (System::basename((unwrap_break_err!(AbsynUtil::classFilename(unwrap_break_err!(ProgramUtil::getPathedClassInProgram(class_.clone(), program.clone(), false, false), '__try0)), '__try0)).clone())).clone();
        (vars, unitDefinitions) = unwrap_break_err!(createVars(dlow.clone(), inInitDAE.clone(), tempVars.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createVars")).clone()), '__try0);
        }
        let __pa1 = ::match_deref::match_deref! { match &(dlow.clone()) {
            Deref @ BackendDAE::BackendDAE { shared: Deref @ BackendDAE::Shared { info: BackendDAE::ExtraInfo { description: __pa1, .. }, .. }, .. } => __pa1.clone(),
            _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
        } };
        description = __pa1.clone();
        nx = unwrap_break_err!(getNumScalars(vars.stateVars.clone()), '__try0);
        ny = unwrap_break_err!(getNumScalars(vars.algVars.clone()), '__try0);
        ndy = unwrap_break_err!(getNumScalars(vars.discreteAlgVars.clone()), '__try0);
        ny_int = unwrap_break_err!(getNumScalars(vars.intAlgVars.clone()), '__try0);
        ny_bool = unwrap_break_err!(getNumScalars(vars.boolAlgVars.clone()), '__try0);
        numOutVars = unwrap_break_err!(getNumScalars(vars.outputVars.clone()), '__try0);
        numInVars = unwrap_break_err!(getNumScalars(vars.inputVars.clone()), '__try0);
        na = unwrap_break_err!(getNumScalars(vars.aliasVars.clone()), '__try0);
        na_int = unwrap_break_err!(getNumScalars(vars.intAliasVars.clone()), '__try0);
        na_bool = unwrap_break_err!(getNumScalars(vars.boolAliasVars.clone()), '__try0);
        np = unwrap_break_err!(getNumScalars(vars.paramVars.clone()), '__try0);
        np_int = unwrap_break_err!(getNumScalars(vars.intParamVars.clone()), '__try0);
        np_bool = unwrap_break_err!(getNumScalars(vars.boolParamVars.clone()), '__try0);
        ny_string = unwrap_break_err!(getNumScalars(vars.stringAlgVars.clone()), '__try0);
        np_string = unwrap_break_err!(getNumScalars(vars.stringParamVars.clone()), '__try0);
        na_string = unwrap_break_err!(getNumScalars(vars.stringAliasVars.clone()), '__try0);
        next = unwrap_break_err!(getNumScalars(vars.extObjVars.clone()), '__try0);
        numOptimizeConstraints = unwrap_break_err!(getNumScalars(vars.realOptimizeConstraintsVars.clone()), '__try0);
        numOptimizeFinalConstraints = unwrap_break_err!(getNumScalars(vars.realOptimizeFinalConstraintsVars.clone()), '__try0);
        numRealInputVars = getNumberOfRealInputs(vars.inputVars.clone());
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: get lengths")).clone()), '__try0);
        }
        varInfo = unwrap_break_err!(createVarInfo(dlow.clone(), nx.clone(), ny.clone(), ndy.clone(), np.clone(), na.clone(), next.clone(), numOutVars.clone(), numInVars.clone(), ny_int.clone(), np_int.clone(), na_int.clone(), ny_bool.clone(), np_bool.clone(), na_bool.clone(), ny_string.clone(), np_string.clone(), na_string.clone(), numStateSets.clone(), numOptimizeConstraints.clone(), numOptimizeFinalConstraints.clone(), numRealInputVars.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: createVarInfo")).clone()), '__try0);
        }
        hasLargeEqSystems = unwrap_break_err!(hasLargeEquationSystems(dlow.clone(), inInitDAE.clone()), '__try0);
        if debug.clone() {
            unwrap_break_err!(execStat((literal!("simCode: hasLargeEquationSystems")).clone()), '__try0);
        }
        modelInfo = SimCode::ModelInfo { name: class_.clone(), description: (dlow.shared.info.description.clone()).clone(), version: (version.clone()).clone(), author: (author.clone()).clone(), license: (license.clone()).clone(), copyright: (copyright.clone()).clone(), directory: (directory.clone()).clone(), fileName: (fileName.clone()).clone(), varInfo: varInfo.clone(), vars: vars.clone(), functions: functions.clone(), labels: labels.clone(), resourcePaths: if (unwrap_break_err!(Flags::getConfigBool(Flags::BUILDING_FMU.clone()), '__try0)) {unwrap_break_err!(getResources(program.classes.clone(), dlow.clone(), inInitDAE.clone()), '__try0)} else {metamodelica::nil()}, sortedClasses: unwrap_break_err!(List::sort(program.classes.clone(), (std::sync::Arc::new(fnptr!(AbsynUtil::classNameGreater, Arc<Absyn::Class>, Arc<Absyn::Class>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<Absyn::Class>, Arc<Absyn::Class>) -> Result<bool> + 'static>)), '__try0), nClocks: metamodelica::arrayLength(dlow.shared.partitionsInfo.basePartitions.clone()), nSubClocks: metamodelica::arrayLength(dlow.shared.partitionsInfo.subPartitions.clone()), nSpatialDistributions: numSpatialDistributions.clone() + 1, hasLargeLinearEquationSystems: hasLargeEqSystems.clone(), linearSystems: metamodelica::nil(), nonLinearSystems: metamodelica::nil(), unitDefinitions: unitDefinitions.clone() };
        Ok::<_, anyhow::Error>((author.clone(), copyright.clone(), description.clone(), directory.clone(), fileName.clone(), hasLargeEqSystems.clone(), license.clone(), modelInfo.clone(), na.clone(), na_bool.clone(), na_int.clone(), na_string.clone(), ndy.clone(), next.clone(), np.clone(), np_bool.clone(), np_int.clone(), np_string.clone(), numInVars.clone(), numOptimizeConstraints.clone(), numOptimizeFinalConstraints.clone(), numOutVars.clone(), numRealInputVars.clone(), nx.clone(), ny.clone(), ny_bool.clone(), ny_int.clone(), ny_string.clone(), unitDefinitions.clone(), varInfo.clone(), vars.clone(), version.clone()))
    } {
        Ok((__try0_o0, __try0_o1, __try0_o2, __try0_o3, __try0_o4, __try0_o5, __try0_o6, __try0_o7, __try0_o8, __try0_o9, __try0_o10, __try0_o11, __try0_o12, __try0_o13, __try0_o14, __try0_o15, __try0_o16, __try0_o17, __try0_o18, __try0_o19, __try0_o20, __try0_o21, __try0_o22, __try0_o23, __try0_o24, __try0_o25, __try0_o26, __try0_o27, __try0_o28, __try0_o29, __try0_o30, __try0_o31)) => {
            author = __try0_o0;
            copyright = __try0_o1;
            description = __try0_o2;
            directory = __try0_o3;
            fileName = __try0_o4;
            hasLargeEqSystems = __try0_o5;
            license = __try0_o6;
            modelInfo = __try0_o7;
            na = __try0_o8;
            na_bool = __try0_o9;
            na_int = __try0_o10;
            na_string = __try0_o11;
            ndy = __try0_o12;
            next = __try0_o13;
            np = __try0_o14;
            np_bool = __try0_o15;
            np_int = __try0_o16;
            np_string = __try0_o17;
            numInVars = __try0_o18;
            numOptimizeConstraints = __try0_o19;
            numOptimizeFinalConstraints = __try0_o20;
            numOutVars = __try0_o21;
            numRealInputVars = __try0_o22;
            nx = __try0_o23;
            ny = __try0_o24;
            ny_bool = __try0_o25;
            ny_int = __try0_o26;
            ny_string = __try0_o27;
            unitDefinitions = __try0_o28;
            varInfo = __try0_o29;
            vars = __try0_o30;
            version = __try0_o31;
        }
        Err(__try0_err) => {
            Error::addInternalError((literal!("createModelInfo failed")).clone(), metamodelica::sourceInfo!())?;
            return Err(__try0_err);
        }
    }
    Ok(modelInfo)
}

fn createVarInfo(mut dlow: Arc<BackendDAE::BackendDAE>, mut nx: i32, mut ny: i32, mut ndy: i32, mut np: i32, mut na: i32, mut next: i32, mut numOutVars: i32, mut numInVars: i32, mut ny_int: i32, mut np_int: i32, mut na_int: i32, mut ny_bool: i32, mut np_bool: i32, mut na_bool: i32, mut ny_string: i32, mut np_string: i32, mut na_string: i32, mut numStateSets: i32, mut numOptimizeConstraints: i32, mut numOptimizeFinalConstraints: i32, mut numRealInputVars: i32) -> Result<SimCode::VarInfo> {
    let mut varInfo: SimCode::VarInfo = <SimCode::VarInfo as ::std::default::Default>::default();
    let mut numZeroCrossings: i32 = 0;
    let mut numTimeEvents: i32 = 0;
    let mut numRelations: i32 = 0;
    let mut numMathEventFunctions: i32 = 0;
    (numZeroCrossings, numTimeEvents, numRelations, numMathEventFunctions) = BackendDAEUtil::numberOfZeroCrossings(dlow.clone())?;
    varInfo = SimCode::VarInfo { numZeroCrossings: numZeroCrossings.clone(), numTimeEvents: numTimeEvents.clone(), numRelations: numRelations.clone(), numMathEventFunctions: numMathEventFunctions.clone(), numStateVars: nx.clone(), numAlgVars: ny.clone(), numDiscreteReal: ndy.clone(), numIntAlgVars: ny_int.clone(), numBoolAlgVars: ny_bool.clone(), numAlgAliasVars: na.clone(), numIntAliasVars: na_int.clone(), numBoolAliasVars: na_bool.clone(), numParams: np.clone(), numIntParams: np_int.clone(), numBoolParams: np_bool.clone(), numOutVars: numOutVars.clone(), numInVars: numInVars.clone(), numExternalObjects: next.clone(), numStringAlgVars: ny_string.clone(), numStringParamVars: np_string.clone(), numStringAliasVars: na_string.clone(), numEquations: 0, numLinearSystems: 0, numNonLinearSystems: 0, numMixedSystems: 0, numStateSets: numStateSets.clone(), numJacobians: 0, numOptimizeConstraints: numOptimizeConstraints.clone(), numOptimizeFinalConstraints: numOptimizeFinalConstraints.clone(), numSensitivityParameters: 0, numSetcVars: 0, numDataReconVars: 0, numRealInputVars: numRealInputVars.clone(), numSetbVars: 0, numRelatedBoundaryConditions: 0 };
    Ok(varInfo)
}

fn getNumberOfRealInputs(mut inputVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> i32 {
    let mut numRealInputs: i32 = 0;
    for mut var in &*inputVars.clone() {
        let mut var = var.clone();
        if isRealInput(var.clone()) {
            numRealInputs = numRealInputs.clone() + 1;
        }
    }
    numRealInputs
}

fn isRealInput(mut var: SimCodeVar::SimVar) -> bool {
    let mut isReal: bool = false;
    isReal = (::match_deref::match_deref! { match &(var.clone()) {
        SimCodeVar::SimVar { type_: Deref @ DAE::Type::T_REAL { .. }, .. } => true,
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    isReal
}

fn evaluateStartValues(mut inVar: BackendDAE::Var, mut inShared: Arc<BackendDAE::Shared>) -> Result<(BackendDAE::Var, Arc<BackendDAE::Shared>)> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut outShared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
    (outVar, outShared) = 'mc: {
        let __mc_input = (inVar.clone(), inShared.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (BackendDAE::Var { values: o2, bindExp: o1, .. }, Deref @ BackendDAE::Shared { graph, cache, .. }) => {
                    let mut startValue: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut startValue_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exps: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut attr: Arc<DAE::VariableAttributes> = Arc::new(<DAE::VariableAttributes as ::std::default::Default>::default());
                    let mut attr_: Arc<DAE::VariableAttributes> = Arc::new(<DAE::VariableAttributes as ::std::default::Default>::default());
                    let mut value: Arc<Values::Value> = Arc::new(Values::Value::META_FAIL);
                    let mut inVar: BackendDAE::Var = inVar.clone();
                    if isSome(o1.clone()) {
                        startValue = Util::getOption(o1.clone())?;
                        startValue_ = (::match_deref::match_deref! { match &(startValue.clone()) {
        Deref @ DAE::Exp::CALL { expLst: exps, .. } if (Expression::isConstWorkList(exps.clone())?) => {
                    let mut startValue1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (_, value) = Ceval::ceval(cache.clone(), graph.clone(), startValue.clone(), false, openmodelica_ast::Absyn::Msg::NO_MSG, 0)?;
                    startValue1 = ValuesUtil::valueExp(value.clone(), None)?;
                    startValue1.clone()
        },
        Deref @ DAE::Exp::ASUB { exp: Deref @ DAE::Exp::CALL { expLst: exps, .. }, sub: _ } if (Expression::isConstWorkList(exps.clone())?) => {
                    let mut startValue1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (_, value) = Ceval::ceval(cache.clone(), graph.clone(), startValue.clone(), false, openmodelica_ast::Absyn::Msg::NO_MSG, 0)?;
                    startValue1 = ValuesUtil::valueExp(value.clone(), None)?;
                    startValue1.clone()
        },
        _ => {
                    startValue.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
                        if !(referenceEq(&*(startValue.clone()),&*(startValue_.clone()))) {
                            inVar.bindExp = Some(startValue_.clone());
                        }
                    }
                    if isSome(o2.clone()) {
                        attr = Util::getOption(o2.clone())?;
                        attr_ = evaluateVariableAttributes(attr.clone(), inShared.clone())?;
                        if !(referenceEq(&*(attr.clone()),&*(attr_.clone()))) {
                            inVar.values = Some(attr_.clone());
                        }
                    }
                    Ok((inVar.clone(), inShared.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((inVar.clone(), inShared.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outVar, outShared))
}

fn evaluateVariableAttributes(mut attrIn: Arc<DAE::VariableAttributes>, mut shared: Arc<BackendDAE::Shared>) -> Result<Arc<DAE::VariableAttributes>> {
    let mut attrOut: Arc<DAE::VariableAttributes> = Arc::new(<DAE::VariableAttributes as ::std::default::Default>::default());
    attrOut = 'mc: {
        let __mc_input = (attrIn.clone(), shared.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::VariableAttributes::VAR_ATTR_REAL { start: Some(exp), .. }, Deref @ BackendDAE::Shared { graph, cache, .. }) => {
                    let mut exp_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exps: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut value: Arc<Values::Value> = Arc::new(Values::Value::META_FAIL);
                    let mut attrIn: Arc<DAE::VariableAttributes> = attrIn.clone();
                    exp_ = (::match_deref::match_deref! { match &(exp.clone()) {
        Deref @ DAE::Exp::CALL { expLst: exps, .. } if (Expression::isConstWorkList(exps.clone())?) => {
                    let mut exp1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (_, value) = Ceval::ceval(cache.clone(), graph.clone(), exp.clone(), false, openmodelica_ast::Absyn::Msg::NO_MSG, 0)?;
                    exp1 = ValuesUtil::valueExp(value.clone(), None)?;
                    exp1.clone()
        },
        Deref @ DAE::Exp::ASUB { exp: Deref @ DAE::Exp::CALL { expLst: exps, .. }, sub: _ } if (Expression::isConstWorkList(exps.clone())?) => {
                    let mut exp1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (_, value) = Ceval::ceval(cache.clone(), graph.clone(), exp.clone(), false, openmodelica_ast::Absyn::Msg::NO_MSG, 0)?;
                    exp1 = ValuesUtil::valueExp(value.clone(), None)?;
                    exp1.clone()
        },
        _ => {
                    exp.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
                    if !(referenceEq(&*(exp.clone()),&*(exp_.clone()))) {
                        assign_variant_field!(attrIn => DAE::VariableAttributes::VAR_ATTR_REAL; start = Some(exp.clone()));
                    }
                    Ok(attrIn.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(attrIn.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(attrOut)
}

fn preCalculateStartValues(mut systIn: Arc<BackendDAE::EqSystem>, mut globalKnownVars: BackendDAE::Variables, mut shared: Arc<BackendDAE::Shared>) -> Result<Arc<BackendDAE::EqSystem>> {
    let mut systOut: Arc<BackendDAE::EqSystem> = Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default());
    let mut varMap: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut varMapArr: metamodelica::Array<i32> = Default::default();
    let mut vars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut vars1: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut eqs: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
    let mut mStart: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut mTStart: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut syst: Arc<BackendDAE::EqSystem> = Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default());
    let mut varLst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut noStartVarLst: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut stateInfo: Arc<metamodelica::List<(i32, BackendDAE::VarKind)>> = metamodelica::nil();
    let mut stateIdcs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut stateKinds: Arc<metamodelica::List<BackendDAE::VarKind>> = metamodelica::nil();
    vars = systIn.orderedVars.clone();
    stateInfo = List::fold1(List::intRange(BackendVariable::varsSize(vars.clone())), (std::sync::Arc::new(getStateInfo) as std::sync::Arc<dyn ::std::ops::Fn(i32, BackendDAE::Variables, Arc<metamodelica::List<(i32, BackendDAE::VarKind)>>) -> Result<Arc<metamodelica::List<(i32, BackendDAE::VarKind)>>> + 'static>), vars.clone(), metamodelica::nil())?;
    (vars, _) = BackendVariable::traverseBackendDAEVarsWithUpdate(vars.clone(), (std::sync::Arc::new(setVarKindForStates) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, BackendDAE::VarKind) -> Result<(BackendDAE::Var, BackendDAE::VarKind)> + 'static>), openmodelica_backend_types::BackendDAE::VarKind::VARIABLE)?;
    varLst = BackendVariable::varList(vars.clone())?;
    varMap = List::intRange(BackendVariable::varsSize(vars.clone()));
    (noStartVarLst, varMap) = List::filterOnTrueSync(varLst.clone(), (std::sync::Arc::new(BackendVariable::varHasNoStartValue) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<bool> + 'static>), varMap.clone())?;
    eqs = BackendEquation::copyEquationArray(systIn.orderedEqs.clone());
    BackendDAEUtil::traverseBackendDAEExpsEqns(eqs.clone(), (std::sync::Arc::new(replaceCrefWithStartValue) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, BackendDAE::Variables) -> Result<(Arc<DAE::Exp>, BackendDAE::Variables)> + 'static>), globalKnownVars.clone())?;
    BackendDAEUtil::traverseBackendDAEExpsEqns(eqs.clone(), (std::sync::Arc::new(replaceCrefWithStartValue) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, BackendDAE::Variables) -> Result<(Arc<DAE::Exp>, BackendDAE::Variables)> + 'static>), vars.clone())?;
    vars1 = BackendVariable::listVar1(noStartVarLst.clone())?;
    syst = BackendDAEUtil::createEqSystem(vars1.clone(), eqs.clone(), metamodelica::nil(), openmodelica_backend_types::BackendDAE::BaseClockPartitionKind::UNKNOWN_PARTITION, BackendEquation::emptyEqns());
    (syst, mStart, mTStart) = BackendDAEUtil::getAdjacencyMatrix(syst.clone(), openmodelica_backend_types::BackendDAE::IndexType::NORMAL, None, BackendDAEUtil::isInitializationDAE(shared.clone()))?;
    varMapArr = metamodelica::arrayFromVec(varMap.clone().into_iter().cloned().collect());
    vars = preCalculateStartValues1(List::intRange(metamodelica::arrayLength(mStart.clone())), mStart.clone(), mTStart.clone(), varMapArr.clone(), eqs.clone(), vars.clone())?;
    stateIdcs = List::map(stateInfo.clone(), std::sync::Arc::new(fnptr!(Util::tuple21, _)))?;
    stateKinds = List::map(stateInfo.clone(), std::sync::Arc::new(fnptr!(Util::tuple22, _)))?;
    vars = List::threadFold(stateIdcs.clone(), stateKinds.clone(), (std::sync::Arc::new(BackendVariable::setVarKindForVar) as std::sync::Arc<dyn ::std::ops::Fn(i32, BackendDAE::VarKind, BackendDAE::Variables) -> Result<BackendDAE::Variables> + 'static>), vars.clone())?;
    (vars, _) = BackendVariable::traverseBackendDAEVarsWithUpdate(vars.clone(), (std::sync::Arc::new(evaluateStartValues) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, Arc<BackendDAE::Shared>) -> Result<(BackendDAE::Var, Arc<BackendDAE::Shared>)> + 'static>), shared.clone())?;
    systOut = BackendDAEUtil::setEqSystVars(systIn.clone(), vars.clone())?;
    Ok(systOut)
}

fn preCalculateStartValues1(mut eqIndexes: Arc<metamodelica::List<i32>>, mut m: metamodelica::Array<Arc<metamodelica::List<i32>>>, mut mT: metamodelica::Array<Arc<metamodelica::List<i32>>>, mut varMap: metamodelica::Array<i32>, mut eqs: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>>, mut varsIn: BackendDAE::Variables) -> Result<BackendDAE::Variables> {
    let mut varArr: BackendDAE::Variables = varsIn.clone();
    let mut eqIdx: i32 = 0;
    let mut varIdx: i32 = 0;
    let mut varIdx0: i32 = 0;
    let mut rest: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut newEqIdcs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut workList: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut mEntries: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    let mut eq: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
    let mut eqArr: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
    let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut eqLst: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut lhs: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut rhs: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    rest = eqIndexes.clone();
    while !(rest.clone().is_empty()) || !(workList.clone().is_empty()) {
        if rest.clone().is_empty() {
            rest = Dangerous::listReverseInPlace(workList.clone());
            workList = metamodelica::nil();
        }
        let (__pa0, __pa1) = ::match_deref::match_deref! { match &(rest.clone()) {
            Deref @ metamodelica::List::Cons { head: __pa0, tail: __pa1 } => (__pa0.clone(), __pa1.clone()),
            _ => bail!("pattern mismatch"),
        } };
        eqIdx = __pa0.clone();
        rest = __pa1.clone();
        if '__try2: {
            let __pa3 = ::match_deref::match_deref! { match &(m.clone().borrow()[(eqIdx.clone()-1) as usize].clone()) {
                Deref @ metamodelica::List::Cons { head: __pa3, tail: Deref @ metamodelica::List::Nil } => __pa3.clone(),
                _ => break '__try2 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            varIdx0 = __pa3.clone();
            varIdx = varMap.clone().borrow()[(intAbs(varIdx0.clone())-1) as usize].clone();
            var = unwrap_break_err!(BackendVariable::getVarAt(varArr.clone(), varIdx.clone()), '__try2);
            cref = unwrap_break_err!(BackendVariable::varCref(var.clone()), '__try2);
            eq = unwrap_break_err!(BackendEquation::get(eqs.clone(), eqIdx.clone()), '__try2);
            rhs = unwrap_break_err!(BackendEquation::getEquationRHS(eq.clone()), '__try2);
            lhs = unwrap_break_err!(BackendEquation::getEquationLHS(eq.clone()), '__try2);
            let __pa5 = ::match_deref::match_deref! { match &(unwrap_break_err!(ExpressionSolve::solve(lhs.clone(), rhs.clone(), unwrap_break_err!(Expression::crefExp(cref.clone()), '__try2), None), '__try2)) {
                (__pa5, Deref @ metamodelica::List::Nil) => __pa5.clone(),
                _ => break '__try2 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            rhs = __pa5.clone();
            let true = (Expression::isScalarConst(rhs.clone())) else { break '__try2 Err::<_, _>(anyhow::anyhow!("pattern mismatch")) };
            var = unwrap_break_err!(BackendVariable::setVarStartValue(var.clone(), rhs.clone()), '__try2);
            varArr = unwrap_break_err!(BackendVariable::setVarAt(varArr.clone(), varIdx.clone(), var.clone()), '__try2);
            newEqIdcs = mT.clone().borrow()[(varIdx0.clone()-1) as usize].clone();
            (newEqIdcs, _) = unwrap_break_err!(List::deleteMemberOnTrue(eqIdx.clone(), newEqIdcs.clone(), (std::sync::Arc::new(fnptr!(intEq, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>)), '__try2);
            workList = List::append_reverse(newEqIdcs.clone(), workList.clone());
            eqLst = unwrap_break_err!(List::map1r(newEqIdcs.clone(), (std::sync::Arc::new(BackendEquation::get) as std::sync::Arc<dyn ::std::ops::Fn(Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>>, i32) -> Result<Arc<BackendDAE::Equation>> + 'static>), eqs.clone()), '__try2);
            (eqLst, _) = unwrap_break_err!(BackendEquation::traverseExpsOfEquationList(eqLst.clone(), (std::sync::Arc::new(replaceCrefWithStartValue) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, BackendDAE::Variables) -> Result<(Arc<DAE::Exp>, BackendDAE::Variables)> + 'static>), varArr.clone()), '__try2);
            eqArr = unwrap_break_err!(List::threadFold(newEqIdcs.clone(), eqLst.clone(), (std::sync::Arc::new(BackendEquation::setAtIndexFirst) as std::sync::Arc<dyn ::std::ops::Fn(i32, Arc<BackendDAE::Equation>, Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>>) -> Result<Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>>> + 'static>), eqs.clone()), '__try2);
            mEntries = unwrap_break_err!(List::map1(newEqIdcs.clone(), (std::sync::Arc::new(Array::getIndexFirst) as std::sync::Arc<dyn ::std::ops::Fn(i32, _) -> Result<_> + 'static>), m.clone()), '__try2);
            mEntries = ({
        let mut __acc: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
        for mut e in (mEntries.clone()).into_iter().cloned() {
            let __x = (unwrap_break_err!(List::deleteMemberOnTrue(varIdx0.clone(), e.clone(), (std::sync::Arc::new(fnptr!(intEq, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>)), '__try2)).0;
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
            unwrap_break_err!(List::threadMap1_0(newEqIdcs.clone(), mEntries.clone(), (std::sync::Arc::new(Array::updateIndexFirst) as std::sync::Arc<dyn ::std::ops::Fn(i32, _, _) -> Result<()> + 'static>), m.clone()), '__try2);
            Ok::<(), anyhow::Error>(())
        }.is_err() {
        }
    }
    Ok(varArr)
}

fn artificialVarKind(mut inVarKind: BackendDAE::VarKind) -> bool {
    let mut isVar: bool = false;
    isVar = (match inVarKind.clone() {
        BackendDAE::VarKind::VARIABLE { .. } => false,
        BackendDAE::VarKind::PARAM { .. } => false,
        BackendDAE::VarKind::CONST { .. } => false,
        BackendDAE::VarKind::DISCRETE { .. } => false,
        BackendDAE::VarKind::STATE { .. } => false,
        BackendDAE::VarKind::ALG_STATE { .. } => false,
        BackendDAE::VarKind::DUMMY_STATE { .. } => false,
        _ => true,
    });
    isVar
}

fn replaceCrefWithStartValue(mut expIn: Arc<DAE::Exp>, mut varsIn: BackendDAE::Variables) -> Result<(Arc<DAE::Exp>, BackendDAE::Variables)> {
    let mut expOut: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut varsOut: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    (expOut, varsOut) = 'mc: {
        let __mc_input = expIn.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CREF { componentRef: Deref @ DAE::ComponentRef::CREF_IDENT { ident: Deref @ "time", .. }, .. } => {
                    Ok((Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(0.0_f64) }), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CREF { componentRef: cref, .. } => {
                    let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
                    let mut exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (var, _) = BackendVariable::getVarSingle(cref.clone(), varsIn.clone())?;
                    let true = (!(artificialVarKind(BackendVariable::varKind(var.clone())?))) else { bail!("pattern mismatch") };
                    if BackendVariable::varHasBindExp(var.clone()) {
                        exp = BackendVariable::varBindExp(var.clone())?;
                        if 0 != ExpressionBasics::compare(exp.clone(), expIn.clone())? {
                            (exp, _) = replaceCrefWithStartValue(exp.clone(), varsIn.clone())?;
                        }
                    } else if BackendVariable::varHasStartValue(var.clone())? {
                        exp = BackendVariable::varStartValue(var.clone())?;
                    } else {
                        exp = expIn.clone();
                    }
                    exp = if (Expression::isConst(exp.clone())?) {exp.clone()} else {expIn.clone()};
                    Ok((exp.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CALL { expLst, path: Deref @ Absyn::Path::IDENT { name: Deref @ "sample" }, .. } => {
                    let mut exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut startTime: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    startTime = (expLst.clone()).get(2)?;
                    (startTime, _) = replaceCrefWithStartValue(startTime.clone(), varsIn.clone())?;
                    if Expression::isZero(startTime.clone())? {
                        exp = Arc::new(DAE::Exp::BCONST { bool: true });
                    } else {
                        exp = expIn.clone();
                    }
                    Ok((exp.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::BINARY { exp2, operator: op, exp1 } => {
                    let mut exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp1_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp2_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (exp1_, _) = replaceCrefWithStartValue(exp1.clone(), varsIn.clone())?;
                    (exp2_, _) = replaceCrefWithStartValue(exp2.clone(), varsIn.clone())?;
                    if referenceEq(&*(exp1.clone()),&*(exp1_.clone())) && referenceEq(&*(exp2.clone()),&*(exp2_.clone())) {
                        exp = expIn.clone();
                    } else {
                        exp = Arc::new(DAE::Exp::BINARY { exp1: exp1_.clone(), operator: op.clone(), exp2: exp2_.clone() });
                    }
                    Ok((exp.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::LBINARY { exp2, operator: op, exp1 } => {
                    let mut exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp1_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp2_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (exp1_, _) = replaceCrefWithStartValue(exp1.clone(), varsIn.clone())?;
                    (exp2_, _) = replaceCrefWithStartValue(exp2.clone(), varsIn.clone())?;
                    if referenceEq(&*(exp1.clone()),&*(exp1_.clone())) && referenceEq(&*(exp2.clone()),&*(exp2_.clone())) {
                        exp = expIn.clone();
                    } else {
                        exp = Arc::new(DAE::Exp::LBINARY { exp1: exp1_.clone(), operator: op.clone(), exp2: exp2_.clone() });
                    }
                    Ok((exp.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::RELATION { exp2: Deref @ DAE::Exp::RCONST { real: r }, operator: DAE::Operator::GREATER { .. }, exp1: Deref @ DAE::Exp::CREF { componentRef: Deref @ DAE::ComponentRef::CREF_IDENT { ident: Deref @ "time", .. }, .. }, .. } => {
                    let true = (realLe(r.clone(), metamodelica::OrderedFloat(0.0_f64))) else { bail!("pattern mismatch") };
                    Ok((expIn.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::RELATION { exp2: Deref @ DAE::Exp::RCONST { real: r }, operator: DAE::Operator::GREATEREQ { .. }, exp1: Deref @ DAE::Exp::CREF { componentRef: Deref @ DAE::ComponentRef::CREF_IDENT { ident: Deref @ "time", .. }, .. }, .. } => {
                    let true = (realLe(r.clone(), metamodelica::OrderedFloat(0.0_f64))) else { bail!("pattern mismatch") };
                    Ok((expIn.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::RELATION { exp2: Deref @ DAE::Exp::CREF { componentRef: Deref @ DAE::ComponentRef::CREF_IDENT { ident: Deref @ "time", .. }, .. }, operator: DAE::Operator::LESS { .. }, exp1: Deref @ DAE::Exp::RCONST { real: r }, .. } => {
                    let true = (realLe(r.clone(), metamodelica::OrderedFloat(0.0_f64))) else { bail!("pattern mismatch") };
                    Ok((expIn.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::RELATION { exp2: Deref @ DAE::Exp::CREF { componentRef: Deref @ DAE::ComponentRef::CREF_IDENT { ident: Deref @ "time", .. }, .. }, operator: DAE::Operator::LESSEQ { .. }, exp1: Deref @ DAE::Exp::RCONST { real: r }, .. } => {
                    let true = (realLe(r.clone(), metamodelica::OrderedFloat(0.0_f64))) else { bail!("pattern mismatch") };
                    Ok((expIn.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::RELATION { optionExpisASUB, index: idx, exp2, operator: op, exp1 } => {
                    let mut exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp1_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp2_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (exp1_, _) = replaceCrefWithStartValue(exp1.clone(), varsIn.clone())?;
                    (exp2_, _) = replaceCrefWithStartValue(exp2.clone(), varsIn.clone())?;
                    if referenceEq(&*(exp1.clone()),&*(exp1_.clone())) && referenceEq(&*(exp2.clone()),&*(exp2_.clone())) {
                        exp = expIn.clone();
                    } else {
                        exp = Arc::new(DAE::Exp::RELATION { exp1: exp1_.clone(), operator: op.clone(), exp2: exp2_.clone(), index: idx.clone(), optionExpisASUB: optionExpisASUB.clone() });
                    }
                    Ok((exp.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::IFEXP { expElse: exp3, expThen: exp2, expCond: exp1 } => {
                    let mut exp: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp1_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp2_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut exp3_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    (exp1_, _) = replaceCrefWithStartValue(exp1.clone(), varsIn.clone())?;
                    (exp2_, _) = replaceCrefWithStartValue(exp2.clone(), varsIn.clone())?;
                    (exp3_, _) = replaceCrefWithStartValue(exp3.clone(), varsIn.clone())?;
                    if referenceEq(&*(exp1.clone()),&*(exp1_.clone())) && referenceEq(&*(exp2.clone()),&*(exp2_.clone())) && referenceEq(&*(exp3.clone()),&*(exp3_.clone())) {
                        exp = expIn.clone();
                    } else {
                        exp = Arc::new(DAE::Exp::IFEXP { expCond: exp1_.clone(), expThen: exp2_.clone(), expElse: exp3_.clone() });
                    }
                    Ok((exp.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((expIn.clone(), varsIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((expOut, varsOut))
}

fn setVarKindForStates(mut inVar: BackendDAE::Var, mut kindIn: BackendDAE::VarKind) -> Result<(BackendDAE::Var, BackendDAE::VarKind)> {
    let mut outVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut kindOut: BackendDAE::VarKind = BackendDAE::VarKind::ALG_STATE;
    (outVar, kindOut) = (match inVar.clone() {
        BackendDAE::Var { varKind: BackendDAE::VarKind::STATE { index: 1, .. }, .. } => {
            let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
            var = BackendVariable::setVarKind(inVar.clone(), kindIn.clone())?;
            (var.clone(), kindIn.clone())
        },
        _ => {
            (inVar.clone(), kindIn.clone())
        },
    });
    Ok((outVar, kindOut))
}

fn getStateInfo(mut idx: i32, mut vars: BackendDAE::Variables, mut stateInfoIn: Arc<metamodelica::List<(i32, BackendDAE::VarKind)>>) -> Result<Arc<metamodelica::List<(i32, BackendDAE::VarKind)>>> {
    let mut stateInfoOut: Arc<metamodelica::List<(i32, BackendDAE::VarKind)>> = metamodelica::nil();
    stateInfoOut = 'mc: {
        let __mc_input = stateInfoIn.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
                    let mut kind: BackendDAE::VarKind = BackendDAE::VarKind::ALG_STATE;
                    var = BackendVariable::getVarAt(vars.clone(), idx.clone())?;
                    let true = (BackendVariable::isStateVar(var.clone())) else { bail!("pattern mismatch") };
                    kind = BackendVariable::varKind(var.clone())?;
                    Ok(metamodelica::cons((idx.clone(), kind.clone()), stateInfoIn.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(stateInfoIn.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(stateInfoOut)
}

#[derive(Clone, Copy, Debug, Eq, PartialEq, Hash)]
#[repr(i32)]
pub enum SimVarsIndex {
    state = 1,
    derivative = 2,
    alg = 3,
    discreteAlg = 4,
    realOptimizeConstraints = 5,
    realOptimizeFinalConstraints = 6,
    param = 7,
    alias = 8,
    intAlg = 9,
    intParam = 10,
    intAlias = 11,
    boolAlg = 12,
    boolParam = 13,
    boolAlias = 14,
    stringAlg = 15,
    stringParam = 16,
    stringAlias = 17,
    extObj = 18,
    inputs = 19,
    outputs = 20,
    r#const = 21,
    intConst = 22,
    boolConst = 23,
    stringConst = 24,
    sensitivity = 25,
    setcvars = 26,
    datareconinputvars = 27,
    setBVars = 28,
    jacobian = 29,
    seed = 30,
}
impl PartialOrd for SimVarsIndex {
    fn partial_cmp(&self, other: &Self) -> Option<std::cmp::Ordering> { Some(self.cmp(other)) }
}
impl Ord for SimVarsIndex {
    fn cmp(&self, other: &Self) -> std::cmp::Ordering { (*self as i32).cmp(&(*other as i32)) }
}

// In special order for fmi: real => intger => boolean => string => external
fn createVars(mut inSimDAE: Arc<BackendDAE::BackendDAE>, mut inInitDAE: Arc<BackendDAE::BackendDAE>, mut tempvars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(SimCodeVar::SimVars, Arc<metamodelica::List<SimCode::UnitDefinition>>)> {
    let mut outVars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    let mut unitDefinitions: Arc<metamodelica::List<SimCode::UnitDefinition>> = metamodelica::nil();
    let mut globalKnownVars1: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut globalKnownVars2: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut localKnownVars1: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut localKnownVars2: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut extvars1: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut extvars2: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut aliasVars1: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut aliasVars2: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut systs1: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let mut systs2: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let mut shared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
    let mut hs: Mutable::Mutable<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr))>;
    let mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>> = arrayCreate(30i32, metamodelica::nil());
    let mut primeSize: i32 = 0;
    let mut iterationVarsLst: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>> = None;
    let mut timeInterval: Option<Arc<DAE::Exp>> = None;
    let debug: bool = false;
    let (__pa4, __pa0, __pa1, __pa2, __pa3, __pa5) = ::match_deref::match_deref! { match &(inSimDAE.clone()) {
        Deref @ BackendDAE::BackendDAE { shared: __pa4 @ Deref @ BackendDAE::Shared { aliasVars: __pa0, externalObjects: __pa1, localKnownVars: __pa2, globalKnownVars: __pa3, .. }, eqs: __pa5 } => (__pa4.clone(), __pa0.clone(), __pa1.clone(), __pa2.clone(), __pa3.clone(), __pa5.clone()),
        _ => bail!("pattern mismatch"),
    } };
    aliasVars1 = __pa0.clone();
    extvars1 = __pa1.clone();
    localKnownVars1 = __pa2.clone();
    globalKnownVars1 = __pa3.clone();
    shared = __pa4.clone();
    systs1 = __pa5.clone();
    let (__pa7, __pa8, __pa9, __pa10, __pa11) = ::match_deref::match_deref! { match &(inInitDAE.clone()) {
        Deref @ BackendDAE::BackendDAE { shared: Deref @ BackendDAE::Shared { aliasVars: __pa7, externalObjects: __pa8, localKnownVars: __pa9, globalKnownVars: __pa10, .. }, eqs: __pa11 } => (__pa7.clone(), __pa8.clone(), __pa9.clone(), __pa10.clone(), __pa11.clone()),
        _ => bail!("pattern mismatch"),
    } };
    aliasVars2 = __pa7.clone();
    extvars2 = __pa8.clone();
    localKnownVars2 = __pa9.clone();
    globalKnownVars2 = __pa10.clone();
    systs2 = __pa11.clone();
    (_, iterationVarsLst) = BackendDAEOptimize::listAllIterationVariables0(inInitDAE.eqs.clone())?;
    iterationVars = if (iterationVarsLst.clone().is_empty()) {None} else {Some(UnorderedSet::fromList(iterationVarsLst.clone(), (std::sync::Arc::new(ComponentReferenceBasics::hashComponentRef) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?)};
    primeSize = Util::nextPrime(((metamodelica::OrderedFloat(1.4_f64) * (metamodelica::OrderedFloat((BackendVariable::varsSize(globalKnownVars1.clone()) + BackendVariable::varsSize(globalKnownVars2.clone()) + BackendVariable::varsSize(localKnownVars1.clone()) + BackendVariable::varsSize(localKnownVars2.clone()) + BackendVariable::varsSize(aliasVars1.clone()) + BackendVariable::varsSize(aliasVars2.clone()) + BackendVariable::varsSize(extvars1.clone()) + BackendVariable::varsSize(extvars2.clone()) + BackendDAEUtil::daeSize(inSimDAE.clone())? + BackendDAEUtil::daeSize(inInitDAE.clone())?) as f64))).0.floor() as i32));
    hs = Mutable::create(HashSet::emptyHashSetSized(primeSize.clone()));
    if !(Flags::isSet(Flags::NO_START_CALC.clone())?) {
        systs1 = List::map2(systs1.clone(), (std::sync::Arc::new(preCalculateStartValues) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, BackendDAE::Variables, Arc<BackendDAE::Shared>) -> Result<Arc<BackendDAE::EqSystem>> + 'static>), globalKnownVars1.clone(), shared.clone())?;
        (globalKnownVars1, _) = BackendVariable::traverseBackendDAEVarsWithUpdate(globalKnownVars1.clone(), (std::sync::Arc::new(evaluateStartValues) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, Arc<BackendDAE::Shared>) -> Result<(BackendDAE::Var, Arc<BackendDAE::Shared>)> + 'static>), shared.clone())?;
        if debug.clone() {
            execStat((literal!("createVars: evaluateStartValues")).clone())?;
        }
    }
    simVars = List::fold1(({
        let mut __acc: Arc<metamodelica::List<BackendDAE::Variables>> = metamodelica::nil();
        for mut syst in (systs1.clone()).into_iter().cloned() {
            let __x = BackendVariable::daeVars(syst.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }), (std::sync::Arc::new(BackendVariable::traverseBackendDAEVars) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, _, _) -> Result<_> + 'static>), (std::sync::Arc::new({ let __pe_b2 = aliasVars1.clone(); let __pe_b3 = globalKnownVars1.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: variable list")).clone())?;
    }
    simVars = BackendVariable::traverseBackendDAEVars(globalKnownVars1.clone(), (std::sync::Arc::new({ let __pe_b2 = aliasVars1.clone(); let __pe_b3 = globalKnownVars1.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: known variable list")).clone())?;
    }
    simVars = BackendVariable::traverseBackendDAEVars(localKnownVars1.clone(), (std::sync::Arc::new({ let __pe_b2 = aliasVars1.clone(); let __pe_b3 = globalKnownVars1.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: local known variables list")).clone())?;
    }
    simVars = BackendVariable::traverseBackendDAEVars(aliasVars1.clone(), (std::sync::Arc::new({ let __pe_b2 = aliasVars1.clone(); let __pe_b3 = globalKnownVars1.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: removed variables list")).clone())?;
    }
    simVars = BackendVariable::traverseBackendDAEVars(extvars1.clone(), (std::sync::Arc::new({ let __pe_b2 = aliasVars1.clone(); let __pe_b3 = globalKnownVars1.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: external object list")).clone())?;
    }
    simVars = List::fold1(({
        let mut __acc: Arc<metamodelica::List<BackendDAE::Variables>> = metamodelica::nil();
        for mut syst in (systs2.clone()).into_iter().cloned() {
            let __x = BackendVariable::daeVars(syst.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }), (std::sync::Arc::new(BackendVariable::traverseBackendDAEVars) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Variables, _, _) -> Result<_> + 'static>), (std::sync::Arc::new({ let __pe_b2 = aliasVars2.clone(); let __pe_b3 = globalKnownVars2.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = None; let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: variable list (init)")).clone())?;
    }
    simVars = BackendVariable::traverseBackendDAEVars(globalKnownVars2.clone(), (std::sync::Arc::new({ let __pe_b2 = aliasVars2.clone(); let __pe_b3 = globalKnownVars2.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: known variable list (init)")).clone())?;
    }
    simVars = BackendVariable::traverseBackendDAEVars(localKnownVars2.clone(), (std::sync::Arc::new({ let __pe_b2 = aliasVars2.clone(); let __pe_b3 = globalKnownVars2.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: local known variables list (init)")).clone())?;
    }
    simVars = BackendVariable::traverseBackendDAEVars(aliasVars2.clone(), (std::sync::Arc::new({ let __pe_b2 = aliasVars2.clone(); let __pe_b3 = globalKnownVars2.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: removed variables list (init)")).clone())?;
    }
    simVars = BackendVariable::traverseBackendDAEVars(extvars2.clone(), (std::sync::Arc::new({ let __pe_b2 = aliasVars2.clone(); let __pe_b3 = globalKnownVars2.clone(); let __pe_b4 = hs.clone(); let __pe_b5 = shared.timeInterval.clone(); let __pe_b6 = iterationVars.clone(); move |__pe_a0, __pe_a1| extractVarsFromList(__pe_a0, __pe_a1, __pe_b2.clone(), __pe_b3.clone(), __pe_b4.clone(), __pe_b5.clone(), __pe_b6.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> + 'static>), simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: external object list (init)")).clone())?;
    }
    addTempVars(simVars.clone(), tempvars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: addTempVars")).clone())?;
    }
    sortSimvars(simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: sortSimVars")).clone())?;
    }
    if stringEqual((Config::simCodeTarget()?).clone(), (literal!("Cpp")).clone()) {
        extendIncompleteArray(simVars.clone())?;
        if debug.clone() {
            execStat((literal!("createVars: Cpp, extendIncompleteArray")).clone())?;
        }
    }
    fixIndex(simVars.clone())?;
    setVariableIndex(simVars.clone())?;
    unitDefinitions = getFmiUnitDefinitions(simVars.clone())?;
    if debug.clone() {
        execStat((literal!("createVars: fix and set index")).clone())?;
    }
    outVars = SimCodeVar::SimVars { stateVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::state.clone()) as i32)), derivativeVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::derivative.clone()) as i32)), algVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::alg.clone()) as i32)), discreteAlgVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::discreteAlg.clone()) as i32)), intAlgVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::intAlg.clone()) as i32)), boolAlgVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::boolAlg.clone()) as i32)), inputVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::inputs.clone()) as i32)), outputVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::outputs.clone()) as i32)), aliasVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::alias.clone()) as i32)), intAliasVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::intAlias.clone()) as i32)), boolAliasVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::boolAlias.clone()) as i32)), paramVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::param.clone()) as i32)), intParamVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::intParam.clone()) as i32)), boolParamVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::boolParam.clone()) as i32)), stringAlgVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::stringAlg.clone()) as i32)), stringParamVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::stringParam.clone()) as i32)), stringAliasVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::stringAlias.clone()) as i32)), extObjVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::extObj.clone()) as i32)), constVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::r#const.clone()) as i32)), intConstVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::intConst.clone()) as i32)), boolConstVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::boolConst.clone()) as i32)), stringConstVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::stringConst.clone()) as i32)), jacobianVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::jacobian.clone()) as i32)), seedVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::seed.clone()) as i32)), realOptimizeConstraintsVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::realOptimizeConstraints.clone()) as i32)), realOptimizeFinalConstraintsVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::realOptimizeFinalConstraints.clone()) as i32)), sensitivityVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::sensitivity.clone()) as i32)), dataReconSetcVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::setcvars.clone()) as i32)), dataReconinputVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::datareconinputvars.clone()) as i32)), dataReconSetBVars: metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((SimVarsIndex::setBVars.clone()) as i32)) };
    GCExt::free(simVars.clone());
    Ok((outVars, unitDefinitions))
}

fn extractVarsFromList(mut var: BackendDAE::Var, mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>, mut aliasVars: BackendDAE::Variables, mut vars: BackendDAE::Variables, mut hs: Mutable::Mutable<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr))>, mut timeInterval: Option<Arc<DAE::Exp>>, mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>) -> Result<(BackendDAE::Var, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>)> {
    let mut var: BackendDAE::Var = var;
    let mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>> = simVars;
    if if (ComponentReference::isPreCref(var.varName.clone()) || ComponentReference::isStartCref(var.varName.clone())) {false} else {!(BaseHashSet::has(var.varName.clone(), Mutable::access(hs.clone()))?)} {
        if !(BackendVariable::isAlgebraicOldState(var.clone())) {
            extractVarFromVar(var.clone(), aliasVars.clone(), vars.clone(), simVars.clone(), hs.clone(), timeInterval.clone(), iterationVars.clone())?;
        }
    }
    Ok((var, simVars))
}

pub fn getDefaultFmiInitialAttribute(mut variability: SimCodeVar::Variability, mut causality: SimCodeVar::Causality) -> SimCodeVar::Initial {
    let mut initial_: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
    initial_ = (match (variability.clone(), causality.clone()) {
        (SimCodeVar::Variability::CONSTANT { .. }, SimCodeVar::Causality::OUTPUT { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::EXACT,
        (SimCodeVar::Variability::CONSTANT { .. }, SimCodeVar::Causality::LOCAL { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::EXACT,
        (SimCodeVar::Variability::FIXED { .. }, SimCodeVar::Causality::PARAMETER { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::EXACT,
        (SimCodeVar::Variability::FIXED { .. }, SimCodeVar::Causality::CALCULATED_PARAMETER { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED,
        (SimCodeVar::Variability::FIXED { .. }, SimCodeVar::Causality::LOCAL { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED,
        (SimCodeVar::Variability::TUNABLE { .. }, SimCodeVar::Causality::PARAMETER { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::EXACT,
        (SimCodeVar::Variability::TUNABLE { .. }, SimCodeVar::Causality::CALCULATED_PARAMETER { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED,
        (SimCodeVar::Variability::TUNABLE { .. }, SimCodeVar::Causality::LOCAL { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED,
        (SimCodeVar::Variability::DISCRETE { .. }, SimCodeVar::Causality::OUTPUT { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED,
        (SimCodeVar::Variability::DISCRETE { .. }, SimCodeVar::Causality::LOCAL { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED,
        (SimCodeVar::Variability::CONTINUOUS { .. }, SimCodeVar::Causality::OUTPUT { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED,
        (SimCodeVar::Variability::CONTINUOUS { .. }, SimCodeVar::Causality::LOCAL { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED,
        _ => openmodelica_simcode_types::SimCodeVar::Initial::NONE_INITIAL,
    });
    initial_
}

pub fn getFmiInitialAttributeStr(mut simVar: SimCodeVar::SimVar) -> Result<ArcStr> {
    let mut out_string: ArcStr = literal!("");
    let mut var_initial: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
    let mut default_initial: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
    if isNone(simVar.initial_.clone()) {
        return Ok(out_string.clone());
    }
    let __pa0 = ::match_deref::match_deref! { match &(simVar.initial_.clone()) {
        Some(__pa0) => __pa0.clone(),
        _ => bail!("pattern mismatch"),
    } };
    var_initial = __pa0.clone();
    default_initial = getDefaultFmiInitialAttribute(Util::getOptionOrDefault(simVar.variability.clone(), openmodelica_simcode_types::SimCodeVar::Variability::CONTINUOUS), Util::getOptionOrDefault(simVar.causality.clone(), openmodelica_simcode_types::SimCodeVar::Causality::LOCAL));
    if var_initial.clone() == default_initial.clone() && !(Flags::isSet(Flags::DUMP_FORCE_FMI_ATTRIBUTES.clone())?) {
        var_initial = openmodelica_simcode_types::SimCodeVar::Initial::NONE_INITIAL;
    }
    out_string = ((match var_initial.clone() {
        SimCodeVar::Initial::EXACT => literal!("exact"),
        SimCodeVar::Initial::APPROX => literal!("approx"),
        SimCodeVar::Initial::CALCULATED => literal!("calculated"),
        SimCodeVar::Initial::NONE_INITIAL => literal!(""),
    })).clone();
    Ok(out_string)
}

pub fn simGenericCallString(mut call: SimCode::SimGenericCall) -> Result<ArcStr> {
    let mut r#str: ArcStr = arcstr::literal!("");
    r#str = ((match call.clone() {
        SimCode::SimGenericCall::SINGLE_GENERIC_CALL { .. } => {
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("single generic call ")); __mm_s.push_str(&*intString(var_field!(call.index, SimCode::SimGenericCall::SINGLE_GENERIC_CALL).clone())); __mm_s.push_str(&*literal!(" ")); __mm_s.push_str(&*List::toString(var_field!(call.iters, SimCode::SimGenericCall::SINGLE_GENERIC_CALL).clone(), (std::sync::Arc::new(BackendDump::simIteratorString) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::SimIterator) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("{")).clone(), (literal!(", ")).clone(), (literal!("}")).clone(), true, 0)?); ArcStr::from(__mm_s) }).clone();
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("\n  ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(call.lhs, SimCode::SimGenericCall::SINGLE_GENERIC_CALL).clone())?); __mm_s.push_str(&*literal!(" = ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(call.rhs, SimCode::SimGenericCall::SINGLE_GENERIC_CALL).clone())?); __mm_s.push_str(&*literal!(";")); ArcStr::from(__mm_s) }).clone();
            r#str.clone()
        },
        SimCode::SimGenericCall::IF_GENERIC_CALL { .. } => {
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("if generic call ")); __mm_s.push_str(&*intString(var_field!(call.index, SimCode::SimGenericCall::IF_GENERIC_CALL).clone())); __mm_s.push_str(&*literal!(" ")); __mm_s.push_str(&*List::toString(var_field!(call.iters, SimCode::SimGenericCall::IF_GENERIC_CALL).clone(), (std::sync::Arc::new(BackendDump::simIteratorString) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::SimIterator) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("{")).clone(), (literal!(", ")).clone(), (literal!("}")).clone(), true, 0)?); ArcStr::from(__mm_s) }).clone();
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*List::toString(var_field!(call.branches, SimCode::SimGenericCall::IF_GENERIC_CALL).clone(), (std::sync::Arc::new(simBranchString) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::SimBranch) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("")).clone(), (literal!("\n")).clone(), (literal!("")).clone(), true, 0)?); ArcStr::from(__mm_s) }).clone();
            r#str.clone()
        },
        SimCode::SimGenericCall::WHEN_GENERIC_CALL { .. } => {
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("when generic call ")); __mm_s.push_str(&*intString(var_field!(call.index, SimCode::SimGenericCall::WHEN_GENERIC_CALL).clone())); __mm_s.push_str(&*literal!(" ")); __mm_s.push_str(&*List::toString(var_field!(call.iters, SimCode::SimGenericCall::WHEN_GENERIC_CALL).clone(), (std::sync::Arc::new(BackendDump::simIteratorString) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::SimIterator) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("{")).clone(), (literal!(", ")).clone(), (literal!("}")).clone(), true, 0)?); ArcStr::from(__mm_s) }).clone();
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*List::toString(var_field!(call.branches, SimCode::SimGenericCall::WHEN_GENERIC_CALL).clone(), (std::sync::Arc::new(simBranchString) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::SimBranch) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("")).clone(), (literal!("\n")).clone(), (literal!("")).clone(), true, 0)?); ArcStr::from(__mm_s) }).clone();
            r#str.clone()
        },
        _ => literal!(""),
    })).clone();
    Ok(r#str)
}

pub fn simBranchString(mut branch: SimCode::SimBranch) -> Result<ArcStr> {
    fn simBranchBodyString(mut tpl: (Arc<DAE::Exp>, Arc<DAE::Exp>)) -> Result<ArcStr> {
        let mut r#str: ArcStr = { let mut __mm_s = String::new(); __mm_s.push_str(&*ExpressionBasics::printExpStr(Util::tuple21(tpl.clone()))?); __mm_s.push_str(&*literal!(" = ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(Util::tuple22(tpl.clone()))?); __mm_s.push_str(&*literal!(";")); ArcStr::from(__mm_s) };
        Ok(r#str)
    }

    let mut r#str: ArcStr = arcstr::literal!("");
    r#str = ((match branch.clone() {
        SimCode::SimBranch::SIM_BRANCH { .. } => {
            let mut b: bool = false;
            b = isSome(var_field!(branch.condition, SimCode::SimBranch::SIM_BRANCH).clone());
            r#str = (if (b.clone()) {{ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("if ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(Util::getOption(var_field!(branch.condition, SimCode::SimBranch::SIM_BRANCH).clone())?)?); __mm_s.push_str(&*literal!(" then\n")); ArcStr::from(__mm_s) }} else {literal!("else\n")}).clone();
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*List::toString(var_field!(branch.body, SimCode::SimBranch::SIM_BRANCH).clone(), (std::sync::Arc::new(simBranchBodyString) as std::sync::Arc<dyn ::std::ops::Fn((Arc<DAE::Exp>, Arc<DAE::Exp>)) -> Result<ArcStr> + 'static>), (literal!("  ")).clone(), (literal!("  ")).clone(), (literal!("\n")).clone(), (literal!("")).clone(), true, 0)?); ArcStr::from(__mm_s) }).clone();
            r#str = (if (b.clone()) {{ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("end if;")); ArcStr::from(__mm_s) }} else {r#str.clone()}).clone();
            r#str.clone()
        },
        SimCode::SimBranch::SIM_BRANCH_STMT { .. } => {
            let mut b: bool = false;
            b = isSome(var_field!(branch.condition, SimCode::SimBranch::SIM_BRANCH_STMT).clone());
            r#str = (if (b.clone()) {{ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("if ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(Util::getOption(var_field!(branch.condition, SimCode::SimBranch::SIM_BRANCH_STMT).clone())?)?); __mm_s.push_str(&*literal!(" then\n")); ArcStr::from(__mm_s) }} else {literal!("else\n")}).clone();
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*List::toString(var_field!(branch.body, SimCode::SimBranch::SIM_BRANCH_STMT).clone(), (std::sync::Arc::new(DAEDump::ppStatementStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Statement>) -> Result<ArcStr> + 'static>), (literal!("  ")).clone(), (literal!("  ")).clone(), (literal!("\n")).clone(), (literal!("")).clone(), true, 0)?); ArcStr::from(__mm_s) }).clone();
            r#str = (if (b.clone()) {{ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("\nend if;")); ArcStr::from(__mm_s) }} else {r#str.clone()}).clone();
            r#str.clone()
        },
        _ => {
            literal!("")
        },
    })).clone();
    Ok(r#str)
}

// one dlow var can result in multiple simvars: input and output are a subset
// of algvars for example
fn extractVarFromVar(mut dlowVar: BackendDAE::Var, mut inAliasVars: BackendDAE::Variables, mut inVars: BackendDAE::Variables, mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>, mut hs: Mutable::Mutable<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr))>, mut timeInterval: Option<Arc<DAE::Exp>>, mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>) -> Result<()> {
    let mut scalar_crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut scalarVar: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut binding: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut scalar_bindings: Arc<metamodelica::List<Option<Arc<DAE::Exp>>>> = metamodelica::nil();
    let mut binding_opt: Option<Arc<DAE::Exp>> = None;
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    if Types::isArray(dlowVar.varType.clone()) {
        scalar_crefs = ComponentReference::expandCref(dlowVar.varName.clone(), false)?;
        match '__try0: {
            scalar_bindings = (::match_deref::match_deref! { match &(dlowVar.bindExp.clone()) {
        Some(binding @ Deref @ DAE::Exp::ARRAY { .. }) => ({
        let mut __acc: Arc<metamodelica::List<Option<Arc<DAE::Exp>>>> = metamodelica::nil();
        for mut b in (unwrap_break_err!(Expression::expandArray(binding.clone()), '__try0)).into_iter().cloned() {
            let __x = Some(b.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }),
        Some(binding @ Deref @ DAE::Exp::MATRIX { .. }) => ({
        let mut __acc: Arc<metamodelica::List<Option<Arc<DAE::Exp>>>> = metamodelica::nil();
        for mut b in (unwrap_break_err!(Expression::expandArray(binding.clone()), '__try0)).into_iter().cloned() {
            let __x = Some(b.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }),
        _ => List::fill(dlowVar.bindExp.clone(), (scalar_crefs.clone().len() as i32)),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
            if (scalar_bindings.clone().len() as i32) != (scalar_crefs.clone().len() as i32) {
                break '__try0 Err::<_, _>(anyhow::anyhow!("fail"));
            }
            Ok::<_, anyhow::Error>((scalar_bindings.clone(),))
        } {
            Ok((__try0_o0,)) => {
                scalar_bindings = __try0_o0;
            }
            Err(_) => {
                scalar_bindings = List::fill(dlowVar.bindExp.clone(), (scalar_crefs.clone().len() as i32));
            }
        }
        if Config::simCodeTarget()? != literal!("Cpp") {
            Mutable::update(hs.clone(), BaseHashSet::add(dlowVar.varName.clone(), Mutable::access(hs.clone()))?);
            for mut tpl in &*List::zip(scalar_crefs.clone(), scalar_bindings.clone()) {
                let mut tpl = tpl.clone();
                (cref, binding_opt) = tpl.clone();
                scalarVar = BackendVariable::copyVarNewName(cref.clone(), dlowVar.clone());
                scalarVar.bindExp = binding_opt.clone();
                scalarVar.varType = ComponentReference::crefTypeFull(cref.clone())?;
                extractVarFromVar2(scalarVar.clone(), inAliasVars.clone(), inVars.clone(), simVars.clone(), hs.clone(), timeInterval.clone(), iterationVars.clone())?;
            }
        } else {
            extractVarFromVar2(dlowVar.clone(), inAliasVars.clone(), inVars.clone(), simVars.clone(), hs.clone(), timeInterval.clone(), iterationVars.clone())?;
            for mut cref in &*scalar_crefs.clone() {
                let mut cref = cref.clone();
                Mutable::update(hs.clone(), BaseHashSet::add(cref.clone(), Mutable::access(hs.clone()))?);
            }
        }
    } else {
        extractVarFromVar2(dlowVar.clone(), inAliasVars.clone(), inVars.clone(), simVars.clone(), hs.clone(), timeInterval.clone(), iterationVars.clone())?;
    }
    Ok(())
}

fn getExportVar(mut var: BackendDAE::Var) -> Result<Option<Arc<DAE::ComponentRef>>> {
    let mut exportVar: Option<Arc<DAE::ComponentRef>> = None;
    if Flags::getConfigEnum(Flags::FMI_FILTER.clone())? == Flags::FMI_NONE.clone() {
        exportVar = Some(var.varName.clone());
    } else if Flags::getConfigEnum(Flags::FMI_FILTER.clone())? == Flags::FMI_INTERNAL.clone() {
        if !(ComponentReference::isInternalCref(var.varName.clone()) && (!(BackendVariable::isStateVar(var.clone())) && !(BackendVariable::isClockedStateVar(var.clone())))) {
            exportVar = Some(var.varName.clone());
        }
    } else if Flags::getConfigEnum(Flags::FMI_FILTER.clone())? == Flags::FMI_PROTECTED.clone() {
        if !(ComponentReference::isInternalCref(var.varName.clone()) && (!(BackendVariable::isStateVar(var.clone())) && !(BackendVariable::isClockedStateVar(var.clone())))) {
            if !(BackendVariable::isProtected(var.clone()) && (!(BackendVariable::isStateVar(var.clone())) && !(BackendVariable::isClockedStateVar(var.clone())) && !(ComponentReference::isPreviousCref(var.varName.clone())))) {
                exportVar = Some(var.varName.clone());
            }
        }
    } else if Flags::getConfigEnum(Flags::FMI_FILTER.clone())? == Flags::FMI_BLACKBOX.clone() {
        if BackendVariable::isInput(var.clone()) || BackendVariable::isOutputVar(var.clone()) {
            exportVar = Some(var.varName.clone());
        } else if BackendVariable::isStateVar(var.clone()) || BackendVariable::isClockedStateVar(var.clone()) {
            exportVar = Some(ComponentReference::getConcealedCref());
        }
    } else {
        Error::addInternalError((literal!("Unknown value detected for --fmiFilter")).clone(), metamodelica::sourceInfo!())?;
        bail!("fail");
    }
    Ok(exportVar)
}

// one dlow var can result in multiple simvars: input and output are a subset
// of algvars for example
fn extractVarFromVar2(mut dlowVar: BackendDAE::Var, mut inAliasVars: BackendDAE::Variables, mut inVars: BackendDAE::Variables, mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>, mut hs: Mutable::Mutable<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr))>, mut timeInterval: Option<Arc<DAE::Exp>>, mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>) -> Result<()> {
    let mut simVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut derivSimvar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut isalias: bool = false;
    let mut isAlg: bool = false;
    let mut isParam: bool = false;
    let mut isConst: bool = false;
    let mut name: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    simVar = dlowvarToSimvar(dlowVar.clone(), Some(inAliasVars.clone()), inVars.clone(), iterationVars.clone())?;
    isalias = isAliasVar(simVar.clone());
    simVar.exportVar = getExportVar(dlowVar.clone())?;
    if BackendVariable::isStringParam(dlowVar.clone()) && isSome(simVar.initialValue.clone()) && !(Expression::isEvaluatedConst(Util::getOption(simVar.initialValue.clone())?)) {
        simVar.exportVar = None;
    }
    Mutable::update(hs.clone(), BaseHashSet::add(simVar.name.clone(), Mutable::access(hs.clone()))?);
    if !(isalias.clone()) && (BackendVariable::isStateVar(dlowVar.clone()) || BackendVariable::isAlgState(dlowVar.clone())) {
        derivSimvar = derVarFromStateVar(simVar.clone(), timeInterval.clone(), iterationVars.clone())?;
        Mutable::update(hs.clone(), BaseHashSet::add(derivSimvar.name.clone(), Mutable::access(hs.clone()))?);
    } else {
        derivSimvar = simVar.clone();
    }
    if !(isalias.clone()) && BackendVariable::isVarOnTopLevelAndInputNoDerInput(dlowVar.clone()) {
        simVar = (match simVar.clone() {
        SimCodeVar::SimVar { .. } => {
            simVar.inputIndex = Some(arrayCreate(1, -2));
            simVar.clone()
        },
        _ => {
            Error::addInternalError((literal!("Failed to SimCodeUtil.extractVarFromVar of input variable")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
    });
    }
    isAlg = BackendVariable::isVarAlg(dlowVar.clone());
    isParam = BackendVariable::isParam(dlowVar.clone());
    isConst = BackendVariable::isConst(dlowVar.clone());
    if BackendVariable::isVarOnTopLevelAndInputNoDerInput(dlowVar.clone()) {
        addSimVar(simVar.clone(), SimVarsIndex::inputs.clone(), simVars.clone());
    }
    if BackendVariable::isVarOnTopLevelAndOutput(dlowVar.clone()) {
        addSimVar(simVar.clone(), SimVarsIndex::outputs.clone(), simVars.clone());
    }
    if isalias.clone() {
        if Types::isReal(dlowVar.varType.clone()) {
            addSimVar(simVar.clone(), SimVarsIndex::alias.clone(), simVars.clone());
        } else if Types::isInteger(dlowVar.varType.clone()) || Types::isEnumeration(dlowVar.varType.clone()) {
            addSimVar(simVar.clone(), SimVarsIndex::intAlias.clone(), simVars.clone());
        } else if Types::isBoolean(dlowVar.varType.clone()) {
            addSimVar(simVar.clone(), SimVarsIndex::boolAlias.clone(), simVars.clone());
        } else if Types::isString(dlowVar.varType.clone()) {
            addSimVar(simVar.clone(), SimVarsIndex::stringAlias.clone(), simVars.clone());
        }
    } else if BackendVariable::isStateVar(dlowVar.clone()) || BackendVariable::isAlgState(dlowVar.clone()) {
        addSimVar(simVar.clone(), SimVarsIndex::state.clone(), simVars.clone());
        addSimVar(derivSimvar.clone(), SimVarsIndex::derivative.clone(), simVars.clone());
    } else if isAlg.clone() || isParam.clone() || isConst.clone() {
        if Types::isReal(dlowVar.varType.clone()) {
            if isAlg.clone() {
                if BackendVariable::isVarDiscrete(dlowVar.clone()) {
                    addSimVar(simVar.clone(), SimVarsIndex::discreteAlg.clone(), simVars.clone());
                } else {
                    addSimVar(simVar.clone(), SimVarsIndex::alg.clone(), simVars.clone());
                }
            } else if isParam.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::param.clone(), simVars.clone());
            } else if isConst.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::r#const.clone(), simVars.clone());
            }
        } else if Types::isInteger(dlowVar.varType.clone()) || Types::isEnumeration(dlowVar.varType.clone()) {
            if isAlg.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::intAlg.clone(), simVars.clone());
            } else if isParam.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::intParam.clone(), simVars.clone());
            } else if isConst.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::intConst.clone(), simVars.clone());
            }
        } else if Types::isBoolean(dlowVar.varType.clone()) {
            if isAlg.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::boolAlg.clone(), simVars.clone());
            } else if isParam.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::boolParam.clone(), simVars.clone());
            } else if isConst.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::boolConst.clone(), simVars.clone());
            }
        } else if Types::isString(dlowVar.varType.clone()) {
            if isAlg.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::stringAlg.clone(), simVars.clone());
            } else if isParam.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::stringParam.clone(), simVars.clone());
            } else if isConst.clone() {
                addSimVar(simVar.clone(), SimVarsIndex::stringConst.clone(), simVars.clone());
            }
        } else {
            Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Failed to find SimVar list for Var: ")); __mm_s.push_str(&*BackendDump::varString(dlowVar.clone())?); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
        }
    } else if BackendVariable::isExtObj(dlowVar.clone()) {
        addSimVar(simVar.clone(), SimVarsIndex::extObj.clone(), simVars.clone());
    } else if BackendVariable::isRealOptimizeConstraintsVars(dlowVar.clone()) {
        addSimVar(simVar.clone(), SimVarsIndex::realOptimizeConstraints.clone(), simVars.clone());
    } else if BackendVariable::isRealOptimizeFinalConstraintsVars(dlowVar.clone()) {
        addSimVar(simVar.clone(), SimVarsIndex::realOptimizeFinalConstraints.clone(), simVars.clone());
    } else if BackendVariable::isOptInputVar(dlowVar.clone()) {
        addSimVar(simVar.clone(), SimVarsIndex::alg.clone(), simVars.clone());
    } else if BackendVariable::isDAEmodeVar(dlowVar.clone()) {
    } else {
        Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Failed to find the correct SimVar list for Var: ")); __mm_s.push_str(&*BackendDump::varString(dlowVar.clone())?); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
    }
    Ok(())
}

fn addSimVar(mut simVar: SimCodeVar::SimVar, mut index: SimVarsIndex, mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> () {
    metamodelica::Dangerous::arrayUpdateNoBoundsChecking(simVars.clone(), ((index.clone()) as i32), metamodelica::cons(simVar.clone(), metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((index.clone()) as i32))));
    ()
}

fn derVarFromStateVar(mut state: SimCodeVar::SimVar, mut timeInterval: Option<Arc<DAE::Exp>>, mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>) -> Result<SimCodeVar::SimVar> {
    let mut deriv: SimCodeVar::SimVar = state.clone();
    let mut unit: Unit::Unit = <Unit::Unit as ::std::default::Default>::default();
    let mut nominal: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    deriv.arrayCref = Util::applyOption(deriv.arrayCref.clone(), (std::sync::Arc::new(fnptr!(ComponentReference::crefPrefixDer, Arc<DAE::ComponentRef>)) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
    deriv.name = ComponentReference::crefPrefixDer(deriv.name.clone());
    deriv.varKind = openmodelica_backend_types::BackendDAE::VarKind::STATE_DER;
    if deriv.comment.clone() != literal!("") {
        deriv.comment = { let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("der(")); __mm_s.push_str(&*deriv.comment.clone()); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) };
    }
    match '__try0: {
        unit = unwrap_break_err!(Unit::parseUnitString((deriv.unit.clone()).clone(), unwrap_break_err!(Unit::getKnownUnits(), '__try0), Absyn::dummyInfo.clone()), '__try0);
        unit = unwrap_break_err!(Unit::unitDiv(unit.clone(), Unit::SECOND().clone()), '__try0);
        deriv.unit = unwrap_break_err!(Unit::unitString(unit.clone(), unwrap_break_err!(Unit::getKnownUnitsInverse(), '__try0)), '__try0);
        Ok::<_, anyhow::Error>((deriv.clone(),))
    } {
        Ok((__try0_o0,)) => {
            deriv = __try0_o0;
        }
        Err(_) => {
            deriv.unit = literal!("");
        }
    }
    deriv.displayUnit = literal!("");
    deriv.minValue = None;
    deriv.maxValue = None;
    if isSome(iterationVars.clone()) && UnorderedSet::contains(deriv.name.clone(), Util::getOption(iterationVars.clone())?)? {
        if isSome(timeInterval.clone()) {
            nominal = Expression::makeDiv(Util::getOptionOrDefault(deriv.nominalValue.clone(), Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) })), Util::getOption(timeInterval.clone())?)?;
            deriv.nominalValue = Some((ExpressionSimplify::simplify(nominal.clone())?).0);
        }
    } else {
        deriv.nominalValue = None;
    }
    deriv.isFixed = false;
    deriv.aliasvar = openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS;
    deriv.causality = Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL);
    deriv.variable_index = None;
    deriv.isValueChangeable = false;
    deriv.variability = Some(openmodelica_simcode_types::SimCodeVar::Variability::CONTINUOUS);
    if isSome(iterationVars.clone()) && UnorderedSet::contains(deriv.name.clone(), Util::getOption(iterationVars.clone())?)? {
        deriv.initial_ = Some(openmodelica_simcode_types::SimCodeVar::Initial::APPROX);
        deriv.initialValue = Some(Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(0.0_f64) }));
    } else {
        deriv.initial_ = Some(openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED);
        deriv.initialValue = None;
    }
    if Flags::getConfigEnum(Flags::FMI_FILTER.clone())? == Flags::FMI_BLACKBOX.clone() {
        deriv.exportVar = Some(ComponentReference::getConcealedCref());
    } else {
        deriv.exportVar = Some(deriv.name.clone());
    }
    Ok(deriv)
}

pub fn simVarString(mut inVar: SimCodeVar::SimVar) -> Result<ArcStr> {
    let mut s: ArcStr = arcstr::literal!("");
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("index:")); __mm_s.push_str(&*intString(inVar.index.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(inVar.name.clone())?); ArcStr::from(__mm_s) }).clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*(match inVar.aliasvar.clone() {
        SimCodeVar::AliasVariable::NOALIAS { .. } => {
            literal!(" (no alias) ")
        },
        SimCodeVar::AliasVariable::ALIAS { varName: ref cr } => {
            { let mut __mm_s = String::new(); __mm_s.push_str(&*literal!(" (alias: ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cr.clone())?); __mm_s.push_str(&*literal!(") ")); ArcStr::from(__mm_s) }
        },
        SimCodeVar::AliasVariable::NEGATEDALIAS { varName: ref cr } => {
            { let mut __mm_s = String::new(); __mm_s.push_str(&*literal!(" (negated alias: ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cr.clone())?); __mm_s.push_str(&*literal!(") ")); ArcStr::from(__mm_s) }
        },
    })); ArcStr::from(__mm_s) }).clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*if (inVar.isProtected.clone()) {literal!(" protected ")} else {literal!("")}); ArcStr::from(__mm_s) }).clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*if (Util::getOptionOrDefault(inVar.hideResult.clone(), false)) {literal!(" hideResult ")} else {literal!("")}); ArcStr::from(__mm_s) }).clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!(" initial: ")); __mm_s.push_str(&*if (isSome(inVar.initialValue.clone())) {ExpressionDump::printOptExpStr(inVar.initialValue.clone())?} else {literal!("")}); ArcStr::from(__mm_s) }).clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*if (isSome(inVar.arrayCref.clone())) {{ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\tarrCref:")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(Util::getOption(inVar.arrayCref.clone())?)?); ArcStr::from(__mm_s) }} else {literal!("\tno arrCref")}); ArcStr::from(__mm_s) }).clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!(" index:(")); __mm_s.push_str(&*if (isSome(inVar.variable_index.clone())) {intString(Util::getOption(inVar.variable_index.clone())?)} else {literal!("")}); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) }).clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!(" [")); __mm_s.push_str(&*stringDelimitList(inVar.numArrayElement.clone(), (literal!(",")).clone())); __mm_s.push_str(&*literal!("]")); ArcStr::from(__mm_s) }).clone();
    Ok(s)
}

pub fn dumpVarLst(mut varLst: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut header: ArcStr) -> Result<()> {
    let mut var: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    if !(varLst.clone().is_empty()) {
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*header.clone()); __mm_s.push_str(&*literal!("\n----------------------\n")); ArcStr::from(__mm_s) }).clone());
        for mut var in &*varLst.clone() {
            let mut var = var.clone();
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*simVarString(var.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        }
    }
    Ok(())
}

pub fn printVarLstCrefs(mut inVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<ArcStr> {
    let mut r#str: ArcStr = arcstr::literal!("");
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut var: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    r#str = (literal!("\t\tcrefs: ")).clone();
    for mut var in &*inVars.clone() {
        let mut var = var.clone();
        let SimCodeVar::SIMVAR { name: __pa0, .. } = (var.clone()) else { bail!("pattern mismatch") };
        cref = __pa0.clone();
        r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*ComponentReference::debugPrintComponentRefTypeStr(cref.clone())?); __mm_s.push_str(&*literal!(" , ")); ArcStr::from(__mm_s) }).clone();
    }
    Ok(r#str)
}

fn dumpVariablesString(mut vars: Arc<metamodelica::List<Arc<SimCodeFunction::Variable::Variable>>>, mut delimiter: ArcStr) -> Result<()> {
    let () = (::match_deref::match_deref! { match &(vars.clone()) {
        Deref @ metamodelica::List::Nil => {
            ()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Variable::VARIABLE { kind, ty, name: cref, .. }, tail: rest } => {
            let mut s1: ArcStr = arcstr::literal!("");
            let mut s2: ArcStr = arcstr::literal!("");
            (s1, _) = DAEDump::printTypeStr(ty.clone())?;
            s1 = (TypesDump::printTypeStr(ty.clone())?).clone();
            s2 = (DAEDump::dumpKindStr(kind.clone())?).clone();
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cref.clone())?); __mm_s.push_str(&*literal!(" (")); __mm_s.push_str(&*s1.clone()); __mm_s.push_str(&*literal!(", ")); __mm_s.push_str(&*s2.clone()); __mm_s.push_str(&*literal!(") ")); __mm_s.push_str(&*delimiter.clone()); ArcStr::from(__mm_s) }).clone());
            dumpVariablesString(rest.clone(), (delimiter.clone()).clone())?;
            ()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Variable::FUNCTION_PTR { name: s1, .. }, tail: rest } => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("<func> ")); __mm_s.push_str(&*s1.clone()); __mm_s.push_str(&*delimiter.clone()); ArcStr::from(__mm_s) }).clone());
            dumpVariablesString(rest.clone(), (delimiter.clone()).clone())?;
            ()
        },
        _ => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("<unknown>")); __mm_s.push_str(&*delimiter.clone()); ArcStr::from(__mm_s) }).clone());
            ()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(())
}

pub fn dumpModelInfo(mut modelInfo: SimCode::ModelInfo) -> Result<()> {
    let mut nsv: i32 = 0;
    let mut nalgv: i32 = 0;
    let mut varInfo: SimCode::VarInfo = <SimCode::VarInfo as ::std::default::Default>::default();
    let mut simVars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    let mut stateVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut derivativeVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut algVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut discreteAlgVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut intAlgVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut boolAlgVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut aliasVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut intAliasVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut paramVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut boolParamVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut intParamVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut extObjVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut constVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut intConstVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut stringConstVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut functions: Arc<metamodelica::List<Arc<SimCodeFunction::Function::Function>>> = metamodelica::nil();
    let SimCode::MODELINFO { functions: __pa0, varInfo: __pa1, vars: __pa2, .. } = (modelInfo.clone()) else { bail!("pattern mismatch") };
    functions = __pa0.clone();
    varInfo = __pa1.clone();
    simVars = __pa2.clone();
    let SimCodeVar::SIMVARS { stringConstVars: __pa3, intConstVars: __pa4, constVars: __pa5, extObjVars: __pa6, boolParamVars: __pa7, intParamVars: __pa8, paramVars: __pa9, intAliasVars: __pa10, aliasVars: __pa11, discreteAlgVars: __pa12, intAlgVars: __pa13, boolAlgVars: __pa14, algVars: __pa15, derivativeVars: __pa16, stateVars: __pa17, .. } = (simVars.clone()) else { bail!("pattern mismatch") };
    stringConstVars = __pa3.clone();
    intConstVars = __pa4.clone();
    constVars = __pa5.clone();
    extObjVars = __pa6.clone();
    boolParamVars = __pa7.clone();
    intParamVars = __pa8.clone();
    paramVars = __pa9.clone();
    intAliasVars = __pa10.clone();
    aliasVars = __pa11.clone();
    discreteAlgVars = __pa12.clone();
    intAlgVars = __pa13.clone();
    boolAlgVars = __pa14.clone();
    algVars = __pa15.clone();
    derivativeVars = __pa16.clone();
    stateVars = __pa17.clone();
    let SimCode::VARINFO { numAlgVars: __pa18, numStateVars: __pa19, .. } = (varInfo.clone()) else { bail!("pattern mismatch") };
    nalgv = __pa18.clone();
    nsv = __pa19.clone();
    dumpVarLst(stateVars.clone(), ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("stateVars (")); __mm_s.push_str(&*intString(nsv.clone())); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) }).clone())?;
    dumpVarLst(derivativeVars.clone(), (literal!("derivativeVars")).clone())?;
    dumpVarLst(algVars.clone(), ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("algVars (")); __mm_s.push_str(&*intString(nalgv.clone())); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) }).clone())?;
    dumpVarLst(discreteAlgVars.clone(), (literal!("discreteAlgVars")).clone())?;
    dumpVarLst(intAlgVars.clone(), (literal!("intAlgVars")).clone())?;
    dumpVarLst(boolAlgVars.clone(), (literal!("boolAlgVars")).clone())?;
    dumpVarLst(aliasVars.clone(), (literal!("aliasVars")).clone())?;
    dumpVarLst(intAliasVars.clone(), (literal!("intAliasVars")).clone())?;
    dumpVarLst(paramVars.clone(), (literal!("paramVars")).clone())?;
    dumpVarLst(intParamVars.clone(), (literal!("intParamVars")).clone())?;
    dumpVarLst(boolParamVars.clone(), (literal!("boolParamVars")).clone())?;
    dumpVarLst(extObjVars.clone(), (literal!("extObjVars")).clone())?;
    dumpVarLst(constVars.clone(), (literal!("constVars")).clone())?;
    dumpVarLst(intConstVars.clone(), (literal!("intConstVars")).clone())?;
    dumpVarLst(stringConstVars.clone(), (literal!("stringConstVars")).clone())?;
    println!("{}", (literal!("functions:\n-----------\n\n")).clone());
    dumpFunctions(functions.clone())?;
    Ok(())
}

fn dumpFunctions(mut functions: Arc<metamodelica::List<Arc<SimCodeFunction::Function::Function>>>) -> Result<()> {
    let () = (::match_deref::match_deref! { match &(functions.clone()) {
        Deref @ metamodelica::List::Nil => {
            ()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::FUNCTION { variableDeclarations, functionArguments, outVars, name: path, .. }, tail: rest } => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Function: ")); __mm_s.push_str(&*AbsynUtil::pathStringNoQual(path.clone(), (literal!(".")).clone(), true, false)?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            println!("{}", (literal!("\toutVars: ")).clone());
            dumpVariablesString(outVars.clone(), (literal!(" , ")).clone())?;
            println!("{}", (literal!("\n\tfunctionArguments: ")).clone());
            dumpVariablesString(functionArguments.clone(), (literal!(" , ")).clone())?;
            println!("{}", (literal!("\n\tvariableDeclarations: ")).clone());
            dumpVariablesString(variableDeclarations.clone(), (literal!(" , ")).clone())?;
            println!("{}", (literal!("\n")).clone());
            dumpFunctions(rest.clone())?;
            ()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::PARALLEL_FUNCTION { name: path, .. }, tail: rest } => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Parallel Function: ")); __mm_s.push_str(&*AbsynUtil::pathStringNoQual(path.clone(), (literal!(".")).clone(), true, false)?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            dumpFunctions(rest.clone())?;
            ()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::KERNEL_FUNCTION { name: path, .. }, tail: rest } => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Kernel Function: ")); __mm_s.push_str(&*AbsynUtil::pathStringNoQual(path.clone(), (literal!(".")).clone(), true, false)?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            dumpFunctions(rest.clone())?;
            ()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::EXTERNAL_FUNCTION { outVars, name: path, .. }, tail: rest } => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("External Function: ")); __mm_s.push_str(&*AbsynUtil::pathStringNoQual(path.clone(), (literal!(".")).clone(), true, false)?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            println!("{}", (literal!("\toutVars: ")).clone());
            dumpVariablesString(outVars.clone(), (literal!(" , ")).clone())?;
            println!("{}", (literal!("\n")).clone());
            dumpFunctions(rest.clone())?;
            ()
        },
        Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::RECORD_CONSTRUCTOR { locals, funArgs, name: path, .. }, tail: rest } => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Record: ")); __mm_s.push_str(&*AbsynUtil::pathStringNoQual(path.clone(), (literal!(".")).clone(), true, false)?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            println!("{}", (literal!("\tfunArgs: ")).clone());
            dumpVariablesString(funArgs.clone(), (literal!(" , ")).clone())?;
            println!("{}", (literal!("\n\tlocals: ")).clone());
            dumpVariablesString(locals.clone(), (literal!(" , ")).clone())?;
            println!("{}", (literal!("\n")).clone());
            dumpFunctions(rest.clone())?;
            ()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(())
}

pub fn dumpSimEqSystemLst(mut eqSysLstIn: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut delimiter: ArcStr) -> Result<()> {
    let mut sys: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    for mut sys in &*eqSysLstIn.clone() {
        let mut sys = sys.clone();
        dumpSimEqSystem(sys.clone())?;
        println!("{}", (delimiter.clone()).clone());
    }
    Ok(())
}

pub fn simEqSystemString(mut eqSysIn: Arc<SimCode::SimEqSystem>) -> Result<ArcStr> {
    let mut r#str: ArcStr = arcstr::literal!("");
    r#str = ('mc: {
        let __mc_input = eqSysIn.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_RESIDUAL { exp, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(exp.clone())?); __mm_s.push_str(&*literal!(" (RESIDUAL)")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_FOR_RESIDUAL { exp, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(exp.clone())?); __mm_s.push_str(&*literal!(" (FOR_RESIDUAL)")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { exp, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(exp.clone())?); __mm_s.push_str(&*literal!(" (GENERIC_RESIDUAL)")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { exp, cref, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cref.clone())?); __mm_s.push_str(&*literal!("=")); __mm_s.push_str(&*ExpressionBasics::printExpStr(exp.clone())?); __mm_s.push_str(&*literal!(" [")); __mm_s.push_str(&*DAEDump::daeTypeStr(Expression::r#typeof(exp.clone())?)?); __mm_s.push_str(&*literal!("]")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { cons, exp, cref, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cref.clone())?); __mm_s.push_str(&*literal!("=")); __mm_s.push_str(&*ExpressionBasics::printExpStr(exp.clone())?); __mm_s.push_str(&*literal!(" [constraints: ")); __mm_s.push_str(&*ExpressionDump::constraintDTlistToString(cons.clone(), (literal!("")).clone())?); __mm_s.push_str(&*literal!("]")); __mm_s.push_str(&*literal!(" [")); __mm_s.push_str(&*DAEDump::daeTypeStr(Expression::r#typeof(exp.clone())?)?); __mm_s.push_str(&*literal!("]")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { exp, lhs, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(lhs.clone())?); __mm_s.push_str(&*literal!("=")); __mm_s.push_str(&*ExpressionBasics::printExpStr(exp.clone())?); __mm_s.push_str(&*literal!(" [")); __mm_s.push_str(&*DAEDump::daeTypeStr(Expression::r#typeof(exp.clone())?)?); __mm_s.push_str(&*literal!("]")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_IFEQUATION { index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (IF)")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_ALGORITHM { statements: stmts, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    let mut sLst: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
                    sLst = List::map(stmts.clone(), (std::sync::Arc::new(DAEDump::ppStatementStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Statement>) -> Result<ArcStr> + 'static>))?;
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*List::foldr(sLst.clone(), (std::sync::Arc::new(fnptr!(stringAppend, ArcStr, ArcStr)) as std::sync::Arc<dyn ::std::ops::Fn(ArcStr, ArcStr) -> Result<ArcStr> + 'static>), (literal!("")).clone())?); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_INVERSE_ALGORITHM { statements: stmts, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    let mut sLst: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
                    sLst = List::map(stmts.clone(), (std::sync::Arc::new(DAEDump::ppStatementStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Statement>) -> Result<ArcStr> + 'static>))?;
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*List::foldr(sLst.clone(), (std::sync::Arc::new(fnptr!(stringAppend, ArcStr, ArcStr)) as std::sync::Arc<dyn ::std::ops::Fn(ArcStr, ArcStr) -> Result<ArcStr> + 'static>), (literal!("")).clone())?); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { jacobianMatrix: jac, residual, beqs, vars, indexLinearSystem: idxLS, index: idx, .. }, alternativeTearing: None, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (LINEAR) index:")); __mm_s.push_str(&*intString(idxLS.clone())); __mm_s.push_str(&*literal!(" jacobian: ")); __mm_s.push_str(&*boolString(isSome(jac.clone()))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\tvariables:\n")); __mm_s.push_str(&*stringDelimitList(List::map(vars.clone(), (std::sync::Arc::new(simVarString) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))?, (literal!("\n")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\n\tb-vector:\n")); __mm_s.push_str(&*stringDelimitList(List::map(beqs.clone(), (std::sync::Arc::new(ExpressionBasics::printExpStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>))?, (literal!("\n")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\t")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*stringDelimitList(List::map(residual.clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>))?, (literal!("\n\t")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { jacobianMatrix: jac, residual, beqs, vars, indexLinearSystem: idxLS, index: idx, .. }, alternativeTearing: Some(Deref @ SimCode::LinearSystem { .. }), .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("strict set:\n")); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (LINEAR) index:")); __mm_s.push_str(&*intString(idxLS.clone())); __mm_s.push_str(&*literal!(" jacobian: ")); __mm_s.push_str(&*boolString(isSome(jac.clone()))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\tvariables:\n\t")); __mm_s.push_str(&*stringDelimitList(List::map(vars.clone(), (std::sync::Arc::new(simVarString) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))?, (literal!("\t\n")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\n\tb-vector:\n")); __mm_s.push_str(&*stringDelimitList(List::map(beqs.clone(), (std::sync::Arc::new(ExpressionBasics::printExpStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>))?, (literal!("\t\n")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\t")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*stringDelimitList(List::map(residual.clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>))?, (literal!("\n\t")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { crefs, eqs, jacobianMatrix: jac, indexNonLinearSystem: idxNLS, index: idx, .. }, alternativeTearing: None, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (NONLINEAR) index:")); __mm_s.push_str(&*intString(idxNLS.clone())); __mm_s.push_str(&*literal!(" jacobian: ")); __mm_s.push_str(&*boolString(isSome(jac.clone()))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("crefs: ")); __mm_s.push_str(&*stringDelimitList(List::map(crefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::printComponentRefStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))?, (literal!(" , ")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\t")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*stringDelimitList(List::map(eqs.clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>))?, (literal!("\n\t")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { crefs, eqs, jacobianMatrix: jac, indexNonLinearSystem: idxNLS, index: idx, .. }, alternativeTearing: Some(Deref @ SimCode::NonlinearSystem { .. }), .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("strict set:\n")); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (NONLINEAR) index:")); __mm_s.push_str(&*intString(idxNLS.clone())); __mm_s.push_str(&*literal!(" jacobian: ")); __mm_s.push_str(&*boolString(isSome(jac.clone()))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("crefs: ")); __mm_s.push_str(&*stringDelimitList(List::map(crefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::printComponentRefStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))?, (literal!(" , ")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\t")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*stringDelimitList(List::map(eqs.clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>))?, (literal!("\n\t")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_MIXED { discEqs: eqs, cont, indexMixedSystem: idxMS, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (MIXED) index:")); __mm_s.push_str(&*intString(idxMS.clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*simEqSystemString(cont.clone())?); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*stringDelimitList(List::map(eqs.clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>))?, (literal!("\n\t")).clone())); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_WHEN { elseWhen, whenStmtLst, conditions: crefs, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" WHEN:( ")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*stringDelimitList(List::map(crefs.clone(), (std::sync::Arc::new(ComponentReference::crefStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))?, (literal!(", ")).clone())); __mm_s.push_str(&*literal!(" ) then: ")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*dumpWhenOps(whenStmtLst.clone())?); ArcStr::from(__mm_s) }).clone();
                    if isSome(elseWhen.clone()) {
                        s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!(" ELSEWHEN: ")); ArcStr::from(__mm_s) }).clone();
                        s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*simEqSystemString(Util::getOption(elseWhen.clone())?)?); ArcStr::from(__mm_s) }).clone();
                    }
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_FOR_LOOP { exp, cref, endIt, startIt, iter: iterator, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(" FOR-LOOP: ")); __mm_s.push_str(&*literal!(" for ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(iterator.clone())?); __mm_s.push_str(&*literal!(" in (")); __mm_s.push_str(&*ExpressionBasics::printExpStr(startIt.clone())?); __mm_s.push_str(&*literal!(":")); __mm_s.push_str(&*ExpressionBasics::printExpStr(endIt.clone())?); __mm_s.push_str(&*literal!(") loop\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(cref.clone())?); __mm_s.push_str(&*literal!("=")); __mm_s.push_str(&*ExpressionBasics::printExpStr(exp.clone())?); __mm_s.push_str(&*literal!("[")); __mm_s.push_str(&*DAEDump::daeTypeStr(Expression::r#typeof(exp.clone())?)?); __mm_s.push_str(&*literal!("]\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("end for;")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_FOR_EQUATION { body: eqs, endIt, startIt, iter: iterator, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(" FOR-EQUATION: ")); __mm_s.push_str(&*literal!(" for ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(iterator.clone())?); __mm_s.push_str(&*literal!(" in (")); __mm_s.push_str(&*ExpressionBasics::printExpStr(startIt.clone())?); __mm_s.push_str(&*literal!(":")); __mm_s.push_str(&*ExpressionBasics::printExpStr(endIt.clone())?); __mm_s.push_str(&*literal!(") loop\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*stringDelimitList(List::map(eqs.clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>))?, (literal!("\n\t")).clone())); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("end for;")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_ALIAS { .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*ArcStr::from(::std::format!("{}", var_field!((*eqSysIn).index, SimCode::SimEqSystem::SES_ALIAS).clone()))); __mm_s.push_str(&*literal!(": alias of ")); __mm_s.push_str(&*ArcStr::from(::std::format!("{}", var_field!((*eqSysIn).aliasOf, SimCode::SimEqSystem::SES_ALIAS).clone()))); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_ALGEBRAIC_SYSTEM { linearSystem: lin, tornSystem: torn, partOfMixed: partMixed, algSysIndex: idx2, index: idx, .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (ALGEBRAIC_SYSTEM) algSysIndex: ")); __mm_s.push_str(&*intString(idx2.clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\tpartOfMixed system: ")); __mm_s.push_str(&*boolString(partMixed.clone())); __mm_s.push_str(&*literal!(", tornSystem: ")); __mm_s.push_str(&*boolString(torn.clone())); __mm_s.push_str(&*literal!(", linearSystem: ")); __mm_s.push_str(&*boolString(lin.clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*omsiFuncEqnString(var_field!((*eqSysIn).residual, SimCode::SimEqSystem::SES_ALGEBRAIC_SYSTEM).clone())?); ArcStr::from(__mm_s) }).clone();
                    let () = (::match_deref::match_deref! { match &(var_field!((*eqSysIn).matrix, SimCode::SimEqSystem::SES_ALGEBRAIC_SYSTEM).clone()) {
        Some(matrix @ Deref @ SimCode::DerivativeMatrix { columns: _, .. }) => {
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*derivativeMatrixString(matrix.clone())?); ArcStr::from(__mm_s) }).clone();
                    ()
        },
        _ => bail!("match: no arm matched"),
    } });
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_RESIZABLE_ASSIGN { .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(var_field!((*eqSysIn).index, SimCode::SimEqSystem::SES_RESIZABLE_ASSIGN).clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (SES_RESIZABLE_ASSIGN) ")); __mm_s.push_str(&*literal!(" call index: ")); __mm_s.push_str(&*intString(var_field!((*eqSysIn).call_index, SimCode::SimEqSystem::SES_RESIZABLE_ASSIGN).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_GENERIC_ASSIGN { .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(var_field!((*eqSysIn).index, SimCode::SimEqSystem::SES_GENERIC_ASSIGN).clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (SES_GENERIC_ASSIGN) ")); __mm_s.push_str(&*literal!(" call index: ")); __mm_s.push_str(&*intString(var_field!((*eqSysIn).call_index, SimCode::SimEqSystem::SES_GENERIC_ASSIGN).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\tindices: ")); __mm_s.push_str(&*List::toString(var_field!((*eqSysIn).scal_indices, SimCode::SimEqSystem::SES_GENERIC_ASSIGN).clone(), (std::sync::Arc::new(fnptr!(intString, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("{")).clone(), (literal!(", ")).clone(), (literal!("}")).clone(), true, 10)?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_ENTWINED_ASSIGN { .. } => {
                    let mut s: ArcStr = arcstr::literal!("");
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(var_field!((*eqSysIn).index, SimCode::SimEqSystem::SES_ENTWINED_ASSIGN).clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (SES_ENTWINED_ASSIGN)\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\tcall order: ")); __mm_s.push_str(&*List::toString(var_field!((*eqSysIn).call_order, SimCode::SimEqSystem::SES_ENTWINED_ASSIGN).clone(), (std::sync::Arc::new(fnptr!(intString, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("{")).clone(), (literal!(", ")).clone(), (literal!("}")).clone(), true, 10)?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*List::toString(var_field!((*eqSysIn).single_calls, SimCode::SimEqSystem::SES_ENTWINED_ASSIGN).clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("\t")).clone(), (literal!("\n")).clone(), (literal!("")).clone(), true, 0)?); ArcStr::from(__mm_s) }).clone();
                    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                    Ok(s.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(literal!("SOMETHING DIFFERENT\n"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    }).clone();
    Ok(r#str)
}

pub fn dumpSimEqSystem(mut eqSysIn: Arc<SimCode::SimEqSystem>) -> Result<()> {
    let () = 'mc: {
        let __mc_input = eqSysIn.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { simJac, jacobianMatrix: jac, .. }, alternativeTearing: None, .. } => {
                    println!("{}", (simEqSystemString(eqSysIn.clone())?).clone());
                    dumpJacobianMatrix(jac.clone())?;
                    println!("{}", (literal!("\tsimJac:\n")).clone());
                    dumpSimJac(simJac.clone())?;
                    Ok(())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { simJac, jacobianMatrix: jac, .. }, alternativeTearing: Some(Deref @ SimCode::LinearSystem { simJac: simJac2, jacobianMatrix: jac2, residual: residual2, indexLinearSystem: idxLS2, index: idx2, .. }), .. } => {
                    println!("{}", (simEqSystemString(eqSysIn.clone())?).clone());
                    println!("{}", (literal!("\n\tsimJac:\n")).clone());
                    dumpSimJac(simJac.clone())?;
                    dumpJacobianMatrix(jac.clone())?;
                    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\ncasual set:\n")); __mm_s.push_str(&*intString(idx2.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (LINEAR) index:")); __mm_s.push_str(&*intString(idxLS2.clone())); __mm_s.push_str(&*literal!(" jacobian: ")); __mm_s.push_str(&*boolString(isSome(jac.clone()))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    println!("{}", (literal!("\t")).clone());
                    dumpSimEqSystemLst(residual2.clone(), (literal!("\n\t")).clone())?;
                    println!("{}", (literal!("\n\tsimJac:\n")).clone());
                    dumpSimJac(simJac2.clone())?;
                    dumpJacobianMatrix(jac2.clone())?;
                    Ok(())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { jacobianMatrix: jac, .. }, alternativeTearing: None, .. } => {
                    println!("{}", (simEqSystemString(eqSysIn.clone())?).clone());
                    dumpJacobianMatrix(jac.clone())?;
                    Ok(())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { jacobianMatrix: jac, .. }, alternativeTearing: Some(Deref @ SimCode::NonlinearSystem { crefs: crefs2, eqs: eqs2, jacobianMatrix: jac2, indexNonLinearSystem: idxNLS2, index: idx2, .. }), .. } => {
                    println!("{}", (simEqSystemString(eqSysIn.clone())?).clone());
                    dumpJacobianMatrix(jac.clone())?;
                    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\ncasual set:\n")); __mm_s.push_str(&*intString(idx2.clone())); __mm_s.push_str(&*literal!(": ")); __mm_s.push_str(&*literal!(" (NONLINEAR) index:")); __mm_s.push_str(&*intString(idxNLS2.clone())); __mm_s.push_str(&*literal!(" jacobian: ")); __mm_s.push_str(&*boolString(isSome(jac2.clone()))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\t\tcrefs: ")); __mm_s.push_str(&*stringDelimitList(List::map(crefs2.clone(), (std::sync::Arc::new(ComponentReferenceBasics::printComponentRefStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))?, (literal!(" , ")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    println!("{}", (literal!("\t")).clone());
                    dumpSimEqSystemLst(eqs2.clone(), (literal!("\n\t")).clone())?;
                    println!("{}", (literal!("\n")).clone());
                    dumpJacobianMatrix(jac2.clone())?;
                    Ok(())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    println!("{}", (simEqSystemString(eqSysIn.clone())?).clone());
                    Ok(())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(())
}

fn dumpSimJac(mut simJac: Arc<metamodelica::List<(i32, i32, Arc<SimCode::SimEqSystem>)>>) -> Result<()> {
    let mut i1: i32 = 0;
    let mut i2: i32 = 0;
    let mut res: ArcStr = arcstr::literal!("");
    let mut simEqSys: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut tpl: (i32, i32, Arc<SimCode::SimEqSystem>) = (0, 0, Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default()));
    if simJac.clone().is_empty() {
        res = (literal!("no simJac")).clone();
    } else {
        res = (literal!("")).clone();
    }
    for mut tpl in &*simJac.clone() {
        let mut tpl = tpl.clone();
        (i1, i2, simEqSys) = tpl.clone();
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*res.clone()); __mm_s.push_str(&*literal!("[")); __mm_s.push_str(&*intString(i1.clone())); __mm_s.push_str(&*literal!(", ")); __mm_s.push_str(&*intString(i2.clone())); __mm_s.push_str(&*literal!("] ->")); ArcStr::from(__mm_s) }).clone());
        dumpSimEqSystem(simEqSys.clone())?;
    }
    println!("{}", (literal!("\n")).clone());
    Ok(())
}

fn dumpWhenOps(mut whenStmtLst: Arc<metamodelica::List<BackendDAE::WhenOperator>>) -> Result<ArcStr> {
    let mut res: ArcStr = arcstr::literal!("");
    let mut e: BackendDAE::WhenOperator = <BackendDAE::WhenOperator as ::std::default::Default>::default();
    res = (literal!("")).clone();
    for mut whenOps in &*whenStmtLst.clone() {
        let mut whenOps = whenOps.clone();
        res = ((match whenOps.clone() {
        mut e @ BackendDAE::WhenOperator::ASSIGN { .. } => { let mut __mm_s = String::new(); __mm_s.push_str(&*res.clone()); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(e.left, BackendDAE::WhenOperator::ASSIGN).clone())?); __mm_s.push_str(&*literal!(" = ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(e.right, BackendDAE::WhenOperator::ASSIGN).clone())?); __mm_s.push_str(&*literal!("[")); __mm_s.push_str(&*DAEDump::daeTypeStr(Expression::r#typeof(var_field!(e.right, BackendDAE::WhenOperator::ASSIGN).clone())?)?); __mm_s.push_str(&*literal!("]")); ArcStr::from(__mm_s) },
        mut e @ BackendDAE::WhenOperator::REINIT { .. } => { let mut __mm_s = String::new(); __mm_s.push_str(&*res.clone()); __mm_s.push_str(&*literal!("reinit(")); __mm_s.push_str(&*ComponentReference::debugPrintComponentRefTypeStr(var_field!(e.stateVar, BackendDAE::WhenOperator::REINIT).clone())?); __mm_s.push_str(&*literal!(", ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(e.value, BackendDAE::WhenOperator::REINIT).clone())?); __mm_s.push_str(&*literal!(") [")); __mm_s.push_str(&*DAEDump::daeTypeStr(Expression::r#typeof(var_field!(e.value, BackendDAE::WhenOperator::REINIT).clone())?)?); __mm_s.push_str(&*literal!("]")); ArcStr::from(__mm_s) },
        mut e @ BackendDAE::WhenOperator::ASSERT { .. } => { let mut __mm_s = String::new(); __mm_s.push_str(&*res.clone()); __mm_s.push_str(&*literal!("assert(")); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(e.condition, BackendDAE::WhenOperator::ASSERT).clone())?); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(e.message, BackendDAE::WhenOperator::ASSERT).clone())?); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(e.level, BackendDAE::WhenOperator::ASSERT).clone())?); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) },
        mut e @ BackendDAE::WhenOperator::TERMINATE { .. } => { let mut __mm_s = String::new(); __mm_s.push_str(&*res.clone()); __mm_s.push_str(&*literal!("terminate(")); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(e.message, BackendDAE::WhenOperator::TERMINATE).clone())?); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) },
        mut e @ BackendDAE::WhenOperator::NORETCALL { .. } => { let mut __mm_s = String::new(); __mm_s.push_str(&*res.clone()); __mm_s.push_str(&*ExpressionBasics::printExpStr(var_field!(e.exp, BackendDAE::WhenOperator::NORETCALL).clone())?); __mm_s.push_str(&*literal!(" [")); __mm_s.push_str(&*DAEDump::daeTypeStr(Expression::r#typeof(var_field!(e.exp, BackendDAE::WhenOperator::NORETCALL).clone())?)?); __mm_s.push_str(&*literal!("]")); ArcStr::from(__mm_s) },
    })).clone();
    }
    Ok(res)
}

fn dumpJacobianMatrixLst(mut simjacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>) -> Result<()> {
    for mut jac in &*simjacs.clone() {
        let mut jac = jac.clone();
        println!("{}", (literal!("\nsymbolic directional derivative:\n")).clone());
        dumpJacobianMatrix(Some(jac.clone()))?;
        println!("{}", (literal!("\n")).clone());
    }
    Ok(())
}

fn dumpJacobianMatrix(mut jacOpt: Option<Arc<SimCode::JacobianMatrix>>) -> Result<()> {
    let () = (::match_deref::match_deref! { match &(jacOpt.clone()) {
        Some(jac) => {
            let mut idx: i32 = 0;
            let mut cols: Arc<metamodelica::List<Arc<SimCode::JacobianColumn>>> = metamodelica::nil();
            let mut colEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut colVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let (__pa0, __pa1) = ::match_deref::match_deref! { match &(jac.clone()) {
                Deref @ SimCode::JacobianMatrix { jacobianIndex: __pa0, columns: __pa1, .. } => (__pa0.clone(), __pa1.clone()),
                _ => bail!("pattern mismatch"),
            } };
            idx = __pa0.clone();
            cols = __pa1.clone();
            colEqs = List::flatten(({
        let mut __acc: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
        for mut a in (cols.clone()).into_iter().cloned() {
            let __x = a.columnEqns.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }))?;
            colVars = List::flatten(({
        let mut __acc: Arc<metamodelica::List<Arc<metamodelica::List<SimCodeVar::SimVar>>>> = metamodelica::nil();
        for mut a in (cols.clone()).into_iter().cloned() {
            let __x = a.columnVars.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    }))?;
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\tJacobian idx: ")); __mm_s.push_str(&*intString(idx.clone())); __mm_s.push_str(&*literal!("\n\t")); ArcStr::from(__mm_s) }).clone());
            dumpSimEqSystemLst(colEqs.clone(), (literal!("\n\t")).clone())?;
            println!("{}", (literal!("\n")).clone());
            dumpVarLst(colVars.clone(), ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("columnVars(")); __mm_s.push_str(&*intString((colVars.clone().len() as i32))); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) }).clone())?;
            ()
        },
        None => {
            ()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(())
}

fn extObjInfoString(mut info: SimCode::ExtObjInfo) -> Result<()> {
    let mut vars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut aliases: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    let mut cr1: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut cr2: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let SimCode::EXTOBJINFO { aliases: __pa0, vars: __pa1 } = (info.clone()) else { bail!("pattern mismatch") };
    aliases = __pa0.clone();
    vars = __pa1.clone();
    dumpVarLst(vars.clone(), (literal!("external object info")).clone())?;
    if !(aliases.clone().is_empty()) {
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("external object aliase (")); __mm_s.push_str(&*intString((aliases.clone().len() as i32))); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) }).clone());
        for mut tpl in &*aliases.clone() {
            let mut tpl = tpl.clone();
            (cr1, cr2) = tpl.clone();
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*ComponentReference::crefStr(cr1.clone())?); __mm_s.push_str(&*literal!(" = ")); __mm_s.push_str(&*ComponentReference::crefStr(cr2.clone())?); ArcStr::from(__mm_s) }).clone());
        }
        println!("{}", (literal!("\n")).clone());
    }
    Ok(())
}

fn dumpClockPartition(mut clockPart: SimCode::ClockedPartition) -> Result<()> {
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("BaseClock:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); __mm_s.push_str(&*ExpressionDump::clockKindString(clockPart.baseClock.clone())?); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*stringDelimitList(List::map(clockPart.subPartitions.clone(), (std::sync::Arc::new(subPartitionString) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::SubPartition) -> Result<ArcStr> + 'static>))?, (literal!("\n\n")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", (literal!("\n")).clone());
    Ok(())
}

fn previousString(mut isPreviousVar: bool) -> ArcStr {
    let mut s: ArcStr = arcstr::literal!("");
    if isPreviousVar.clone() {
        s = (literal!(" (PREVIOUS)")).clone();
    } else {
        s = (literal!(" ")).clone();
    }
    s
}

fn subPartitionString(mut subPart: SimCode::SubPartition) -> Result<ArcStr> {
    let mut r#str: ArcStr = arcstr::literal!("");
    let mut simVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut arePrevious: Arc<metamodelica::List<bool>> = metamodelica::nil();
    let mut simVarStrings: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    simVars = List::map(subPart.vars.clone(), std::sync::Arc::new(fnptr!(Util::tuple21, _)))?;
    arePrevious = List::map(subPart.vars.clone(), std::sync::Arc::new(fnptr!(Util::tuple22, _)))?;
    simVarStrings = List::threadMap(List::map(simVars.clone(), (std::sync::Arc::new(simVarString) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))?, List::map(arePrevious.clone(), (std::sync::Arc::new(fnptr!(previousString, bool)) as std::sync::Arc<dyn ::std::ops::Fn(bool) -> Result<ArcStr> + 'static>))?, (std::sync::Arc::new(fnptr!(stringAppend, ArcStr, ArcStr)) as std::sync::Arc<dyn ::std::ops::Fn(ArcStr, ArcStr) -> Result<ArcStr> + 'static>))?;
    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("SubPartition Vars:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*stringDelimitList(simVarStrings.clone(), (literal!("\n")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("partition equations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*stringDelimitList(List::map(subPart.equations.clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>))?, (literal!("\n")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("removedEquations equations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*stringDelimitList(List::map(subPart.removedEquations.clone(), (std::sync::Arc::new(simEqSystemString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>))?, (literal!("\n")).clone())); ArcStr::from(__mm_s) }).clone();
    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("SubClock:\n")); __mm_s.push_str(&*BackendDump::subClockString(subPart.subClock.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("Hold Events: ")); __mm_s.push_str(&*boolString(subPart.holdEvents.clone())); ArcStr::from(__mm_s) }).clone();
    Ok(r#str)
}

pub fn dumpSimCodeDAEmodeDataString(mut inDaeModedata: Option<SimCode::DaeModeData>) -> Result<ArcStr> {
    let mut r#str: ArcStr = arcstr::literal!("");
    let () = (match inDaeModedata.clone() {
        Some(mut dmd) => {
            let mut sparsity: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
            let mut sparsityT: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\ndaeMode:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("residual Equations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
            println!("{}", (r#str.clone()).clone());
            dumpSimEqSystemLst(List::flatten(dmd.daeEquations.clone())?, (literal!("\n")).clone())?;
            dumpVarLst(dmd.residualVars.clone(), ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("residualVars(")); __mm_s.push_str(&*intString((dmd.residualVars.clone().len() as i32))); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) }).clone())?;
            dumpVarLst(dmd.algebraicVars.clone(), ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("algebraicDAEVars(")); __mm_s.push_str(&*intString((dmd.algebraicVars.clone().len() as i32))); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) }).clone())?;
            dumpVarLst(dmd.auxiliaryVars.clone(), ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("auxVars(")); __mm_s.push_str(&*intString((dmd.auxiliaryVars.clone().len() as i32))); __mm_s.push_str(&*literal!(")")); ArcStr::from(__mm_s) }).clone())?;
            if isSome(dmd.sparsityPattern.clone()) {
                r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Sparsity Pattern:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                println!("{}", (r#str.clone()).clone());
                let (__pa0, __pa1) = ::match_deref::match_deref! { match &(Util::getOption(dmd.sparsityPattern.clone())?) {
                    Deref @ SimCode::JacobianMatrix { sparsityT: __pa0, sparsity: __pa1, .. } => (__pa0.clone(), __pa1.clone()),
                    _ => bail!("pattern mismatch"),
                } };
                sparsityT = __pa0.clone();
                sparsity = __pa1.clone();
                dumpSparsePatternInt(sparsity.clone())?;
                r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Sparsity Pattern Transposed:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
                println!("{}", (r#str.clone()).clone());
                dumpSparsePatternInt(sparsityT.clone())?;
            }
            ()
        },
        None => {
            ()
        },
    });
    Ok(r#str)
}

pub fn dumpSimCodeDebug(mut simCode: SimCode::SimCode) -> Result<()> {
    let mut jacObs: Arc<metamodelica::List<Option<Arc<SimCode::JacobianMatrix>>>> = metamodelica::nil();
    println!("{}", (literal!("\n\n*********************\n* SimCode Equations *\n*********************\n\n")).clone());
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nallEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.allEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n\n\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nodeEquations (")); __mm_s.push_str(&*intString((simCode.odeEquations.clone().len() as i32))); __mm_s.push_str(&*literal!(" systems):\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    List::map1_0(simCode.odeEquations.clone(), (std::sync::Arc::new(dumpSimEqSystemLst) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, ArcStr) -> Result<()> + 'static>), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n\n\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nalgebraicEquations (")); __mm_s.push_str(&*intString((simCode.algebraicEquations.clone().len() as i32))); __mm_s.push_str(&*literal!(" systems):\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    List::map1_0(simCode.algebraicEquations.clone(), (std::sync::Arc::new(dumpSimEqSystemLst) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, ArcStr) -> Result<()> + 'static>), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n\n\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("clockPartitions (")); __mm_s.push_str(&*intString((simCode.clockedPartitions.clone().len() as i32))); __mm_s.push_str(&*literal!(" systems):\n\n")); ArcStr::from(__mm_s) }).clone());
    List::map_0(simCode.clockedPartitions.clone(), (std::sync::Arc::new(dumpClockPartition) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::ClockedPartition) -> Result<()> + 'static>))?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n\n\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\ninitialEquations: (")); __mm_s.push_str(&*intString((simCode.initialEquations.clone().len() as i32))); __mm_s.push_str(&*literal!(")\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.initialEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n\n\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\ninitialEquations_lambda0: (")); __mm_s.push_str(&*intString((simCode.initialEquations_lambda0.clone().len() as i32))); __mm_s.push_str(&*literal!(")\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.initialEquations_lambda0.clone(), (literal!("\n")).clone())?;
    if Flags::getConfigEnum(Flags::SYM_SOLVER.clone())? > 0 {
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\ninlineEquations: (")); __mm_s.push_str(&*intString((simCode.inlineEquations.clone().len() as i32))); __mm_s.push_str(&*literal!(" systems)\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        dumpSimEqSystemLst(simCode.inlineEquations.clone(), (literal!("\n")).clone())?;
    }
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nremovedInitialEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.removedInitialEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nstartValueEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.startValueEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nnominalValueEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.nominalValueEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nminValueEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.minValueEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nmaxValueEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.maxValueEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nparameterEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.parameterEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nremovedEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.removedEquations.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nalgorithmAndEquationAsserts:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.algorithmAndEquationAsserts.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nequationsForZeroCrossings:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.equationsForZeroCrossings.clone(), (literal!("\n")).clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\ngeneric calls:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", (List::toString(simCode.generic_loop_calls.clone(), (std::sync::Arc::new(simGenericCallString) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::SimGenericCall) -> Result<ArcStr> + 'static>), (literal!("")).clone(), (literal!("")).clone(), (literal!("\n")).clone(), (literal!("")).clone(), true, 0)?).clone());
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\njacobianEquations:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpSimEqSystemLst(simCode.jacobianEquations.clone(), (literal!("\n")).clone())?;
    extObjInfoString(simCode.extObjInfo.clone())?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\njacobianMatrices:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    jacObs = List::map(simCode.jacobianMatrices.clone(), std::sync::Arc::new(fnptr!(Util::makeOption, _)))?;
    List::map_0(jacObs.clone(), (std::sync::Arc::new(dumpJacobianMatrix) as std::sync::Arc<dyn ::std::ops::Fn(Option<Arc<SimCode::JacobianMatrix>>) -> Result<()> + 'static>))?;
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nmodelInfo:\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    dumpModelInfo(simCode.modelInfo.clone())?;
    dumpSimCodeDAEmodeDataString(simCode.daeModeData.clone())?;
    Ok(())
}

fn isAliasVar(mut var: SimCodeVar::SimVar) -> bool {
    let mut res: bool = false;
    res = (match var.clone() {
        SimCodeVar::SimVar { aliasvar: SimCodeVar::AliasVariable::NOALIAS { .. }, .. } => false,
        _ => true,
    });
    res
}

fn sortSimvars(mut simvars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<()> {
    let mut i: i32 = 0;
    let mut arr: metamodelica::Array<i32> = Default::default();
    for __ord0 in (1)..=(30) {
        let mut i = match __ord0 { 1 => SimVarsIndex::state, 2 => SimVarsIndex::derivative, 3 => SimVarsIndex::alg, 4 => SimVarsIndex::discreteAlg, 5 => SimVarsIndex::realOptimizeConstraints, 6 => SimVarsIndex::realOptimizeFinalConstraints, 7 => SimVarsIndex::param, 8 => SimVarsIndex::alias, 9 => SimVarsIndex::intAlg, 10 => SimVarsIndex::intParam, 11 => SimVarsIndex::intAlias, 12 => SimVarsIndex::boolAlg, 13 => SimVarsIndex::boolParam, 14 => SimVarsIndex::boolAlias, 15 => SimVarsIndex::stringAlg, 16 => SimVarsIndex::stringParam, 17 => SimVarsIndex::stringAlias, 18 => SimVarsIndex::extObj, 19 => SimVarsIndex::inputs, 20 => SimVarsIndex::outputs, 21 => SimVarsIndex::r#const, 22 => SimVarsIndex::intConst, 23 => SimVarsIndex::boolConst, 24 => SimVarsIndex::stringConst, 25 => SimVarsIndex::sensitivity, 26 => SimVarsIndex::setcvars, 27 => SimVarsIndex::datareconinputvars, 28 => SimVarsIndex::setBVars, 29 => SimVarsIndex::jacobian, 30 => SimVarsIndex::seed, _ => unreachable!("enum ordinal out of range") };
        metamodelica::Dangerous::arrayUpdateNoBoundsChecking(simvars.clone(), ((i.clone()) as i32 /* Integer(...) with unknown arg type */), List::sort(metamodelica::Dangerous::arrayGetNoBoundsChecking(simvars.clone(), ((i.clone()) as i32 /* Integer(...) with unknown arg type */)), (std::sync::Arc::new(simVarCompareByCrefSubsAtEndlLexical) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, SimCodeVar::SimVar) -> Result<bool> + 'static>))?);
    }
    for mut v in &*metamodelica::Dangerous::arrayGetNoBoundsChecking(simvars.clone(), ((SimVarsIndex::inputs.clone()) as i32)) {
        let mut v = v.clone();
        i = (match v.clone() {
        SimCodeVar::SimVar { inputIndex: Some(mut arr), .. } => {
            {let _arr = arr.clone(); _arr.borrow_mut()[(1-1) as usize] = i.clone(); _arr};
            i.clone() + 1
        },
        _ => {
            Error::addInternalError((literal!("Failed to update indexes of simvars")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
    });
    }
    Ok(())
}

pub fn simVarCompareByCrefSubsAtEndlLexical(mut var1: SimCodeVar::SimVar, mut var2: SimCodeVar::SimVar) -> Result<bool> {
    let mut outBool: bool = false;
    let mut cr1: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut cr2: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    cr1 = var1.name.clone();
    cr2 = var2.name.clone();
    outBool = ComponentReferenceBasics::crefLexicalGreaterSubsAtEnd(cr1.clone(), cr2.clone())?;
    Ok(outBool)
}

fn extendIncompleteArray(mut simvars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<()> {
    let mut simVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut set: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
    set = HashSet::emptyHashSet();
    for __ord0 in (1)..=(30) {
        let mut i = match __ord0 { 1 => SimVarsIndex::state, 2 => SimVarsIndex::derivative, 3 => SimVarsIndex::alg, 4 => SimVarsIndex::discreteAlg, 5 => SimVarsIndex::realOptimizeConstraints, 6 => SimVarsIndex::realOptimizeFinalConstraints, 7 => SimVarsIndex::param, 8 => SimVarsIndex::alias, 9 => SimVarsIndex::intAlg, 10 => SimVarsIndex::intParam, 11 => SimVarsIndex::intAlias, 12 => SimVarsIndex::boolAlg, 13 => SimVarsIndex::boolParam, 14 => SimVarsIndex::boolAlias, 15 => SimVarsIndex::stringAlg, 16 => SimVarsIndex::stringParam, 17 => SimVarsIndex::stringAlias, 18 => SimVarsIndex::extObj, 19 => SimVarsIndex::inputs, 20 => SimVarsIndex::outputs, 21 => SimVarsIndex::r#const, 22 => SimVarsIndex::intConst, 23 => SimVarsIndex::boolConst, 24 => SimVarsIndex::stringConst, 25 => SimVarsIndex::sensitivity, 26 => SimVarsIndex::setcvars, 27 => SimVarsIndex::datareconinputvars, 28 => SimVarsIndex::setBVars, 29 => SimVarsIndex::jacobian, 30 => SimVarsIndex::seed, _ => unreachable!("enum ordinal out of range") };
        set = List::fold(metamodelica::Dangerous::arrayGetNoBoundsChecking(simvars.clone(), ((i.clone()) as i32 /* Integer(...) with unknown arg type */)), (std::sync::Arc::new(collectArrayFirstVars) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))> + 'static>), set.clone())?;
    }
    for __ord1 in (1)..=(30) {
        let mut i = match __ord1 { 1 => SimVarsIndex::state, 2 => SimVarsIndex::derivative, 3 => SimVarsIndex::alg, 4 => SimVarsIndex::discreteAlg, 5 => SimVarsIndex::realOptimizeConstraints, 6 => SimVarsIndex::realOptimizeFinalConstraints, 7 => SimVarsIndex::param, 8 => SimVarsIndex::alias, 9 => SimVarsIndex::intAlg, 10 => SimVarsIndex::intParam, 11 => SimVarsIndex::intAlias, 12 => SimVarsIndex::boolAlg, 13 => SimVarsIndex::boolParam, 14 => SimVarsIndex::boolAlias, 15 => SimVarsIndex::stringAlg, 16 => SimVarsIndex::stringParam, 17 => SimVarsIndex::stringAlias, 18 => SimVarsIndex::extObj, 19 => SimVarsIndex::inputs, 20 => SimVarsIndex::outputs, 21 => SimVarsIndex::r#const, 22 => SimVarsIndex::intConst, 23 => SimVarsIndex::boolConst, 24 => SimVarsIndex::stringConst, 25 => SimVarsIndex::sensitivity, 26 => SimVarsIndex::setcvars, 27 => SimVarsIndex::datareconinputvars, 28 => SimVarsIndex::setBVars, 29 => SimVarsIndex::jacobian, 30 => SimVarsIndex::seed, _ => unreachable!("enum ordinal out of range") };
        (simVars, set) = List::mapFold(metamodelica::Dangerous::arrayGetNoBoundsChecking(simvars.clone(), ((i.clone()) as i32 /* Integer(...) with unknown arg type */)), (std::sync::Arc::new(setArrayElementnoFirst) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> + 'static>), set.clone())?;
        metamodelica::Dangerous::arrayUpdateNoBoundsChecking(simvars.clone(), ((i.clone()) as i32 /* Integer(...) with unknown arg type */), simVars.clone());
    }
    Ok(())
}

fn setArrayElementnoFirst(mut iVar: SimCodeVar::SimVar, mut iSet: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>)))> {
    let mut oVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut oSet: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
    (oVar, oSet) = 'mc: {
        let __mc_input = iVar.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                SimCodeVar::SimVar { arrayCref: Some(_), .. } => {
                    Ok((iVar.clone(), iSet.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                SimCodeVar::SimVar { arrayCref: None, numArrayElement: Deref @ metamodelica::List::Cons { head: _, tail: _ }, name: cr, .. } => {
                    let mut var: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
                    let mut set: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
                    let mut cr = (*cr).clone();
                    ::match_deref::match_deref! { match &(ComponentReference::crefLastSubs(cr.clone())?) {
                        Deref @ metamodelica::List::Cons { head: _, tail: _ } => (),
                        _ => bail!("pattern mismatch"),
                    } };
                    cr = ComponentReferenceBasics::crefStripLastSubs(cr.clone())?;
                    let false = (BaseHashSet::has(cr.clone(), iSet.clone())?) else { bail!("pattern mismatch") };
                    var = addSimVarArrayCref(iVar.clone(), cr.clone());
                    set = BaseHashSet::add(cr.clone(), iSet.clone())?;
                    Ok((var.clone(), set.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((iVar.clone(), iSet.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((oVar, oSet))
}

fn addSimVarArrayCref(mut iVar: SimCodeVar::SimVar, mut arrayCref: Arc<DAE::ComponentRef>) -> SimCodeVar::SimVar {
    let mut oVar: SimCodeVar::SimVar = iVar.clone();
    oVar.arrayCref = Some(arrayCref.clone());
    oVar
}

fn collectArrayFirstVars(mut var: SimCodeVar::SimVar, mut iSet: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>))> {
    let mut oSet: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<Arc<DAE::ComponentRef>>>), i32, i32, (HashSet::FuncHashCref, HashSet::FuncCrefEqual, HashSet::FuncCrefStr));
    oSet = (::match_deref::match_deref! { match &(var.clone()) {
        SimCodeVar::SimVar { arrayCref: Some(_), name: cr, .. } => {
            let mut cr = (*cr).clone();
            cr = ComponentReferenceBasics::crefStripLastSubs(cr.clone())?;
            BaseHashSet::add(cr.clone(), iSet.clone())?
        },
        _ => {
            iSet.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(oSet)
}

fn fixIndex(mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<()> {
    let mut ix: i32 = 0;
    let mut lst: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut isCpp: bool = Config::simCodeTarget()? == literal!("Cpp");
    for __ord0 in (((SimVarsIndex::state.clone()) as i32))..=(((SimVarsIndex::realOptimizeFinalConstraints.clone()) as i32)) {
        let mut i = match __ord0 { 1 => SimVarsIndex::state, 2 => SimVarsIndex::derivative, 3 => SimVarsIndex::alg, 4 => SimVarsIndex::discreteAlg, 5 => SimVarsIndex::realOptimizeConstraints, 6 => SimVarsIndex::realOptimizeFinalConstraints, 7 => SimVarsIndex::param, 8 => SimVarsIndex::alias, 9 => SimVarsIndex::intAlg, 10 => SimVarsIndex::intParam, 11 => SimVarsIndex::intAlias, 12 => SimVarsIndex::boolAlg, 13 => SimVarsIndex::boolParam, 14 => SimVarsIndex::boolAlias, 15 => SimVarsIndex::stringAlg, 16 => SimVarsIndex::stringParam, 17 => SimVarsIndex::stringAlias, 18 => SimVarsIndex::extObj, 19 => SimVarsIndex::inputs, 20 => SimVarsIndex::outputs, 21 => SimVarsIndex::r#const, 22 => SimVarsIndex::intConst, 23 => SimVarsIndex::boolConst, 24 => SimVarsIndex::stringConst, 25 => SimVarsIndex::sensitivity, 26 => SimVarsIndex::setcvars, 27 => SimVarsIndex::datareconinputvars, 28 => SimVarsIndex::setBVars, 29 => SimVarsIndex::jacobian, 30 => SimVarsIndex::seed, _ => unreachable!("enum ordinal out of range") };
        lst = metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((i.clone()) as i32));
        if !(isCpp.clone()) {
            metamodelica::Dangerous::arrayUpdateNoBoundsChecking(simVars.clone(), ((i.clone()) as i32), (rewriteIndex(lst.clone(), ix.clone())).0);
            ix = ix.clone() + (lst.clone().len() as i32);
        } else {
            metamodelica::Dangerous::arrayUpdateNoBoundsChecking(simVars.clone(), ((i.clone()) as i32), (rewriteIndexColumnMajor(lst.clone(), 0)?).0);
        }
    }
    for __ord1 in (((SimVarsIndex::param.clone()) as i32))..=(((SimVarsIndex::stringConst.clone()) as i32)) {
        let mut i = match __ord1 { 1 => SimVarsIndex::state, 2 => SimVarsIndex::derivative, 3 => SimVarsIndex::alg, 4 => SimVarsIndex::discreteAlg, 5 => SimVarsIndex::realOptimizeConstraints, 6 => SimVarsIndex::realOptimizeFinalConstraints, 7 => SimVarsIndex::param, 8 => SimVarsIndex::alias, 9 => SimVarsIndex::intAlg, 10 => SimVarsIndex::intParam, 11 => SimVarsIndex::intAlias, 12 => SimVarsIndex::boolAlg, 13 => SimVarsIndex::boolParam, 14 => SimVarsIndex::boolAlias, 15 => SimVarsIndex::stringAlg, 16 => SimVarsIndex::stringParam, 17 => SimVarsIndex::stringAlias, 18 => SimVarsIndex::extObj, 19 => SimVarsIndex::inputs, 20 => SimVarsIndex::outputs, 21 => SimVarsIndex::r#const, 22 => SimVarsIndex::intConst, 23 => SimVarsIndex::boolConst, 24 => SimVarsIndex::stringConst, 25 => SimVarsIndex::sensitivity, 26 => SimVarsIndex::setcvars, 27 => SimVarsIndex::datareconinputvars, 28 => SimVarsIndex::setBVars, 29 => SimVarsIndex::jacobian, 30 => SimVarsIndex::seed, _ => unreachable!("enum ordinal out of range") };
        metamodelica::Dangerous::arrayUpdateNoBoundsChecking(simVars.clone(), ((i.clone()) as i32), (rewriteIndex(metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((i.clone()) as i32)), 0)).0);
    }
    Ok(())
}

pub fn rewriteIndex(mut inVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut index: i32) -> (Arc<metamodelica::List<SimCodeVar::SimVar>>, i32) {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut index: i32 = index;
    for mut var in &*inVars.clone() {
        let mut var = var.clone();
        var.index = index.clone();
        outVars = metamodelica::cons(var.clone(), outVars.clone());
        index = index.clone() + 1;
    }
    outVars = Dangerous::listReverseInPlace(outVars.clone());
    (outVars, index)
}

fn rewriteIndexColumnMajor(mut inVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut index: i32) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, i32)> {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut index: i32 = index;
    let mut subs: Arc<metamodelica::List<Arc<DAE::Subscript>>> = metamodelica::nil();
    let mut arrayDimensions: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut elementIndex: i32 = 0;
    for mut var in &*inVars.clone() {
        let mut var = var.clone();
        subs = ComponentReference::crefLastSubs(var.name.clone())?;
        if (subs.clone().len() as i32) > 1 {
            arrayDimensions = List::map(var.numArrayElement.clone(), (std::sync::Arc::new(stringInt) as std::sync::Arc<dyn ::std::ops::Fn(ArcStr) -> Result<i32> + 'static>))?;
            elementIndex = SimCodeUtilShared::getScalarElementIndex(subs.clone(), arrayDimensions.clone());
            var.index = index.clone() - elementIndex.clone() + convertIndexToColumnMajor(elementIndex.clone(), arrayDimensions.clone())?;
        } else {
            var.index = index.clone();
        }
        outVars = metamodelica::cons(var.clone(), outVars.clone());
        index = index.clone() + 1;
    }
    outVars = Dangerous::listReverseInPlace(outVars.clone());
    Ok((outVars, index))
}

fn setVariableIndex(mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<()> {
    let mut index_: i32 = 1;
    let mut fmi_index_: i32 = 1;
    let mut lst: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    for __ord0 in (((SimVarsIndex::state.clone()) as i32))..=(((SimVarsIndex::stringConst.clone()) as i32)) {
        let mut i = match __ord0 { 1 => SimVarsIndex::state, 2 => SimVarsIndex::derivative, 3 => SimVarsIndex::alg, 4 => SimVarsIndex::discreteAlg, 5 => SimVarsIndex::realOptimizeConstraints, 6 => SimVarsIndex::realOptimizeFinalConstraints, 7 => SimVarsIndex::param, 8 => SimVarsIndex::alias, 9 => SimVarsIndex::intAlg, 10 => SimVarsIndex::intParam, 11 => SimVarsIndex::intAlias, 12 => SimVarsIndex::boolAlg, 13 => SimVarsIndex::boolParam, 14 => SimVarsIndex::boolAlias, 15 => SimVarsIndex::stringAlg, 16 => SimVarsIndex::stringParam, 17 => SimVarsIndex::stringAlias, 18 => SimVarsIndex::extObj, 19 => SimVarsIndex::inputs, 20 => SimVarsIndex::outputs, 21 => SimVarsIndex::r#const, 22 => SimVarsIndex::intConst, 23 => SimVarsIndex::boolConst, 24 => SimVarsIndex::stringConst, 25 => SimVarsIndex::sensitivity, 26 => SimVarsIndex::setcvars, 27 => SimVarsIndex::datareconinputvars, 28 => SimVarsIndex::setBVars, 29 => SimVarsIndex::jacobian, 30 => SimVarsIndex::seed, _ => unreachable!("enum ordinal out of range") };
        (lst, index_, fmi_index_) = setVariableIndexHelper(metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((i.clone()) as i32)), index_.clone(), fmi_index_.clone())?;
        metamodelica::Dangerous::arrayUpdateNoBoundsChecking(simVars.clone(), ((i.clone()) as i32), lst.clone());
    }
    Ok(())
}

pub fn setVariableIndexHelper(mut inVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut inIndex: i32, mut inFMIIndex: i32) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, i32, i32)> {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut outIndex: i32 = 0;
    let mut outFMIIndex: i32 = 0;
    let (__pa0, (__pa1, __pa2)) = List::mapFold(inVars.clone(), (std::sync::Arc::new(setVariableIndexHelper2) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (i32, i32)) -> Result<(SimCodeVar::SimVar, (i32, i32))> + 'static>), (inIndex.clone(), inFMIIndex.clone()))?;
    outVars = __pa0.clone();
    outIndex = __pa1.clone();
    outFMIIndex = __pa2.clone();
    Ok((outVars, outIndex, outFMIIndex))
}

fn setVariableIndexHelper2(mut var: SimCodeVar::SimVar, mut tpl: (i32, i32)) -> Result<(SimCodeVar::SimVar, (i32, i32))> {
    let mut var: SimCodeVar::SimVar = var;
    let mut tpl: (i32, i32) = tpl;
    let mut index: i32 = 0;
    let mut fmi_index: i32 = 0;
    (index, fmi_index) = tpl.clone();
    var.variable_index = Some(index.clone());
    index = index.clone() + SimCodeUtilShared::getNumElems(var.clone())?;
    if isSome(var.exportVar.clone()) {
        var.fmi_index = Some(fmi_index.clone());
        fmi_index = fmi_index.clone() + SimCodeUtilShared::getNumElems(var.clone())?;
    } else {
        var.fmi_index = None;
    }
    tpl = (index.clone(), fmi_index.clone());
    Ok((var, tpl))
}

fn getFmiUnitDefinitions(mut simVars: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>) -> Result<Arc<metamodelica::List<SimCode::UnitDefinition>>> {
    let mut unitDefinitions: Arc<metamodelica::List<SimCode::UnitDefinition>> = metamodelica::nil();
    let mut unitNameKeys: (metamodelica::Array<Arc<metamodelica::List<(ArcStr, i32)>>>, (i32, i32, metamodelica::Array<Option<ArcStr>>), i32, i32, (HashSetString::FuncHashCref, HashSetString::FuncCrefEqual, HashSetString::FuncCrefStr));
    unitNameKeys = HashSetString::emptyHashSet();
    for __ord0 in (((SimVarsIndex::state.clone()) as i32))..=(((SimVarsIndex::stringConst.clone()) as i32)) {
        let mut i = match __ord0 { 1 => SimVarsIndex::state, 2 => SimVarsIndex::derivative, 3 => SimVarsIndex::alg, 4 => SimVarsIndex::discreteAlg, 5 => SimVarsIndex::realOptimizeConstraints, 6 => SimVarsIndex::realOptimizeFinalConstraints, 7 => SimVarsIndex::param, 8 => SimVarsIndex::alias, 9 => SimVarsIndex::intAlg, 10 => SimVarsIndex::intParam, 11 => SimVarsIndex::intAlias, 12 => SimVarsIndex::boolAlg, 13 => SimVarsIndex::boolParam, 14 => SimVarsIndex::boolAlias, 15 => SimVarsIndex::stringAlg, 16 => SimVarsIndex::stringParam, 17 => SimVarsIndex::stringAlias, 18 => SimVarsIndex::extObj, 19 => SimVarsIndex::inputs, 20 => SimVarsIndex::outputs, 21 => SimVarsIndex::r#const, 22 => SimVarsIndex::intConst, 23 => SimVarsIndex::boolConst, 24 => SimVarsIndex::stringConst, 25 => SimVarsIndex::sensitivity, 26 => SimVarsIndex::setcvars, 27 => SimVarsIndex::datareconinputvars, 28 => SimVarsIndex::setBVars, 29 => SimVarsIndex::jacobian, 30 => SimVarsIndex::seed, _ => unreachable!("enum ordinal out of range") };
        (unitDefinitions, unitNameKeys) = getFmiUnitDefinitionsHelper(metamodelica::Dangerous::arrayGetNoBoundsChecking(simVars.clone(), ((i.clone()) as i32)), unitDefinitions.clone(), unitNameKeys.clone())?;
    }
    Ok(unitDefinitions)
}

fn getFmiUnitDefinitionsHelper(mut inVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut unitDefinitions: Arc<metamodelica::List<SimCode::UnitDefinition>>, mut unitNameKeys: (metamodelica::Array<Arc<metamodelica::List<(ArcStr, i32)>>>, (i32, i32, metamodelica::Array<Option<ArcStr>>), i32, i32, (Arc<dyn ::std::ops::Fn(ArcStr) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(ArcStr, ArcStr) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(ArcStr) -> Result<ArcStr> + 'static>))) -> Result<(Arc<metamodelica::List<SimCode::UnitDefinition>>, (metamodelica::Array<Arc<metamodelica::List<(ArcStr, i32)>>>, (i32, i32, metamodelica::Array<Option<ArcStr>>), i32, i32, (Arc<dyn ::std::ops::Fn(ArcStr) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(ArcStr, ArcStr) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(ArcStr) -> Result<ArcStr> + 'static>)))> {
    let mut unitDefinitions: Arc<metamodelica::List<SimCode::UnitDefinition>> = unitDefinitions;
    let mut unitNameKeys: (metamodelica::Array<Arc<metamodelica::List<(ArcStr, i32)>>>, (i32, i32, metamodelica::Array<Option<ArcStr>>), i32, i32, (HashSetString::FuncHashCref, HashSetString::FuncCrefEqual, HashSetString::FuncCrefStr)) = unitNameKeys;
    let mut unit: Unit::Unit = <Unit::Unit as ::std::default::Default>::default();
    for mut var in &*inVars.clone() {
        let mut var = var.clone();
        if isSome(var.exportVar.clone()) {
            if !(stringEq((var.unit.clone()).clone(), (literal!("")).clone())) && !(BaseHashSet::has((var.unit.clone()).clone(), unitNameKeys.clone())?) {
                unitNameKeys = BaseHashSet::add((var.unit.clone()).clone(), unitNameKeys.clone())?;
                match '__try0: {
                    unit = unwrap_break_err!(Unit::parseUnitString((var.unit.clone()).clone(), unwrap_break_err!(Unit::getKnownUnits(), '__try0), Absyn::dummyInfo.clone()), '__try0);
                    unitDefinitions = metamodelica::cons(SimCode::UnitDefinition { name: (var.unit.clone()).clone(), baseUnit: unwrap_break_err!(transformUnitToBaseUnit(unit.clone()), '__try0) }, unitDefinitions.clone());
                    Ok::<_, anyhow::Error>((unitDefinitions.clone(),))
                } {
                    Ok((__try0_o0,)) => {
                        unitDefinitions = __try0_o0;
                    }
                    Err(_) => {
                        unitDefinitions = metamodelica::cons(SimCode::UnitDefinition { name: (var.unit.clone()).clone(), baseUnit: openmodelica_simcode_types::SimCode::BaseUnit::NOBASEUNIT }, unitDefinitions.clone());
                    }
                }
            }
        }
    }
    Ok((unitDefinitions, unitNameKeys))
}

pub fn transformUnitToBaseUnit(mut unit: Unit::Unit) -> Result<SimCode::BaseUnit> {
    let mut baseUnit: SimCode::BaseUnit = SimCode::BaseUnit::NOBASEUNIT;
    let mut mol: i32 = 0;
    let mut cd: i32 = 0;
    let mut m: i32 = 0;
    let mut s: i32 = 0;
    let mut A: i32 = 0;
    let mut K: i32 = 0;
    let mut kg: i32 = 0;
    let mut factor: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
    let Unit::UNIT { s: __pa0, m: __pa1, g: __pa2, A: __pa3, K: __pa4, mol: __pa5, cd: __pa6, factor: __pa7 } = (unit.clone()) else { bail!("pattern mismatch") };
    s = __pa0.clone();
    m = __pa1.clone();
    kg = __pa2.clone();
    A = __pa3.clone();
    K = __pa4.clone();
    mol = __pa5.clone();
    cd = __pa6.clone();
    factor = __pa7.clone();
    baseUnit = SimCode::BaseUnit::BASEUNIT { s: s.clone(), m: m.clone(), kg: kg.clone(), A: A.clone(), K: K.clone(), mol: mol.clone(), cd: cd.clone(), factor: factor.clone() * (metamodelica::OrderedFloat((10) as f64)).powf(metamodelica::OrderedFloat((-(3 * kg.clone())) as f64)), offset: metamodelica::OrderedFloat(0.0_f64) };
    Ok(baseUnit)
}

pub fn createCrefToSimVarHT(mut modelInfo: SimCode::ModelInfo) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> {
    let mut outHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
    let mut size: i32 = 0;
    let mut varInfo: SimCode::VarInfo = <SimCode::VarInfo as ::std::default::Default>::default();
    let mut arraySimVars: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (HashTableCrILst::FuncHashCref, HashTableCrILst::FuncCrefEqual, HashTableCrILst::FuncCrefStr, HashTableCrILst::FuncExpStr));
    let mut vars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    match '__try0: {
        varInfo = modelInfo.varInfo.clone();
        vars = modelInfo.vars.clone();
        size = varInfo.numStateVars.clone() + varInfo.numAlgVars.clone() + varInfo.numIntAlgVars.clone() + varInfo.numBoolAlgVars.clone() + varInfo.numAlgAliasVars.clone() + varInfo.numIntAliasVars.clone() + varInfo.numBoolAliasVars.clone() + varInfo.numParams.clone() + varInfo.numIntParams.clone() + varInfo.numBoolParams.clone() + varInfo.numOutVars.clone() + varInfo.numInVars.clone() + varInfo.numOptimizeConstraints.clone() + varInfo.numOptimizeFinalConstraints.clone();
        size = intMax(size.clone(), 1023);
        outHT = HashTableCrefSimVar::emptyHashTableSized(size.clone());
        arraySimVars = HashTableCrILst::emptyHashTableSized(size.clone());
        outHT = unwrap_break_err!(List::fold(vars.stateVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.derivativeVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.algVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        arraySimVars = unwrap_break_err!(List::fold(vars.algVars.clone(), (std::sync::Arc::new(getArraySimVars) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>) -> Result<ArcStr> + 'static>))> + 'static>), arraySimVars.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.discreteAlgVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.intAlgVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.boolAlgVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.paramVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        arraySimVars = unwrap_break_err!(List::fold(vars.paramVars.clone(), (std::sync::Arc::new(getArraySimVars) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>) -> Result<ArcStr> + 'static>))> + 'static>), arraySimVars.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.intParamVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.boolParamVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.aliasVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        arraySimVars = unwrap_break_err!(List::fold(vars.aliasVars.clone(), (std::sync::Arc::new(getArraySimVars) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>) -> Result<ArcStr> + 'static>))> + 'static>), arraySimVars.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.intAliasVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.boolAliasVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.stringAlgVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.stringParamVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.stringAliasVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.extObjVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.constVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.intConstVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.boolConstVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.stringConstVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.sensitivityVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.jacobianVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.seedVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.realOptimizeConstraintsVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        outHT = unwrap_break_err!(List::fold(vars.realOptimizeFinalConstraintsVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), outHT.clone()), '__try0);
        Ok::<_, anyhow::Error>((arraySimVars.clone(), outHT.clone(), size.clone(), varInfo.clone(), vars.clone()))
    } {
        Ok((__try0_o0, __try0_o1, __try0_o2, __try0_o3, __try0_o4)) => {
            arraySimVars = __try0_o0;
            outHT = __try0_o1;
            size = __try0_o2;
            varInfo = __try0_o3;
            vars = __try0_o4;
        }
        Err(__try0_err) => {
            Error::addInternalError((literal!("function createCrefToSimVarHT failed")).clone(), metamodelica::sourceInfo!())?;
            return Err(__try0_err);
        }
    }
    Ok(outHT)
}

fn getArraySimVars(mut iSimVar: SimCodeVar::SimVar, mut iArrayMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>) -> Result<ArcStr> + 'static>))> {
    let mut oArrayMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (HashTableCrILst::FuncHashCref, HashTableCrILst::FuncCrefEqual, HashTableCrILst::FuncCrefStr, HashTableCrILst::FuncExpStr));
    let mut name: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut arrayCref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut tmpArrayMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<i32>>)>>), i32, (HashTableCrILst::FuncHashCref, HashTableCrILst::FuncCrefEqual, HashTableCrILst::FuncCrefStr, HashTableCrILst::FuncExpStr)) = iArrayMapping.clone();
    let mut arrayVars: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut index: i32 = 0;
    oArrayMapping = (::match_deref::match_deref! { match &(iSimVar.clone()) {
        SimCodeVar::SimVar { numArrayElement: Deref @ metamodelica::List::Cons { head: _, tail: _ }, index, name, .. } => {
            arrayCref = ComponentReferenceBasics::crefStripLastSubs(name.clone())?;
            if BaseHashTable::hasKey(arrayCref.clone(), iArrayMapping.clone())? {
                arrayVars = BaseHashTable::get(arrayCref.clone(), iArrayMapping.clone())?;
                tmpArrayMapping = BaseHashTable::add((arrayCref.clone(), metamodelica::cons(index.clone(), arrayVars.clone())), tmpArrayMapping.clone())?;
            } else {
                tmpArrayMapping = BaseHashTable::add((arrayCref.clone(), list![index.clone()]), tmpArrayMapping.clone())?;
            }
            tmpArrayMapping.clone()
        },
        _ => iArrayMapping.clone(),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(oArrayMapping)
}

fn getAliasVar(mut inVar: BackendDAE::Var, mut inAliasVars: Option<BackendDAE::Variables>) -> Result<SimCodeVar::AliasVariable> {
    let mut outAlias: SimCodeVar::AliasVariable = SimCodeVar::AliasVariable::NOALIAS;
    outAlias = 'mc: {
        let __mc_input = (inVar.clone(), inAliasVars.clone());
        if let Ok(__v) = (|| -> Result<_> {
            let (BackendDAE::Var { varName: ref name, .. }, Some(mut aliasVars)) = __mc_input.clone() else { bail!("nomatch") };
            let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
            let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut alias: SimCodeVar::AliasVariable = SimCodeVar::AliasVariable::NOALIAS;
            let __pa0 = ::match_deref::match_deref! { match &(BackendVariable::getVar(name.clone(), aliasVars.clone())?) {
                (Deref @ metamodelica::List::Cons { head: __pa0, tail: _ }, _) => __pa0.clone(),
                _ => bail!("pattern mismatch"),
            } };
            var = __pa0.clone();
            e = BackendVariable::varBindExp(var.clone())?;
            (e, _) = ExpressionSimplify::simplify(e.clone())?;
            alias = getAliasVar1(e.clone(), var.clone())?;
            Ok(alias.clone())
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let _ = __mc_input.clone() else { bail!("nomatch") };
            Ok(openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS)
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outAlias)
}

fn getAliasVar1(mut inExp: Arc<DAE::Exp>, mut inVar: BackendDAE::Var) -> Result<SimCodeVar::AliasVariable> {
    let mut outAlias: SimCodeVar::AliasVariable = SimCodeVar::AliasVariable::NOALIAS;
    outAlias = 'mc: {
        let __mc_input = inExp.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CREF { componentRef: name, .. } => {
                    Ok(SimCodeVar::AliasVariable::ALIAS { varName: name.clone() })
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::UNARY { exp: Deref @ DAE::Exp::CREF { componentRef: name, .. }, operator: DAE::Operator::UMINUS { ty: _ } } => {
                    Ok(SimCodeVar::AliasVariable::NEGATEDALIAS { varName: name.clone() })
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::UNARY { exp: Deref @ DAE::Exp::CREF { componentRef: name, .. }, operator: DAE::Operator::UMINUS_ARR { ty: _ } } => {
                    Ok(SimCodeVar::AliasVariable::NEGATEDALIAS { varName: name.clone() })
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::LUNARY { exp: Deref @ DAE::Exp::CREF { componentRef: name, .. }, operator: DAE::Operator::NOT { ty: _ } } => {
                    Ok(SimCodeVar::AliasVariable::NEGATEDALIAS { varName: name.clone() })
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::CREF { componentRef: name, .. }, tail: Deref @ metamodelica::List::Nil }, path: fname, .. } => {
                    let mut name = (*name).clone();
                    Builtin::isDer(fname.clone())?;
                    name = ComponentReference::crefPrefixDer(name.clone());
                    Ok(SimCodeVar::AliasVariable::ALIAS { varName: name.clone() })
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::UNARY { exp: Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::CREF { componentRef: name, .. }, tail: Deref @ metamodelica::List::Nil }, path: fname, .. }, operator: DAE::Operator::UMINUS { ty: _ } } => {
                    let mut name = (*name).clone();
                    Builtin::isDer(fname.clone())?;
                    name = ComponentReference::crefPrefixDer(name.clone());
                    Ok(SimCodeVar::AliasVariable::NEGATEDALIAS { varName: name.clone() })
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::UNARY { exp: Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::CREF { componentRef: name, .. }, tail: Deref @ metamodelica::List::Nil }, path: fname, .. }, operator: DAE::Operator::UMINUS_ARR { ty: _ } } => {
                    let mut name = (*name).clone();
                    Builtin::isDer(fname.clone())?;
                    name = ComponentReference::crefPrefixDer(name.clone());
                    Ok(SimCodeVar::AliasVariable::NEGATEDALIAS { varName: name.clone() })
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outAlias)
}

fn unparseCommentOptionNoAnnotationNoQuote(mut absynComment: Option<Arc<SCode::Comment>>) -> ArcStr {
    let mut commentStr: ArcStr = arcstr::literal!("");
    commentStr = ((::match_deref::match_deref! { match &(absynComment.clone()) {
        Some(Deref @ SCode::Comment { annotation_: _, comment: Some(__esc_commentStr) }) => {
            commentStr = (*__esc_commentStr).clone();
            commentStr.clone()
        },
        _ => literal!(""),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })).clone();
    commentStr
}

// =============================================================================
// section for ???
//
// =============================================================================
/*mahge: kernel functions*/
fn dlowvarToSimvar(mut dlowVar: BackendDAE::Var, mut optAliasVars: Option<BackendDAE::Variables>, mut inVars: BackendDAE::Variables, mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>) -> Result<SimCodeVar::SimVar> {
    let mut simVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    simVar = (match (dlowVar.clone(), inVars.clone()) {
        (BackendDAE::Var { encrypted: mut encrypted, source: mut source, varType: ref tp, comment: mut comment, hideResult: mut hideResultExp, values: mut dae_var_attr, arryDim: ref inst_dims, varKind: mut kind @ BackendDAE::VarKind::CONST { .. }, varName: ref cr, .. }, mut vars) => {
            let mut numArrayElement: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            let mut commentStr: ArcStr = arcstr::literal!("");
            let mut unit: ArcStr = arcstr::literal!("");
            let mut displayUnit: ArcStr = arcstr::literal!("");
            let mut initVal: Option<Arc<DAE::Exp>> = None;
            let mut isFixed: bool = false;
            let mut type_: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut isDiscrete: bool = false;
            let mut isValueChangeable: bool = false;
            let mut arrayCref: Option<Arc<DAE::ComponentRef>> = None;
            let mut aliasvar: SimCodeVar::AliasVariable = SimCodeVar::AliasVariable::NOALIAS;
            let mut caus: SimCodeVar::Causality = SimCodeVar::Causality::CALCULATED_PARAMETER;
            let mut initial_: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
            let mut variability: SimCodeVar::Variability = SimCodeVar::Variability::CONSTANT;
            let mut isProtected: bool = false;
            let mut hideResult: Option<bool> = None;
            let mut relativeQuantity: bool = false;
            commentStr = (unparseCommentOptionNoAnnotationNoQuote(comment.clone())).clone();
            (unit, displayUnit) = extractVarUnit(dae_var_attr.clone())?;
            isProtected = BackendVariable::isProtected(dlowVar.clone());
            hideResult = getHideResult(hideResultExp.clone(), cr.clone(), source.clone())?;
            initVal = dlowVar.bindExp.clone();
            isFixed = BackendVariable::varFixed(dlowVar.clone());
            type_ = tp.clone();
            isDiscrete = BackendVariable::isVarDiscrete(dlowVar.clone());
            arrayCref = ComponentReference::getArrayCref(cr.clone())?;
            aliasvar = getAliasVar(dlowVar.clone(), optAliasVars.clone())?;
            numArrayElement = List::map(inst_dims.clone(), (std::sync::Arc::new(ExpressionDump::dimensionIntString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Dimension>) -> Result<ArcStr> + 'static>))?;
            isValueChangeable = BackendVariable::isChangeable(dlowVar.clone())?;
            caus = getCausality(dlowVar.clone(), vars.clone(), isValueChangeable.clone())?;
            variability = openmodelica_simcode_types::SimCodeVar::Variability::CONSTANT;
            initial_ = setInitialAttribute(dlowVar.clone(), variability.clone(), caus.clone(), isFixed.clone(), iterationVars.clone(), aliasvar.clone(), vars.clone())?;
            initVal = updateStartValue(dlowVar.clone(), initVal.clone(), initial_.clone(), caus.clone())?;
            if BackendVariable::isRealVar(dlowVar.clone()) {
                relativeQuantity = SCodeUtil::optCommentHasBooleanNamedAnnotationFalse(comment.clone(), (literal!("absoluteValue")).clone())?;
            } else {
                relativeQuantity = false;
            }
            SimCodeVar::SimVar { name: cr.clone(), varKind: kind.clone(), comment: (commentStr.clone()).clone(), unit: (unit.clone()).clone(), displayUnit: (displayUnit.clone()).clone(), index: -1, minValue: None, maxValue: None, initialValue: initVal.clone(), nominalValue: None, isFixed: isFixed.clone(), type_: type_.clone(), isDiscrete: isDiscrete.clone(), arrayCref: arrayCref.clone(), aliasvar: aliasvar.clone(), source: source.clone(), causality: Some(caus.clone()), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: isValueChangeable.clone(), isProtected: isProtected.clone(), hideResult: hideResult.clone(), isEncrypted: encrypted.clone(), inputIndex: None, initNonlinear: dlowVar.initNonlinear.clone(), matrixName: None, variability: Some(variability.clone()), initial_: Some(initial_.clone()), exportVar: None, relativeQuantity: relativeQuantity.clone() }
        },
        (BackendDAE::Var { encrypted: mut encrypted, source: mut source, varType: ref tp, comment: mut comment, hideResult: mut hideResultExp, values: mut dae_var_attr, arryDim: ref inst_dims, varKind: mut kind @ BackendDAE::VarKind::PARAM { .. }, varName: ref cr, .. }, mut vars) => {
            let mut numArrayElement: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            let mut commentStr: ArcStr = arcstr::literal!("");
            let mut unit: ArcStr = arcstr::literal!("");
            let mut displayUnit: ArcStr = arcstr::literal!("");
            let mut minValue: Option<Arc<DAE::Exp>> = None;
            let mut maxValue: Option<Arc<DAE::Exp>> = None;
            let mut initVal: Option<Arc<DAE::Exp>> = None;
            let mut nomVal: Option<Arc<DAE::Exp>> = None;
            let mut isFixed: bool = false;
            let mut type_: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut isDiscrete: bool = false;
            let mut isValueChangeable: bool = false;
            let mut arrayCref: Option<Arc<DAE::ComponentRef>> = None;
            let mut aliasvar: SimCodeVar::AliasVariable = SimCodeVar::AliasVariable::NOALIAS;
            let mut caus: SimCodeVar::Causality = SimCodeVar::Causality::CALCULATED_PARAMETER;
            let mut initial_: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
            let mut variability: SimCodeVar::Variability = SimCodeVar::Variability::CONSTANT;
            let mut isProtected: bool = false;
            let mut hideResult: Option<bool> = None;
            let mut relativeQuantity: bool = false;
            commentStr = (unparseCommentOptionNoAnnotationNoQuote(comment.clone())).clone();
            (unit, displayUnit) = extractVarUnit(dae_var_attr.clone())?;
            isProtected = BackendVariable::isProtected(dlowVar.clone());
            hideResult = getHideResult(hideResultExp.clone(), cr.clone(), source.clone())?;
            (minValue, maxValue) = getMinMaxValues(dlowVar.clone())?;
            initVal = getStartValue(dlowVar.clone())?;
            nomVal = getNominalValue(dlowVar.clone())?;
            isFixed = BackendVariable::varFixed(dlowVar.clone());
            type_ = tp.clone();
            isDiscrete = BackendVariable::isVarDiscrete(dlowVar.clone());
            arrayCref = ComponentReference::getArrayCref(cr.clone())?;
            aliasvar = getAliasVar(dlowVar.clone(), optAliasVars.clone())?;
            numArrayElement = List::map(inst_dims.clone(), (std::sync::Arc::new(ExpressionDump::dimensionIntString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Dimension>) -> Result<ArcStr> + 'static>))?;
            isValueChangeable = BackendVariable::isChangeable(dlowVar.clone())?;
            caus = getCausality(dlowVar.clone(), vars.clone(), isValueChangeable.clone())?;
            variability = openmodelica_simcode_types::SimCodeVar::Variability::FIXED;
            initial_ = setInitialAttribute(dlowVar.clone(), variability.clone(), caus.clone(), isFixed.clone(), iterationVars.clone(), aliasvar.clone(), vars.clone())?;
            initVal = updateStartValue(dlowVar.clone(), initVal.clone(), initial_.clone(), caus.clone())?;
            if BackendVariable::isRealVar(dlowVar.clone()) {
                relativeQuantity = SCodeUtil::optCommentHasBooleanNamedAnnotationFalse(comment.clone(), (literal!("absoluteValue")).clone())?;
            } else {
                relativeQuantity = false;
            }
            SimCodeVar::SimVar { name: cr.clone(), varKind: kind.clone(), comment: (commentStr.clone()).clone(), unit: (unit.clone()).clone(), displayUnit: (displayUnit.clone()).clone(), index: -1, minValue: minValue.clone(), maxValue: maxValue.clone(), initialValue: initVal.clone(), nominalValue: nomVal.clone(), isFixed: isFixed.clone(), type_: type_.clone(), isDiscrete: isDiscrete.clone(), arrayCref: arrayCref.clone(), aliasvar: aliasvar.clone(), source: source.clone(), causality: Some(caus.clone()), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: isValueChangeable.clone(), isProtected: isProtected.clone(), hideResult: hideResult.clone(), isEncrypted: encrypted.clone(), inputIndex: None, initNonlinear: dlowVar.initNonlinear.clone(), matrixName: None, variability: Some(variability.clone()), initial_: Some(initial_.clone()), exportVar: Some(cr.clone()), relativeQuantity: relativeQuantity.clone() }
        },
        (BackendDAE::Var { encrypted: mut encrypted, source: mut source, varType: ref tp, comment: mut comment, hideResult: mut hideResultExp, values: mut dae_var_attr, arryDim: ref inst_dims, varKind: mut kind @ BackendDAE::VarKind::STATE { .. }, varName: ref cr, .. }, mut vars) => {
            let mut numArrayElement: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            let mut commentStr: ArcStr = arcstr::literal!("");
            let mut unit: ArcStr = arcstr::literal!("");
            let mut displayUnit: ArcStr = arcstr::literal!("");
            let mut minValue: Option<Arc<DAE::Exp>> = None;
            let mut maxValue: Option<Arc<DAE::Exp>> = None;
            let mut initVal: Option<Arc<DAE::Exp>> = None;
            let mut nomVal: Option<Arc<DAE::Exp>> = None;
            let mut isFixed: bool = false;
            let mut type_: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut isDiscrete: bool = false;
            let mut isValueChangeable: bool = false;
            let mut arrayCref: Option<Arc<DAE::ComponentRef>> = None;
            let mut aliasvar: SimCodeVar::AliasVariable = SimCodeVar::AliasVariable::NOALIAS;
            let mut caus: SimCodeVar::Causality = SimCodeVar::Causality::CALCULATED_PARAMETER;
            let mut initial_: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
            let mut variability: SimCodeVar::Variability = SimCodeVar::Variability::CONSTANT;
            let mut isProtected: bool = false;
            let mut hideResult: Option<bool> = None;
            let mut relativeQuantity: bool = false;
            let () = (match BackendVariable::varStateSelect(dlowVar.clone()) {
        DAE::StateSelect::NEVER { .. } if (BackendVariable::isNaturalState(dlowVar.clone())) => {
            Error::addSourceMessage(Error::STATE_STATESELECT_NEVER.clone(), list![(ComponentReferenceBasics::printComponentRefStr(cr.clone())?).clone()], source.info.clone())?;
            bail!("fail");
            ()
        },
        _ => (),
    });
            commentStr = (unparseCommentOptionNoAnnotationNoQuote(comment.clone())).clone();
            (unit, displayUnit) = extractVarUnit(dae_var_attr.clone())?;
            isProtected = BackendVariable::isProtected(dlowVar.clone());
            hideResult = getHideResult(hideResultExp.clone(), cr.clone(), source.clone())?;
            (minValue, maxValue) = getMinMaxValues(dlowVar.clone())?;
            initVal = getStartValue(dlowVar.clone())?;
            nomVal = getNominalValue(dlowVar.clone())?;
            isFixed = BackendVariable::varFixed(dlowVar.clone());
            type_ = tp.clone();
            isDiscrete = BackendVariable::isVarDiscrete(dlowVar.clone());
            arrayCref = ComponentReference::getArrayCref(cr.clone())?;
            aliasvar = getAliasVar(dlowVar.clone(), optAliasVars.clone())?;
            numArrayElement = List::map(inst_dims.clone(), (std::sync::Arc::new(ExpressionDump::dimensionIntString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Dimension>) -> Result<ArcStr> + 'static>))?;
            isValueChangeable = BackendVariable::isChangeable(dlowVar.clone())?;
            caus = getCausality(dlowVar.clone(), vars.clone(), isValueChangeable.clone())?;
            variability = openmodelica_simcode_types::SimCodeVar::Variability::CONTINUOUS;
            initial_ = setInitialAttribute(dlowVar.clone(), variability.clone(), caus.clone(), isFixed.clone(), iterationVars.clone(), aliasvar.clone(), vars.clone())?;
            initVal = updateStartValue(dlowVar.clone(), initVal.clone(), initial_.clone(), caus.clone())?;
            if BackendVariable::isRealVar(dlowVar.clone()) {
                relativeQuantity = SCodeUtil::optCommentHasBooleanNamedAnnotationFalse(comment.clone(), (literal!("absoluteValue")).clone())?;
            } else {
                relativeQuantity = false;
            }
            SimCodeVar::SimVar { name: cr.clone(), varKind: kind.clone(), comment: (commentStr.clone()).clone(), unit: (unit.clone()).clone(), displayUnit: (displayUnit.clone()).clone(), index: -1, minValue: minValue.clone(), maxValue: maxValue.clone(), initialValue: initVal.clone(), nominalValue: nomVal.clone(), isFixed: isFixed.clone(), type_: type_.clone(), isDiscrete: isDiscrete.clone(), arrayCref: arrayCref.clone(), aliasvar: aliasvar.clone(), source: source.clone(), causality: Some(caus.clone()), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: isValueChangeable.clone(), isProtected: isProtected.clone(), hideResult: hideResult.clone(), isEncrypted: encrypted.clone(), inputIndex: None, initNonlinear: dlowVar.initNonlinear.clone(), matrixName: None, variability: Some(variability.clone()), initial_: Some(initial_.clone()), exportVar: Some(cr.clone()), relativeQuantity: relativeQuantity.clone() }
        },
        (BackendDAE::Var { encrypted: mut encrypted, source: mut source, varType: ref tp, comment: mut comment, hideResult: mut hideResultExp, values: mut dae_var_attr, arryDim: ref inst_dims, varKind: mut kind, varName: ref cr, .. }, mut vars) => {
            let mut numArrayElement: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            let mut commentStr: ArcStr = arcstr::literal!("");
            let mut unit: ArcStr = arcstr::literal!("");
            let mut displayUnit: ArcStr = arcstr::literal!("");
            let mut minValue: Option<Arc<DAE::Exp>> = None;
            let mut maxValue: Option<Arc<DAE::Exp>> = None;
            let mut initVal: Option<Arc<DAE::Exp>> = None;
            let mut nomVal: Option<Arc<DAE::Exp>> = None;
            let mut isFixed: bool = false;
            let mut type_: Arc<DAE::Type> = Arc::new(DAE::Type::T_NORETCALL);
            let mut isDiscrete: bool = false;
            let mut isValueChangeable: bool = false;
            let mut arrayCref: Option<Arc<DAE::ComponentRef>> = None;
            let mut aliasvar: SimCodeVar::AliasVariable = SimCodeVar::AliasVariable::NOALIAS;
            let mut caus: SimCodeVar::Causality = SimCodeVar::Causality::CALCULATED_PARAMETER;
            let mut initial_: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
            let mut variability: SimCodeVar::Variability = SimCodeVar::Variability::CONSTANT;
            let mut isProtected: bool = false;
            let mut hideResult: Option<bool> = None;
            let mut relativeQuantity: bool = false;
            let () = (match BackendVariable::varStateSelect(dlowVar.clone()) {
        DAE::StateSelect::ALWAYS { .. } if (kind.clone() == openmodelica_backend_types::BackendDAE::VarKind::VARIABLE && !(ComponentReference::isPreviousCref(cr.clone()))) => {
            Error::addSourceMessage(Error::NON_STATE_STATESELECT_ALWAYS.clone(), list![(ComponentReferenceBasics::printComponentRefStr(cr.clone())?).clone()], source.info.clone())?;
            ()
        },
        _ => (),
    });
            commentStr = (unparseCommentOptionNoAnnotationNoQuote(comment.clone())).clone();
            (unit, displayUnit) = extractVarUnit(dae_var_attr.clone())?;
            isProtected = BackendVariable::isProtected(dlowVar.clone());
            hideResult = getHideResult(hideResultExp.clone(), cr.clone(), source.clone())?;
            (minValue, maxValue) = getMinMaxValues(dlowVar.clone())?;
            initVal = getStartValue(dlowVar.clone())?;
            nomVal = getNominalValue(dlowVar.clone())?;
            isFixed = BackendVariable::varFixed(dlowVar.clone());
            type_ = tp.clone();
            isDiscrete = BackendVariable::isVarDiscrete(dlowVar.clone());
            arrayCref = ComponentReference::getArrayCref(cr.clone())?;
            aliasvar = getAliasVar(dlowVar.clone(), optAliasVars.clone())?;
            numArrayElement = List::map(inst_dims.clone(), (std::sync::Arc::new(ExpressionDump::dimensionIntString) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Dimension>) -> Result<ArcStr> + 'static>))?;
            isValueChangeable = BackendVariable::isChangeable(dlowVar.clone())?;
            caus = getCausality(dlowVar.clone(), vars.clone(), isValueChangeable.clone())?;
            variability = getVariabilityAttribute(dlowVar.clone());
            initial_ = setInitialAttribute(dlowVar.clone(), variability.clone(), caus.clone(), isFixed.clone(), iterationVars.clone(), aliasvar.clone(), vars.clone())?;
            initVal = updateStartValue(dlowVar.clone(), initVal.clone(), initial_.clone(), caus.clone())?;
            if BackendVariable::isRealVar(dlowVar.clone()) {
                relativeQuantity = SCodeUtil::optCommentHasBooleanNamedAnnotationFalse(comment.clone(), (literal!("absoluteValue")).clone())?;
            } else {
                relativeQuantity = false;
            }
            SimCodeVar::SimVar { name: cr.clone(), varKind: kind.clone(), comment: (commentStr.clone()).clone(), unit: (unit.clone()).clone(), displayUnit: (displayUnit.clone()).clone(), index: -1, minValue: minValue.clone(), maxValue: maxValue.clone(), initialValue: initVal.clone(), nominalValue: nomVal.clone(), isFixed: isFixed.clone(), type_: type_.clone(), isDiscrete: isDiscrete.clone(), arrayCref: arrayCref.clone(), aliasvar: aliasvar.clone(), source: source.clone(), causality: Some(caus.clone()), variable_index: None, fmi_index: None, numArrayElement: numArrayElement.clone(), isValueChangeable: isValueChangeable.clone(), isProtected: isProtected.clone(), hideResult: hideResult.clone(), isEncrypted: encrypted.clone(), inputIndex: None, initNonlinear: dlowVar.initNonlinear.clone(), matrixName: None, variability: Some(variability.clone()), initial_: Some(initial_.clone()), exportVar: Some(cr.clone()), relativeQuantity: relativeQuantity.clone() }
        },
    });
    Ok(simVar)
}

fn getCausality(mut dlowVar: BackendDAE::Var, mut inVars: BackendDAE::Variables, mut isValueChangeable: bool) -> Result<SimCodeVar::Causality> {
    let mut caus: SimCodeVar::Causality = SimCodeVar::Causality::CALCULATED_PARAMETER;
    caus = 'mc: {
        let __mc_input = (dlowVar.clone(), inVars.clone(), isValueChangeable.clone());
        if let Ok(__v) = (|| -> Result<_> {
            let (BackendDAE::Var { varDirection: DAE::VarDirection::OUTPUT { .. }, .. }, _, _) = __mc_input.clone() else { bail!("nomatch") };
            Ok(openmodelica_simcode_types::SimCodeVar::Causality::OUTPUT)
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let (BackendDAE::Var { varDirection: DAE::VarDirection::INPUT { .. }, varName: ref cr, .. }, mut globalKnownVars, _) = __mc_input.clone() else { bail!("nomatch") };
            BackendVariable::getVar(cr.clone(), globalKnownVars.clone())?;
            Ok(openmodelica_simcode_types::SimCodeVar::Causality::INPUT)
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let (BackendDAE::Var { varKind: BackendDAE::VarKind::PARAM { .. }, .. }, _, true) = __mc_input.clone() else { bail!("nomatch") };
            Ok(openmodelica_simcode_types::SimCodeVar::Causality::PARAMETER)
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let (BackendDAE::Var { varKind: BackendDAE::VarKind::PARAM { .. }, .. }, _, false) = __mc_input.clone() else { bail!("nomatch") };
            Ok(openmodelica_simcode_types::SimCodeVar::Causality::CALCULATED_PARAMETER)
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let _ = __mc_input.clone() else { bail!("nomatch") };
            Ok(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL)
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(caus)
}

fn setInitialAttribute(mut dlow: BackendDAE::Var, mut variability: SimCodeVar::Variability, mut causality: SimCodeVar::Causality, mut isFixed: bool, mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>, mut aliasvar: SimCodeVar::AliasVariable, mut globalknownVars: BackendDAE::Variables) -> Result<SimCodeVar::Initial> {
    let mut initial_: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
    initial_ = (match (variability.clone(), causality.clone()) {
        (SimCodeVar::Variability::CONSTANT { .. }, SimCodeVar::Causality::OUTPUT { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::EXACT,
        (SimCodeVar::Variability::CONSTANT { .. }, SimCodeVar::Causality::LOCAL { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::EXACT,
        (SimCodeVar::Variability::FIXED { .. }, SimCodeVar::Causality::PARAMETER { .. }) => openmodelica_simcode_types::SimCodeVar::Initial::EXACT,
        (SimCodeVar::Variability::FIXED { .. }, SimCodeVar::Causality::CALCULATED_PARAMETER { .. }) => getInitialAttributeHelperForParameters(dlow.clone(), isFixed.clone(), iterationVars.clone())?,
        (SimCodeVar::Variability::FIXED { .. }, SimCodeVar::Causality::LOCAL { .. }) => getInitialAttributeHelperForParameters(dlow.clone(), isFixed.clone(), iterationVars.clone())?,
        (SimCodeVar::Variability::DISCRETE { .. }, SimCodeVar::Causality::OUTPUT { .. }) => getInitialAttributeHelper(dlow.clone(), isFixed.clone(), iterationVars.clone())?,
        (SimCodeVar::Variability::DISCRETE { .. }, SimCodeVar::Causality::LOCAL { .. }) => getInitialAttributeHelper(dlow.clone(), isFixed.clone(), iterationVars.clone())?,
        (SimCodeVar::Variability::CONTINUOUS { .. }, SimCodeVar::Causality::OUTPUT { .. }) => getInitialAttributeHelper(dlow.clone(), isFixed.clone(), iterationVars.clone())?,
        (SimCodeVar::Variability::CONTINUOUS { .. }, SimCodeVar::Causality::LOCAL { .. }) => getInitialAttributeHelper(dlow.clone(), isFixed.clone(), iterationVars.clone())?,
        (_, _) => openmodelica_simcode_types::SimCodeVar::Initial::NONE_INITIAL,
    });
    if initial_.clone() == openmodelica_simcode_types::SimCodeVar::Initial::EXACT && !(aliasvar.clone() == openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS) {
        initial_ = openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED;
    }
    Ok(initial_)
}

fn getInitialAttributeHelperForParameters(mut var: BackendDAE::Var, mut isFixed: bool, mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>) -> Result<SimCodeVar::Initial> {
    let mut initial_: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
    if !(isFixed.clone()) && isSome(iterationVars.clone()) && UnorderedSet::contains(var.varName.clone(), Util::getOption(iterationVars.clone())?)? && startValueIsConstOrNone(var.clone())? {
        initial_ = openmodelica_simcode_types::SimCodeVar::Initial::APPROX;
    } else {
        initial_ = openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED;
    }
    Ok(initial_)
}

fn startValueIsConstOrNone(mut var: BackendDAE::Var) -> Result<bool> {
    let mut b: bool = false;
    let mut start_value: Option<Arc<DAE::Exp>> = getStartValue(var.clone())?;
    if isNone(start_value.clone()) && isNone(var.bindExp.clone()) {
        b = true;
    } else if isNone(start_value.clone()) && isSome(var.bindExp.clone()) {
        b = Expression::isEvaluatedConst(Util::getOption(var.bindExp.clone())?);
    } else {
        b = Expression::isEvaluatedConst(Util::getOption(start_value.clone())?);
    }
    Ok(b)
}

fn getInitialAttributeHelper(mut var: BackendDAE::Var, mut isFixed: bool, mut iterationVars: Option<Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>>) -> Result<SimCodeVar::Initial> {
    let mut initial_: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
    if isFixed.clone() && startValueIsConstOrNone(var.clone())? {
        initial_ = openmodelica_simcode_types::SimCodeVar::Initial::EXACT;
    } else if isSome(iterationVars.clone()) && UnorderedSet::contains(var.varName.clone(), Util::getOption(iterationVars.clone())?)? && startValueIsConstOrNone(var.clone())? {
        initial_ = openmodelica_simcode_types::SimCodeVar::Initial::APPROX;
    } else {
        initial_ = openmodelica_simcode_types::SimCodeVar::Initial::CALCULATED;
    }
    Ok(initial_)
}

fn getVariabilityAttribute(mut var: BackendDAE::Var) -> SimCodeVar::Variability {
    let mut variability: SimCodeVar::Variability = SimCodeVar::Variability::CONSTANT;
    if BackendVariable::isParam(var.clone()) {
        variability = openmodelica_simcode_types::SimCodeVar::Variability::FIXED;
    } else if BackendVariable::isConst(var.clone()) {
        variability = openmodelica_simcode_types::SimCodeVar::Variability::CONSTANT;
    } else if BackendVariable::isVarDiscrete(var.clone()) || BackendVariable::isClockedStateVar(var.clone()) {
        variability = openmodelica_simcode_types::SimCodeVar::Variability::DISCRETE;
    } else {
        variability = openmodelica_simcode_types::SimCodeVar::Variability::CONTINUOUS;
    }
    variability
}

fn isInitialExactOrApprox(mut initial_: SimCodeVar::Initial) -> bool {
    let mut val: bool = false;
    val = (match initial_.clone() {
        SimCodeVar::Initial::EXACT { .. } => true,
        SimCodeVar::Initial::APPROX { .. } => true,
        _ => false,
    });
    val
}

fn isCausalityInput(mut causality: SimCodeVar::Causality) -> bool {
    let mut val: bool = false;
    val = (match causality.clone() {
        SimCodeVar::Causality::INPUT { .. } => true,
        _ => false,
    });
    val
}

fn setDefaultStartValue(mut type_: Arc<DAE::Type>) -> Result<Option<Arc<DAE::Exp>>> {
    let mut exp: Option<Arc<DAE::Exp>> = None;
    exp = (::match_deref::match_deref! { match &(type_.clone()) {
        Deref @ DAE::Type::T_INTEGER { .. } => Some(Arc::new(DAE::Exp::ICONST { integer: 0 })),
        Deref @ DAE::Type::T_REAL { .. } => Some(Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(0.0_f64) })),
        Deref @ DAE::Type::T_BOOL { .. } => Some(Arc::new(DAE::Exp::BCONST { bool: false })),
        Deref @ DAE::Type::T_STRING { .. } => Some(Arc::new(DAE::Exp::SCONST { string: (literal!("")).clone() })),
        Deref @ DAE::Type::T_ENUMERATION { .. } => Some(Types::getNthEnumLiteral(type_.clone(), 1)?),
        _ => None,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(exp)
}

fn startValueIsConstOrDefault(mut start_value: Option<Arc<DAE::Exp>>, mut type_: Arc<DAE::Type>) -> Result<Option<Arc<DAE::Exp>>> {
    let mut outstart_value: Option<Arc<DAE::Exp>> = None;
    if isNone(start_value.clone()) {
        outstart_value = None;
    } else if Expression::isConstValue(Util::getOption(start_value.clone())?)? {
        outstart_value = start_value.clone();
    } else {
        outstart_value = setDefaultStartValue(type_.clone())?;
    }
    Ok(outstart_value)
}

fn updateStartValue(mut var: BackendDAE::Var, mut startValue: Option<Arc<DAE::Exp>>, mut initial_: SimCodeVar::Initial, mut causality: SimCodeVar::Causality) -> Result<Option<Arc<DAE::Exp>>> {
    let mut startValue: Option<Arc<DAE::Exp>> = startValue;
    if Flags::getConfigBool(Flags::BUILDING_FMU.clone())? && FMI::isFMIVersion20((FMI::getFMIVersionString()?).clone())? {
        startValue = (::match_deref::match_deref! { match &(startValue.clone()) {
        Some(_) if (isInitialExactOrApprox(initial_.clone())) => startValue.clone(),
        None if (isInitialExactOrApprox(initial_.clone())) => setDefaultStartValue(var.varType.clone())?,
        Some(_) if (isCausalityInput(causality.clone())) => startValueIsConstOrDefault(startValue.clone(), var.varType.clone())?,
        None if (isCausalityInput(causality.clone())) => setDefaultStartValue(var.varType.clone())?,
        _ => startValueIsConstOrDefault(startValue.clone(), var.varType.clone())?,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    }
    Ok(startValue)
}

fn traversingdlowvarToSimvarFold(mut v: BackendDAE::Var, mut inTpl: (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)> {
    let mut outTpl: (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables) = (metamodelica::nil(), <BackendDAE::Variables as ::std::default::Default>::default());
    (_, outTpl) = traversingdlowvarToSimvar(v.clone(), inTpl.clone())?;
    Ok(outTpl)
}

pub fn traversingdlowvarToSimvar(mut var: BackendDAE::Var, mut inTpl: (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> {
    let mut var: BackendDAE::Var = var;
    let mut outTpl: (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables) = (metamodelica::nil(), <BackendDAE::Variables as ::std::default::Default>::default());
    let mut sv_lst: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut sv: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut vars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    (sv_lst, vars) = inTpl.clone();
    sv = dlowvarToSimvar(var.clone(), None, vars.clone(), None)?;
    outTpl = (metamodelica::cons(sv.clone(), sv_lst.clone()), vars.clone());
    Ok((var, outTpl))
}

pub fn getMatchingExpsList(mut inExps: Arc<metamodelica::List<Arc<DAE::Exp>>>, mut inFn: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>) -> Result<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>)> + 'static>) -> Result<Arc<metamodelica::List<Arc<DAE::Exp>>>> {
    pub type MatchFn = std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>) -> Result<(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<DAE::Exp>>>)> + 'static>;

    let mut outExpLst: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
    (_, outExpLst) = Expression::traverseExpList(inExps.clone(), inFn.clone(), metamodelica::nil())?;
    Ok(outExpLst)
}

fn extractVarUnit(mut var_attr: Option<Arc<DAE::VariableAttributes>>) -> Result<(ArcStr, ArcStr)> {
    let mut unitStr: ArcStr = arcstr::literal!("");
    let mut displayUnitStr: ArcStr = arcstr::literal!("");
    (unitStr, displayUnitStr) = 'mc: {
        let __mc_input = var_attr.clone();
        if let Ok((__v, __wb0, __wb1)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Some(Deref @ DAE::VariableAttributes::VAR_ATTR_REAL { displayUnit: duexp, unit: uexp, .. }) => {
                    let mut displayUnitStr: ArcStr = displayUnitStr.clone();
                    let mut unitStr: ArcStr = unitStr.clone();
                    unitStr = (extractVarUnitStr(uexp.clone())?).clone();
                    displayUnitStr = (extractVarUnitStr(duexp.clone())?).clone();
                    Ok(((unitStr.clone(), displayUnitStr.clone()), displayUnitStr.clone(), unitStr.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { displayUnitStr = __wb0; unitStr = __wb1; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((literal!(""), literal!("")))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((unitStr, displayUnitStr))
}

fn extractVarUnitStr(mut exp: Option<Arc<DAE::Exp>>) -> Result<ArcStr> {
    let mut r#str: ArcStr = arcstr::literal!("");
    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    if isSome(exp.clone()) {
        let __pa0 = ::match_deref::match_deref! { match &(exp.clone()) {
            Some(__pa0) => __pa0.clone(),
            _ => bail!("pattern mismatch"),
        } };
        e = __pa0.clone();
        e = Expression::arrayFirstScalar(e.clone())?;
        r#str = ((::match_deref::match_deref! { match &(e.clone()) {
        Deref @ DAE::Exp::SCONST { .. } => var_field!((*e).string, DAE::Exp::SCONST).clone(),
        _ => {
            Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Unexpected expression (should have been handled earlier, probably in the front-end. Unit/displayUnit expression is not a string literal: ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(e.clone())?); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })).clone();
    } else {
        r#str = (literal!("")).clone();
    }
    Ok(r#str)
}

fn getMinMaxValues(mut inDAELowVar: BackendDAE::Var) -> Result<(Option<Arc<DAE::Exp>>, Option<Arc<DAE::Exp>>)> {
    let mut outMinValue: Option<Arc<DAE::Exp>> = None;
    let mut outMaxValue: Option<Arc<DAE::Exp>> = None;
    (outMinValue, outMaxValue) = 'mc: {
        let __mc_input = inDAELowVar.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varType: Deref @ DAE::Type::T_REAL { .. }, .. } => {
                    let mut minValue: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let mut maxValue: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let (__pa0, __pa1) = ::match_deref::match_deref! { match &(DAEUtil::getMinMaxValues(dae_var_attr.clone())) {
                        (Some(__pa0), Some(__pa1)) => (__pa0.clone(), __pa1.clone()),
                        _ => bail!("pattern mismatch"),
                    } };
                    minValue = __pa0.clone();
                    maxValue = __pa1.clone();
                    Ok((Some(minValue.clone()), Some(maxValue.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varType: Deref @ DAE::Type::T_REAL { .. }, .. } => {
                    let mut minValue: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let __pa0 = ::match_deref::match_deref! { match &(DAEUtil::getMinMaxValues(dae_var_attr.clone())) {
                        (Some(__pa0), None) => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    minValue = __pa0.clone();
                    Ok((Some(minValue.clone()), None))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varType: Deref @ DAE::Type::T_REAL { .. }, .. } => {
                    let mut maxValue: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    let __pa0 = ::match_deref::match_deref! { match &(DAEUtil::getMinMaxValues(dae_var_attr.clone())) {
                        (None, Some(__pa0)) => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    maxValue = __pa0.clone();
                    Ok((None, Some(maxValue.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((None, None))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((outMinValue, outMaxValue))
}

fn getStartValue(mut daelowVar: BackendDAE::Var) -> Result<Option<Arc<DAE::Exp>>> {
    let mut initVal: Option<Arc<DAE::Exp>> = None;
    initVal = 'mc: {
        let __mc_input = daelowVar.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { tplExp: Some(_), varKind: BackendDAE::VarKind::PARAM { .. }, .. } => {
                    Ok(None)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varKind: BackendDAE::VarKind::VARIABLE { .. }, .. } => {
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getStartAttrFail(dae_var_attr.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varKind: BackendDAE::VarKind::DISCRETE { .. }, .. } => {
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getStartAttrFail(dae_var_attr.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varKind: BackendDAE::VarKind::STATE { .. }, .. } => {
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getStartAttrFail(dae_var_attr.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varKind: BackendDAE::VarKind::ALG_STATE { .. }, .. } => {
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getStartAttrFail(dae_var_attr.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varKind: BackendDAE::VarKind::DUMMY_DER { .. }, .. } => {
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getStartAttrFail(dae_var_attr.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varKind: BackendDAE::VarKind::DUMMY_STATE { .. }, .. } => {
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getStartAttrFail(dae_var_attr.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { bindExp: Some(e), varKind: BackendDAE::VarKind::PARAM { .. }, .. } => {
                    if !((!(stringEq((Config::simCodeTarget()?).clone(), (literal!("Cpp")).clone())))) { bail!("guard") }
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { varType: tp, values: dae_var_attr, varKind: BackendDAE::VarKind::PARAM { .. }, .. } => {
                    if !((!(stringEq((Config::simCodeTarget()?).clone(), (literal!("Cpp")).clone())))) { bail!("guard") }
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getStartAttr(dae_var_attr.clone(), tp.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { bindExp: Some(e), varKind: BackendDAE::VarKind::PARAM { .. }, .. } => {
                    if !((Expression::isConst(e.clone())? && stringEq((Config::simCodeTarget()?).clone(), (literal!("Cpp")).clone()))) { bail!("guard") }
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varKind: BackendDAE::VarKind::PARAM { .. }, .. } => {
                    if !((stringEq((Config::simCodeTarget()?).clone(), (literal!("Cpp")).clone()))) { bail!("guard") }
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getStartAttrFail(dae_var_attr.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { bindExp: Some(e), varKind: BackendDAE::VarKind::EXTOBJ { fullClassName: _ }, .. } => {
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, .. } => {
                    if !((BackendVariable::isVarNonDiscreteAlg(daelowVar.clone()))) { bail!("guard") }
                    Ok(Some(DAEUtil::getStartAttrFail(dae_var_attr.clone())?))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(None)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(initVal)
}

fn getNominalValue(mut daelowVar: BackendDAE::Var) -> Result<Option<Arc<DAE::Exp>>> {
    let mut nomVal: Option<Arc<DAE::Exp>> = None;
    nomVal = 'mc: {
        let __mc_input = daelowVar.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { values: dae_var_attr, varType: Deref @ DAE::Type::T_REAL { .. }, .. } => {
                    let mut e: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
                    e = DAEUtil::getNominalAttrFail(dae_var_attr.clone())?;
                    Ok(Some(e.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(None)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(nomVal)
}

pub fn functionInfo(mut r#fn: Arc<SimCodeFunction::Function::Function>) -> Result<SourceInfo> {
    let mut info: SourceInfo = <SourceInfo as ::std::default::Default>::default();
    info = (::match_deref::match_deref! { match &(r#fn.clone()) {
        Deref @ SimCodeFunction::Function::FUNCTION { info: __esc_info, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCodeFunction::Function::EXTERNAL_FUNCTION { info: __esc_info, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCodeFunction::Function::RECORD_CONSTRUCTOR { info: __esc_info, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(info)
}

pub fn functionPath(mut r#fn: Arc<SimCodeFunction::Function::Function>) -> Result<Arc<Absyn::Path>> {
    let mut name: Arc<Absyn::Path> = Arc::new(<Absyn::Path as ::std::default::Default>::default());
    name = (::match_deref::match_deref! { match &(r#fn.clone()) {
        Deref @ SimCodeFunction::Function::FUNCTION { name: __esc_name, .. } => {
            name = (*__esc_name).clone();
            name.clone()
        },
        Deref @ SimCodeFunction::Function::PARALLEL_FUNCTION { name: __esc_name, .. } => {
            name = (*__esc_name).clone();
            name.clone()
        },
        Deref @ SimCodeFunction::Function::KERNEL_FUNCTION { name: __esc_name, .. } => {
            name = (*__esc_name).clone();
            name.clone()
        },
        Deref @ SimCodeFunction::Function::EXTERNAL_FUNCTION { name: __esc_name, .. } => {
            name = (*__esc_name).clone();
            name.clone()
        },
        Deref @ SimCodeFunction::Function::RECORD_CONSTRUCTOR { name: __esc_name, .. } => {
            name = (*__esc_name).clone();
            name.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(name)
}

pub fn eqInfo(mut eq: Arc<SimCode::SimEqSystem>) -> Result<SourceInfo> {
    let mut info: SourceInfo = <SourceInfo as ::std::default::Default>::default();
    info = (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ SimCode::SimEqSystem::SES_RESIDUAL { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_FOR_RESIDUAL { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_RESIZABLE_ASSIGN { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_GENERIC_ASSIGN { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_ENTWINED_ASSIGN { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_WHEN { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_FOR_LOOP { source: Deref @ DAE::ElementSource { info: __esc_info, .. }, .. } => {
            info = (*__esc_info).clone();
            info.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(info)
}

pub fn simEqSystemIndex(mut eq: Arc<SimCode::SimEqSystem>) -> Result<i32> {
    let mut index: i32 = 0;
    index = (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ SimCode::SimEqSystem::SES_RESIDUAL { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_FOR_RESIDUAL { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_RESIZABLE_ASSIGN { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_GENERIC_ASSIGN { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_ENTWINED_ASSIGN { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_IFEQUATION { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_ALGORITHM { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_INVERSE_ALGORITHM { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { index: __esc_index, .. }, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { index: __esc_index, .. }, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_MIXED { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_WHEN { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_FOR_LOOP { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_ALIAS { index: __esc_index, .. } => {
            index = (*__esc_index).clone();
            index.clone()
        },
        _ => {
            Error::addMessage(Error::INTERNAL_ERROR.clone(), list![(literal!("SimCodeUtil.simEqSystemIndex failed")).clone()])?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(index)
}

fn adjustStatesForInlineSolver(mut inStates: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outStates: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    match '__try0: {
        for mut var in &*inStates.clone() {
            let mut var = var.clone();
            var.name = unwrap_break_err!(ComponentReference::appendStringLastIdent((literal!("$Old")).clone(), var.name.clone()), '__try0);
            var.varKind = openmodelica_backend_types::BackendDAE::VarKind::ALG_STATE_OLD;
            outStates = metamodelica::cons(var.clone(), outStates.clone());
        }
        Ok::<(), anyhow::Error>(())
    } {
        Ok(()) => {}
        Err(__try0_err) => {
            Error::addMessage(Error::INTERNAL_ERROR.clone(), list![(literal!("SimCodeUtil.adjustStatesForInlineSolver failed")).clone()])?;
            return Err(__try0_err);
        }
    }
    Ok(outStates)
}

/* *************************************/
/* ************ for index ***************/
fn setVariableDerIndex(mut inDlow: Arc<BackendDAE::BackendDAE>, mut inEqSystems: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>>) -> Result<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>> {
    let mut outOrder: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>> = metamodelica::nil();
    outOrder = 'mc: {
        let __mc_input = inEqSystems.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Nil => {
                    Ok(metamodelica::nil())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: syst, tail: systs } => {
                    let mut variableIndex: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>> = metamodelica::nil();
                    let mut variableIndex2: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>> = metamodelica::nil();
                    let mut variableIndex3: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>> = metamodelica::nil();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!(" set  variabale der index for eqsystem")); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    variableIndex = setVariableDerIndex2(inDlow.clone(), syst.clone())?;
                    variableIndex2 = setVariableDerIndex(inDlow.clone(), systs.clone())?;
                    variableIndex3 = listAppend(variableIndex.clone(), variableIndex2.clone());
                    Ok(variableIndex3.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    println!("{}", (literal!(" Failure in setVariableDerIndex\n")).clone());
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outOrder)
}

fn setVariableDerIndex2(mut inDlow: Arc<BackendDAE::BackendDAE>, mut syst: Arc<BackendDAE::EqSystem>) -> Result<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>> {
    let mut outOrder: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>> = metamodelica::nil();
    outOrder = 'mc: {
        let __mc_input = syst.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut dovars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
                    let mut deqns: Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> = <Arc<ExpandableArray::ExpandableArray<Arc<BackendDAE::Equation>>> as ::std::default::Default>::default();
                    let mut eqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut vars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
                    let mut derExps: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    let mut variableIndex: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>> = metamodelica::nil();
                    let mut firstOrderVars: Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>> = metamodelica::nil();
                    let mut firstOrderVarsFiltered: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!(" set variabale der index")); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    dovars = BackendVariable::daeVars(syst.clone());
                    deqns = BackendEquation::getEqnsFromEqSystem(syst.clone());
                    vars = BackendVariable::varList(dovars.clone())?;
                    eqns = BackendEquation::equationList(deqns.clone())?;
                    derExps = makeCallDerExp(vars.clone())?;
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!(" possible der exp: ")); __mm_s.push_str(&*stringDelimitList(List::map(derExps.clone(), (std::sync::Arc::new(ExpressionBasics::printExpStr) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<ArcStr> + 'static>))?, (literal!(", ")).clone())); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    eqns = flattenEqns(eqns.clone(), inDlow.clone())?;
                    (variableIndex, firstOrderVars) = List::map2_2(derExps.clone(), (std::sync::Arc::new(locateDerAndSerachOtherSide) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, Arc<metamodelica::List<Arc<BackendDAE::Equation>>>) -> Result<((Arc<DAE::ComponentRef>, i32), Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> + 'static>), eqns.clone(), eqns.clone())?;
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", (literal!("united variables\n")).clone());
                    }
                    firstOrderVarsFiltered = List::fold(firstOrderVars.clone(), std::sync::Arc::new(fnptr!(List::union, _, _)), metamodelica::nil())?;
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", (literal!("list fold variables\n")).clone());
                    }
                    variableIndex = setFirstOrderInSecondOrderVarIndex(variableIndex.clone(), firstOrderVarsFiltered.clone())?;
                    Ok(variableIndex.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    println!("{}", (literal!(" Failure in setVariableDerIndex2\n")).clone());
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outOrder)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn flattenEqns(mut eqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, mut dlow: Arc<BackendDAE::BackendDAE>) -> Result<Arc<metamodelica::List<Arc<BackendDAE::Equation>>>> {
    let mut oeqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
    oeqns = 'mc: {
        let __mc_input = eqns.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Nil => {
                    Ok(metamodelica::nil())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: eq @ Deref @ BackendDAE::Equation::EQUATION { .. }, tail: rest } => {
                    let mut rec: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    rec = flattenEqns(rest.clone(), dlow.clone())?;
                    rec = List::unionElt(eq.clone(), rec.clone());
                    Ok(rec.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: eq @ Deref @ BackendDAE::Equation::WHEN_EQUATION { .. }, tail: rest } => {
                    let mut rec: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut r#str: ArcStr = arcstr::literal!("");
                    r#str = (BackendDump::equationString(eq.clone())?).clone();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Found When eq ")); __mm_s.push_str(&*r#str.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    rec = flattenEqns(rest.clone(), dlow.clone())?;
                    Ok(rec.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: eq @ Deref @ BackendDAE::Equation::ALGORITHM { .. }, tail: rest } => {
                    let mut rec: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    rec = flattenEqns(rest.clone(), dlow.clone())?;
                    rec = List::unionElt(eq.clone(), rec.clone());
                    Ok(rec.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: eq @ Deref @ BackendDAE::Equation::ARRAY_EQUATION { .. }, tail: rest } => {
                    let mut rec: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    rec = flattenEqns(rest.clone(), dlow.clone())?;
                    rec = List::unionElt(eq.clone(), rec.clone());
                    Ok(rec.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: eq @ Deref @ BackendDAE::Equation::COMPLEX_EQUATION { .. }, tail: rest } => {
                    let mut rec: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    rec = flattenEqns(rest.clone(), dlow.clone())?;
                    rec = List::unionElt(eq.clone(), rec.clone());
                    Ok(rec.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: _, tail: _ } => {
                    let true = (Flags::isSet(Flags::FAILTRACE.clone())?) else { bail!("pattern mismatch") };
                    println!("{}", (literal!(" FAILURE IN flattenEqns possible unsupported equation...\n")).clone());
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(oeqns)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn makeCallDerExp(mut inVars: Arc<metamodelica::List<BackendDAE::Var>>) -> Result<Arc<metamodelica::List<Arc<DAE::Exp>>>> {
    let mut outDerExps: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
    outDerExps = 'mc: {
        let __mc_input = inVars.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Nil => {
                    Ok(metamodelica::nil())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: BackendDAE::Var { varName: cr, varKind: BackendDAE::VarKind::STATE { .. }, .. }, tail: vars } => {
                    let mut rec: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    rec = makeCallDerExp(vars.clone())?;
                    Ok(metamodelica::cons(Arc::new(DAE::Exp::CALL { path: Arc::new(Absyn::Path::IDENT { name: (literal!("der")).clone() }), expLst: list![Arc::new(DAE::Exp::CREF { componentRef: cr.clone(), ty: DAE::T_REAL_DEFAULT().clone() })], attr: DAE::callAttrBuiltinReal().clone() }), rec.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: _, tail: vars } => {
                    let mut rec: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
                    rec = makeCallDerExp(vars.clone())?;
                    Ok(rec.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outDerExps)
}

fn locateDerAndSerachOtherSide(mut derExp: Arc<DAE::Exp>, mut inEqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>, mut inEqnsOrg: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>) -> Result<((Arc<DAE::ComponentRef>, i32), Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> {
    let mut out: (Arc<DAE::ComponentRef>, i32) = (Arc::new(DAE::ComponentRef::WILD), 0);
    let mut sysOrdOneVars: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    (out, sysOrdOneVars) = 'mc: {
        let __mc_input = (derExp.clone(), inEqns.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::CREF { componentRef: cr, ty: _ }, tail: Deref @ metamodelica::List::Nil }, .. }, Deref @ metamodelica::List::Nil) => {
                    Ok(((cr.clone(), 0), metamodelica::nil()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: deriveVar @ Deref @ DAE::Exp::CREF { componentRef: cr, ty: _ }, tail: Deref @ metamodelica::List::Nil }, .. }, Deref @ metamodelica::List::Cons { head: eq @ Deref @ BackendDAE::Equation::EQUATION { scalar: e2, exp: e1, .. }, tail: eqs }) => {
                    let mut eqsOrg: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut rec: i32 = 0;
                    let mut i1: i32 = 0;
                    let mut highestIndex: (Arc<DAE::ComponentRef>, i32) = (Arc::new(DAE::ComponentRef::WILD), 0);
                    let true = (ExpressionBasics::expEqual(e1.clone(), derExp.clone())?) else { bail!("pattern mismatch") };
                    eqsOrg = List::removeOnTrue(eq.clone(), std::sync::Arc::new(fnptr!(valueEq, _, _)), inEqnsOrg.clone())?;
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nFound equation containing ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(derExp.clone())?); __mm_s.push_str(&*literal!(" Other side: ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(e2.clone())?); __mm_s.push_str(&*literal!(", extracted crefs: ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(deriveVar.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    (rec, crefs) = locateDerAndSerachOtherSide2(Arc::new(DAE::Exp::CALL { path: Arc::new(Absyn::Path::IDENT { name: (literal!("der")).clone() }), expLst: list![e2.clone()], attr: DAE::callAttrBuiltinReal().clone() }), eqsOrg.clone())?;
                    let (ref __pa1 @ (_, ref __pa0), _) = locateDerAndSerachOtherSide(derExp.clone(), eqs.clone(), eqsOrg.clone())?;
                    i1 = __pa0.clone();
                    highestIndex = __pa1.clone();
                    rec = rec.clone() + 1;
                    highestIndex = if (i1.clone() > rec.clone()) {highestIndex.clone()} else {(cr.clone(), rec.clone() - 1)};
                    Ok((highestIndex.clone(), crefs.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: deriveVar @ Deref @ DAE::Exp::CREF { componentRef: cr, ty: _ }, tail: Deref @ metamodelica::List::Nil }, .. }, Deref @ metamodelica::List::Cons { head: eq @ Deref @ BackendDAE::Equation::EQUATION { scalar: e2, exp: e1, .. }, tail: eqs }) => {
                    let mut eqsOrg: Arc<metamodelica::List<Arc<BackendDAE::Equation>>> = metamodelica::nil();
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut rec: i32 = 0;
                    let mut i1: i32 = 0;
                    let mut highestIndex: (Arc<DAE::ComponentRef>, i32) = (Arc::new(DAE::ComponentRef::WILD), 0);
                    let true = (ExpressionBasics::expEqual(e2.clone(), derExp.clone())?) else { bail!("pattern mismatch") };
                    eqsOrg = List::removeOnTrue(eq.clone(), std::sync::Arc::new(fnptr!(valueEq, _, _)), inEqnsOrg.clone())?;
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\nFound equation containing ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(derExp.clone())?); __mm_s.push_str(&*literal!(" Other side: ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(e1.clone())?); __mm_s.push_str(&*literal!(", extracted crefs: ")); __mm_s.push_str(&*ExpressionBasics::printExpStr(deriveVar.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                    }
                    (rec, crefs) = locateDerAndSerachOtherSide2(Arc::new(DAE::Exp::CALL { path: Arc::new(Absyn::Path::IDENT { name: (literal!("der")).clone() }), expLst: list![e1.clone()], attr: DAE::callAttrBuiltinReal().clone() }), eqsOrg.clone())?;
                    let (ref __pa1 @ (_, ref __pa0), _) = locateDerAndSerachOtherSide(derExp.clone(), eqs.clone(), eqsOrg.clone())?;
                    i1 = __pa0.clone();
                    highestIndex = __pa1.clone();
                    rec = rec.clone() + 1;
                    highestIndex = if (i1.clone() > rec.clone()) {highestIndex.clone()} else {(cr.clone(), rec.clone() - 1)};
                    Ok((highestIndex.clone(), crefs.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::EQUATION { scalar: e2, exp: e1, .. }, tail: eqs }) => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut highestIndex: (Arc<DAE::ComponentRef>, i32) = (Arc::new(DAE::ComponentRef::WILD), 0);
                    let false = (ExpressionBasics::expEqual(e1.clone(), derExp.clone())?) else { bail!("pattern mismatch") };
                    let false = (ExpressionBasics::expEqual(e2.clone(), derExp.clone())?) else { bail!("pattern mismatch") };
                    (highestIndex, crefs) = locateDerAndSerachOtherSide(derExp.clone(), eqs.clone(), inEqnsOrg.clone())?;
                    Ok((highestIndex.clone(), crefs.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::ARRAY_EQUATION { right: e2, left: e1, .. }, tail: eqs }) => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut highestIndex: (Arc<DAE::ComponentRef>, i32) = (Arc::new(DAE::ComponentRef::WILD), 0);
                    let false = (ExpressionBasics::expEqual(e1.clone(), derExp.clone())?) else { bail!("pattern mismatch") };
                    let false = (ExpressionBasics::expEqual(e2.clone(), derExp.clone())?) else { bail!("pattern mismatch") };
                    (highestIndex, crefs) = locateDerAndSerachOtherSide(derExp.clone(), eqs.clone(), inEqnsOrg.clone())?;
                    Ok((highestIndex.clone(), crefs.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::COMPLEX_EQUATION { right: e2, left: e1, .. }, tail: eqs }) => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut highestIndex: (Arc<DAE::ComponentRef>, i32) = (Arc::new(DAE::ComponentRef::WILD), 0);
                    let false = (ExpressionBasics::expEqual(e1.clone(), derExp.clone())?) else { bail!("pattern mismatch") };
                    let false = (ExpressionBasics::expEqual(e2.clone(), derExp.clone())?) else { bail!("pattern mismatch") };
                    (highestIndex, crefs) = locateDerAndSerachOtherSide(derExp.clone(), eqs.clone(), inEqnsOrg.clone())?;
                    Ok((highestIndex.clone(), crefs.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::IF_EQUATION { .. }, tail: eqs }) => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut highestIndex: (Arc<DAE::ComponentRef>, i32) = (Arc::new(DAE::ComponentRef::WILD), 0);
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", (literal!("\nFound  if equation is not supported yet  searching for varibale index\n")).clone());
                    }
                    (highestIndex, crefs) = locateDerAndSerachOtherSide(derExp.clone(), eqs.clone(), inEqnsOrg.clone())?;
                    Ok((highestIndex.clone(), crefs.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::ALGORITHM { .. }, tail: eqs }) => {
                    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut highestIndex: (Arc<DAE::ComponentRef>, i32) = (Arc::new(DAE::ComponentRef::WILD), 0);
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        println!("{}", (literal!("\nFound  algorithm is not supported yet  searching for varibale index\n")).clone());
                    }
                    (highestIndex, crefs) = locateDerAndSerachOtherSide(derExp.clone(), eqs.clone(), inEqnsOrg.clone())?;
                    Ok((highestIndex.clone(), crefs.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((out, sysOrdOneVars))
}

fn locateDerAndSerachOtherSide2(mut inDer: Arc<DAE::Exp>, mut inEqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>) -> Result<(i32, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> {
    let mut oi: i32 = 0;
    let mut firstOrderDers: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    (oi, firstOrderDers) = 'mc: {
        let __mc_input = inDer.clone();
        if let Ok((__v, __wb0, __wb1)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::CREF { componentRef: _, ty: _ }, tail: Deref @ metamodelica::List::Nil }, .. } => {
                    let mut firstOrderDers: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = firstOrderDers.clone();
                    let mut oi: i32 = oi.clone();
                    (oi, firstOrderDers) = locateDerAndSerachOtherSide22(inDer.clone(), inEqns.clone())?;
                    Ok(((oi.clone(), firstOrderDers.clone()), firstOrderDers.clone(), oi.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { firstOrderDers = __wb0; oi = __wb1; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok((0, metamodelica::nil()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((oi, firstOrderDers))
}

fn locateDerAndSerachOtherSide22(mut inDer: Arc<DAE::Exp>, mut inEqns: Arc<metamodelica::List<Arc<BackendDAE::Equation>>>) -> Result<(i32, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> {
    let mut oi: i32 = 0;
    let mut firstOrderDers: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    (oi, firstOrderDers) = 'mc: {
        let __mc_input = inEqns.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Nil => {
                    Ok((0, metamodelica::nil()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::EQUATION { scalar: e2, exp: e1, .. }, tail: _ } => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let true = (ExpressionBasics::expEqual(inDer.clone(), e1.clone())?) else { bail!("pattern mismatch") };
                    let __pa0 = ::match_deref::match_deref! { match &(Expression::extractCrefsFromExp(e1.clone())?) {
                        Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Nil } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    cr = __pa0.clone();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        BackendDump::debugStrExpStrExpStrExpStr((literal!(" found derivative for ")).clone(), inDer.clone(), (literal!(" in equation ")).clone(), e1.clone(), (literal!(" = ")).clone(), e2.clone(), (literal!("\n")).clone())?;
                    }
                    Ok((1, list![cr.clone()]))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::EQUATION { scalar: e2, exp: e1, .. }, tail: _ } => {
                    let mut cr: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    let true = (ExpressionBasics::expEqual(inDer.clone(), e2.clone())?) else { bail!("pattern mismatch") };
                    let __pa0 = ::match_deref::match_deref! { match &(Expression::extractCrefsFromExp(e2.clone())?) {
                        Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Nil } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    cr = __pa0.clone();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        BackendDump::debugStrExpStrExpStrExpStr((literal!(" found derivative for ")).clone(), inDer.clone(), (literal!(" in equation ")).clone(), e1.clone(), (literal!(" = ")).clone(), e2.clone(), (literal!("\n")).clone())?;
                    }
                    Ok((1, list![cr.clone()]))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok((__v, __wb0, __wb1)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: Deref @ BackendDAE::Equation::EQUATION { scalar: e2, exp: e1, .. }, tail: rest } => {
                    let mut firstOrderDers: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = firstOrderDers.clone();
                    let mut oi: i32 = oi.clone();
                    if Flags::isSet(Flags::FAILTRACE.clone())? {
                        BackendDump::debugExpStrExpStrExpStr(inDer.clone(), (literal!(" NOT contained in ")).clone(), e1.clone(), (literal!(" = ")).clone(), e2.clone(), (literal!("\n")).clone())?;
                    }
                    (oi, firstOrderDers) = locateDerAndSerachOtherSide22(inDer.clone(), rest.clone())?;
                    Ok(((oi.clone(), firstOrderDers.clone()), firstOrderDers.clone(), oi.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { firstOrderDers = __wb0; oi = __wb1; break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((oi, firstOrderDers))
}

fn setFirstOrderInSecondOrderVarIndex(mut inRefs: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>, mut firstOrderInSec: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>> {
    let mut outRefs: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>> = metamodelica::nil();
    outRefs = 'mc: {
        let __mc_input = inRefs.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Nil => {
                    Ok(metamodelica::nil())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: (cr, _), tail: rest } => {
                    let mut rest = (*rest).clone();
                    let true = (List::any(firstOrderInSec.clone(), (std::sync::Arc::new({ let __pe_b1 = cr.clone(); move |__pe_a0| ComponentReferenceBasics::crefEqual(__pe_a0, __pe_b1.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    rest = setFirstOrderInSecondOrderVarIndex(rest.clone(), firstOrderInSec.clone())?;
                    Ok(metamodelica::cons((cr.clone(), 2), rest.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: (cr, 1), tail: rest } => {
                    let mut rest = (*rest).clone();
                    rest = setFirstOrderInSecondOrderVarIndex(rest.clone(), firstOrderInSec.clone())?;
                    Ok(metamodelica::cons((cr.clone(), 1), rest.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: (cr, idx), tail: rest } => {
                    let mut rest = (*rest).clone();
                    rest = setFirstOrderInSecondOrderVarIndex(rest.clone(), firstOrderInSec.clone())?;
                    Ok(metamodelica::cons((cr.clone(), idx.clone()), rest.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outRefs)
}

/* ******** for dimension *******/
fn calculateVariableDimensions(mut in_vars: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>, mut inNvar1: i32, mut inNvar2: i32) -> (i32, i32) {
    let mut OutInteger1: i32 = 0;
    let mut OutInteger2: i32 = 0;
    (OutInteger1, OutInteger2) = (::match_deref::match_deref! { match &(in_vars.clone()) {
        Deref @ metamodelica::List::Nil => {
            (inNvar1.clone(), inNvar2.clone())
        },
        Deref @ metamodelica::List::Cons { head: (_, 0), tail: rest } => {
            calculateVariableDimensions(rest.clone(), inNvar1.clone() + 1, inNvar2.clone())
        },
        Deref @ metamodelica::List::Cons { head: (_, _), tail: rest } => {
            calculateVariableDimensions(rest.clone(), inNvar1.clone(), inNvar2.clone() + 1)
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    (OutInteger1, OutInteger2)
}

/* *******************/
fn dimensions(mut dae_low: Arc<BackendDAE::BackendDAE>) -> Result<(i32, i32)> {
    let mut OutInteger1: i32 = 0;
    let mut OutInteger2: i32 = 0;
    (OutInteger1, OutInteger2) = 'mc: {
        let __mc_input = dae_low.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ BackendDAE::BackendDAE { eqs: eqsystems, .. } => {
                    let mut nvar1: i32 = 0;
                    let mut nvar2: i32 = 0;
                    let mut ordered_states: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>> = metamodelica::nil();
                    ordered_states = setVariableDerIndex(dae_low.clone(), eqsystems.clone())?;
                    (nvar1, nvar2) = calculateVariableDimensions(ordered_states.clone(), 0, 0);
                    Ok((nvar1.clone(), nvar2.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    println!("{}", (literal!(" failure in dimensions\n")).clone());
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok((OutInteger1, OutInteger2))
}

/* *****************/
/* *****************/
fn compareSimVarName(mut var1: SimCodeVar::SimVar, mut var2: SimCodeVar::SimVar) -> Result<bool> {
    let mut result: bool = false;
    let mut name1: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut name2: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let SimCodeVar::SIMVAR { name: __pa0, .. } = (var1.clone()) else { bail!("pattern mismatch") };
    name1 = __pa0.clone();
    let SimCodeVar::SIMVAR { name: __pa1, .. } = (var2.clone()) else { bail!("pattern mismatch") };
    name2 = __pa1.clone();
    result = ComponentReferenceBasics::crefEqual(name1.clone(), name2.clone())?;
    Ok(result)
}

pub fn compareVarIndexGt(mut var1: SimCodeVar::SimVar, mut var2: SimCodeVar::SimVar) -> Result<bool> {
    let mut result: bool = false;
    let mut index1: i32 = 0;
    let mut index2: i32 = 0;
    let __pa0 = ::match_deref::match_deref! { match &(var1.clone()) {
        SimCodeVar::SimVar { variable_index: Some(__pa0), .. } => __pa0.clone(),
        _ => bail!("pattern mismatch"),
    } };
    index1 = __pa0.clone();
    let __pa1 = ::match_deref::match_deref! { match &(var2.clone()) {
        SimCodeVar::SimVar { variable_index: Some(__pa1), .. } => __pa1.clone(),
        _ => bail!("pattern mismatch"),
    } };
    index2 = __pa1.clone();
    result = index1.clone() > index2.clone();
    Ok(result)
}

pub fn compareSimVarTupleIndexGt<anyT: Clone + 'static>(mut var1: (SimCodeVar::SimVar, anyT), mut var2: (SimCodeVar::SimVar, anyT)) -> Result<bool> {
    let mut result: bool = false;
    let mut index1: i32 = 0;
    let mut index2: i32 = 0;
    let __pa0 = ::match_deref::match_deref! { match &(var1.clone()) {
        (SimCodeVar::SimVar { variable_index: Some(__pa0), .. }, _) => __pa0.clone(),
        _ => bail!("pattern mismatch"),
    } };
    index1 = __pa0.clone();
    let __pa1 = ::match_deref::match_deref! { match &(var2.clone()) {
        (SimCodeVar::SimVar { variable_index: Some(__pa1), .. }, _) => __pa1.clone(),
        _ => bail!("pattern mismatch"),
    } };
    index2 = __pa1.clone();
    result = index1.clone() > index2.clone();
    Ok(result)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
pub fn countDynamicExternalFunctions(mut inFncLst: Arc<metamodelica::List<Arc<SimCodeFunction::Function::Function>>>) -> Result<i32> {
    let mut outDynLoadFuncs: i32 = 0;
    outDynLoadFuncs = 'mc: {
        let __mc_input = inFncLst.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Nil => {
                    Ok(0)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::EXTERNAL_FUNCTION { dynamicLoad: true, .. }, tail: rest } => {
                    let mut i: i32 = 0;
                    i = countDynamicExternalFunctions(rest.clone())?;
                    Ok(intAdd(i.clone(), 1))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                Deref @ metamodelica::List::Cons { head: _, tail: rest } => {
                    let mut i: i32 = 0;
                    i = countDynamicExternalFunctions(rest.clone())?;
                    Ok(i.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outDynLoadFuncs)
}

fn getFilesFromSimVar(mut var: SimCodeVar::SimVar, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> (SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>) {
    let mut var: SimCodeVar::SimVar = var;
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = getFilesFromDAEElementSource(var.source.clone(), inFiles.clone());
    (var, outFiles)
}

fn getFilesFromSimVars(mut inSimVars: SimCodeVar::SimVars, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((inSimVars.clone(), inFiles.clone())) {
        (SimCodeVar::SimVars { stateVars, derivativeVars, algVars, discreteAlgVars, intAlgVars, boolAlgVars, inputVars, outputVars, aliasVars, intAliasVars, boolAliasVars, paramVars, intParamVars, boolParamVars, stringAlgVars, stringParamVars, stringAliasVars, extObjVars, constVars, intConstVars, boolConstVars, stringConstVars, jacobianVars, seedVars, realOptimizeConstraintsVars, realOptimizeFinalConstraintsVars, sensitivityVars, .. }, files) => {
            let mut files = (*files).clone();
            (_, files) = List::mapFoldList(list![stateVars.clone(), derivativeVars.clone(), algVars.clone(), discreteAlgVars.clone(), intAlgVars.clone(), boolAlgVars.clone(), inputVars.clone(), outputVars.clone(), aliasVars.clone(), intAliasVars.clone(), boolAliasVars.clone(), paramVars.clone(), intParamVars.clone(), boolParamVars.clone(), stringAlgVars.clone(), stringParamVars.clone(), stringAliasVars.clone(), extObjVars.clone(), constVars.clone(), intConstVars.clone(), boolConstVars.clone(), stringConstVars.clone(), jacobianVars.clone(), seedVars.clone(), realOptimizeConstraintsVars.clone(), realOptimizeFinalConstraintsVars.clone(), sensitivityVars.clone()], (std::sync::Arc::new(fnptr!(getFilesFromSimVar, SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)> + 'static>), files.clone())?;
            files.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(outFiles)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn getFilesFromFunctions(mut functions: Arc<metamodelica::List<Arc<SimCodeFunction::Function::Function>>>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((functions.clone(), inFiles.clone())) {
        (Deref @ metamodelica::List::Nil, files) => {
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::FUNCTION { info, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromAbsynInfo(info.clone(), files.clone());
            files = getFilesFromFunctions(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::EXTERNAL_FUNCTION { info, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromAbsynInfo(info.clone(), files.clone());
            files = getFilesFromFunctions(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ SimCodeFunction::Function::RECORD_CONSTRUCTOR { info, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromAbsynInfo(info.clone(), files.clone());
            files = getFilesFromFunctions(rest.clone(), files.clone())?;
            files.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outFiles)
}

fn getFilesFromSimEqSystemOpt(mut inSimEqSystemOpt: Option<Arc<SimCode::SimEqSystem>>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((inSimEqSystemOpt.clone(), inFiles.clone())) {
        (None, files) => {
            files.clone()
        },
        (Some(sys), files) => {
            let mut files = (*files).clone();
            (_, files) = getFilesFromSimEqSystem(sys.clone(), files.clone())?;
            files.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outFiles)
}

fn getFilesFromSimEqSystem(mut inSimEqSystem: Arc<SimCode::SimEqSystem>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<(Arc<SimCode::SimEqSystem>, Arc<metamodelica::List<SimCode::FileInfo>>)> {
    let mut outSimEqSystem: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    (outSimEqSystem, outFiles) = (::match_deref::match_deref! { match &((inSimEqSystem.clone(), inFiles.clone())) {
        (Deref @ SimCode::SimEqSystem::SES_RESIDUAL { source, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_FOR_RESIDUAL { source, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { source, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { source, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { source, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { source, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_ALGORITHM { statements, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromStatements(statements.clone(), files.clone())?;
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_INVERSE_ALGORITHM { statements, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromStatements(statements.clone(), files.clone())?;
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { simJac, vars, .. }, .. }, files) => {
            let mut systems: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut files = (*files).clone();
            (_, files) = List::mapFold(vars.clone(), (std::sync::Arc::new(fnptr!(getFilesFromSimVar, SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)> + 'static>), files.clone())?;
            systems = List::map(simJac.clone(), std::sync::Arc::new(fnptr!(Util::tuple33, _)))?;
            files = getFilesFromSimEqSystems(list![systems.clone()], files.clone())?;
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { eqs: systems, .. }, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromSimEqSystems(list![systems.clone()], files.clone())?;
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_MIXED { discEqs: systems, discVars: vars, cont: system, .. }, files) => {
            let mut files = (*files).clone();
            (_, files) = List::mapFold(vars.clone(), (std::sync::Arc::new(fnptr!(getFilesFromSimVar, SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)> + 'static>), files.clone())?;
            files = getFilesFromSimEqSystems(list![metamodelica::cons(system.clone(), systems.clone())], files.clone())?;
            (inSimEqSystem.clone(), files.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_WHEN { elseWhen: systemOpt, whenStmtLst, source, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromWhenOperators(whenStmtLst.clone(), files.clone())?;
            files = getFilesFromSimEqSystemOpt(systemOpt.clone(), files.clone())?;
            (inSimEqSystem.clone(), files.clone())
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok((outSimEqSystem, outFiles))
}

fn getFilesFromSimEqSystems(mut inSimEqSystems: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    (_, outFiles) = List::mapFoldList(inSimEqSystems.clone(), (std::sync::Arc::new(getFilesFromSimEqSystem) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<(Arc<SimCode::SimEqSystem>, Arc<metamodelica::List<SimCode::FileInfo>>)> + 'static>), inFiles.clone())?;
    Ok(outFiles)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn getFilesFromStatementsElse(mut inElse: Arc<DAE::Else>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((inElse.clone(), inFiles.clone())) {
        (Deref @ DAE::Else::NOELSE { .. }, files) => {
            files.clone()
        },
        (Deref @ DAE::Else::ELSEIF { else_: elsePart, statementLst: stmts, .. }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromStatements(stmts.clone(), files.clone())?;
            files = getFilesFromStatementsElse(elsePart.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ DAE::Else::ELSE { statementLst: stmts }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromStatements(stmts.clone(), files.clone())?;
            files.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outFiles)
}

fn getFilesFromStatementsElseWhen(mut inStatementOpt: Option<Arc<DAE::Statement>>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((inStatementOpt.clone(), inFiles.clone())) {
        (None, files) => {
            files.clone()
        },
        (Some(stmt), files) => {
            getFilesFromStatements(list![stmt.clone()], files.clone())?
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outFiles)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn getFilesFromStatements(mut inStatements: Arc<metamodelica::List<Arc<DAE::Statement>>>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((inStatements.clone(), inFiles.clone())) {
        (Deref @ metamodelica::List::Nil, files) => {
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_ASSIGN { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_TUPLE_ASSIGN { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_ASSIGN_ARR { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_IF { else_: elsePart, statementLst: stmts, source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(stmts.clone(), files.clone())?;
            files = getFilesFromStatementsElse(elsePart.clone(), files.clone())?;
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_FOR { statementLst: stmts, source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(stmts.clone(), files.clone())?;
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_PARFOR { statementLst: stmts, source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(stmts.clone(), files.clone())?;
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_WHILE { statementLst: stmts, source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(stmts.clone(), files.clone())?;
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_WHEN { elseWhen, statementLst: stmts, source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(stmts.clone(), files.clone())?;
            files = getFilesFromStatementsElseWhen(elseWhen.clone(), files.clone())?;
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_ASSERT { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_TERMINATE { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_REINIT { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_NORETCALL { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_RETURN { source }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_BREAK { source }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_FAILURE { body: stmts, source }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromStatements(stmts.clone(), files.clone())?;
            files = getFilesFromStatements(rest.clone(), files.clone())?;
            files.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outFiles)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn getFilesFromWhenOperators(mut inWhenOperators: Arc<metamodelica::List<BackendDAE::WhenOperator>>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((inWhenOperators.clone(), inFiles.clone())) {
        (Deref @ metamodelica::List::Nil, files) => {
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: BackendDAE::WhenOperator::ASSIGN { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromWhenOperators(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: BackendDAE::WhenOperator::REINIT { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromWhenOperators(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: BackendDAE::WhenOperator::ASSERT { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromWhenOperators(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: BackendDAE::WhenOperator::TERMINATE { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromWhenOperators(rest.clone(), files.clone())?;
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: BackendDAE::WhenOperator::NORETCALL { source, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromDAEElementSource(source.clone(), files.clone());
            files = getFilesFromWhenOperators(rest.clone(), files.clone())?;
            files.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outFiles)
}

fn getFilesFromExtObjInfo(mut inExtObjInfo: SimCode::ExtObjInfo, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    (_, outFiles) = List::mapFold(inExtObjInfo.vars.clone(), (std::sync::Arc::new(fnptr!(getFilesFromSimVar, SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)> + 'static>), inFiles.clone())?;
    Ok(outFiles)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn getFilesFromJacobianMatrices(mut inJacobianMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((inJacobianMatrices.clone(), inFiles.clone())) {
        (Deref @ metamodelica::List::Nil, files) => {
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ SimCode::JacobianMatrix { columns: onemat, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromJacobianMatrix(onemat.clone(), files.clone())?;
            files = getFilesFromJacobianMatrices(rest.clone(), files.clone())?;
            files.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outFiles)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn getFilesFromJacobianMatrix(mut inJacobianMatrices: Arc<metamodelica::List<Arc<SimCode::JacobianColumn>>>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<Arc<metamodelica::List<SimCode::FileInfo>>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = (::match_deref::match_deref! { match &((inJacobianMatrices.clone(), inFiles.clone())) {
        (Deref @ metamodelica::List::Nil, files) => {
            files.clone()
        },
        (Deref @ metamodelica::List::Cons { head: Deref @ SimCode::JacobianColumn { columnVars: vars, columnEqns: systems, .. }, tail: rest }, files) => {
            let mut files = (*files).clone();
            files = getFilesFromSimEqSystems(list![systems.clone()], files.clone())?;
            (_, files) = List::mapFold(vars.clone(), (std::sync::Arc::new(fnptr!(getFilesFromSimVar, SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<(SimCodeVar::SimVar, Arc<metamodelica::List<SimCode::FileInfo>>)> + 'static>), files.clone())?;
            files = getFilesFromJacobianMatrix(rest.clone(), files.clone())?;
            files.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(outFiles)
}

fn collectAllFiles(mut inSimCode: SimCode::SimCode) -> Result<SimCode::SimCode> {
    let mut outSimCode: SimCode::SimCode = inSimCode.clone();
    let mut modelInfo: SimCode::ModelInfo = <SimCode::ModelInfo as ::std::default::Default>::default();
    let mut files: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    if !(Config::acceptMetaModelicaGrammar()?) {
        modelInfo = outSimCode.modelInfo.clone();
        files = getFilesFromSimVars(modelInfo.vars.clone(), files.clone())?;
        files = getFilesFromFunctions(modelInfo.functions.clone(), files.clone())?;
        files = getFilesFromSimEqSystems(metamodelica::cons(outSimCode.allEquations.clone(), metamodelica::cons(outSimCode.startValueEquations.clone(), metamodelica::cons(outSimCode.nominalValueEquations.clone(), metamodelica::cons(outSimCode.minValueEquations.clone(), metamodelica::cons(outSimCode.maxValueEquations.clone(), metamodelica::cons(outSimCode.parameterEquations.clone(), metamodelica::cons(outSimCode.removedEquations.clone(), metamodelica::cons(outSimCode.algorithmAndEquationAsserts.clone(), outSimCode.odeEquations.clone())))))))), files.clone())?;
        files = getFilesFromSimEqSystems(outSimCode.algebraicEquations.clone(), files.clone())?;
        files = getFilesFromExtObjInfo(outSimCode.extObjInfo.clone(), files.clone())?;
        files = getFilesFromJacobianMatrices(outSimCode.jacobianMatrices.clone(), files.clone())?;
        files = List::sort(files.clone(), (std::sync::Arc::new(greaterFileInfo) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::FileInfo, SimCode::FileInfo) -> Result<bool> + 'static>))?;
        outSimCode.modelInfo = modelInfo.clone();
    }
    Ok(outSimCode)
}

fn getFilesFromDAEElementSource(mut inSource: Arc<DAE::ElementSource>, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Arc<metamodelica::List<SimCode::FileInfo>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    outFiles = getFilesFromAbsynInfo(inSource.info.clone(), inFiles.clone());
    outFiles
}

fn getFilesFromAbsynInfo(mut inInfo: SourceInfo, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Arc<metamodelica::List<SimCode::FileInfo>> {
    let mut outFiles: Arc<metamodelica::List<SimCode::FileInfo>> = metamodelica::nil();
    let mut fi: SimCode::FileInfo = <SimCode::FileInfo as ::std::default::Default>::default();
    fi = SimCode::FileInfo { fileName: inInfo.fileName.clone(), isReadOnly: inInfo.isReadOnly.clone() };
    outFiles = List::consOnTrue(!(listMember(fi.clone(), inFiles.clone())), fi.clone(), inFiles.clone());
    outFiles
}

fn equalFileInfo(mut inFileInfo1: SimCode::FileInfo, mut inFileInfo2: SimCode::FileInfo) -> Result<bool> {
    let mut isMatch: bool = false;
    let mut f1: ArcStr = arcstr::literal!("");
    let mut f2: ArcStr = arcstr::literal!("");
    let SimCode::FILEINFO { fileName: __pa0, isReadOnly: _ } = (inFileInfo1.clone()) else { bail!("pattern mismatch") };
    f1 = __pa0.clone();
    let SimCode::FILEINFO { fileName: __pa1, isReadOnly: _ } = (inFileInfo2.clone()) else { bail!("pattern mismatch") };
    f2 = __pa1.clone();
    isMatch = stringEq((f1.clone()).clone(), (f2.clone()).clone());
    Ok(isMatch)
}

fn greaterFileInfo(mut inFileInfo1: SimCode::FileInfo, mut inFileInfo2: SimCode::FileInfo) -> Result<bool> {
    let mut isGreater: bool = false;
    let mut f1: ArcStr = arcstr::literal!("");
    let mut f2: ArcStr = arcstr::literal!("");
    let mut compare: i32 = 0;
    let SimCode::FILEINFO { fileName: __pa0, isReadOnly: _ } = (inFileInfo1.clone()) else { bail!("pattern mismatch") };
    f1 = __pa0.clone();
    let SimCode::FILEINFO { fileName: __pa1, isReadOnly: _ } = (inFileInfo2.clone()) else { bail!("pattern mismatch") };
    f2 = __pa1.clone();
    compare = stringCompare((f1.clone()).clone(), (f2.clone()).clone());
    isGreater = intGt(compare.clone(), 0);
    Ok(isGreater)
}

fn getFileIndexFromFiles(mut file: ArcStr, mut files: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<i32> {
    let mut index: i32 = 0;
    index = List::position1OnTrue(files.clone(), (std::sync::Arc::new(equalFileInfo) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::FileInfo, SimCode::FileInfo) -> Result<bool> + 'static>), SimCode::FileInfo { fileName: (file.clone()).clone(), isReadOnly: false })? - 1;
    Ok(index)
}

pub fn fileName2fileIndex(mut inFileName: ArcStr, mut inFiles: Arc<metamodelica::List<SimCode::FileInfo>>) -> Result<i32> {
    let mut outFileIndex: i32 = 0;
    outFileIndex = 'mc: {
        let __mc_input = (inFileName.clone(), inFiles.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (file, files) => {
                    let mut index: i32 = 0;
                    index = getFileIndexFromFiles((file.clone()).clone(), files.clone())?;
                    Ok(index.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(-1)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outFileIndex)
}

fn makeEqualLengthLists(mut inLst: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, mut i: i32) -> Result<Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>> {
    let mut olst: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    olst = 'mc: {
        let __mc_input = (inLst.clone(), i.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (lst, _) => {
                    let mut l: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    let false = (Flags::isSet(Flags::PTHREADS.clone())?) else { bail!("pattern mismatch") };
                    l = List::flatten(lst.clone())?;
                    Ok(metamodelica::cons(l.clone(), metamodelica::nil()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (lst, 0) => {
                    Ok(lst.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (lst, 1) => {
                    let mut l: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
                    l = List::flatten(lst.clone())?;
                    Ok(metamodelica::cons(l.clone(), metamodelica::nil()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (lst, _) => {
                    let mut q: Arc<metamodelica::List<Arc<PriorityQueue::Tree>>> = metamodelica::nil();
                    let mut prios: Arc<metamodelica::List<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)>> = metamodelica::nil();
                    let mut lst = (*lst).clone();
                    q = List::fold(List::fill((0, metamodelica::nil()), i.clone()), (std::sync::Arc::new(PriorityQueue::insert) as std::sync::Arc<dyn ::std::ops::Fn((i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>), Arc<metamodelica::List<Arc<PriorityQueue::Tree>>>) -> Result<Arc<metamodelica::List<Arc<PriorityQueue::Tree>>>> + 'static>), PriorityQueue::empty().clone())?;
                    prios = List::map(lst.clone(), (std::sync::Arc::new(calcPriority) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> + 'static>))?;
                    q = List::fold(prios.clone(), (std::sync::Arc::new(makeEqualLengthLists2) as std::sync::Arc<dyn ::std::ops::Fn((i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>), Arc<metamodelica::List<Arc<PriorityQueue::Tree>>>) -> Result<Arc<metamodelica::List<Arc<PriorityQueue::Tree>>>> + 'static>), q.clone())?;
                    lst = List::map(PriorityQueue::elements(q.clone())?, std::sync::Arc::new(fnptr!(Util::tuple22, _)))?;
                    Ok(lst.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(olst)
}

fn makeEqualLengthLists2(mut elt: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>), mut iq: Arc<metamodelica::List<Arc<PriorityQueue::Tree>>>) -> Result<Arc<metamodelica::List<Arc<PriorityQueue::Tree>>>> {
    let mut oq: Arc<metamodelica::List<Arc<PriorityQueue::Tree>>> = metamodelica::nil();
    oq = (::match_deref::match_deref! { match &((elt.clone(), iq.clone())) {
        ((i1, l1), q) => {
            let mut l2: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
            let mut i2: i32 = 0;
            let mut q = (*q).clone();
            let (__pa0, (__pa1, __pa2)) = PriorityQueue::deleteAndReturnMin(q.clone())?;
            q = __pa0.clone();
            i2 = __pa1.clone();
            l2 = __pa2.clone();
            q = PriorityQueue::insert((i1.clone() + i2.clone(), listAppend(l2.clone(), l1.clone())), q.clone())?;
            q.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(oq)
}

fn calcPriority(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)> {
    let mut prio: (i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) = (0, metamodelica::nil());
    let mut i: i32 = 0;
    (_, i) = traverseExpsEqSystems(eqs.clone(), (std::sync::Arc::new(Expression::complexityTraverse) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, i32) -> Result<(Arc<DAE::Exp>, i32)> + 'static>), 1, metamodelica::nil())?;
    prio = (i.clone(), eqs.clone());
    Ok(prio)
}

pub fn traverseExpsSimCode<A: Clone + 'static>(mut simCode: SimCode::SimCode, mut func: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>, mut ia: A) -> Result<(SimCode::SimCode, A)> {
    pub type Func<A: Clone + 'static> = std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>;

    let mut outSimCode: SimCode::SimCode = simCode.clone();
    let mut oa: A;
    let mut literals: Arc<metamodelica::List<Arc<DAE::Exp>>> = metamodelica::nil();
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut eqs1: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut partitions: Arc<metamodelica::List<SimCode::ClockedPartition>> = metamodelica::nil();
    (literals, oa) = List::mapFold(outSimCode.literals.clone(), func.clone(), ia.clone())?;
    outSimCode.literals = literals.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.allEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.allEquations = eqs.clone();
    (partitions, oa) = List::map1Fold(outSimCode.clockedPartitions.clone(), (std::sync::Arc::new(traverseExpsPartition) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::ClockedPartition, _, _) -> Result<_> + 'static>), func.clone(), oa.clone())?;
    outSimCode.clockedPartitions = partitions.clone();
    (eqs1, oa) = traverseExpsEqSystemsList(outSimCode.odeEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.odeEquations = eqs1.clone();
    (eqs1, oa) = traverseExpsEqSystemsList(outSimCode.algebraicEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.algebraicEquations = eqs1.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.initialEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.initialEquations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.removedInitialEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.removedInitialEquations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.startValueEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.startValueEquations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.nominalValueEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.nominalValueEquations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.minValueEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.minValueEquations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.maxValueEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.maxValueEquations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.parameterEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.parameterEquations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.removedEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.removedEquations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.algorithmAndEquationAsserts.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.algorithmAndEquationAsserts = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(outSimCode.jacobianEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSimCode.jacobianEquations = eqs.clone();
    Ok((outSimCode, oa))
}

fn traverseExpsPartition<A: Clone + 'static>(mut simPartition: SimCode::ClockedPartition, mut func: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>, mut ia: A) -> Result<(SimCode::ClockedPartition, A)> {
    pub type Func<A: Clone + 'static> = std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>;

    let mut outSimPartition: SimCode::ClockedPartition = simPartition.clone();
    let mut oa: A;
    let mut clk: Arc<DAE::ClockKind> = Arc::new(DAE::ClockKind::INFERRED_CLOCK);
    let mut subPartitions: Arc<metamodelica::List<SimCode::SubPartition>> = metamodelica::nil();
    let (__pa0, __pa1) = ::match_deref::match_deref! { match &(func(Arc::new(DAE::Exp::CLKCONST { clk: simPartition.baseClock.clone() }), ia.clone())?) {
        (Deref @ DAE::Exp::CLKCONST { clk: __pa0 }, __pa1) => (__pa0.clone(), __pa1.clone()),
        _ => bail!("pattern mismatch"),
    } };
    clk = __pa0.clone();
    oa = __pa1.clone();
    (subPartitions, oa) = List::map1Fold(simPartition.subPartitions.clone(), (std::sync::Arc::new(traverseExpsSubPartition) as std::sync::Arc<dyn ::std::ops::Fn(SimCode::SubPartition, _, _) -> Result<_> + 'static>), func.clone(), oa.clone())?;
    outSimPartition.baseClock = clk.clone();
    outSimPartition.subPartitions = subPartitions.clone();
    Ok((outSimPartition, oa))
}

fn traverseExpsSubPartition<A: Clone + 'static>(mut subPartition: SimCode::SubPartition, mut func: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>, mut ia: A) -> Result<(SimCode::SubPartition, A)> {
    pub type Func<A: Clone + 'static> = std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>;

    let mut outSubPartition: SimCode::SubPartition = subPartition.clone();
    let mut oa: A;
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    (eqs, oa) = traverseExpsEqSystems(subPartition.equations.clone(), func.clone(), ia.clone(), metamodelica::nil())?;
    outSubPartition.equations = eqs.clone();
    (eqs, oa) = traverseExpsEqSystems(subPartition.removedEquations.clone(), func.clone(), oa.clone(), metamodelica::nil())?;
    outSubPartition.removedEquations = eqs.clone();
    Ok((outSubPartition, oa))
}

fn traverseExpsEqSystemsList<A: Clone + 'static>(mut ieqs: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, mut func: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>, mut ia: A, mut acc: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>) -> Result<(Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, A)> {
    pub type Func<A: Clone + 'static> = std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>;

    let mut oeqs: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut oa: A;
    (oeqs, oa) = (::match_deref::match_deref! { match &((ieqs.clone(), ia.clone())) {
        (Deref @ metamodelica::List::Nil, a) => {
            (acc.clone().reverse(), a.clone())
        },
        (Deref @ metamodelica::List::Cons { head: eq, tail: eqs }, a) => {
            let mut eq = (*eq).clone();
            let mut a = (*a).clone();
            (eq, a) = traverseExpsEqSystems(eq.clone(), func.clone(), a.clone(), metamodelica::nil())?;
            (oeqs, a) = traverseExpsEqSystemsList(eqs.clone(), func.clone(), a.clone(), metamodelica::cons(eq.clone(), acc.clone()))?;
            (oeqs.clone(), a.clone())
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok((oeqs, oa))
}

fn traverseExpsEqSystems<A: Clone + 'static>(mut ieqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut func: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>, mut ia: A, mut acc: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, A)> {
    pub type Func<A: Clone + 'static> = std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>;

    let mut oeqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut oa: A;
    (oeqs, oa) = (::match_deref::match_deref! { match &((ieqs.clone(), ia.clone())) {
        (Deref @ metamodelica::List::Nil, a) => {
            (acc.clone().reverse(), a.clone())
        },
        (Deref @ metamodelica::List::Cons { head: eq, tail: eqs }, a) => {
            let mut eq = (*eq).clone();
            let mut a = (*a).clone();
            (eq, a) = traverseExpsEqSystem(eq.clone(), func.clone(), a.clone())?;
            (oeqs, a) = traverseExpsEqSystems(eqs.clone(), func.clone(), a.clone(), metamodelica::cons(eq.clone(), acc.clone()))?;
            (oeqs.clone(), a.clone())
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok((oeqs, oa))
}

fn traverseExpsEqSystem<A: Clone + 'static>(mut eq: Arc<SimCode::SimEqSystem>, mut func: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>, mut ia: A) -> Result<(Arc<SimCode::SimEqSystem>, A)> {
    pub type Func<A: Clone + 'static> = std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, A) -> Result<(Arc<DAE::Exp>, A)> + 'static>;

    let mut oeq: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    let mut oa: A;
    (oeq, oa) = (::match_deref::match_deref! { match &((eq.clone(), ia.clone())) {
        (Deref @ SimCode::SimEqSystem::SES_RESIDUAL { index, res_index, exp, source, eqAttr }, a) => {
            let mut exp_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut eq_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut a = (*a).clone();
            (exp_, a) = func(exp.clone(), a.clone())?;
            if referenceEq(&*(exp.clone()),&*(exp_.clone())) {
                eq_ = eq.clone();
            } else {
                eq_ = Arc::new(SimCode::SimEqSystem::SES_RESIDUAL { index: index.clone(), res_index: res_index.clone(), exp: exp_.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
            }
            (eq_.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_FOR_RESIDUAL { index, res_index, iterators: _, exp, source, eqAttr }, a) => {
            let mut exp_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut eq_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut a = (*a).clone();
            (exp_, a) = func(exp.clone(), a.clone())?;
            if referenceEq(&*(exp.clone()),&*(exp_.clone())) {
                eq_ = eq.clone();
            } else {
                eq_ = Arc::new(SimCode::SimEqSystem::SES_FOR_RESIDUAL { index: index.clone(), res_index: res_index.clone(), iterators: var_field!((*eq).iterators, SimCode::SimEqSystem::SES_FOR_RESIDUAL).clone(), exp: exp_.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
            }
            (eq_.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { index, res_index, scal_indices: _, iterators: _, exp, source, eqAttr }, a) => {
            let mut exp_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut eq_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut a = (*a).clone();
            (exp_, a) = func(exp.clone(), a.clone())?;
            if referenceEq(&*(exp.clone()),&*(exp_.clone())) {
                eq_ = eq.clone();
            } else {
                eq_ = Arc::new(SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { index: index.clone(), res_index: res_index.clone(), scal_indices: var_field!((*eq).scal_indices, SimCode::SimEqSystem::SES_GENERIC_RESIDUAL).clone(), iterators: var_field!((*eq).iterators, SimCode::SimEqSystem::SES_GENERIC_RESIDUAL).clone(), exp: exp_.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
            }
            (eq_.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index, cref: cr, exp, source, eqAttr }, a) => {
            let mut exp_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut eq_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut a = (*a).clone();
            (exp_, a) = func(exp.clone(), a.clone())?;
            if referenceEq(&*(exp.clone()),&*(exp_.clone())) {
                eq_ = eq.clone();
            } else {
                eq_ = Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { index: index.clone(), cref: cr.clone(), exp: exp_.clone(), source: source.clone(), eqAttr: eqAttr.clone() });
            }
            (eq_.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { index, cref: cr, exp, source, cons, eqAttr }, a) => {
            let mut exp_: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut eq_: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut a = (*a).clone();
            (exp_, a) = func(exp.clone(), a.clone())?;
            if referenceEq(&*(exp.clone()),&*(exp_.clone())) {
                eq_ = eq.clone();
            } else {
                eq_ = Arc::new(SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { index: index.clone(), cref: cr.clone(), exp: exp_.clone(), source: source.clone(), cons: cons.clone(), eqAttr: eqAttr.clone() });
            }
            (eq_.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index, lhs: leftexp, exp, source, eqAttr }, a) => {
            let mut leftexp = (*leftexp).clone();
            let mut exp = (*exp).clone();
            let mut a = (*a).clone();
            (leftexp, a) = func(leftexp.clone(), a.clone())?;
            (exp, a) = func(exp.clone(), a.clone())?;
            (Arc::new(SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { index: index.clone(), lhs: leftexp.clone(), exp: exp.clone(), source: source.clone(), eqAttr: eqAttr.clone() }), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_IFEQUATION { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_ALGORITHM { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_INVERSE_ALGORITHM { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_LINEAR { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_NONLINEAR { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_MIXED { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_WHEN { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_FOR_LOOP { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_FOR_EQUATION { .. }, a) => {
            (eq.clone(), a.clone())
        },
        (Deref @ SimCode::SimEqSystem::SES_ALIAS { .. }, a) => {
            (eq.clone(), a.clone())
        },
        _ => {
            Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("SimCodeUtil.traverseExpsEqSystem")); __mm_s.push_str(&*literal!(" got unknown equation")); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((oeq, oa))
}

fn setSimCodeLiterals(mut simCode: SimCode::SimCode, mut literals: Arc<metamodelica::List<Arc<DAE::Exp>>>) -> SimCode::SimCode {
    let mut outSimCode: SimCode::SimCode = simCode.clone();
    outSimCode.literals = literals.clone();
    outSimCode
}

fn eqSystemWCET(mut eqs: Arc<SimCode::SimEqSystem>) -> Result<(Arc<SimCode::SimEqSystem>, i32)> {
    let mut tpl: (Arc<SimCode::SimEqSystem>, i32) = (Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default()), 0);
    let mut i: i32 = 0;
    (_, i) = traverseExpsEqSystems(list![eqs.clone()], (std::sync::Arc::new(Expression::complexityTraverse) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, i32) -> Result<(Arc<DAE::Exp>, i32)> + 'static>), 0, metamodelica::nil())?;
    tpl = (eqs.clone(), i.clone());
    Ok(tpl)
}

fn getHideResult(mut hideResultExp: Option<Arc<DAE::Exp>>, mut name: Arc<DAE::ComponentRef>, mut source: Arc<DAE::ElementSource>) -> Result<Option<bool>> {
    let mut hideResult: Option<bool> = None;
    hideResult = (::match_deref::match_deref! { match &(hideResultExp.clone()) {
        None => None,
        Some(Deref @ DAE::Exp::BCONST { bool: false }) => Some(false),
        Some(Deref @ DAE::Exp::BCONST { bool: true }) => Some(true),
        _ => {
            Error::addSourceMessage(Error::HIDE_RESULT_NOT_EVALUATED.clone(), list![(ComponentReferenceBasics::printComponentRefStr(name.clone())?).clone()], ElementSource::getInfo(source.clone()))?;
            None
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(hideResult)
}

pub fn getVarIndexListByMapping(mut iVarToArrayIndexMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, (Arc<metamodelica::List<i32>>, metamodelica::Array<i32>))>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn((Arc<metamodelica::List<i32>>, metamodelica::Array<i32>)) -> Result<ArcStr> + 'static>)), mut iVarName: Arc<DAE::ComponentRef>, mut iColumnMajor: bool, mut iIndexForUndefinedReferences: ArcStr) -> Result<Arc<metamodelica::List<ArcStr>>> {
    let mut oVarIndexList: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    (oVarIndexList, _) = getVarIndexInfosByMapping(iVarToArrayIndexMapping.clone(), iVarName.clone(), iColumnMajor.clone(), (iIndexForUndefinedReferences.clone()).clone())?;
    Ok(oVarIndexList)
}

pub fn getVarIndexByMapping(mut iVarToArrayIndexMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, (Arc<metamodelica::List<i32>>, metamodelica::Array<i32>))>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn((Arc<metamodelica::List<i32>>, metamodelica::Array<i32>)) -> Result<ArcStr> + 'static>)), mut iVarName: Arc<DAE::ComponentRef>, mut iColumnMajor: bool, mut iIndexForUndefinedReferences: ArcStr) -> Result<ArcStr> {
    let mut oConcreteVarIndex: ArcStr = arcstr::literal!("");
    (_, oConcreteVarIndex) = getVarIndexInfosByMapping(iVarToArrayIndexMapping.clone(), iVarName.clone(), iColumnMajor.clone(), (iIndexForUndefinedReferences.clone()).clone())?;
    Ok(oConcreteVarIndex)
}

pub fn providesDirectionalDerivative(mut inSimCode: SimCode::SimCode) -> bool {
    let mut b: bool = false;
    b = (::match_deref::match_deref! { match &(inSimCode.clone()) {
        SimCode::SimCode { modelStructure: Some(SimCode::FmiModelStructure { continuousPartialDerivatives: Some(_), .. }), .. } => true,
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    b
}

fn getVarIndexInfosByMapping(mut iVarToArrayIndexMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, (Arc<metamodelica::List<i32>>, metamodelica::Array<i32>))>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn((Arc<metamodelica::List<i32>>, metamodelica::Array<i32>)) -> Result<ArcStr> + 'static>)), mut iVarName: Arc<DAE::ComponentRef>, mut iColumnMajor: bool, mut iIndexForUndefinedReferences: ArcStr) -> Result<(Arc<metamodelica::List<ArcStr>>, ArcStr)> {
    let mut oVarIndexList: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut oConcreteVarIndex: ArcStr = arcstr::literal!("");
    let mut varName: Arc<DAE::ComponentRef> = iVarName.clone();
    let mut arrayIdx: i32 = 0;
    let mut idx: i32 = 0;
    let mut arraySize: i32 = 0;
    let mut concreteVarIndex: i32 = 0;
    let mut varIndices: metamodelica::Array<i32> = Default::default();
    let mut tmpVarIndexListNew: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut arraySubscripts: Arc<metamodelica::List<Arc<DAE::Subscript>>> = metamodelica::nil();
    let mut arrayDimensions: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut arrayDimensionsReverse: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut toColumnMajor: bool = false;
    let mut isContiguous: bool = false;
    arraySubscripts = ComponentReference::crefLastSubs(varName.clone())?;
    varName = ComponentReferenceBasics::crefStripLastSubs(varName.clone())?;
    if BaseHashTable::hasKey(varName.clone(), iVarToArrayIndexMapping.clone())? {
        (arrayDimensions, varIndices) = BaseHashTable::get(varName.clone(), iVarToArrayIndexMapping.clone())?;
        isContiguous = metamodelica::arrayLength(varIndices.clone()) == 1;
        if isContiguous.clone() {
            arraySize = List::fold(arrayDimensions.clone(), (std::sync::Arc::new(fnptr!(intMul, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), 1)?;
        } else {
            arraySize = metamodelica::arrayLength(varIndices.clone());
        }
        concreteVarIndex = SimCodeUtilShared::getScalarElementIndex(arraySubscripts.clone(), arrayDimensions.clone());
        toColumnMajor = iColumnMajor.clone() && (arrayDimensions.clone().len() as i32) > 1;
        if toColumnMajor.clone() {
            concreteVarIndex = convertIndexToColumnMajor(concreteVarIndex.clone(), arrayDimensions.clone())?;
            arrayDimensionsReverse = arrayDimensions.clone().reverse();
        }
        for mut arrayIdx in 0..=arraySize.clone() - 1 {
            idx = arraySize.clone() - arrayIdx.clone();
            if toColumnMajor.clone() {
                idx = convertIndexToColumnMajor(idx.clone(), arrayDimensionsReverse.clone())?;
            }
            if isContiguous.clone() {
                idx = varIndices.clone().borrow()[(1-1) as usize].clone() + idx.clone() - 1;
            } else {
                idx = varIndices.clone().borrow()[(idx.clone()-1) as usize].clone();
            }
            if intLt(idx.clone(), 0) {
                tmpVarIndexListNew = metamodelica::cons((intString(intMul(idx.clone(), -1) - 1)).clone(), tmpVarIndexListNew.clone());
            } else {
                if intEq(idx.clone(), 0) {
                    tmpVarIndexListNew = metamodelica::cons((iIndexForUndefinedReferences.clone()).clone(), tmpVarIndexListNew.clone());
                } else {
                    tmpVarIndexListNew = metamodelica::cons((intString(idx.clone() - 1)).clone(), tmpVarIndexListNew.clone());
                }
            }
        }
        if isVarIndexListConsecutive(iVarToArrayIndexMapping.clone(), iVarName.clone())? && toColumnMajor.clone() {
            concreteVarIndex = convertIndexToColumnMajor(concreteVarIndex.clone(), arrayDimensions.clone())?;
        }
        oConcreteVarIndex = ((tmpVarIndexListNew.clone()).get(concreteVarIndex.clone())?).clone();
    }
    if tmpVarIndexListNew.clone().is_empty() {
        Error::addMessage(Error::INTERNAL_ERROR.clone(), list![({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("GetVarIndexListByMapping: No Element for ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(varName.clone())?); __mm_s.push_str(&*literal!(" found!")); ArcStr::from(__mm_s) }).clone()])?;
        tmpVarIndexListNew = list![(iIndexForUndefinedReferences.clone()).clone()];
        oConcreteVarIndex = (iIndexForUndefinedReferences.clone()).clone();
    }
    oVarIndexList = tmpVarIndexListNew.clone();
    Ok((oVarIndexList, oConcreteVarIndex))
}

fn convertIndexToColumnMajor(mut idx: i32, mut arrayDimensions: Arc<metamodelica::List<i32>>) -> Result<i32> {
    let mut idxOut: i32 = 0;
    let mut idx0: i32 = 0;
    let mut ndim: i32 = 0;
    let mut length: i32 = 0;
    let mut idxi: i32 = 0;
    let mut fac: i32 = 0;
    ndim = (arrayDimensions.clone().len() as i32);
    length = List::fold(arrayDimensions.clone(), (std::sync::Arc::new(fnptr!(intMul, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), 1)?;
    idx0 = idx.clone() - 1;
    idxOut = 1;
    fac = 1;
    for mut dimi in &*arrayDimensions.clone() {
        let mut dimi = dimi.clone();
        length = intDiv(length.clone(), dimi.clone());
        idxi = intDiv(idx0.clone(), length.clone());
        idx0 = idx0.clone() - idxi.clone() * length.clone();
        idxOut = idxOut.clone() + idxi.clone() * fac.clone();
        fac = fac.clone() * dimi.clone();
    }
    Ok(idxOut)
}

pub fn isVarIndexListConsecutive(mut iVarToArrayIndexMapping: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, (Arc<metamodelica::List<i32>>, metamodelica::Array<i32>))>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn((Arc<metamodelica::List<i32>>, metamodelica::Array<i32>)) -> Result<ArcStr> + 'static>)), mut iVarName: Arc<DAE::ComponentRef>) -> Result<bool> {
    let mut oIsConsecutive: bool = false;
    let mut varName: Arc<DAE::ComponentRef> = iVarName.clone();
    let mut arrayIdx: i32 = 0;
    let mut idx: i32 = 0;
    let mut arraySize: i32 = 0;
    let mut currentIndex: i32 = -1;
    let mut varIndices: metamodelica::Array<i32> = Default::default();
    let mut consecutive: bool = true;
    varName = ComponentReferenceBasics::crefStripLastSubs(varName.clone())?;
    if BaseHashTable::hasKey(varName.clone(), iVarToArrayIndexMapping.clone())? {
        (_, varIndices) = BaseHashTable::get(varName.clone(), iVarToArrayIndexMapping.clone())?;
        arraySize = metamodelica::arrayLength(varIndices.clone());
        for mut arrayIdx in 0..=arraySize.clone() - 1 {
            idx = varIndices.clone().borrow()[(arraySize.clone() - arrayIdx.clone()-1) as usize].clone();
            if intLt(idx.clone(), 0) {
                if intEq(currentIndex.clone(), -1) {
                    currentIndex = intMul(idx.clone(), -1) - 1;
                } else {
                    consecutive = boolAnd(consecutive.clone(), intEq(currentIndex.clone(), intMul(idx.clone(), -1)));
                    currentIndex = intMul(idx.clone(), -1) - 1;
                }
            } else {
                if intEq(idx.clone(), 0) {
                    currentIndex = -2;
                    consecutive = false;
                } else {
                    if intEq(currentIndex.clone(), -1) {
                        currentIndex = idx.clone() - 1;
                    } else {
                        consecutive = boolAnd(consecutive.clone(), intEq(currentIndex.clone(), idx.clone()));
                        currentIndex = idx.clone() - 1;
                    }
                }
            }
        }
    }
    oIsConsecutive = consecutive.clone();
    Ok(oIsConsecutive)
}

pub fn createIdxSCVarMapping(mut simVars: SimCodeVar::SimVars) -> Result<metamodelica::Array<Option<SimCodeVar::SimVar>>> {
    let mut outMapping: metamodelica::Array<Option<SimCodeVar::SimVar>> = Default::default();
    let mut numStateVars: i32 = 0;
    let mut stateVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut derivativeVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut algVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut discreteAlgVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut intAlgVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut boolAlgVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut inputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut outputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut aliasVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut intAliasVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut boolAliasVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut paramVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut intParamVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut boolParamVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut stringAlgVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut stringParamVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut stringAliasVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut extObjVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut constVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut intConstVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut boolConstVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut stringConstVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut jacobianVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut seedVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut realOptimizeConstraintsVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut realOptimizeFinalConstraintsVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut idxSimVarMappingTplList: Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>> = metamodelica::nil();
    let mut highestIdx: i32 = 0;
    let mut varCount: i32 = 0;
    let mut mappingArray: metamodelica::Array<Option<SimCodeVar::SimVar>> = Default::default();
    let SimCodeVar::SIMVARS { stateVars: __pa0, derivativeVars: __pa1, algVars: __pa2, discreteAlgVars: __pa3, intAlgVars: __pa4, boolAlgVars: __pa5, inputVars: __pa6, outputVars: __pa7, aliasVars: __pa8, intAliasVars: __pa9, boolAliasVars: __pa10, paramVars: __pa11, intParamVars: __pa12, boolParamVars: __pa13, stringAlgVars: __pa14, stringParamVars: __pa15, stringAliasVars: __pa16, extObjVars: __pa17, constVars: __pa18, intConstVars: __pa19, boolConstVars: __pa20, stringConstVars: __pa21, jacobianVars: __pa22, seedVars: __pa23, realOptimizeConstraintsVars: __pa24, realOptimizeFinalConstraintsVars: __pa25, sensitivityVars: _, .. } = (simVars.clone()) else { bail!("pattern mismatch") };
    stateVars = __pa0.clone();
    derivativeVars = __pa1.clone();
    algVars = __pa2.clone();
    discreteAlgVars = __pa3.clone();
    intAlgVars = __pa4.clone();
    boolAlgVars = __pa5.clone();
    inputVars = __pa6.clone();
    outputVars = __pa7.clone();
    aliasVars = __pa8.clone();
    intAliasVars = __pa9.clone();
    boolAliasVars = __pa10.clone();
    paramVars = __pa11.clone();
    intParamVars = __pa12.clone();
    boolParamVars = __pa13.clone();
    stringAlgVars = __pa14.clone();
    stringParamVars = __pa15.clone();
    stringAliasVars = __pa16.clone();
    extObjVars = __pa17.clone();
    constVars = __pa18.clone();
    intConstVars = __pa19.clone();
    boolConstVars = __pa20.clone();
    stringConstVars = __pa21.clone();
    jacobianVars = __pa22.clone();
    seedVars = __pa23.clone();
    realOptimizeConstraintsVars = __pa24.clone();
    realOptimizeFinalConstraintsVars = __pa25.clone();
    numStateVars = (stateVars.clone().len() as i32);
    varCount = 0;
    (idxSimVarMappingTplList, highestIdx) = List::fold21(stateVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), metamodelica::nil(), 0)?;
    varCount = varCount.clone() + numStateVars.clone();
    (idxSimVarMappingTplList, highestIdx) = List::fold21(derivativeVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + numStateVars.clone();
    (idxSimVarMappingTplList, highestIdx) = List::fold21(algVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (algVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(discreteAlgVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (discreteAlgVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(intAlgVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (intAlgVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(boolAlgVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (boolAlgVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(stringAlgVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (stringAlgVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(inputVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (inputVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(outputVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (outputVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(aliasVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (aliasVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(intAliasVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (intAliasVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(boolAliasVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (boolAliasVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(stringAliasVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (stringAliasVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(paramVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (paramVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(intParamVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (intParamVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(boolParamVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (boolParamVars.clone().len() as i32);
    (idxSimVarMappingTplList, highestIdx) = List::fold21(stringParamVars.clone(), (std::sync::Arc::new(createAllSCVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, i32, Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> + 'static>), varCount.clone(), idxSimVarMappingTplList.clone(), highestIdx.clone())?;
    varCount = varCount.clone() + (stringParamVars.clone().len() as i32);
    mappingArray = arrayCreate(highestIdx.clone(), None);
    mappingArray = List::fold(idxSimVarMappingTplList.clone(), (std::sync::Arc::new(createAllSCVarMapping1) as std::sync::Arc<dyn ::std::ops::Fn((i32, SimCodeVar::SimVar), metamodelica::Array<Option<SimCodeVar::SimVar>>) -> Result<metamodelica::Array<Option<SimCodeVar::SimVar>>> + 'static>), mappingArray.clone())?;
    outMapping = mappingArray.clone();
    Ok(outMapping)
}

fn createAllSCVarMapping0(mut iSimVar: SimCodeVar::SimVar, mut iOffset: i32, mut iMapping: Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, mut highestIdx: i32) -> Result<(Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>>, i32)> {
    let mut iMapping: Arc<metamodelica::List<(i32, SimCodeVar::SimVar)>> = iMapping;
    let mut highestIdx: i32 = highestIdx;
    let mut simVarIdx: i32 = 0;
    let SimCodeVar::SIMVAR { index: __pa0, .. } = (iSimVar.clone()) else { bail!("pattern mismatch") };
    simVarIdx = __pa0.clone();
    simVarIdx = simVarIdx.clone() + 1 + iOffset.clone();
    highestIdx = if (intGt(simVarIdx.clone(), highestIdx.clone())) {simVarIdx.clone()} else {highestIdx.clone()};
    iMapping = metamodelica::cons((simVarIdx.clone(), iSimVar.clone()), iMapping.clone());
    Ok((iMapping, highestIdx))
}

fn createAllSCVarMapping1(mut iSimVarIdxTpl: (i32, SimCodeVar::SimVar), mut iMapping: metamodelica::Array<Option<SimCodeVar::SimVar>>) -> Result<metamodelica::Array<Option<SimCodeVar::SimVar>>> {
    let mut outMapping: metamodelica::Array<Option<SimCodeVar::SimVar>> = Default::default();
    let mut simVarIdx: i32 = 0;
    let mut simVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    (simVarIdx, simVar) = iSimVarIdxTpl.clone();
    outMapping = {let _arr = iMapping.clone(); _arr.borrow_mut()[(simVarIdx.clone()-1) as usize] = Some(simVar.clone()); _arr};
    Ok(outMapping)
}

pub fn getEnumerationTypes(mut inVars: SimCodeVar::SimVars) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    outVars = (match inVars.clone() {
        SimCodeVar::SimVars { .. } => {
            outVars = getEnumerationTypesHelper(inVars.stateVars.clone(), metamodelica::nil())?;
            outVars = getEnumerationTypesHelper(inVars.derivativeVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.algVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.discreteAlgVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.intAlgVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.boolAlgVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.inputVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.outputVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.aliasVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.intAliasVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.boolAliasVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.paramVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.intParamVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.boolParamVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.stringAlgVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.stringParamVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.stringAliasVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.extObjVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.constVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.intConstVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.boolConstVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.stringConstVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.sensitivityVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.jacobianVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.seedVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.realOptimizeConstraintsVars.clone(), outVars.clone())?;
            outVars = getEnumerationTypesHelper(inVars.realOptimizeFinalConstraintsVars.clone(), outVars.clone())?;
            outVars.clone().reverse()
        },
        _ => metamodelica::nil(),
    });
    Ok(outVars)
}

fn getEnumerationTypesHelper(mut inVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut inAccumVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = inAccumVars.clone();
    for mut var in &*inVars.clone() {
        let mut var = var.clone();
        let () = (match var.clone() {
        SimCodeVar::SimVar { .. } => {
            if Types::isEnumeration(var.type_.clone()) && !(List::exist1(outVars.clone(), (std::sync::Arc::new(fnptr!(enumerationTypeExists, SimCodeVar::SimVar, Arc<DAE::Type>)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, Arc<DAE::Type>) -> Result<bool> + 'static>), var.type_.clone())?) {
                outVars = metamodelica::cons(var.clone(), outVars.clone());
            }
            ()
        },
        _ => (),
    });
    }
    Ok(outVars)
}

fn enumerationTypeExists(mut var: SimCodeVar::SimVar, mut inType: Arc<DAE::Type>) -> bool {
    let mut b: bool = false;
    b = (::match_deref::match_deref! { match &((var.clone(), inType.clone())) {
        (SimCodeVar::SimVar { type_: ty @ Deref @ DAE::Type::T_ENUMERATION { .. }, .. }, Deref @ DAE::Type::T_ENUMERATION { .. }) => {
            AbsynUtil::pathEqual(var_field!((**ty).path, DAE::Type::T_ENUMERATION).clone(), var_field!((*inType).path, DAE::Type::T_ENUMERATION).clone())
        },
        _ => {
            false
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    b
}

pub fn equationIndexEqual(mut eq1: Arc<SimCode::SimEqSystem>, mut eq2: Arc<SimCode::SimEqSystem>) -> Result<bool> {
    let mut isEqual: bool = false;
    isEqual = intEq(simEqSystemIndex(eq1.clone())?, simEqSystemIndex(eq2.clone())?);
    Ok(isEqual)
}

//--------------------------
// backendMapping section
//--------------------------
fn setUpBackendMapping(mut dae: Arc<BackendDAE::BackendDAE>) -> Result<SimCode::BackendMapping> {
    let mut mapping: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    mapping = 'mc: {
        let __mc_input = dae.clone();
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut sizeE: i32 = 0;
                    let mut sizeV: i32 = 0;
                    let mut eqMatch: metamodelica::Array<i32> = Default::default();
                    let mut varMatch: metamodelica::Array<i32> = Default::default();
                    let mut tree: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
                    let mut eqs: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
                    let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
                    let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
                    let mut varMap: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
                    let mut eqMap: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
                    let mut simVarMapping: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>> = Default::default();
                    let mut tpl: Arc<metamodelica::List<(i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>)>> = metamodelica::nil();
                    let mut mapping: SimCode::BackendMapping = mapping.clone();
                    let __pa0 = ::match_deref::match_deref! { match &(dae.clone()) {
                        Deref @ BackendDAE::BackendDAE { eqs: __pa0, .. } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    eqs = __pa0.clone();
                    tpl = List::map1(eqs.clone(), (std::sync::Arc::new(setUpSystMapping) as std::sync::Arc<dyn ::std::ops::Fn(Arc<BackendDAE::EqSystem>, bool) -> Result<(i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>)> + 'static>), BackendDAEUtil::isInitializationDAE(dae.shared.clone()))?;
                    sizeE = List::applyAndFold(tpl.clone(), (std::sync::Arc::new(fnptr!(intAdd, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), std::sync::Arc::new(fnptr!(Util::tuple61, _)), 0)?;
                    sizeV = List::applyAndFold(tpl.clone(), (std::sync::Arc::new(fnptr!(intAdd, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), std::sync::Arc::new(fnptr!(Util::tuple62, _)), 0)?;
                    eqMap = metamodelica::nil();
                    varMap = metamodelica::nil();
                    simVarMapping = arrayCreate(sizeV.clone(), metamodelica::nil());
                    eqMatch = arrayCreate(sizeE.clone(), 0);
                    varMatch = arrayCreate(sizeV.clone(), 0);
                    m = arrayCreate(sizeE.clone(), metamodelica::nil());
                    mt = arrayCreate(sizeV.clone(), metamodelica::nil());
                    (_, _, m, mt, eqMatch, varMatch) = List::fold(tpl.clone(), (std::sync::Arc::new(appendAdjacencyMatrices) as std::sync::Arc<dyn ::std::ops::Fn((i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>), (i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>)) -> Result<(i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>)> + 'static>), (0, 0, m.clone(), mt.clone(), eqMatch.clone(), varMatch.clone()))?;
                    tree = arrayCreate(sizeE.clone(), metamodelica::nil());
                    tree = List::fold4(List::intRange(sizeE.clone()), (std::sync::Arc::new(setUpEqTree) as std::sync::Arc<dyn ::std::ops::Fn(i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>, metamodelica::Array<Arc<metamodelica::List<i32>>>) -> Result<metamodelica::Array<Arc<metamodelica::List<i32>>>> + 'static>), m.clone(), mt.clone(), eqMatch.clone(), varMatch.clone(), tree.clone())?;
                    tree = Array::map(tree.clone(), std::sync::Arc::new(fnptr!(List::unique, _)))?;
                    mapping = SimCode::BackendMapping::BACKENDMAPPING { m: m.clone(), mT: mt.clone(), eqMapping: eqMap.clone(), varMapping: varMap.clone(), eqMatch: eqMatch.clone(), varMatch: varMatch.clone(), eqTree: tree.clone(), simVarMapping: simVarMapping.clone() };
                    Ok((mapping.clone(), mapping.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { mapping = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(openmodelica_simcode_types::SimCode::BackendMapping::NO_MAPPING)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(mapping)
}

fn setUpEqTree(mut beq: i32, mut m: metamodelica::Array<Arc<metamodelica::List<i32>>>, mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>>, mut eqMatch: metamodelica::Array<i32>, mut varMatch: metamodelica::Array<i32>, mut treeIn: metamodelica::Array<Arc<metamodelica::List<i32>>>) -> Result<metamodelica::Array<Arc<metamodelica::List<i32>>>> {
    let mut treeOut: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut assVar: i32 = 0;
    let mut preEqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut depVars: Arc<metamodelica::List<i32>> = metamodelica::nil();
    assVar = eqMatch.clone().borrow()[(beq.clone()-1) as usize].clone();
    depVars = m.clone().borrow()[(beq.clone()-1) as usize].clone();
    depVars = List::filter1OnTrue(depVars.clone(), (std::sync::Arc::new(fnptr!(setUpEqTree_Help, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>), assVar.clone())?;
    preEqs = List::map1(depVars.clone(), (std::sync::Arc::new(Array::getIndexFirst) as std::sync::Arc<dyn ::std::ops::Fn(i32, _) -> Result<_> + 'static>), varMatch.clone())?;
    {let _arr = treeIn.clone(); let _val = listAppend(treeIn.borrow()[(beq.clone()-1) as usize].clone(), preEqs.clone()); _arr.borrow_mut()[(beq.clone()-1) as usize] = _val; _arr};
    treeOut = treeIn.clone();
    Ok(treeOut)
}

fn setUpEqTree_Help(mut iVal: i32, mut iRef: i32) -> bool {
    let mut oBool: bool = false;
    oBool = intGt(iVal.clone(), 0) && intNe(iVal.clone(), iRef.clone());
    oBool
}

fn appendAdjacencyMatrices(mut tplIn: (i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>), mut foldIn: (i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>)) -> Result<(i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>)> {
    let mut foldOut: (i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>) = (0, 0, Default::default(), Default::default(), Default::default(), Default::default());
    foldOut = (match (tplIn.clone(), foldIn.clone()) {
        ((mut addE, mut addV, mut m, mut mt, mut eqMatch, mut varMatch), (mut sizeE, mut sizeV, mut mIn, mut mtIn, mut eqMatchIn, mut varMatchIn)) => {
            m = Array::map1(m.clone(), (std::sync::Arc::new(addIntLst) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>, i32) -> Result<Arc<metamodelica::List<i32>>> + 'static>), sizeV.clone())?;
            mt = Array::map1(mt.clone(), (std::sync::Arc::new(addIntLst) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<i32>>, i32) -> Result<Arc<metamodelica::List<i32>>> + 'static>), sizeE.clone())?;
            eqMatch = Array::map1(eqMatch.clone(), (std::sync::Arc::new(fnptr!(intAdd, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), sizeV.clone())?;
            varMatch = Array::map1(varMatch.clone(), (std::sync::Arc::new(fnptr!(intAdd, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), sizeE.clone())?;
            mIn = List::fold2(List::intRange(addE.clone()), (std::sync::Arc::new(updateInAdjacencyMatrix) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>) -> Result<metamodelica::Array<Arc<metamodelica::List<i32>>>> + 'static>), sizeE.clone(), m.clone(), mIn.clone())?;
            mtIn = List::fold2(List::intRange(addV.clone()), (std::sync::Arc::new(updateInAdjacencyMatrix) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>) -> Result<metamodelica::Array<Arc<metamodelica::List<i32>>>> + 'static>), sizeV.clone(), mt.clone(), mtIn.clone())?;
            eqMatchIn = List::fold2(List::intRange(addE.clone()), (std::sync::Arc::new(updateInMatching) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32, metamodelica::Array<i32>, metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> + 'static>), sizeE.clone(), eqMatch.clone(), eqMatchIn.clone())?;
            varMatchIn = List::fold2(List::intRange(addV.clone()), (std::sync::Arc::new(updateInMatching) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32, metamodelica::Array<i32>, metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> + 'static>), sizeV.clone(), varMatch.clone(), varMatchIn.clone())?;
            (sizeE.clone() + addE.clone(), sizeV.clone() + addV.clone(), mIn.clone(), mtIn.clone(), eqMatchIn.clone(), varMatchIn.clone())
        },
    });
    Ok(foldOut)
}

fn updateInAdjacencyMatrix(mut idx: i32, mut offset: i32, mut mAppend: metamodelica::Array<Arc<metamodelica::List<i32>>>, mut mIn: metamodelica::Array<Arc<metamodelica::List<i32>>>) -> Result<metamodelica::Array<Arc<metamodelica::List<i32>>>> {
    let mut mOut: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut entry: Arc<metamodelica::List<i32>> = metamodelica::nil();
    entry = mAppend.clone().borrow()[(idx.clone()-1) as usize].clone();
    mOut = {let _arr = mIn.clone(); _arr.borrow_mut()[(idx.clone() + offset.clone()-1) as usize] = entry.clone(); _arr};
    Ok(mOut)
}

fn updateInMatching(mut idx: i32, mut offset: i32, mut matchingAppend: metamodelica::Array<i32>, mut matchingIn: metamodelica::Array<i32>) -> Result<metamodelica::Array<i32>> {
    let mut matchingOut: metamodelica::Array<i32> = Default::default();
    let mut entry: i32 = 0;
    entry = matchingAppend.clone().borrow()[(idx.clone()-1) as usize].clone();
    matchingOut = {let _arr = matchingIn.clone(); _arr.borrow_mut()[(idx.clone() + offset.clone()-1) as usize] = entry.clone(); _arr};
    Ok(matchingOut)
}

fn addIntLst(mut lstIn: Arc<metamodelica::List<i32>>, mut x: i32) -> Result<Arc<metamodelica::List<i32>>> {
    let mut lstOut: Arc<metamodelica::List<i32>> = metamodelica::nil();
    lstOut = List::map1(lstIn.clone(), (std::sync::Arc::new(fnptr!(intAdd, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), x.clone())?;
    Ok(lstOut)
}

fn setUpSystMapping(mut dae: Arc<BackendDAE::EqSystem>, mut isInitial: bool) -> Result<(i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>)> {
    let mut outTpl: (i32, i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<Arc<metamodelica::List<i32>>>, metamodelica::Array<i32>, metamodelica::Array<i32>) = (0, 0, Default::default(), Default::default(), Default::default(), Default::default());
    let mut sizeV: i32 = 0;
    let mut sizeE: i32 = 0;
    let mut ass1: metamodelica::Array<i32> = Default::default();
    let mut ass2: metamodelica::Array<i32> = Default::default();
    let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut matching: Arc<BackendDAE::Matching> = Arc::new(BackendDAE::Matching::NO_MATCHING);
    outTpl = 'mc: {
        let __mc_input = dae.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut ass1: metamodelica::Array<i32> = ass1.clone();
                    let mut ass2: metamodelica::Array<i32> = ass2.clone();
                    let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = m.clone();
                    let mut matching: Arc<BackendDAE::Matching> = matching.clone();
                    let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = mt.clone();
                    let mut sizeE: i32 = sizeE.clone();
                    let mut sizeV: i32 = sizeV.clone();
                    let (__pa0, __pa1, __pa2) = ::match_deref::match_deref! { match &(dae.clone()) {
                        Deref @ BackendDAE::EqSystem { matching: __pa0, mT: Some(__pa1), m: Some(__pa2), .. } => (__pa0.clone(), __pa1.clone(), __pa2.clone()),
                        _ => bail!("pattern mismatch"),
                    } };
                    matching = __pa0.clone();
                    mt = __pa1.clone();
                    m = __pa2.clone();
                    let (__pa3, __pa4) = ::match_deref::match_deref! { match &(matching.clone()) {
                        Deref @ BackendDAE::Matching::MATCHING { ass2: __pa3, ass1: __pa4, .. } => (__pa3.clone(), __pa4.clone()),
                        _ => bail!("pattern mismatch"),
                    } };
                    ass2 = __pa3.clone();
                    ass1 = __pa4.clone();
                    sizeE = BackendDAEUtil::equationArraySizeDAE(dae.clone());
                    sizeV = BackendVariable::daenumVariables(dae.clone());
                    Ok((sizeE.clone(), sizeV.clone(), m.clone(), mt.clone(), ass2.clone(), ass1.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    let mut ass1: metamodelica::Array<i32> = ass1.clone();
                    let mut ass2: metamodelica::Array<i32> = ass2.clone();
                    let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = m.clone();
                    let mut matching: Arc<BackendDAE::Matching> = matching.clone();
                    let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = mt.clone();
                    let mut sizeE: i32 = sizeE.clone();
                    let mut sizeV: i32 = sizeV.clone();
                    let __pa0 = ::match_deref::match_deref! { match &(dae.clone()) {
                        Deref @ BackendDAE::EqSystem { matching: __pa0, mT: None, m: None, .. } => __pa0.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    matching = __pa0.clone();
                    let (__pa1, __pa2) = ::match_deref::match_deref! { match &(matching.clone()) {
                        Deref @ BackendDAE::Matching::MATCHING { ass2: __pa1, ass1: __pa2, .. } => (__pa1.clone(), __pa2.clone()),
                        _ => bail!("pattern mismatch"),
                    } };
                    ass2 = __pa1.clone();
                    ass1 = __pa2.clone();
                    (_, m, mt) = BackendDAEUtil::getAdjacencyMatrix(dae.clone(), openmodelica_backend_types::BackendDAE::IndexType::NORMAL, None, isInitial.clone())?;
                    sizeE = BackendDAEUtil::equationArraySizeDAE(dae.clone());
                    sizeV = BackendVariable::daenumVariables(dae.clone());
                    Ok((sizeE.clone(), sizeV.clone(), m.clone(), mt.clone(), ass2.clone(), ass1.clone()))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outTpl)
}

fn setBackendVarMapping(mut dae: Arc<BackendDAE::BackendDAE>, mut ht: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>)), mut modelInfo: SimCode::ModelInfo, mut bmapIn: SimCode::BackendMapping) -> Result<SimCode::BackendMapping> {
    let mut bmapOut: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    bmapOut = 'mc: {
        let __mc_input = bmapIn.clone();
        if let Ok(__v) = (|| -> Result<_> {
            let _ = __mc_input.clone() else { bail!("nomatch") };
            let mut eqMatch: metamodelica::Array<i32> = Default::default();
            let mut varMatch: metamodelica::Array<i32> = Default::default();
            let mut tree: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
            let mut varInfo: SimCode::VarInfo = <SimCode::VarInfo as ::std::default::Default>::default();
            let mut allVars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
            let mut bVarIdcs: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut simVarIdcs: Arc<metamodelica::List<i32>> = metamodelica::nil();
            let mut eqs: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
            let mut vars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
            let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let mut simVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
            let mut eqMapping: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
            let mut varMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
            let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
            let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
            let mut simVarMapping: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>> = Default::default();
            let mut htStates: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
            let mut size: i32 = 0;
            let SimCode::BACKENDMAPPING { simVarMapping: __pa0, eqTree: __pa1, varMatch: __pa2, eqMatch: __pa3, varMapping: __pa4, eqMapping: __pa5, mT: __pa6, m: __pa7 } = (bmapIn.clone()) else { bail!("pattern mismatch") };
            simVarMapping = __pa0.clone();
            tree = __pa1.clone();
            varMatch = __pa2.clone();
            eqMatch = __pa3.clone();
            varMapping = __pa4.clone();
            eqMapping = __pa5.clone();
            mt = __pa6.clone();
            m = __pa7.clone();
            let __pa8 = ::match_deref::match_deref! { match &(dae.clone()) {
                Deref @ BackendDAE::BackendDAE { eqs: __pa8, .. } => __pa8.clone(),
                _ => bail!("pattern mismatch"),
            } };
            eqs = __pa8.clone();
            vars = BackendVariable::equationSystemsVarsLst(eqs.clone())?;
            crefs = List::map(vars.clone(), (std::sync::Arc::new(BackendVariable::varCref) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
            size = (crefs.clone().len() as i32);
            bVarIdcs = List::intRange(size.clone());
            simVars = List::map1(crefs.clone(), (std::sync::Arc::new(BaseHashTable::get) as std::sync::Arc<dyn ::std::ops::Fn(_, _) -> Result<_> + 'static>), ht.clone())?;
            let SimCode::MODELINFO { vars: __pa9, varInfo: __pa10, .. } = (modelInfo.clone()) else { bail!("pattern mismatch") };
            allVars = __pa9.clone();
            varInfo = __pa10.clone();
            htStates = List::fold(allVars.stateVars.clone(), (std::sync::Arc::new(HashTableCrefSimVar::addSimVarToHashTable) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))> + 'static>), HashTableCrefSimVar::emptyHashTableSized(1 + ((metamodelica::OrderedFloat(1.4_f64) * metamodelica::OrderedFloat((size.clone()) as f64)).0.floor() as i32)))?;
            simVarIdcs = List::map2(simVars.clone(), (std::sync::Arc::new(getSimVarIndex) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, SimCode::VarInfo, (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<i32> + 'static>), varInfo.clone(), htStates.clone())?;
            varMapping = makeVarMapTuple(simVarIdcs.clone(), bVarIdcs.clone(), metamodelica::nil())?;
            List::fold1(simVars.clone(), (std::sync::Arc::new(fillSimVarMapping) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar, metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>, i32) -> Result<i32> + 'static>), simVarMapping.clone(), 1)?;
            Ok(SimCode::BackendMapping::BACKENDMAPPING { m: m.clone(), mT: mt.clone(), eqMapping: eqMapping.clone(), varMapping: varMapping.clone(), eqMatch: eqMatch.clone(), varMatch: varMatch.clone(), eqTree: tree.clone(), simVarMapping: simVarMapping.clone() })
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let _ = __mc_input.clone() else { bail!("nomatch") };
            Ok(openmodelica_simcode_types::SimCode::BackendMapping::NO_MAPPING)
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(bmapOut)
}

fn fillSimVarMapping(mut iSimVar: SimCodeVar::SimVar, mut iSimVarMapping: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>>, mut iVarIdx: i32) -> Result<i32> {
    let mut oVarIdx: i32 = 0;
    {let _arr = iSimVarMapping.clone(); _arr.borrow_mut()[(iVarIdx.clone()-1) as usize] = list![iSimVar.clone()]; _arr};
    oVarIdx = iVarIdx.clone() + 1;
    Ok(oVarIdx)
}

fn getSimVarIndex(mut var: SimCodeVar::SimVar, mut varInfo: SimCode::VarInfo, mut htStates: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<i32> {
    let mut idx: i32 = 0;
    let mut offset: i32 = 0;
    if BaseHashTable::hasKey(var.name.clone(), htStates.clone())? {
        idx = var.index.clone();
    } else {
        offset = varInfo.numStateVars.clone();
        idx = var.index.clone();
        idx = idx.clone() + 2 * offset.clone();
    }
    Ok(idx)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn makeVarMapTuple(mut sVar: Arc<metamodelica::List<i32>>, mut bVar: Arc<metamodelica::List<i32>>, mut foldIn: Arc<metamodelica::List<(i32, i32)>>) -> Result<Arc<metamodelica::List<(i32, i32)>>> {
    let mut foldOut: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    foldOut = (::match_deref::match_deref! { match &((sVar.clone(), bVar.clone())) {
        (Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil) => {
            foldIn.clone()
        },
        (Deref @ metamodelica::List::Cons { head: i1, tail: rest1 }, Deref @ metamodelica::List::Cons { head: i2, tail: rest2 }) => {
            let mut fold: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
            fold = makeVarMapTuple(rest1.clone(), rest2.clone(), metamodelica::cons((i1.clone(), i2.clone()), foldIn.clone()))?;
            fold.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(foldOut)
}

fn setEqMapping(mut simEqs: Arc<metamodelica::List<i32>>, mut bEq: Arc<metamodelica::List<i32>>, mut mapIn: SimCode::BackendMapping) -> Result<SimCode::BackendMapping> {
    let mut mapOut: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    mapOut = (match mapIn.clone() {
        SimCode::BackendMapping::BACKENDMAPPING { simVarMapping: mut simVarMapping, eqTree: mut tree, varMatch: mut varMatch, eqMatch: mut eqMatch, varMapping: mut varMapping, eqMapping: mut eqMapping, mT: mut mt, m: mut m } => {
            let mut eqMapping = eqMapping.clone();
            eqMapping = List::fold1(simEqs.clone(), (std::sync::Arc::new(fnptr!(appendEqIdcs, i32, Arc<metamodelica::List<i32>>, Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>>)) as std::sync::Arc<dyn ::std::ops::Fn(i32, Arc<metamodelica::List<i32>>, Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>>) -> Result<Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>>> + 'static>), bEq.clone(), eqMapping.clone())?;
            SimCode::BackendMapping::BACKENDMAPPING { m: m.clone(), mT: mt.clone(), eqMapping: eqMapping.clone(), varMapping: varMapping.clone(), eqMatch: eqMatch.clone(), varMatch: varMatch.clone(), eqTree: tree.clone(), simVarMapping: simVarMapping.clone() }
        },
        SimCode::BackendMapping::NO_MAPPING { .. } => {
            mapIn.clone()
        },
    });
    Ok(mapOut)
}

fn appendEqIdcs(mut iCurrentIdx: i32, mut iEqIdx: Arc<metamodelica::List<i32>>, mut iSccIdc: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>>) -> Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> {
    let mut oSccIdc: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    oSccIdc = metamodelica::cons((iCurrentIdx.clone(), iEqIdx.clone()), iSccIdc.clone());
    oSccIdc
}

pub fn getSimVarsInSimEq(mut simEq: i32, mut map: SimCode::BackendMapping, mut opt: i32) -> Result<Arc<metamodelica::List<i32>>> {
    let mut simVars: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut bVars: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut bEqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut bVarsLst: Arc<metamodelica::List<Arc<metamodelica::List<i32>>>> = metamodelica::nil();
    let mut eqMapping: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let mut varMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let SimCode::BACKENDMAPPING { varMapping: __pa0, eqMapping: __pa1, mT: __pa2, m: __pa3, .. } = (map.clone()) else { bail!("pattern mismatch") };
    varMapping = __pa0.clone();
    eqMapping = __pa1.clone();
    mt = __pa2.clone();
    m = __pa3.clone();
    bEqs = getBackendEqsForSimEq(simEq.clone(), map.clone())?;
    bVarsLst = List::map1(bEqs.clone(), (std::sync::Arc::new(Array::getIndexFirst) as std::sync::Arc<dyn ::std::ops::Fn(i32, _) -> Result<_> + 'static>), m.clone())?;
    bVars = List::flatten(bVarsLst.clone())?;
    bVars = if (intEq(opt.clone(), 2)) {List::filter1OnTrue(bVars.clone(), (std::sync::Arc::new(fnptr!(intGt, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>), 0)?} else {bVars.clone()};
    bVars = if (intEq(opt.clone(), 3)) {List::filter1OnTrue(bVars.clone(), (std::sync::Arc::new(fnptr!(intLt, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>), 0)?} else {bVars.clone()};
    if !(List::isMemberOnTrue(opt.clone(), list![1, 2, 3], (std::sync::Arc::new(fnptr!(intEq, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>))?) {
        println!("{}", (literal!("invalid option for getSimVarsInSimEq\n")).clone());
    }
    bVars = List::unique(bVars.clone());
    bVars = List::map(bVars.clone(), Arc::new(fnptr!(intAbs, i32)))?;
    simVars = List::map1(bVars.clone(), (std::sync::Arc::new(getSimVarForBackendVar) as std::sync::Arc<dyn ::std::ops::Fn(i32, SimCode::BackendMapping) -> Result<i32> + 'static>), map.clone())?;
    Ok(simVars)
}

pub fn getSimEqsOfSimVar(mut simVar: i32, mut map: SimCode::BackendMapping, mut opt: i32) -> Result<Arc<metamodelica::List<i32>>> {
    let mut simEqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut bVar: i32 = 0;
    let mut bEqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut eqMapping: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let mut varMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let SimCode::BACKENDMAPPING { varMapping: __pa0, eqMapping: __pa1, mT: __pa2, m: __pa3, .. } = (map.clone()) else { bail!("pattern mismatch") };
    varMapping = __pa0.clone();
    eqMapping = __pa1.clone();
    mt = __pa2.clone();
    m = __pa3.clone();
    bVar = getBackendVarForSimVar(simVar.clone(), map.clone())?;
    bEqs = mt.clone().borrow()[(bVar.clone()-1) as usize].clone();
    bEqs = if (intEq(opt.clone(), 2)) {List::filter1OnTrue(bEqs.clone(), (std::sync::Arc::new(fnptr!(intGt, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>), 0)?} else {bEqs.clone()};
    bEqs = if (intEq(opt.clone(), 3)) {List::filter1OnTrue(bEqs.clone(), (std::sync::Arc::new(fnptr!(intLt, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>), 0)?} else {bEqs.clone()};
    if !(List::isMemberOnTrue(opt.clone(), list![1, 2, 3], (std::sync::Arc::new(fnptr!(intEq, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>))?) {
        println!("{}", (literal!("invalid option for getSimEqsOfSimVar\n")).clone());
    }
    bEqs = List::map(bEqs.clone(), Arc::new(fnptr!(intAbs, i32)))?;
    simEqs = List::map1(bEqs.clone(), (std::sync::Arc::new(getSimEqsForBackendEqs) as std::sync::Arc<dyn ::std::ops::Fn(i32, SimCode::BackendMapping) -> Result<i32> + 'static>), map.clone())?;
    simEqs = List::unique(simEqs.clone());
    Ok(simEqs)
}

pub fn getReqSimEqSysForSimVar(mut simVar: i32, mut simCode: SimCode::SimCode) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut ses: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut sesIdcs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut sesLst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut bmap: SimCode::BackendMapping = SimCode::BackendMapping::NO_MAPPING;
    let mut bmapOpt: Option<SimCode::BackendMapping> = None;
    let SimCode::SIMCODE { backendMapping: __pa0, allEquations: __pa1, .. } = (simCode.clone()) else { bail!("pattern mismatch") };
    bmapOpt = __pa0.clone();
    sesLst = __pa1.clone();
    bmap = Util::getOption(bmapOpt.clone())?;
    sesIdcs = getReqSimEqsForSimVar(simVar.clone(), bmap.clone())?;
    ses = List::map1(sesIdcs.clone(), (std::sync::Arc::new(getSimEqSysForIndex) as std::sync::Arc<dyn ::std::ops::Fn(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<SimCode::SimEqSystem>> + 'static>), sesLst.clone())?;
    Ok(ses)
}

pub fn getSimEqSysForIndex(mut idx: i32, mut allSimEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<SimCode::SimEqSystem>> {
    let mut outSimEq: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    if let Ok(__iflet0) = List::getMemberOnTrue(idx.clone(), allSimEqs.clone(), (std::sync::Arc::new(indexIsEqual) as std::sync::Arc<dyn ::std::ops::Fn(i32, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>)) {
        outSimEq = __iflet0;
    } else {
        println!("{}", (literal!("getSimEqSysForIndex failed!\n")).clone());
        bail!("fail");
    }
    Ok(outSimEq)
}

pub fn getSimVarMappingOfBackendMapping(mut iBackendMappingOpt: Option<SimCode::BackendMapping>) -> metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut oSimVarMapping: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>> = Default::default();
    let mut simVarMapping: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>> = Default::default();
    oSimVarMapping = (match iBackendMappingOpt.clone() {
        Some(SimCode::BackendMapping::BACKENDMAPPING { simVarMapping: mut simVarMapping, .. }) => simVarMapping.clone(),
        _ => arrayCreate(0, metamodelica::nil()),
    });
    oSimVarMapping
}

fn indexIsEqual(mut idx: i32, mut ses: Arc<SimCode::SimEqSystem>) -> Result<bool> {
    let mut b: bool = false;
    let mut idx2: i32 = 0;
    idx2 = simEqSystemIndex(ses.clone())?;
    b = intEq(idx.clone(), idx2.clone());
    Ok(b)
}

pub fn getReqSimEqsForSimVar(mut simVar: i32, mut map: SimCode::BackendMapping) -> Result<Arc<metamodelica::List<i32>>> {
    let mut simEqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut bVar: i32 = 0;
    let mut bEq: i32 = 0;
    let mut beqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut eqMatch: metamodelica::Array<i32> = Default::default();
    let mut varMatch: metamodelica::Array<i32> = Default::default();
    let mut tree: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let SimCode::BACKENDMAPPING { eqTree: __pa0, varMatch: __pa1, eqMatch: __pa2, mT: __pa3, m: __pa4, .. } = (map.clone()) else { bail!("pattern mismatch") };
    tree = __pa0.clone();
    varMatch = __pa1.clone();
    eqMatch = __pa2.clone();
    mt = __pa3.clone();
    m = __pa4.clone();
    bVar = getBackendVarForSimVar(simVar.clone(), map.clone())?;
    bEq = varMatch.clone().borrow()[(bVar.clone()-1) as usize].clone();
    beqs = collectReqSimEqs(bEq.clone(), tree.clone(), metamodelica::nil())?;
    simEqs = List::map1(beqs.clone(), (std::sync::Arc::new(getSimEqsForBackendEqs) as std::sync::Arc<dyn ::std::ops::Fn(i32, SimCode::BackendMapping) -> Result<i32> + 'static>), map.clone())?;
    simEqs = List::unique(simEqs.clone());
    Ok(simEqs)
}

pub fn getAssignedSimEqSysIdx(mut simVarIdx: i32, mut map: SimCode::BackendMapping) -> Result<i32> {
    let mut simEqSysIdx: i32 = 0;
    let mut bVarIdx: i32 = 0;
    let mut bEqIdx: i32 = 0;
    let mut varMatch: metamodelica::Array<i32> = Default::default();
    bVarIdx = getBackendVarForSimVar(simVarIdx.clone(), map.clone())?;
    let SimCode::BACKENDMAPPING { varMatch: __pa0, .. } = (map.clone()) else { bail!("pattern mismatch") };
    varMatch = __pa0.clone();
    bEqIdx = varMatch.clone().borrow()[(bVarIdx.clone()-1) as usize].clone();
    simEqSysIdx = getSimEqsForBackendEqs(bEqIdx.clone(), map.clone())?;
    Ok(simEqSysIdx)
}

fn collectReqSimEqs(mut eq: i32, mut tree: metamodelica::Array<Arc<metamodelica::List<i32>>>, mut eqsIn: Arc<metamodelica::List<i32>>) -> Result<Arc<metamodelica::List<i32>>> {
    let mut eqsOut: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut preEqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut reqEqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    preEqs = tree.clone().borrow()[(eq.clone()-1) as usize].clone();
    (_, preEqs, _) = List::intersection1OnTrue(preEqs.clone(), eqsIn.clone(), (std::sync::Arc::new(fnptr!(intEq, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<bool> + 'static>))?;
    reqEqs = listAppend(preEqs.clone(), eqsIn.clone());
    eqsOut = List::fold1(preEqs.clone(), (std::sync::Arc::new(collectReqSimEqs) as std::sync::Arc<dyn ::std::ops::Fn(i32, metamodelica::Array<Arc<metamodelica::List<i32>>>, Arc<metamodelica::List<i32>>) -> Result<Arc<metamodelica::List<i32>>> + 'static>), tree.clone(), reqEqs.clone())?;
    Ok(eqsOut)
}

fn getBackendVarForSimVar(mut simVar: i32, mut map: SimCode::BackendMapping) -> Result<i32> {
    let mut bVar: i32 = 0;
    let mut varMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let SimCode::BACKENDMAPPING { varMapping: __pa0, .. } = (map.clone()) else { bail!("pattern mismatch") };
    varMapping = __pa0.clone();
    (_, bVar) = List::getMemberOnTrue(simVar.clone(), varMapping.clone(), (std::sync::Arc::new(fnptr!(findSimVar, i32, (i32, i32))) as std::sync::Arc<dyn ::std::ops::Fn(i32, (i32, i32)) -> Result<bool> + 'static>))?;
    Ok(bVar)
}

fn getSimVarForBackendVar(mut bVar: i32, mut map: SimCode::BackendMapping) -> Result<i32> {
    let mut simVar: i32 = 0;
    let mut varMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let SimCode::BACKENDMAPPING { varMapping: __pa0, .. } = (map.clone()) else { bail!("pattern mismatch") };
    varMapping = __pa0.clone();
    (simVar, _) = List::getMemberOnTrue(bVar.clone(), varMapping.clone(), (std::sync::Arc::new(fnptr!(findBackendVar, i32, (i32, i32))) as std::sync::Arc<dyn ::std::ops::Fn(i32, (i32, i32)) -> Result<bool> + 'static>))?;
    Ok(simVar)
}

fn getBackendEqsForSimEq(mut simEq: i32, mut map: SimCode::BackendMapping) -> Result<Arc<metamodelica::List<i32>>> {
    let mut bEqs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut eqMapping: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let SimCode::BACKENDMAPPING { eqMapping: __pa0, .. } = (map.clone()) else { bail!("pattern mismatch") };
    eqMapping = __pa0.clone();
    (_, bEqs) = List::getMemberOnTrue(simEq.clone(), eqMapping.clone(), (std::sync::Arc::new(fnptr!(findSimEqs, i32, (i32, Arc<metamodelica::List<i32>>))) as std::sync::Arc<dyn ::std::ops::Fn(i32, (i32, Arc<metamodelica::List<i32>>)) -> Result<bool> + 'static>))?;
    Ok(bEqs)
}

fn getSimEqsForBackendEqs(mut bEq: i32, mut map: SimCode::BackendMapping) -> Result<i32> {
    let mut simEq: i32 = 0;
    let mut eqMapping: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let SimCode::BACKENDMAPPING { eqMapping: __pa0, .. } = (map.clone()) else { bail!("pattern mismatch") };
    eqMapping = __pa0.clone();
    (simEq, _) = List::getMemberOnTrue(bEq.clone(), eqMapping.clone(), (std::sync::Arc::new(fnptr!(findBEqs, i32, (i32, Arc<metamodelica::List<i32>>))) as std::sync::Arc<dyn ::std::ops::Fn(i32, (i32, Arc<metamodelica::List<i32>>)) -> Result<bool> + 'static>))?;
    Ok(simEq)
}

fn findSimVar(mut simVar: i32, mut varTpl: (i32, i32)) -> bool {
    let mut b: bool = false;
    let mut simVar1: i32 = 0;
    (simVar1, _) = varTpl.clone();
    b = intEq(simVar.clone(), simVar1.clone());
    b
}

fn findBackendVar(mut bVar: i32, mut varTpl: (i32, i32)) -> bool {
    let mut b: bool = false;
    let mut bVar1: i32 = 0;
    (_, bVar1) = varTpl.clone();
    b = intEq(bVar.clone(), bVar1.clone());
    b
}

fn findSimEqs(mut simEq: i32, mut eqTpl: (i32, Arc<metamodelica::List<i32>>)) -> bool {
    let mut b: bool = false;
    let mut simEq1: i32 = 0;
    (simEq1, _) = eqTpl.clone();
    b = intEq(simEq.clone(), simEq1.clone());
    b
}

fn findBEqs(mut bEq: i32, mut eqTpl: (i32, Arc<metamodelica::List<i32>>)) -> bool {
    let mut b: bool = false;
    let mut bEq1: Arc<metamodelica::List<i32>> = metamodelica::nil();
    (_, bEq1) = eqTpl.clone();
    b = listMember(bEq.clone(), bEq1.clone());
    b
}

pub fn getAssignedCrefsOfSimEq(mut idx: i32, mut simCode: SimCode::SimCode) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> {
    let mut crefsOut: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    crefsOut = (match simCode.clone() {
        SimCode::SimCode { allEquations: ref allEqs, .. } => {
            let mut simEqSyst: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
            let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            simEqSyst = List::getMemberOnTrue(idx.clone(), allEqs.clone(), (std::sync::Arc::new(indexIsEqual) as std::sync::Arc<dyn ::std::ops::Fn(i32, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>))?;
            crefs = getSimEqSystemCrefsLHS(simEqSyst.clone())?;
            crefs.clone()
        },
    });
    Ok(crefsOut)
}

fn getSimEqSystemCrefsLHS(mut simEqSys: Arc<SimCode::SimEqSystem>) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> {
    let mut crefsOut: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    crefsOut = (::match_deref::match_deref! { match &(simEqSys.clone()) {
        Deref @ SimCode::SimEqSystem::SES_RESIDUAL { .. } => {
            println!("{}", (literal!("implement SES_RESIDUAL in SimCodeUtil.getSimEqSystemCrefsLHS!\n")).clone());
            metamodelica::nil()
        },
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { cref, .. } => {
            list![cref.clone()]
        },
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { cref, .. } => {
            list![cref.clone()]
        },
        Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { lhs, .. } => {
            list![Expression::expCref(lhs.clone())?]
        },
        Deref @ SimCode::SimEqSystem::SES_IFEQUATION { .. } => {
            println!("{}", (literal!("implement SES_IFEQUATION in SimCodeUtil.getSimEqSystemCrefsLHS!\n")).clone());
            metamodelica::nil()
        },
        Deref @ SimCode::SimEqSystem::SES_ALGORITHM { .. } => {
            println!("{}", (literal!("implement SES_ALGORITHM in SimCodeUtil.getSimEqSystemCrefsLHS!\n")).clone());
            metamodelica::nil()
        },
        Deref @ SimCode::SimEqSystem::SES_INVERSE_ALGORITHM { .. } => {
            println!("{}", (literal!("implement SES_INVERSE_ALGORITHM in SimCodeUtil.getSimEqSystemCrefsLHS!\n")).clone());
            metamodelica::nil()
        },
        Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { residual, vars: simVars, .. }, .. } => {
            let mut crefs2: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            crefs2 = ({
        let mut __acc: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
        for mut v in (simVars.clone()).into_iter().cloned() {
            let __x = v.name.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
            listAppend(crefs2.clone(), crefs2.clone())
        },
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { crefs, .. }, .. } => {
            crefs.clone()
        },
        Deref @ SimCode::SimEqSystem::SES_MIXED { discVars: simVars, .. } => {
            ({
        let mut __acc: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
        for mut v in (simVars.clone()).into_iter().cloned() {
            let __x = v.name.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    })
        },
        Deref @ SimCode::SimEqSystem::SES_WHEN { whenStmtLst: Deref @ metamodelica::List::Cons { head: BackendDAE::WhenOperator::ASSIGN { left: lhs, .. }, tail: Deref @ metamodelica::List::Nil }, .. } => {
            let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            crefs = Expression::getAllCrefs(lhs.clone())?;
            crefs.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(crefsOut)
}

pub fn replaceSimVarName(mut cref: Arc<DAE::ComponentRef>, mut simVarIn: SimCodeVar::SimVar) -> Result<SimCodeVar::SimVar> {
    let mut simVarOut: SimCodeVar::SimVar = simVarIn.clone();
    simVarOut.name = cref.clone();
    simVarOut.arrayCref = ComponentReference::getArrayCref(cref.clone())?;
    Ok(simVarOut)
}

pub fn replaceSimVarIndex(mut idx: i32, mut simVarIn: SimCodeVar::SimVar) -> SimCodeVar::SimVar {
    let mut simVarOut: SimCodeVar::SimVar = simVarIn.clone();
    simVarOut.index = idx.clone();
    simVarOut
}

pub fn addSimVarToAlgVars(mut simVar: SimCodeVar::SimVar, mut simCodeIn: SimCode::SimCode) -> SimCode::SimCode {
    let mut simCodeOut: SimCode::SimCode = simCodeIn.clone();
    let mut modelInfo: SimCode::ModelInfo = <SimCode::ModelInfo as ::std::default::Default>::default();
    let mut vars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    modelInfo = simCodeOut.modelInfo.clone();
    vars = modelInfo.vars.clone();
    vars.algVars = listAppend(vars.algVars.clone(), list![simVar.clone()]);
    modelInfo.vars = vars.clone();
    simCodeOut.modelInfo = modelInfo.clone();
    simCodeOut
}

pub fn addSimEqSysToODEquations(mut simEqSys: Arc<SimCode::SimEqSystem>, mut sysIdx: i32, mut simCodeIn: SimCode::SimCode) -> Result<SimCode::SimCode> {
    let mut simCodeOut: SimCode::SimCode = simCodeIn.clone();
    let mut odes: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    odes = (simCodeOut.odeEquations.clone()).get(sysIdx.clone())?;
    odes = metamodelica::cons(simEqSys.clone(), odes.clone());
    simCodeOut.odeEquations = List::set(simCodeOut.odeEquations.clone(), sysIdx.clone(), odes.clone())?;
    simCodeOut.allEquations = metamodelica::cons(simEqSys.clone(), simCodeOut.allEquations.clone());
    Ok(simCodeOut)
}

pub fn addSimEqSysToInitialEquations(mut simEqSys: Arc<SimCode::SimEqSystem>, mut simCodeIn: SimCode::SimCode) -> SimCode::SimCode {
    let mut simCodeOut: SimCode::SimCode = simCodeIn.clone();
    simCodeOut.initialEquations = listAppend(simCodeOut.initialEquations.clone(), list![simEqSys.clone()]);
    simCodeOut
}

pub fn replaceODEandALLequations(mut allEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut odeEqs: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, mut simCodeIn: SimCode::SimCode) -> SimCode::SimCode {
    let mut simCodeOut: SimCode::SimCode = simCodeIn.clone();
    simCodeOut.allEquations = allEqs.clone();
    simCodeOut.odeEquations = odeEqs.clone();
    simCodeOut
}

pub fn replaceModelInfo(mut modelInfoIn: SimCode::ModelInfo, mut simCodeIn: SimCode::SimCode) -> SimCode::SimCode {
    let mut simCodeOut: SimCode::SimCode = simCodeIn.clone();
    simCodeOut.modelInfo = modelInfoIn.clone();
    simCodeOut
}

pub fn replaceSimEqSysIndex(mut simEqSysIn: Arc<SimCode::SimEqSystem>, mut inputIndex: i32) -> Result<Arc<SimCode::SimEqSystem>> {
    let mut simEqSysOut: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    simEqSysOut = (::match_deref::match_deref! { match &(simEqSysIn.clone()) {
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_RESIDUAL { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_RESIDUAL; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_FOR_RESIDUAL { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_FOR_RESIDUAL; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_GENERIC_RESIDUAL; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_SIMPLE_ASSIGN; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_IFEQUATION { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_IFEQUATION; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_ALGORITHM { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_ALGORITHM; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_INVERSE_ALGORITHM { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_INVERSE_ALGORITHM; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem, .. } => {
            let mut simEqSys = (*simEqSys).clone();
            let mut lSystem = (*lSystem).clone();
            assign_field!(lSystem.index = inputIndex.clone());
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_LINEAR; lSystem = lSystem.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem, .. } => {
            let mut simEqSys = (*simEqSys).clone();
            let mut nlSystem = (*nlSystem).clone();
            assign_field!(nlSystem.index = inputIndex.clone());
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_NONLINEAR; nlSystem = nlSystem.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_MIXED { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_MIXED; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_WHEN { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_WHEN; index = inputIndex.clone());
            simEqSys.clone()
        },
        simEqSys @ Deref @ SimCode::SimEqSystem::SES_FOR_LOOP { .. } => {
            let mut simEqSys = (*simEqSys).clone();
            assign_variant_field!(simEqSys => SimCode::SimEqSystem::SES_FOR_LOOP; index = inputIndex.clone());
            simEqSys.clone()
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(simEqSysOut)
}

pub fn getMaxSimEqSystemIndex(mut simCode: SimCode::SimCode) -> Result<i32> {
    let mut idxOut: i32 = 0;
    let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut jacobianEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut equationsForZeroCrossings: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut algorithmAndEquationAsserts: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut removedEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut parameterEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut maxValueEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut minValueEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut nominalValueEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut startValueEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut initialEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut odeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut algebraicEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let SimCode::SIMCODE { jacobianEquations: __pa0, equationsForZeroCrossings: __pa1, algorithmAndEquationAsserts: __pa2, removedEquations: __pa3, parameterEquations: __pa4, maxValueEquations: __pa5, minValueEquations: __pa6, nominalValueEquations: __pa7, startValueEquations: __pa8, initialEquations: __pa9, algebraicEquations: __pa10, odeEquations: __pa11, allEquations: __pa12, .. } = (simCode.clone()) else { bail!("pattern mismatch") };
    jacobianEquations = __pa0.clone();
    equationsForZeroCrossings = __pa1.clone();
    algorithmAndEquationAsserts = __pa2.clone();
    removedEquations = __pa3.clone();
    parameterEquations = __pa4.clone();
    maxValueEquations = __pa5.clone();
    minValueEquations = __pa6.clone();
    nominalValueEquations = __pa7.clone();
    startValueEquations = __pa8.clone();
    initialEquations = __pa9.clone();
    algebraicEquations = __pa10.clone();
    odeEquations = __pa11.clone();
    allEquations = __pa12.clone();
    for mut eq in &*jacobianEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*equationsForZeroCrossings.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*algorithmAndEquationAsserts.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*removedEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*parameterEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*maxValueEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*minValueEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*nominalValueEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*nominalValueEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*startValueEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*initialEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*allEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*jacobianEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*jacobianEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    for mut eq in &*jacobianEquations.clone() {
        let mut eq = eq.clone();
        idxOut = intMax(idxOut.clone(), simEqSystemIndex(eq.clone())?);
    }
    Ok(idxOut)
}

pub fn getLSindex(mut simEqSys: Arc<SimCode::SimEqSystem>) -> i32 {
    let mut lsIdx: i32 = 0;
    lsIdx = (::match_deref::match_deref! { match &(simEqSys.clone()) {
        Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { indexLinearSystem: idx, .. }, .. } => {
            idx.clone()
        },
        _ => {
            -1
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    lsIdx
}

pub fn getNLSindex(mut simEqSys: Arc<SimCode::SimEqSystem>) -> i32 {
    let mut nlsIdx: i32 = 0;
    nlsIdx = (::match_deref::match_deref! { match &(simEqSys.clone()) {
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { indexNonLinearSystem: idx, .. }, .. } => {
            idx.clone()
        },
        _ => {
            -1
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    nlsIdx
}

pub fn getMixedindex(mut simEqSys: Arc<SimCode::SimEqSystem>) -> i32 {
    let mut mIdx: i32 = 0;
    mIdx = (::match_deref::match_deref! { match &(simEqSys.clone()) {
        Deref @ SimCode::SimEqSystem::SES_MIXED { indexMixedSystem: idx, .. } => {
            idx.clone()
        },
        _ => {
            -1
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    mIdx
}

pub fn getRemovedEquationSimEqSysIdxes(mut simCode: SimCode::SimCode) -> Result<Arc<metamodelica::List<i32>>> {
    let mut simEqSysIdcs: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut remEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let SimCode::SIMCODE { removedEquations: __pa0, .. } = (simCode.clone()) else { bail!("pattern mismatch") };
    remEqs = __pa0.clone();
    simEqSysIdcs = List::map(remEqs.clone(), (std::sync::Arc::new(simEqSystemIndex) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>))?;
    Ok(simEqSysIdcs)
}

pub fn getDaeEqsNotPartOfOdeSystem(mut iSimCode: SimCode::SimCode) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut oEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut allEqs: metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>> = Default::default();
    let mut allEqIdxMapping: Arc<metamodelica::List<(i32, Arc<SimCode::SimEqSystem>)>> = metamodelica::nil();
    let mut allEquations: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut odeEquations: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut highestIdx: i32 = 0;
    let mut tmpEqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let SimCode::SIMCODE { odeEquations: __pa0, allEquations: __pa1, .. } = (iSimCode.clone()) else { bail!("pattern mismatch") };
    odeEquations = __pa0.clone();
    allEquations = __pa1.clone();
    (allEqIdxMapping, highestIdx) = List::fold(allEquations.clone(), (std::sync::Arc::new(getDaeEqsNotPartOfOdeSystem0) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, (Arc<metamodelica::List<(i32, Arc<SimCode::SimEqSystem>)>>, i32)) -> Result<(Arc<metamodelica::List<(i32, Arc<SimCode::SimEqSystem>)>>, i32)> + 'static>), (metamodelica::nil(), 0))?;
    allEqs = arrayCreate(highestIdx.clone(), None);
    allEqs = List::fold(allEqIdxMapping.clone(), (std::sync::Arc::new(getDaeEqsNotPartOfOdeSystem1) as std::sync::Arc<dyn ::std::ops::Fn((i32, Arc<SimCode::SimEqSystem>), metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>) -> Result<metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>> + 'static>), allEqs.clone())?;
    allEqs = List::fold(odeEquations.clone(), (std::sync::Arc::new(getDaeEqsNotPartOfOdeSystem2) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>) -> Result<metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>> + 'static>), allEqs.clone())?;
    tmpEqs = metamodelica::nil();
    tmpEqs = Array::fold(allEqs.clone(), (std::sync::Arc::new(fnptr!(getDaeEqsNotPartOfOdeSystem4, Option<Arc<SimCode::SimEqSystem>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>)) as std::sync::Arc<dyn ::std::ops::Fn(Option<Arc<SimCode::SimEqSystem>>, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> + 'static>), tmpEqs.clone())?;
    oEqs = Dangerous::listReverseInPlace(tmpEqs.clone());
    Ok(oEqs)
}

fn getDaeEqsNotPartOfOdeSystem0(mut iEqSystem: Arc<SimCode::SimEqSystem>, mut iMappingWithHighestIdx: (Arc<metamodelica::List<(i32, Arc<SimCode::SimEqSystem>)>>, i32)) -> Result<(Arc<metamodelica::List<(i32, Arc<SimCode::SimEqSystem>)>>, i32)> {
    let mut outMappingWithHighestIdx: (Arc<metamodelica::List<(i32, Arc<SimCode::SimEqSystem>)>>, i32) = (metamodelica::nil(), 0);
    let mut index: i32 = 0;
    let mut highestIdx: i32 = 0;
    let mut allEqIdxMapping: Arc<metamodelica::List<(i32, Arc<SimCode::SimEqSystem>)>> = metamodelica::nil();
    index = simEqSystemIndex(iEqSystem.clone())?;
    (allEqIdxMapping, highestIdx) = iMappingWithHighestIdx.clone();
    allEqIdxMapping = metamodelica::cons((index.clone(), iEqSystem.clone()), allEqIdxMapping.clone());
    highestIdx = intMax(highestIdx.clone(), index.clone());
    outMappingWithHighestIdx = (allEqIdxMapping.clone(), highestIdx.clone());
    Ok(outMappingWithHighestIdx)
}

fn getDaeEqsNotPartOfOdeSystem1(mut iEqSystem: (i32, Arc<SimCode::SimEqSystem>), mut iEqArray: metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>) -> Result<metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>> {
    let mut oEqArray: metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>> = Default::default();
    let mut eqSysIdx: i32 = 0;
    let mut eqSys: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    (eqSysIdx, eqSys) = iEqSystem.clone();
    oEqArray = {let _arr = iEqArray.clone(); _arr.borrow_mut()[(eqSysIdx.clone()-1) as usize] = Some(eqSys.clone()); _arr};
    Ok(oEqArray)
}

fn getDaeEqsNotPartOfOdeSystem2(mut iEqSystem: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut iEqArray: metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>) -> Result<metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>> {
    let mut oEqArray: metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>> = Default::default();
    oEqArray = List::fold(iEqSystem.clone(), (std::sync::Arc::new(getDaeEqsNotPartOfOdeSystem3) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>) -> Result<metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>> + 'static>), iEqArray.clone())?;
    Ok(oEqArray)
}

fn getDaeEqsNotPartOfOdeSystem3(mut iEqSystem: Arc<SimCode::SimEqSystem>, mut iEqArray: metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>) -> Result<metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>>> {
    let mut oEqArray: metamodelica::Array<Option<Arc<SimCode::SimEqSystem>>> = Default::default();
    let mut eqSysIdx: i32 = 0;
    eqSysIdx = simEqSystemIndex(iEqSystem.clone())?;
    oEqArray = {let _arr = iEqArray.clone(); _arr.borrow_mut()[(eqSysIdx.clone()-1) as usize] = None; _arr};
    Ok(oEqArray)
}

fn getDaeEqsNotPartOfOdeSystem4(mut iEqSystemOpt: Option<Arc<SimCode::SimEqSystem>>, mut iResList: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> {
    let mut oResList: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut eqSys: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    oResList = (::match_deref::match_deref! { match &(iEqSystemOpt.clone()) {
        Some(eqSys) => metamodelica::cons(eqSys.clone(), iResList.clone()),
        _ => iResList.clone(),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    oResList
}

pub fn dumpIdxScVarMapping(mut iMapping: metamodelica::Array<Option<SimCodeVar::SimVar>>) -> Result<()> {
    println!("{}", (literal!("Idx-ScVar-Mapping:\n")).clone());
    Array::fold(iMapping.clone(), (std::sync::Arc::new(dumpIdxScVarMapping0) as std::sync::Arc<dyn ::std::ops::Fn(Option<SimCodeVar::SimVar>, i32) -> Result<i32> + 'static>), 1)?;
    Ok(())
}

fn dumpIdxScVarMapping0(mut iVar: Option<SimCodeVar::SimVar>, mut iIdx: i32) -> Result<i32> {
    let mut oIdx: i32 = 0;
    let mut name: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut refString: ArcStr = arcstr::literal!("");
    oIdx = (match iVar.clone() {
        Some(SimCodeVar::SimVar { name: mut name, .. }) => {
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Idx: ")); __mm_s.push_str(&*intString(iIdx.clone())); __mm_s.push_str(&*literal!(" -- ")); ArcStr::from(__mm_s) }).clone());
            refString = (ComponentReferenceBasics::printComponentRefStr(name.clone())?).clone();
            println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*refString.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
            iIdx.clone() + 1
        },
        _ => iIdx.clone() + 1,
    });
    Ok(oIdx)
}

fn dumpBackendMapping(mut mapIn: SimCode::BackendMapping) -> Result<()> {
    let mut eqMatch: metamodelica::Array<i32> = Default::default();
    let mut varMatch: metamodelica::Array<i32> = Default::default();
    let mut tree: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut eqMapping: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let mut varMapping: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut m: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut mt: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut simVarMapping: metamodelica::Array<Arc<metamodelica::List<SimCodeVar::SimVar>>> = Default::default();
    let SimCode::BACKENDMAPPING { simVarMapping: __pa0, eqTree: __pa1, varMatch: __pa2, eqMatch: __pa3, varMapping: __pa4, eqMapping: __pa5, mT: __pa6, m: __pa7 } = (mapIn.clone()) else { bail!("pattern mismatch") };
    simVarMapping = __pa0.clone();
    tree = __pa1.clone();
    varMatch = __pa2.clone();
    eqMatch = __pa3.clone();
    varMapping = __pa4.clone();
    eqMapping = __pa5.clone();
    mt = __pa6.clone();
    m = __pa7.clone();
    dumpEqMapping(eqMapping.clone())?;
    Ok(())
}

fn dumpEqMapping(mut eqMapping: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>>) -> Result<()> {
    let mut lst: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let mut s: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    lst = eqMapping.clone().reverse();
    println!("{}", (literal!("------------\n")).clone());
    println!("{}", (literal!("BackendEquation ---> SimEqSys\n")).clone());
    (s, _) = List::mapFold(lst.clone(), (std::sync::Arc::new(dumpEqMappingTuple) as std::sync::Arc<dyn ::std::ops::Fn((i32, Arc<metamodelica::List<i32>>), i32) -> Result<(ArcStr, i32)> + 'static>), 1)?;
    println!("{}", stringDelimitList(s.clone(), (literal!("\n")).clone()));
    println!("{}", (literal!("\n------------\n")).clone());
    println!("{}", (literal!("\n")).clone());
    Ok(())
}

fn dumpVarMapping(mut varMapping: Arc<metamodelica::List<(i32, i32)>>) -> Result<()> {
    let mut lst: Arc<metamodelica::List<(i32, i32)>> = metamodelica::nil();
    let mut s: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    lst = varMapping.clone().reverse();
    println!("{}", (literal!("------------\n")).clone());
    println!("{}", (literal!("BackendVar ---> SimVar\n")).clone());
    (s, _) = List::mapFold(lst.clone(), (std::sync::Arc::new(fnptr!(dumpVarMappingTuple, (i32, i32), i32)) as std::sync::Arc<dyn ::std::ops::Fn((i32, i32), i32) -> Result<(ArcStr, i32)> + 'static>), 1)?;
    println!("{}", stringDelimitList(s.clone(), (literal!("\n")).clone()));
    println!("{}", (literal!("\n------------\n")).clone());
    println!("{}", (literal!("\n")).clone());
    Ok(())
}

fn dumpEqMappingTuple(mut tpl: (i32, Arc<metamodelica::List<i32>>), mut noIn: i32) -> Result<(ArcStr, i32)> {
    let mut s: ArcStr = arcstr::literal!("");
    let mut noOut: i32 = 0;
    let mut i1: i32 = 0;
    let mut lst: Arc<metamodelica::List<i32>> = metamodelica::nil();
    (i1, lst) = tpl.clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(noIn.clone())); __mm_s.push_str(&*literal!("): ")); __mm_s.push_str(&*stringDelimitList(List::map(lst.clone(), (std::sync::Arc::new(fnptr!(intString, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))?, (literal!(",")).clone())); __mm_s.push_str(&*literal!(" ---> ")); __mm_s.push_str(&*intString(i1.clone())); ArcStr::from(__mm_s) }).clone();
    noOut = noIn.clone() + 1;
    Ok((s, noOut))
}

fn dumpVarMappingTuple(mut tpl: (i32, i32), mut noIn: i32) -> (ArcStr, i32) {
    let mut s: ArcStr = arcstr::literal!("");
    let mut noOut: i32 = 0;
    let mut i1: i32 = 0;
    let mut i2: i32 = 0;
    (i1, i2) = tpl.clone();
    s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*intString(noIn.clone())); __mm_s.push_str(&*literal!("): ")); __mm_s.push_str(&*intString(i2.clone())); __mm_s.push_str(&*literal!(" ---> ")); __mm_s.push_str(&*intString(i1.clone())); ArcStr::from(__mm_s) }).clone();
    noOut = noIn.clone() + 1;
    (s, noOut)
}

pub fn dumpOMSIData(mut omsiData: SimCode::OMSIData, mut head: ArcStr) -> () {
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*head.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    println!("{}", (literal!("OMSIFunction initialization:\n")).clone());
    dumpOMSIFunc(omsiData.initialization.clone(), (literal!("")).clone());
    println!("{}", (literal!("----------------------\n")).clone());
    println!("{}", (literal!("OMSIFunction simulation:\n")).clone());
    dumpOMSIFunc(omsiData.simulation.clone(), (literal!("")).clone());
    ()
}

pub fn dumpOMSIFunc(mut omsiFunc: Arc<SimCode::OMSIFunction>, mut head: ArcStr) -> () {
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*head.clone()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    if '__try0: {
        println!("{}", (literal!("equations:\n")).clone());
        println!("{}", (literal!("----------------------\n")).clone());
        unwrap_break_err!(dumpSimEqSystemLst(omsiFunc.equations.clone(), (literal!("\n")).clone()), '__try0);
        unwrap_break_err!(dumpVarLst(omsiFunc.inputVars.clone(), (literal!("inputVars")).clone()), '__try0);
        unwrap_break_err!(dumpVarLst(omsiFunc.innerVars.clone(), (literal!("innerVars")).clone()), '__try0);
        unwrap_break_err!(dumpVarLst(omsiFunc.outputVars.clone(), (literal!("outputVars")).clone()), '__try0);
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("numer of all vars: ")); __mm_s.push_str(&*ArcStr::from(::std::format!("{}", omsiFunc.nAllVars.clone()))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        println!("{}", (literal!("Context\n")).clone());
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("number of algebraic systems: ")); __mm_s.push_str(&*ArcStr::from(::std::format!("{}", omsiFunc.nAlgebraicSystems.clone()))); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
        Ok::<(), anyhow::Error>(())
    }.is_err() {
        println!("{}", (literal!("ERROR in dumpOMSIFunc\n")).clone());
    }
    ()
}

pub fn omsiFuncEqnString(mut omsiFunc: Arc<SimCode::OMSIFunction>) -> Result<ArcStr> {
    let mut s: ArcStr = literal!("");
    for mut eqs in &*omsiFunc.equations.clone() {
        let mut eqs = eqs.clone();
        s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*simEqSystemString(eqs.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    }
    Ok(s)
}

pub fn derivativeMatrixString(mut matrix: Arc<SimCode::DerivativeMatrix>) -> Result<ArcStr> {
    let mut s: ArcStr = literal!("");
    for mut col in &*matrix.columns.clone() {
        let mut col = col.clone();
        s = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*s.clone()); __mm_s.push_str(&*omsiFuncEqnString(col.clone())?); ArcStr::from(__mm_s) }).clone();
    }
    Ok(s)
}

pub fn createFMISimulationFlags(mut printWarning: bool) -> Result<Option<SimCode::FmiSimulationFlags>> {
    let mut fmiSimulationFlags: Option<SimCode::FmiSimulationFlags> = None;
    let mut fmiFlagsList: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut pathToFile: ArcStr = arcstr::literal!("");
    let mut msg: ArcStr = arcstr::literal!("");
    let mut tmpName: ArcStr = arcstr::literal!("");
    let mut tmpValue: ArcStr = arcstr::literal!("");
    let mut tmpSplitted: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut nameValueTuples: Arc<metamodelica::List<(ArcStr, ArcStr)>> = metamodelica::nil();
    fmiFlagsList = Flags::getConfigStringList(Flags::FMI_FLAGS.clone())?;
    if fmiFlagsList.clone().is_empty() {
        fmiSimulationFlags = None;
    } else if (fmiFlagsList.clone().len() as i32) == 1 && stringEqual((listHead(fmiFlagsList.clone())?).clone(), (literal!("none")).clone()) {
        fmiSimulationFlags = None;
    } else if (fmiFlagsList.clone().len() as i32) == 1 && stringEqual((listHead(fmiFlagsList.clone())?).clone(), (literal!("default")).clone()) {
        fmiSimulationFlags = Some(SimCode::defaultFmiSimulationFlags.clone());
    } else if (fmiFlagsList.clone().len() as i32) == 1 && stringEqual((List::last(Util::stringSplitAtChar((listHead(fmiFlagsList.clone())?).clone(), (literal!(".")).clone())?)?).clone(), (literal!("json")).clone()) {
        pathToFile = (listHead(fmiFlagsList.clone())?).clone();
        if System::regularFileExists((pathToFile.clone()).clone()) {
            fmiSimulationFlags = Some(SimCode::FmiSimulationFlags::FMI_SIMULATION_FLAGS_FILE { path: (pathToFile.clone()).clone() });
            return Ok(fmiSimulationFlags.clone());
        } else if System::regularFileExists((Util::absoluteOrRelative((pathToFile.clone()).clone())).clone()) {
            fmiSimulationFlags = Some(SimCode::FmiSimulationFlags::FMI_SIMULATION_FLAGS_FILE { path: (Util::absoluteOrRelative((pathToFile.clone()).clone())).clone() });
            return Ok(fmiSimulationFlags.clone());
        } else {
            if printWarning.clone() {
                msg = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Could not find file \"")); __mm_s.push_str(&*pathToFile.clone()); __mm_s.push_str(&*literal!("\nIgnoring \"--fmiFlags=")); __mm_s.push_str(&*pathToFile.clone()); __mm_s.push_str(&*literal!("\" and using default setting.\n")); ArcStr::from(__mm_s) }).clone();
                Error::addCompilerWarning((msg.clone()).clone())?;
            }
            fmiSimulationFlags = Some(SimCode::defaultFmiSimulationFlags.clone());
            return Ok(fmiSimulationFlags.clone());
        }
    } else {
        for mut flag in &*fmiFlagsList.clone() {
            let mut flag = flag.clone();
            tmpSplitted = Util::stringSplitAtChar((flag.clone()).clone(), (literal!(":")).clone())?;
            if !((tmpSplitted.clone().len() as i32) == 2) {
                if printWarning.clone() {
                    msg = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Can't process flag \"")); __mm_s.push_str(&*flag.clone()); __mm_s.push_str(&*literal!("\".\nSeperate flag name and flag value with \":\".\n")); ArcStr::from(__mm_s) }).clone();
                    Error::addCompilerWarning((msg.clone()).clone())?;
                }
                fmiSimulationFlags = Some(SimCode::defaultFmiSimulationFlags.clone());
                return Ok(fmiSimulationFlags.clone());
            }
            let (__pa0, __pa1) = ::match_deref::match_deref! { match &(tmpSplitted.clone()) {
                Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Cons { head: __pa1, tail: Deref @ metamodelica::List::Nil } } => (__pa0.clone(), __pa1.clone()),
                _ => bail!("pattern mismatch"),
            } };
            tmpName = __pa0.clone();
            tmpValue = __pa1.clone();
            if stringEqual((tmpName.clone()).clone(), (literal!("s")).clone()) {
                if !(stringEqual((tmpValue.clone()).clone(), (literal!("euler")).clone())) && !(stringEqual((tmpValue.clone()).clone(), (literal!("cvode")).clone())) {
                    if printWarning.clone() {
                        msg = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Unknown value \"")); __mm_s.push_str(&*tmpValue.clone()); __mm_s.push_str(&*literal!("\" for flag \"s\".")); ArcStr::from(__mm_s) }).clone();
                        Error::addCompilerWarning((msg.clone()).clone())?;
                    }
                }
            } else {
                if printWarning.clone() {
                    msg = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Adding unknown FMU simulation flag \"")); __mm_s.push_str(&*tmpName.clone()); __mm_s.push_str(&*literal!("\" .")); ArcStr::from(__mm_s) }).clone();
                    Error::addCompilerWarning((msg.clone()).clone())?;
                }
            }
            nameValueTuples = metamodelica::cons((tmpName.clone(), tmpValue.clone()), nameValueTuples.clone());
        }
        nameValueTuples = Dangerous::listReverseInPlace(nameValueTuples.clone());
        fmiSimulationFlags = Some(SimCode::FmiSimulationFlags::FMI_SIMULATION_FLAGS { nameValueTuples: nameValueTuples.clone() });
    }
    Ok(fmiSimulationFlags)
}

pub fn createFMIModelStructure(mut inSymjacs: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>>, mut inModelInfo: SimCode::ModelInfo, mut inUniqueEqIndex: i32, mut inInitDAE: Arc<BackendDAE::BackendDAE>, mut inSimDAE: Arc<BackendDAE::BackendDAE>) -> Result<(Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>, Option<SimCode::FmiModelStructure>, SimCode::ModelInfo, Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>>, i32)> {
    let mut symJacFMI: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut outFmiModelStructure: Option<SimCode::FmiModelStructure> = None;
    let mut outModelInfo: SimCode::ModelInfo = inModelInfo.clone();
    let mut symJacs: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut uniqueEqIndex: i32 = inUniqueEqIndex.clone();
    let mut spTA: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>> = metamodelica::nil();
    let mut spTA1: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>> = metamodelica::nil();
    let mut sparseInts: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let mut allUnknowns: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
    let mut derivatives: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
    let mut outputs: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
    let mut discreteStates: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
    let mut allInitialUnknowns: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
    let mut varsA: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut varsB: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut varsC: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut varsD: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut clockedStates: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut allOutputVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut allParamVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut tmpInitialUnknowns: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut diffCrefsA: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut diffCrefsA1: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut diffedCrefsA: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut diffedCrefsA1: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut derdiffCrefsA: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut optcontPartDer: Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> = None;
    let mut optinitialPartDer: Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> = None;
    let mut spPattern: (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32) = (metamodelica::nil(), metamodelica::nil(), (metamodelica::nil(), metamodelica::nil()), 0);
    let mut spPattern1: (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32) = (metamodelica::nil(), metamodelica::nil(), (metamodelica::nil(), metamodelica::nil()), 0);
    let mut spColors: Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>> = metamodelica::nil();
    let mut spColors1: Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>> = metamodelica::nil();
    let mut nlPattern: (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32) = (metamodelica::nil(), metamodelica::nil(), (metamodelica::nil(), metamodelica::nil()), 0);
    let mut contPartDer: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>> = metamodelica::nil();
    let mut initPartDer: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>> = metamodelica::nil();
    let mut contSimJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
    let mut initSimJac: Arc<SimCode::JacobianMatrix> = Arc::new(<SimCode::JacobianMatrix as ::std::default::Default>::default());
    let mut contPartSimDer: Option<Arc<SimCode::JacobianMatrix>> = None;
    let mut initPartSimDer: Option<Arc<SimCode::JacobianMatrix>> = None;
    let mut vars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    let mut crefSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
    let mut intLst: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut fmiDerInit: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>> = metamodelica::nil();
    let mut symJacsInit: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut symJacFMIINIT: Arc<metamodelica::List<Arc<SimCode::JacobianMatrix>>> = metamodelica::nil();
    let mut sortedUnknownCrefs: Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    let mut sortedknownCrefs: Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    match '__try0: {
        let (__pa1, __pa5, __pa2, __pa3, __pa4, __pa6, __pa7) = ::match_deref::match_deref! { match &(SymbolicJacobian::getJacobianMatrixbyName(inSymjacs.clone(), (literal!("FMIDER")).clone())) {
            Some((__pa1, __pa5 @ (_, __pa2, (__pa3, __pa4), _), __pa6, __pa7)) => (__pa1.clone(), __pa5.clone(), __pa2.clone(), __pa3.clone(), __pa4.clone(), __pa6.clone(), __pa7.clone()),
            _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
        } };
        optcontPartDer = __pa1.clone();
        spTA = __pa2.clone();
        diffCrefsA = __pa3.clone();
        diffedCrefsA = __pa4.clone();
        spPattern = __pa5.clone();
        spColors = __pa6.clone();
        nlPattern = __pa7.clone();
        crefSimVarHT = unwrap_break_err!(createCrefToSimVarHT(inModelInfo.clone()), '__try0);
        (spTA, derdiffCrefsA) = unwrap_break_err!(translateSparsePatterCref2DerCref(spTA.clone(), crefSimVarHT.clone(), metamodelica::nil(), metamodelica::nil()), '__try0);
        varsA = getSimVars2Crefs(diffedCrefsA.clone(), crefSimVarHT.clone());
        varsB = getSimVars2Crefs(derdiffCrefsA.clone(), crefSimVarHT.clone());
        varsC = listAppend(varsA.clone(), varsB.clone());
        varsD = getSimVars2Crefs(diffCrefsA.clone(), crefSimVarHT.clone());
        varsA = listAppend(varsC.clone(), varsD.clone());
        sparseInts = unwrap_break_err!(sortSparsePattern(varsA.clone(), spTA.clone(), true), '__try0);
        allUnknowns = translateSparsePatterInts2FMIUnknown(sparseInts.clone(), metamodelica::nil());
        intLst = ({
        let mut __acc: Arc<metamodelica::List<i32>> = metamodelica::nil();
        for mut v in (inModelInfo.vars.derivativeVars.clone()).into_iter().cloned() {
            let __x = getVariableFMIIndex(v.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
        derivatives = ({
        let mut __acc: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
        for mut fmiUnknown in (allUnknowns.clone()).into_iter().cloned() {
            if !(unwrap_break_err!(List::any(intLst.clone(), (std::sync::Arc::new({ let __pe_b1 = fmiUnknown.clone(); move |__pe_a0| Ok(isFmiUnknown(__pe_a0, __pe_b1.clone())) }) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<bool> + 'static>)), '__try0)) { continue; }
            let __x = fmiUnknown.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
        varsA = unwrap_break_err!(List::filterOnTrue(inModelInfo.vars.algVars.clone(), (std::sync::Arc::new(fnptr!(isOutputSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>)), '__try0);
        varsB = unwrap_break_err!(List::filterOnTrue(inModelInfo.vars.intAlgVars.clone(), (std::sync::Arc::new(fnptr!(isOutputSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>)), '__try0);
        varsC = unwrap_break_err!(List::filterOnTrue(inModelInfo.vars.boolAlgVars.clone(), (std::sync::Arc::new(fnptr!(isOutputSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>)), '__try0);
        varsD = unwrap_break_err!(List::filterOnTrue(inModelInfo.vars.stringAlgVars.clone(), (std::sync::Arc::new(fnptr!(isOutputSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>)), '__try0);
        allOutputVars = listAppend(listAppend(varsA.clone(), varsB.clone()), listAppend(varsC.clone(), varsD.clone()));
        intLst = ({
        let mut __acc: Arc<metamodelica::List<i32>> = metamodelica::nil();
        for mut v in (allOutputVars.clone()).into_iter().cloned() {
            let __x = getVariableFMIIndex(v.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
        outputs = ({
        let mut __acc: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
        for mut fmiUnknown in (allUnknowns.clone()).into_iter().cloned() {
            if !(unwrap_break_err!(List::any(intLst.clone(), (std::sync::Arc::new({ let __pe_b1 = fmiUnknown.clone(); move |__pe_a0| Ok(isFmiUnknown(__pe_a0, __pe_b1.clone())) }) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<bool> + 'static>)), '__try0)) { continue; }
            let __x = fmiUnknown.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
        clockedStates = unwrap_break_err!(List::filterOnTrue(inModelInfo.vars.algVars.clone(), (std::sync::Arc::new(fnptr!(isClockedStateSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>)), '__try0);
        intLst = ({
        let mut __acc: Arc<metamodelica::List<i32>> = metamodelica::nil();
        for mut v in (clockedStates.clone()).into_iter().cloned() {
            let __x = getVariableFMIIndex(v.clone());
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
        discreteStates = ({
        let mut __acc: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
        for mut fmiUnknown in (allUnknowns.clone()).into_iter().cloned() {
            if !(unwrap_break_err!(List::any(intLst.clone(), (std::sync::Arc::new({ let __pe_b1 = fmiUnknown.clone(); move |__pe_a0| Ok(isFmiUnknown(__pe_a0, __pe_b1.clone())) }) as std::sync::Arc<dyn ::std::ops::Fn(i32) -> Result<bool> + 'static>)), '__try0)) { continue; }
            let __x = fmiUnknown.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
        if !(checkForEmptyBDAE(optcontPartDer.clone())) {
            contPartDer = list![(optcontPartDer.clone(), spPattern.clone(), spColors.clone(), nlPattern.clone())];
            let (__pa8, __pa9) = ::match_deref::match_deref! { match &(unwrap_break_err!(createSymbolicJacobianssSimCode(contPartDer.clone(), crefSimVarHT.clone(), uniqueEqIndex.clone(), list![(literal!("FMIDER")).clone()], metamodelica::nil()), '__try0)) {
                (Deref @ metamodelica::List::Cons { head: __pa8, tail: Deref @ metamodelica::List::Nil }, __pa9) => (__pa8.clone(), __pa9.clone()),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            contSimJac = __pa8.clone();
            uniqueEqIndex = __pa9.clone();
            let (__pa11, __pa12, __pa13) = ::match_deref::match_deref! { match &(addAlgebraicLoopsModelInfoSymJacs(list![contSimJac.clone()], inModelInfo.clone())) {
                (Deref @ metamodelica::List::Cons { head: __pa11, tail: Deref @ metamodelica::List::Nil }, __pa12, __pa13) => (__pa11.clone(), __pa12.clone(), __pa13.clone()),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            contSimJac = __pa11.clone();
            outModelInfo = __pa12.clone();
            symJacs = __pa13.clone();
            contPartSimDer = Some(contSimJac.clone());
            symJacFMI = list![rewriteJacPartIdx(contSimJac.clone(), inModelInfo.nSubClocks.clone())];
        } else {
            contPartSimDer = None;
        }
        tmpInitialUnknowns = metamodelica::nil();
        tmpInitialUnknowns = unwrap_break_err!(List::filterCons(allOutputVars.clone(), (std::sync::Arc::new(fnptr!(isInitialApproxOrCalculatedSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>), tmpInitialUnknowns.clone()), '__try0);
        allParamVars = getAllParamSimVars(inModelInfo.clone());
        tmpInitialUnknowns = unwrap_break_err!(List::filterCons(allParamVars.clone(), (std::sync::Arc::new(fnptr!(isCausalityCalculatedParameterSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>), tmpInitialUnknowns.clone()), '__try0);
        tmpInitialUnknowns = unwrap_break_err!(List::filterCons(inModelInfo.vars.stateVars.clone(), (std::sync::Arc::new(fnptr!(isInitialApproxOrCalculatedSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>), tmpInitialUnknowns.clone()), '__try0);
        tmpInitialUnknowns = unwrap_break_err!(List::filterCons(inModelInfo.vars.derivativeVars.clone(), (std::sync::Arc::new(fnptr!(isInitialApproxOrCalculatedSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>), tmpInitialUnknowns.clone()), '__try0);
        tmpInitialUnknowns = Dangerous::listReverseInPlace(tmpInitialUnknowns.clone());
        if !(tmpInitialUnknowns.clone().is_empty()) {
            (allInitialUnknowns, fmiDerInit, sortedUnknownCrefs, sortedknownCrefs) = unwrap_break_err!(getFmiInitialUnknowns(inInitDAE.clone(), inSimDAE.clone(), crefSimVarHT.clone(), tmpInitialUnknowns.clone()), '__try0);
        } else {
            allInitialUnknowns = metamodelica::nil();
        }
        if !(fmiDerInit.clone().is_empty()) {
            let (__pa15, __pa19, __pa16, __pa17, __pa18, __pa20, __pa21) = ::match_deref::match_deref! { match &(SymbolicJacobian::getJacobianMatrixbyName(fmiDerInit.clone(), (literal!("FMIDERINIT")).clone())) {
                Some((__pa15, __pa19 @ (_, __pa16, (__pa17, __pa18), _), __pa20, __pa21)) => (__pa15.clone(), __pa19.clone(), __pa16.clone(), __pa17.clone(), __pa18.clone(), __pa20.clone(), __pa21.clone()),
                _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
            } };
            optinitialPartDer = __pa15.clone();
            spTA1 = __pa16.clone();
            diffCrefsA1 = __pa17.clone();
            diffedCrefsA1 = __pa18.clone();
            spPattern1 = __pa19.clone();
            spColors1 = __pa20.clone();
            nlPattern = __pa21.clone();
            if !(checkForEmptyBDAE(optinitialPartDer.clone())) {
                initPartDer = list![(optinitialPartDer.clone(), spPattern1.clone(), spColors1.clone(), nlPattern.clone())];
                let (__pa22, __pa23) = ::match_deref::match_deref! { match &(unwrap_break_err!(createSymbolicJacobianssSimCode(initPartDer.clone(), crefSimVarHT.clone(), uniqueEqIndex.clone(), list![(literal!("FMIDERINIT")).clone()], metamodelica::nil()), '__try0)) {
                    (Deref @ metamodelica::List::Cons { head: __pa22, tail: Deref @ metamodelica::List::Nil }, __pa23) => (__pa22.clone(), __pa23.clone()),
                    _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
                } };
                initSimJac = __pa22.clone();
                uniqueEqIndex = __pa23.clone();
                let (__pa25, __pa26) = ::match_deref::match_deref! { match &(addAlgebraicLoopsModelInfoSymJacs(list![initSimJac.clone()], inModelInfo.clone())) {
                    (Deref @ metamodelica::List::Cons { head: __pa25, tail: Deref @ metamodelica::List::Nil }, _, __pa26) => (__pa25.clone(), __pa26.clone()),
                    _ => break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")),
                } };
                initSimJac = __pa25.clone();
                symJacsInit = __pa26.clone();
                initPartSimDer = Some(initSimJac.clone());
                symJacFMIINIT = list![rewriteJacPartIdx(initSimJac.clone(), inModelInfo.nSubClocks.clone())];
                symJacFMI = listAppend(symJacFMIINIT.clone(), symJacFMI.clone());
                symJacs = listAppend(symJacsInit.clone(), symJacs.clone());
            }
        }
        outFmiModelStructure = Some(SimCode::FmiModelStructure { fmiOutputs: SimCode::FmiOutputs { fmiUnknownsList: outputs.clone() }, fmiDerivatives: SimCode::FmiDerivatives { fmiUnknownsList: derivatives.clone() }, continuousPartialDerivatives: contPartSimDer.clone(), initialPartialDerivatives: initPartSimDer.clone(), fmiDiscreteStates: SimCode::FmiDiscreteStates { fmiUnknownsList: discreteStates.clone() }, fmiInitialUnknowns: SimCode::FmiInitialUnknowns { fmiUnknownsList: allInitialUnknowns.clone(), sortedUnknownCrefs: sortedUnknownCrefs.clone(), sortedknownCrefs: sortedknownCrefs.clone() } });
        Ok::<_, anyhow::Error>((clockedStates.clone(), contPartSimDer.clone(), derivatives.clone(), discreteStates.clone(), outFmiModelStructure.clone(), outputs.clone(), varsA.clone()))
    } {
        Ok((__try0_o0, __try0_o1, __try0_o2, __try0_o3, __try0_o4, __try0_o5, __try0_o6)) => {
            clockedStates = __try0_o0;
            contPartSimDer = __try0_o1;
            derivatives = __try0_o2;
            discreteStates = __try0_o3;
            outFmiModelStructure = __try0_o4;
            outputs = __try0_o5;
            varsA = __try0_o6;
        }
        Err(_) => {
            match '__try28: {
                derivatives = ({
        let mut __acc: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
        for mut v in (unwrap_break_err!(getScalarVars(inModelInfo.vars.derivativeVars.clone()), '__try28)).into_iter().cloned() {
            let __x = SimCode::FmiUnknown { index: getVariableFMIIndex(v.clone()), dependencies: metamodelica::nil(), dependenciesKind: metamodelica::nil() };
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                varsA = unwrap_break_err!(List::filterOnTrue(inModelInfo.vars.algVars.clone(), (std::sync::Arc::new(fnptr!(isOutputSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>)), '__try28);
                outputs = ({
        let mut __acc: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
        for mut v in (unwrap_break_err!(getScalarVars(varsA.clone()), '__try28)).into_iter().cloned() {
            let __x = SimCode::FmiUnknown { index: getVariableFMIIndex(v.clone()), dependencies: metamodelica::nil(), dependenciesKind: metamodelica::nil() };
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                clockedStates = unwrap_break_err!(List::filterOnTrue(inModelInfo.vars.algVars.clone(), (std::sync::Arc::new(fnptr!(isClockedStateSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<bool> + 'static>)), '__try28);
                discreteStates = ({
        let mut __acc: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
        for mut v in (unwrap_break_err!(getScalarVars(clockedStates.clone()), '__try28)).into_iter().cloned() {
            let __x = SimCode::FmiUnknown { index: getVariableFMIIndex(v.clone()), dependencies: metamodelica::nil(), dependenciesKind: metamodelica::nil() };
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
                contPartSimDer = None;
                initPartSimDer = None;
                outFmiModelStructure = Some(SimCode::FmiModelStructure { fmiOutputs: SimCode::FmiOutputs { fmiUnknownsList: outputs.clone() }, fmiDerivatives: SimCode::FmiDerivatives { fmiUnknownsList: derivatives.clone() }, continuousPartialDerivatives: contPartSimDer.clone(), initialPartialDerivatives: initPartSimDer.clone(), fmiDiscreteStates: SimCode::FmiDiscreteStates { fmiUnknownsList: discreteStates.clone() }, fmiInitialUnknowns: SimCode::FmiInitialUnknowns { fmiUnknownsList: metamodelica::nil(), sortedUnknownCrefs: metamodelica::nil(), sortedknownCrefs: metamodelica::nil() } });
                Ok::<_, anyhow::Error>((clockedStates.clone(), contPartSimDer.clone(), derivatives.clone(), discreteStates.clone(), initPartSimDer.clone(), outFmiModelStructure.clone(), outputs.clone(), varsA.clone()))
            } {
                Ok((__try28_o0, __try28_o1, __try28_o2, __try28_o3, __try28_o4, __try28_o5, __try28_o6, __try28_o7)) => {
                    clockedStates = __try28_o0;
                    contPartSimDer = __try28_o1;
                    derivatives = __try28_o2;
                    discreteStates = __try28_o3;
                    initPartSimDer = __try28_o4;
                    outFmiModelStructure = __try28_o5;
                    outputs = __try28_o6;
                    varsA = __try28_o7;
                }
                Err(__try28_err) => {
                    Error::addInternalError((literal!("SimCodeUtil.createFMIModelStructure failed")).clone(), metamodelica::sourceInfo!())?;
                    return Err(__try28_err);
                }
            }
        }
    }
    Ok((symJacFMI, outFmiModelStructure, outModelInfo, symJacs, uniqueEqIndex))
}

fn isInitialApproxOrCalculatedSimVar(mut simVar: SimCodeVar::SimVar) -> bool {
    let mut outBoolean: bool = false;
    let mut default_causality: SimCodeVar::Causality = openmodelica_simcode_types::SimCodeVar::Causality::LOCAL;
    let mut default_variability: SimCodeVar::Variability = openmodelica_simcode_types::SimCodeVar::Variability::CONTINUOUS;
    let mut default_initial: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
    outBoolean = (match simVar.clone() {
        SimCodeVar::SimVar { initial_: Some(SimCodeVar::Initial::CALCULATED { .. }), .. } => true,
        SimCodeVar::SimVar { initial_: Some(SimCodeVar::Initial::APPROX { .. }), .. } => true,
        SimCodeVar::SimVar { initial_: None, .. } => {
            default_initial = getDefaultFmiInitialAttribute(Util::getOptionOrDefault(simVar.variability.clone(), default_variability.clone()), Util::getOptionOrDefault(simVar.causality.clone(), default_causality.clone()));
            outBoolean = isInitialApproxOrCalculated(default_initial.clone());
            outBoolean.clone()
        },
        _ => false,
    });
    outBoolean
}

fn isInitialApproxOrCalculated(mut initial_: SimCodeVar::Initial) -> bool {
    let mut outBoolean: bool = false;
    outBoolean = (match initial_.clone() {
        SimCodeVar::Initial::APPROX { .. } => true,
        SimCodeVar::Initial::CALCULATED { .. } => true,
        _ => false,
    });
    outBoolean
}

fn isCausalityCalculatedParameterSimVar(mut inVar: SimCodeVar::SimVar) -> bool {
    let mut outBoolean: bool = false;
    outBoolean = (match inVar.clone() {
        SimCodeVar::SimVar { causality: Some(SimCodeVar::Causality::CALCULATED_PARAMETER { .. }), .. } => true,
        _ => false,
    });
    outBoolean
}

fn getCrefFromSimVar(mut inSimVar: SimCodeVar::SimVar) -> Arc<DAE::ComponentRef> {
    let mut outCref: Arc<DAE::ComponentRef> = inSimVar.name.clone();
    outCref
}

fn isInitialExact(mut initial_: SimCodeVar::Initial) -> bool {
    let mut outBoolean: bool = false;
    outBoolean = (match initial_.clone() {
        SimCodeVar::Initial::EXACT { .. } => true,
        _ => false,
    });
    outBoolean
}

fn getFmiInitialUnknowns(mut inInitDAE: Arc<BackendDAE::BackendDAE>, mut inSimDAE: Arc<BackendDAE::BackendDAE>, mut crefSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>)), mut initialUnknownList: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<(Arc<metamodelica::List<SimCode::FmiUnknown>>, Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>>, Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>>, Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>>)> {
    let mut outFmiUnknownlist: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
    let mut fmiDerInit: Arc<metamodelica::List<(Option<(Arc<BackendDAE::BackendDAE>, ArcStr, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>>, (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32))>> = metamodelica::nil();
    let mut sortedUnknownCrefs: Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    let mut sortedknownCrefs: Arc<metamodelica::List<(i32, Arc<DAE::ComponentRef>)>> = metamodelica::nil();
    let mut initialUnknownCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut indepCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut depCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut tmpBDAE: Arc<BackendDAE::BackendDAE> = Arc::new(<BackendDAE::BackendDAE as ::std::default::Default>::default());
    let mut orderedVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut indepVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut depVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut fmiDerInitIndepVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut fmiDerInitDepVars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut sparsePattern: (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32) = (metamodelica::nil(), metamodelica::nil(), (metamodelica::nil(), metamodelica::nil()), 0);
    let mut sparseColoring: Arc<metamodelica::List<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>>> = metamodelica::nil();
    let mut rowspt: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>> = metamodelica::nil();
    let mut sparseInts: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>> = metamodelica::nil();
    let mut vars1: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut vars2: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut shared: Arc<BackendDAE::Shared> = Arc::new(<BackendDAE::Shared as ::std::default::Default>::default());
    let mut currentSystem: Arc<BackendDAE::EqSystem> = Arc::new(<BackendDAE::EqSystem as ::std::default::Default>::default());
    let mut eqs: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let mut lhs: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut rhs: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    let mut eqn: Arc<BackendDAE::Equation> = Arc::new(BackendDAE::Equation::DUMMY_EQUATION);
    let mut strMatchingAlgorithm: ArcStr = arcstr::literal!("");
    let mut strIndexReductionMethod: ArcStr = arcstr::literal!("");
    let mut outAdjacencyMatrix: metamodelica::Array<Arc<metamodelica::List<i32>>> = Default::default();
    let mut match1: metamodelica::Array<i32> = Default::default();
    let mut match2: metamodelica::Array<i32> = Default::default();
    let mut debug: bool = false;
    let mut initialUnknowns: Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>> = <Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>> as ::std::default::Default>::default();
    initialUnknownCrefs = List::map(initialUnknownList.clone(), (std::sync::Arc::new(fnptr!(getCrefFromSimVar, SimCodeVar::SimVar)) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<Arc<DAE::ComponentRef>> + 'static>))?;
    initialUnknowns = UnorderedSet::fromList(initialUnknownCrefs.clone(), (std::sync::Arc::new(ComponentReferenceBasics::hashComponentRef) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?;
    if debug.clone() {
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\n FmiInitialUnknownsDependencyList :")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefListStr(initialUnknownCrefs.clone())?); ArcStr::from(__mm_s) }).clone());
    }
    tmpBDAE = BackendDAEUtil::copyBackendDAE(inInitDAE.clone())?;
    tmpBDAE = BackendDAEOptimize::collapseIndependentBlocks(tmpBDAE.clone())?;
    let (__pa0, __pa1) = ::match_deref::match_deref! { match &(tmpBDAE.clone()) {
        Deref @ BackendDAE::BackendDAE { eqs: Deref @ metamodelica::List::Cons { head: __pa0, tail: Deref @ metamodelica::List::Nil }, shared: __pa1 } => (__pa0.clone(), __pa1.clone()),
        _ => bail!("pattern mismatch"),
    } };
    currentSystem = __pa0.clone();
    shared = __pa1.clone();
    if debug.clone() {
        BackendDump::dumpVariables(currentSystem.orderedVars.clone(), (literal!("orderedVariables")).clone())?;
        BackendDump::dumpEquationArray(currentSystem.orderedEqs.clone(), (literal!("orderedEquation")).clone())?;
        BackendDump::dumpVariables(shared.globalKnownVars.clone(), (literal!("globalknownVars")).clone())?;
        BackendDump::dumpVariables(inSimDAE.shared.globalKnownVars.clone(), (literal!("SimulationGlobalknownVars")).clone())?;
    }
    for mut var in &*BackendVariable::varList(inSimDAE.shared.globalKnownVars.clone())? {
        let mut var = var.clone();
        if BackendVariable::isParam(var.clone()) && !(BackendVariable::containsCref(var.varName.clone(), currentSystem.orderedVars.clone())) {
            lhs = BackendVariable::varExp(var.clone())?;
            rhs = BackendVariable::varBindExpStartValueNoFail(var.clone())?;
            eqn = Arc::new(BackendDAE::Equation::EQUATION { exp: lhs.clone(), scalar: rhs.clone(), source: DAE::emptyElementSource().clone(), attr: BackendDAE::EQ_ATTR_DEFAULT_BINDING.clone() });
            BackendEquation::add(eqn.clone(), currentSystem.orderedEqs.clone())?;
            currentSystem = BackendVariable::addVarDAE(var.clone(), currentSystem.clone())?;
        } else if !(BackendVariable::containsCref(var.varName.clone(), currentSystem.orderedVars.clone())) {
            shared = BackendVariable::addGlobalKnownVarDAE(var.clone(), shared.clone())?;
        }
    }
    (outAdjacencyMatrix, _, _, _) = BackendDAEUtil::adjacencyMatrixScalar(currentSystem.clone(), openmodelica_backend_types::BackendDAE::IndexType::NORMAL, None, BackendDAEUtil::isInitializationDAE(shared.clone()))?;
    (match1, match2) = Matching::PerfectMatching(outAdjacencyMatrix.clone())?;
    assign_field!(currentSystem.matching = Arc::new(BackendDAE::Matching::MATCHING { ass1: match1.clone(), ass2: match2.clone(), comps: BackendDAEUtil::getStrongComponents(currentSystem.clone()) }));
    tmpBDAE = Arc::new(BackendDAE::BackendDAE { eqs: list![currentSystem.clone()], shared: shared.clone() });
    strMatchingAlgorithm = (BackendDAEUtil::getMatchingAlgorithmString()?).clone();
    strIndexReductionMethod = (BackendDAEUtil::getIndexReductionMethodString()?).clone();
    tmpBDAE = BackendDAEUtil::causalizeDAE(tmpBDAE.clone(), None, BackendDAEUtil::getMatchingAlgorithm(Some((strMatchingAlgorithm.clone()).clone()))?, BackendDAEUtil::getIndexReductionMethod(Some((strIndexReductionMethod.clone()).clone()))?, false)?;
    if debug.clone() {
        BackendDump::dumpBackendDAE(tmpBDAE.clone(), (literal!("Check Initilization DAE")).clone())?;
    }
    orderedVars = BackendVariable::equationSystemsVarsLst(tmpBDAE.eqs.clone())?;
    indepVars = metamodelica::nil();
    depVars = metamodelica::nil();
    (depVars, indepVars) = getDepAndIndepVarsForInitialUnknowns(orderedVars.clone(), depVars.clone(), indepVars.clone(), initialUnknowns.clone(), crefSimVarHT.clone())?;
    (depVars, indepVars) = getDepAndIndepVarsForInitialUnknowns(BackendVariable::varList(tmpBDAE.shared.globalKnownVars.clone())?, depVars.clone(), indepVars.clone(), initialUnknowns.clone(), crefSimVarHT.clone())?;
    if debug.clone() {
        BackendDump::dumpVarList(depVars.clone(), (literal!("depVars")).clone())?;
        BackendDump::dumpVarList(indepVars.clone(), (literal!("indepVars")).clone())?;
    }
    (sparsePattern, sparseColoring) = SymbolicJacobian::generateSparsePattern(tmpBDAE.clone(), indepVars.clone(), depVars.clone(), false)?;
    if debug.clone() {
        dumpFmiInitialUnknownsDependencies(sparsePattern.clone(), (literal!("FmiInitialUnknownDependency")).clone())?;
    }
    let (_, __pa3, (__pa4, __pa5), _) = sparsePattern.clone();
    rowspt = __pa3.clone();
    indepCrefs = __pa4.clone();
    depCrefs = __pa5.clone();
    vars1 = getSimVars2Crefs(indepCrefs.clone(), crefSimVarHT.clone());
    vars2 = getSimVars2Crefs(depCrefs.clone(), crefSimVarHT.clone());
    vars2 = listAppend(vars1.clone(), vars2.clone());
    indepCrefs = metamodelica::nil();
    depCrefs = metamodelica::nil();
    for mut i in &*rowspt.clone() {
        let mut i = i.clone();
        (cref, crefs) = i.clone();
        depCrefs = metamodelica::cons(cref.clone(), depCrefs.clone());
        for mut cr in &*crefs.clone() {
            let mut cr = cr.clone();
            if !(listMember(cr.clone(), indepCrefs.clone())) {
                indepCrefs = metamodelica::cons(cr.clone(), indepCrefs.clone());
            }
        }
    }
    if !(Flags::isSet(Flags::FMI20_DEPENDENCIES.clone())?) && !(stringEq((Config::simCodeTarget()?).clone(), (literal!("Cpp")).clone())) {
        fmiDerInitDepVars = getDependentAndIndepentVarsForJacobian(depCrefs.clone(), BackendVariable::listVar(orderedVars.clone())?, crefSimVarHT.clone());
        fmiDerInitIndepVars = getDependentAndIndepentVarsForJacobian(indepCrefs.clone(), BackendVariable::listVar(orderedVars.clone())?, crefSimVarHT.clone());
        if debug.clone() {
            BackendDump::dumpVarList(fmiDerInitDepVars.clone(), (literal!("fmiDerInit_unknownVars")).clone())?;
            BackendDump::dumpVarList(fmiDerInitIndepVars.clone(), (literal!("fmiDerInit_knownVars")).clone())?;
        }
        fmiDerInit = SymbolicJacobian::createFMIModelDerivativesForInitialization(inInitDAE.clone(), inSimDAE.clone(), fmiDerInitDepVars.clone(), fmiDerInitIndepVars.clone(), currentSystem.orderedVars.clone(), sparsePattern.clone(), sparseColoring.clone())?;
        sortedknownCrefs = sortInitialUnknowsSimVars(getSimVars2Crefs(indepCrefs.clone(), crefSimVarHT.clone()));
        sortedUnknownCrefs = sortInitialUnknowsSimVars(getSimVars2Crefs(depCrefs.clone(), crefSimVarHT.clone()));
    }
    sparseInts = sortSparsePattern(vars2.clone(), rowspt.clone(), true)?;
    outFmiUnknownlist = translateSparsePatterInts2FMIUnknown(sparseInts.clone(), metamodelica::nil());
    Ok((outFmiUnknownlist, fmiDerInit, sortedUnknownCrefs, sortedknownCrefs))
}

fn getDependentAndIndepentVarsForJacobian(mut crefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut orderedVars: BackendDAE::Variables, mut crefSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Arc<metamodelica::List<BackendDAE::Var>> {
    let mut outVar: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut simVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    for mut cr in &*crefs.clone() {
        let mut cr = cr.clone();
        if '__try0: {
            (var, _) = unwrap_break_err!(BackendVariable::getVarSingle(cr.clone(), orderedVars.clone()), '__try0);
            simVar = unwrap_break_err!(BaseHashTable::get(cr.clone(), crefSimVarHT.clone()), '__try0);
            if BackendVariable::isRealVar(var.clone()) && isSome(simVar.exportVar.clone()) {
                outVar = metamodelica::cons(var.clone(), outVar.clone());
            }
            Ok::<(), anyhow::Error>(())
        }.is_err() {
            outVar = metamodelica::nil();
        }
    }
    outVar
}

fn getDepAndIndepVarsForInitialUnknowns(mut inVar: Arc<metamodelica::List<BackendDAE::Var>>, mut depVars: Arc<metamodelica::List<BackendDAE::Var>>, mut indepVars: Arc<metamodelica::List<BackendDAE::Var>>, mut initialUnknowns: Arc<UnorderedSet::UnorderedSet<Arc<DAE::ComponentRef>>>, mut crefSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<(Arc<metamodelica::List<BackendDAE::Var>>, Arc<metamodelica::List<BackendDAE::Var>>)> {
    let mut outdepVars: Arc<metamodelica::List<BackendDAE::Var>> = depVars.clone();
    let mut outindepVars: Arc<metamodelica::List<BackendDAE::Var>> = indepVars.clone();
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut referenceVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut variability: SimCodeVar::Variability = SimCodeVar::Variability::CONSTANT;
    let mut isConst: bool = false;
    for mut var in &*inVar.clone() {
        let mut var = var.clone();
        cref = BackendVariable::varCref(var.clone())?;
        if UnorderedSet::contains(cref.clone(), initialUnknowns.clone())? {
            outdepVars = metamodelica::cons(var.clone(), outdepVars.clone());
        }
        if '__try0: {
            referenceVar = unwrap_break_err!(BaseHashTable::get(cref.clone(), crefSimVarHT.clone()), '__try0);
            isConst = (match referenceVar.variability.clone() {
        Some(SimCodeVar::Variability::CONSTANT { .. }) => true,
        _ => false,
    });
            if !(isConst.clone()) && (isCausalityInputSimVar(referenceVar.clone()) || isInitialExactSimVar(referenceVar.clone())) {
                outindepVars = metamodelica::cons(var.clone(), outindepVars.clone());
            }
            Ok::<(), anyhow::Error>(())
        }.is_err() {
        }
    }
    Ok((outdepVars, outindepVars))
}

fn isCausalityInputSimVar(mut simVar: SimCodeVar::SimVar) -> bool {
    let mut outBoolean: bool = false;
    outBoolean = (match simVar.clone() {
        SimCodeVar::SimVar { causality: Some(SimCodeVar::Causality::INPUT { .. }), .. } => true,
        _ => false,
    });
    outBoolean
}

fn isInitialExactSimVar(mut simVar: SimCodeVar::SimVar) -> bool {
    let mut outBoolean: bool = false;
    let mut default_causality: SimCodeVar::Causality = openmodelica_simcode_types::SimCodeVar::Causality::LOCAL;
    let mut default_variability: SimCodeVar::Variability = openmodelica_simcode_types::SimCodeVar::Variability::CONTINUOUS;
    let mut default_initial: SimCodeVar::Initial = SimCodeVar::Initial::APPROX;
    outBoolean = (match simVar.clone() {
        SimCodeVar::SimVar { initial_: Some(SimCodeVar::Initial::EXACT { .. }), .. } => true,
        SimCodeVar::SimVar { initial_: None, .. } => {
            default_initial = getDefaultFmiInitialAttribute(Util::getOptionOrDefault(simVar.variability.clone(), default_variability.clone()), Util::getOptionOrDefault(simVar.causality.clone(), default_causality.clone()));
            outBoolean = isInitialExact(default_initial.clone());
            outBoolean.clone()
        },
        _ => false,
    });
    outBoolean
}

fn dumpFmiInitialUnknownsDependencies(mut sparsePattern: (Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, (Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>), i32), mut heading: ArcStr) -> Result<()> {
    let mut rowspT: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>> = metamodelica::nil();
    let mut var: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut dependencylist: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*heading.clone()); __mm_s.push_str(&*literal!("\n")); __mm_s.push_str(&*arcstr::literal!(UNDERLINE)); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    (_, rowspT, _, _) = sparsePattern.clone();
    for mut i in &*rowspT.clone() {
        let mut i = i.clone();
        (var, dependencylist) = i.clone();
        println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(var.clone())?); __mm_s.push_str(&*literal!("=====>")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefListStr(dependencylist.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
    }
    Ok(())
}

pub fn getAllParamSimVars(mut inModelInfo: SimCode::ModelInfo) -> Arc<metamodelica::List<SimCodeVar::SimVar>> {
    let mut allParamVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    allParamVars = listAppend(listAppend(inModelInfo.vars.paramVars.clone(), inModelInfo.vars.intParamVars.clone()), listAppend(inModelInfo.vars.boolParamVars.clone(), inModelInfo.vars.stringParamVars.clone()));
    allParamVars
}

fn isClockedStateSimVar(mut inVar: SimCodeVar::SimVar) -> bool {
    let mut outBoolean: bool = false;
    outBoolean = (match inVar.clone() {
        SimCodeVar::SimVar { varKind: BackendDAE::VarKind::CLOCKED_STATE { previousName: _, .. }, .. } => true,
        _ => false,
    });
    outBoolean
}

fn isOutputSimVar(mut inVar: SimCodeVar::SimVar) -> bool {
    let mut outBoolean: bool = false;
    outBoolean = (match inVar.clone() {
        SimCodeVar::SimVar { causality: Some(SimCodeVar::Causality::OUTPUT { .. }), .. } => true,
        _ => false,
    });
    outBoolean
}

fn isFmiUnknown(mut index: i32, mut inFMIUnknown: SimCode::FmiUnknown) -> bool {
    let mut out: bool = false;
    out = (match inFMIUnknown.clone() {
        SimCode::FmiUnknown { index: mut i, .. } if (intEq(i.clone(), index.clone())) => {
            true
        },
        _ => {
            false
        },
    });
    out
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn translateSparsePatterInts2FMIUnknown(mut inSparsePattern: Arc<metamodelica::List<(i32, Arc<metamodelica::List<i32>>)>>, mut inAccum: Arc<metamodelica::List<SimCode::FmiUnknown>>) -> Arc<metamodelica::List<SimCode::FmiUnknown>> {
    let mut outFmiUnknown: Arc<metamodelica::List<SimCode::FmiUnknown>> = metamodelica::nil();
    outFmiUnknown = (::match_deref::match_deref! { match &(inSparsePattern.clone()) {
        Deref @ metamodelica::List::Nil => {
            inAccum.clone().reverse()
        },
        Deref @ metamodelica::List::Cons { head: (unknown, dependencies), tail: rest } => {
            let mut dependenciesKind: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            dependenciesKind = List::fill((literal!("dependent")).clone(), (dependencies.clone().len() as i32));
            translateSparsePatterInts2FMIUnknown(rest.clone(), metamodelica::cons(SimCode::FmiUnknown { index: unknown.clone(), dependencies: dependencies.clone(), dependenciesKind: dependenciesKind.clone() }, inAccum.clone()))
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    outFmiUnknown
}

fn translateSparsePatterCref2DerCref(mut sparsePattern: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, mut inSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>)), mut inAccum: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, mut inAccum2: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<(Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)> {
    let mut outSparsePattern: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>> = metamodelica::nil();
    let mut outDerCrefs: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    (outSparsePattern, outDerCrefs) = (::match_deref::match_deref! { match &(sparsePattern.clone()) {
        Deref @ metamodelica::List::Nil => {
            (inAccum.clone().reverse(), inAccum2.clone().reverse())
        },
        Deref @ metamodelica::List::Cons { head: (cref, crefs), tail: rest } => {
            let mut simVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut cref = (*cref).clone();
            simVar = BaseHashTable::get(cref.clone(), inSimVarHT.clone())?;
            if BackendVariable::isVarKindState(simVar.varKind.clone()) {
                cref = ComponentReference::crefPrefixDer(cref.clone());
            }
            translateSparsePatterCref2DerCref(rest.clone(), inSimVarHT.clone(), metamodelica::cons((cref.clone(), crefs.clone()), inAccum.clone()), metamodelica::cons(cref.clone(), inAccum2.clone()))?
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((outSparsePattern, outDerCrefs))
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn mergeSparsePatter(mut inA: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, mut inB: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>, mut inAccum: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>) -> Result<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>>> {
    let mut out: Arc<metamodelica::List<(Arc<DAE::ComponentRef>, Arc<metamodelica::List<Arc<DAE::ComponentRef>>>)>> = metamodelica::nil();
    out = (::match_deref::match_deref! { match &((inA.clone(), inB.clone())) {
        (Deref @ metamodelica::List::Nil, Deref @ metamodelica::List::Nil) => {
            inAccum.clone().reverse()
        },
        (Deref @ metamodelica::List::Nil, _) => {
            inB.clone()
        },
        (_, Deref @ metamodelica::List::Nil) => {
            inA.clone()
        },
        (Deref @ metamodelica::List::Cons { head: (crefA, listA), tail: restA }, Deref @ metamodelica::List::Cons { head: (crefB, listB), tail: restB }) => {
            let mut listOut: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
            let true = (ComponentReferenceBasics::crefEqual(crefA.clone(), crefB.clone())?) else { bail!("pattern mismatch") };
            listOut = List::unionOnTrue(listA.clone(), listB.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?;
            mergeSparsePatter(restA.clone(), restB.clone(), metamodelica::cons((crefA.clone(), listOut.clone()), inAccum.clone()))?
        },
        _ => bail!("match: no arm matched"),
    } });
    Ok(out)
}

pub fn getStateSimVarIndexFromIndex(mut inStateVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut inIndex: i32) -> Result<i32> {
    let mut outVariableIndex: i32 = 0;
    let mut stateVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    stateVar = (inStateVars.clone()).get(inIndex.clone() + 1 - if (Config::simCodeTarget()? == literal!("Cpp")) {0} else {(inStateVars.clone().len() as i32)})?;
    outVariableIndex = getVariableIndex(stateVar.clone());
    Ok(outVariableIndex)
}

fn getNumScalars(mut vars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<i32> {
    let mut numScalars: i32 = 0;
    numScalars = List::applyAndFold(vars.clone(), (std::sync::Arc::new(fnptr!(intAdd, i32, i32)) as std::sync::Arc<dyn ::std::ops::Fn(i32, i32) -> Result<i32> + 'static>), (std::sync::Arc::new(SimCodeUtilShared::getNumElems) as std::sync::Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<i32> + 'static>), 0)?;
    Ok(numScalars)
}

pub fn getScalarElements(mut var: SimCodeVar::SimVar) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut elts: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut dims: Arc<metamodelica::List<i32>> = metamodelica::nil();
    let mut elt: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut index: i32 = 0;
    let mut fmi_index: i32 = 0;
    elts = (::match_deref::match_deref! { match &(var.clone()) {
        SimCodeVar::SimVar { exportVar: None, type_: Deref @ DAE::Type::T_ARRAY { .. }, .. } => metamodelica::nil(),
        SimCodeVar::SimVar { fmi_index: Some(fmi_index), variable_index: Some(index), type_: Deref @ DAE::Type::T_ARRAY { .. }, .. } => {
            dims = List::map(List::lastN(var.numArrayElement.clone(), (var.numArrayElement.clone().len() as i32))?, (std::sync::Arc::new(stringInt) as std::sync::Arc<dyn ::std::ops::Fn(ArcStr) -> Result<i32> + 'static>))?;
            elt = var.clone();
            elt.type_ = Types::arrayElementType(var.type_.clone());
            elts = fillScalarElements(elt.clone(), dims.clone(), 1, metamodelica::nil(), metamodelica::nil())?;
            (elts, _, _) = setVariableIndexHelper(elts.clone(), index.clone(), fmi_index.clone())?;
            elts.clone()
        },
        _ => list![var.clone()],
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(elts)
}

fn fillScalarElements(mut eltIn: SimCodeVar::SimVar, mut dims: Arc<metamodelica::List<i32>>, mut dimIdx: i32, mut subsIn: Arc<metamodelica::List<Arc<DAE::Subscript>>>, mut elts: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut elts: Arc<metamodelica::List<SimCodeVar::SimVar>> = elts;
    let mut elt: SimCodeVar::SimVar = eltIn.clone();
    let mut subs: Arc<metamodelica::List<Arc<DAE::Subscript>>> = metamodelica::nil();
    for mut i in (1..=(dims.clone()).get(dimIdx.clone())?).rev() {
        subs = metamodelica::cons(Arc::new(DAE::Subscript::INDEX { exp: Arc::new(DAE::Exp::ICONST { integer: i.clone() }) }), subsIn.clone());
        if dimIdx.clone() < (dims.clone().len() as i32) {
            elts = fillScalarElements(eltIn.clone(), dims.clone(), dimIdx.clone() + 1, subs.clone(), elts.clone())?;
        } else {
            subs = subs.clone().reverse();
            elt.name = ComponentReference::crefSetLastSubs(elt.name.clone(), subs.clone())?;
            elt.exportVar = Some(ComponentReference::crefSetLastSubs(Util::getOption(elt.exportVar.clone())?, subs.clone())?);
            let () = (match elt.clone() {
        SimCodeVar::SimVar { varKind: BackendDAE::VarKind::CLOCKED_STATE { isStartFixed: mut fixed, previousName: ref cref }, .. } => {
            elt.varKind = BackendDAE::VarKind::CLOCKED_STATE { previousName: ComponentReference::crefSetLastSubs(cref.clone(), subs.clone())?, isStartFixed: fixed.clone() };
            ()
        },
        _ => {
            ()
        },
    });
            elts = metamodelica::cons(elt.clone(), elts.clone());
        }
    }
    Ok(elts)
}

fn getScalarVars(mut inVars: Arc<metamodelica::List<SimCodeVar::SimVar>>) -> Result<Arc<metamodelica::List<SimCodeVar::SimVar>>> {
    let mut outVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    outVars = metamodelica::nil();
    for mut var in &*inVars.clone().reverse() {
        let mut var = var.clone();
        outVars = listAppend(getScalarElements(var.clone())?, outVars.clone());
    }
    Ok(outVars)
}

pub fn getVariableIndex(mut inVar: SimCodeVar::SimVar) -> i32 {
    let mut outVariableIndex: i32 = 0;
    outVariableIndex = (match inVar.clone() {
        SimCodeVar::SimVar { variable_index: Some(mut variableIndex), .. } => {
            variableIndex.clone()
        },
        _ => {
            0
        },
    });
    outVariableIndex
}

pub fn getVariableFMIIndex(mut inVar: SimCodeVar::SimVar) -> i32 {
    let mut outVariableIndex: i32 = 0;
    outVariableIndex = (match inVar.clone() {
        SimCodeVar::SimVar { fmi_index: Some(mut variableIndex), .. } => {
            variableIndex.clone()
        },
        _ => {
            0
        },
    });
    outVariableIndex
}

pub fn getValueReference(mut inSimVar: SimCodeVar::SimVar, mut inSimCode: SimCode::SimCode, mut inElimNegAliases: bool) -> Result<ArcStr> {
    let mut outValueReference: ArcStr = arcstr::literal!("");
    outValueReference = ((match (inSimVar.clone(), inElimNegAliases.clone(), Config::simCodeTarget()?) {
        (SimCodeVar::SimVar { aliasvar: SimCodeVar::AliasVariable::NEGATEDALIAS { varName: _ }, .. }, false, _) => {
            getDefaultValueReference(inSimVar.clone(), inSimCode.modelInfo.varInfo.clone())?
        },
        (_, _, _) if (stringEqual((Config::simCodeTarget()?).clone(), (literal!("Cpp")).clone()) || stringEqual((Config::simCodeTarget()?).clone(), (literal!("omsic")).clone())) => {
            let mut simVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
            let mut valueReference: ArcStr = arcstr::literal!("");
            simVar = (::match_deref::match_deref! { match &(inSimVar.clone()) {
        SimCodeVar::SimVar { aliasvar: SimCodeVar::AliasVariable::ALIAS { varName: cref }, .. } => {
            cref2simvar(cref.clone(), inSimCode.clone())?
        },
        SimCodeVar::SimVar { aliasvar: SimCodeVar::AliasVariable::NEGATEDALIAS { varName: cref }, .. } => {
            cref2simvar(cref.clone(), inSimCode.clone())?
        },
        SimCodeVar::SimVar { name: Deref @ DAE::ComponentRef::CREF_QUAL { componentRef, ident: Deref @ "$PRE", .. }, .. } => {
            cref2simvar(componentRef.clone(), inSimCode.clone())?
        },
        _ => {
            inSimVar.clone()
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
            valueReference = (getVarIndexByMapping(inSimCode.varToArrayIndexMapping.clone(), simVar.name.clone(), true, (literal!("-1")).clone())?).clone();
            if stringEqual((valueReference.clone()).clone(), (literal!("-1")).clone()) {
                Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("invalid return value from getVarIndexByMapping for ")); __mm_s.push_str(&*simVarString(simVar.clone())?); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
            }
            valueReference.clone()
        },
        (SimCodeVar::SimVar { aliasvar: SimCodeVar::AliasVariable::ALIAS { varName: ref cref }, .. }, _, _) => {
            getDefaultValueReference(cref2simvar(cref.clone(), inSimCode.clone())?, inSimCode.modelInfo.varInfo.clone())?
        },
        _ => {
            getDefaultValueReference(inSimVar.clone(), inSimCode.modelInfo.varInfo.clone())?
        },
    })).clone();
    Ok(outValueReference)
}

fn getDefaultValueReference(mut inSimVar: SimCodeVar::SimVar, mut inVarInfo: SimCode::VarInfo) -> Result<ArcStr> {
    let mut outDefaultValueReference: ArcStr = arcstr::literal!("");
    let mut reference: i32 = 0;
    let mut numReal: i32 = 2 * inVarInfo.numStateVars.clone() + inVarInfo.numAlgVars.clone() + inVarInfo.numDiscreteReal.clone() + inVarInfo.numParams.clone() + inVarInfo.numAlgAliasVars.clone();
    let mut numInteger: i32 = inVarInfo.numIntAlgVars.clone() + inVarInfo.numIntParams.clone() + inVarInfo.numIntAliasVars.clone();
    let mut numBoolean: i32 = inVarInfo.numBoolAlgVars.clone() + inVarInfo.numBoolParams.clone() + inVarInfo.numBoolAliasVars.clone();
    reference = getVariableIndex(inSimVar.clone());
    if reference.clone() > numReal.clone() + numInteger.clone() + numBoolean.clone() {
        reference = reference.clone() - numReal.clone() - numInteger.clone() - numBoolean.clone();
    } else if reference.clone() > numReal.clone() + numInteger.clone() {
        reference = reference.clone() - numReal.clone() - numInteger.clone();
    } else if reference.clone() > numReal.clone() {
        reference = reference.clone() - numReal.clone();
    } else if reference.clone() < 0 {
        Error::addInternalError((literal!("invalid return value from getVariableIndex")).clone(), metamodelica::sourceInfo!())?;
    }
    outDefaultValueReference = ArcStr::from(::std::format!("{}", reference.clone() - 1));
    Ok(outDefaultValueReference)
}

pub fn getLocalValueReference(mut inSimVar: SimCodeVar::SimVar, mut inSimCode: SimCode::SimCode, mut inCrefToSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>)), mut inElimNegAliases: bool) -> Result<ArcStr> {
    let mut outValueReference: ArcStr = arcstr::literal!("");
    outValueReference = ('mc: {
        let __mc_input = (inSimVar.clone(), inCrefToSimVarHT.clone());
        if let Ok(__v) = (|| -> Result<_> {
            let (SimCodeVar::SimVar { name: ref cref, .. }, mut crefToSimVarHT) = __mc_input.clone() else { bail!("nomatch") };
            let mut valueReference: ArcStr = arcstr::literal!("");
            valueReference = (localCref2Index(cref.clone(), crefToSimVarHT.clone())?).clone();
            if stringEqual((valueReference.clone()).clone(), (literal!("-1")).clone()) {
                valueReference = (getValueReference(inSimVar.clone(), inSimCode.clone(), inElimNegAliases.clone())?).clone();
            }
            Ok(valueReference.clone())
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let _ = __mc_input.clone() else { bail!("nomatch") };
            Error::addInternalError((literal!("getLocalValueReference failed.")).clone(), metamodelica::sourceInfo!())?;
            Ok(literal!("ERROR: getLocalValueReference failed"))
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    }).clone();
    Ok(outValueReference)
}

fn getHighestDerivation(mut inDAE: Arc<BackendDAE::BackendDAE>) -> Result<i32> {
    let mut highestDerivation: i32 = 0;
    let mut vars: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut states: Arc<metamodelica::List<BackendDAE::Var>> = metamodelica::nil();
    let mut ders: metamodelica::Array<i32> = Default::default();
    let mut depth: metamodelica::Array<i32> = Default::default();
    let mut allStates: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut index: i32 = 0;
    let mut pos: i32 = 0;
    let mut length: i32 = 0;
    let mut curIndex: i32 = 0;
    let mut derCref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    vars = BackendDAEUtil::getAllVarLst(inDAE.clone())?;
    states = List::filterOnTrue(vars.clone(), (std::sync::Arc::new(fnptr!(BackendVariable::isStateVar, BackendDAE::Var)) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var) -> Result<bool> + 'static>))?;
    length = (states.clone().len() as i32);
    if length.clone() == 0 {
        highestDerivation = 0;
        return Ok(highestDerivation.clone());
    }
    ders = arrayCreate(length.clone(), -1);
    depth = arrayCreate(length.clone(), -1);
    allStates = BackendVariable::listVar1(states.clone())?;
    curIndex = 1;
    for mut state in &*states.clone() {
        let mut state = state.clone();
        let () = 'mc: {
        let __mc_input = state.clone();
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { varKind: BackendDAE::VarKind::STATE { derName: Some(derCref), index, .. }, .. } => {
                    let mut pos: i32 = pos.clone();
                    let mut var: BackendDAE::Var = var.clone();
                    (var, pos) = BackendVariable::getVarSingle(derCref.clone(), allStates.clone())?;
                    if !(BackendVariable::varEqual(state.clone(), var.clone())?) {
                        {let _arr = ders.clone(); _arr.borrow_mut()[(curIndex.clone()-1) as usize] = pos.clone(); _arr};
                    } else {
                        {let _arr = depth.clone(); _arr.borrow_mut()[(curIndex.clone()-1) as usize] = 0; _arr};
                    }
                    Ok(())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                BackendDAE::Var { .. } => {
                    {let _arr = depth.clone(); _arr.borrow_mut()[(curIndex.clone()-1) as usize] = 0; _arr};
                    Ok(())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
        curIndex = curIndex.clone() + 1;
    }
    for mut i in 1..=length.clone() {
        getHighestDerivationVisit(i.clone(), ders.clone(), depth.clone())?;
    }
    highestDerivation = ({
        let mut __acc: Option<i32> = None;
        for mut i in (depth.clone()).borrow().iter() {
            let __x = i.clone();
            __acc = Some(match __acc { None => __x, Some(__cur) => if __x > __cur { __x } else { __cur } });
        }
        __acc.unwrap_or((-i32::MAX))
    });
    GCExt::free(ders.clone());
    GCExt::free(depth.clone());
    Ok(highestDerivation)
}

fn getHighestDerivationVisit(mut i: i32, mut ders: metamodelica::Array<i32>, mut depth: metamodelica::Array<i32>) -> Result<i32> {
    let mut d: i32 = depth.clone().borrow()[(i.clone()-1) as usize].clone();
    if d.clone() >= 0 {
        return Ok(d.clone());
    } else if d.clone() == -2 {
        d = 0;
        return Ok(d.clone());
    }
    {let _arr = depth.clone(); _arr.borrow_mut()[(i.clone()-1) as usize] = -2; _arr};
    d = getHighestDerivationVisit(ders.clone().borrow()[(i.clone()-1) as usize].clone(), ders.clone(), depth.clone())?;
    {let _arr = depth.clone(); _arr.borrow_mut()[(i.clone()-1) as usize] = d.clone(); _arr};
    Ok(d)
}

fn hasLargeEquationSystems(mut iDlow: Arc<BackendDAE::BackendDAE>, mut iInitDAE: Arc<BackendDAE::BackendDAE>) -> Result<bool> {
    let mut oHasLargeEqSystem: bool = false;
    let mut hasLargeEqSystem: bool = false;
    let mut eqs: Arc<metamodelica::List<Arc<BackendDAE::EqSystem>>> = metamodelica::nil();
    let __pa0 = ::match_deref::match_deref! { match &(iDlow.clone()) {
        Deref @ BackendDAE::BackendDAE { eqs: __pa0, .. } => __pa0.clone(),
        _ => bail!("pattern mismatch"),
    } };
    eqs = __pa0.clone();
    for mut eqsys in &*eqs.clone() {
        let mut eqsys = eqsys.clone();
        if boolNot(hasLargeEqSystem.clone()) {
            hasLargeEqSystem = hasLargeEquationSystems1(BackendDAEUtil::getStrongComponents(eqsys.clone()))?;
        }
    }
    if boolNot(hasLargeEqSystem.clone()) {
        let __pa1 = ::match_deref::match_deref! { match &(iInitDAE.clone()) {
            Deref @ BackendDAE::BackendDAE { eqs: __pa1, .. } => __pa1.clone(),
            _ => bail!("pattern mismatch"),
        } };
        eqs = __pa1.clone();
        for mut eqsys in &*eqs.clone() {
            let mut eqsys = eqsys.clone();
            if boolNot(hasLargeEqSystem.clone()) {
                hasLargeEqSystem = hasLargeEquationSystems1(BackendDAEUtil::getStrongComponents(eqsys.clone()))?;
            }
        }
    }
    if Flags::isSet(Flags::DUMP_DGESV.clone())? {
        if boolNot(hasLargeEqSystem.clone()) {
            println!("{}", (literal!("This model has no large linear or nonlinear equation system, thus the use of dgesv (OMCompiler/3rdParty/) is prefered.\n")).clone());
        } else {
            println!("{}", (literal!("This model has at least one large or nonlinear linear equation system, thus the use of Lapack is prefered.\n")).clone());
        }
    }
    oHasLargeEqSystem = hasLargeEqSystem.clone();
    Ok(oHasLargeEqSystem)
}

fn hasLargeEquationSystems1(mut iComps: Arc<metamodelica::List<Arc<BackendDAE::StrongComponent>>>) -> Result<bool> {
    let mut oHasLargeEquationSystems: bool = false;
    let mut hasLargeEqSystem: bool = false;
    let mut vars: Arc<metamodelica::List<i32>> = metamodelica::nil();
    for mut comp in &*iComps.clone() {
        let mut comp = comp.clone();
        if boolNot(hasLargeEqSystem.clone()) {
            if boolOr(BackendDAEUtil::isLinearEqSystemComp(comp.clone()), BackendDAEUtil::isNonLinearEqSystemComp(comp.clone())) {
                let __pa0 = ::match_deref::match_deref! { match &(comp.clone()) {
                    Deref @ BackendDAE::StrongComponent::EQUATIONSYSTEM { vars: __pa0, .. } => __pa0.clone(),
                    _ => bail!("pattern mismatch"),
                } };
                vars = __pa0.clone();
                hasLargeEqSystem = intGt((vars.clone().len() as i32), 10);
            } else {
                if boolOr(BackendDAEUtil::isLinearTornSystemComp(comp.clone()), BackendDAEUtil::isNonLinearTornSystemComp(comp.clone())) {
                    let __pa1 = ::match_deref::match_deref! { match &(comp.clone()) {
                        Deref @ BackendDAE::StrongComponent::TORNSYSTEM { strictTearingSet: BackendDAE::TearingSet { tearingvars: __pa1, .. }, .. } => __pa1.clone(),
                        _ => bail!("pattern mismatch"),
                    } };
                    vars = __pa1.clone();
                    hasLargeEqSystem = intGt((vars.clone().len() as i32), 10);
                }
            }
        }
    }
    oHasLargeEquationSystems = hasLargeEqSystem.clone();
    Ok(oHasLargeEquationSystems)
}

fn createSimVarsForSensitivities(mut inStateSimVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut inParamSimVars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut nFixedParameters: i32) -> Result<(Arc<metamodelica::List<SimCodeVar::SimVar>>, i32)> {
    let mut outSimCodeVars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut countSensitivityParams: i32 = 0;
    let debug: bool = false;
    let mut tmpVariables: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut emptyVars: BackendDAE::Variables = <BackendDAE::Variables as ::std::default::Default>::default();
    let mut var: BackendDAE::Var = <BackendDAE::Var as ::std::default::Default>::default();
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut sensitivityParams: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    emptyVars = BackendVariable::emptyVars(BaseHashTable::bigBucketSize.clone());
    tmpVariables = BackendVariable::emptyVarsSized((inStateSimVars.clone().len() as i32) * nFixedParameters.clone());
    for mut param in &*inParamSimVars.clone() {
        let mut param = param.clone();
        if param.isValueChangeable.clone() && ComponentReference::crefIsIdent(param.name.clone()) {
            countSensitivityParams = countSensitivityParams.clone() + 1;
            sensitivityParams = metamodelica::cons(param.clone(), sensitivityParams.clone());
            for mut state in &*inStateSimVars.clone() {
                let mut state = state.clone();
                cref = ComponentReferenceBasics::makeCrefIdent((literal!("$Sensitivities")).clone(), DAE::T_REAL_DEFAULT().clone(), metamodelica::nil());
                cref = ComponentReference::joinCrefs(cref.clone(), param.name.clone())?;
                cref = ComponentReference::joinCrefs(cref.clone(), state.name.clone())?;
                if debug.clone() {
                    println!("{}", ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("createSimVarsForSensitivities for Var: ")); __mm_s.push_str(&*ComponentReference::crefStr(cref.clone())?); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone());
                }
                var = BackendVariable::makeVar(cref.clone())?;
                tmpVariables = BackendVariable::addNewVar(var.clone(), tmpVariables.clone())?;
            }
        }
    }
    (outSimCodeVars, _) = BackendVariable::traverseBackendDAEVars(tmpVariables.clone(), (std::sync::Arc::new(traversingdlowvarToSimvar) as std::sync::Arc<dyn ::std::ops::Fn(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables)) -> Result<(BackendDAE::Var, (Arc<metamodelica::List<SimCodeVar::SimVar>>, BackendDAE::Variables))> + 'static>), (metamodelica::nil(), emptyVars.clone()))?;
    outSimCodeVars = outSimCodeVars.clone().reverse();
    outSimCodeVars = listAppend(sensitivityParams.clone().reverse(), outSimCodeVars.clone());
    Ok((outSimCodeVars, countSensitivityParams))
}

/* ****************************************************************************************************
        FMU EXPERIMENTAL
        author: F. Bergero. 12/10/2015
*****************************************************************************************************/
// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn getNLSysRHS(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut res: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> {
    let mut unknowns: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    unknowns = 'mc: {
        let __mc_input = (eqs.clone(), res.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Nil, _) => {
                    Ok(res.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: Deref @ SimCode::SimEqSystem::SES_RESIDUAL { exp, .. }, tail: tail }, _) => {
                    Ok(getNLSysRHS(tail.clone(), listAppend(res.clone(), Expression::getAllCrefs(exp.clone())?))?)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: Deref @ SimCode::SimEqSystem::SES_FOR_RESIDUAL { exp, .. }, tail: tail }, _) => {
                    Ok(getNLSysRHS(tail.clone(), listAppend(res.clone(), Expression::getAllCrefs(exp.clone())?))?)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: Deref @ SimCode::SimEqSystem::SES_GENERIC_RESIDUAL { exp, .. }, tail: tail }, _) => {
                    Ok(getNLSysRHS(tail.clone(), listAppend(res.clone(), Expression::getAllCrefs(exp.clone())?))?)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, _) => {
                    println!("{}", (literal!("getNLSysRHS failed\n")).clone());
                    Ok(bail!("fail"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(unknowns)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn computeDependenciesHelper(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut unknowns: Arc<metamodelica::List<Arc<DAE::ComponentRef>>>, mut res: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut deps: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    deps = 'mc: {
        let __mc_input = (eqs.clone(), res.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Nil, r) => {
                    Ok(r.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: head @ Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { exp, cref, .. }, tail: tail }, r) => {
                    let mut new_unknowns: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let true = (List::isMemberOnTrue(cref.clone(), unknowns.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    new_unknowns = Expression::getAllCrefs(exp.clone())?;
                    Ok(computeDependenciesHelper(tail.clone(), listAppend(unknowns.clone(), new_unknowns.clone()), listAppend(r.clone(), list![head.clone()]))?)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: head @ Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN_CONSTRAINTS { exp, cref, .. }, tail: tail }, r) => {
                    let mut new_unknowns: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let true = (List::isMemberOnTrue(cref.clone(), unknowns.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?) else { bail!("pattern mismatch") };
                    new_unknowns = Expression::getAllCrefs(exp.clone())?;
                    Ok(computeDependenciesHelper(tail.clone(), listAppend(unknowns.clone(), new_unknowns.clone()), listAppend(r.clone(), list![head.clone()]))?)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: head @ Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: Deref @ SimCode::LinearSystem { beqs, .. }, .. }, tail: tail }, r) => {
                    let mut new_unknowns: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let mut linsys_unk: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    linsys_unk = getSimEqSystemCrefsLHS(head.clone())?;
                    let false = (List::intersectionOnTrue(linsys_unk.clone(), unknowns.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?.is_empty()) else { bail!("pattern mismatch") };
                    new_unknowns = List::flatten(List::map(beqs.clone(), (std::sync::Arc::new(Expression::getAllCrefs) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>) -> Result<Arc<metamodelica::List<Arc<DAE::ComponentRef>>>> + 'static>))?)?;
                    Ok(computeDependenciesHelper(tail.clone(), listAppend(unknowns.clone(), new_unknowns.clone()), listAppend(r.clone(), list![head.clone()]))?)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: head @ Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: Deref @ SimCode::NonlinearSystem { eqs: nlsys_eqs, crefs: nlsys_unk, .. }, .. }, tail: tail }, r) => {
                    let mut new_unknowns: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
                    let false = (List::intersectionOnTrue(nlsys_unk.clone(), unknowns.clone(), (std::sync::Arc::new(ComponentReferenceBasics::crefEqual) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>))?.is_empty()) else { bail!("pattern mismatch") };
                    new_unknowns = getNLSysRHS(nlsys_eqs.clone(), metamodelica::nil())?;
                    Ok(computeDependenciesHelper(tail.clone(), listAppend(unknowns.clone(), new_unknowns.clone()), listAppend(r.clone(), list![head.clone()]))?)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (Deref @ metamodelica::List::Cons { head: _, tail: tail }, r) => {
                    Ok(computeDependenciesHelper(tail.clone(), unknowns.clone(), r.clone())?)
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(deps)
}

pub fn computeDependencies(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut cref: Arc<DAE::ComponentRef>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut deps: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    deps = (::match_deref::match_deref! { match &(cref.clone()) {
        _ => computeDependenciesHelper(eqs.clone().reverse(), list![cref.clone()], metamodelica::nil())?.reverse(),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(deps)
}

pub fn getSimEqSystemsByIndexLst(mut idcs: Arc<metamodelica::List<i32>>, mut allSes: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut sesOut: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    sesOut = List::map1(idcs.clone(), (std::sync::Arc::new(getSimEqSysForIndex) as std::sync::Arc<dyn ::std::ops::Fn(i32, Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<SimCode::SimEqSystem>> + 'static>), allSes.clone())?;
    Ok(sesOut)
}

pub fn getInputIndex(mut var: SimCodeVar::SimVar) -> Result<i32> {
    let mut inputIndex: i32 = 0;
    let mut v: metamodelica::Array<i32> = Default::default();
    inputIndex = (match var.clone() {
        SimCodeVar::SimVar { inputIndex: Some(mut v), .. } if (metamodelica::arrayLength(v.clone()) == 1) => v.clone().borrow()[(1-1) as usize].clone(),
        SimCodeVar::SimVar { inputIndex: Some(_), .. } => {
            Error::addInternalError((literal!("Failed to SimCodeUtil.getInputIndex of variable")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
        _ => -1,
    });
    Ok(inputIndex)
}

pub fn resetFunctionIndex() -> Result<()> {
    { let __v = DoubleEnded::fromList(metamodelica::nil())?; openmodelica_util::Globals::codegenFunctionList.with(|__root| *__root.borrow_mut() = __v) };
    Ok(())
}

pub fn addFunctionIndex(mut prefix: ArcStr, mut suffix: ArcStr) -> ArcStr {
    let mut newName: ArcStr = arcstr::literal!("");
    let mut delst: DoubleEnded::MutableList<ArcStr> = <DoubleEnded::MutableList<ArcStr> as ::std::default::Default>::default();
    delst = openmodelica_util::Globals::codegenFunctionList.with(|__root| __root.borrow().clone());
    newName = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*prefix.clone()); __mm_s.push_str(&*ArcStr::from(::std::format!("{}", DoubleEnded::length(delst.clone())))); __mm_s.push_str(&*suffix.clone()); ArcStr::from(__mm_s) }).clone();
    DoubleEnded::push_back(delst.clone(), (newName.clone()).clone());
    newName
}

pub fn getFunctionIndex() -> Arc<metamodelica::List<ArcStr>> {
    let mut files: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut delst: DoubleEnded::MutableList<ArcStr> = <DoubleEnded::MutableList<ArcStr> as ::std::default::Default>::default();
    delst = openmodelica_util::Globals::codegenFunctionList.with(|__root| __root.borrow().clone());
    files = DoubleEnded::toListAndClear(delst.clone(), metamodelica::nil());
    files
}

pub fn nVariablesReal(mut varInfo: SimCode::VarInfo) -> i32 {
    let mut n: i32 = 0;
    n = 2 * varInfo.numStateVars.clone() + varInfo.numAlgVars.clone() + varInfo.numDiscreteReal.clone() + varInfo.numOptimizeConstraints.clone() + varInfo.numOptimizeFinalConstraints.clone();
    n
}

pub fn getSimCode() -> Result<SimCode::SimCode> {
    let mut code: SimCode::SimCode = <SimCode::SimCode as ::std::default::Default>::default();
    let mut ocode: Option<SimCode::SimCode> = None;
    ocode = crate::Globals::optionSimCode.with(|__root| __root.borrow().clone());
    code = (match ocode.clone() {
        Some(mut __esc_code) => {
            code = __esc_code.clone();
            code.clone()
        },
        _ => {
            Error::addInternalError((literal!("Tried to generate code that requires the SimCode structure, but this is not set (function context?)")).clone(), metamodelica::sourceInfo!())?;
            bail!("fail")
        },
    });
    Ok(code)
}

pub fn cref2simvar(mut inCref: Arc<DAE::ComponentRef>, mut simCode: SimCode::SimCode) -> Result<SimCodeVar::SimVar> {
    let mut outSimVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut crefToSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (HashTableCrefSimVar::FuncHashCref, HashTableCrefSimVar::FuncCrefEqual, HashTableCrefSimVar::FuncCrefStr, HashTableCrefSimVar::FuncExpStr));
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut badcref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    match '__try0: {
        let SimCode::SIMCODE { crefToSimVarHT: __pa1, .. } = (simCode.clone()) else { break '__try0 Err::<_, _>(anyhow::anyhow!("pattern mismatch")) };
        crefToSimVarHT = __pa1.clone();
        cref = if (simCode.scalarized.clone()) {inCref.clone()} else {unwrap_break_err!(ComponentReference::crefStripSubs(inCref.clone()), '__try0)};
        outSimVar = unwrap_break_err!(simVarFromHT(cref.clone(), crefToSimVarHT.clone()), '__try0);
        Ok::<_, anyhow::Error>((outSimVar.clone(),))
    } {
        Ok((__try0_o0,)) => {
            outSimVar = __try0_o0;
        }
        Err(_) => {
            badcref = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("ERROR_cref2simvar_failed ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(inCref.clone())?); ArcStr::from(__mm_s) }).clone(), DAE::T_REAL_DEFAULT().clone(), metamodelica::nil());
            outSimVar = SimCodeVar::SimVar { name: badcref.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: -2, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: DAE::T_REAL_DEFAULT().clone(), isDiscrete: false, arrayCref: None, aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL), variable_index: None, fmi_index: None, numArrayElement: metamodelica::nil(), isValueChangeable: false, isProtected: true, hideResult: None, isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(badcref.clone()), relativeQuantity: false };
        }
    }
    Ok(outSimVar)
}

pub fn simVarFromHT(mut inCref: Arc<DAE::ComponentRef>, mut crefToSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<SimCodeVar::SimVar> {
    let mut outSimVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut cref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut badcref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    let mut sv: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut subs: Arc<metamodelica::List<Arc<DAE::Subscript>>> = metamodelica::nil();
    match '__try0: {
        if unwrap_break_err!(BaseHashTable::hasKey(inCref.clone(), crefToSimVarHT.clone()), '__try0) {
            sv = unwrap_break_err!(BaseHashTable::get(inCref.clone(), crefToSimVarHT.clone()), '__try0);
        } else {
            if unwrap_break_err!(Flags::isSet(Flags::NF_SCALARIZE.clone()), '__try0) {
                sv = unwrap_break_err!(BaseHashTable::get(unwrap_break_err!(ComponentReferenceBasics::crefStripLastSubs(inCref.clone()), '__try0), crefToSimVarHT.clone()), '__try0);
                subs = unwrap_break_err!(ComponentReference::crefLastSubs(inCref.clone()), '__try0);
                sv.name = unwrap_break_err!(ComponentReference::crefSetLastSubs(sv.name.clone(), subs.clone()), '__try0);
            } else {
                sv = unwrap_break_err!(BaseHashTable::get(unwrap_break_err!(ComponentReference::crefStripSubs(inCref.clone()), '__try0), crefToSimVarHT.clone()), '__try0);
                subs = unwrap_break_err!(ComponentReferenceBasics::crefSubs(inCref.clone()), '__try0);
                sv.name = unwrap_break_err!(ComponentReference::crefApplySubs(unwrap_break_err!(ComponentReference::crefStripSubs(sv.name.clone()), '__try0), subs.clone()), '__try0);
            }
            sv.variable_index = (match sv.variable_index.clone() {
        Some(mut index) => {
            Some(index.clone() + SimCodeUtilShared::getScalarElementIndex(subs.clone(), unwrap_break_err!(List::map(sv.numArrayElement.clone(), (std::sync::Arc::new(stringInt) as std::sync::Arc<dyn ::std::ops::Fn(ArcStr) -> Result<i32> + 'static>)), '__try0)) - 1)
        },
        _ => {
            sv.variable_index.clone()
        },
    });
            sv.fmi_index = (match sv.fmi_index.clone() {
        Some(mut fmiIndex) => {
            Some(fmiIndex.clone() + SimCodeUtilShared::getScalarElementIndex(subs.clone(), unwrap_break_err!(List::map(sv.numArrayElement.clone(), (std::sync::Arc::new(stringInt) as std::sync::Arc<dyn ::std::ops::Fn(ArcStr) -> Result<i32> + 'static>)), '__try0)) - 1)
        },
        _ => {
            sv.fmi_index.clone()
        },
    });
        }
        sv = (match sv.aliasvar.clone() {
        SimCodeVar::AliasVariable::NOALIAS { .. } => sv.clone(),
        SimCodeVar::AliasVariable::ALIAS { varName: ref cref } => unwrap_break_err!(simVarFromHT(cref.clone(), crefToSimVarHT.clone()), '__try0),
        SimCodeVar::AliasVariable::NEGATEDALIAS { .. } => sv.clone(),
    });
        Ok::<_, anyhow::Error>((sv.clone(),))
    } {
        Ok((__try0_o0,)) => {
            sv = __try0_o0;
        }
        Err(_) => {
            badcref = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("ERROR_simVarFromHT_failed ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(inCref.clone())?); ArcStr::from(__mm_s) }).clone(), DAE::T_REAL_DEFAULT().clone(), metamodelica::nil());
            sv = SimCodeVar::SimVar { name: badcref.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: -2, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: DAE::T_REAL_DEFAULT().clone(), isDiscrete: false, arrayCref: None, aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL), variable_index: None, fmi_index: None, numArrayElement: metamodelica::nil(), isValueChangeable: false, isProtected: true, hideResult: None, isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(badcref.clone()), relativeQuantity: false };
        }
    }
    outSimVar = sv.clone();
    Ok(outSimVar)
}

pub fn createJacContext(mut name: ArcStr, mut jacHT: Option<(metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))>) -> SimCodeFunction::Context {
    let mut outContext: SimCodeFunction::Context = SimCodeFunction::Context::DAE_MODE_CONTEXT;
    outContext = SimCodeFunction::Context::JACOBIAN_CONTEXT { name: (name.clone()).clone(), jacHT: jacHT.clone() };
    outContext
}

pub fn localCref2SimVar(mut inCref: Arc<DAE::ComponentRef>, mut inCrefToSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<SimCodeVar::SimVar> {
    let mut outSimVar: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    outSimVar = 'mc: {
        let __mc_input = (inCref.clone(), inCrefToSimVarHT.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (cref, crefToSimVarHT) => {
                    let mut sv: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
                    sv = BaseHashTable::get(cref.clone(), crefToSimVarHT.clone())?;
                    Ok(sv.clone())
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (_, _) => {
                    let mut badcref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
                    badcref = ComponentReferenceBasics::makeCrefIdent(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("ERROR_localCref2SimVar_failed ")); __mm_s.push_str(&*ComponentReferenceBasics::printComponentRefStr(inCref.clone())?); ArcStr::from(__mm_s) }).clone(), DAE::T_REAL_DEFAULT().clone(), metamodelica::nil());
                    Ok(SimCodeVar::SimVar { name: badcref.clone(), varKind: openmodelica_backend_types::BackendDAE::VarKind::VARIABLE, comment: (literal!("")).clone(), unit: (literal!("")).clone(), displayUnit: (literal!("")).clone(), index: -2, minValue: None, maxValue: None, initialValue: None, nominalValue: None, isFixed: false, type_: DAE::T_REAL_DEFAULT().clone(), isDiscrete: false, arrayCref: None, aliasvar: openmodelica_simcode_types::SimCodeVar::AliasVariable::NOALIAS, source: DAE::emptyElementSource().clone(), causality: Some(openmodelica_simcode_types::SimCodeVar::Causality::LOCAL), variable_index: None, fmi_index: None, numArrayElement: metamodelica::nil(), isValueChangeable: false, isProtected: true, hideResult: None, isEncrypted: false, inputIndex: None, initNonlinear: false, matrixName: None, variability: None, initial_: None, exportVar: Some(badcref.clone()), relativeQuantity: false })
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    };
    Ok(outSimVar)
}

pub fn localCref2Index(mut inCref: Arc<DAE::ComponentRef>, mut inCrefToSimVarHT: (metamodelica::Array<Arc<metamodelica::List<(Arc<DAE::ComponentRef>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<DAE::ComponentRef>, SimCodeVar::SimVar)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>, Arc<DAE::ComponentRef>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<DAE::ComponentRef>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(SimCodeVar::SimVar) -> Result<ArcStr> + 'static>))) -> Result<ArcStr> {
    let mut outIndex: ArcStr = arcstr::literal!("");
    outIndex = ('mc: {
        let __mc_input = (inCref.clone(), inCrefToSimVarHT.clone());
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                (cref, crefToSimVarHT) => {
                    let mut sv: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
                    sv = BaseHashTable::get(cref.clone(), crefToSimVarHT.clone())?;
                    Ok(ArcStr::from(::std::format!("{}", sv.index.clone())))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            ::match_deref::match_deref! { match &__mc_input {
                _ => {
                    Ok(literal!("-1"))
                }
                _ => bail!("nomatch"),
            }}
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    }).clone();
    Ok(outIndex)
}

pub fn codegenExpSanityCheck(mut e: Arc<DAE::Exp>, mut context: SimCodeFunction::Context) -> Result<Arc<DAE::Exp>> {
    let mut e: Arc<DAE::Exp> = e;
    let mut vars: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
    let mut simCode: SimCode::SimCode = <SimCode::SimCode as ::std::default::Default>::default();
    let mut index: i32 = 0;
    let mut crf_lst: Arc<metamodelica::List<Arc<DAE::ComponentRef>>> = metamodelica::nil();
    if SimCodeFunctionUtil::inFunctionContext(context.clone()) {
        return Ok(e.clone());
    }
    e = (::match_deref::match_deref! { match &(e.clone()) {
        Deref @ DAE::Exp::CREF { ty: Deref @ DAE::Type::T_ARRAY { .. }, .. } => {
            simCode = getSimCode()?;
            crf_lst = ComponentReference::expandCref(var_field!((*e).componentRef, DAE::Exp::CREF).clone(), true)?;
            vars = ({
        let mut __acc: Arc<metamodelica::List<SimCodeVar::SimVar>> = metamodelica::nil();
        for mut cr in (crf_lst.clone()).into_iter().cloned() {
            let __x = cref2simvar(cr.clone(), simCode.clone())?;
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
            if !(vars.clone().is_empty()) {
                let (__pa0, __pa1) = ::match_deref::match_deref! { match &(vars.clone()) {
                    Deref @ metamodelica::List::Cons { head: SimCodeVar::SimVar { index: __pa0, .. }, tail: __pa1 } => (__pa0.clone(), __pa1.clone()),
                    _ => bail!("pattern mismatch"),
                } };
                index = __pa0.clone();
                vars = __pa1.clone();
                for mut v in &*vars.clone() {
                    let mut v = v.clone();
                    if v.index.clone() != index.clone() + 1 {
                        (e, _) = Expression::expandCrefs(e.clone(), false, 0)?;
                        break;
                    }
                    index = v.index.clone();
                }
            }
            e.clone()
        },
        _ => e.clone(),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(e)
}

pub fn absoluteClockIdxForBaseClock(mut baseClockIdx: i32, mut allBaseClockPartitions: Arc<metamodelica::List<SimCode::ClockedPartition>>) -> i32 {
    let mut absBaseClockIdx: i32 = 0;
    let mut i: i32 = 1;
    absBaseClockIdx = 1;
    while i.clone() < baseClockIdx.clone() {
        absBaseClockIdx = absBaseClockIdx.clone() + (getSubPartition((allBaseClockPartitions.clone()).get(i.clone()).unwrap()).len() as i32);
        i = i.clone() + 1;
    }
    absBaseClockIdx
}

pub fn getClockedPartitions(mut simcode: SimCode::SimCode) -> Arc<metamodelica::List<SimCode::ClockedPartition>> {
    let mut clockedPartitions: Arc<metamodelica::List<SimCode::ClockedPartition>> = metamodelica::nil();
    clockedPartitions = simcode.clockedPartitions.clone();
    clockedPartitions
}

pub fn isScalarLiteralAssignment(mut eq: Arc<SimCode::SimEqSystem>) -> Result<bool> {
    let mut b: bool = false;
    b = (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { .. } => Expression::isSimpleLiteralValue(var_field!((*eq).exp, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone(), false)?,
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(b)
}

pub fn selectScalarLiteralAssignments(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = eqs;
    eqs = ({
        let mut __acc: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
        for mut e in (eqs.clone()).into_iter().cloned() {
            if !(isScalarLiteralAssignment(e.clone())?) { continue; }
            let __x = e.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
    Ok(eqs)
}

pub fn filterScalarLiteralAssignments(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = eqs;
    eqs = ({
        let mut __acc: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
        for mut e in (eqs.clone()).into_iter().cloned() {
            if !(!(isScalarLiteralAssignment(e.clone())?)) { continue; }
            let __x = e.clone();
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
    Ok(eqs)
}

pub fn sortSimpleAssignmentBasedOnLhs(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = eqs;
    eqs = List::sort(eqs.clone(), (std::sync::Arc::new({ let __pe_b2 = getSimCode()?; move |__pe_a0, __pe_a1| lhsGreaterThan(__pe_a0, __pe_a1, __pe_b2.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>))?;
    Ok(eqs)
}

fn lhsGreaterThan(mut eq1: Arc<SimCode::SimEqSystem>, mut eq2: Arc<SimCode::SimEqSystem>, mut simCode: SimCode::SimCode) -> Result<bool> {
    let mut b: bool = false;
    b = (::match_deref::match_deref! { match &((eq1.clone(), eq2.clone())) {
        (Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { .. }, Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { .. }) => simvarGraterThan(cref2simvar(var_field!((*eq1).cref, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone(), simCode.clone())?, cref2simvar(var_field!((*eq2).cref, SimCode::SimEqSystem::SES_SIMPLE_ASSIGN).clone(), simCode.clone())?),
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(b)
}

fn simvarGraterThan(mut v1: SimCodeVar::SimVar, mut v2: SimCodeVar::SimVar) -> bool {
    let mut b: bool = false;
    let mut t1: i32 = 0;
    let mut t2: i32 = 0;
    let mut k1: i32 = 0;
    let mut k2: i32 = 0;
    k1 = metamodelica::valueConstructor((&v1.varKind.clone())).unwrap();
    k2 = metamodelica::valueConstructor((&v2.varKind.clone())).unwrap();
    t1 = metamodelica::valueConstructor((&*v1.type_.clone())).unwrap();
    t2 = metamodelica::valueConstructor((&*v2.type_.clone())).unwrap();
    b = if (t1.clone() == t2.clone()) {if (k1.clone() == k2.clone()) {v1.index.clone() > v2.index.clone()} else {k1.clone() > k2.clone()}} else {t1.clone() > t2.clone()};
    b
}

pub fn getNumContinuousEquations(mut eqns: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut numStates: i32) -> i32 {
    let mut n: i32 = 0;
    let mut numEqns: i32 = 0;
    for mut eqn in &*eqns.clone() {
        let mut eqn = eqn.clone();
        numEqns = numEqns.clone() + getNumContinuousEquationsSingleEq(eqn.clone());
    }
    n = numEqns.clone() + numStates.clone();
    n
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
fn getNumContinuousEquationsSingleEq(mut eqn: Arc<SimCode::SimEqSystem>) -> i32 {
    let mut n: i32 = 0;
    n = (::match_deref::match_deref! { match &(eqn.clone()) {
        Deref @ SimCode::SimEqSystem::SES_MIXED { .. } => {
            getNumContinuousEquationsSingleEq(var_field!((*eqn).cont, SimCode::SimEqSystem::SES_MIXED).clone())
        },
        Deref @ SimCode::SimEqSystem::SES_LINEAR { lSystem: ls @ Deref @ SimCode::LinearSystem { index: _, .. }, .. } => {
            (ls.vars.clone().len() as i32)
        },
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { nlSystem: nls @ Deref @ SimCode::NonlinearSystem { index: _, .. }, .. } => {
            (nls.crefs.clone().len() as i32)
        },
        _ => {
            1
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    n
}

fn crefSimCodeIndexGreaterThan(mut cr1: Arc<DAE::ComponentRef>, mut cr2: Arc<DAE::ComponentRef>, mut simCode: SimCode::SimCode) -> Result<bool> {
    let mut b: bool = false;
    let mut v1: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    let mut v2: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
    v1 = cref2simvar(cr1.clone(), simCode.clone())?;
    v2 = cref2simvar(cr2.clone(), simCode.clone())?;
    b = simvarGraterThan(v1.clone(), v2.clone());
    Ok(b)
}

pub fn lookupVR(mut cr: Arc<DAE::ComponentRef>, mut simCode: SimCode::SimCode) -> Result<i32> {
    let mut vr: i32 = 0;
    vr = AvlTreeCRToInt::get(simCode.valueReferences.clone(), cr.clone())?;
    Ok(vr)
}

pub fn lookupVRForRealOutputDerivative(mut cr: Arc<DAE::ComponentRef>, mut simCode: SimCode::SimCode, mut fmuType: ArcStr) -> Result<i32> {
    let mut vr: i32 = 0;
    let mut outputRealDerivativeCref: Arc<DAE::ComponentRef> = Arc::new(DAE::ComponentRef::WILD);
    if fmuType.clone() == literal!("cs") {
        outputRealDerivativeCref = ComponentReference::appendStringLastIdent((literal!("_der")).clone(), cr.clone())?;
        outputRealDerivativeCref = ComponentReference::prependStringCref((literal!("$")).clone(), outputRealDerivativeCref.clone())?;
        vr = AvlTreeCRToInt::get(simCode.valueReferences.clone(), outputRealDerivativeCref.clone())?;
    } else {
        vr = -1;
    }
    Ok(vr)
}

fn getValueReferenceMapping(mut modelInfo: SimCode::ModelInfo) -> Result<Arc<AvlTreeCRToInt::Tree>> {
    let mut tree: Arc<AvlTreeCRToInt::Tree> = Arc::new(AvlTreeCRToInt::Tree::EMPTY);
    let mut i: i32 = 0;
    let mut vars: SimCodeVar::SimVars = <SimCodeVar::SimVars as ::std::default::Default>::default();
    tree = Arc::new(openmodelica_simcode_types::AvlTreeCRToInt::Tree::EMPTY);
    vars = modelInfo.vars.clone();
    (i, tree) = getValueReferenceMapping2(vars.stateVars.clone(), 0, tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.derivativeVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.algVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.discreteAlgVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.paramVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.aliasVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.intAlgVars.clone(), 0, tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.intParamVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.intAliasVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.boolAlgVars.clone(), 0, tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.boolParamVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.boolAliasVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.stringAlgVars.clone(), 0, tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.stringParamVars.clone(), i.clone(), tree.clone())?;
    (i, tree) = getValueReferenceMapping2(vars.stringAliasVars.clone(), i.clone(), tree.clone())?;
    Ok(tree)
}

fn getValueReferenceMapping2(mut vars: Arc<metamodelica::List<SimCodeVar::SimVar>>, mut i: i32, mut tree: Arc<AvlTreeCRToInt::Tree>) -> Result<(i32, Arc<AvlTreeCRToInt::Tree>)> {
    let mut i: i32 = i;
    let mut tree: Arc<AvlTreeCRToInt::Tree> = tree;
    for mut v in &*vars.clone() {
        let mut v = v.clone();
        tree = AvlTreeCRToInt::add(tree.clone(), v.name.clone(), i.clone(), (std::sync::Arc::new(AvlTreeCRToInt::addConflictDefault) as std::sync::Arc<dyn ::std::ops::Fn(_, _, _) -> Result<_> + 'static>))?;
        i = i.clone() + 1;
    }
    Ok((i, tree))
}

fn getResources(mut classes: Arc<metamodelica::List<Arc<Absyn::Class>>>, mut dlow1: Arc<BackendDAE::BackendDAE>, mut dlow2: Arc<BackendDAE::BackendDAE>) -> Result<Arc<metamodelica::List<ArcStr>>> {
    type Func = std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<AvlSetString::Tree>) -> Result<(Arc<DAE::Exp>, Arc<AvlSetString::Tree>)> + 'static>;

    let mut resources: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut tree: Arc<AvlSetString::Tree> = Arc::new(AvlSetString::Tree::EMPTY);
    let mut fns: Arc<metamodelica::List<DAE::Function>> = metamodelica::nil();
    let mut unknownUri: Mutable::Mutable<bool>;
    let mut f1: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<AvlSetString::Tree>) -> Result<(Arc<DAE::Exp>, Arc<AvlSetString::Tree>)> + 'static>;
    let mut f2: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<AvlSetString::Tree>) -> Result<(Arc<DAE::Exp>, Arc<AvlSetString::Tree>)> + 'static>;
    let mut file: ArcStr = arcstr::literal!("");
    fns = DAEUtil::getFunctionList(dlow1.shared.functionTree.clone(), false)?;
    tree = Arc::new(openmodelica_util::AvlSetString::Tree::EMPTY);
    unknownUri = Mutable::create(false);
    f1 = (std::sync::Arc::new({ let __pe_b2 = unknownUri.clone(); move |__pe_a0, __pe_a1| findResources(__pe_a0, __pe_a1, __pe_b2.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, Arc<AvlSetString::Tree>) -> Result<(Arc<DAE::Exp>, Arc<AvlSetString::Tree>)> + 'static>);
    f2 = (std::sync::Arc::new({ let __pe_b2: Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, _) -> Result<_> + 'static> = f1.clone(); move |__pe_a0, __pe_a1| Expression::traverseSubexpressions(__pe_a0, __pe_a1, __pe_b2.clone()) }) as std::sync::Arc<dyn ::std::ops::Fn(Arc<DAE::Exp>, _) -> Result<_> + 'static>);
    (_, tree) = DAEUtil::traverseDAEFunctions(fns.clone(), f2.clone(), tree.clone())?;
    tree = BackendDAEUtil::traverseBackendDAEExpsNoCopyWithUpdate(dlow1.clone(), f2.clone(), tree.clone())?;
    tree = BackendDAEUtil::traverseBackendDAEExpsNoCopyWithUpdate(dlow2.clone(), f2.clone(), tree.clone())?;
    resources = AvlSetString::listKeys(tree.clone(), metamodelica::nil());
    if Mutable::access(unknownUri.clone()) {
        for mut cl in &*classes.clone() {
            let mut cl = cl.clone();
            file = cl.info.fileName.clone();
            if System::basename((file.clone()).clone()) == literal!("package.mo") {
                resources = metamodelica::cons((System::dirname((file.clone()).clone())).clone(), resources.clone());
            }
        }
    }
    Ok(resources)
}

fn findResources(mut e: Arc<DAE::Exp>, mut tree: Arc<AvlSetString::Tree>, mut unknownUri: Mutable::Mutable<bool>) -> Result<(Arc<DAE::Exp>, Arc<AvlSetString::Tree>)> {
    let mut e: Arc<DAE::Exp> = e;
    let mut tree: Arc<AvlSetString::Tree> = tree;
    let mut f: ArcStr = arcstr::literal!("");
    let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    tree = (::match_deref::match_deref! { match &(e.clone()) {
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Exp::SCONST { string: f }, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "OpenModelica_fmuLoadResource" }, .. } => AvlSetString::add(tree.clone(), (f.clone()).clone())?,
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: e1, tail: _ }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "OpenModelica_uriToFilename" }, .. } => {
            Error::addMessage(Error::FMI_URI_RESOLVE.clone(), list![(ExpressionBasics::printExpStr(e1.clone())?).clone()])?;
            Mutable::update(unknownUri.clone(), true);
            tree.clone()
        },
        _ => tree.clone(),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok((e, tree))
}

fn aliasSimEqSystems(mut eqs: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, mut cache: (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))) -> Result<(Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>, (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)))> {
    let mut eqs: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = eqs;
    let mut cache: (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (HashTableSimCodeEqCache::FuncHashCref, HashTableSimCodeEqCache::FuncCrefEqual, HashTableSimCodeEqCache::FuncCrefStr, HashTableSimCodeEqCache::FuncExpStr)) = cache;
    (eqs, cache) = List::mapFold(eqs.clone(), (std::sync::Arc::new(aliasSimEqs) as std::sync::Arc<dyn ::std::ops::Fn(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)))> + 'static>), cache.clone())?;
    Ok((eqs, cache))
}

fn aliasSimEqs(mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut cache: (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))) -> Result<(Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)))> {
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = eqs;
    let mut cache: (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (HashTableSimCodeEqCache::FuncHashCref, HashTableSimCodeEqCache::FuncCrefEqual, HashTableSimCodeEqCache::FuncCrefStr, HashTableSimCodeEqCache::FuncExpStr)) = cache;
    (eqs, cache) = List::mapFold(eqs.clone(), (std::sync::Arc::new(aliasSimEq) as std::sync::Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))) -> Result<(Arc<SimCode::SimEqSystem>, (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)))> + 'static>), cache.clone())?;
    Ok((eqs, cache))
}

fn aliasSimEq(mut eq: Arc<SimCode::SimEqSystem>, mut cache: (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>))) -> Result<(Arc<SimCode::SimEqSystem>, (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<i32> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>, Arc<SimCode::SimEqSystem>) -> Result<bool> + 'static>, Arc<dyn ::std::ops::Fn(Arc<SimCode::SimEqSystem>) -> Result<ArcStr> + 'static>, Arc<dyn ::std::ops::Fn(i32) -> Result<ArcStr> + 'static>)))> {
    let mut eq: Arc<SimCode::SimEqSystem> = eq;
    let mut cache: (metamodelica::Array<Arc<metamodelica::List<(Arc<SimCode::SimEqSystem>, i32)>>>, (i32, i32, metamodelica::Array<Option<(Arc<SimCode::SimEqSystem>, i32)>>), i32, (HashTableSimCodeEqCache::FuncHashCref, HashTableSimCodeEqCache::FuncCrefEqual, HashTableSimCodeEqCache::FuncCrefStr, HashTableSimCodeEqCache::FuncExpStr)) = cache;
    let mut ix: i32 = 0;
    let mut aliasOf: i32 = 0;
    ix = simEqSystemIndex(eq.clone())?;
    if BaseHashTable::hasKey(eq.clone(), cache.clone())? {
        aliasOf = BaseHashTable::get(eq.clone(), cache.clone())?;
        if aliasOf.clone() != ix.clone() {
            eq = Arc::new(SimCode::SimEqSystem::SES_ALIAS { index: ix.clone(), aliasOf: BaseHashTable::get(eq.clone(), cache.clone())? });
        }
        return Ok((eq.clone(), cache.clone()));
    }
    if (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ SimCode::SimEqSystem::SES_SIMPLE_ASSIGN { .. } => true,
        Deref @ SimCode::SimEqSystem::SES_ARRAY_CALL_ASSIGN { .. } => true,
        Deref @ SimCode::SimEqSystem::SES_ALGORITHM { statements: Deref @ metamodelica::List::Cons { head: Deref @ DAE::Statement::STMT_NORETCALL { .. }, tail: Deref @ metamodelica::List::Nil }, .. } => true,
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } }) {
        cache = BaseHashTable::add((eq.clone(), ix.clone()), cache.clone())?;
    }
    Ok((eq, cache))
}

pub fn unbalancedEqSystemPartition(mut inList: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>, mut maxLength: i32) -> Result<Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>>> {
    let mut partitions: Arc<metamodelica::List<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>>> = metamodelica::nil();
    let mut length: i32 = 0;
    let mut eqLength: i32 = 0;
    let mut lst: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut cur: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut first: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    lst = inList.clone();
    cur = metamodelica::nil();
    partitions = metamodelica::nil();
    length = 0;
    while !(lst.clone().is_empty()) {
        let (__pa0, __pa1) = ::match_deref::match_deref! { match &(lst.clone()) {
            Deref @ metamodelica::List::Cons { head: __pa0, tail: __pa1 } => (__pa0.clone(), __pa1.clone()),
            _ => bail!("pattern mismatch"),
        } };
        first = __pa0.clone();
        lst = __pa1.clone();
        eqLength = getNumContinuousEquationsSingleEq(first.clone());
        if length.clone() > 0 && length.clone() + eqLength.clone() > maxLength.clone() {
            partitions = metamodelica::cons(cur.clone(), partitions.clone());
            length = 0;
            cur = metamodelica::nil();
        }
        length = eqLength.clone() + length.clone();
        cur = metamodelica::cons(first.clone(), cur.clone());
    }
    if !(cur.clone().is_empty()) {
        partitions = metamodelica::cons(cur.clone(), partitions.clone());
    }
    Ok(partitions)
}

pub fn selectNLEqSys(mut simEqSysIn: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>) -> Result<Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>>> {
    let mut eqs: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
    let mut e: Arc<SimCode::SimEqSystem> = Arc::new(<SimCode::SimEqSystem as ::std::default::Default>::default());
    eqs = ({
        let mut __acc: Arc<metamodelica::List<Arc<SimCode::SimEqSystem>>> = metamodelica::nil();
        for mut eq in (simEqSysIn.clone()).into_iter().cloned() {
            if !((::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { .. } => true,
        Deref @ SimCode::SimEqSystem::SES_MIXED { cont: Deref @ SimCode::SimEqSystem::SES_NONLINEAR { .. }, .. } => true,
        _ => false,
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })) { continue; }
            let __x = (::match_deref::match_deref! { match &(eq.clone()) {
        Deref @ SimCode::SimEqSystem::SES_NONLINEAR { .. } => eq.clone(),
        Deref @ SimCode::SimEqSystem::SES_MIXED { cont: e @ Deref @ SimCode::SimEqSystem::SES_NONLINEAR { .. }, .. } => e.clone(),
        _ => bail!("match: no arm matched"),
    } });
            __acc = cons(__x, __acc);
        }
        __acc.reverse()
    });
    Ok(eqs)
}

pub fn generateRunnerBatScript(mut code: SimCode::SimCode) -> Result<ArcStr> {
    let mut fileName: ArcStr = arcstr::literal!("");
    let mut file: File::File = File::File(File::noReference())?;
    fileName = ('mc: {
        let __mc_input = code.clone();
        if let Ok((__v, __wb0)) = (|| -> Result<_> {
            let SimCode::SimCode { .. } = __mc_input.clone() else { bail!("nomatch") };
            let mut r#str: ArcStr = arcstr::literal!("");
            let mut locations: ArcStr = arcstr::literal!("");
            let mut locations_lst: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
            let mut fileName: ArcStr = fileName.clone();
            fileName = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*code.fileNamePrefix.clone()); __mm_s.push_str(&*literal!(".bat")); ArcStr::from(__mm_s) }).clone();
            File::open(file.clone(), (fileName.clone()).clone(), File::Mode::Write.clone());
            (locations_lst, _) = getDirectoriesForDLLsFromLinkLibs(code.makefileParams.libs.clone());
            locations = stringDelimitList(locations_lst.clone(), (literal!(";")).clone());
            locations = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*locations.clone()); __mm_s.push_str(&*literal!(";")); __mm_s.push_str(&*Settings::getInstallationDirectoryPath()?); __mm_s.push_str(&*literal!("/bin/")); ArcStr::from(__mm_s) }).clone();
            r#str = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("@echo off\n")); __mm_s.push_str(&*literal!("SET PATH=")); __mm_s.push_str(&*locations.clone()); __mm_s.push_str(&*literal!(";%PATH%;\n")); __mm_s.push_str(&*literal!("SET ERRORLEVEL=\n")); __mm_s.push_str(&*literal!("CALL \"%CD%/")); __mm_s.push_str(&*code.fileNamePrefix.clone()); __mm_s.push_str(&*literal!(".exe\" %*\n")); __mm_s.push_str(&*literal!("SET RESULT=%ERRORLEVEL%\n")); __mm_s.push_str(&*literal!("\n")); __mm_s.push_str(&*literal!("EXIT /b %RESULT%\n")); ArcStr::from(__mm_s) }).clone();
            File::write(file.clone(), (r#str.clone()).clone());
            Ok((fileName.clone(), fileName.clone()))
        })() { fileName = __wb0; break 'mc __v; }
        if let Ok(__v) = (|| -> Result<_> {
            let _ = __mc_input.clone() else { bail!("nomatch") };
            Error::addInternalError((literal!("SimCodeMain.generateRunnerBatScript failed")).clone(), metamodelica::sourceInfo!())?;
            Ok(literal!(""))
        })() { break 'mc __v; }
        bail!("matchcontinue: no arm matched")
    }).clone();
    Ok(fileName)
}

pub fn getDirectoriesForDLLsFromLinkLibs(mut libsAndLinkDirs: Arc<metamodelica::List<ArcStr>>) -> (Arc<metamodelica::List<ArcStr>>, Arc<metamodelica::List<ArcStr>>) {
    let mut outLocations: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut outLibs: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    for mut r#str in &*libsAndLinkDirs.clone() {
        let mut r#str = r#str.clone();
        if StringUtil::startsWith((r#str.clone()).clone(), (literal!("\"-L")).clone()) {
            outLocations = listAppend(list![(System::trim((r#str.clone()).clone(), (literal!("\"-L")).clone())).clone()], outLocations.clone());
        } else if StringUtil::startsWith((r#str.clone()).clone(), (literal!("-l")).clone()) {
            outLibs = listAppend(list![(System::trim((r#str.clone()).clone(), (literal!("-l")).clone())).clone()], outLibs.clone());
        }
    }
    outLocations = outLocations.clone().reverse();
    outLibs = outLibs.clone().reverse();
    (outLocations, outLibs)
}

pub fn getCmakeCrossPlatformSuffixes() -> ArcStr {
    let mut cmakeCode: ArcStr = arcstr::literal!("");
    cmakeCode = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\n# Cross-Platform CMAKE_FIND_LIBRARY_SUFFIXES\n")); __mm_s.push_str(&*literal!("if(WIN32)\n")); __mm_s.push_str(&*literal!("  set(CMAKE_FIND_LIBRARY_SUFFIXES \".lib\" \".dll\" \".a\")\n")); __mm_s.push_str(&*literal!("elseif(APPLE)\n")); __mm_s.push_str(&*literal!("  set(CMAKE_FIND_LIBRARY_SUFFIXES \".dylib\" \".a\")\n")); __mm_s.push_str(&*literal!("else()\n")); __mm_s.push_str(&*literal!("  set(CMAKE_FIND_LIBRARY_SUFFIXES \".so\" \".a\")\n")); __mm_s.push_str(&*literal!("endif()\n")); ArcStr::from(__mm_s) }).clone();
    cmakeCode
}

pub fn getCmakeLinkLibrariesCode(mut libs: Arc<metamodelica::List<ArcStr>>) -> Result<ArcStr> {
    fn addDockerVol(mut istring: ArcStr) -> ArcStr {
        let mut ostring: ArcStr = { let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\"${DOCKER_VOL_DIR}")); __mm_s.push_str(&*istring.clone()); __mm_s.push_str(&*literal!("\"")); ArcStr::from(__mm_s) };
        ostring
    }

    let mut cmakecode: ArcStr = literal!("");
    let mut locations: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut libraries: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    (locations, libraries) = getDirectoriesForDLLsFromLinkLibs(libs.clone());
    locations = listAppend(list![({ let mut __mm_s = String::new(); __mm_s.push_str(&*Settings::getInstallationDirectoryPath()?); __mm_s.push_str(&*literal!("/lib/${CMAKE_LIBRARY_ARCHITECTURE}/omc")); ArcStr::from(__mm_s) }).clone()], locations.clone());
    locations = listAppend(list![({ let mut __mm_s = String::new(); __mm_s.push_str(&*Settings::getInstallationDirectoryPath()?); __mm_s.push_str(&*literal!("/bin")); ArcStr::from(__mm_s) }).clone()], locations.clone());
    locations = List::map(locations.clone(), (std::sync::Arc::new(fnptr!(addDockerVol, ArcStr)) as std::sync::Arc<dyn ::std::ops::Fn(ArcStr) -> Result<ArcStr> + 'static>))?;
    cmakecode = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*cmakecode.clone()); __mm_s.push_str(&*literal!("set(EXTERNAL_LIBDIRECTORIES ")); __mm_s.push_str(&*stringDelimitList(locations.clone(), (literal!("\n                            ")).clone())); __mm_s.push_str(&*literal!(")\n")); ArcStr::from(__mm_s) }).clone();
    cmakecode = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*cmakecode.clone()); __mm_s.push_str(&*getCmakeCrossPlatformSuffixes()); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    for mut lib in &*libraries.clone() {
        let mut lib = lib.clone();
        cmakecode = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*cmakecode.clone()); __mm_s.push_str(&*literal!("find_library(")); __mm_s.push_str(&*lib.clone()); __mm_s.push_str(&*literal!("\n")); __mm_s.push_str(&*literal!("             NAMES ")); __mm_s.push_str(&*lib.clone()); __mm_s.push_str(&*literal!("\n")); __mm_s.push_str(&*literal!("             PATHS ${EXTERNAL_LIBDIRECTORIES} NO_DEFAULT_PATH)\n")); __mm_s.push_str(&*literal!("message(STATUS \"Linking ${")); __mm_s.push_str(&*lib.clone()); __mm_s.push_str(&*literal!("}\")")); __mm_s.push_str(&*literal!("\n")); __mm_s.push_str(&*literal!("target_link_libraries(${FMU_NAME_HASH} PRIVATE ${")); __mm_s.push_str(&*lib.clone()); __mm_s.push_str(&*literal!("})")); __mm_s.push_str(&*literal!("\n")); __mm_s.push_str(&*literal!("list(APPEND RUNTIME_DEPENDS ${")); __mm_s.push_str(&*lib.clone()); __mm_s.push_str(&*literal!("})")); __mm_s.push_str(&*literal!("\n")); ArcStr::from(__mm_s) }).clone();
    }
    Ok(cmakecode)
}

pub fn getCmakeSundialsLinkCode(mut fmiSimulationFlags: Option<SimCode::FmiSimulationFlags>) -> Result<(ArcStr, ArcStr)> {
    let mut needCvode: ArcStr = literal!("OFF");
    let mut cvodeDirectory: ArcStr = literal!("\"\"");
    if cvodeFmiFlagIsSet(fmiSimulationFlags.clone())? {
        needCvode = (literal!("ON")).clone();
        cvodeDirectory = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("\"")); __mm_s.push_str(&*Settings::getInstallationDirectoryPath()?); __mm_s.push_str(&*literal!("/lib/${CMAKE_LIBRARY_ARCHITECTURE}/omc\"")); ArcStr::from(__mm_s) }).clone();
    }
    Ok((needCvode, cvodeDirectory))
}

pub fn cvodeFmiFlagIsSet(mut fmiSimulationFlags: Option<SimCode::FmiSimulationFlags>) -> Result<bool> {
    let mut needsCvode: bool = false;
    let () = (match fmiSimulationFlags.clone() {
        Some(SimCode::FmiSimulationFlags::FMI_SIMULATION_FLAGS { nameValueTuples: mut nameValueTuples }) => {
            let mut setting: ArcStr = arcstr::literal!("");
            let mut value: ArcStr = arcstr::literal!("");
            if !(nameValueTuples.clone().is_empty()) {
                for mut tpl in &*nameValueTuples.clone() {
                    let mut tpl = tpl.clone();
                    (setting, value) = tpl.clone();
                    if stringEqual((setting.clone()).clone(), (literal!("s")).clone()) && stringEqual((value.clone()).clone(), (literal!("cvode")).clone()) {
                        needsCvode = true;
                        break;
                    }
                }
            }
            ()
        },
        Some(SimCode::FmiSimulationFlags::FMI_SIMULATION_FLAGS_FILE { path: mut configFile }) => {
            let mut fileContent: ArcStr = arcstr::literal!("");
            fileContent = (System::readFile((configFile.clone()).clone())?).clone();
            if !(-1 == System::stringFind((fileContent.clone()).clone(), (literal!("\"cvode\"")).clone())?) {
                needsCvode = true;
            }
            ()
        },
        _ => {
            ()
        },
    });
    Ok(needsCvode)
}

pub fn make2CMakeInclude(mut includes: Arc<metamodelica::List<ArcStr>>) -> ArcStr {
    let mut cmakecode: ArcStr = literal!("");
    for mut include in &*includes.clone() {
        let mut include = include.clone();
        cmakecode = ({ let mut __mm_s = String::new(); __mm_s.push_str(&*cmakecode.clone()); __mm_s.push_str(&*literal!("\n                                               ")); __mm_s.push_str(&*literal!("\"")); __mm_s.push_str(&*System::trim((include.clone()).clone(), (literal!("\"-I")).clone())); __mm_s.push_str(&*literal!("\"")); ArcStr::from(__mm_s) }).clone();
    }
    cmakecode
}

pub fn getCMakeVersion(mut pathToCMake: ArcStr) -> Result<SemanticVersion::Version> {
    let mut cmakeVersion: SemanticVersion::Version = <SemanticVersion::Version as ::std::default::Default>::default();
    let mut retVal: i32 = 0;
    let mut cmakeVersionLogFile: ArcStr = literal!("systemCall_cmakeVersion.log");
    let mut regexOut: Arc<metamodelica::List<ArcStr>> = metamodelica::nil();
    let mut numMatches: i32 = 0;
    let mut cmakeVersionString: ArcStr = arcstr::literal!("");
    retVal = System::systemCallRestrictedEnv(({ let mut __mm_s = String::new(); __mm_s.push_str(&*pathToCMake.clone()); __mm_s.push_str(&*literal!(" --version")); ArcStr::from(__mm_s) }).clone(), (cmakeVersionLogFile.clone()).clone())?;
    if 0 != retVal.clone() {
        System::removeFile((cmakeVersionLogFile.clone()).clone());
        Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Failed to get version from ")); __mm_s.push_str(&*pathToCMake.clone()); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
        bail!("fail");
    }
    (numMatches, regexOut) = System::regex((System::trimWhitespace((System::readFile((cmakeVersionLogFile.clone()).clone())?).clone())).clone(), (literal!("[0-9]+\\.[0-9]+\\.[0-9]+")).clone(), 1, true, false);
    if numMatches.clone() == 0 {
        System::removeFile((cmakeVersionLogFile.clone()).clone());
        Error::addInternalError(({ let mut __mm_s = String::new(); __mm_s.push_str(&*literal!("Failed to read semantic version from ")); __mm_s.push_str(&*pathToCMake.clone()); ArcStr::from(__mm_s) }).clone(), metamodelica::sourceInfo!())?;
        bail!("fail");
    }
    cmakeVersionString = (listHead(regexOut.clone())?).clone();
    cmakeVersion = SemanticVersion::parse((cmakeVersionString.clone()).clone(), false)?;
    System::removeFile((cmakeVersionLogFile.clone()).clone());
    Ok(cmakeVersion)
}

// NOTE: #[tailcall::tailcall] disabled: function body contains a `match_deref!{…}` match,
// and the tailcall rewriter cannot see arms hidden behind the macro's `Deref @` patterns.
pub fn getExpNominal(mut expr: Arc<DAE::Exp>) -> Result<Arc<DAE::Exp>> {
    let mut nominal: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
    nominal = (::match_deref::match_deref! { match &(expr.clone()) {
        Deref @ DAE::Exp::ICONST { .. } => {
            Arc::new(DAE::Exp::RCONST { real: intReal(var_field!((*expr).integer, DAE::Exp::ICONST).clone()).abs() })
        },
        Deref @ DAE::Exp::RCONST { .. } => {
            Arc::new(DAE::Exp::RCONST { real: var_field!((*expr).real, DAE::Exp::RCONST).clone().abs() })
        },
        Deref @ DAE::Exp::CREF { ty: t, componentRef: cr } => {
            let mut v: SimCodeVar::SimVar = <SimCodeVar::SimVar as ::std::default::Default>::default();
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            v = cref2simvar(cr.clone(), getSimCode()?)?;
            (::match_deref::match_deref! { match &(v.nominalValue.clone()) {
        Some(Deref @ DAE::Exp::RCONST { real: r1 }) => Arc::new(DAE::Exp::RCONST { real: r1.clone().abs() }),
        Some(e1) => Expression::makePureBuiltinCall((literal!("abs")).clone(), list![e1.clone()], t.clone()),
        None => (match v.varKind.clone() {
        BackendDAE::VarKind::PARAM { .. } => Expression::makePureBuiltinCall((literal!("abs")).clone(), list![expr.clone()], t.clone()),
        _ => Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) }),
    }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::BINARY { operator: DAE::Operator::ADD { .. }, .. } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut r2: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &((getExpNominal(var_field!((*expr).exp1, DAE::Exp::BINARY).clone())?, getExpNominal(var_field!((*expr).exp2, DAE::Exp::BINARY).clone())?)) {
        (Deref @ DAE::Exp::RCONST { real: r1 }, Deref @ DAE::Exp::RCONST { real: r2 }) => Arc::new(DAE::Exp::RCONST { real: r1.clone() + r2.clone() }),
        (e1, e2) => Arc::new(DAE::Exp::BINARY { exp1: e1.clone(), operator: var_field!((*expr).operator, DAE::Exp::BINARY).clone(), exp2: e2.clone() }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::BINARY { operator: DAE::Operator::SUB { ty: t }, .. } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut r2: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &((getExpNominal(var_field!((*expr).exp1, DAE::Exp::BINARY).clone())?, getExpNominal(var_field!((*expr).exp2, DAE::Exp::BINARY).clone())?)) {
        (Deref @ DAE::Exp::RCONST { real: r1 }, Deref @ DAE::Exp::RCONST { real: r2 }) => Arc::new(DAE::Exp::RCONST { real: r1.clone() + r2.clone() }),
        (e1, e2) => Arc::new(DAE::Exp::BINARY { exp1: e1.clone(), operator: DAE::Operator::ADD { ty: t.clone() }, exp2: e2.clone() }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::BINARY { operator: DAE::Operator::MUL { .. }, .. } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut r2: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &((getExpNominal(var_field!((*expr).exp1, DAE::Exp::BINARY).clone())?, getExpNominal(var_field!((*expr).exp2, DAE::Exp::BINARY).clone())?)) {
        (Deref @ DAE::Exp::RCONST { real: r1 }, Deref @ DAE::Exp::RCONST { real: r2 }) => Arc::new(DAE::Exp::RCONST { real: r1.clone() * r2.clone() }),
        (e1, e2) => Arc::new(DAE::Exp::BINARY { exp1: e1.clone(), operator: var_field!((*expr).operator, DAE::Exp::BINARY).clone(), exp2: e2.clone() }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::BINARY { operator: DAE::Operator::DIV { .. }, .. } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut r2: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &((getExpNominal(var_field!((*expr).exp1, DAE::Exp::BINARY).clone())?, getExpNominal(var_field!((*expr).exp2, DAE::Exp::BINARY).clone())?)) {
        (Deref @ DAE::Exp::RCONST { real: r1 }, Deref @ DAE::Exp::RCONST { real: r2 }) => Arc::new(DAE::Exp::RCONST { real: r1.clone() / r2.clone() }),
        (e1, e2) => Arc::new(DAE::Exp::BINARY { exp1: e1.clone(), operator: var_field!((*expr).operator, DAE::Exp::BINARY).clone(), exp2: e2.clone() }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::BINARY { operator: DAE::Operator::POW { .. }, .. } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut r2: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e1: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &((getExpNominal(var_field!((*expr).exp1, DAE::Exp::BINARY).clone())?, getExpNominal(var_field!((*expr).exp2, DAE::Exp::BINARY).clone())?)) {
        (Deref @ DAE::Exp::RCONST { real: r1 }, Deref @ DAE::Exp::RCONST { real: r2 }) => Arc::new(DAE::Exp::RCONST { real: (r1.clone()).powf(r2.clone()) }),
        (e1, e2) => Arc::new(DAE::Exp::BINARY { exp1: e1.clone(), operator: var_field!((*expr).operator, DAE::Exp::BINARY).clone(), exp2: e2.clone() }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::UNARY { operator: DAE::Operator::UMINUS { .. }, .. } => {
            getExpNominal(var_field!((*expr).exp, DAE::Exp::UNARY).clone())?
        },
        Deref @ DAE::Exp::IFEXP { .. } => {
            Arc::new(DAE::Exp::IFEXP { expCond: var_field!((*expr).expCond, DAE::Exp::IFEXP).clone(), expThen: getExpNominal(var_field!((*expr).expThen, DAE::Exp::IFEXP).clone())?, expElse: getExpNominal(var_field!((*expr).expElse, DAE::Exp::IFEXP).clone())? })
        },
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "abs" }, .. } => {
            getExpNominal(e1.clone())?
        },
        Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "sign" }, .. } => {
            Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) })
        },
        Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: t, .. }, expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "sqrt" } } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &(getExpNominal(e1.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r1 } => Arc::new(DAE::Exp::RCONST { real: (r1.clone()).sqrt() }),
        e2 => Expression::makePureBuiltinCall((literal!("sqrt")).clone(), list![e2.clone()], t.clone()),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Cons { head: e2, tail: Deref @ metamodelica::List::Nil } }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "div" }, .. } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut r2: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            (::match_deref::match_deref! { match &((getExpNominal(e1.clone())?, getExpNominal(e2.clone())?)) {
        (Deref @ DAE::Exp::RCONST { real: r1 }, Deref @ DAE::Exp::RCONST { real: r2 }) => Arc::new(DAE::Exp::RCONST { real: std::cmp::max(metamodelica::OrderedFloat(1.0_f64), r1.clone() / r2.clone().abs()) }),
        _ => Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: _, tail: Deref @ metamodelica::List::Cons { head: e2, tail: Deref @ metamodelica::List::Nil } }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "mod" }, .. } => {
            let mut r2: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            (::match_deref::match_deref! { match &(getExpNominal(e2.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r2 } => Arc::new(DAE::Exp::RCONST { real: r2.clone() }),
        _ => Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: _, tail: Deref @ metamodelica::List::Cons { head: e2, tail: Deref @ metamodelica::List::Nil } }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "rem" }, .. } => {
            let mut r2: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            (::match_deref::match_deref! { match &(getExpNominal(e2.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r2 } => Arc::new(DAE::Exp::RCONST { real: r2.clone() }),
        _ => Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) }),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "ceil" }, .. } => {
            getExpNominal(e1.clone())?
        },
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "floor" }, .. } => {
            getExpNominal(e1.clone())?
        },
        Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "sin" }, .. } => {
            Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) })
        },
        Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "cos" }, .. } => {
            Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) })
        },
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "tan" }, .. } => {
            getExpNominal(e1.clone())?
        },
        Deref @ DAE::Exp::CALL { expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "asin" }, .. } => {
            getExpNominal(e1.clone())?
        },
        Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "acos" }, .. } => {
            Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) })
        },
        Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "atan" }, .. } => {
            Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) })
        },
        Deref @ DAE::Exp::CALL { path: Deref @ Absyn::Path::IDENT { name: Deref @ "atan" }, .. } => {
            Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) })
        },
        Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: t, .. }, expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "sinh" } } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &(getExpNominal(e1.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r1 } => Arc::new(DAE::Exp::RCONST { real: (r1.clone()).sinh() }),
        e2 => Expression::makePureBuiltinCall((literal!("sinh")).clone(), list![e2.clone()], t.clone()),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: t, .. }, expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "cosh" } } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &(getExpNominal(e1.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r1 } => Arc::new(DAE::Exp::RCONST { real: (r1.clone()).cosh() }),
        e2 => Expression::makePureBuiltinCall((literal!("cosh")).clone(), list![e2.clone()], t.clone()),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: t, .. }, expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "tanh" } } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &(getExpNominal(e1.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r1 } => Arc::new(DAE::Exp::RCONST { real: (r1.clone()).tanh() }),
        e2 => Expression::makePureBuiltinCall((literal!("tanh")).clone(), list![e2.clone()], t.clone()),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: t, .. }, expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "exp" } } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &(getExpNominal(e1.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r1 } => Arc::new(DAE::Exp::RCONST { real: (r1.clone()).exp() }),
        e2 => Expression::makePureBuiltinCall((literal!("exp")).clone(), list![e2.clone()], t.clone()),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: t, .. }, expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "log" } } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &(getExpNominal(e1.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r1 } => Arc::new(DAE::Exp::RCONST { real: (r1.clone()).ln() }),
        e2 => Expression::makePureBuiltinCall((literal!("log")).clone(), list![e2.clone()], t.clone()),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        Deref @ DAE::Exp::CALL { attr: Deref @ DAE::CallAttributes { ty: t, .. }, expLst: Deref @ metamodelica::List::Cons { head: e1, tail: Deref @ metamodelica::List::Nil }, path: Deref @ Absyn::Path::IDENT { name: Deref @ "log10" } } => {
            let mut r1: metamodelica::Real = metamodelica::OrderedFloat(0.0_f64);
            let mut e2: Arc<DAE::Exp> = Arc::new(<DAE::Exp as ::std::default::Default>::default());
            (::match_deref::match_deref! { match &(getExpNominal(e1.clone())?) {
        Deref @ DAE::Exp::RCONST { real: r1 } => Arc::new(DAE::Exp::RCONST { real: (r1.clone()).log10() }),
        e2 => Expression::makePureBuiltinCall((literal!("log10")).clone(), list![e2.clone()], t.clone()),
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } })
        },
        _ => {
            Arc::new(DAE::Exp::RCONST { real: metamodelica::OrderedFloat(1.0_f64) })
        },
        _ => unreachable!("match_deref! exhaustiveness placeholder"),
    } });
    Ok(nominal)
}

